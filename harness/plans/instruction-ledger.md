# Instruction Ledger

Standing ledger of instruction changes adopted by retros (`skills/retro/SKILL.md`). Rows enter as `ADOPTED` with `Review by` naming the next retro; that retro replaces the status with a verdict: `KEPT` (the expected effect is visible), `KEPT-UNPROVEN` (cheap and uncontradicted, but no supporting evidence yet), `AMENDED`, or `REVERTED`. Two consecutive `KEPT-UNPROVEN` verdicts revert by default, which is how "unsupported after two reviews" stays trackable when each verdict overwrites the last.

| Id | Retro | Change | Owner doc | Evidence pattern | Expected effect | Review by | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| IL-1 | retro-2026-08-03 | Fixture waits carry named ceilings like every run | `docs/orchestration.md` | no hang-class receipt recurs | validation never exceeds its ceiling silently | retro-2026-08-04 | KEPT: item 23's fixtures ship named ceilings and the suite prints them ("agent fixture wait: ... cap: 20s"); no hang recurred in ten receipts |
| IL-1b | retro-2026-08-04 | Every timing budget on a process-backed path scales with measured load, not only the waits a fixture writes: adapter and dispatcher windows count too | `scripts/agents/fixture-budget.sh` | no fixed-second budget goes red under load | a budget that is generous on a laptop stops being a red gate on a loaded runner | next retro | ADOPTED: CI run on 5ca7aeb died on the simulator's hard-coded two-second handshake while the suite was green locally, the second time this class has bitten after CC-1 fixed the fixture-wait half (`b45665b`) |
| IL-2 | retro-2026-08-03 | Verify asserts world-state facts over agent narration | `skills/verify/SKILL.md` | no narration-marker defect recurs | retro-2026-08-04 | KEPT: item 23's census fixtures assert resolution counts and custody joins, not agent phrasing; no narration-marker defect this period |
| IL-3 | retro-2026-08-03 | Audit falls back to grep when rg is absent | `scripts/audit-harness.sh` | audit runs on machines without ripgrep | retro-2026-08-04 | KEPT: implemented in item 23 (`command -v rg` guard); the shim can retire once a full run confirms it |
| IL-4 | retro-2026-08-03 | Agents launch only through the dispatcher; recordless agent processes are UNTRACKED defects | `docs/orchestration.md` | no untracked-launch incident recurs | retro-2026-08-04 | KEPT: item 23 shipped the census, which named nine real recordless agents live, this session's own main included |
| IL-4b | retro-2026-08-03 | Session-start hooks arm supervision on every runtime | `scripts/enforcement/` | an unarmed session cannot occur on a hook-supporting runtime | retro-2026-08-04 | KEPT-UNPROVEN: the three hook configs shipped in item 23, but no live hook-fired arming has been observed on any runtime; the smoke tests are its proof |
| IL-10 | retro-2026-08-05 | The continuation signal is checked for accuracy, not just emitted, and reaches every runtime through the dispatcher rather than through hooks alone | `scripts/agents/open-work.py` | no turn is lost to a plan that misdescribes itself | a stale plan cannot send the check confidently after the wrong work | next retro | ADOPTED: this session lost a turn when the check reported Mission Zero while the real next action was elsewhere, because the plan still claimed work was gate-blocked after the human had directed it. IL-4b is still unproven on every runtime, so hooks cannot be the only delivery path |
| IL-9 | retro-2026-08-05 | A rule stated in more than one place is checked mechanically, not by critique rounds: plans retire a term with a `RETIRED:` marker and the suite fails when any plan still prescribes it | `scripts/assert-plan-consistency.sh` | no critique round's findings are wholly drift between statements of one rule | paid rounds stop being spent on contradictions a script finds instantly | next retro | ADOPTED: eight of nine rounds of the BM-1 spec critique found nothing but this, and the check reproduces the exact drift they took four rounds to surface |
| IL-8 | retro-2026-08-04 | Continuation is checked by the harness, not requested in prose: the end-of-turn hook reports a plan that names an unblocked next step while nothing is in flight | `scripts/agents/open-work.py` | no turn ends silently with unblocked work named in a plan | stopping short becomes a visible fact instead of something only a reader notices | next retro | ADOPTED: this session ended a turn on "next I'll dispatch 20b" with nothing blocking it, which is the failure the deterministic mission runner exists to remove for missions and this hook removes for interactive turns; S4-12 proves it reports, and stays silent while a job is in flight, while a stream waits on the human, and when the next step is settled |
| IL-7 | retro-2026-08-04 | Stage explicit paths, never a directory, whenever another agent may hold work in the same tree | `docs/orchestration.md` | no peer file is committed by a stream that does not own it | a peer's in-flight file cannot be swept into an unrelated commit | next retro | ADOPTED: `git add plans/` committed a peer Codex session's `plans/llm-wiki-pattern.md` at `0b9ca1b`, minutes after this session recorded that it would leave that file alone under the peer rules |
| IL-5 | retro-2026-08-04 | Retained or extracted critique findings are count-verified against the round's verdict before the round is called closed | `skills/design-critique/SKILL.md` | no finding-loss incident recurs | a retention that drops findings fails instead of reading as complete | next retro | ADOPTED |
| IL-6 | retro-2026-08-04 | A design or plan edit that ships no code and no fixture states which shipped artifact it will change and when | `docs/project-adaptation.md` | design-to-implement receipt ratio returns toward parity | plan edits stop outrunning shipped code unnoticed | next retro | ADOPTED |

