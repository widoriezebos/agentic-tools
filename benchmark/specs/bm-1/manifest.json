{
  "id": "bm-1",
  "version": "0.2",
  "title": "taskrun: a dependency-aware task runner (Java, Maven, command line)",
  "comparisonEligible": false,
  "comparisonEligibleNote": "Version 0.x is a trial spec. benchmark-compare refuses any cohort below 1.0. Promotion to 1.0 happens once trial runs have set the fences from observed use, per the promotion rule in plans/benchmark-spec-bm1-design.md section S-6.",
  "product": {
    "sourcePaths": [
      "src/main/**",
      "src/test/**",
      "pom.xml"
    ],
    "entryPoint": "java -jar target/taskrun.jar",
    "requirementCount": 26
  },
  "seededGap": {
    "requirement": 10,
    "decisionFile": "DECISIONS.md",
    "entryGrammar": "requirement <n>: <key>: <value>",
    "declaredRuleKey": "order-rule",
    "allowedValues": [
      "alphabetical",
      "config-order",
      "dependency-depth"
    ],
    "note": "Published in spec.md in full: the entry grammar, the key, and the three permitted values with their exact semantics. A builder cannot satisfy a machine-checked declaration whose format is withheld (BA-1-1), and a rule without executable semantics leaves the grader holding a hidden right answer (BA-2-1)."
  },
  "metrics": {
    "acceptance": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null,
      "formula": "passed checks / total checks across the battery the grader authors in grader/checks.md: at least one check per requirement, each a separately counted case, clauses decomposed where they naturally separate; the battery freezes with the kit at 1.0"
    },
    "requirement_coverage": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null,
      "formula": "requirements whose metric=requirement_<n> line is 1, / 26"
    },
    "cache_correctness": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null,
      "formula": "passing checks / 7: unchanged-skips, changed-command-reruns, changed-input-reruns, force-reruns-all, missing-output-reruns, failed-rerun-keeps-last-success, deleted-cache-cold"
    },
    "failure_propagation": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null,
      "formula": "1 only if all three hold: dependents blocked, unrelated branch ran, exit 1"
    },
    "determinism": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": 1,
      "formula": "1 only if order and per-task states identical across 3 cold-cache repetitions"
    },
    "config_errors": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null,
      "formula": "passing cases / 6: missing dep, cycle, duplicate output, invalid name, escaping path, path inside cache dir; each must name what its requirement demands and exit 2"
    },
    "format_agreement": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null,
      "formula": "1 only if text and JSON agree on order, states and counts for one run and one dry-run, each pair from identical cold caches"
    },
    "build_clean": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": 1,
      "formula": "1 only if ./mvnw -o -q package succeeds from a fresh clone"
    },
    "own_tests_pass": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null,
      "formula": "1 only if the README-stated test command exits 0 AND requirements-map.json covers 1..26 with identifiers that each run alone",
      "note": "Shape, not quality: a ceremonial suite passes this. Test QUALITY is unmeasured in v0.1 and that is recorded rather than papered over; it returns with the mutation design (BA-3-4)."
    },
    "plan_seconds": {
      "domain": [
        0,
        null
      ],
      "direction": "min",
      "bound": 20,
      "note": "Requirement 25: 1000 tasks in a single chain, cache primed once, then the median of three timed runs from process start to exit. Pinned because 'an ordinary developer machine' is not a measurement.",
      "formula": "median of 3 timed runs after one priming run, per requirement 25"
    },
    "dependency_count": {
      "domain": [
        0,
        null
      ],
      "direction": "min",
      "bound": 0,
      "note": "Compile-scope and runtime-scope dependencies in pom.xml. Test-scope JUnit 5 is expected and not counted.",
      "formula": "count of compile- and runtime-scope dependencies in pom.xml"
    },
    "gap_handled": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null,
      "formula": "1 only if DECISIONS.md declares order-rule in the published grammar AND reported order matches that rule on a grader-authored configuration where the three rules disagree"
    }
  },
  "metricsNote": "A null bound is deliberate: calibration proves each metric fires, it does not set thresholds. Bounds are set once, at promotion to spec 1.0, from the trial runs (feasibility) and the probe scores (a floor above what a broken implementation reaches), per the promotion rule. Version 0.x produces scores, not verdicts, so nothing needs a bound before then (BA-3-6). A metric whose procedure cannot run on the target is omitted from the output, never invented: omission means unmeasurable, and the compare layer already treats a missing metric as no-verdict.",
  "noiseFloors": null,
  "noiseFloorsNote": "Unknown until repeated runs measure the spread. Until then this spec produces scores, not verdicts.",
  "fences": {
    "cycles": 8,
    "jobs": 12,
    "concurrency": 2,
    "jobCapMin": 15,
    "wallClockHours": 3,
    "hostTurnCapMin": 20,
    "ledgerCycleBudget": 8,
    "ledgerNoGainBudget": 8,
    "approved": "2026-08-05, under the human's blanket approval; still an experiment per the promotion rule",
    "hostMaxTurns": 150,
    "hostMaxBudgetUsd": "5.00"
  },
  "fencesNote": "Provisional and unapproved. These are an experiment, not a belief: trial runs record what they consume and each fence becomes ceil(observed_max * 1.5), excluding trials that parked at that fence. A human must approve them before any run spends money.",
  "roster": {
    "host": {
      "runtime": "claude",
      "model": "claude-opus-5"
    },
    "delegates": {
      "codex": "gpt-5.6-luna",
      "claude": "claude-opus-5"
    },
    "ruling": "Benchmark missions run their host on Opus, never Fable (human, 2026-08-05). Delegates run gpt-5.6-luna, not gpt-5.6-sol: the human capped cost explicitly on the same day, before any benchmark had ever run, and an unmeasured per-run cost is not a thing to discover with the expensive model. | 2026-08-18: Wido ruled the code critic runs on claude/claude-opus-5 as bm-1's STANDING roster \u2014 a fresh ruling this date (his mission-scoped vm-smoke-5 option-A answer inspired it but authorized one mission only, per the issue-8/7 critique); implementer and design critic stay on gpt-5.6-luna under the 2026-08-05 cost ruling.",
    "delegateRoles": {
      "design-critic": {
        "runtime": "codex",
        "model": "gpt-5.6-luna"
      },
      "implementer": {
        "runtime": "codex",
        "model": "gpt-5.6-luna"
      },
      "code-critic": {
        "runtime": "claude",
        "model": "claude-opus-5"
      }
    }
  },
  "rosterNote": "Pinned by exact model identifier when a run is prepared, never by a phrase like 'cheap models', because comparability requires two cohorts to share an identical roster.",
  "seed": {
    "path": "seed/",
    "description": "A git repository containing README.md, spec.md, and an empty tests/ directory. Empty because BM-1 builds from scratch."
  },
  "grader": {
    "path": "grader/",
    "heldOut": true,
    "heldOutNote": "Never copied into the repository the agents work in. Provisioning copies spec.md and seed/ only.",
    "mutation": {
      "status": "deferred, not designed",
      "note": "Not built in grader v0.1, and the deferral is a disposition of BA-2-2, not a postponement of the old text: that finding refuted the claim that mutating the builder's own code is architecture-neutral. With no reference corpus, mutants come from arbitrary builder source, and attributing a surviving mutant to the requirement whose tests should have caught it is not derivable from requirements-map.json alone. A later version needs a design of its own, critiqued before it is built. Until then mutation_catch stays in the metric table with a null bound and is never emitted."
    },
    "calibration": {
      "probes": "small purpose-built wrong implementations, each DECLARING the metric it targets",
      "rule": "A probe must fail its target metric on every repetition. Other metrics may legitimately drop too, because the metrics overlap by construction (a wrong order hits determinism, format agreement and requirement 10's acceptance at once); demanding one-metric isolation is the same impossibility the mutant corpus already hit (SP-2-1, BA-3-2). Each probe also names the metrics it must NOT disturb, and calibration checks both directions.",
      "repetitions": 3,
      "probeVectors": {
        "cache-blind-command": {
          "target": "cache_correctness",
          "mustNotDisturb": [
            "determinism",
            "failure_propagation",
            "format_agreement"
          ]
        },
        "alternating-order": {
          "target": "determinism",
          "mustNotDisturb": [
            "cache_correctness",
            "failure_propagation"
          ]
        },
        "runs-blocked-dependents": {
          "target": "failure_propagation",
          "mustNotDisturb": [
            "cache_correctness",
            "determinism",
            "format_agreement"
          ]
        },
        "formats-disagree": {
          "target": "format_agreement",
          "mustNotDisturb": [
            "cache_correctness",
            "determinism",
            "failure_propagation"
          ]
        },
        "incomplete": {
          "target": null,
          "condition": "acceptance strictly between 0 and 1"
        },
        "note": "A probe is a near-correct minimal implementation with exactly its one flaw: correct enough on its siblings' target behaviours that those metrics stay up. config_errors and the Maven-dependent metrics are outside every vector, because demanding full configuration validation of a twenty-line probe would make the probes bigger than the point. The alternating-order probe legitimately disturbs format_agreement and gap_handled, since its order differs across the separate invocations those checks compare."
      }
    },
    "outsideOnlyException": "Requirement 24 is graded by reading pom.xml. No finite set of runs can prove an unexercised branch imports nothing, so the outside-only rule states its one exception rather than pretending (BA-2-11).",
    "v01Scope": {
      "builds": [
        "grade.sh emitting metric=<name>=<value> lines in the mission gate grammar, exit 0 when measurement ran",
        "the held-out acceptance suite, one or more checks per requirement, driving the built jar from outside; per-requirement lines metric=requirement_<n>=1|0",
        "behavioural batteries: cache_correctness, failure_propagation, determinism (each repetition from a deleted cache), config_errors, format_agreement (cold cache both sides)",
        "build_clean, own_tests_pass, plan_seconds per requirement 25, dependency_count from pom.xml (the stated outside-only exception), gap_handled (declared rule checked against behaviour on a disagreeing configuration)",
        "seed watches, emitted as unscored observation lines (wrapper intact, release level, files replaced), never as a metric: a scored version for a from-scratch case encodes hidden preferences (BA-3-3)",
        "calibration probes, each declaring its target metric and the metrics it must not disturb; the target fails on every repetition, overlap elsewhere is expected (BA-3-2); scores recorded in grader/calibration.md"
      ],
      "doesNotBuild": [
        "mutation_catch",
        "any reference implementation",
        "noise floors, which come from repeated runs"
      ]
    }
  },
  "language": {
    "runtime": "Java 21",
    "buildTool": "Maven, via the wrapper shipped in the repository",
    "entryPoint": "java -jar target/taskrun.jar",
    "buildCommand": "./mvnw -o -q package",
    "testCommand": "./mvnw -o test",
    "testIdentifierForm": "<fully.qualified.ClassName>#<methodName>",
    "note": "Spring Boot was considered and dropped: the deliverable is a command-line tool and a web framework would have added ceremony without adding anything the benchmark measures."
  },
  "environment": {
    "delegateNetwork": "allowed",
    "delegateNetworkReason": "Per-spec, not a benchmark-wide rule, and allowed here. This spec is idiosyncratic enough that no published task runner satisfies it: the stdout grammar, the four states, the cache directory, the JSON shape and the decision-record rule are all ours, so a downloaded implementation still has to be rewritten to match, which is most of the work. The constraint worth protecting, no third-party dependencies, is graded from pom.xml rather than enforced by cutting the network, which is the right place for it: a requirement should be scored, not made impossible to violate, or we never learn that an agent would have violated it.",
    "denyMechanism": "A spec that does need isolation sets dispatch.permissions.network=deny in the provisioned target's metasystem.conf, which narrows every preset for every role. Reasons that would justify it: a published solution a search would hand over wholesale, or a metric that cannot tolerate run-to-run variance from fetched content.",
    "maven": "Builds still run with -o against a pre-populated repository, because the build must be deterministic and must work whether or not the network is up. That is a spec requirement, not a sandbox constraint."
  },
  "deferredMetrics": {
    "mutation_catch": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null,
      "note": "Deferred with its refutation recorded under grader.mutation. Not emitted in v0.1; a consumer of the metric vector must not expect it."
    }
  },
  "watches": {
    "seed_wrapper_intact": "the Maven wrapper files are present and mvnw runs",
    "seed_release_level": "the release level pom.xml declares",
    "seed_files_replaced": "count of seed-shipped files the builder replaced or deleted",
    "note": "Observations, never gates and never targets. A scored seed_respected metric for a build-from-scratch case can only encode hidden preferences about legitimate build edits (BA-3-3); grading what a builder does with inherited code is BM-2's business, where the seed is a working system and inheritance is the point. The human's intent to grade seed handling is honoured there, and observed here.",
    "grammar": "watch=<name>=<value> lines on standard output beside the metric lines: seed_wrapper_intact 0 or 1, seed_release_level the integer pom.xml declares, seed_files_replaced a count"
  },
  "completionGate": {
    "decision": "self-assessment, decided 2026-08-05 under the human's blanket approval",
    "command": "the builder's stated test command passes AND requirements-map.json covers 1..26 AND ./mvnw -o -q package succeeds from a clean state",
    "rationale": "The grader is held out, so the mission cannot iterate against it; the run stops when the builders believe they are done, and the held-out grading afterwards measures how right that belief was, which is among the most interesting numbers the benchmark produces."
  },
  "missionContract": {
    "note": "Everything provisioning needs to write a preflightable contract. Decided 2026-08-05 under the human's blanket approval; these are benchmark policy, not something a provisioning script may invent.",
    "instruments": [
      "gate.sh",
      "guard-deps.sh"
    ],
    "instrumentsNote": "Copied to the target root from the spec directory and tagged; the builder must never edit them, which gate.paths enforces.",
    "gate": {
      "command": "bash gate.sh",
      "metric": "self-assessment",
      "direction": "max",
      "threshold": ">=1",
      "noiseFloor": 0,
      "paths": "gate.sh,guard-deps.sh",
      "refKind": "tag",
      "refKindNote": "A tag, never a branch: sealing against a branch cannot survive its own signing commit, which the first real preflight found."
    },
    "guard": {
      "name": "no-runtime-deps",
      "command": "bash guard-deps.sh",
      "metric": "no-runtime-deps",
      "floor": 1,
      "noise": 0,
      "cadence": 1,
      "rationale": "Requirement 24's floor, checked every cycle and deliberately orthogonal to the completion gate: a build can stay green and the tests can stay passing while a dependency quietly does the work. A guard stops the run; a metric would only score it afterwards.",
      "note": "Guard name and metric are deliberately identical: the mission contract grammar has no separate guard-to-metric mapping, so a guard named X must emit metric=X. The first draft named the guard `deps` while its script emitted `no-runtime-deps`, which the runner would have refused."
    },
    "truth": {
      "paths": "gate.sh,guard-deps.sh",
      "certification": "certified"
    },
    "streams": {
      "build": "Build the task runner described in spec.md so that gate.sh reports self-assessment=1."
    },
    "envelope": {
      "dependencies": "jdk21,maven"
    },
    "exposure": "EUR:40",
    "exposureNote": "EUR:40 is the TOTAL run ceiling Wido re-confirmed on 2026-08-18 for the MIXED roster: Opus host, gpt-5.6-luna design/implementation delegates, and the claude-opus-5 code critic all spend under this one figure; the sealed host.max-budget-usd=5.00 bounds host spend per turn only, not the total. Revised down from EUR:75 when the human first capped cost; the first trial's recorded usage recalibrates it."
  }
}
