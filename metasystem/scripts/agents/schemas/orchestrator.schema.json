{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "metasystem.version=1",
  "title": "Orchestrator return",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "turnId",
    "missionId",
    "cycle",
    "dispatched",
    "certified",
    "streamUpdatesRequested",
    "askCandidates",
    "factsForLedger",
    "gaps",
    "identity"
  ],
  "properties": {
    "turnId": {
      "type": "string"
    },
    "missionId": {
      "type": "string"
    },
    "cycle": {
      "type": "integer"
    },
    "dispatched": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "jobId",
          "role",
          "stream"
        ],
        "properties": {
          "jobId": {
            "type": "string"
          },
          "role": {
            "type": "string"
          },
          "stream": {
            "type": "string"
          }
        }
      }
    },
    "certified": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "jobId",
          "verdict",
          "evidence",
          "authorizationDigest"
        ],
        "properties": {
          "jobId": {
            "type": "string"
          },
          "verdict": {
            "enum": [
              "accepted",
              "rejected"
            ]
          },
          "evidence": {
            "type": "string"
          },
          "authorizationDigest": {
            "type": [
              "string",
              "null"
            ],
            "description": "The integration authorization this certification consumes (host-implementer wall). Required key; null is lawful ONLY on a rejected verdict. The 64-hex grammar and record existence are enforced by the runner's adjudication, never trusted from this claim."
          }
        }
      }
    },
    "streamUpdatesRequested": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "streamId",
          "requestedState",
          "reason"
        ],
        "properties": {
          "streamId": {
            "type": "string"
          },
          "requestedState": {
            "enum": [
              "active",
              "parked-reserved",
              "done"
            ]
          },
          "reason": {
            "type": "string"
          }
        }
      }
    },
    "askCandidates": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "streamId",
          "reasonClass",
          "question",
          "supersedes"
        ],
        "properties": {
          "streamId": {
            "type": "string"
          },
          "reasonClass": {
            "enum": [
              "reserved-decision",
              "red-test",
              "merge-conflict",
              "host-failure"
            ]
          },
          "question": {
            "type": "string"
          },
          "supersedes": {
            "type": [
              "string",
              "null"
            ],
            "description": "The earlier ask this candidate replaces and sharpens, or null when it replaces none (issue #11). Always present: the return schema is closed and fully enumerated, so an absent field is a protocol error and null is the way to say none. Must be a bare ask id matching ^[a-z0-9][a-z0-9-]*$ \u2014 the runner's adjudication enforces the grammar BEFORE any path join (the in-house schema engine carries no pattern keyword), then validates the ask is OPEN and on the SAME stream, closes it (supersededBy), and hides it from Open Asks; answering the successor answers the chain."
          }
        }
      }
    },
    "factsForLedger": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "gaps": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "identity": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "runtime",
        "model",
        "sessionId"
      ],
      "properties": {
        "runtime": {
          "type": "string"
        },
        "model": {
          "type": "string"
        },
        "sessionId": {
          "type": [
            "string",
            "null"
          ],
          "description": "Echo the prompt's Host-Session header exactly (null when it says none), or report the session id your own runtime shows you \u2014 both are accepted. Never invent a session id."
        }
      }
    }
  }
}
