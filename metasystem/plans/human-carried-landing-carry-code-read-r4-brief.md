Working Mode: implement
Orchestrator Identity: m1d (lineage main-1788941004-20871-6e7a43, coordinator under goal human-carried-landing-carry, fourth and closing read of the carried landing, chain hcl-build3-20260911, final work round 3)
Date: 2026-09-11

# Review brief: the closing read of the carried-landing implementation

FINDING IDS: chain-unique, continue the carry's register: HCL-C-74, HCL-C-75, ... never F-n.

## What you are reading

Chain hcl-build3-20260911 for goal human-carried-landing-carry
(metasystem/plans/goals/human-carried-landing-carry.md), final work round
3 (job hcl-build3-20260911-r3), reviewed tree 17a09a326a80fba66af49ef4bebb52ec0b075658 (the
installation subtree the conformance review names as reviewedTree). The
specification is metasystem/plans/human-carried-landing-carry-design.md
revision 6 with the coordinator's three amendments (the stop list's
empty-staging rule excludes the rerun states; single-machine mode out of
scope with `carry-remote-required`; `goal carry` speaks after its
transaction). The third read (`metasystem/artifacts/agents/hcl-context/code-read-r3.md`,
Opus, hcl-cc3-20260911) found four material findings; the fold
(`metasystem/artifacts/agents/hcl-context/build3-fold-r3-brief.md`)
decided each: HCL-C-66 the test name in the contract list; HCL-C-67 the
crash-local leg with the page's oracle (one code commit found by its
Carry trailer, one carrying and one carried row, the rebased tree
compared, never the sha); HCL-C-68 the group-word-beside-a-code-refusal
row, mutant-proven; HCL-C-69 the append-only admission extended to every
register in registers.go with lines bound to rows on the tree, and the
carried-second leg keeping the accepted-risk register. Round 3 built
those four and nothing else. The coordinator ran every bed on the host.

Write your register as this new file: `metasystem/records/misc/human-carried-landing-carry-code-read-r4.md`
(a read-only runtime returns it in the job return; the coordinator
projects it).

This is the closing read of a DESTRUCTIVE-REACH chain. Read the diff of
round 3 against round 2 (the four items) and the four decisions side by
side. A material finding is: a fold decision not carried out as decided;
a defect the four changes introduce; a way a carry lands what the human
did not name or refuses a verified human on the machine's own judgement,
in the changed code; a fixture among the four that does not test what it
says. Page fixtures the earlier folds recorded as follow-ups are not
findings. Do not re-read what the third read settled.

## Mandate

1. The four decisions, one line each: folded as decided at file:line, or
   not, with what differs.
2. HCL-C-69's admission: every path it admits is bound to a row on the
   tree; a foreign or unbound line, an edit, a deletion, and a goal-less
   new file are refused. Trace a `human-carried` accepted-risk line and an
   accepted-risk line of another chain.
3. HCL-C-67's leg: the oracle cannot pass on a tree that resets and
   restamps instead of keeping the rebased commit.
4. What must not change: the identity gate, never carrying a record
   failure, the temporary word not a proof, the shared testing contract
   and the workspace receipt not weaker, the ordinary digest rule, trunk's
   changes intact.

## Return

Findings by id with file and line, the claim, the evidence, and what
changes if it stands; the four lines; then a verdict line: closable as
is, or one fold naming which findings. Wall-clock budget: 30 minutes.
