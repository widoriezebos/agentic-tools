# Codex handshake — design, revision 5 (2026-09-02, Sol round 3: the Devin bypass)

Goal `codex-handshake-budget-load-fragile` (plans/goals/codex-handshake-budget-load-fragile.md, revision 6).
Brief: plans/codex-handshake-design-brief.md. Author: dispatch delegate m0b+main-1788250419-3170380-8a1fb3, jobs ch-design-r1b (revision 1), ch-design-r2 (revision 2), ch-design-r3 (revision 3), ch-design-r4 (revision 4) and ch-design-r5 (this revision).
Revision 2 closed the six findings of Sol's round-1 register (records/misc/codex-handshake-critique-r1.md: CHS-R1-SNAPSHOT-01, -EXIT-02, -DEADLINE-03, -PROGRESS-04, -FIXTURE-05, -FILES-06) and the orchestrator's evidence note. Revision 3 closes the one finding of Sol's round-2 register (records/misc/codex-handshake-critique-r2.md: CHS-R2-CRITIC-FOLD-01, both halves per the orchestrator's note: the critic class in D2.5 and the sessionless follow-up in D2.7) and touches nothing else but the consistency of §3, §4 and §5 with that fold; every closure is marked `[closes <id>]` where it lands. Part 1 (§2) drew no finding in either round and is unchanged. Revision 3 left one point OPEN in §7, the claim of a sessionless follow-up; the orchestrator decided it (m0b, 2026-09-02 17:05Z) and revision 4 folds that decision into D2.7, then passes over §4 and the `exit-before-session` round-2 leg of §5 for consistency. Revision 5 closes the one finding of Sol's round-3 register (records/misc/codex-handshake-critique-r3.md: CHS-R3-EXIT-01, the Devin adapter's direct `fail_pending` calls that bypass the critic fold): the fold is single-sourced into `fail_pending` through one engine verb (D2.5, the sub-bullet marked with the id), and §4, §5 and §6 are passed over for consistency with it; everything else held in round 3 and does not move. §7 records one open point revision 5 could not decide (the home of the Devin critic-shaped fixture row: no fixture drives the Devin adapter's branch today).
Every path below is relative to `metasystem/`; line numbers are from commit 30d07afb unless a line says otherwise; the references revision 5 adds (devin.sh, runtime-common.sh, adjudicate.go, adapter_verbs.go, main.go, adapter-deadline-fixtures.sh, devincollect_test.go) are from 36bcda71, at which the brief's cited lines were re-read and match.

## 1. Problem and evidence

- The goal record's measurement: on m1, codex-cli 0.148.0 takes 16 to 18 seconds from launch to its `thread.started` event with the operator's ten interactive plugins loaded, and 1 second with `-c 'plugins={}'`; MCP servers change nothing. Jobs breach-design-crit2 and breach-design-crit2b failed `handshake_timeout` while the child was alive. R-35-m3 (memory/rulings.md:62) names any mechanism that turns slowness into failure a defect: patience is progress-based, the number is a hang bound.
- Second machine, second result. Goal codex-handshake-budget (m1b, now marked duplicate) recorded 8 to 14 second starts on m1b where "plugins disabled changed nothing", against m1's 1-second `plugins={}` measurement; Part 1 is therefore expected to fix m1 and is unproven on m1b, and Part 2 is the fix that covers both machines whether or not Part 1 helps on m1b. (orchestrator's evidence note)
- The chain today. The adapter probe writes `sessionEstablishedTimeoutSec` into the snapshot (scripts/agents/adapters/codex.sh:64-71); `capability.Select` validates it as 1..60 and copies it out (internal/capability/select.go:101-104, 164); `job build-record` puts it on the job record (internal/dispatch/build.go:416, 627). At launch the dispatcher stamps `handshakeDeadline = now + budget` through the ownership patch (scripts/agents/dispatch.sh:825-835; internal/dispatch/ownership.go:78-80). `await_handshake` polls the record until that deadline and then calls `__handshake-timeout` (dispatch.sh:870-895), whose `internal_handshake_timeout` writes `{"error":"handshake_timeout","phase":"handshake"}` after its stand-down checks (dispatch.sh:2351-2417). `ComputeReapFacts` derives `HandshakeWaiting = now < handshakeDeadline + 2` (internal/dispatch/reapfacts.go:87-102) and `reap_one_locked` defers to a live supervisor while it is true (dispatch.sh:1026-1039).
- Two premise corrections against the brief. (a) The literal is no longer 10: commit ec17b373 raised codex.sh:71 to 30 on Wido's word (R-47-m1b, rulings.md:93); claude.sh:73 says 20, devin.sh:81 says 30. The design below does not depend on the number. (b) `metasystem proc alive --identity-file` does not exist: `runCensusAlive` accepts only `--pid --start-time --start-ticks --boot-id --root` (cmd/metasystem/census.go:154-171); the built engine answers `flag provided but not defined: -identity-file` (exit 2). dispatch.sh:311 and :854-859 call it anyway and always get a refusal. This design uses `proc classify` (the `tag_state` helper, dispatch.sh:280-283) for the supervisor, which is what `job_supervisor_matches` already trusts (dispatch.sh:298-308). The dead flag is recorded in §7, not fixed here.
- Codex is silent before its session signal. The specimen set is every design-critic job on m0b whose `artifacts/agents/design-critic-*/rounds/1/events.jsonl` begins with a `thread.started` line (the set was not enumerated by id in revision 1; the implementer lists it in the evidence when re-checking); in every one, `{"type":"thread.started",...}` is line one and the matching job log holds only the supervisor's own start line before it. Nothing reaches stdout or stderr before `thread.started`; the goal record's failed specimens had empty event files.
- The custodian's own deadline enforcement never runs before the session: `check_record_deadlines` (scripts/agents/adapters/runtime-common.sh:285-295) is called only from `wait_for_cli` (:298-310), which the adapter reaches after its pre-session loop (codex.sh:154-166) has already seen the session or the child has exited. A hung child whose dispatcher has died is therefore bounded by nothing until `capMin`, and pending→timeout is not a lawful transition (internal/dispatch/record.go:40).

## 2. Part 1 — delegates run without the operator's plugins

- D1.1 The override is the argv pair `-c`, `plugins={}` (the value is TOML, `codex exec --help`: the `-c` value "is parsed as TOML"; `{}` is an empty inline table, so the `plugins` table is empty for that process). It is one package constant in internal/adapter/codex.go, `codexPluginsOverride = "plugins={}"`, used by both verbs.
- D1.2 Position: immediately after the `-c approval_policy="never"` pair on both verbs, before `sandbox_workspace_write.network_access`. Dispatch argv: `codex exec --json -m M --sandbox S -C W -c approval_policy="never" -c plugins={} -c sandbox_workspace_write.network_access=N -c metasystem_instance_tag="T" [-c model_reasoning_effort=…] [--add-dir …] --output-schema … -o … -`. Resume argv: `codex exec resume --json -c model="M" -c sandbox_mode="S" -c approval_policy="never" -c plugins={} -c sandbox_workspace_write.network_access=N -c metasystem_instance_tag="T" [-c model_reasoning_effort=…] [-c sandbox_workspace_write.writable_roots=…] --output-schema … -o … SESSION -`. The two fixed policy settings (approvals, plugins) come first; the envelope-derived settings follow. Positions are pinned by test, so the choice is a contract, not a preference.
- D1.3 Repeated on resume: yes. Every `codex exec` process loads its configuration from the files afresh; `-c` is a per-process override, not thread state, and the rollout a resumed thread reads back carries the conversation, not the previous process's overrides. codex.go:91-98 already repeats `approval_policy`, network and the tag on resume for exactly this reason. Omitting it on resume would load the plugins on every follow-up and reproduce the 16 to 18 second start there.
- D1.4 Rationale for the record. A delegate's whole surface is the brief, the role file, the return schema and the sandbox envelope; the operator's plugins are interactive conveniences that no role requirement names, no snapshot probes, and no job hashes. Passing `plugins={}` makes the runtime surface identical on m1 (ten plugins), m0b (none) and the VM seats: the run becomes deterministic across machines, and the handshake time stops depending on whose account launched it.
- D1.5 Configuration identity: the override is invisible to `codex_config_identity` (codex.sh:32-44 → `config identity` → `BuildConfigIdentity`, internal/config/confidentity.go:22-57, which hashes the contents of config.toml files) and stays invisible by design. The identity fingerprints the operator's files so a snapshot matches the configuration the probe saw; the override is adapter code, changes on every machine at once with the script, and is covered by the supervision fingerprint of the active scripts. Accepted consequence: an operator editing their `plugins` table still changes `configHash` and forces a re-probe although delegates ignore plugins; the config-identity filter was retired under R-33-m0 and is not reopened here.
- D1.6 Tests. internal/adapter/runtime_test.go `TestBuildCodexCommand` (:88-120): the exact `want` argv gains `"-c", "plugins={}"` after `approval_policy="never"`; the resume needle list gains `plugins={}`. internal/adapter/codexcommand_test.go (the brief's named file; today it holds only `TestCodexPermissionSettings`) gains `TestBuildCodexCommandDisablesOperatorPlugins`: for both verbs, exactly one adjacent pair `-c`,`plugins={}`, at the index directly after the `approval_policy="never"` pair, and no other argv element containing `plugins`. `TestBuildCodexCommandCarriesReasoningEffort` and `…CarriesExtraDirs` are substring tests and stay unchanged. codex.sh itself does not change for Part 1.

## 3. Part 2 — liveness-based patience replaces the fixed cap

Definitions. The custodian is the adapter supervisor process (record `pid`, `ownershipProof`). The child is the CLI process it forked, registered as a `custodyProcesses` entry by `register_cli_custody` → `CustodyAdd` (runtime-common.sh:102-115; internal/dispatch/custody.go:16-95). The waiter is the dispatcher's `await_handshake`. The backstop is `reap_one_locked` and the Go reaper (internal/supervise/reaper.go:122-157). The session signal for Codex is the first `thread.started` / `thread.created` / `session.created` event carrying an id (`CodexEventField`, codex.go:16-37) → `record_handshake` → `__handshake` → `HandshakeEval` → status running with `sessionId`. The bound `B` is the record's `sessionEstablishedTimeoutSec`. Times on the record: `handshakeDeadline` and `handshakeProgressAt` are integer epoch seconds from the writer's clock (`date +%s` in shell, `time.Now().Unix()` in Go), the clock `handshakeDeadline` already uses; `endedAt`, `startedAt` and `ownershipProof.provenAt` stay ISO strings as today.

- D2.1 Progress is exactly three events; each sets `handshakeDeadline := now + B` and `handshakeProgressAt := now` on the record, so `handshakeDeadline == handshakeProgressAt + B` holds after every stamp. P1 launch: the ownership patch (dispatch.sh:825-835 → `BuildOwnershipPatch`, ownership.go:78-80) writes BOTH fields from one clock reading (D2.4). P2 child registered: `CustodyAdd` refreshes both fields in the same locked record write when the record has no `sessionId` yet (a post-session custody entry, such as the fake's critique child, refreshes nothing). P3 output growth: the custodian's pre-session loop compares the byte sizes of the events stream (`$events`) and the job log (`$log`, the child's stderr sink) with the last observed sizes and calls `__handshake-progress` when either grew, at most once per second. For Codex 0.148 P3 never fires before `thread.started` (§1 evidence); a healthy Codex start must therefore signal within `B` seconds of P2, which is the child's fork, not the dispatcher's launch. Rejected as progress: the child's CPU time or process-tree growth (motion, not progress: a spinning deadlock advances both, and each needs a per-platform prober the measured cause does not justify once Part 1 removes it) and the heartbeat file (touched unconditionally by the custodian's loop, so it proves the custodian, not the child).
- D2.2 Liveness the waiter checks is the custodian's exact identity: `tag_state pid tag` (`proc classify`, dispatch.sh:280-283), where `live` and `unknown` keep waiting and `dead` or `stale` ends the wait. The child's liveness is the custodian's own business: its loop `while kill -0 "$cli_pid"` (codex.sh:154) ends the instant the child exits, and the custodian names that exit (D2.5). No new liveness verb is needed.
- D2.3 The bound and its snapshot-level discriminator. [closes CHS-R1-SNAPSHOT-01] The snapshot is immutable evidence, so its old field keeps its old meaning forever and the new meaning gets a new field:
  - Every adapter probe writes a NEW capability `handshakeProgressBoundSec` beside the old one, with the same integer: codex.sh 30, claude.sh 20, devin.sh 30, and the fake probe (`WriteFakeCapabilitySnapshot`, internal/adapter/fake.go:216-263, writes `handshakeProgressBoundSec: handshakeSec` next to `sessionEstablishedTimeoutSec: handshakeSec`; without it every fixture would run launch-anchored). `sessionEstablishedTimeoutSec` keeps being written with its launch-anchored meaning, so a new snapshot read by an older engine still selects.
  - `capability.Select` (select.go:101-104, 160-166): when `handshakeProgressBoundSec` is present it is validated exactly like the old field (integer, 1..60, else "capability snapshot has an invalid handshake progress bound") and `Result` gains `HandshakeBound: "progress"` (`json:"handshakeBound"`) and `HandshakeBoundSec: <that value>` (`json:"handshakeBoundSec"`); when it is absent, `HandshakeBound: "launch"` and `HandshakeBoundSec: <sessionEstablishedTimeoutSec>`. `SessionEstablishedTimeoutSec` stays in `Result` unchanged for its other readers. An old snapshot therefore keeps its launch semantics until it is re-probed; no snapshot is reinterpreted.
  - dispatch.sh `read_snapshot_fields` (:1592-1598) reads `handshakeBound` into `handshake_bound` and takes `handshake_budget` from `handshakeBoundSec` (equal to the old field on every shipped snapshot). Both `job build-record` call sites (:1542, :2008) pass `--handshake-bound "$handshake_bound"` next to `--handshake-budget`. build.go (both record builders, :416 and :627, `BuildParams`/`FollowUpParams` gain `HandshakeBound string`) copies it to a new record field `handshakeBound` and refuses any value but `launch` or `progress`; `sessionEstablishedTimeoutSec` on the job record stays the ONE number waiter, custodian and reaper read, and `handshakeBound` joins `immutableFields` (record.go:60-75). A record without the field (one created before the upgrade) reads as `launch`.
  - Who branches, and where: exactly one place gates the semantics, `RefreshHandshakeDeadline` (D2.4), which refuses without error when `handshakeBound` is not `progress`; under `launch` P2 and P3 are therefore no-ops and `handshakeDeadline` stays the launch stamp for the record's life. The waiter's loop, `check_record_deadlines` and `ComputeReapFacts` do NOT branch: all three read `handshakeDeadline`, which is the last stamp under either meaning, and the reaper's `startedAt + budget` fallback (reapfacts.go:95-100) stays for a record with no deadline. Two cosmetic reads of the field: the custodian's `cache_record_deadlines` also caches `handshake_bound` and `note_cli_progress` returns at once unless it is `progress` (the engine would refuse anyway); the dispatcher's outcome line (below) words `handshake_timeout` by it.
  - Why this and not a rename or a record-only marker: a rename invalidates every existing snapshot's selection for nothing; a record-only marker (revision 1's `handshakeProgressAt` presence) cannot tell an old snapshot from a new one because the record is built from the snapshot, which is exactly Sol's finding. The comment at codex.sh:64-65 is rewritten to describe both fields; the numbers do not move, and select.go's 1..60 validation stays as the ceiling on the ceiling.
- D2.4 `handshakeDeadline` stays the ONE number waiter, custodian and reaper read. It becomes refreshable: it is in neither `immutableFields` nor `ownershipPatchFields` (ownership.go:9-13), so a pending→pending record CAS may carry it, and `handshakeProgressAt` travels with it under the same rule. [closes CHS-R1-PROGRESS-04] Exactly THREE writers: (1) P1, `BuildOwnershipPatch` (ownership.go:58-82) gains a parameter `handshakeProgressAt int64` and writes `handshakeProgressAt` whenever it writes `handshakeDeadline` (both required together, `deadline == progressAt + B` not checked there because the builder does not read the record); the verb `job ownership-patch` (cmd/metasystem/dispatch_verbs.go:558) gains `--handshake-progress-at`, and dispatch.sh:829-835 takes ONE `launch_epoch=$(date +%s)` reading, derives `handshake_deadline=$(( launch_epoch + handshake_budget ))`, and passes both flags. (2) P2, `CustodyAdd`. (3) P3, the new engine verb `job handshake-progress --root R --job J` behind `__handshake-progress`. Writers 2 and 3 go through one function `RefreshHandshakeDeadline(record map[string]any, now time.Time) bool` in the new file internal/dispatch/handshake_progress.go: it returns false and writes nothing unless `status == "pending"`, `sessionId` is empty, `handshakeBound == "progress"` and `sessionEstablishedTimeoutSec >= 1`; it never moves the deadline earlier (`now + B <= handshakeDeadline` → false); otherwise it sets both fields and returns true. The same file holds `HandshakeProgress(root, job string, now time.Time) error` under `withRecordSessionLock`, the verb's body.
- D2.5 Exit verdict. [closes CHS-R1-EXIT-02; rewritten in revision 3, closes the class half of CHS-R2-CRITIC-FOLD-01] In `adjudicateTurnStage` initial stage (internal/adapter/adjudicate.go:198-207), a CLI that ended before the handshake yields `fail-pending handshake_failed:exit=<status> handshake` for every status, zero included; `<status>` is the shell wait status (128 plus the signal number on signal death). This replaces `runtime_error` (non-zero) and `handshake_missing_session_id` (zero) on that path only; `HandshakeEval`'s `handshake_missing_session_id` (internal/dispatch/handshake.go:123-125) is a different fact, a session signal without an id, and stays.
  - The critic fold is RESTORED (revision 2 had withdrawn it, and Sol showed why that wedges a chain: the only road to `CritiqueRegisterAdvance` is the follow-up gate, dispatch.sh:1727, which admits a failed parent only when `error == protocol_error`). `criticFailureFold` (adjudicate.go:159-180) gains one rule before its `switch`: when `strings.HasPrefix(verdict, "fail-pending handshake_failed:exit=") && strings.HasSuffix(verdict, " handshake")` and the record's role is `design-critic`, `code-critic` or `warden`, it returns `"fail-pending protocol_error handshake"`, the same string its `runtime_error handshake` case has always produced; the four existing cases stay verbatim (the `fail-pending runtime_error handshake` case is no longer reachable from the initial stage after this revision and is kept for the record). Non-critic roles keep the unfolded `handshake_failed:exit=N`. Consequences that make the fold free: the follow-up gate (dispatch.sh:1727) and `syntheticProtocolFindingID` (internal/dispatch/finding_register.go:157-160, hashes the literal `protocol_error`, the role and the round job) are untouched; `foldProtocolError` (:139-155) keeps copying `error`, `phase` and `protocolError` into the evidence digest, so a folded critic's digest reads `protocol_error` / `handshake` / null and the digest is NOT widened in this design (the exit status is read from the round record the finding's `critic` field names, below). The readers of the literal `protocol_error` named in revision 2 (record.go:437; internal/adapter/selftestrun.go:89-92) still do not change.
  - The exit status is carried OUTSIDE the error class, for every role, so the fold erases nothing: a new integer record field `handshakeExitStatus`. Writer: the custodian's fail-pending branch in `complete_from_cli` (scripts/agents/adapters/runtime-common.sh:365-368), which already holds the CLI status it passed as `--cli-status` (:350). Mechanics, one field per layer: `WriteResultPatch` (internal/adapter/patch.go:43-59) gains a fifth parameter `handshakeExitStatus int`, written as the integer key `handshakeExitStatus` when it is 0 through 255 and omitted when it is -1; `adapter result-patch` (cmd/metasystem/adapter_verbs.go:161-179) gains `--handshake-exit-status N` (default -1, meaning absent; any other value outside 0..255 is a usage error, exit 2); `write_patch` (runtime-common.sh:171-173) and `fail_pending` (:175-184) each gain an optional trailing argument (exit status) forwarded as that flag when non-empty; `complete_from_cli`'s `fail-pending` branch calls `fail_pending "$2" "$3" "$usage_file" "$status"` when `$3 == handshake` and `fail_pending "$2" "$3" "$usage_file"` otherwise. `complete_from_cli` runs only after `wait_for_cli` returned (:298-310), so "the phase is handshake and the child exited" is exactly that branch; the other two `fail_pending … handshake` callers (`permissions_mismatch`, :125, before the fork; `handshake_timeout`, :275, child alive) write no field, and `finish_running` never does. The CAS admits the field: `handshakeExitStatus` is in neither `immutableFields` (record.go:60-75) nor `dedicatedMetadataFields`, and the pending→failed transition copies every patch key (record.go:522-539); no job-record schema exists under scripts/agents/schemas to extend. The field is therefore present on a folded critic (`error: protocol_error, phase: handshake, handshakeExitStatus: N`) and on every other role (`error: handshake_failed:exit=N, phase: handshake, handshakeExitStatus: N`), and absent on every hang, permission, launch and post-session failure.
  - The dispatcher's outcome line reads the field, not the error (below). The job log line `child exited before the session signal with status N` stays for every role. `patienceNeverStarted` (internal/missionrunner/patience.go:44-53) treats the prefix `handshake_failed:` as never-started next to `permissions_mismatch:`; a folded critic carries `protocol_error`, which is not in that set, exactly as today's `runtime_error`-folded critic is not, and this revision does not widen patience.
  - The fold is single-sourced into `fail_pending`. [closes CHS-R3-EXIT-01; revision 5] Sol's round-3 evidence: devin.sh:650-662 (36bcda71), on a status-zero exit with no session and empty stdout, calls `fail_pending` directly, `handshake_missing_session_id handshake` when the presence scan (`adapter devin-collect --presence-only`, :654-657) reports `candidatesPresent: true` and `empty_reply delivery` otherwise, bypassing `complete_from_cli` (:690), adjudication, `criticFailureFold` and the `handshakeExitStatus` forwarding above; a Devin critic in that shape is not `protocol_error`, the follow-up gate (dispatch.sh:1727) refuses it and the chain wedges, today and under revision 4 alike (the `empty_reply` half wedges today too, per the orchestrator's note on the register). The D64 rule at devin.sh:647-649 stands (the presence decision is the engine's; the shell only routes verdicts), so Devin is NOT re-routed through `complete_from_cli`; instead the fold moves to the one function every adapter's direct failure already passes through, and the Go table stays the single source:
    - The engine. `criticFailureFold` (adjudicate.go:155-180) is refactored so the fold operates on an (error, phase) pair: a new exported `CriticFailureFoldClass(recordPath, errorClass, phase string) string` in adjudicate.go reads the record as `criticFailureFold` does today (unreadable record, or a role other than `design-critic`, `code-critic`, `warden`: returns `errorClass` unchanged) and folds exactly these pairs to `protocol_error`, else returns `errorClass` unchanged: (`runtime_error`, `runtime`), (`runtime_error`, `handshake`), (`empty_reply`, `delivery`), (any error with the prefix `handshake_failed:exit=`, `handshake`) (the rev-3 rule above), and (`handshake_missing_session_id`, `handshake`). The last pair is new in revision 5 and exists for one caller, devin.sh:658: the adjudicator's initial stage no longer emits that class (it emits `handshake_failed:exit=0` under D2.5), `HandshakeEval`'s `handshake_missing_session_id` (handshake.go:123-125) is written by the dispatcher's `__handshake`, never through `fail_pending`, and so the pair folds precisely the Devin critic whose zero-exit turn left a parseable but uncorrelated reply, which is a paid critic attempt with no register-consumable class exactly like the empty one; the non-critic Devin name does not move (the delivery design's pinned outcome, records/delegate-delivery/delegate-delivery-design.md:206, is a non-critic contract and stays). `protocol_error` is unlisted, so the fold is idempotent on it. `criticFailureFold(recordPath, verdict)` becomes one wrapper over the pair form: it splits a verdict of the shape `fail-pending <error> <phase>` or `finish failed <error> <phase>`, calls `CriticFailureFoldClass` on the pair and reassembles the verdict with the returned class; any other verdict string (`finish completed null completed`, `repair`, `settle`, `protocol-error`, `delivery-repair`) passes through untouched. Its four existing outcomes are byte-identical (the four `case` strings of :168-176 map to three of the pairs), and `TestAdjudicateCriticCrashAndEmptyReplyFoldToProtocolError` (adjudicate_test.go:47) and `TestCriticFailureFoldKeepsHandshakeExit` (D2.5 above) stay green unchanged as the wrapper's pins.
    - The verb. `adapter critic-fold --record R --error E --phase P` (cmd/metasystem/adapter_verbs.go, `runAdapterCriticFold`, registered in the `adapter` verb table of cmd/metasystem/main.go:161-195 directly after the `result-patch` row at :169, summary "print the terminal error class a critic record folds a failure to") prints `CriticFailureFoldClass(R, E, P)` followed by a newline and exits 0; an empty `--record`, `--error` or `--phase` is a usage error (exit 2, usage line on stderr, nothing on stdout). It prints the input unchanged for every non-critic role, every unlisted pair and an unreadable record, so it never refuses a fold request it could parse.
    - The shell. `fail_pending` (runtime-common.sh:175-184; after revision 4 it already takes the optional fourth argument, the exit status) asks the engine before it writes: `folded=$("$ms" adapter critic-fold --record "$record" --error "$error" --phase "$phase" 2>>"$log") && [[ -n "$folded" ]] && error=$folded`, on one line so that `set -e` cannot end the custodian's turn on the verb's failure; a verb that cannot run (an engine lacking it, or a usage refusal) leaves `error` as the caller passed it, because `fail_pending` is the last writer a failing turn has and an unwritten record is worse than an unfolded one. It then writes the folded class through `write_patch` and, when the fourth argument is present, `handshakeExitStatus` through the `--handshake-exit-status` flag, and runs the pending→failed CAS as today. `$record` is the adapter global every adapter sets before its first `fail_pending` (claude.sh:126, codex.sh and devin.sh alike; `adjudicate_turn`, :340-341, already passes it). Nothing double-folds: `complete_from_cli`'s `fail-pending` branch (:365-368) receives a verdict the adjudicator has already folded, so a critic's class arrives as `protocol_error` and the verb returns it unchanged, and a non-critic's class arrives as `handshake_failed:exit=N` and the verb returns it unchanged because the role is not a critic role; `complete_from_cli` itself does not change in revision 5 beyond what revision 4 specified. Consequences for every other direct caller, enumerated at 36bcda71: runtime-common.sh:125 (`permissions_mismatch:*` handshake), :134 (`resume_collision` resume), :275 (`handshake_timeout` handshake), codex.sh:150 and :153 (`prefork_marker`, `custody_registration`), claude.sh:132, :133, :140, :149 (`invalid_native_budget`, `invalid_native_turn_limit`, `prefork_marker`, `custody_registration`), devin.sh:283-325 (the `setup`-phase classes), :337-356, :380, :538, :545, :570, :581, :602 (`prefork_marker`, `custody_registration`, `acp_server_died`, `session_baseline_unavailable`, `session_baseline_unreadable`, `ambiguous_session_correlation`) and the ACP `acp_terminal` helper at :398 (`acp_client_mechanical` and its siblings) are unlisted pairs and write what they write today for every role. claude.sh:134 and :138 write `runtime_error handshake` before the fork (a command that could not be built); that pair IS listed, so a Claude critic that never launched is now `protocol_error handshake` where today it is `runtime_error handshake`, the same class the adjudicator has always given a Claude critic that crashed before its session; a Claude implementer keeps `runtime_error`. devin.sh:706 forwards a verdict `adjudicate_turn empty-delivery` already folded, so the verb is idempotent there. `finish_running` (:186) does not fold: revision 5 is one fold in `fail_pending`, and the running→failed direct writers stay as they are.
    - devin.sh:650-662. The candidates-present branch becomes `fail_pending handshake_missing_session_id handshake "$usage_file" "$cli_status"`: the status is zero on this branch (:650), so the record gains `handshakeExitStatus: 0` for every role, and the dispatcher's outcome line (below) reads "exited with status 0 before establishing its session" for it. The empty branch stays `fail_pending empty_reply delivery "$usage_file"` with no status argument (a delivery verdict, not a handshake one, so the field stays absent as D2.5 promises for post-session and delivery failures) and is now folded for critics like every other `empty_reply delivery`. Resulting records: a Devin implementer reads `error: handshake_missing_session_id, phase: handshake, handshakeExitStatus: 0` or `error: empty_reply, phase: delivery`; a Devin critic reads `error: protocol_error, phase: handshake, handshakeExitStatus: 0` or `error: protocol_error, phase: delivery`, and the sessionless follow-up of D2.7 admits both. The job log line `devin cli exit status=0` (:623) stays.
    - What pins Devin's two outcomes today, found by search at 36bcda71: no shell fixture drives scripts/agents/adapters/devin.sh's initial-turn branch (`rg -n 'adapters/devin' scripts/agents/*fixtures*.sh` matches nothing; scripts/agents/acp-fixtures.sh drives the Devin HOST script and a fake ACP server, not the adapter); the presence decision behind the two outcomes is pinned in Go by `TestCollectPresenceBars` (internal/adapter/devincollect_test.go:208-229: nothing on disk and a torn named file are not present, any non-empty stdout is present), which does not read a role and is unchanged. The one fixture that drives the REAL `fail_pending` is scripts/agents/adapter-deadline-fixtures.sh (ADPT-DL-001 to -005, :102-160, the real runtime-common.sh under a stub `$dispatch` that logs every CAS argv and the real `$ms`, so the new verb resolves there); its driver's record (:66-68) carries no `role`, so the verb returns the class unchanged and ADPT-DL-002's one `expect pending --status failed` CAS (:123-124) and every other assertion stay green unchanged. The critic-shaped row the brief asks for is fixed in its assertions and OPEN in its home (§7): a `{"role":"design-critic"}` record, the call `fail_pending handshake_missing_session_id handshake "" 0`, and exact `json get` comparisons on the written patch `pending-failure.json`: `error == protocol_error`, `phase == handshake`, `handshakeExitStatus == 0`; a second call on a `{"role":"implementer"}` record asserts `error == handshake_missing_session_id` and `handshakeExitStatus == 0`.
- D2.7 The sessionless follow-up. [closes the session half of CHS-R2-CRITIC-FOLD-01; the claim, open in revision 3, is decided below] Today dispatch.sh:1757 refuses every parent without a `sessionId`, so even a `fail-pending protocol_error handshake` critic (the goal's own specimens, breach-design-crit2 and -crit2b) could never reach the register fold at :1855-1859; the class fold above is worthless without this half. The exact change at :1757: `session` is read as today (:1756); then
  ```
  if [[ -z "$session" || "$session" == null ]]; then
    [[ "$status" == failed && "$error" == protocol_error ]] \
      || die 1 "follow-up has no resumable session id; use the fresh-context embed fallback"
    session=; sessionless_parent=1
  fi
  ```
  where `sessionless_parent` is a new local initialised to 0 at :1686 and `status`/`error` are the values the gate read at :1726 (in the repeated-follow-up branch, :1730-1751, `latest` is re-pointed at the parent record, so `status` and `error` are re-read from `$latest` immediately before this test). A `completed` parent without a session is still refused, with the unchanged message, so the existing `null-session-follow-up` fixture (scripts/agents/dispatch-fixtures.sh:2258-2259, a completed verifier whose `sessionId` was nulled) stays green unchanged. `resumed_for_claim=$session` (:1761) is then empty. The register fold at :1855-1859 runs exactly as today because the parent is `failed` (`CritiqueRegisterAdvance` accepts it at finding_register.go:52) and folds one synthetic finding for the parent round. The fresh-context road (:1888-1896) is taken when `[[ "$resume_cap" != true ]] || (( sessionless_parent ))` (the fake and every current snapshot declare `resume: true`, internal/adapter/fake.go:253, so the second disjunct is what carries this case): `resume_mode=fresh-context`, `adapter_verb=dispatch`, and `continuation_args` holds `prior-brief=$payload/brief.md` always and `prior-return=$payload/rounds/$parent_round/return.json` only when that file exists (`[[ -f … ]]`); a pre-session exit leaves no return, and `compose-role-packet` refuses an unreadable continuation (internal/dispatch/composition.go:212-215), which is why the slot is omitted rather than pointed at a missing file. `build-follow-record` (internal/dispatch/build.go:571-577, :617, :628) already writes `sessionId: null` and `resumeMode: fresh-context` for that mode without reading the parent session. Round N+1 therefore starts fresh, with the prior brief embedded, on the same chain.
  - The claim [decided by the orchestrator, m0b, 2026-09-02 17:05Z; revision 4]. The sessionless follow-up stays a follow-up: the truthful record. The rejected alternative, claiming it as `--dispatch-mode fresh`, would write a round-2 record whose `dispatchMode` reads `fresh` while its `round`, `parentJob` and `resumeMode` say follow-up, a record that lies, and both the gate's repeated-follow-up branch (:1730-1731) and the critic hazard check (internal/dispatch/hazard.go:280) key on `dispatchMode`. And it is done WITHOUT bumping `LaunchFingerprintVersion` (internal/dispatch/claim_fingerprint.go:17). Reason, for the record: hazard.go:339 admits a linked evidence job only when its `fingerprintVersion` equals the current constant, and internal/dispatch/claim.go:113 refuses as legacy a standing record whose version differs; a bump would therefore disown every existing proven evidence job (every critique register's `--reviews` chain) and every record in flight across the upgrade. The extension is version-compatible, in four pieces:
    - The request. `LaunchFingerprintRequest` (claim_fingerprint.go:36-51) and `CanonicalLaunchRequest` (:55-70) each gain `ParentSessionless bool`; the canonical one is tagged `json:"parentSessionless,omitempty"`, so the canonical JSON of every existing request (the committed golden-vector shape, :54) is byte-identical. The flag is NOT hashed: the v2 wire value list (:255-262) and the preamble (:84) are untouched, so every existing v2 digest is reproduced byte for byte and `TestLaunchFingerprintV1GoldenVectors` (claim_fingerprint_test.go:16) stays green unchanged. Two requests that differ only in the flag hash alike; that is harmless because the flag is lawful only where the unflagged tuple is refused (next piece), and `dispatchMode`, which IS hashed, already separates the sessionless follow-up from the fresh dispatch of the same child.
    - Validation, both layers. `CanonicalizeLaunchFingerprint` (:104-106) and `validateCanonicalLaunchRequest` (:282-284) each change the rule "follow-up requires a resumed session" to "follow-up requires a resumed session unless ParentSessionless"; each gains two refusals: a fresh dispatch with the flag set ("fresh dispatch cannot mark a sessionless parent") and the flag set with a non-empty resumed session ("a sessionless parent cannot carry a resumed session", the contradiction). :137-152 copies the flag into the canonical request. Test rows in internal/dispatch/claim_fingerprint_test.go (the file that pins `validateCanonicalLaunchRequest` today, through `CanonicalizeLaunchFingerprint` → `LaunchFingerprintV2`), beside `TestFingerprintChangesForDispatchModeAndResumedSession` (:161) and built from `launchFingerprintRequestForTest` (:191): `TestFingerprintAdmitsSessionlessFollowUp` (follow-up, resumed `""`, flag set: canonicalizes, and its digest differs from the fresh request's digest), `TestFingerprintRefusesSessionlessFlagOnFresh` (fresh, resumed `""`, flag set: refused), `TestFingerprintRefusesSessionlessFlagWithResumedSession` (follow-up, resumed `"runtime-session-a"`, flag set: refused), and `TestFingerprintFlagKeepsExistingDigests` (the fresh and the resumed follow-up requests of :161 hash to the same digests with the flag field present and false as they did before the field existed, pinned against the two digests recorded when the test is written; this row is the no-bump guarantee).
    - The record. claim.go:690-692 writes `parentSessionless: true` beside `dispatchMode` and `resumedSessionId` only when the flag is set, and writes nothing otherwise, so no existing record changes shape; the key joins `immutableFields` (record.go:60-75) and the setup-transition carry list (record.go:346-354) so the husk swap keeps it. `resumedSessionId` on the round-2 record is the empty string, written from the request at claim.go:692 as every fresh dispatch's is, and immutable (record.go:69). What hazard.go does with that empty value, read for this revision: :280 refuses an independent-critique reference whose `parentJob` is present, or whose `dispatchMode` is not `fresh`, or whose `resumedSessionId` is non-empty; a sessionless round 2 is refused there by its parent and its mode, and its empty `resumedSessionId` is exactly what a fresh critic carries, so :280 needs no guard. :386 reads `resumedSessionId` with `allowEmpty` true, so the empty string reconstructs. But :347 recomputes the digest through `LaunchFingerprintV2`, which validates the reconstructed request; without the flag that validation refuses "follow-up requires a resumed session" and :349 reports "fingerprint inconsistent with its admission identity", a true refusal (a follow-up is never admissible evidence, :280; internal/dispatch/review_reference.go:23) with a false reason. ONE guard therefore: `hazardEvidenceLaunchRequest` (:379) reads `parentSessionless` from the record (a boolean; absent means false) into `CanonicalLaunchRequest.ParentSessionless`, so a sessionless round-2 record reproduces its digest and is refused at :280 with the honest reason. Test row in the hazard test file that pins `validateHazardEvidenceAdmissionProvenance` today (the implementer names it): a sessionless follow-up record with `parentSessionless: true`, `resumedSessionId: ""` and a digest computed with the flag passes provenance and is refused by :280 as "not a fresh-context chain".
    - The verbs and the shell. `job claim-launch`, with and without `--preflight` (cmd/metasystem/dispatch_verbs.go:166-255), gains the boolean flag `--parent-sessionless` (default false) copied into `LaunchFingerprintRequest.ParentSessionless` at :245-252. `job claim-occupancy-prepare` (:343-355) carries no fingerprint request and no `--resumed-session` today (dispatch.sh:1961 passes only `--root`, `--session`, `--output`), so there is no field for it to set and it gains no flag; the brief's premise that all three verbs set the field is corrected here, not decided. dispatch.sh: a new local `claim_session` is set right after the gate, `claim_session=$session`, and `claim_session=$child` when `sessionless_parent == 1`; the three lines :1934, :1961, :1965 pass `--session "$runtime:$claim_session"` (the child's own key for the sessionless case, as a fresh dispatch does at :1471, :1498, :1502); the two claim-launch lines keep `--resumed-session "$resumed_for_claim"` (the empty string, :1761) and add `--parent-sessionless` when `sessionless_parent == 1`. With a parent session all three lines produce the argv they produce today, byte for byte. The occupancy claim on a never-seen key is the fresh-dispatch case `claim-occupancy-prepare` already serves.
- D2.6 Custodian enforcement before the session. codex.sh's pre-session loop calls `note_cli_progress` (P3, new in runtime-common.sh) and `check_record_deadlines "$cli_pid"` on every tick, so the F4/D32 in-process enforcement (runtime-common.sh:259-283) finally covers the handshake phase. `cache_record_deadlines` re-arms after `register_cli_custody` and after each `__handshake-progress` (the custodian is the writer, so its cached copy is deterministic); the cap deadline keeps its read-once rule. [closes CHS-R1-DEADLINE-03] The custodian fires on the first tick where `now >= handshakeDeadline + HandshakeCustodianGraceSec` (new constant `HandshakeCustodianGraceSec int64 = 1` beside `HandshakeBackstopGraceSec` in reapfacts.go:44, mirrored as `handshake_custodian_grace_sec=1` in runtime-common.sh with the same one-constant comment dispatch.sh:75-77 carries): runtime-common.sh:290 becomes `(( now_epoch < handshake_deadline_epoch + handshake_custodian_grace_sec )) || enforce_expired_deadline handshake "$cli_child"`, the same instant the present `<=` test fires at, now spelled through the constant. On firing it terminates the child, sweeps the group, and writes `handshake_timeout` through `fail_pending`, standing down if the session landed (:261-263), all unchanged.

One equality boundary, integer seconds, three actors [closes CHS-R1-DEADLINE-03]: the waiter refuses on the first poll where `now >= handshakeDeadline`; the custodian on the first tick where `now >= handshakeDeadline + 1`; the reaper's `HandshakeWaiting` is `now < handshakeDeadline + 2` (reapfacts.go:94, unchanged). Consequence: with the waiter's 50 ms poll and the custodian's 100 ms heartbeat, each verdict lands within one poll interval after its boundary second begins, so the waiter's refusal is written no earlier than `handshakeDeadline` and at most one poll interval after it, one full second before the custodian could act and two before the backstop.

Waiter algorithm (`await_handshake`, dispatch.sh:870-895, rewritten; the `-le 60` guard on the argument stays as the fallback bound when a record carries no deadline):

```
every 50 ms, in this order, until one branch returns:
  status running|completed with a sessionId   -> accepted, return 0
  status failed|cancelled|timeout              -> refused; outcome text from record.error
  tag_state(pid, instanceTag) is dead or stale -> lease_run_held __reap-held --job (process-lost path); refused
  now >= handshakeDeadline (re-read each poll) -> __handshake-timeout (hung); refused
  now >= capDeadline (ISO, lexical compare)    -> __handshake-timeout; refused (the never-hit fail-stop)
```

The dispatcher's outcome line (dispatch.sh:1623) is worded from the record, for every role alike, in this order: when `handshakeExitStatus` is a non-null integer → "the runtime exited with status N before establishing its session" (read from the FIELD, so a folded critic whose `error` is `protocol_error` and an implementer whose `error` is `handshake_failed:exit=N` print the same sentence; the error class is not consulted for this row); otherwise by `error`: `handshake_timeout` with `handshakeBound` `progress` → "the runtime made no progress for B seconds before establishing its session", with `launch` or absent → "the runtime did not establish its session within B seconds of launch"; `process-lost` → "the runtime's supervisor was lost before establishing its session"; any other error → the error string verbatim. `HANDSHAKE-FAILED` stays the outcome class.

Verdict table:

| Observation | Who writes | Record | Dispatcher |
| --- | --- | --- | --- |
| Session signal seen before any verdict | custodian via `__handshake` | running, sessionId | accepted |
| Child exited before the signal | custodian, adjudicate initial; `CriticFailureFoldClass` for the three critic roles, through the adjudicator's wrapper or through `fail_pending`'s `critic-fold` call on devin.sh's direct branch (D2.5) | failed, phase handshake, `handshakeExitStatus: N` for every role; `error` is `handshake_failed:exit=N` (`handshake_missing_session_id` on devin.sh:658) for non-critics and `protocol_error` for design-critic, code-critic and warden (D2.5) | HANDSHAKE-FAILED, "exited with status N" from the field |
| Child alive, no progress for the bound | waiter at `now >= deadline`; custodian at `now >= deadline + 1` if the waiter is gone | failed, `handshake_timeout`, phase handshake | HANDSHAKE-FAILED, bound quoted, worded by `handshakeBound` |
| Custodian provably dead | reaper (`reap_one_locked` or the Go reaper), any time | failed, `process-lost`, phase supervision | HANDSHAKE-FAILED |
| Dispatcher gone, custodian alive | nobody until the custodian's own verdict (exit or `deadline + 1` hang); the reaper defers while `HandshakeWaiting` and takes no action on a live custodian afterwards (dispatch.sh:1064-1075; reaper.go:147-157, unchanged) | as the custodian decides | none |
| Session lands after a verdict was written | the late writer stands down (dispatch.sh:2368-2401; runtime-common.sh:261-263) | unchanged | unchanged |

What the reaper reads and decides: `status`, `sessionId`, `sessionEstablishedTimeoutSec`, `handshakeDeadline`, `startedAt`, through `ComputeReapFacts` unchanged; it does not read `handshakeBound`, because under `launch` the deadline is simply never refreshed. Because the custodian refreshes the same `handshakeDeadline` the waiter polls, `HandshakeWaiting` tracks the very window the waiter is inside and the two cannot disagree. With the dispatcher gone it decides exactly what it decides today: defer to a live or unreadable custodian, wind down and record `process-lost` for a dead one, never kill a live custodian in the handshake. The custodian's in-process enforcement (D2.6) is what makes a dead dispatcher harmless.

## 4. Files and functions touched

- internal/adapter/codex.go `BuildCodexCommand`: the `codexPluginsOverride` constant inserted after `approval_policy` on both verbs (D1.1, D1.2).
- internal/adapter/codexcommand_test.go, internal/adapter/runtime_test.go: the pins in D1.6.
- internal/adapter/adjudicate.go `adjudicateTurnStage` initial stage: the `handshake_failed:exit=N` verdict; the fold table is refactored into the pair form `CriticFailureFoldClass(recordPath, errorClass, phase)` (the five pairs of D2.5, including the rev-3 `handshake_failed:exit=` prefix rule and the rev-5 (`handshake_missing_session_id`, `handshake`) pair) and `criticFailureFold` becomes the one verdict-string wrapper over it (D2.5). [CHS-R2-CRITIC-FOLD-01] [CHS-R3-EXIT-01] The shell passes `--cli-status` already.
- internal/adapter/adjudicate_test.go, adjudicate_outage_test.go: the initial-stage rows in `TestAdjudicateTurnPureVerdicts` (:19-22) move from `runtime_error` / `handshake_missing_session_id` to `handshake_failed:exit=7` / `handshake_failed:exit=0`; new `TestCriticFailureFoldKeepsHandshakeExit` next to `TestAdjudicateCriticCrashAndEmptyReplyFoldToProtocolError` (:47): a `{"role":"design-critic"}` record with `AdjudicateParams{Stage: "initial", CLIStatus: 137, RecordPath: record}` yields `fail-pending protocol_error handshake`, and a `{"role":"implementer"}` record with the same params stays `fail-pending handshake_failed:exit=137 handshake` (D2.5). [CHS-R2-CRITIC-FOLD-01] New `TestCriticFailureFoldClassPairs` beside them: the pair-form table of §5 (D2.5). [CHS-R3-EXIT-01]
- cmd/metasystem/adapter_verbs.go `runAdapterCriticFold` and the `critic-fold` row in cmd/metasystem/main.go's `adapter` verb table (:161-195, after `result-patch` at :169): the verb of D2.5. cmd/metasystem/adapter_runtime_verbs_test.go (the existing adapter verb test file; no test pins `runAdapterResultPatch` today) gains `TestRunAdapterCriticFold` (§5). [CHS-R3-EXIT-01]
- internal/adapter/patch.go `WriteResultPatch` and cmd/metasystem/adapter_verbs.go `runAdapterResultPatch`: the `handshakeExitStatus` parameter and `--handshake-exit-status` flag (D2.5); internal/adapter patch tests (the file that pins `WriteResultPatch` today; the implementer names it) gain one row with the field present and one with it absent. [CHS-R2-CRITIC-FOLD-01]
- internal/adapter/fake.go `WriteFakeCapabilitySnapshot` (:252-263): writes `handshakeProgressBoundSec`; internal/adapter/fake_test.go:287 pins both fields (D2.3). [CHS-R1-SNAPSHOT-01]
- internal/capability/select.go `Select` and `Result`: `handshakeProgressBoundSec` read, validation, `HandshakeBound` and `HandshakeBoundSec` with the launch default; internal/capability/select_test.go: one snapshot with the new field selects `progress`, one without selects `launch` with the old value (D2.3). [CHS-R1-SNAPSHOT-01]
- internal/dispatch/build.go both record builders (:416, :627) and their params: `HandshakeBound`, the `launch|progress` refusal, the `handshakeBound` record field; internal/dispatch/record.go `immutableFields` gains `handshakeBound`; internal/dispatch/decisions_test.go `TestBuildRecordsCohereWithLifecycle` (:364) and `TestBuildRecordCarriesConfiguredMaximalModelEffort` (:447) pass `HandshakeBound: "progress"` and one new assertion reads it back (D2.3). [CHS-R1-SNAPSHOT-01]
- cmd/metasystem/dispatch_verbs.go: `--handshake-bound` on both `job build-record` verbs (:763, :810); `--handshake-progress-at` on `job ownership-patch` (:558); register `job handshake-progress --root R --job J` (also cmd/metasystem/main.go); `--parent-sessionless` on `job claim-launch` with and without `--preflight` (:173-175 flag block, :245-252 request), copied into `LaunchFingerprintRequest.ParentSessionless` (D2.7). `job claim-occupancy-prepare` (:343-355) is unchanged: it has no fingerprint request to carry the flag. [CHS-R2-CRITIC-FOLD-01]
- internal/dispatch/ownership.go `BuildOwnershipPatch`: the `handshakeProgressAt` parameter, written with the deadline (P1, D2.4); internal/dispatch/exact_identity_test.go `TestOwnershipPatchPersistsTheNativeExactIdentity` (:54, the existing pin) gains the two-field assertion. [CHS-R1-PROGRESS-04]
- internal/dispatch/reapfacts.go: `HandshakeCustodianGraceSec = 1`; the `HandshakeWaiting` comment says the deadline is progress-refreshed; logic unchanged.
- internal/dispatch/custody.go `CustodyAdd`: calls `RefreshHandshakeDeadline` inside the same locked write (P2); new file internal/dispatch/handshake_progress.go with `RefreshHandshakeDeadline` and `HandshakeProgress` under `withRecordSessionLock` (D2.4).
- internal/dispatch test files [closes CHS-R1-FILES-06]: new internal/dispatch/handshake_progress_test.go, owned by the new source file, holds `TestRefreshHandshakeDeadlineNeverMovesEarlier`, `TestRefreshHandshakeDeadlineRefusesLaunchBoundAndSessions` and `TestHandshakeProgressWritesUnderTheSessionLock`; internal/dispatch/decisions_test.go holds `TestCustodyAddRefreshesHandshakeDeadline` next to `TestCustodyAddDedupesAndRefusesTerminal` (:121) and the new `TestComputeReapFacts` row (:243-289); internal/dispatch/stage4_custody_test.go (`TestCustodyWriteLandsBeforeMarkerRemoval`, :213, calls `CustodyAdd` at :234) and internal/dispatch/exact_identity_test.go (`TestCustodyAddPersistsAndDeduplicatesTheNativeExactIdentity`, :144-160) are existing `CustodyAdd` pins whose fixture records carry no `handshakeBound`, so the refresh refuses and they stay green unchanged; the implementer runs them and reports.
- internal/missionrunner/patience.go: `handshake_failed:` never-started prefix; patience_test.go row.
- scripts/agents/dispatch.sh: `read_snapshot_fields` and both build-record call sites (D2.3); the launch stamp from one clock reading with `--handshake-progress-at` (D2.4); `await_handshake` per the algorithm; `__handshake-progress` internal verb (valid id, record exists, calls the engine verb); the outcome message at :1623 worded from `handshakeExitStatus` first and `handshakeBound` second (D2.5); the `handshake_backstop_grace_sec` comment names the custodian grace; the sessionless-parent gate at :1757, the `sessionless_parent` local at :1686, and the fresh-context condition and conditional `prior-return` continuation at :1888-1896 (D2.7); the `claim_session` local and the three claim lines :1934, :1961, :1965 (`--session "$runtime:$claim_session"`; `--parent-sessionless` on the two claim-launch lines when `sessionless_parent == 1`; D2.7). [CHS-R2-CRITIC-FOLD-01]
- scripts/agents/adapters/runtime-common.sh: `note_cli_progress` (gated on the cached `handshake_bound`); `handshake_custodian_grace_sec=1` applied in `check_record_deadlines` with the `<` comparison of D2.6; `cache_record_deadlines` caches `handshake_bound` and gains its re-arm hook; `write_patch` and `fail_pending` take the optional exit-status argument and `complete_from_cli`'s fail-pending branch passes `$status` when the phase is `handshake` (D2.5); the log line in that branch (`child exited before the session signal with status N`) is written for every role from `$status`, not from the verdict, because a critic's verdict no longer carries the number. [CHS-R2-CRITIC-FOLD-01] `fail_pending` (:175-184) calls `adapter critic-fold` on its (error, phase) before `write_patch`, keeping the caller's class when the verb cannot run (D2.5); `finish_running` and `complete_from_cli` do not change for this. [CHS-R3-EXIT-01]
- scripts/agents/adapters/codex.sh, claude.sh, devin.sh [closes CHS-R1-SNAPSHOT-01]: each probe writes `handshakeProgressBoundSec` (30, 20, 30) beside its unchanged `sessionEstablishedTimeoutSec`; codex.sh's snapshot comment (:64-65) describes both fields; codex.sh's pre-session loop (:154-166) calls `note_cli_progress` and `check_record_deadlines`. claude.sh changes nothing but the probe line (its loop stays as §6 says).
- scripts/agents/adapters/devin.sh: the probe line above, and the status-zero no-session branch (:650-662 at 36bcda71): :658 becomes `fail_pending handshake_missing_session_id handshake "$usage_file" "$cli_status"`, :661 stays `fail_pending empty_reply delivery "$usage_file"` (D2.5); the branch's structure, the presence call (:654-656) and the D64 comment (:647-649) do not move. [CHS-R3-EXIT-01]
- scripts/agents/adapters/fake.sh: the three behaviors in §5 and their usage text (:22-30); the `exit-before-session` behavior acts only in round 1 (§5, because the fresh-context packet embeds the round-1 brief and its marker into round 2's prompt, which `behavior_present`, :58, greps).
- scripts/agents/dispatch-fixtures.sh: the scenarios in §5 and the `agent_iso_epoch` helper; the follow-up leg of `exit-before-session` and its register assertions. [CHS-R2-CRITIC-FOLD-01]
- internal/dispatch/finding_register.go: unchanged by this design.
- internal/dispatch/claim_fingerprint.go: `ParentSessionless bool` on `LaunchFingerprintRequest` (:36-51) and, tagged `json:"parentSessionless,omitempty"`, on `CanonicalLaunchRequest` (:55-70); the "unless ParentSessionless" rule and the two new refusals in both `CanonicalizeLaunchFingerprint` (:104-106) and `validateCanonicalLaunchRequest` (:282-284); the copy at :137-152; the wire list (:255-262), the preamble (:84) and `LaunchFingerprintVersion` (:17) untouched (D2.7). internal/dispatch/claim_fingerprint_test.go: `TestFingerprintAdmitsSessionlessFollowUp`, `TestFingerprintRefusesSessionlessFlagOnFresh`, `TestFingerprintRefusesSessionlessFlagWithResumedSession`, `TestFingerprintFlagKeepsExistingDigests`, beside :161; `TestLaunchFingerprintV1GoldenVectors` (:16) and testdata/claim-fingerprint-v1.json unchanged. [CHS-R2-CRITIC-FOLD-01]
- internal/dispatch/claim.go:690-692: `parentSessionless: true` written beside `dispatchMode` and `resumedSessionId` only when the request carries the flag (D2.7). internal/dispatch/record.go: `parentSessionless` joins `immutableFields` (:60-75) and the setup-transition carry list (:346-354). [CHS-R2-CRITIC-FOLD-01]
- internal/dispatch/hazard.go `hazardEvidenceLaunchRequest` (:379): reads `parentSessionless` back into the reconstructed request, the one guard D2.7 names; :280 and :386 unchanged. The hazard test file that pins `validateHazardEvidenceAdmissionProvenance` today (the implementer names it) gains the sessionless round-2 row of D2.7. [CHS-R2-CRITIC-FOLD-01]
- docs/design/dispatch-sequence.md steps 15 and 16 (:159-186) and the handshake bullet of the reap decision (:216-223); docs/glossary.md Handshake entry (:68-70): "failed as handshake_timeout when a live child shows no progress for the recorded bound, as handshake_failed with its exit status when it exits first"; docs/orchestration.md's capability table (:160-170, the row after `nativeBudget`) gains the `handshakeProgressBoundSec` row ("present: the handshake bound is measured from the last progress stamp; absent: from launch, the old field's meaning").

## 5. Tests and fixtures by name

Go: `TestBuildCodexCommand`, `TestBuildCodexCommandDisablesOperatorPlugins` (new); `TestAdjudicateTurnPureVerdicts` initial-stage rows and `TestCriticFailureFoldKeepsHandshakeExit` (new: the design-critic row folds to `protocol_error handshake`, the implementer row stays `handshake_failed:exit=137 handshake`; both with `CLIStatus: 137` and no `HandshakeDone`); `TestCriticFailureFoldClassPairs` (new, adjudicate_test.go, the pair form of D2.5 [CHS-R3-EXIT-01]: against a `{"role":"design-critic"}` record the pairs (`runtime_error`, `runtime`), (`runtime_error`, `handshake`), (`empty_reply`, `delivery`), (`handshake_failed:exit=137`, `handshake`) and (`handshake_missing_session_id`, `handshake`) each return `protocol_error`; (`protocol_error`, `handshake`) returns `protocol_error` (idempotent); (`handshake_timeout`, `handshake`), (`permissions_mismatch:network`, `handshake`), (`empty_reply`, `handshake`) and (`handshake_missing_session_id`, `delivery`) return their input; against a `{"role":"implementer"}` record every listed pair returns its input; a missing record path returns its input; and the wrapper `criticFailureFold` maps each of the four verdict strings of adjudicate.go:169-175 to the string it maps to today and passes `finish completed null completed`, `repair`, `settle` and `protocol-error` through untouched); `TestRunAdapterCriticFold` (new, cmd/metasystem/adapter_runtime_verbs_test.go: the verb prints `protocol_error` for a design-critic record with `--error empty_reply --phase delivery`, prints `empty_reply` for an implementer record with the same pair, prints `handshake_timeout` for a design-critic record with `--error handshake_timeout --phase handshake`, and exits 2 with empty stdout when `--phase` is omitted); the `WriteResultPatch` rows with and without `handshakeExitStatus`; `TestOwnershipPatchPersistsTheNativeExactIdentity` (deadline and `handshakeProgressAt` written together, `deadline - progressAt` equals the budget the test passes); `TestCustodyAddRefreshesHandshakeDeadline` (new, decisions_test.go: a pending `progress` record with a launch deadline gains a later deadline and `handshakeProgressAt`; a pending `launch` record and a running record with a session gain nothing); `TestRefreshHandshakeDeadlineNeverMovesEarlier`, `TestRefreshHandshakeDeadlineRefusesLaunchBoundAndSessions`, `TestHandshakeProgressWritesUnderTheSessionLock` (new, handshake_progress_test.go); `TestComputeReapFacts` gains one row: a refreshed future deadline keeps `HandshakeWaiting` true past `startedAt + budget`; select_test.go's two snapshots; fake_test.go's two fields; patience: `handshake_failed:exit=1` without usage is never-started.

Fixtures in scripts/agents/dispatch-fixtures.sh. Caps, all from scripts/agents/fixture-budget.sh `harness_fixture_cap` and scaled by the one measured factor `s` (1 through 48): `agent-status` (base 5, the file's `agent_status_cap_sec`, used by `wait_for_agent_status` and `wait_for_agent_chain_unlock`), `agent-command` (base 20, the file's `agent_fixture_cap_sec`, used by `wait_for_agent_fixture_process` and every `agent_fails` run), `adapter-handshake` (base 2, the fake snapshot's bound `B`, fake.sh:301). One new helper `agent_iso_epoch <iso>` converts a record's ISO second to an epoch integer, GNU `date -u -d "$1" +%s` first and BSD `date -u -j -f %Y-%m-%dT%H:%M:%SZ "$1" +%s` as the fallback, failing loudly if neither parses. [closes CHS-R1-FIXTURE-05]

- `no-signal` (existing, :1635-1647, `FAKE:no-session-signal`, `--wait`). fake.sh's `no-session-signal` branch: reads `handshakeProgressAt` (the P1 stamp) from the record, sleeps until `date +%s` is strictly greater than it (at most one second), registers a `util hold` child through `__register-custody` (P2), then models the custodian: polls the record's `handshakeDeadline` and on the first tick where `now >= deadline + 1` writes `handshake_timeout` through the result-patch CAS from pending, standing down on a lost compare and logging `fake custodian enforced the handshake deadline` when its write wins. Assertions after `agent_fails`, each an exact comparison of `json get` fields: status `failed`; error `handshake_timeout`; `handshakeBound == progress`; `handshakeDeadline == handshakeProgressAt + sessionEstablishedTimeoutSec` (the deadline is the refreshed one, integer arithmetic); `handshakeProgressAt > agent_iso_epoch(ownershipProof.provenAt)` (the refresh happened after launch, not at it); `agent_iso_epoch(endedAt) >= handshakeDeadline` (the failure was written no earlier than the refreshed deadline; `endedAt` is stamped by `RecordCAS` at the terminal write, record.go:541-542); and the job log contains `handshake-timeout recorded from pending` (dispatch.sh:2405, the waiter's write) and not the fake custodian's line (the waiter, one second earlier, won). Timing: at most 1 s of fake delay plus `2s` of bound plus one 50 ms poll, under the `agent-command` cap of `20s` for every `s`.
- `slow-session` (new, `FAKE:slow-session`): the fake registers a `util hold` child through `__register-custody` (P2), then waits until one second past the deadline it read from the record before registering, calling `__handshake-progress` every heartbeat meanwhile (P3), then handshakes and completes. Assert status completed, `handshakeProgressAt` present and `handshakeDeadline == handshakeProgressAt + sessionEstablishedTimeoutSec`, and the job log line "session landed after the launch deadline". Timing: `2s + 1` seconds under the `agent-command` cap of `20s`.
- `exit-before-session` (new, `FAKE:exit-before-session`, dispatched with `--role design-critic --job-id exit-before-session` through `agent_fails` so the critic shape is the one proven; `agent-command` cap, base `20s`). [CHS-R1-EXIT-02] [closes the proof half of CHS-R2-CRITIC-FOLD-01] Round 1: the fake's branch runs only when the record's `round` is 1 (fake.sh already reads `round`, :189; in round 2 the embedded prior brief still carries the marker and the fake must fall through to its happy path); it registers a `util hold` child through `__register-custody`, sends it KILL, waits (status 137), and writes the folded verdict the way its `handshake-failure` branch already writes verdicts (fake.sh:171-178, "through the one patch writer like every other failure path"): `adapter result-patch --output "$patch" --error protocol_error --phase handshake --usage "" --handshake-exit-status 137`, then `__record-cas --job "$job" --expect pending --status failed --patch "$patch"`, then `exit 1`. fake.sh does not source runtime-common.sh (no reference to it, to `complete_from_cli` or to `handshake_done` in the file), so the fold itself is proven by the Go row in adjudicate_test.go and the fixture proves the record shape, the outcome wording and the follow-up road; sourcing runtime-common.sh into the fake for one branch was rejected as a larger change than the proof needs. Assertions after `agent_fails` (exact `json get` comparisons on `jobs/exit-before-session.json`): status `failed`; `error == protocol_error` (the critic fold); `phase == handshake`; `handshakeExitStatus == 137`; the dispatcher output contains "exited with status 137"; the job log contains `child exited before the session signal with status 137`. Round 2: `run_agent_fixture exit-before-session-follow-up exit-before-session-r2 "$agent_dispatch" follow-up --job exit-before-session --message "$follow_message" --wait` (`agent-command` cap, base `20s`; `$follow_message` is the template copy the neighbouring follow-up fixtures already use, dispatch-fixtures.sh:2247). Assert on `jobs/exit-before-session-r2.json` (exact `json get` comparisons): `dispatchMode == follow-up` (the truthful record, D2.7), `resumeMode == fresh-context` (the field `build-follow-record` writes, build.go:628; the same assertion shape as the old-capabilities fixture, dispatch-fixtures.sh:2408), `resumedSessionId == ""` (the key present with the empty string, not null, claim.go:692), `parentSessionless == true`, `sessionKey == fake:exit-before-session-r2` (the child's own key), `fingerprintVersion == 2` (no bump), `round == 2`, `parentJob == exit-before-session`, `status == completed`; on `artifacts/agents/exit-before-session/rounds/2/prompt.md`: `# Prior Brief` present and `# Prior Return` absent; on the child's `composition.sources`: `"source":"engine:prior-brief"` present and `"source":"engine:prior-return"` absent; on the root record `jobs/exit-before-session.json`: `findingRegisterRound == 1` and `findingRegister` holds exactly one element, whose `findingId` starts with `synthetic-` and whose `critic == exit-before-session` (the field names are finding_register.go:18-19 and :147-152): the register's one synthetic finding for round 1. The round-2 leg is fully specified by D2.7 as of revision 4; the round-1 assertions do not depend on it.
- `hang-gone-dispatcher` (new): dispatch `FAKE:no-session-signal` without `--wait` in a background subshell the fixture owns, `dispatcher_pid=$!`; `wait_for_agent_status hang-gone-dispatcher pending` (`agent-status`); `wait_for_agent_chain_unlock hang-gone-dispatcher` (`agent-status`; the chain lock is released at dispatch.sh:1613 after `__launch` returns and before `await_handshake`, so the unlock proves the waiter is inside its loop); `kill -TERM "$dispatcher_pid"` and `wait_for_agent_fixture_process hang-gone-dispatcher hang-gone-dispatcher "$dispatcher_pid"` (`agent-command`; its non-zero return is expected and ignored with the if-form); `dispatch.sh reap --job hang-gone-dispatcher` immediately and assert status still `pending` (the reaper defers to the live custodian); `wait_for_agent_status hang-gone-dispatcher failed` (`agent-status`); assert `error == handshake_timeout` and the job log line `fake custodian enforced the handshake deadline` (the custodian's `deadline + 1` enforcement, not the reaper); a final `reap --job` leaves status and error unchanged. Timing from launch to the custodian's write: at most `1 + 2s + 1 + 0.1` seconds, under the `agent-status` cap of `5s` for every `s >= 1` (0.9 s of margin at `s = 1`, widening with `s`).
- `pending-process-loss` (existing) keeps covering the dead custodian → `process-lost` row; `handshake-failure` and `missing-session` keep their names.
- The Devin critic-shaped row [CHS-R3-EXIT-01]: its assertions are fixed in D2.5 (a design-critic record, `fail_pending handshake_missing_session_id handshake "" 0`, the written patch reads `error == protocol_error`, `phase == handshake`, `handshakeExitStatus == 0`; an implementer record on the same call reads `error == handshake_missing_session_id`, `handshakeExitStatus == 0`); its home is the open point of §7, because no fixture drives scripts/agents/adapters/devin.sh today and the one fixture that drives the real `fail_pending`, scripts/agents/adapter-deadline-fixtures.sh (ADPT-DL-001 to -005), is a deadline harness whose five legs stay green unchanged (its record carries no `role`). The existing `missing-session` fixture (dispatch-fixtures.sh:1656) drives the fake, not Devin, and is unchanged.

## 6. Out of scope

- claude.sh and devin.sh: their literals and pre-session loops are untouched beyond the one probe line D2.3 adds and, for devin.sh, the two `fail_pending` lines D2.5 names (:658 gains the status argument; :661 is unchanged in text and folded by the library). [CHS-R3-EXIT-01] What each inherits, precisely: P1 (the ownership patch), P2 (`CustodyAdd`) and the waiter, because those are the engine's and the dispatcher's; the critic fold, because it lives in `fail_pending` and every direct failure both adapters write goes through it (for claude.sh that folds the pre-fork `runtime_error handshake` at :134 and :138 for critics and nothing else; for devin.sh it folds :658 and :661 for critics and none of its setup, ACP, baseline or correlation classes); the `handshake_failed:exit=N` verdict and the `handshakeExitStatus` field through `complete_from_cli`, which claude.sh reaches at :178 and devin.sh at :677 and :690 with `$cli_status` in hand; and the field on a direct call only where the call passes a status, which is devin.sh:658 and no line of claude.sh. What neither inherits: P3 progress (`note_cli_progress` is called from codex.sh's loop only), the in-loop custodian enforcement of D2.6 (`check_record_deadlines` on every pre-session tick is codex.sh's), and any change to their bounds; those wait for their own specimens, as goal codex-handshake-budget's constraint says. Under `progress` their handshake deadline is stamped by P1 and refreshed by P2 only (under `launch`, by P1 alone, as D2.3 says), so a Claude or Devin start must signal within `B` seconds of its child's registration.
- The dead `proc alive --identity-file` calls at dispatch.sh:311 and :854-859 (§1): a separate backlog item; only the lost-CAS kill path and the fake's extra proof depend on them.
- Goal codex-handshake-budget (queued on m1b, marked duplicate) describes the same defect with a launch-anchored ceiling; the orchestrator dispositions it against this design.
- CPU-time progress, a rename of `sessionEstablishedTimeoutSec`, the codex selftest, and any change to `capMin` semantics.

## 7. Open questions

The one point revision 3 left here, the claim of a sessionless follow-up, was decided by the orchestrator on 2026-09-02 17:05Z and is folded into D2.7: candidate (a), the follow-up stays a follow-up and claims with the child's own key, taken without the fingerprint-version bump revision 3 assumed it needed, because the flag is a validation marker carried in the canonical JSON under `omitempty` and never hashed.

OPEN (revision 5, reported as a gap, not decided): the home of the Devin critic-shaped fixture row of D2.5 and §5. The round-3 brief asked for the row to be added to the fixture that pins Devin's two outcomes today; at 36bcda71 no fixture drives scripts/agents/adapters/devin.sh (search recorded in D2.5), so the row's assertions are fixed here and its placement is the orchestrator's. The facts that bound the choice: scripts/agents/adapter-deadline-fixtures.sh is the only fixture that sources the real runtime-common.sh and drives the real `fail_pending` under a stub dispatch that logs the CAS argv, so a row there proves the fold exactly as devin.sh:658 will call it without driving devin.sh; a Devin adapter fixture does not exist and would be new; the Go rows `TestCriticFailureFoldClassPairs` and `TestRunAdapterCriticFold` prove the table and the verb regardless of where the shell row lands.

Two facts the implementer must not rediscover: `util hold` has no exit-status flag (its flags are `--tag` and `--stopped-file`), which is why the exit fixture uses KILL for a deterministic 137; and `codex exec resume` accepts `-c` (its help lists it) but no `--sandbox` or `-C`, so the override is the only lawful carrier on that verb.
