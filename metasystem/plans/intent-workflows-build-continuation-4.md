Working Mode: implementation
Orchestrator Identity: root Codex, main-1790272787-5030-13dde3
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| complete-parent-and-planning | 9000 |

Continue your existing implementation; all product changes remain uncommitted.
Root accepted the follow-on planning design after TWO Fable rounds, material 3
to 2, explicit fixtures-as-arbiter and mandatory Sol review. Read these completely:
- metasystem/plans/designs/intent-planning-continuity.md (accepted)
- metasystem/plans/intent-planning-dispositions.md (all five findings joined)
- metasystem/plans/intent-planning-fable-review-r2.md (especially IP-C4/C5)
The original accepted parent and full build brief still bind. This is authorized
additional implementation of a concrete observed capability gap, not a fresh
unbounded redesign. Estimated total combined patch including tests is 9000 lines;
task-only size override uses the human's explicit machinery bypass. Do not invent
smaller estimates or change committed gates to fit.

Preserve your product work and user changes. No commit/pull/push, conf.local,
root plans/rulings/AGENTS edits or real services/providers. Root owns design and
orchestration records. Implement product/test/public docs. Use Go owners and
existing locks/state; shell only plumbing. No dependencies or workflow database.

Complete the new planning journey after the current parent's coherent checkpoint:
1. design G --brief FILE [--out FILE] [--after N], show design --goal G
   [--out FILE] [--attempt N], stop design G [--out FILE] [--attempt N].
   Existing launch design roster and admission, invoking checkout, staged output,
   exact reserved project header; no build claim or worktree from authoring.
   Freeze request BEFORE spawn, canonical document lock in existing launch store,
   old writer proof, rejoin failed/successful requests before inspecting new output.
   Preserve user edits; actual project publication owner compares bound bytes,
   validates identity/goal/draft and retains conflicting proposals. No silent merge.
2. Stranded Starting recovery is an ATOMIC conditional Store.Update. Check all
   absent refs + no uncertain output owner; late Supervise then refuses terminal
   BEFORE child. Do not call unlocked status then unconditional m.fail. Drive both
   lock winners, delayed supervisor command exit and existing uncertain custody.
3. Actual design review authority is NOT just reviewBriefFacts! Fresh and follow-up
   dispatch need claimed goal/stop capability and tier3. Author writes no claim;
   first paid critique checks eligibility and acquires an ordinary lawful claim
   when appropriate, using existing acquireClaim, no worktree. Preserve actor/epoch
   exceptions, foreign holder, quota, risk, budget, first-slice accounting.
4. Same bounded design critique, old bound decisions + new subject. Freeze request
   to OPERATION ID before launch; child ID is minted ONLY under existing follow-up
   owner lock and retained AFTER response. Never predict child or hold its lock
   across calling follow-up. Replay uses retained child, or an exposed owner read
   of findOperationRecord after lost response. Validate full actual provenance,
   never adopt an unrelated matching-looking job. Completion, changed goal revision
   and an intervening later round still rejoin old request without new spend.
5. review design FILE --retry N follows parent bounded retry, includes
   timeout/budget-cap. Parent shell's implementer-only cap branch currently
   precedes examination-retry and will shadow capped critics unless repaired.
   Same goal/canonical-design path cannot buy a fresh root while chain open,
   failed/capped/exhausted. Actual subject refresh, cap, live-process refusal and
   lost result/replayed retry require real owner fixture, not argv recording.
6. status/wait include design attempts via owner reads; ambiguity prints executable
   document choices. Claimed-but-dead supervisor -> existing stop owner.
7. Nine compact root starting points, without full flag grammar: goals, open,
   approve, build, review, land, status, ask, check. Complete focused/all views keep
   all capabilities, including design. Ordinary text speaks tasks, no read units,
   certified chains, close-check/mirror/store or internal family prerequisites.
8. wait proof REF and wait file PATH --until present|absent use existing observers;
   relative paths resolve against cwd. status plus test/landing outputs provide
   the real proof REF; fixture obtains it publicly. No invented second wait store.
9. Complete normal docs/skills migration to actual public grammar. Root will update
   AGENTS once verified. Keep maintainer protocols only explicitly diagnostic.

Do not lose still-unfinished parent work from return 3. In particular all 48 old
capabilities + 13 advanced outcomes remain in the capability map. Legacy job
closure must have a real review job --dispositions route before hiding close:
goal review alone does not cover it. The final combined return must reconcile
EVERY IW-1..7 and IP-1..4, not silently declare a partial checkpoint complete.
No material accepted finding becomes clean without actual remedy or owner-verified
human risk acceptance. No terminal status substitutes for dead-process proof.

Every named parent/follow-on fixture must exist, be registered, and prove the
stated behavior. Favor connected real owners with fake runtime/service boundaries.
Register Go tests in testing.json without lowering coverage or deleting tests.
Use focused checks first; do not rerun unchanged broad suites. Keep
METASYSTEM_TESTING_WORKERS=9 and inherited other workers. Root will select/run final
acceptance on the exact finished candidate, then independent Sol reviews full diff.
Provide a repeatable isolated CLI runtime recipe for author->draft->review->revise,
including synthetic config/fake providers and PUBLIC produced refs; no private real
configuration. Test output alone is not a claim of live end-to-end execution.

Maximum 150 NEW tool calls or 35 actual minutes, with a coherent checkpoint before
exhaustion. Report actual metrics or say unmeasured. Return to
metasystem/artifacts/agents/intent-workflows-build-return-4.md, with actual exits,
owner proof vs seams, precise remaining work and any concrete design failure.
Root will continue until complete; no certification from a checkpoint alone.
