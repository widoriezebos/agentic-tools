# go-production-grade — critique round 7 dispositions (human-authorized post-exhaustion round)

Critic: Codex (same thread), 2026-08-12. The round was preceded by the designer's revalidation of the plan against the consolidation refactor (~15 commits landed mid-loop). First attempt hung (zombie: running status, hour-stale log) and was cancelled; the relaunch completed in ~22 minutes. Verdict line: 6 material (R7-F1..F6), 3 non-material, NOT READY. All 9 dispositioned, 0 refuted; every material claim re-verified in this checkout, including the live ratchet bypass. Four of the six material findings are defects in code that landed during the loop — the tree, not the plan's earlier text, is now the main source of new findings.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| R7-F1 | accept | Right: conditioning the pre-publication chain sync on "did this call create directories" has a retry hole — a failed chain sync leaves the directories visible, so a retry sees them pre-existing, skips the chain, and can report `durable=true` on an unproven chain. | The rule is now unconditional: every invocation syncs the chain from the target directory up to the writer's named durable root, before the temp write; the fault matrix gains the two-call test (fail chain sync → retry must re-sync before publication). |
| R7-F2 | accept | Verified `audit/coverage.go:46-53` + `CheckCoverage`: the join is two-way over *measured* packages, but `ParseCoverage` keeps only lines with a numeric `coverage:` value and `go test` prints none for `[no test files]` — a new testless package is invisible (the critic's live run: unregistered testless package, exit 0). The plan's "an unregistered new package fails the gate" was overclaimed. | Recorded as part of B8; Phase 0c step 0: join against an independent `go list ./internal/...` inventory, end-to-end testless-package refusal test; the holds section now states the shipped join's true scope. |
| R7-F3 | accept | Verified: `registry.LockedAppend` documents "this owns durability" (`append.go:8-12`) while `AppendFrame` never syncs file or directory (grep: no `Sync` in the package); `receipt.appendLine` reports success after `WriteString`/`Close`, unsynced. A sync-call-derived inventory structurally cannot find these. | New defect B6. The Phase 4 inventory is redefined to every contractually durable state mutation; the two append families get their own design under the same outcome model (creation, partial writes, post-write sync, lock-holder concurrency, committed-with-doubt, fault tests). |
| R7-F4 | accept | Verified `audit/metasystem.go:185-211`: `Stat`/walk/read failures are skipped silently, and the test asserts fail-open on an unreadable file containing a forbidden reference — the suite's audit gate can pass while blind. | New defect B7, owned by Phase 4: in-scope policy files must be readable; walk/read failures refuse the audit; only explicitly optional absent roots stay skippable; the fail-open test becomes failure assertions. |
| R7-F5 | accept | Verified `dispatch.sh:75` (`shasum` — Perl tool, not in a minimal Linux userland), `supervision-hook.sh:80,95` (`pgrep` — procps), `mktemp`. "A POSIX userland" understated the contract, and the expected-compatible tier inherited the gap. | Supported-platforms amended: Phase 1 derives and publishes the complete production command inventory with the supplying Linux package per non-POSIX command, adds an arming/adoption preflight, and decides per command whether the Go binary should own the capability instead (it already hashes with crypto/sha256). |
| R7-F6 | accept | Verified `go-gate.sh:57`: `gofmt -l ... 2>/dev/null \|\| true` — a missing or crashing gofmt emits nothing and the "hard gate" passes. | Part of B8, Phase 0c step 0: capture gofmt's status separately, refuse non-zero, negative test via a failing shim. |
| R7-F7 | accept (record) | Non-material (agreed): two more sync-discarding writers exist (`supervise_arming.go` `writeIdentityJSON`, adapter snapshot `exclusiveWriteJSON`) than the plan's "six"; Phase 4's fresh audit forces the same result. | Count updated to "eight known at the round-7 sweep; the audit's result governs". |
| R7-F8 | accept (record) | Non-material (agreed), and a fair catch on the revalidation overclaiming "every anchor refreshed": the verdict synopsis still carried the 9-finding/672-map/no-ratchet numbers. | Synopsis corrected (12 findings, 699 maps, ratchet shipped with B8 caveat); B4/B5 anchors refreshed. |
| R7-F9 | accept (record) | Non-material (agreed): the enlarged tree adds unbounded exec sites (`validate/conformance.go:78,87`, `report/frontier.go:154`); B4's required peer audit dispositions them before implementation. | The two sites are named in this row so the B4 audit starts from them. |
