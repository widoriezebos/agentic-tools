Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal native-spend-cap-retirement)
Date: 2026-09-01

# Goal

Revision 2 of plans/spend-cap-retirement-design.md: fold all four material
findings of records/misc/spend-cap-critique-r1.md (in your worktree; the
critic's full return is durable at
artifacts/agents/design-critic-20a8f1bdf17c3df866738e14/rounds/1/return.json).
The backstop direction is affirmed; the folds sharpen its justification.

# Workspace

The delegate worktree. Revise exactly one file:
metasystem/plans/spend-cap-retirement-design.md.

# The mandate, by id

- SCR-R1-LEVEL-001: derive the level from ELAPSED-TIME evidence (real
  recorded round durations and burn rates, the wall-clock horizon the time
  enforcers permit) — not reservation allowances. State the derivation; if
  the honest answer differs from $50, change the number.
- SCR-R1-PROTECTION-002: define the native counter's accounting basis and
  overshoot honestly — it is a measured-cost threshold checked between
  calls, not a hard bound; size the worst overshoot and state what the
  backstop can and cannot own. Drop or reground the pricing-anomaly
  justification per the critic's evidence.
- SCR-R1-OWNER-003: fix the wall-clock inventory's owner assignments and
  name the backstop's REAL purpose: bounding the primary Claude process
  when the time enforcers are unavailable, with the accepted liveness
  residuals enumerated.
- SCR-R1-ENVELOPE-004: withdraw the categorical no-fan-out claim — a
  Bash-capable delegate can launch a fresh paid Claude session (including
  background mode) that inherits no cap and may escape the process group.
  State this as an OPEN unowned spend scenario with its honest exposure,
  name the candidate owners (permission preset, census/kill-guard reach,
  a follow-on goal), and record the critic's unproven-residual gaps
  verbatim rather than resolving them by assertion.

Self-grade updated; reject condition restated.

# Constraints

Wall-clock budget: 20 minutes. No changes beyond the four folds.

# Expected Return

Version-2 implementer JSON; diffBoundary exactly
metasystem/plans/spend-cap-retirement-design.md.

# Gap Rule

stop and report a gap; never fill it silently.
