# Fable: manual work through public review

New source-confirmed capability gap, round 1 of maximum 2, failsafe round 2.
Root authored plans/designs/intent-manual-review.md; read it in full after resolving
project design-of --goal verbs-match-intent through the live m1e launcher. User
requires repeated design/critique/implementation until full intent/capability
shielding and explicitly permits machinery bypass. Prior parent and planning
chains are CLOSED on fixtures; do not reopen them or review unfinished Opus code.

Criterion verbatim:
> Would an implementer working from this design build **step 1** DIFFERENT, or WRONG, because of this finding?
> Does step 1 WORK, and is it SAFE, without this finding?

Step 1 is diagnostic manual review; the second manual goal-delivery slice is
required for this user's full capability request. Challenge real contract/owner
errors in both, not optional scale or generality. In particular:
- Is there a smaller ACTUAL existing public route? Review commit currently requires
  Goal-Unit range; job requires implementer; goal requires UnitRunner. Bare diff
  exists internally and manual branch.CommitStaged needs no builder.
- Does the proposed read sequence extraction preserve actual partition admission,
  output/custody and compaction without another workflow framework?
- Are source capture, frozen context, replay and public references real enough to
  build without guessing? Diagnostic feedback must never become certification.
- Can the narrow CommitPatch share CommitStaged core and existing tokens/claim/
  range/publication without a fake UnitRunner result or another job registry?
- Is manual request replay/amendment and namespace selection small and honest,
  particularly source=destination and lost commit/push response? Name concrete
  required owner correction, not a speculative generic importer.

Maximum 30 tool calls / 12 minutes; <=1700-word report with reviewed design SHA,
MATERIAL count, stable IM-C1... findings, both tests' answers, source evidence,
smallest correction and unexamined scope. Read-only product investigation; no
edits except report, no agents/builds/tests/goals/commits/conf.local. Opus parent
work continues concurrently. Write plans/intent-manual-review-fable-report-r1.md
under metasystem. Return path/verdict. Root dispositions; you do not rewrite design.
