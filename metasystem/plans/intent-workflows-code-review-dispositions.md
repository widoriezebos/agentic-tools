# Intent workflows: implementation review dispositions

Subject: candidate 2f7fafcb5a97ce7bb4c8f53e4f92363f7cb3deff, tree
4af2104498b73f640474f4abadb7041af1ccb20e; base e4b86cfd8.
Independent critic: Claude Fable 5.1. The original first read and its completion
are one substantive review round. Three rounds maximum; stop at zero unresolved
material findings. Root read both reports and the owning selection/retry paths.
Runtime tests remain separate evidence, with their actual failures retained.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| ICR-1 | accepted | The manual path excludes retry, commit review drops it, and repair can prescribe an invalid command. A failed examination therefore cannot be recovered by public intent. | Existing review selection/commit adapter and recovery continuation; actual failed manual examination, custody, same-chain retry and replay fixture. Corrected in returns 16–17; final independent confirmation pending. |
| ICR-3 | accepted | Both manual and built items can await examination. Selecting the build silently violates the accepted ambiguity contract and can spend on the wrong subject. Two manual items also falsely report no result. | Combine eligible subjects at the existing selector; status counts both producers and offers executable named choices. Preserve preference for unfinished work over already published work. Corrected in returns 16–17; final independent confirmation pending. |
| ICR-6 | accepted | Actual built help omits the implemented settings validation capability. This violates complete discovery. | Check usage/example, settings cross-reference and existing discovery assertion. Corrected in returns 16–17; final independent confirmation pending. |
| ICR-2 | noted | No selection predicate accepts the zero tail; no independent behavior defect established. ICR-3 necessarily uses the returned filtered collection. | No separate cleanup. |
| ICR-4 | noted | Incomplete review is recoverable after ICR-1. Do not add another liveness registry. Truthful incomplete-review wording may accompany the required recovery correction if needed. | No separate state model. |
| ICR-5 | noted | Claim remains visible and releasable through public intent; no authority or first-use failure demonstrated. | Later, when it hurts. |
| ICR-7 | accepted | Root treats this as a contract finding: the human expressly requires the internal structure to be shielded. Normal help's whole-close-owner, lineage and box prose describes implementation rather than user decisions. | Plain-language replacements in existing help; preserve authority, review allowance, source-preservation and replay facts. Corrected in returns 16–17; final independent confirmation pending. |
| ICR-8 | noted | The behaviors have actual differently named fixtures. Do not add wrappers solely to satisfy stale prose names. | Corrected the workflow design and planning dispositions to point to the existing actual fixtures. |

Root's additional observations U1 and U2 are joined to ICR-6 and ICR-7; they
are not duplicate findings. Raw unit routes remain compatibility capabilities;
normal goal/work routes carry the task without requiring those types.

The reviewer completed all changed product/test source. Runtime logs and builder
narration are assessed by root, not treated as independent code certification.
Unchanged owner tests and serial-allowance reason text remain bounded review
limits, not unexamined critical implementation. No AGREE is claimed yet.

## Observed acceptance defects

The frozen candidate exceeded the unchanged always-loaded instruction budget
(1513 words against 1500). Root shortened only the existing delegation paragraph;
the existing focused budget test now passes. The first shortening still measured
1501 and was corrected; neither red result is concealed.

The original coverage run also found a missing fixture process-exit join,
executable writes outside the existing testexec lock, new wall-clock waits outside
the existing test policy, and stale refusal emission locations after source moved.
TestEnvironmentStandardInventory's failure is downstream of the same fixture
policy failures, not permission to remove tests. Correct owning fixtures and
source-location records; keep all assertions, workers, policies and floors.
Original native and coverage owners continue through independent groups on the
immutable candidate. A correction in the separate primary checkout cannot change
those results; no expensive acceptance rerun starts before their batch is complete.

## Correction evidence after the complete first read

Root read return 17 and the changed control flow. The manual fixture now actually
passes Retry and FollowUp into RunBranchRead, and exercises its recorded same-chain
retry, repeated request and live-examination refusal. Its follow-up transport is a
fake; the separate unchanged dispatch-b runtime provides the real follow-up boundary.
Canonical continuations discard stale flags and follow the public review command,
including when printed by repair. The mixed-producer fixture actually builds one
item and submits another, refuses ambiguity, selects each explicitly and chooses the
remaining item after completion. It also found and fixed an untracked result being
incorrectly treated as a clean worktree.

The exact fixture process identity is now joined after shutdown before retaining
the original no-survivor assertion. The actual headerless race test passed in
59.7 seconds. No production shutdown semantics or test allowance was weakened.

Root found one further explicit completion-contract gap: an authentic manual
examination with no findings still asks for an empty decisions file. This differs
from built work and from the design's promise that clean review completes itself.
It is assigned to the original builder through the existing empty-join/full-close
boundary, with a real manual close/collection/publication/replay fixture. This is
part of the accepted completion correction, not a new workflow owner.

The original immutable acceptance runs are terminal: native selection had 139
passes, five failures and 21 cancelled/unavailable/not-run groups when its global
45-minute context ended. Full race/coverage observed all 12,672 expected tests with
no missing or unexpected observation, but seven top-level failures prevent success.
Thirty of 35 independent shell sections passed; five failed. Three sections remain
unrun because their actual fast-build prerequisite has not yet passed. Every failure
is preserved, and no old red run is relabelled as a green final result.

Coverage repair adds meaningful failure and lifecycle tests in five packages; all
29 new tests pass with race and Git absent. Fresh focused package coverage is above
each unchanged floor. Root reviewed all six new files, replaced one permission-based
failure fixture with a user-independent path failure, and imported the exact reviewed
patch. This is focused evidence; the full final native coverage judge remains due.

The two additional shell mismatches are understood: runtime-contract-audits still
expects the superseded Codex/Astra roster, while the budget-extension fixture starts
a second design chain without completing the first. The existing assertions will
be aligned with the human's approved roster and the existing same-chain contract;
budget and design guards remain intact. The actor-source inventory adds precisely
the independently reviewed record-writer classification call, keeping its scanner.


Root read return 18 and its complete correction. The manual zero-findings fixture
first reproduced the empty-file chore, then passed actual close, collection and
publication with repeat safety and a malformed-return negative control. Fourteen
focused command tests passed in 174.2 seconds. The new helper only creates the
existing empty join for a completed readable return with no findings; the full
close owner still decides whether closure is lawful.

Correction to root's fixture search: TestIntentReviewRetryAuthority does exist in
internal/goal/branch/read_retry_test.go (the earlier search was limited to command
files). It proves replay even after the retry failed; its focused run passes.
Round-limit evidence is composed: the manual adapter reaches the actual branch
retry, whose production FollowUp uses dispatch.sh, where register advancement and
CritiqueExhaustionAdvance precede child reservation. The existing capped-round fold
counts the round through criticRoundsConsumed and the unchanged exhaustion fixtures
refuse further rounds. No dedicated manual round-exhaustion journey is claimed.
Fable must judge this explicit boundary, not a nonexistent fixture-name gap.

The budget fixture correction's first three runs did not complete. Closing and
revising a design only exposed additional lawful design admission checks before
budget accounting. Root stopped that mechanism and selected the existing verifier
read role for the budget-only fixture, retaining every budget assertion and the
separate design-chain tests. The template roster block itself passes against the
new defaults; whole-section confirmation remains due.


The final budget-only verifier fixture now passes dispatch-a in 526 seconds, own
exit 0. It preserves the original budget/extension/no-job/no-follow-up assertions.
Only the isolated fixture copy receives the existing none permission preset, so
its read retains the original shared-checkout assertion; production configuration
and all design admission guards stay intact. The earlier failed close/revise
mechanism and the verifier's initial wrong permissions input remain recorded.
The exact two-script patch is bb6e809dc2b51e89d35020d50e48fc3724db0e03fcc47720c8c73bb9f464de24.
All writers are now idle for final freeze and independent confirmation.


## Independent confirmation round 2

Fable read the complete correction diff against R3, verified the whole-patch scope
and identities, and confirmed ICR-1/3/6/7 and the new fixtures. Two material
continuation defects remain. The reviewer explicitly judged the composed retry-cap
proof and guarded all-package coverage substitution sufficient for first use.
Root read the full report and JSON, retaining its stated review limits.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| ICR-9 | accepted | A manual retry's continuation preserves --retry, so the owner keeps joining the retained request even after the examination finishes. The next command must observe/complete the selected review. | Reuse canonicalReviewArgv for in-progress committed review; complete the retried round and follow the printed command to actual close/collection/publication. Pending original builder correction. |
| ICR-10 | accepted | closeWorkReview missed the existing reserved-goal qualifier. Root independently reproduced the wrong command for goal design in the real-close fixture, own exit 1. | Existing reviewGoalWords now constructs its continuation. The same real-close fixture and all reserved-goal cases pass, own exit 0. No new grammar or owner. |
| ICR-11 | accepted | As with ICR-7, ordinary help describing storage/dispatch owners violates the explicit human contract even when it does not block execution. | Root replaced only the named help strings with user decisions and task outcomes; schemas and authority are unchanged. Final discovery/confirmation due. |
| ICR-12 | noted | Both an unread item and an unpublished read require completion; asking the user to select among two actionable items is safe and supplies exact commands. | Keep the explicit choice in this release; consider preferential ordering only if it causes actual friction. |
| ICR-13 | noted | A completed record later containing corrupt evidence is outside an ordinary producer failure; retry admission preserves the completed-round invariant. No automatic overwrite or new repair authority is justified. | Later, when it hurts: distinguish damaged completed evidence in the diagnostic and design a deliberate recovery if needed. No corrupt record is admitted as success. |

The first R3 fast-static-build exposed five staticcheck findings: one unused helper,
one unused field, two dead test assignments and an actual shadowed error in carried
adoption that could ignore retained-subject or binding failure. These are assigned
together to the original builder, including meaningful failure-path coverage. The
remaining independent native groups and already-failed sections continue on untouched
R3 to collect the batch; no red static result is reclassified as successful.


Root read all return19 and the actual binding code. The carried-adoption correction
now checks bindErr from both retention and binding before staging. The new real
negative fixture reproduced the shadowed-code defect and passes plain and race;
root requested a deterministic filesystem obstruction in place of chmod because
permission bits do not fail for every test identity. Staticcheck now passes after
the four dead values were removed. No authority or failure assertion is weakened.

All five previously failed shell sections now pass on immutable R3, including
the full dispatcher and runtime-contract sections. The independent native run
found one stale recovery-command expectation: TestIntentReviewCommitClosesThenPublishes
still expected the initial model override in the author-decision continuation.
The canonical continuation intentionally drops that transient flag. Keep its
initial model-forwarding, whole-close, lost-publication and replay assertions while
correcting this exact expected command. Main20 combines that correction with ICR-9
and the portable binding-negative fixture; no additional broad run is delegated.

Root's final built help read found one further instance of ICR-11: `status unit U`
was still listed in ordinary work/status help although raw units are compatibility
references. Its handler is preserved; ordinary discovery uses goal/work and job
references. The existing coverage expectation and discovery check now establish
that distinction. Root also removed mission-runner, poll-lock, launch/unit and
old `up` alias narration from those same normal help strings and clarified the
root build/check summaries. No grammar, return schema or execution changed.

A second R3 native failure was a duplicated old refusal line in
TestUnitRunRefusesMainBranch: the actual source and audited register both name
unit_run.go:258, but this test still expected255. Root corrected only that
expected source anchor; the exact refusal, owner, shape and no-main-build guard
remain tested. This is not a change to refusal policy.


Main20 now proves a capped manual retry through the printed continuation, actual
round-two register advance, real close, collection, publication and repeat. Its
focused suite passes. The permission-independent carried fixture also reproduces
red with the old shadowed code and passes after the fix. Root has read the full
return and source, and accepts the composed reserved-goal proof boundary.

Its attempt to exercise a protocol-failed first round exposed one remaining public
recovery gap: the existing close guard correctly retains the missing-evidence
finding, but tells the caller to use internal goal/job commands. Root retains that
guard and assigns only a public adapter remedy through existing typed finding reads
and accept-risk. No successful retry silently accepts the earlier missing evidence.
This extends the current evidence-backed C3 correction, before Fable's third/final
confirmation. The capped positive journey stays; the protocol-failed case must
prove the truthful public human decision without fabricating authority or closure.
Root also simplified normal review progress/publication text; result data is kept.

Latest main e5925c951 has been merged, and the complete pre-merge changes restored
without conflict. The safety stash is retained. All baseline testing group/surface
identities survive the merge; the actual final policy/audit checks remain due.


Root followed the printed protocol-failure risk command, rather than accepting an
arbitrary refusal. It exposed two real boundaries: the review belongs to its goal
checkout, and synthetic missing-evidence findings have no ordinary critic prose.
The remedy now selects that checkout explicitly. The existing typed decision reader
recognizes only the canonical protocol-failure and unbound-return identifiers,
and describes the recorded missing evidence with its critic identity and digest.
It preserves unproven status, goal binding and explicit human acceptance; it never
manufactures a successful examination. The ordinary accepted-risk validator is
unchanged. The public test now reaches the agent authority guard, then completes
the same command with declared human-fixture authority and the real decision,
risk record, register and proof writers. The fixture's existing goal repository
adapter is explicit; no full external-close-after-human-decision journey is claimed.
The complete human path and both synthetic reader cases pass, as do refusal and
test-policy audits. Earlier missing-title/evidence and wrong-repository failures
remain in the external evidence. Main21's return is fully read and all builders
are idle. ICR-9's actual completion correction is accepted.

Main 9e8f98108 is merged without conflict, preserving its sitting/decisions UI
changes and expanded interface test inventory. The final immutable subject uses
that main base. Its incoming-main patch is separate from the verb correction patch.


## Independent confirmation round 3 and final wording correction

Fable verified the complete R4 patch identity and read all 27 correction files.
It confirmed ICR-9/10, the completed protocol-failure human-risk path, the portable
carried-adoption refusal and the unchanged evidence/authority boundaries. It found
no new behavioral defect. Its final report is intent-workflows-fable-code-review-r3.md.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| ICR-14 | accepted | Four ordinary descriptor details still mention internal owners or close/collection mechanics. This is the same explicit human language contract as ICR-11; the critic confirms the behavior works and is safe. | Root replaces only goals, resume, split and repair detail strings with their user-visible effects. Rebuild and actual rendered help plus existing public discovery/coverage/Partner fixtures verify the correction. |
| ICR-12 | noted | Unchanged safe explicit choice, with exact commands for both pending items. | No required change. |
| ICR-13 | noted | Unchanged completed-record corruption boundary; no false success or overwrite is admitted. | Existing later list. |

The third critique is terminal, with its original fix-first verdict retained.
There is no fourth substantive critique or claim that Fable reviewed later bytes.
Root owns this literal-only correction and its verification. The runtime selection
continues on immutable R4. Its source, authority, control flow, tests and internal
coverage remain unchanged by the four help descriptions; focused rebuilt discovery
is the additional consumer proof. Final certification still requires the actual
selected results and unchanged coverage-floor judge, then root's joined assessment.
