# go-production-grade — critique round 8 dispositions (human-authorized confirmation round)

Critic: Codex (same thread), 2026-08-12. Scope was strictly the six round-7 amendments. Verdict line: 2 material (R8-F1, R8-F2), NOT READY. Both dispositioned, 0 refuted; both are mechanism defects inside the durable-writes model — the seventh consecutive round whose material findings land in that one paragraph.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| R8-F1 | accept | Right: "up to (exclusive) the durable root" never syncs the root itself, so the first created child's directory entry is unproven — and when the target *is* `~/.metasystem`, the exclusive range is empty and nothing syncs. Persisting an entry requires syncing the directory that contains it. | The chain sync is now inclusive of a guaranteed-pre-existing durable anchor (repository checkout; `$HOME` for `~/.metasystem`, which may itself be created); the two-call fault test gains a freshly-created-tree variant. |
| R8-F2 | accept | Right: the two-outcome model cannot describe a failed append that leaves a torn frame visible — neither "no new bytes" nor "committed". The transport/uniformity bullets also still said "six writers" after B6 widened the inventory. | A decided third outcome for the append families only: visible-but-uncommitted fragment — plain error, no event; reader-side detection (registry framing's `CorruptionError`, receipt line integrity) with truncate/skip repair under the lock; durability claimed only after post-write fsync; fault tests assert error return and reader recovery. Transport and uniformity bullets now speak of the full inventory. The plan also draws the boundary explicitly: outcomes, invariants, and transports are decided here; remaining append mechanics belong to Phase 4's gated design. |

## Loop status after round 8

Totals across eight rounds: 50 material + 11 non-material raised; 61/61 dispositioned; 0 refuted. Trajectory 13 → 16 → 7 → 1 → 2 → 1 → 6 → 2, where round 7's spike came from mid-loop tree changes, not plan regressions. Since round 4, every material finding has been inside the progressively more precise durable-writes model — the loop is no longer reviewing the plan, it is co-authoring one paragraph of Phase 4's design at critique prices, and that design is already gated (obligation gate plus its own review) before implementation. The designer's recommendation to the human: close here; the durable-writes design gets attacked again at its own gate regardless.
