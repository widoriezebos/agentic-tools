# Dispositions, round 1: reservation settlement design critique

Design: plans/dispatch-cap-settlement-design.md (revision 1, job
cap-settle-design). Critic chain: cap-settle-crit (gpt-5.6-sol, xhigh,
DESIGN-BEARING). Adjudicated by the dispatch delegate
m1b+main-1788333346-60696-6a3256. Materiality criterion as in the
critique brief. Round budget three; failsafe round 3.

## Round 1 — 5 material findings, verdictMaterialCount=5

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| DCS-R1-TIMESTAMP-AUTHORITY | accepted | Verified: `endedAt` is absent from immutableFields (record.go:60-75); RecordCAS refuses only immutable and prohibited fields on an open record and stamps `endedAt` only when it is still empty at terminalization (record.go:522-545), so a lawful open-record patch carrying `endedAt` would be preserved and settle the job early or late. A settlement computed from a value any open-record writer may set is not transition-owned. | Revision 2: `endedAt` becomes transition-owned — RecordCAS refuses any patch that carries `endedAt` (the same refusal shape as an immutable field), and the terminal transition alone stamps it; the settlement reads that stamp. A test proves the refusal and that the stamp survives. No shipped writer patches `endedAt` on an open record (RAN: grep over internal and dispatch.sh — build.go initialises it null, claim.go copies it into an outcome view). |
| DCS-R1-START-PROOF | accepted | Verified: dispatch.sh spawns the detached runtime (:812-820), publishes ownership (:834-865), then opens the start gate (:867). The record carries `pid`, `pidStartedAt` (the kernel start of the runtime, epoch seconds) and `ownershipProof.provenAt`, all written by the trusted launcher's ownership patch (live record cap-settle-design shows the shape). ACCEPTED: the start instant is `pidStartedAt` when present — the runtime's real start, not the record's assembly time — so observed minutes measure the process. REFUTED as to a new durable field atomic with the start gate: the only exposure is a dispatcher that dies between spawn and ownership write, which leaves a record with no process identity and charges 0 for a process that ran seconds; that is bounded, never a refusal of lawful work, and the alternative (charging the cap) is the bug itself. Recorded as a residual with a token (memory/known-issues.md) rather than mechanism. | Revision 2: observed minutes run from `pidStartedAt` to `endedAt`; `startedAt` is the fallback only when `pidStartedAt` is absent on a record that carries `pid` (fail closed if neither parses). The residual is named in section 8 with its bound. |
| DCS-R1-REFUSAL-COVERAGE | accepted | Verified: admission.go:174-198 builds one breach object per limit and :201-220 renders only those; governed.go:132-140 returns the generic string. The goal's DONE says every budget refusal names the two numbers; the design only decorated the reserved-minutes breach. | Revision 2: the admission renderer prints one reserved line — `reserved observed=<n> open-caps=<m> limit=<L>` — on EVERY refusal whatever limit tripped, and governed.go's refusal carries the same line; the per-limit breach objects are unchanged. T10 extends to an attempt-only refusal. |
| DCS-R1-DISCHARGED-HUSK | accepted | Verified: budget.go:353-361 parses `startedAt` for the post-discharge filter before the charge rule, and a pending-setup husk (build.go:148-164) has only `createdAt`; a failed or cancelled husk after a consumed discharge is unknownBudget today and would stay so under the design's insertion point. | Revision 2: the post-discharge filter uses `startedAt` when present, else `createdAt`; a record with neither is unknownBudget; the husk then reaches the charge rule and charges 0. T6 gains a discharge-branch subtest. |
| DCS-R1-GOVERNED-COMPONENT-PROOF | accepted | Verified against the design's own text: section 3 assigns governed terminal and live amounts to the two components; T8 covers delegated records only, so an implementation that leaves the governed components out passes every named test while the parts are false whenever governed work exists. | Revision 2: T8 gains a governed subtest (one terminal governed attempt with ObservedCostMinutes, one live governed run with ExecutionCostMinutes) asserting both components and the sum; the governed_budget_coverage test asserts the components too. |

Round 1 closes on five accepted findings (one with its refuted
half recorded); revision 2 goes back to the same critic chain as round 2.
