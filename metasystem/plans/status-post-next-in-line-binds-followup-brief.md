Working Mode: build
Orchestrator Identity: m2+main-1788441779-14484-82d6ed (dispatch delegate under goal status-post-next-in-line-binds)
Date: 2026-09-04

# Follow-up: both gaps decided

1. Yes: `cmd/metasystem/channel_verbs.go` is in bounds; extend the status post's saved state there with the goal id its decision line names, so the poll can match a reply in that thread.
2. Add one authority class to the goal package's approval law: a **verified channel answer**, proven by the channel's own code check (the human user id the channel is configured with, the message reference, the code step, and the question or status-thread id it answered). The approval path accepts it the way it accepts an enrolled-terminal word: no review date, no "further approve needs freshly observed enrolled-terminal authority" refusal (that guard is for relayed console words), recorded in the goal's history with those four facts as the word. Keep the class narrow: only the poll's disposition may mint it, only after the code verified at send time, and only for the goal named by the token. Document the class in one line next to the other proof forms in `docs/backlog-mechanism.md`. A sibling item (channel-budget-answer-binds-nothing) will reuse this class for budget raises; do not implement that here.

Everything else in the build brief stands. Every path in your return is relative to the repository root (starting with `metasystem/`).
