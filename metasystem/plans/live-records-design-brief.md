Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal digest-landing-race)
Date: 2026-09-02

# Goal

Author the design document live-records-landing-design.md, a NEW file you
create in the metasystem plans directory: the generic seam by which
machinery-written tracked files coexist with the clean-tree landing gate.
The goal record (metasystem/plans/goals/digest-landing-race.md, in your
worktree) is the mandate; its three candidate shapes are the starting
menu — choose one or design better, and justify against the others.

# Evidence

- The narrator digest is the first specimen: the steward runner appends
  continuously; scripts/agents/land.sh demands a clean tree after staging;
  m2 recorded three refusals in one session; the m0b seat resolved roughly
  fifteen digest rebase collisions by manual union in the last day (its
  landing messages of 2026-09-01/02 are the trail).
- The adopted union rule already exists as retro row IL-34
  (memory/instruction-ledger.md): append-only fleet logs merge by union
  mechanically. Your design gives that row its mechanism.
- Constraints from the goal record, binding: the no-softening byte-equality
  law on counselor appends survives — no dropped or reordered bytes on any
  live record; a new role adopts the convention without touching land.sh
  again; the first migration proves it on the narrator digest.

# Design questions to settle mechanically

1. The shape: (a) untracked-live with landed snapshots, (b) a declared
   live-record registry land.sh re-stages atomically inside the gate, or
   (c) writers honoring the landing lease — or better. Decide with the
   crash and concurrency interleavings stated (writer appends during the
   re-stage; two landings race; a landing dies between re-stage and push).
2. Union semantics: where does the mechanical union live (git merge
   driver, land.sh step, engine verb), what proves byte-preservation, and
   what happens on a NON-append divergence (refuse loudly, never guess).
3. The registry or convention surface: how a new self-writing role
   declares itself; what refuses an undeclared live path.
4. Migration: the narrator digest as first specimen, the steward runner's
   writer untouched or minimally changed, the fixture set.
5. Blast: every current consumer of the digest file and of land.sh's
   clean-tree guard, enumerated.

Self-grade with reject condition.

# Constraints

Wall-clock budget: 30 minutes. Design document only; one new file in the
metasystem plans directory.

# Expected Return

Version-2 implementer JSON; diffBoundary lists exactly the one new design
file you created.

# Gap Rule

stop and report a gap; never fill it silently.
