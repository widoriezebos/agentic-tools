Working Mode: implementation
Orchestrator Identity: root Codex, main-1790272787-5030-13dde3
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| complete-manual-submission-and-public-surface | 22000 |

Resume original Opus session. Root read return7 in full: actual pass162 new
tools,30m10s; IM3/4 and real planning continuation still missing. No broad suites
now. User authorizes full redesign, machinery bypass and ignoring stop hooks.
Root merges latest main before this start. Preserve new Partner/UI changes.

Your priority is FULL MANUAL SUBMISSION, not another pass on diagnostic polish.
Implement accepted plans/designs/intent-manual-review.md and manual build brief,
including latest appended capture correction. Deliver working IM3/4: public
review G --changes|--patch PATCH --brief FILE [--work NAME] [--after COMMIT], actual
claim/worktree, consistent staging, unchanged CommitStaged, push, independent
committed review, public whole closure/publication and land. Required named tests
must prove source=destination, distinct destination, replay/amend/adopt/lost response,
other work preservation and real checkout lock. Use canonical branch range for
manual work; never invent UnitRun records or a manual registry.

IMPORTANT root traced a real first-use flaw: generated receipt/narrator/goal files
are ClassExcluded by branch.PathClass, but CommitStaged REJECTS them if staged as
Unit. Implicit --changes must omit existing canonical ClassExcluded state and the
exact named in-repo brief, preserving their bytes/staging. Other non-unit classes
retain existing refusal. Explicit --patch must not be silently filtered. Source
equals destination must preserve normal UNSTAGED coordination appends, and must
never discard unrelated pre-existing staging. Keep CommitStaged guards unchanged.
Diagnostic reviews otherwise capture all changes including coordination records.
Backend owner is adding exact brief exclusion using literal Git selection/private
index; don't parse diff strings to remove paths or maintain another exclusion list.

Exclusive parallel boundaries:
- Read owner owns ONLY internal/launch/{unit_run.go,read_sequence.go,
  standalone_read.go,standalone_read_test.go}. Correction4 active; root imports its
  incremental patch and updates artifacts/agents/manual-read-owner-integrated.json.
- Carried builder owns cmd/intent_exception.go, cmd/intent_carried*_test.go,
  ONLY carried test/helpers in intent_authority_positive_test.go, plus
  internal/landing/carried_prepare*.go. Do not edit those. Its side checkout is
  ../agentic-tools-intent-carried-20260926. It will deliver actual carried land.sh.
- A separate Opus planning completion owns cmd/intent_design*.go,
  cmd/intent_planning*.go, internal/launch/design_request*.go,
  internal/dispatch/design_chain.go and scripts/agents/dispatch{,-fixtures}.sh.
  Do not edit these until root imports its return. It owns real dispatch follow-up,
  author prompt output-path fix, clean design close and author lifecycle proof.
- You own shared CLI wiring/selection/delivery/help/docs/testing.json, manual
  owner/files/tests. Planning returns any necessary shared seam patch for you.
  Root owns plans/AGENTS/receipts/rulings; no edits there, commits/pulls/pushes.

After manual journey exists: finish public diagnostic stop/retry proof using real
Manager custody and shared read owner. Integrate planning/carried public seam and
test registration once root marker arrives. Hide close in discovery ONLY after
public design closure really passes. Remove normal help's design schema headers
and close/collect chores. Public continuations for every reserved goal name must
remain collision-safe; all source paths resolve from caller location. A public
producer for proof REF feeding wait proof is still owed; retain full capability
inventory. No misleading author/critic process callbacks as complete CLI proof.

Root owns serial-policy cleanup of authority-positive tests after carried import;
do not invent exemptions or edit those test files. Keep full floors and assertions.
Focused tests first, nine workers, preserve inherited allowances. Record actual
commands/exits and owner-vs-seam. No broad green reruns while product is incomplete.
Budget200 NEW tools or40 actual minutes, no smaller old brief tool checkpoint.
Output artifacts/agents/intent-workflows-build-return-8.md with complete residual
gaps and executable disposable CLI recipe. Never claim complete from partial rows.
