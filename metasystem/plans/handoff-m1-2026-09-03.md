# Handoff m1, 2026-09-03 (written 14:50 local for a takeover by another machine)

Everything m1 knows, in the order a taker needs it. Supersedes plans/handoff-m1-2026-09-02.md.

## The direction (Wido's words today, binding)

Rulings R-66-m1, R-67-m1 and R-68-m1 in memory/rulings.md carry it verbatim. The target state is
the arc `headless-fleet`: nodes are headless metasystem processes that turn approved backlog into
candidates and releases and send exactly two kinds of message, an unblocking question and a
boundary-broken raise; one brain seat (goal fleet-coordinator-brain, queued) coordinates the nodes
with Wido, owns every backlog manipulation, and may act under an explicit, scoped, time-boxed and
revocable power of attorney recorded as a ruling row; Wido alone approves execution. The first
headless run (goal first-headless-run, approved for execution by R-66-m1) runs on m0 the moment
human-approval-for-execution lands.

## Landed by m1 today

- The path-class manifest, first part (4b1cd47e): scripts/agents/path-classes.txt with four
  classes, the verb `metasystem path class [--explain]`, internal/pathclass, the landing floor,
  the critique waiver and the instruction-owner check reading it, the two hand lists deleted.
- The tiering machinery's re-aimed design (plans/severity-tiered-rigor-design.md revision 2) and
  its design-review brief; the goal's own review has not run.
- Rulings R-64-m1 (the manifest's fail-closed codes) and R-66/67/68-m1 (the direction).
- Goals opened: adoption-inventory-from-install-set, merge-stage-critic-close, first-headless-run,
  fleet-coordinator-brain. Arc headless-fleet holds seven goals; the four claimed ones join when
  their holders release.

## In flight: the manifest's second part, ready to land

Chain path-class-build2, terminal round path-class-build2-r5, reviewed tree
852f7250a87ee190465a0a367bea7f9d2a7fd8aa. One code review has run
(path-class-build2-cc1): one material finding, PCM-CC8-001 (the evaluator could never answer
`outside`, so in the root layout every application file would refuse as unclassified), folded by
round 5 per plans/path-class-manifest-build2-fix4-brief.md. What remains: the closing review with
plans/path-class-manifest-build2-code-critique2-brief.md against round 5, then the landing.

The exact round-5 tree is pushed to origin as the branch **preserve/path-class-build2-r5**, whose
`metasystem` subtree hashes to the reviewed tree above; the chain branches agent/path-class-build1c
and agent/path-class-build2 are pushed too. That is how the work survives this machine: artifacts/
is runtime class and never leaves the checkout, and a patch file cannot be committed because the
landing gate refuses its trailing whitespace. A taker recovers the change with
`git diff <base>..preserve/path-class-build2-r5 -- metasystem/` or by checking that branch out.
The chain's job records do NOT travel: a taker either re-runs one code review against a fresh
chain built from that tree, or lands it as a reviewed chain on m1, where the records live.

### Landing recipe (proven on 4b1cd47e)

1. `bin/metasystem job critique-register-advance --repo . --root-job <critic> --round-job <critic>`
2. `scripts/agents/dispatch.sh close --job path-class-build2`
3. Save records/narrator-digest.log aside, `git checkout --` it, pull, re-append its lines after.
4. `git apply --index --directory=metasystem artifacts/agents/path-class-build2/rounds/5/diff.patch`
   (on another machine: apply the diff of preserve/path-class-build2-r5 against its base instead)
5. Check `git rev-parse $(git write-tree):metasystem` equals the reviewed tree above.
6. `scripts/agents/land.sh -m <message file> --chain path-class-build2 --staged-only --skip-transport`
7. Re-append the digest lines; `scripts/agents/go-build.sh`.

Never run `validate conformance --stage merge`: it refuses every real chain, because a
DESIGN-BEARING critic root can never close (goal merge-stage-critic-close).

### The landing message for the second part

Subject line: `Landing rules by class`. Body: the evaluator decides by the class of every changed
path (behavior only through a reviewed chain; record under register carriage with the record
rules; ledger only through the goal verbs; runtime never; unclassified refuses naming the base
manifest); exact revert of a record refuses with its own code; record ownership binds the base
goal file's holder to the landing actor; the nine fail-closed codes of design section 6 are
promoted under R-64-m1 while direct-fix-floor-refused stays observed; land.sh carries the goal to
commit.sh, which validates Goal-Item against the ledger. Trailers: `Goal-Item: path-class-manifest.
Landed by m1.` and the co-author lines.

### Gate evidence

artifacts/agents/path-class-build2/orchestrator-gate-replay.txt holds the host replay of round 4
(five Go packages green under -race, internal/landing coverage 73.7%, three fixture scripts
green). Round 5 changed only internal/landing/observe.go and its test; replay before landing.

## Next after that

1. The second landing bar: briefs plans/path-class-manifest-bar-two-brief.md and
   -critique-brief.md are landed. Before dispatching, mint the R-65 ruling row and re-check that
   the day's carriage landings pass the new floor (today they read would-refuse
   register-carriage-path-refused under the first part's floor; the second part's record rules
   are what make them pass).
2. The tiering machinery: dispatch the design review with
   plans/severity-tiered-rigor-critique-brief.md once the manifest goal releases m1's claim, then
   build part one (the tier on the goal, the budget boxes, the ladder refusals).
3. Open with Wido: the reserved minutes per tier box while the dispatch reservation bug stands
   (recommendation: attempts times the dispatch cap until dispatch-cap-necessity lands).

## Fleet

- m0: human-approval-for-execution, in its closing review and a norm fix. It has orders to claim
  first-headless-run the moment that feature lands.
- m0b: the fleet conversation channel's first slice landed (66dfaf77). It needs three things only
  Wido can give, and nothing goes live until then: a Slack bot token and channel id, his Slack
  member id, and a base32 TOTP secret, all in metasystem.conf.local on each asking machine.
- m1b: the token spend fence, design closed, build running.
- Relay: remote-control messages strand unsent on VM prompts; send with C-c, a tmux buffer paste,
  then Enter. Sessions idle after a delegate round completes; nudge them and record the specimen
  on goal never-idle-ironclad.

## Takeover state, written by the m2 seat (this Mac) at 15:40 local, 2026-09-03

- Owner now: the Claude session on m2 (git nickname m2, lineage main-1788441779-14484-82d6ed).
  The m1 records of chain path-class-build2 are not on this machine; the change is
  recovered from origin branch preserve/path-class-build2-r5 (metasystem subtree
  852f7250, the reviewed tree). Its patch applies cleanly on origin/main 97b4ef9b.
- Replayed here on that tree: five Go packages green under -race (internal/landing
  76.3% coverage), the land fixtures green (4 legs). Two fixture scripts are RED:
  1. scripts/agents/path-class-fixtures.sh needs ripgrep (`rg`), which this Mac does
     not have and the command inventory in docs/project-rules.md does not declare.
     The same check run with grep finds no reader of the deleted tables. Already on
     main (the first part); tier-1 backlog item opened: path-class-fixture-ripgrep.
  2. scripts/agents/static-reproof-fixtures.sh, the leg added in this chain that
     expects a register-carriage landing of an unlisted root file (README) to refuse
     with path-unclassified: on the round-5 tree the commit LANDS. The round-5 fold of
     PCM-CC8-001 makes an application-owned path in the root layout answer `outside`,
     and the outside row passes register carriage. Round 5 was never replayed against
     the fixtures on m1. This is a material finding for the closing review: the leg
     and the evaluator disagree; either the leg picks a path inside the installation
     that has no class, or the design's root-layout answer is wrong.
- Waiting on Wido (both needed before the closing review and the landing can happen here):
  1. Re-arm this Mac's steward: dispatch is refused ("census fingerprint does not match
     the armed code") and `metasystem up` reports ENROLLMENT_DRIFT after the pull and
     engine rebuild. Either at an agent-free terminal on this Mac
     (`bin/metasystem steward restart --repo .`), or by his verbatim word in the chat
     with a review date (the R-29-m2 path, `steward arm --temporary-human-word`).
  2. Move the claim of goal path-class-manifest from m1 to m2: `goal steal` is a
     human act (--by) and needs a fresh norm token row (goal=path-class-manifest
     minutes=3000 goalRevision=9), which the seat mints under R-58-m1 on his word.
- Not blocked, done: the 14 digest lines this Mac's steward wrote while its checkout
  was 750 commits behind are re-appended to records/narrator-digest.log (uncommitted).
- 17:15 local: Wido's word in chat ("I won't do that; you will have to") armed this Mac's steward
  through the temporary-word path, review date 2026-09-06. The fleet Telegram channel is configured
  in metasystem.conf.local and message id 8 was posted. Dispatch's census precondition passes at the
  outer repository scope. FOUND: every `metasystem up` (the stop hook runs it at each turn end) kills a
  runner whose first tick has not completed, so the steward never completes a tick on this Mac
  (backlog item up-kills-runner-before-first-tick). Do not run `up` while the runner is attempting.
- Lineage note: the checkout lease announces main-1788441180-48662-67e8bb for this session; the goal
  opens of this takeover were journaled under the seat-minted main-1788441779-14484-82d6ed.
- 18:1x local: Wido answered "yes" to moving the manifest claim here; token row R-70-m2 appended to
  memory/rulings.md (uncommitted). The steal was refused by the new execution-approval gate
  (human-approval-for-execution landed while this seat waited): "APPROVAL_REQUIRED ... only the
  human approves it with goal approve". The harness classifier blocks the seat from typing
  `goal approve --temporary-human-word`; Wido types it himself or grants the permission. Then:
  steal, dispatch the apply brief, conformance review, closing review, land.
