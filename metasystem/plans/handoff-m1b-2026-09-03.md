# m1b — takeover handoff, 2026-09-03

Written for a different machine to pick up m1b's work without losing
anything. Everything named here is on origin/main unless the section
"On this machine only" says otherwise.

- Owner: m1b (Claude session on the host Mac, clone at
  /Users/wido/LocalStorage/GitHub/agentic-tools-m1b, branch main,
  announced main main-1788333346-60696-6a3256, steward armed
  temporarily under R-37-m3, review 2026-09-06).

## Goal and current status

m1b's one feature under Wido's word R-61-m1 is **goal
token-spend-fence**, step 1 (alert mode), CLAIMED by m1b, budget 3000
minutes / 30 attempts (R-62-m1). The tier-3 ladder is complete through
its reviews and is in its build:

| Rung | State | Artefact |
| --- | --- | --- |
| Design | done | plans/token-spend-fence-design.md (revision 2, 299 lines) |
| One review | done, 4 material findings, all accepted | plans/token-spend-fence-dispositions.md |
| One fold | done | revision 2's changelog |
| Closing review | done, 2 findings bound as test obligations | plans/token-spend-fence-dispositions-closing.md |
| Build | round 3 done, work on branch agent/fence-build, one gap open | plans/token-spend-fence-build-brief.md |
| Code review | not started | plans/token-spend-fence-code-review-brief.md (ready) |
| Land | not started | with --chain |

What the fence does in step 1: spend in tokens and money per goal,
machine and day from the runtimes' own usage records; the seat's own
spend from this machine's Claude Code transcripts, delegates excluded
by their recorded session ids; ceilings in metasystem.conf with
defaults derived from 2026-09-02; one health line per tick; one alert
episode per crossing, per multiple; **nothing is refused** until Wido
agrees the calibration (step 2 is his word, not this goal).

## The build's work is preserved on origin

The last build round (job fence-build-r3) implemented nearly the whole
feature and its own tests passed before it stopped on a gap. Its bytes
were uncommitted in a worktree, so the seat committed them UNALTERED on
the delegate's own branch and pushed it:

- **branch `agent/fence-build`, commit b5a73581** — 472 insertions:
  internal/spend (measure, transcript, the fixture bed bed-20260902),
  internal/config/spend.go and its validation, the lifted
  mission.JobUsageAt with its Source field, the spend health role and
  line, the typed tick carriage, the per-crossing alert episodes, and
  the named tests including both closing obligations.
- That commit is **not a landing**: not main, chain not closed, not
  reviewed. It is evidence to continue from.

Two things the continuing machine must resolve:

1. **The open gap the round stopped on** (its return, round 3): the
   design does not say what happens when a present transcript file or
   directory cannot be listed or read. Returning the error makes the
   role unknown, which contradicts section 6's exhaustive unknown
   conditions; skipping it contradicts "every present file enters the
   lifetime goal scope". The seat did not choose. Answer it as a gap
   answer in plans/token-spend-fence-dispositions-closing.md, then
   resume the build with a follow-up brief. The suggested answer, not
   yet authorized: count an unreadable transcript file as its own
   `seat unreadable files=<n>` ledger line and health segment, like the
   unreadable job record, leaving the role alive.
2. **Conformance currently refuses the round**: it declared its fixture
   directory `internal/spend/testdata/bed-20260902/` instead of the
   files inside it, so `validate conformance --stage review` reports
   changed paths outside the declared boundary. The next implementer
   round declares every file, or the follow-up round's diffBoundary
   does.

## On this machine only

Nothing of the feature. What stays here is gitignored runtime state
(job records, returns, usage, the worktree itself). The claim is
released: **token-spend-fence is queued and claimable by any machine.**

## Decisions made (and who made them)

- Wido, R-61-m1: the four features approved, any budget; m1b takes the
  token spend fence. R-62-m1: its budget token at revision 3.
- Wido, R-63-m1b, typed verbatim: the resume of the breach-stopped
  goal dispatch-cap-necessity ("Resume dispatch-cap-necessity, review
  at my terminal by 2026-09-06"), which m1b then released.
- m1b, during the build: two gap answers recorded in
  plans/token-spend-fence-dispositions-closing.md — the build brief's
  stale "dead on a crossing" line was corrected to revision 2's
  alive-on-crossing rule, and mission.JobMeasurement gains an additive
  Source field so AggregateUsage's output bytes stay identical.
- m1b: the closing review's two findings are named test obligations
  bound beside the design (R-60-m1), not another design round.

## Other goals this seat touched, all released

- **dispatch-cap-necessity** (the reservation-accounting bug): design
  revision 4 landed, critique loop closed at round 4 by the stop
  criterion, scope cut risen to Wido
  (plans/dispatch-cap-settlement-scope-cut.md), build brief ready at
  plans/dispatch-cap-settlement-build-brief.md. Released, unclaimed.
  Its two cut defects are goals governed-exhaustion-reprojection and
  lease-sweep-death-evidence, each budgeted 4h/10/240m/1.
- **two-bars-for-changes**: the caller-class slice is parked at design
  revision 3 with three dispositions files; two invariant-grade
  findings were open when Wido ordered the stop. Build brief landed at
  plans/two-bars-caller-class-build-brief.md.

## Waiting on the human

Nothing blocks the fence. Standing items: the 2026-09-06 terminal
re-ratification of every temporary word (the steward arm, R-63-m1b);
Wido's calibration word before the fence's step 2 enforces anything;
price rows for the roster's models in metasystem.conf, which the
design deliberately leaves empty.

## Dead ends (do not retry without new evidence)

- Landing with a pathspec while the steward's digest is live: the
  wrapper refuses because the projected bytes move under it. Stage the
  digest and land with --staged-only, retrying; that is what every
  landing here does.
- Running the SessionStart supervision hook from inside a tool call: it
  cannot identify the agent process and refuses.
- `goal release` on a breach-stopped goal: only a human resume clears
  the fence, and the claim quota counts the stopped claim
  (the recorded defect breach-stop-wedges-seat).

## Next step

Claim token-spend-fence (budget token R-62-m1 if the norm gate asks),
fetch branch agent/fence-build, answer the transcript-read gap above,
and resume the build with a follow-up brief on a NEW implementer chain
seeded from that branch (the old chain's job records are on m1b and
unreachable). Then
`validate conformance --stage review`, one Fable code review from
plans/token-spend-fence-code-review-brief.md with --reviews on the
terminal build round, register-advance, close the chain, land with
--chain. The engine must be rebuilt after any pull that changes Go
bytes, and the steward re-armed under R-37-m3.
