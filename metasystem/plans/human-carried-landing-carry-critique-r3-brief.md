Working Mode: design
Orchestrator Identity: m1d (lineage main-1788941004-20871-6e7a43, coordinator under goal human-carried-landing-carry)
Date: 2026-09-11

# Review brief: third and last read of the carried-landing design, revision 5

FINDING IDS: chain-unique, continue the register: HCL-C-35, HCL-C-36, ... never F-n. A re-opened finding keeps its own id.

## What you are reading

`metasystem/plans/human-carried-landing-carry-design.md`, revision 5, at
commit 316c99b2d in your worktree (a local scaffold commit: the page
lands on `main` together with this read), sha256 138f6bb7311dec3c648fb49124234ef7255d9ff25441605b37a25995d4403a04, folded by
job hcl-design5-20260911 on the Fable lane from your second read
(`metasystem/artifacts/agents/hcl-context/critique-r2.md`, with the
coordinator's dispositions at its end, all nine accepted). The fold brief
is `metasystem/artifacts/agents/hcl-context/design-fold-r2-brief.md`; the
first read and its fold are `critique-r1.md` and `design-fold-r1-brief.md`
in the same directory. The "Declared Outputs" digest line the dispatcher
stamps into your prompt is the digest of the outputs manifest, not of the
design.

Write your register as this new file: `metasystem/records/misc/human-carried-landing-carry-critique-r3.md`
If your runtime is read-only, return it in the job return; the
coordinator projects it.

Read-only design critique; implement nothing, run no bed. This is the
goal's third and last design read (the box allows three review rounds):
after it the page is built as it stands, and anything you find that is
material but not folded is carried into the implementation's closing read
as a named check. So be exact: a material finding here is one that would
make two implementers build different things or lets a carry land what
the human did not name; everything else is a note for the code read.
Read the code at HEAD.

## Settled, do not re-derive

The dispositions of reads 1 and 2 are binding. Points 01 and 03 on the
old page; the two proofs; the word as a workspace tree; one named
refusal or group per word; the review deferred never deleted; the cap and
the debt as asks; one chain; the four seams; the format fence; the seat
binding and `--transfer`; the fleet-visible `carrying` row before the
push.

## Mandate, in order of consequence

1. **The nine, one line each.** For HCL-C-33, -21, -30, -26, -25, -03,
   -34, -07, -32: answered by the text with the fixture it needs, or not.
2. **The reservation row (HCL-C-33).** The `carrying` row before the push
   as fleet-visible debt: its lifecycle (written, closed by `carried`,
   abandoned, expired), what a peer's landing sees at each point, and
   what a crash between the row and the push leaves for the next seat.
   Find a two-seat interleaving that still stacks, or one that blocks the
   fleet on a dead seat with no closer.
3. **The match rule, second form (HCL-C-21).** Sufficiency for a code
   word; "the only insufficiency is G" for a group word. Find a landing
   with two defects that still lands, or a single named defect that
   asks.
4. **Consumption, supersede, replay, recovery (HCL-C-30, -25, -26).**
   The complete predicate in the mutation; the full-payload replay
   table; the named recovery path for the counselor hook.
5. **The owner fence and the contract (HCL-C-03, -34).** The owner
   directories; execution of every new test by the owning groups.
6. **Buildability.** One chain, one root round at the cap plus two
   corrections: say whether it is buildable and name anything that must
   be a follow-up to keep the chain landable, without weakening the
   goal's sentence.

## What is not in your mandate

Style, length, whether the feature should exist, durations. Do not run
beds. Do not re-derive reads 1 and 2.

## Return

One register at the path above: findings by id (HCL-C-35 onward, or a
re-opened id), each with the section, the claim, the evidence (file and
line at HEAD), and what changes if it stands; then the nine one-line
verdicts; then a verdict line: buildable as is (with the notes for the
code read listed), or one fold naming which findings. Wall-clock budget:
40 minutes.
