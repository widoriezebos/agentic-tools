Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal account-provenance)
Date: 2026-09-02

# Goal

Independent critique of metasystem/plans/account-provenance-design.md
(landed, in your worktree). Attack: the proof claim per runtime (does the
named identity surface actually attest the account, or is it
self-declaration in disguise — demand the honest bound); disagreement rule
between announcement and launch records; the retirement condition for the
landing-message convention; registry coverage for a new runtime without
code edits; fixtures. A clean return closes the design; the build fits the
box.

# Constraints

Wall-clock budget: 20 minutes. Return per the design-critic schema.

# Gap Rule

stop and report a gap; never fill it silently.
