Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal supervision-hook-wrong-root)
Date: 2026-09-02

# Goal

Round-2 critique of metasystem/plans/supervision-hook-root-design.md
revision 2 (landed, in your worktree), which folded your five round-1
findings (metasystem/records/misc/hook-root-critique-r1.md, landed) by
withdrawing the marker search entirely: the world derives from the firing
hook script's own installation via the engine's state-root authority. Judge
each fold BY ID, and attack the new mechanism: symlinked installs, a hook
invoked through a copied or relocated script, the engine binary absent or
mismatched at the derived root, and whether the derivation truly answers
the worktree question by construction. A clean return closes the design
phase; the build ships.

# Constraints

Wall-clock budget: 25 minutes. Return per the design-critic schema.

# Gap Rule

stop and report a gap; never fill it silently.
