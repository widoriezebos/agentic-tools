# Handoff m3, 2026-09-05 (the seat on Wido's Mac, git nickname m3, lineage mac-m3)

Written at the end of the unattended night of 4 to 5 September. Wido's
ceiling for the night, verbatim: "Gateway, then channel-poll-not-automatic,
then wait". Both are done as far as the night could take them; the seat
now waits.

## Landed tonight

- 31ce3c73 — fleet-channel-gateway step 2 (the inbox record and the ledger
  validator's channel tree).
- 57c310a1 — fleet-channel-gateway step 3a, "The ledger receive is a library:
  verify, match, commit, with no caller". Three implementer rounds, two closing
  critic roots (fcg-build3a-cc, two rounds; fcg-build3a-cc2), both registers
  clear. Full goal package green on the round-2 and round-3 trees (352 and 353
  tests, ~16 min each; the package is slow because its fixtures run real git
  transactions).
- 1f8c6221 — channel-poll-not-automatic, tier 1, landed as a declared tier-1
  direct fix (root job cpna-build1, 18 changed lines in two files): `channel
  wait` polls the channel every `--poll-seconds` (default 30, first poll at
  once, 0 disables) through the steward's channel duty while the record has no
  answer; the channel fixture's Telegram leg now proves a verified reply is
  dispositioned by the wait alone. Goal concluded (4546dcf9). The steward tick
  already ran that duty every tick since 66dfaf77.

## fleet-channel-gateway: where it stopped, and why

The claim was released (c443e38a) so the machine's one claim could go to the
tier-1 goal; re-claiming needs a fresh approval row (claim and release bump the
revision). Box after 3a: attempts 16/24, reserved 600/1200 min, elapsed
4h54m/3d. The goal-level spend ceiling (125M tokens) is crossed in alert-only
mode; raising it is Wido's word (R-60-m1).

Step 3b (the caller's Confirm and posting) was dispatched once (fcg-build3b,
gpt-5.6-sol) and stopped under the gap rule with no file changed. The gap,
verbatim from its return:

> OpenWork item (i) requires changing an existing tuple (open, null phase,
> null posting, null thread, null receipt reference) to posting {question,
> me, now}. ChannelMatrix has no FROM row for this operation. Its "ask" row
> applies only when the file is absent, and ClassifyChannelTransition
> explicitly permits that row only with present=false. Reusing it therefore
> rejects the transition; inventing a local predicate or changing the matrix
> violates the brief. The design must add the missing existing-question
> intent transition or authorize a precise equivalent.

The seat confirmed it against the design: the matrix (design lines 352-381)
has no such row and the open-work pass item (i) (lines 703-712) requires it.
It is law-changing and belongs to the design chain. The 3b brief
(plans/fleet-channel-gateway-build3b-brief.md) carries an added paragraph
naming the 3a design findings as not the implementer's to fix; the 3b job
itself could not be closed by `dispatch.sh close` (it wants an independent
critique reference) and stands as a completed, unclosed round.

## The design round that is due (fcg-design-cc1, round 3 of 3, the failsafe)

Open in its register: FCG-C-16 (critical: the shared operation identifier is
not an idempotent confirmation on a machine that already terminalised it as
abandoned, rejected or expired), FCG-C-17 (high: ValidateChannelTree admits
no listener heartbeat record, yet FCG-STATUS-09 writes one), FCG-C-18 (high:
a valid follow-up in an answered question's thread becomes a permanent poison
update). To be added to that round, from 3a's closing critic (accepted,
carried; plans/fleet-channel-gateway-build3a-code-review-dispositions.md):

- F-1: the stop question's trailing full stop binds in the inbox (FCG-C-22
  tolerance) but the resume authority's containsContiguousFields does not drop
  it. Recommendation: extend the same last-field tolerance to
  containsContiguousFields, or make both strict; 3c must not wire the
  stop-question answer to resume until the two rules agree.
- F-2: a newline inside the human's verbatim text meets the one-line history
  grammar. Recommendation: fold newlines to single spaces in the history line
  and keep the verbatim text in the inbox record. Latent in today's Poll too.
- F-3: a reply threaded to an orphan post while the question's thread is null
  has no committable matrix row. Recommendation: an answer row that admits
  thread-null when the binding came through an orphan post.
- The 3b gap above: a "question-intent" row (present, open, null posting ->
  posting {question, me, now}) or an authorised equivalent.
- N-4 (noted): the caller keys on the result outcome, never on `decided`.

Round 3 is the last review round of the design chain; it should carry every
item above in one dispatch, with Wido's word on any of them that changes what
the channel does (F-1 and the poison-update rule touch authority).

## Rules already settled for the 3c brief (from the code critics)

control.json by rename (cc2 F-2, cc F-9); a fresh directory per fake (F-3);
the loader sentinel (F-5); unmatched.jsonl retirement (F-7); key on outcome,
never on decided (N-4); verification includes ./cmd/...; no stop-question
answer wired to resume until F-1 is settled. The fleet cut-over (3c) needs the
other seats present: daytime.

## Two records still local on m3, not landed

- plans/channel-poll-not-automatic-build1-brief.md (the tier-1 brief; its
  delivered copy is artifacts/agents/cpna-build1/rounds/1/prompt.md). The
  tier-1 class admits three files, so the brief could not ride with the fix,
  and after `goal done` the register-carriage class refuses it as a record
  owned by a goal no longer held. Lesson recorded: land a tier-1 brief as
  register-carriage under the held goal before concluding it.
- plans/fleet-channel-gateway-build3b-brief.md (the added paragraph naming the
  carried design findings): owned by fleet-channel-gateway, lands with its
  next claim.

## What waits on Wido

- Re-approvals due 2026-09-06 (every standing temporary word, including the
  R-37-m3 re-arm word and the goal approvals).
- A fresh approval row for fleet-channel-gateway before it can be re-claimed
  (the release bumped it to revision 14).
- Whether to raise the goal-level spend ceiling for fleet-channel-gateway
  (crossed, alert-only).
- The design round's authority-touching items (F-1, FCG-C-18).
- Telegram: nothing arrived overnight; the seat polls at :20 and :50 and posts
  status at :50.
