Working Mode: implementation
Orchestrator Identity: root Codex, main-1790272787-5030-13dde3
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| complete-planning-and-public-composition | 14000 |

Resume same Opus context. Root read full return5; main is merged through8db599e25
(latest change only UI design). Parent product ~6300 changed lines, still uncommitted.
User-authorized full intent redesign/machinery bypass/ignored stale hook persists.

PRIORITY THIS PASS: IMPLEMENT THE ACCEPTED PLANNING JOURNEY, IP1..4, as a complete
vertical flow with named focused tests and a runnable fake-provider recipe. It
has waited through several parent-proof passes; the remaining parent proof is
retained below, not waived, and will finish before final combined certification.
Read accepted design intent-planning-continuity.md and ALL IP-C1..5 dispositions
and final report. Brief4 gives implementation-specific details, still binding.

Parallel owner work: a separate Opus works in ../agentic-tools-intent-read-owner-20260926
from private seed f2d5da5fe541cd5d41aef30d64475d928f83c7d6. It owns ONLY
internal/launch/unit_run.go read-sequence extraction, new standalone read owner
and related launch read tests. DO NOT edit unit_run.go, its read tests or create
standalone-read files while it runs. You own planning request/manager startup/
supervise/project publication/CLI/dispatch integration. Root will import its
computed patch and API before asking you to wire manual diagnostics. No conflicts
should be necessary. No manual CLI/backend implementation this pass unless root
has integrated that owner's returned code. The accepted manual full-build brief
is now plans/intent-manual-review-build-brief.md, implementation authorized later.

Planning essentials:
- design G --brief FILE [--out FILE] [--after N]; invoking checkout, no build
  claim/worktree; real design roster and admission, staged output. Freeze request
  before spawn and resolve retained before newly produced output/ambiguous docs.
  Project owner conditional publication validates record identity/goal/draft and
  compares actual expected bytes; preserve human edits and conflicting proposal.
- Atomic stranded Starting recovery in Store.Update checks absent Supervisor/
  Child/ProcessGroup and no uncertain owner; Supervise's first locked claim checks
  terminal state BEFORE any child. Test both interleavings and delayed process
  exit, not age as death proof. Real stop owner for recorded supervisor custody.
- show design --goal G [--out FILE] [--attempt N]; stop design G same selection;
  status/wait see real author attempts through owner APIs and public output refs.
- First paid design critique acquires lawful claim only when eligible unclaimed
  agent; actual dispatch still needs tier3/stop capability/actor-epoch/accounting,
  no worktree. Preserve exceptions exactly, no silent approval or takeover.
- review design FILE --dispositions FILE [--after N] and --retry N: same bounded
  critic chain, actual newest subject. Freeze operation id; follow_up mints child
  under its lock; retain ACTUAL returned child afterward. Replay resolves retained
  child or exposed owner findOperationRecord after lost response, validates full
  provenance. No predicting child ids, no holding chain lock across follow_up,
  no rerun followup just to lookup completed operation. Later goal revision or
  intervening round still rejoins exact old completed request.
- Open/failed/capped/exhausted design at same goal/canonicalpath cannot buy fresh
  root; timeout now folds with real death proof. Retry same cap, live refusal,
  failed retry replay and actual child adoption. IP-C4/C5 fixtures mandatory.
- Complete design closure public replacement, then close compatibility can hide.
  Docs/skills describe implemented whole tasks; root updates AGENTS at final.
- Public proof references from actual test/landing/status producer feed wait proof.
  Do not invent another wait store.

Bounded public namespace correction (same principle as manual IM-C3):
status goal G [--work NAME] forwards goal work. wait goal G [--work NAME] WITHOUT
--for forwards running work; explicit --for landing|human-act is existing event
observer. Legacy flag event selectors retain defaults. status ui/checkout/job/unit
and wait file/proof/job/unit/question/resume currently cannot serve identical valid
goal names. Correct generated continuations too. TestIntentReservedGoalNames,
existing target behavior preserved; no lookup-by-goal-existence guess. Root folded
this mechanical correction into accepted parent; no new protocol/policy.

Root resolved the whole-close fixture obstacle (do this small change this pass
if it unlocks planning closure): no pre-close mirror guard exists in closeChain/
finishedReview/criticClosure. Missing register was fixture incompleteness, but
unmirrored before whole close is EXPECTED. TestIntentConnectedJourneyRealClose
already does actual whole close/collection/publication. At lines247-278 replace
manual do(close,criticB2,...) THEN do(review commit...) with ONE public review commit
--dispositions. Its finish() sets authentic computed subject, fake-provider return,
session/capability metadata and real CritiqueRegisterAdvance; realCloseOwner mirrors.
Don't pre-mirror, fake a closure stamp or remove the positive assertion.

Root design correction for truthful close refusal, source-grounded smallest form:
Preflight actual record-writer owner BEFORE CritiqueClosedWithRegister may mutate
root register. classifyVerbCaller(root,int64(os.Getpid())) (executing engine, not
caller-supplied pid or Getppid); preserve class/holder/jobId/stewardJob, then call
internal/authority.Authorize("record-writer",map,rootJob). Classifier error means
could not establish authority. Error specifically from Authorize means actual
record-writer authority refused. Existing shell guards still apply to root and
terminal members; preflight is not a completion guarantee. All later full-close
failures remain honest generic operation failure plus owner stderr, supplemental
CloseCheck facts and public repair review; NEVER infer cause from !Holder,
closability or recordWritable. File O_WRONLY is not atomic parent-dir replacement
permission anyway. Remove those posthoc guesses; don't invent typed I/O errors
or a new close protocol. Tests real authority matrix + genuine operational failure.

Remaining parent obligations are still REQUIRED, from return5/checklist:
positive brain/floor/reconcile/migrate/carry and full carried transport; real goal
and question event wait; actual review-attestation publication state; collected+
unpublished+built-unread coexistence. Unit commit push is not read publication.
Read owner/status should expose actual branch publication fact; don't just delete
false wording and mark selection done. Public stage says review/result, not private
collection bookkeeping. No broad proof until critical/high code/fixtures ready.

Three broad steward failures in owners3 were missing installed bin/metasystem:
when next running those actual tests, build candidate to its isolated bin first.
No need rerun broad owners unchanged now. Two known intent regressions fixed and
focused rerun passed; retain their broad failure honestly for final selected run.
Keep METASYSTEM_TESTING_WORKERS=9 and inherited other allowances/caches. Register
new tests without dropping incoming main entries or changing coverage floors.

No commits/pulls/pushes, plans/rulings/AGENTS/receipts, conf.local, real services/
paid test providers or dependencies. Product boundary includes accepted launch
startup/design, project conditional publication, dispatch owner lookup and CLI;
standalone read exclusive boundary above must remain untouched. Root owns final
exact-candidate diagnostic acceptance (no forged queued-goal approval) and Sol read.

Up to200 NEW tool blocks or40 actual minutes; checkpoint coherently if needed.
Do not stop early claiming guessed tool exhaustion: authoritative cumulative
checkpoint5 tools473/modelcalls460,81 NEW tools, actual15m57.3s. Report actual exits,
API/recipe, all IW/IP/IM remaining gaps. Return6:
metasystem/artifacts/agents/intent-workflows-build-return-6.md.
