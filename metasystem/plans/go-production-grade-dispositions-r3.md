# go-production-grade — critique round 3 dispositions

Critic: Codex (same thread), 2026-08-11. Verdict line: 7 material findings, NOT READY, blocking R3-F1..F7. All 8 findings (7 material + 1 non-material) accepted, 0 refuted; the checkable claims were re-verified in this checkout before adjudication. Round closes 8/8 dispositioned. This exhausts the first three-round budget with material findings open; per the design-critique skill the continuation is one focused follow-up on the same critic chain enumerating R3-F1..F7, with one fresh budget, after which an unconverged loop stops and waits on the human.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| R3-F1 | accept | Verified `adopt.sh:230→237`: payload lands before the build check, and adoption currently succeeds without Go when a prebuilt binary exists — "always rebuild" silently changed adoption's contract and could fail after mutating the target. | Phase 0a is declared behavior-changing: Go becomes an adoption prerequisite, preflighted before any target mutation; new reserved decision 5 (B3/Phase 0a) asks the human to approve it. |
| R3-F2 | accept | Verified `go-gate.sh:27-41`: the gate's build carries a concurrency fence against swapping the binary under a live gate run; adoption's direct `go build` (`adopt.sh:240`) has none, and making it unconditional would widen that race. | Phase 0a: adoption builds through the same single fenced build path as the gate; acceptance adds a concurrent gate/adoption exercise that must hit the fence. |
| R3-F3 | accept | The Linux floor file was to be "seeded from the first green run" while the gate fails any package without a floor entry — circular; no green run can exist before the entries do. | Phase 0c: explicit two-pass bootstrap (seed mode with every non-floor check enforced and coverage collected → write floors → full enforcing re-run); Phase 1's acceptance is the enforcing second pass. |
| R3-F4 | accept | Join gap: r2's disposition accepted trailing-data preservation but the amended grammar list omitted it. The dispatch reader is a single `Decode` with no EOF check (`record.go:291`), so trailing bytes are currently tolerated. | The frozen-grammar list in Phase 5 now names trailing data and multiple top-level values explicitly, per family. |
| R3-F5 | accept | Verified — and the finding understated it: six writers discard the directory sync (`dispatch/record.go:356`, `mission/ledger.go:461`, `missionrunner/engine.go:186`, `host/host.go:64`, `adapter/adapter.go:104`, `lease/lease.go:171`; ran it, grep for `_ = dir.Sync()`), several under comments promising durability. The "both writers" B5 premise was false. | B5 is inventory-driven: the phase opens with the pattern audit, each writer's durability contract is recorded and dispositioned, and the audit joins the B5 checklist so no writer is missed silently. |
| R3-F6 | accept | Verified the mechanism: the sync failure surfaces only after the rename has published the record; `RecordCAS` has applied the transition, `recordExit` would map the error to exit 1, and `dispatch.sh record_cas` emits lifecycle events only on exit 0 — naive propagation manufactures committed-state-reported-as-failure with a suppressed event. | Phase 4's durable-writes design distinguishes pre-publication failure from committed-but-durability-unknown, specifies exit codes, event emission, and readback/retry per caller, and requires fault-injection tests asserting both the returned outcome and the visible state. |
| R3-F7 | accept | Phase 1 verifies the Linux host, then Phases 4–5 change behavior, and nothing re-ran the Linux suite before the profile promotion at closeout; the standing cross-build was also amd64-only while the verified host's architecture is an open human decision. | Phase 7 opens with a fresh full suite on the recorded verified Linux host after the last behavior-changing phase; the standing cross-build covers amd64 and arm64. |
| R3-F8 | accept (record) | The ordering rationale overclaimed ("before the large restructurings") relative to the numbering (Phase 4 follows 2–3). Non-material: the numbered sequence is implementable as written. | Rationale narrowed to what the numbering delivers: defects land before typed documents. |
