# Build brief: the stop notice says delivery is unconfirmed (member 3b of stop-hook-never-forces-an-empty-turn)

DIRECT MODE (Wido via m1e). Working Mode: implement. Model gpt-5.6-sol. Date 2026-09-19.
Goal-Unit: stop-hook-never-forces-an-empty-turn/stop-notice-says-delivery-unconfirmed
Declared size: 700 changed lines

Your working directory is the module root `metasystem/` of a detached git worktree; every file of this unit is under it. Bash and Go only. Leave every change uncommitted; never run git add, git apply, git stash, git rebase or git commit. Never edit `memory/receipts.log`, anything under `records/` or `plans/`.

## Start state

START STATE CHECK, first command: `git rev-parse HEAD`. The sha must start with START_STATE_SHORT (full START_STATE_SHA). Second command: `git status --porcelain` must print nothing. Put both under `## Start state` in the return. If either fails, write `MISMATCH <sha or tree>` as the first line of the return and stop.

## The design section is the specification

The design page copy is `/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools-m1b/127d7cba-2e99-40bb-822e-6c6c855d496f/scratchpad/direct/stop-hook-members-design-r2.frozen.md` (644 lines). Third command: `shasum -a 256` of it must start with `9ba7a005e4336b63`; otherwise `MISMATCH page` and stop. Read exactly lines 119 to 182 with `sed -n '119,182p'`: the section `## Member 3b: stop-notice-says-delivery-unconfirmed` with its subsections 1 DONE, 2 Mechanism, 3 File set, 4 Fixtures, 5 Mutations, 6 Estimate and 7 Landing. Build that section in full: its three DONE rules, the mechanism as written (the sentence, the fourth qualifier, the `stop-replay` action with its wrapper engine, the expiry seam), the file set, the three tests with the reasons they fail without the mechanism. Read no other line of the page; the other sections belong to other units. Where the section and this brief differ, this brief wins.

The section cites `file:line` against this start state. Locate by symbol; the lines are:
- `scripts/agents/supervision-hook.sh` (2713 lines): the qualifier tables `degraded_stop_cause_qualifiers` 1143 to 1151 (one entry per cause in the order of `degraded_stop_cause_keys`: unreadable-output, engine-missing, engine-skew, staging-failed, deadline-expired, bootstrap-failed, bare; only deadline-expired admits both `record-update-failed` and `condition-log-failed`), `degraded_stop_qualifier_keys` 1152 to 1156, `degraded_stop_qualifier_texts` 1157 to 1161; `degraded_stop_form()` 1165 with `local requested=(false false false)` 1168. The deadline parent: `deadline_budget_sec` clamp 1236 to 1243, `deadline_started_epoch` from `date -u +%s` 1244, `deadline_started` from `SECONDS` 1263, the worker start 1264 to 1267, `deadline_expires` 1268, the engine resolution 1270 to 1278, `deadline_log_stop_condition()` 1334 (sets `deadline_log_failure` at 1336, 1341, 1343 and 1346), `deadline_running()` 1377, the wait loop on `SECONDS` about 1380, the worker output check 1435 (a worker answer equal to the engine-missing form is valid), the expiry path 1526 to 1567 (TERM then KILL of the worker whose command line names the hook, then `wait`), the parent's `report stop-block` call 1579 and `deadline_record_failure` 1572 to 1585, `deadline_form_qualifiers` 1588 to 1591, the form calls 1597 to 1600. The engine-missing answer of the ordinary worker at 1649 to 1656: the worker prints `degraded_stop_form allowed engine-missing` and exits 0 before any write. `record_stop_failure()` 1748; the arming failure `record_stop_failure "supervision arming failed" supervision-arming` 2125 after the `up` calls 2097 to 2103; `external_stop_json()` 2072 (the `report stop-block` call with `--refusal-record`); `compose_failed_stop()` 2311 (the `external_stop_json` call 2327, the record-failed test at 2328 (a nonzero refusal rc or an empty response), `record_failure_message` 2329 to 2331); `stop_refusal_record` 2418 (`artifacts/agents/supervision/stop-refusals/<slug>.json` under the resolved checkout); `supervision_dir` 2439, `hook_log_failure=` 2441, `append_stop_condition()` 2442 to 2448 (appends to `artifacts/agents/supervision/hooks.log`); the stop_conditions loop 2567 to 2571; the `hook_log_failure` extras line 2620; the `compose_failed_stop` call 2625.
- `scripts/agents/stop-degraded-forms.sh` (282 lines): the tables 6 to 47, `degraded_stop_form()` 55 (`requested=(false false false)` 58), `degraded_stop_list()` 128 to 172 (the golden's rows: the two bare forms, then per cause every mask over its admitted qualifiers), `degraded_stop_expected_hook()` 174, `degraded_stop_expected_template()` 197, `degraded_stop_sync_or_check()` 237.
- `internal/hooks/testdata/stop-degraded-forms.golden`: 18 tab-separated rows `decision`, `cause`, `qualifiers`, `json`.
- `internal/hooks/degraded_stop_forms_test.go`: `TestDegradedStopRendererIsSideEffectFree` 29, `TestDegradedStopFormsMatchTheGoldenContract` 101 (`want 18` at 114 becomes the new row count).
- `scripts/agents/adapters/fake.sh` (464 lines): `usage()` 4, the dispatch `case` on the command word 385 (arms wait-delivery, output-stream, local-config-paths, contract, signature, identity, config-identity, probe, dispatch and follow-up, cancel, selftest, help). `stop-replay` is a new arm plus one usage line.
- `cmd/metasystem/runtime_conformance_test.go` (120 lines): `TestCapabilityDeclarationsMatchRegistrations` 20, `TestCapabilityFlagsBackedByExecutables` 67, `TestACPExpectationsMatchDialects` 89. `TestFakeStopReplay` is new in this file.
- `scripts/enforcement/claude-code-hooks.json` line 33: the Stop entry command `(bash scripts/agents/supervision-hook.sh claude stop) || printf '%s\n' '{"systemMessage":"Task unknown; Stop allowed; ..."}'`, timeout 60. The action reads that command string from this file and runs it through `sh -c`; it never retypes it.
- `scripts/agents/supervision-hook-fixtures.sh`: the provider payload shape 228 (`{"session_id":"...","cwd":"<root>","hook_event_name":"Stop"}`), `missing_engine_expected` 132 (the engine-missing form is what a missing `METASYSTEM_BIN` answers), the wrapper engine heredoc 1797 to 1822 (blocks the worker's first `steward hook-attempt` after running the real engine for it; ends on the release file, on its parent's death, or at its 61-second cap) and the wrapper's use 1834 to 1838 (`METASYSTEM_BIN`, `METASYSTEM_DEADLINE_REAL_ENGINE`, `METASYSTEM_DEADLINE_RELEASE_FILE`, `METASYSTEM_STOP_DEADLINE_BUDGET_SEC`). Read those lines as the shape; the bed is HOST-ONLY and not in the file set.
- `cmd/metasystem/wait_verb_test.go`: `pendingWaitVerdictCommandFixtureWithOptions` 815 (a fixture checkout the hook resolves), `copyExecutableFixture` 932, `installPendingWaitHookFixture` 946 (copies the hook, its scripts, `adapters/fake.sh` and the engine into the root), `reapPendingWaitFixtureChildren` 1006, `runPendingWaitHook` 1093. Follow these shapes for the bed root, the process fixture and the reaping.
- `cmd/metasystem/landing_verbs_test.go` 1233: `go build -o <temp>/metasystem .` inside a test, the shape for the engine the bed runs.
- `internal/testutil/fixture.go`: `Fixture(t)` 63, `Env(base)` 166 (re-adds the owner tag), `Shell(script, args...)` 173, `Record(pid)` 192, `Hold(pid)` 194, `WaitForNoUnrecordedChildren(ctx)` 197.
- `internal/identity/fixture_survivors.go` 14: `FixtureOwnerEnv = "METASYSTEM_FIXTURE_OWNER"`, the owner tag's environment name (the page left it unchecked; this is it).

## Integrator conditions (binding, on top of the section)

1. The seam is honoured only when the environment carries the owner tag: the parent reads `METASYSTEM_STOP_DEADLINE_FIXTURE` only when `METASYSTEM_FIXTURE_OWNER` is set and non-empty; otherwise the variable is ignored and the parent is unchanged. In seam mode the parent takes `deadline_started_epoch` from the file `started-epoch` in that directory, its wait loop ends when `worker-blocked` exists in that directory instead of when `SECONDS` reaches `deadline_expires`, and everything from the expiry path on runs as in production (the worker is signalled and waited for, the condition line, the record, the form). No test reads the wall clock, sleeps or retries; the only elapsed time is the fixture's own processes, bounded by the wrapper's ending rules.
2. Missing engine: the tree answers a missing `METASYSTEM_BIN` with the engine-missing form from the ordinary worker before any write is attempted (hook 1649 to 1656; the bed's `missing_engine_expected`). So in FakeReplayUnreadableHookState runs five and six both answer exactly that form, with no sentence: `replay-calls.log` is empty for them and the sentence appears in run four only. The fixture asserts that; the return says so under `## New tests` in one line. The engine-missing path is not changed.
3. The wrapper engine of `--cause deadline` blocks the worker's first `steward hook-attempt` call after running the real engine for it (the bed's shape), writes `worker-blocked` into the seam directory just before it starts waiting, and ends when the release file exists, when its parent process is gone, or at the bed's 61-second cap. The action writes the release file after the hook's response has been read and then waits for its own children before it exits. The wrapper of `--cause arming` fails `up` with a nonzero exit and passes every other verb to the real engine. Every wrapper appends each call's first two words to `replay-calls.log`. The wrapper, the log, the seam directory and the release file live under `artifacts/agents/fake-stop-replay/` in the bed root; the action creates that directory.
4. Faults are file states that fail for every user, root included: `hook-log` puts a directory where `artifacts/agents/supervision/hooks.log` must be; `incident-state` puts a regular file where `artifacts/agents/supervision/stop-refusals/` must be; `both` does both; `engine` points `METASYSTEM_BIN` at a missing file under the fake-stop-replay directory with no wrapper. The action prepares the fault and never repairs it; the test owns the bed.
5. The seam-off leg of FakeReplayDeadlineUnreadable: the seam directory is set, the owner tag is removed from the action's environment only for this leg, `METASYSTEM_STOP_DEADLINE_BUDGET_SEC` is 4, and the run is `--cause deadline`. The parent must ignore the seam: the condition line's deadline end is the real start plus 4, not the fixed epoch, which the test sets far in the past (1000000000) so the two can never coincide. Its wrapper ends when the worker is signalled, so the leg costs about four seconds of the fixture's own time and leaves nothing. This replaces the page's cause-arming wording for that leg.
6. The parent test builds the engine once with `go build -o` into a temporary directory (the landing_verbs_test.go 1233 shape) and both subtests use it; no skip when an environment variable is unset. Each subtest uses its own `testutil.Fixture` bed root, runs the action through `fixture.Shell`, records the pid, and ends with `WaitForNoUnrecordedChildren` under the test's own context; fakes fail with what they saw: an unexpected verb in `replay-calls.log`, a response that is not one JSON object, or a leftover child fails the test naming it. Nothing blocks, nothing retries, no bound is raised.
7. Both qualifier tables (hook and forms script) and the golden change together; `degraded_stop_sync_or_check` keeps them in agreement, the two test names in `degraded_stop_forms_test.go` stay, and `want 18` becomes the new list count, which the return states.
8. The hook fixture bed `scripts/agents/supervision-hook-fixtures.sh` refuses to run in this sandbox (`nice(5) failed: operation not permitted`): HOST-ONLY, say so under `## Sandbox reds`. Do not edit the bed. Run `bash -n` on the hook, the forms script and `fake.sh` after every edit.
9. `t.Parallel()` is the first statement of `TestFakeStopReplay` and of both subtests, or the first line is a one-line comment saying why not. A subtest that builds a bed root, installs the hook and runs the action through the process fixture is parallel.
10. Source comments say what the code does and why, in plain English; never a brief, a review, a round, a finding, a seat, a unit name or a goal id in code or comments.

## Contract (`metasystem/testing.json`)

One group changes, nothing else; paste the hunk under `## New tests`:
- group `wait-stop-standard` (its packages include `cmd/metasystem`): append `TestFakeStopReplay` to its `tests`; append to its `inputs` the entries `metasystem/scripts/agents/supervision-hook.sh`, `metasystem/scripts/agents/stop-degraded-forms.sh` and `metasystem/scripts/enforcement/claude-code-hooks.json` (`metasystem/scripts/agents/adapters/**` is already there).
- group `hook-start-audit-standard` runs every test of `internal/hooks` (`tests` is `all`) and already lists the hook, the forms script and the golden as inputs: no change.
If the inventory audit (check 8) demands another placement, follow its message and say what you did in the return; that is not a changed shape.

## Order of work (red before green)

1. Start state, page sha, clean tree.
2. Write `TestFakeStopReplay` with both subtests first, and change `want 18` to the count the new tables will list. Run the RUN SET (check 3) and paste its red under `## Red before green`; a missing `stop-replay` arm and compile errors count as red.
3. Build section 2 with the conditions above until the RUN SET is green.
4. Mutations, one at a time, the three of section 5 (the third one is condition 5's leg): apply it, run only the named test, paste the observed red line verbatim under `## Mutations` (each entry starts with the test name), restore. After the three, `git diff --stat HEAD` must equal the stat before the first mutation; paste both. A mutation that stays green is a finding: fix the test so it turns red, rerun, say so.
5. The required checks.

## Bounds

- Before every `go build`, `go test`, `go vet` or gate run: `test -e /tmp/metasystem-testrun-lock`; while it exists run none of them and check again every 60 seconds. One go test process at a time. No single command may wait longer than 240 seconds: run a longer one in the background with its output to a file and poll the file.
- Every go command runs with this environment set: `GOCACHE=/tmp/stopinc-3b-go-cache GOPROXY=file:///Users/wido/go/pkg/mod/cache/download GOSUMDB=off STATICCHECK_CACHE=/tmp/stopinc-3b-staticcheck`. Never unset or strip it.
- The sandbox refuses kern.proc, `ps`, loopback binds, DNS and `nice`. Whole-package runs of `cmd/metasystem` add `-skip 'TestProcFixtureSurvivors|TestProcessCensus|TestCustodianWatches|TestLauncherDeath|TestAllPids|TestProcessesWithWithheldArguments'`. Classify every remaining sandbox-only red in one line each under `## Sandbox reds` (loopback bind refused, "fake did not start", kern.proc.all EPERM, /bin/ps "operation not permitted", DNS denied, `nice(5) failed`); a red of any other kind is yours to fix. If `TestFakeStopReplay` itself is red only because the sandbox refuses `ps` to the hook's expiry path, paste the red under `## Sandbox reds` with the exact refused call and mark the test HOST-ONLY in one line; every other red of that test is yours.
- If your estimate of the whole change passes 1500 changed lines, stop and return `BUILD: blocked size N` with the reason; do not trim the tests to fit.
- Start no helper process outside a test; every child a test starts ends on every path. List any pid you had to start and how you ended it under `## Helper pids`.

## Required checks (from `metasystem/`, each output pasted verbatim into the return)

1. `gofmt -l ./internal/hooks ./cmd/metasystem` (must print nothing).
2. `go vet ./internal/hooks/ ./cmd/metasystem/` and the same with `-tags batchtest`.
3. RUN SET, once plain and once with `-tags batchtest`, never `-count` above 1: `go test -count=1 -run 'TestFakeStopReplay|TestDegradedStopFormsMatchTheGoldenContract|TestDegradedStopRendererIsSideEffectFree' ./internal/hooks/ ./cmd/metasystem/`.
4. Whole package, plain: `go test -count=1 ./internal/hooks/`.
5. `go test -count=1 -skip 'TestProcFixtureSurvivors|TestProcessCensus|TestCustodianWatches|TestLauncherDeath|TestAllPids|TestProcessesWithWithheldArguments' ./cmd/metasystem/` (background and poll), then `go test -count=1 -run 'TestProofAttempt|TestLandFixture|TestEveryPackageUsesSharedMain|TestBedsResolveStopReportsThroughTheEngine|TestCapabilityDeclarationsMatchRegistrations|TestCapabilityFlagsBackedByExecutables' ./cmd/metasystem/`.
6. `bash -n scripts/agents/supervision-hook.sh`, `bash -n scripts/agents/stop-degraded-forms.sh` and `bash -n scripts/agents/adapters/fake.sh`; then `bash scripts/agents/adapters/fake.sh selftest`.
7. `go build ./...` and `go build -tags batchtest ./...`.
8. `go test -count=1 -run 'TestTestEnvironmentStandardInventoryMatchesPackageTests' ./internal/testenv/` (green with the parent name registered) and `go test -count=1 -run 'TestRepositoryManifestClassifiesEveryTrackedPath' ./internal/pathclass/`.
9. `go run ./cmd/metasystem audit parallel-ratchet --root .`
10. `bash scripts/agents/go-gate.sh --fast` (background it and poll; paste the whole output).
Every check must be green. A red you cannot make green is returned as `BUILD: blocked` with the red pasted, never hidden.

## Diff and return (shell redirection only, absolute paths)

- Diff: from the worktree root (the parent of `metasystem/`) run `git diff HEAD > RETURNDIR/dm-stopinc-3b.diff`. No new file is expected; if one exists, append it with `git diff --no-index /dev/null metasystem/<path> >> RETURNDIR/dm-stopinc-3b.diff` (its rc 1 is normal) and list it in the return. Paste `git diff --numstat HEAD` and `git status --porcelain` under `## Changed files`.
- Return: write RETURNFILE with `cat > RETURNFILE <<'EOF'` style redirection (never through a tool that needs a path inside the repository). First line exactly `BUILD: done` or `BUILD: blocked REASON`. Sections in this order: `## Start state` (the sha and the page sha), `## Changed files`, `## Red before green`, `## Checks` (each command from the list above exactly as run, then its output), `## Mutations`, `## Sandbox reds`, `## New tests` (names, packages, groups, the testing.json hunk, the golden's new row count, the run five and six line of condition 2), `## Helper pids`, `## Commit message` (subject under 72 characters saying what changed for the user, a short body, last line `Goal-Unit: stop-hook-never-forces-an-empty-turn/stop-notice-says-delivery-unconfirmed`). Literal output only; leave no angle-bracket placeholder in the return.

RETURNDIR = /private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools-m1b/127d7cba-2e99-40bb-822e-6c6c855d496f/scratchpad/direct
RETURNFILE = /private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools-m1b/127d7cba-2e99-40bb-822e-6c6c855d496f/scratchpad/direct/stopinc-3b-build-return-r1.md
