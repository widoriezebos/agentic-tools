# Verb cleanup code critique, round 1 of at most 3

- Subject: `cleanup-review-subject.json`. Prepared copy `cleanup-candidate` at commit 6967c7efe42e0e4d43dfdcd9b8f4272f531271b1, tree a3d5684b983a6ac0577e43847b72fc9aed946eb1, base 1f0f51e485db7f906cfa240b1b9a46978f5e03e6. All three verified with `git rev-parse`.
- Diff: `cleanup-full.patch` sha256 3e333b24bb9252673622312355262b125a22d030d9d1a15b6395ecf0436efe26; `git diff base..candidate` recomputed in the copy hashes identically. 74 files, +1810/-2219. `ui.go` read whitespace-insensitively.
- Critic: Fable 5.1, independent of root Astra and the Opus builders. 24 tool calls, static reads plus one scratch build (`go build` to /tmp, outside the product and the evidence tree) probed from a directory that is not a repository. No product edit, no commit, no subagent.
- Materiality: would the change ship a defect, violate its brief, or damage what certifies it? Every item below WORKS and SAFE today unless marked material.
- **Verdict: zero material findings. The loop exits at round 1.** Root's remaining proof (final command group, dispatch/UI packages, static checks, runtime help, goal-cli-fixtures, land-fixtures) is still required before publication; nothing here certifies it, and the design obligation table stays PARTIAL until root records those runs.

## Conformance against the design and its capability mapping

| Obligation | Read | Outcome |
| --- | --- | --- |
| VC-1 ten descriptors gone, 44 remain | `intent.go`, `intent_delivery.go`, `intent_planning.go`, `intent_process.go`: ready, decide, resolve, recover, red, fleet, doctor, ui, fold, close descriptors and their alias-only handlers (`runIntentReady`, `runIntentRecover`, `runIntentFold`, `runIntentClose`, `runIntentUI`, `legacyUICall`, `runIntentSettingsCompatibility`) deleted; `compatibility`/`replacedBy` fields removed; `intent_coverage_test.go` now asserts exactly 44 names and descriptors; scratch `help all --json` returns 44 commands (35 workflow, 6 mixed, 3 administration). | conforms |
| VC-1 shared owners kept | `runIntentFleet`, `runIntentDoctor`, `runIntentRed`, `runIntentDecide`, `runIntentResolve`, `landReady`, `foldReview`, `closeChain`, `reviewUnit`, `waitUnit`, `runner.Continue` all still reached from status --machines, check, incidents, accept-risk, review --finding, land --queue-only, revise job, done job, review run, wait run, revise run. | conforms |
| VC-1 internal boundary | `main.go`: `help FAMILY`, `FAMILY --help`, `help internal` refuse with public guidance (exit 2, empty stdout, no root selected); `metasystem internal` and `internal FAMILY --help` print the maintainer usage with the `internal` prefix. Raw complete family calls still dispatch: `metasystem ui serve --listen … --ready-fd -1` reached `runUIServe` (refused only for the missing root, proving the route); `intentYieldsToLegacy` is unchanged for verb-bearing calls and the wait selectors. Bare family and unknown-verb calls (`goal`, `goal bogus`, `ui start`) now refuse as unknown commands before any effect. | conforms |
| Mapping rows 1-13 | Each retained form calls the owner the table names (verified by reading the handlers): land --queue-only → `runIntentQueueOnly`/`landReady`; accept-risk → `runIntentDecide` via subject inference; review --finding → `runIntentReviewDischarge`; repair goals/waits; incidents claim/close → `runIntentRed`; status --machines → `runIntentFleet`; check → `runIntentDoctor`; start/stop/status/restart ui → `uiTarget`/`runUIVerb`; revise job → `foldReview`; revise run → `runIntentReviseRun` (`runner.Continue{Resume, FollowUp}`); done job → `closeChain`. | conforms |
| VC-2 administration | `administrationUsage` is the one small field; `helpScope()` derives workflow/mixed/administration for text, JSON (`scope`, `administrationUsage`) and the Partner kit (`uitools.Command.Scope/AdministrationUsage`). enroll, settings, restart moved to the administration group; start, stop, status, check, repair, land keep their work forms in `usage` and list administrative forms separately. `help human` now walks the groups and ends with the administration section; `help agent` names it in the protocol instructions; `help administration --json` lists the three pure and six mixed commands. No runtime scope guessing, no authority text invented: the mixed note says "help COMMAND describes their inputs and authority". `kindAndTarget`/answer refusals use `allUsage()` so no form disappears from errors. | conforms |
| VC-2 forms match handlers | New usage lines have handlers: `show review REF` (`intent_goals.go:368`), `stop review REF` (`intent_process.go:485`), `wait review REF` (`intent_work.go:1072`), `status run RUN`, `review run RUN`, `wait run RUN`, `revise run RUN`, `done job J`. `legacyProcessCall` word list moved from `unit` to `run` so `status run RUN` reaches the intent, not the private flag-only status. | conforms |
| VC-3 floor retirement | `goal.EngineFloor`, `EngineFloorOf`, `newestEngineFloor`, `requireAbandonFleetFloor`, `ConfigureAbandon`, `configureAbandonFleetFloor`, `runGoalEngineFloor`, the `goal engine-floor` verb, `supervise/engine_floor.go` (single caller confirmed) and `settings compatibility` read/write removed. `Abandon` keeps human name, fresh enrolled proof, live-job refusal, dependent dispositions, goal-revision locks, publish and history. `root.go:380-385` still validates historic engine-floor lines; `recover.go` refuses to replay them with an explicit "retired" message (tested in `recover_test.go`). | conforms |
| VC-3 proof | `TestAbandonNeedsNoEngineHistoryInAnUnrelatedProject` runs on a non-Git root with and without a historical floor line: asserts no refusal text names floor/minimum-engine/compatibility/go-build.sh, proof and dependent refusals still fire, root history and the historic line are byte-identical, root re-parses clean. Both shell beds lose only the floor setup (`goal-cli-fixtures.sh:1096-1111`, `land-fixtures.sh:1699-1727`); the dependent refusal and every later landing assertion remain; no leftover `engine_commit`/`source_top` reference. Only floor-specific tests were removed; their entries left `testing.json` and the ratchet, the five new tests are registered. | conforms |
| VC-D3 producers | `read_admission.go` (`done job … [--evidence …]`), `refusal/register.go`, `lifecycle/result.go` and `serve.go`, `ui/act/act.go`, `httpd.go`, `intent_process.go:1303` (`start ui`), `intent_delivery.go` (`done job`, `revise job`, `review run`), `intent_work.go` (`revise run … --brief FILE`, `wait run`), skills, docs and the review-brief template all use current public syntax; the tree has no live `fold unit`, `review unit`, `ui restart`, `metasystem close`, `resolve`, `decide` spelling outside plans/records history. | conforms |
| Root's UI finding | `--listen` on start/restart ui and `--wait-seconds` on stop/restart ui flow through `uiOptions()` into `uiLifecycleFor` → the same `uiListen` validation and `uiLifecycleRun`; default 15 s; negative, non-numeric or overflowing values refuse before effects; `checkUIOptionsTarget` refuses both flags on any non-ui target (`start checkout --listen` verified). `start` does not accept `--wait-seconds` and `stop` does not accept `--listen` (parser refusal). The four family wrappers, `runUI`'s mixed parsing and `printUIResult` are gone; `runUIServe` keeps repo, metasystem-root, listen, ready-fd, the readiness-pipe refusal and stderr logging; the serve body is unchanged apart from indentation and the `listen` → `address` rename. | conforms |

## done job (checklist 2)

`runIntentDone` takes the job branch only for exactly `job J`; any `--dispositions`/`--evidence` on the goal form refuses, and the job branch refuses every other descriptor flag (reason, by, lineage, --id/--goal) before `selectRoot`. `dispatchJobID` is the review-job resolver lifted verbatim, so qualified `j1:`/`j2:` references, ambiguity and launches are handled the same way (launch refusal verified in `TestIntentDoneJobRefusesALaunch`). `closeChain` is unchanged except the option name: round-of-chain redirect now returns `done job ROOT`, already-closed is unchanged, `--evidence` is validated with `validIntentJobID` and forwarded as `--reconcile-evidence` to `dispatch.sh close`; writer preflight, critic dispositions and terminal-round checks are untouched. `TestIntentDoneJobCompletesOnlyTheJob` proves the goal file and publication count are unchanged and the owner argv is exact. Scratch runs: `done job x --reason y` and `done g --evidence r` refuse as designed. Returned continuations for a critic chain are `done job <critic> --dispositions FILE` (the retained close, not a new review), and for a reviewed job `review job <reviewed> --dispositions FILE`; no continuation manufactures a review of a critic.

## Nonmaterial notes (do not keep the loop open)

- N1 `main.go:809`: a raw private call carrying `--help` anywhere (`metasystem ui serve --help`, `metasystem up --help`) refuses as `unknown command "ui"`. The boundary is intended; the wording is misleading for a maintainer who knows the family exists. A one-line hint naming `metasystem internal` in that branch would be cheaper than the next question. WORKS and SAFE.
- N2 `intent_goals.go:804`: `done job` with no id is parsed as the goal named `job`; it then fails goal lookup rather than saying the job id is missing. WORKS and SAFE.
- N3 `land-fixtures.sh:1699`: `saved_config` is declared in the trimmed line and still used later in the function; fine, noted only because the diff makes it look orphaned.

## Limitations

- I did not run the Go test suites, `-race`, the shell beds, static checks or the kit/Partner rendering; root's logs under the evidence directory are its own observed outcomes, not something this review certifies.
- The scratch build ran help, refusals and routing outside a repository only; `done job`, `revise run`, `review run`, `wait run`, `stop review`, `show review` and the UI lifecycle were verified by reading their handlers and the new tests, not by driving a live installation.
- The stale-spelling census is a regex over the non-test tree excluding plans, records, artifacts, memory and docs/history; it is an upper bound, not a parse.
