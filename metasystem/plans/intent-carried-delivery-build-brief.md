Working Mode: implementation
Orchestrator Identity: root Codex
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| complete-carried-goal-delivery | 5000 |

You are rostered Opus5.5 implementing ONE independent missing owner composition
for the user's full intent redesign. User explicitly authorizes machinery bypass
and ignoring stale stop hooks. Root authors design; Fable critiqued twice4->2,
all findings joined and exact receipt/Advance corrections accepted. Full independent
Sol critique follows combined implementation. No claims of completed delivery yet.

Your checkout ../agentic-tools-intent-carried-20260926 is private unreviewed seed
05881caa339964f9162decd5fc6be2c59d920548. IMPORTANT its LOCAL carried design is the
older r2 draft. Read the ACCEPTED root-authored absolute primary documents supplied
as inputs: /Users/wido/LocalStorage/GitHub/agentic-tools-intent-workflows-20260926/
metasystem/plans/intent-carried-delivery-design.md and dispositions + final Fable
report. The primary accepted page's final composition order governs your work.
Do not edit either checkout's plans, instructions, goals, receipts or conf.local.

Implement the FULL real exceptional goal delivery, not just a staged-only flag.
Current adapter records word then land.sh always fails for missing paths; no owner
stages the canonical candidate. Reproducer already exists in
cmd/metasystem/intent_authority_positive_test.go, function
TestIntentLandExceptionCarriedGitAdapterReproducesTheTransportRefusal. It drives
actual candidate/carry/land.sh, with only fixture-only human proof appended after
asserting public argv. Its currently guessed intended-success assertion is red.
Use this actual bed and existing scripts/agents/land-fixtures.sh carried setup to
complete needed fixture prerequisites. Never fake a successful land callback or
manually mint a landed commit. May rename that one test to accepted
TestIntentCarriedGoalDeliveryGitAdapter and strengthen it; other seven authority
positive tests belong to returned proof author and must remain unchanged.

Scope:
- cmd/metasystem/intent_exception.go (existing adapter);
- new cmd/metasystem/intent_carried_test.go (or similarly exact carried-only name);
- ONLY carried test + its new helpers in intent_authority_positive_test.go;
- new internal/landing/carried_prepare.go and carried_prepare_test.go, or a similarly
  small named existing-owner extension. Existing landing files only if necessary
  for focused dependency injection or calling the existing receipt/Advance owner;
  do not rewrite carried transaction/coverage/authority policy.
- If a tiny delivery-owner seam is essential, report it and put proposed patch in
  ignored return; do not edit shared cmd intent_delivery.go while main Opus uses it.
NO other cmd files, testing.json, scripts, normal docs, configs, deps, commits/push/
pull or main checkout writes. Main Opus owns manual/planning/public help, register
new tests after root imports. Do not launch other agents or paid test providers.

Binding design choices (not open redesign):
1. PRIMARY resolution, root-specific mutex, whole Git TOP LEVEL apply; no projected
   tree checkout, no branch source mutation. Project endpoint and candidate, diff
   projected trees; derive patch from retained base/workspace tuple, no patch store.
2. Refresh code+canonical ledger BEFORE record/stage, real ReadCarryStatus gets
   returned ledger tip. local/origin/ledger recovery never stages. Existing origin
   contract stays; no silently created remote. Explicit replacement must not rejoin
   old same-workspace word. Channel word adopts only exact matching composition.
3. Fetched origin's product tree is expected base. Local main behind it can safely
   Advance BEFORE taking staging mutex; Advance owns that mutex itself. Recheck
   after reacquiring; no automatic divergent rebase. Missing actual tuple never
   becomes Diff(new main, old candidate), which could revert newer product.
4. Under LockBounded(LockPath(PRIMARY)), posture/drift/preflight then plain
   git apply --index --binary at top level; exact repeated staging is a no-op.
   No --3way, reset/clean rollback, global fake state or second registry.
5. Actual RECEIPT row required. ObserveReceiptLine names tracked Ledger; receipt.Add
   needs EXPLICIT File resolved from that path because CLI default is external
   state! Type implement/Outcome reworked/Verify skipped/Goal G, note CODE+opid and
   preparation for carried delivery. No verify=clean/proof/PENDING/shipped claim.
   First stage existing valid append and recheck (crash-after-append idempotence);
   only append if still missing, then stage and recheck. Preserve every prior row.
6. Release mutex -> unchanged PRIMARY land.sh --staged-only transaction. Existing
   proof, reservation, actual carried commit, consumed word and recovery remain
   decisive. Truthful public continuation under same word on failure. No owner
   guards weakened. Main+goal source/index and outside-prefix paths are test claims.

Required tests named in accepted design: real carried goal transport+receipt,
linked primary with existing append rows, replay failure before push (existing
METASYSTEM_LAND_FIXTURE_CRASH seam), old subject after source changes, fetched origin
consumption with NO stage, local-behind safe advance vs remote-product movement
replacement, dirty/unmerged/mismatched index and conflicting patch refusal, mutex
and exact staging repeat. Use realistic first-use boundary cases, no speculative
framework. Test implementation in Go; real Git only narrowly named adapter claims,
per-instance fakes for pure behavior. No weakening existing assertions/floors.

Run focused tests with METASYSTEM_TESTING_WORKERS=9; preserve inherited worker
allowance/caches. No full gate. Return actual commands/exits and complete changed
paths plus APIs, where real owner vs seam, remaining failures and a real CLI recipe
if possible. No completion claims from fixture callbacks. Budget180NEWtools or
40actual minutes; coherent checkpoint if required, do not guess exhaustion. Output
metasystem/artifacts/agents/intent-carried-delivery-return.md in YOUR checkout.
