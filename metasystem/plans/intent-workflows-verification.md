# Final verification of the intent-first workflows

Goal: verbs-match-intent. Root owns this assessment under the human's explicit
machinery bypass. This is a reviewable test decision, not a governed proof receipt,
new test selector or claim that a failed historical run passed.

## What the last complete run taught us

Immutable R2 (2f7fafcb5a97ce7bb4c8f53e4f92363f7cb3deff) selected 203 groups:
125 affected Go package groups, 40 other native groups and 38 shell sections.
The native run passed 139 groups but its overall 45-minute context expired; five
groups failed before that, two were cancelled, seven became unavailable and twelve
were not run. The separate full native race/coverage run observed all 12,672 expected
tests, with no missing or unexpected test; seven top-level failures made it red.
Thirty independent shell sections passed, five failed, and three required the
not-yet-passed fast static build. The red evidence remains intact.

Corrections are restricted to demonstrated public workflow defects, existing
fixture dependencies and contract expectations, plus meaningful coverage tests.
Coverage floors and verification assertions are preserved.

## Final execution decision

Freeze the complete corrected source and build its own stamped engine. Recompute
the protected testing contract from current main and that exact tree. No candidate
is substituted while a run is active.

1. Run the actual selected fast-static-build and its refusal-register prerequisite.
   They own formatting, vet, static analysis, build, policy audits and project records.
2. Run every remaining selected native group that is not an affected-package group.
   This preserves special build tags, explicit inventories, environment choices and
   cost checks rather than inferring their success from another run.
3. Run the existing full native race/coverage owner on the exact final source and
   check complete test observations. Audit its successful fresh merged counters
   against the unchanged baseline and complete package inventory.
4. Treat the 125 ordinary affected-package groups as covered by that final all-package
   run only after checking the actual final group definitions: Go, tests=all, no extra
   build tags or specialized environment, and package present in the measured native
   inventory. Source currently has no race-specific build constraints. Any mismatch
   requires the actual selected group to run separately. No synthetic GroupResult
   or proof certificate is written. This is root's explicit substitution judgment:
   the same tests run through the same native owner with race and coverage enabled.
5. Re-run the five failed shell sections and the three whose prerequisites were
   previously unmet. Retain the other thirty successful section results only after
   reviewing the correction diff against their actual bodies and consumers. A new
   affected body or consumer requires its own re-run; do not rely only on a broad
   script glob or on a guessed formal execution-identity match.
6. Drive the rebuilt public discovery and representative completion/recovery surface,
   then join the actual runtime evidence to each critical/high design obligation.
   Fable independently confirms the complete computed patch and accepted corrections.

The earlier metadata comparison did not reproduce full execution identities:
input/tool/argument digests matched but inherited environment digests differed.
It is not automatic reuse evidence and is not used as a matching receipt here.
This plan makes the semantic decision explicit instead of minting a match.

Budgets: final native groups and full race/coverage each retain a 45-minute overall
context; section bodies keep their existing per-run limits. Collect independent
failures as a batch. Do not add retries, longer fixture sleeps, weaker assertions,
coverage seeds or allowance changes to conceal a failure. A timed-out aggregate
is incomplete evidence and cannot authorize integration.

## Result

Accepted by root on 26 September 2026 after the third Fable implementation review
and the final required runs. All 203 selected requirements are covered by the
explicit composition below; original run identities remain intact.

- Immutable R4 commit `bcf1679ca371cfdded4f80a400827f71c49898c4`, tree
  `f0a58991da7a91986ec33c3c254c2d2658d8abb2`: full native race/coverage exits 0,
  with 12,742 observations (12,729 passes and 13 declared skips), zero missing or
  unexpected tests and no reruns. The separate complete coverage-floor audit
  exits 0; neither platform's baseline changed.
- All 125 ordinary affected-package selections satisfy the substitution conditions
  above and occur in that actual complete native inventory. Four incoming-main UI
  packages additionally pass fresh race/coverage (638 observations); the complete
  current coverage-floor audit passes with their fresh reports substituted.
- All 40 other native requirements have successful complete results: 14 rerun on
  R4 and 26 unchanged successful R3 groups retained after root's consumer review.
  Specialized tags, environment and cost checks retain their own real results.
- All 38 shell sections have successful results: six final R4 runs and 32 assessed
  earlier results. The final runs include real adoption, goal CLI, all 45 landing
  scenarios, shell/dependency and static-contract audits, and supervision/census.
- Rebuilt discovery passes 60 cases: nine starting verbs and 44 complete public
  task commands. All 49 test references in the 15 critical/high obligations join
  to actual passing runtime evidence. The two dispatcher-only cases retain their
  real R3 section passes; native skips are not relabelled as successful tests.
- Fable's third report confirms behavioral corrections and retains its original
  fix-first verdict for ICR-14's four help descriptions. Root accepts and verifies
  the exact literal-only correction with a fresh build, public discovery/coverage,
  Partner projection and rendered help. No fourth Fable review is claimed.

Evidence is under
`/Users/wido/LocalStorage/agentic-tools-evidence/intent-redesign-20260926/`:
`final-candidate-r4/coverage/native-result.json`, `coverage-floor.exit`,
`fast/native-result.json`, `standards/native-result.json`,
`sections-final-admitted/section-results.json`, `affected-eligibility.json`,
`retained-native-assessment.json`, `retained-section-assessment.json`, and
`obligation-runtime-join.json`; the final root composition is
`final-integration/acceptance-assessment.json`. Later deltas have their own evidence
in `icr14-help-correction/` and `main-merge-78ad66f48/`. The tracked three designs,
capability inventory and critique dispositions preserve the decisions in this
repository rather than relying on the separate evidence directory for the design.

### Completion assessment

The observable contract is complete: a person or agent can discover tasks, select
work, obtain a design, build or submit manual work, review, correct, wait and land,
including public recovery commands. Each behavior composes its named existing
execution, review, publication or authority owner; the matrices name those owners.
Failures and ambiguous selections remain explicit and preserve work. Retries retain
identity, finding decisions and bounded rounds; human-only acts still refuse agents.

This is root's semantic acceptance under the user's machinery bypass, not a
machine-issued governed certificate or a claim that every test ran on one final
tree. Fake model providers and declared authority fixtures prove their stated
boundaries; independent real Fable/Opus sessions provide the external review.
No live human authority was impersonated. Incoming frontend diagnostic checks used
the host Node version as stated below. ICR-12's explicit choice among multiple work
items and ICR-13's corrupt-record diagnosis remain documented safe boundaries,
not unmet completion obligations. Root is satisfied that another iteration would
add speculative scope rather than repair a demonstrated contract gap.

The receipt names the actual standalone delegates and the user-authorized bypass.
Its machine-validated skills field omits code-critique because that claim requires
a governed review job chain; the actual standalone Fable critique and root
adjudication remain fully recorded in the tracked report and dispositions.


## Final correction and incoming-main applicability

The final corrected source will include e5925c951, not just the UI design pages
previously merged from ade7984c4. Its functional overlap with goal acts changes
block/unblock to use the existing endpoint's tree/validation interfaces; default
Git behavior remains the same. Preserve its refusal anchor and testing-surface
addition during merge. Final full coverage must include all new main tests.

The five failed R2 sections pass on R3: agent-protocol, brain, dispatcher,
metasystem-audit and runtime-contract-audits. The R3-to-final product corrections
are public continuation construction, error propagation in carried adoption and
normal help; they do not change those shell consumers. The retained dispatcher
section includes the actual dispatch-b owner journeys; the only design-review
change since it ran deletes an unused field. Test-only Go fixture changes do not
enter the engine executed by these shell sections.

Root compared the actual validate-metasystem section bodies and their script
consumers. In addition to adoption, land and supervision/census whose fast-build
prerequisite was unmet, rerun shell-and-dependency-audits for the changed script
syntax, static-contract-audits for the amended skill/agent guidance, and goal-cli
fixtures for the newly merged block/unblock owner. Retain other successful R2
sections on their stated original tree: their script bodies and the protocol
owners they exercise are unchanged by these corrections. This is a scoped
semantic assessment, not a claim that the broad scripts/** identities match.

Special native consumers affected by final public corrections or incoming main
will be selected explicitly after merge; R3 results remain their own evidence.
The final race/coverage run proves all ordinary packages on the combined source.
Do not reinterpret a red native group as passed merely because one stale assertion
is now fixed; its final required coverage must be joined explicitly.

R3's independent native run is now terminal: 35 groups passed and three failed,
all with complete observations. command-interface-smoke and go-batchtest share
the same stale model-flag expectation; launch-standard has the duplicated old
refusal source-line expectation. No timeout or missing group remains. Correct
those exact expectations, then run these three consumers on the final source.
The ordinary unchanged special-group results retain their original R3 identity.


The final freeze additionally includes main 9e8f98108 (sitting/decisions UI),
so interface-standard must use its expanded project-package inventory. Final
coverage includes these new tests. Root's last typed finding-reader correction
only restores honest descriptive metadata for canonical synthetic failure identities;
actual public human-risk and reader fixtures pass with unchanged acceptance and
closure guards. Re-run dispatch-proof and authority native consumers with the
other named final groups; goal-cli section is already required. The UI changes
are incoming main, not independently reviewed verb implementation.


Fable round3 confirmed every behavior correction and left ICR-14: four public help
descriptions still name internal owners. Root's final delta changes only descriptor
string literals in intent.go and intent_planning.go. No handler, branch, flag, test,
authority, internal package or coverage baseline changes. Preserve the immutable
R4 runs; do not claim they ran on different bytes. Rebuilt public help, discovery,
coverage catalogue and real Partner fixtures verify these changed consumers. The
final acceptance joins those focused results with R4's runtime results. This is a
specific root semantic assessment under the existing machinery bypass, not an
execution-identity match or automatic reuse certificate.


The obligation join explicitly separates native skips from passing runtime proof:
TestIntentDesignReviewRealOwnerJourney and TestIntentDesignRetryRealOwnerJourney
skip outside their armed dispatcher bed. The real R3 dispatcher section ran both
successfully (28.35s and 209.51s, respectively, explicit PASS records), and its
original immutable result is retained. No native skip is represented as a pass.
All 49 named matrix test references resolve to actual functions; final acceptance
joins each to its observed runtime result or that specific dispatcher evidence.


## Latest main integration (78ad66f48)

Main's subsequent sitting-panel correction changes only internal/ui/project Go
behavior, the corresponding frontend source/bundle and records (including this
goal's current next step). Root read that correction and merged it without conflict.
Fresh race/coverage checks pass all 638 observations in ui/project, ui/httpd,
ui/uitools and ui/web; frontend type-check and 112 targeted tests pass in an isolated
copy of the exact incoming source. Those additional frontend checks used the host's
Node26.8.2; the unchanged incoming bundle declares its original Node24.21.0 build.
No dependency or bundle was changed by this work.

The full current coverage audit also passes: retain R4's actual package reports
for unchanged packages, replace all four rerun package reports with their exact
fresh Go whole-package summaries, then use the unchanged floor judge and complete
module inventory. The first raw timestamped-JSON input was refused by that judge's
strict input grammar; its refusal is retained. The final input copies real Go summary
lines, not reconstructed percentages or invented results. Package source/inventory
outside those four consumers is unchanged; no fresh whole-project run is claimed.
