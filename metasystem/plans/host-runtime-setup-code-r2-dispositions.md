# Host runtime setup: code review round 2 dispositions

Review: `host-setup-code-crit1-r2`, exact project tree
`b3dc920c97ca12de60b531e198246b8f595273b6`.
One material finding remains. Five are independently withdrawn.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| HOST-C1-001 | accepted | The existing fake hook runtime must retain its genuine native ancestry and fixture pin semantics. Native r2 fails the re-arm leg. A temporary fixture-only mapping to the real exact fake query passes the entire Claude bed (fake-fixture-diagnostic.json: exit 0, 198.36 sec), so the reviewer's claim that no fixture-only change can pass is contradicted by observation. A small existing-hook-owner branch for synthetic fake also covers the five related supervision legs and avoids duplicating shims; actual providers retain all-host discovery. No production foreign-runtime fallback or invented identity is authorized. | host-setup-recover1-r3; scripts/agents/supervision-hook.sh, existing native beds and final main proof |
| HOST-C1-002 | noted | Independently withdrawn: runtime-bearing template-holder stub matches the real query. The subsequent F1 main diagnostic executed every template-holder assertion successfully. | Retain correction and final native proof |
| HOST-C1-003 | noted | Independently withdrawn after real Bash 3.2 empty/populated-array reproductions; linked, copy and none argument forms are preserved. | Retain correction and complete adoption |
| HOST-C1-004 | noted | Independently withdrawn after none-mode scratch CLI check and shell normalization proof. | Retain correction and complete adoption |
| HOST-C1-005 | noted | Independently withdrawn after deterministic disappearance regression and strict hinted-missing/corrupt probes. | Retain custody regressions |
| HOST-C1-006 | noted | Independently withdrawn after real cosmetic rewrite/byte preservation and genuine timeout drift repair probes. | Retain semantic readiness regression |

The coordinator additionally found missing exact exclusions for the three new
non-refusal status labels in TestHCL03EveryCodeRowed during the adoption witness
Go gate. Round 3 must register those labels without changing the output contract
or weakening collection. The adoption guard failure was instead a coordinator
runner mistake: inherited METASYSTEM_BIN overrode the fixture's refusing stub.
Main must clear that override on the next run; no guard change is authorized.

This table joins every return finding; it does not declare the open material
finding fixed or close the separate design and hazard obligations.
