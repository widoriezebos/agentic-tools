Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal turn-verdict-hardening)
Date: 2026-09-02

# Goal

COMPLETE revision 3 of metasystem/plans/turn-verdict-hardening-design.md
(landed, in your worktree). The previous round folded Sol's five round-2
findings (register metasystem/records/misc/turn-verdict-hardening-critique-r2.md)
into sections 0 through 5 and then hit its time cap; sections 9, 10 and 11
still read as revision 2. Finish the document. Edit in place; diffBoundary is
that one file. Do NOT re-fold sections 1-5 — read them, trust them, and make
the back matter agree with them.

# What to write

1. Section 9 (asks and gaps): record the dispatching seat's DECISION on
   TVH-R2-SLICE1-HIDDEN-WRONG-ROOT-DEPENDENCY — slice 1 is SEQUENCED BEHIND
   goal supervision-hook-wrong-root landing first (its design
   metasystem/plans/supervision-hook-root-design.md is at revision 3, two
   folds from closure; the machine's single claim slot moves there next).
   Add or update gap rows for anything sections 1-5 now leave open.
2. Section 10 (slices and tests): rewrite the slice table so slice 1's row
   carries the wrong-root dependency explicitly, names which hook lines land
   on top of the root fix, and states the specimen claim honestly ("refuses
   all three at the verdict boundary; at the deployed Stop boundary once the
   root fix is in"); slice 2's separate dependency row becomes redundant —
   say so. Add the new tests the folds demand: the two-arc R3 fixture (H1, H2
   in arc A, g in arc B) and the exact-syntax Move check; the
   single-response fixture for F1, F2, F5 and a trap exit; the forged-lineage
   and cross-seat HUMANSTOP minting cases; the clock-at-entry and
   marker-lock-cap assertions. Then the ESTIMATE: Sol's declared gap says the
   207-line two-minute precedent was recovered prewritten work, not authored
   code, so the 85-builder-minute slice-1 figure is unsupported. Either state
   the estimate as unsupported and re-cut slice 1 so a DOUBLED estimate still
   fits one 120 cap plus one correction round, or ground it in jobs that
   authored their diff (name them from the job records under the agents
   artifacts directory). Every slice stays at most 240 reserved minutes.
3. Section 11 (self-grade): confidence, weakest claim, reject condition,
   restated against revision 3 — the Stop budget claim changed in section
   3.2, the R3 SET form changed in 1.2.1, and HUMANSTOP seat authority
   changed in 5.2; the reject condition must name the new fixtures.
4. Consistency pass over section 7 (the ladder) and section 8 (residuals)
   against sections 1-5; fix the revision header at the top so it says
   revision 3 and names both rounds. R-31: no benchmarks.

# Constraints

Wall-clock budget: 15 minutes — the back matter only. Design only; edit
nothing but the design file.

# Expected Return

Version-2 implementer JSON; diffBoundary exactly the design file.

# Gap Rule

stop and report a gap; never fill it silently.
