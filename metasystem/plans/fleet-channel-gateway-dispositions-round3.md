# fleet-channel-gateway — dispositions, critique round 3 (job fcg-design-cc1-r3)

Round 3 was the goal's failsafe round under R-60-m1's rule. Design
revision 4 (plans/fleet-channel-gateway-design.md) answers the ten
findings tagged `reopens-prose`, reviewed at main 7bcee9fb; the three
tagged `fixture-expressible` (FCG-C-12, FCG-C-22, FCG-C-23) become
obligation rows here — each names the fixture or unit case the build
must land, in FCG-EVIDENCE-12 — and the build starts without a fourth
round. One table per file, as `validate critique-closed` joins it.

| Finding id | Disposition | Reasoning and evidence | Amendment |
|---|---|---|---|
| FCG-C-16 | accepted | a confirming refetch that fails leaves the entry PUSHED (txn.go:654-675) and PushedBlocking refuses every other opid (txn.go:508-516, journal.go:423-437); no code path in the listener ran recovery, and the runner tick makes no goal recovery call (runner.go:95-140; the only caller is goalsync_verbs.go:498) | FCG-INBOX-02: Listen and `channel poll` call goal.RecoverWithPolicy(endpoint, dispatchcore.GoalRecoveryPolicy{Now}) before the first iteration, on a pushed-blocking refusal and each iteration while blocked; the wedge lasts at most one push deadline; log line `journal blocked by <opid>; recovery ran: <action>`; unit fixture FCG-12-BLOCKED-JOURNAL |
| FCG-C-11 | accepted | VerbRequest carries only Endpoint, Actor, Ulid, Now, ApprovedRef, ClaimEpoch (verbs.go:168-180) and Approve writes its own Intent.Args, so a requestForEntry `approve` case had nothing to rebuild from | FCG-ANSWER-11: the case is dropped; a dead approve entry terminalises rejected under the existing default (recover.go:403) when the listener's recovery call runs, and the approval intent standing on the question is the re-run, resolved by the duplicate-approval predicate; FCG-BUILD-13 step 1 changes no recovery code |
| FCG-C-21 | accepted | ApprovalRecord has no question (file.go:151-164); a later channel approval of the same goal through another question satisfied "At not before answer.at and Authority channel" and Approve would be skipped or doubled | FCG-ANSWER-11: the predicate is an `approve` History event with AuthorityOutcome VerifiedChannelAnswer and ChannelContext equal to the question id (file.go:301, validated at 604, parsed at 1368); unit test against a goal approved through another question's answer |
| FCG-C-14 | accepted | the legacy Answer has ULID and Opid but no lineage (question.go:29-39); re-deriving the opid from "same ULID, same actor" could mint a different opid and a second answer row | FCG-MIGRATE-10: migrate tests TrailerPresent(tip, legacy.Opid); present → phase recorded with no Answer call; absent → goal.Answer under a fresh own opid before the migrate Publish; FCG-12-MIGRATE gains both `matched` cases |
| FCG-C-15 | accepted | "approve 123456 note 123456": StripCode removes the last field and the copy in the middle is a fact to the validator, so the code was durable | FCG-SECRET-15: after StripCode the committing machine masks every remaining six-digit field (punctuation-trimmed) that VerifyTOTP accepts at sentAt as `[code]`, whatever the outcome; the validator's row is unchanged; unit cases for a repeated and a mid-sentence code and a six-digit fact |
| FCG-C-02 | accepted | step 2 as written sent no offset and added a Confirm call from the old Poll, so what Telegram was told changed before the cut-over; step 3 removed the append while the old Poll still called goal.Answer, so what binds changed | FCG-RECEIVE-03: Receive keeps sending a non-empty cursor as the offset (telegram.go:203-218 unchanged); the listener passes ""; the old Poll never calls Confirm. FCG-BUILD-13: four steps, WORD-07 folded into the cut-over |
| FCG-C-05 | accepted | the schema allowed `step` only for verified, late, replayed and the answer-state row assumed every verified record named a question; a verified reply with no token, or with two, had no legal record | FCG-INBOX-02: `outcome` and `question` are independent axes; `question` is a question id, `unbound` or `unmatched` for any outcome; `step` is non-null exactly for verified, late, replayed; the answer-state row applies only when `question` is a question id; FCG-COMMIT-05 restated |
| FCG-C-10 | accepted | DefaultPublishDeadline bounds only CAS-refused retries and git subprocesses cannot be cancelled; `steward restart` force-kills two seconds after SIGTERM (runner.go:478-492), so a 100 s grace never existed | FCG-POLL-04: no graceful-join promise and no `channel.stop-grace-sec`; the listener may die inside a Publish or before its Confirm, both already handled (re-delivery; the recovery call that is Listen's first act, after the runner's first tick); FCG-POST-08 names the unbounded git case behind the stale window |
| FCG-C-19 | accepted | the answer-state row refused closed/null, so `channel close`'s own transition produced a tree the validator refused, and a legacy question closed unanswered could not migrate | FCG-INBOX-02: closed/null is legal when closedBecause is not `answered`, closedAt, closedBy and closedBecause are required together; the close row sets all three; FCG-MIGRATE-10 gives a closed unanswered legacy question closedBecause "closed before the ledger inbox"; unit cases for the closed/null rows |
| FCG-C-20 | accepted | no orphan-post row; the answer row always ended recorded but non-budget answers were said to end approved; a `late` rejection on a closed question had no intent row; a list or silence post named no question | FCG-INBOX-02: the orphan-post row (FROM any); two answer rows (budget with tuple → recorded; every other → approved with receipt); the rejection intent FROM admits closed for reason `late`; a list post hangs on the newest open question, a silence post on the oldest, neither counted against the ceiling; the closure statement for the matrix |
| FCG-C-12 | accepted | `channel ask` had no failure point, so the asker's crash between post and ref — the case FCG-POST-08 argues about — was never staged | obligation: `channel ask` reads METASYSTEM_CHANNEL_FAIL_AT (`before-post:question`, `after-post:question`, FCG-POST-08); fixture FCG-12-ASK-CRASH in FCG-EVIDENCE-12 lands with build step 3 |
| FCG-C-22 | accepted | ResumeApprovalToken is five fields (verbs.go:34-37); a whole-field match could never bind the goal's real token | obligation: FCG-MATCH-06 (b) matches a contiguous field sequence equal to strings.Fields(wants) with trailing punctuation trimmed from the last matched field, the rule containsContiguousFields (verbs.go:96-108) already is; unit case in internal/channel with the five-field token followed by a full stop, lands with build step 3 |
| FCG-C-23 | accepted | `channel skip` named neither its fields nor whether it refetched, and the fake could not stage a publication refusal | obligation: FCG-RECEIVE-03 writes the verb out (Receive with empty cursor, the record built as the listener would with outcome `skipped`, the poison record replaced as the one overwriting write, Confirm, the printed line); fixture FCG-12-POISON-SKIP stages the poison with a trailer-less commit on the ledger branch and lands with build step 3 |
