# go-production-grade — critique round 1 dispositions

Critic: Codex (GPT-5.4, fresh thread, session 019ff27c-2998-7422-a412-279a92383cef), 2026-08-11. Designer/adjudicator: the main session that wrote the plan. Verdict line: 13 material findings, not ready. Every material code claim below was independently re-verified in this checkout before adjudication (marked "verified"); the round closes with 17/17 findings dispositioned, 0 refuted. Zero refutations is itself a datum: four of the material findings caught real errors in the plan's own evidence (R1-F12, R1-F13) or gaps its constraints could not satisfy (R1-F3, R1-F6), and the rest survived direct re-verification.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| R1-F1 | accept | Verified `internal/identity/custodian.go:54-56`: after a successful probe, a command lacking the tag returns Dead; an unreadable argv yields empty Argv under the darwin best-effort contract (`identity_darwin.go:53-59`), so unreadable reads as false Dead. The port would faithfully reproduce a safety defect. | New defect finding B1; Phase 1 gains an explicit behavior correction (argv-unreadable → Unknown) with focused tests, listed as a reserved-decision item. |
| R1-F2 | accept | Under `hidepid`, a live foreign process's stat read fails ENOENT — indistinguishable from death; Dead authorizes lock takeover (`internal/lock/lock.go:90,120`). "Consider detecting" is not a rule an implementer can build. | New defect finding B2; supported-platforms profile added; Phase 1 adds one-time restricted-procfs detection and supervision arming refuses it with a named error; the same-user-scope invariant is verified during implementation. |
| R1-F3 | accept | `registry.Record` keeps only recognized keys (`records.go:96` onward); `RecordCAS` accepts arbitrary keys (`dispatch/record.go:232-251`). The named exemplar cannot satisfy the phase's own preservation constraint. | Phase 4 rewritten: a lossless wire-document owner (typed known fields plus preserved raw remainder) and explicit CAS merge semantics; `ParseRecord` demoted to typed-parse precedent only. |
| R1-F4 | accept | Writers mutate maps then serialize with `json.Number`, sorted keys, two-space indent, no HTML escaping, trailing newline (`dispatch/record.go:284,313`); mirror hashes a normalized semantic record (`mirror.go:104,201`) that the shell asserts (`validate-metasystem.sh:2266`). Parse→reserialize goldens do not cover mutation paths. | Phase 4 verification now requires differential old-vs-new tests for every writer operation, with the enumerated hard cases (unknown nested keys, number spellings, null-vs-absent, HTML characters, terminal records). |
| R1-F5 | accept | Verified: `dispatch/mission.go:63-88` consults the fixture only after ps failure, restricted to a sole-fake-runtime checkout plus PGID agreement, env `METASYSTEM_FAKE_PROCESS_IDENTITY_FILE`; `mission/contract.go:1322-1350` layers `unix.Kill` + `census.Alive` + ps with fallback env `METASYSTEM_MISSION_PROCESS_IDENTITY_FILE` matching start+tag. The suite arms both independently. Consolidating as "the identical line" would break fixtures. | The ps cutover is removed from the mechanical cluster and becomes its own gated unit whose mini-design must name both contracts (env vars, fallback order, fake-runtime restriction, PGID, start+tag, error text), tests first. |
| R1-F6 | accept | `mission/measure.go:38,46` defines methods on `*contractDoc`; `mission/envelope.go:14,20` reads unexported fields; shared owners in `state.go:569`, `fence.go:39`, `ledger.go:436`, `anchor.go:22`. A one-file move cannot compile. | Phase 3a rewritten: full moved-file set (contract.go, measure.go, envelope.go), the four shared-owner decisions named, dependency direction fixed (mission imports contract, never the reverse), obligation matrix required before the move. |
| R1-F7 | accept | A rounded-down floor permits regression to the floor; nothing forces new or split packages into the check; darwin/linux file sets differ. The gate currently has no thresholds at all (`go-gate.sh:53`). | Phase 0c rewritten as a concrete policy: machine-readable floor file with exact coverprofile values, ≥ comparison, only-increase update procedure, exhaustive package join (gate fails on a package missing from the file), new-package rule, floors measured on the darwin gate host, negative test lowers real coverage. |
| R1-F8 | accept | Verified `scripts/adopt.sh:237-245`: builds only when `bin/metasystem` is missing, copies whatever exists unchecked, and the fallback build omits the commit stamp and any CGO pin. | Phase 0a expanded: one artifact-building owner; adoption verifies or rebuilds (stamp, CGO pin, GOOS/arch match, freshness) with an adoption-level acceptance check. |
| R1-F9 | accept | Production paths exec `git` (`mission/anchor.go:22`), `bash` (`mission/contract.go:858`), and repository scripts (`contract.go:1371`); the inherited "runs on scratch" claim conflates link portability with operational support. | New supported-platforms section: link portability vs operational profile split; the profile names procfs, git, bash, POSIX userland; acceptance runs on a supported-profile host. The correction is recorded here rather than editing the other stream's ledger. |
| R1-F10 | accept | Unbounded `git fetch` (`contract.go:1194`), unbounded fingerprint exec (`contract.go:1371`), context-free runner exec (`missionrunner/launch.go:33`) against a standard that requires explicit timeout/cancellation contracts (`design-principles.md:44-51`). No phase owned this. | New Phase 5 — bounded execution and cancellation — explicitly behavior-changing, with named surfaces, ownership, process-group termination, and hang tests. Later phases renumbered. |
| R1-F11 | accept | Phase 2 batched an admitted behavior-risk cutover into a "mechanical" cluster, against `refactor/SKILL.md:26` (high-blast-radius units gate immediately, never batched into ambiguity) and the obligation-matrix trigger for owner/boundary moves. | Phase 2 reduced to pure renames; the identity cutover and the vocabulary move are separate, immediately-gated units with obligation matrices; Phase 3's package extraction carries one too. |
| R1-F12 | accept | Reproduced the plan's own scan defect: `grep -rh` suppressed filenames, so the `grep -v _test` filter matched nothing — the "non-test" counts included tests. Correct non-test count at recount (2026-08-11): 672 total; missionrunner 152, mission 124, adapter 95, dispatch 78, registry 4. | D1 and the Phase 4 premise corrected; registry re-characterized (production code already typed and nearly map-free — a precedent, not a conversion target). |
| R1-F13 | accept | Verified `cmd/metasystem/supervise_owner.go:146-153`: `os.Exit(0)` inside the injected Sleep closure on TERM/INT. The plan's grep covered `internal/` only, then claimed "outside main". | "What already holds" corrected; the signal-exit contract is recorded; Phase 3 treats this function as a special unit (signal/teardown tests, or exemption). |
| R1-F14 | accept (record) | `capability/select_test.go:14` has a test `init` installing a deterministic clock. Non-material: no implementation change. | Claim qualified to "no non-test `init()`". |
| R1-F15 | accept (record) | Count drift from a moving tree plus unstated populations. Non-material: the judgment (no blanket error rewrites) is unchanged. | Counts refreshed with populations stated. |
| R1-F16 | accept (record) | The critic's environment was read-only; vet/test/race/cross-build claims are unverified there, not contradicted (they ran here 2026-08-11). | Assumptions now require fresh command evidence at the Phase 0 baseline recording, before implementation trusts any historical "ran it". |
| R1-F17 | accept (record) | Phases 0–6 vs a receipt saying "six-phase". Non-material. | The plan now counts itself honestly (phases 0–7 after the Phase 5 insertion) and notes the already-appended receipt as pre-loop history. |
