# go-production-grade — critique round 2 dispositions

Critic: Codex (same thread as round 1), 2026-08-11. Verdict line: 16 material findings, not ready. Round 2 attacked round 1's amendments, as briefed. All 16 material findings accepted, 0 refuted; the two checkable new code claims were re-verified in this checkout before adjudication (no stamp-printing verb exists; both atomic writers discard directory-sync errors). Round closes 18/18 dispositioned.

Loop-health note (design-critique skill, "expect the returns to diminish"): 14 of 16 material findings target round-1 edits rather than the original architecture. They were accepted because each left an implementer a genuine guess (truth tables, detector contracts, oracle mechanics, bootstrap rules) — but round 3 is instructed to weigh new findings against this signal, and the round-3 brief asks for a ready/not-ready judgment on the amended whole, not only new defects.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| R2-F1 | accept | Verified: no verb prints `BuildStamp` (grep of `cmd/metasystem`; the stamp exists only inside written artifacts, `supervise/disk.go:133`), so "verify the existing binary's stamp/freshness" is not implementable today, and a commit stamp cannot prove freshness against a dirty tree anyway. | Phase 0a rewritten: adoption **always rebuilds** for the target — no verify branch; both build sites carry the stamp and the CGO pin. |
| R2-F2 | accept | `custodian.go:29-47`: the fixture branch runs after the kernel-death veto and is authoritative when it has an entry; the plan's B1 text never said where `ArgvKnown` sits relative to that precedence. | B1 now carries the complete verdict table (kernel veto → fixture entry → kernel path with argv-evidence rules) in the plan text. |
| R2-F3 | accept | "Detect restricted procfs" had no mechanism, and the acceptance profile only exercised standard procfs — the detector would ship untested. | B2 specifies the contract: parse the `proc` entry's `hidepid=` option from `/proc/self/mounts` as a pure function; unit tests over fixture mount lines (0/1/2/4, `off`/`invisible` spellings, absent option); a wiring test proving arming refuses on a detected restriction. |
| R2-F4 | accept | Unit A's "or consolidates them explicitly" licensed a behavior change inside a plan that promises preservation everywhere outside the named behavior-changing phases. | The consolidation option is struck. Unit A preserves both contracts behind one owner, full stop; consolidation would be a separate, human-approved change. |
| R2-F5 | accept | The owner was left as "candidates", against the plan's own obligation-matrix standard; and only one of D3's four tables was dispositioned. | Decided in the plan: a new `internal/turn` package owns the shared turn vocabulary; all four D3 tables dispositioned (two move, two stay engine-internal and unexported if the matrix confirms the engine is their only consumer). |
| R2-F6 | accept | The "full moved set" omitted the test files (`mission/contract_test.go` and friends) and treats a hand list as complete when only the compiler can prove closure. | Phase 3a: the plan's list is the *seed*; the obligation matrix enumerates the compiler-verified closure including tests before any move. |
| R2-F7 | accept | Floors measured on darwin cannot gate a Linux run (different file sets); a package split changes the parent's percentage in either direction, which the only-increase rule cannot express. | Phase 0c: per-platform floor files (darwin now, linux seeded at Phase 1 acceptance); splits/renames re-seed affected entries at measured post-change values in the same commit with recorded rationale; only-increase applies between re-seeds. |
| R2-F8 | accept | The profile listed four distro families and two architectures as supported while Phase 1 verifies exactly one host. | Supported-platforms split into **verified** (macOS dev host + the Phase 1 acceptance host) and **expected-compatible** (unverified until a suite run lands on them); claims follow evidence. |
| R2-F9 | accept | `encoding/json` emits struct fields in declaration order; a typed struct plus remainder cannot reproduce the sorted-key canonical output (`record.go:313-325`) by direct marshaling. | Phase 5 (typed docs): the canonical writer renders typed fields into a map, overlays the remainder, and encodes the merged map — sortedness and `json.Number` spelling preserved by construction; structs are never marshaled directly onto the wire. |
| R2-F10 | accept | The current reader's accepted-input grammar (duplicate keys last-wins, trailing data, ill-typed known fields previously tolerated) was nowhere frozen; a stricter typed decoder would silently narrow it. | Phase 5 requires grammar-freeze tests capturing current reader semantics before conversion; the new reader must match them. |
| R2-F11 | accept | Differential proof was demanded with no oracle mechanism: after conversion the old writer no longer exists to diff against. | Phase 5, checkpoint 1: capture a golden corpus from the *current* writers (driven by existing tests and fixtures) and commit it before any conversion; differential = new writer vs corpus. |
| R2-F12 | accept | Phase 5 (bounded execution) restated the standard's requirements but deferred every concrete bound, owner, and observable to the implementer. | The hardening phase now requires a per-surface design table (surface, bound, bound's config owner, failure category, observable error, process-group kill semantics) agreed before implementation, plus per-surface hang tests. |
| R2-F13 | accept | B4 is a known production defect class, yet its fix was sequenced behind the largest, riskiest, partly-optional phase it does not depend on. | Phases reordered: production hardening (bounded execution + durable writes) is now Phase 4; typed documents Phase 5. Hardening-first also stabilizes error text before the typed-docs corpus is captured. |
| R2-F14 | accept | The plan claimed "pinned" for govulncheck while the verification section showed no pinned invocation at all. | Verification commands carry a pinned govulncheck line; Phase 0d records the exact version chosen at implementation and freezes it in the gate script. |
| R2-F15 | accept | Verified: `record.go:355-358` and `ledger.go:460-463` discard directory-sync errors (`_ = dir.Sync()`; the ledger even skips sync when the directory fails to open) while `atomicWriteText`'s comment claims the record "survives a crash exactly as written or not at all" (`record.go:328-330`). Documented durability the code does not deliver. | New defect finding B5; owned by Phase 4 (hardening): propagate directory-sync failures in both writers, align the comment with the delivered guarantee, unit-test the error paths. |
| R2-F16 | accept | Phase 7 deleted this plan — the only place the supported-platforms profile exists — with promotion left to judgment. | Phase 7: promoting the profile into `docs/project-rules.md` is unconditional and precedes deletion. |
| R2-F17 | accept (record) | Raw-map counts drifted again during the loop (moving tree). Non-material: counts are premise-scale, not contract. | The plan already re-derives counts at each phase start; figures stay dated. |
| R2-F18 | accept (record) | Closeout deletes the dispositions ledgers, which the critique skill says to retain verbatim for loop resumption. Non-material to implementation. | Phase 7 notes git history as the retention mechanism: ledgers are deleted from the tree only at ship, after lesson promotion, and remain recoverable at the recorded SHAs. |
