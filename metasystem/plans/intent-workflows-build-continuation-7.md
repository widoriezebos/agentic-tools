Working Mode: implementation
Orchestrator Identity: root Codex, main-1790272787-5030-13dde3
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| complete-public-workflows | 16000 |

Resume same original Opus context. Root fully read return6 and merged goal-only
main e3a86b67e. Planning fixtures/broad intent passed but actual dispatcher remains
unproved; do not repeat broad suites now. User authorizes full intent redesign and
machinery bypass, stale hooks ignored. No commits/pulls/pushes or root documents.

Finish remaining planning as actual working workflow, then manual implementation:
1. Real dispatch.sh follow_up design continuation and positive first critique
claim using existing authentic claim fixture. IP-C4/C5 MUST prove actual refreshed
subject, actual returned child, operation replay after lost response, intervening
round and goal revision, live refusal/death/cap with same chain. Existing dispatch-b
fixture already has real cap/loss model; use filtered normal wrapper. Do not invent
new fake owner and call that sufficient. No test-only gates weakened.
2. Complete clean design critique via public review design --dispositions. The
accepted design requires no close knowledge; only then hide close discovery while
retaining compatibility. No accepted-material-is-clean shortcut. Positive lifecycle
cancel/dead supervisor and a second-checkout author test, public proof reference
producer -> wait proof. Deliver disposable fake-provider CLI recipe root can drive.
3. Register TestDesignPublicationConflict under a minimal normal internal/project
Go group in testing.json (allowed within existing task). Register new tests without
removing incoming main coverage or changing any floors. This is necessary coverage,
not a new user policy decision.

Root's parallel authority proof returned seven real-owner positives and ONE actual
product failing test: TestIntentWaitGoalLandingGitAdapterObservesTheRealLanding.
Public land G succeeds with branch landing message Goal-Source/Goal-Fold/Goal-Last/
Landed-By; matchLandingMessage expects Goal-Item + Landing-Provenance + pass, so wait
never confirms. Correct existing landing event owner to recognize real verified
goal-branch landing evidence, preserving its specific-goal filter and returning
actual commit evidence. Do not mint false attestation/provenance just to satisfy
regex. Use existing VerifyLandedSeries ownership/avoiding import cycles or extend
actual landing message with TRUE existing provenance. Choose smallest source-grounded
owner composition; named test must fail before/pass after. Root will import the
positive tests once independent writer ends. Until then its test source is readable
in ../agentic-tools-intent-authority-proof-20260926 and may be copied only to ignored
scratch or run in external overlay; DO NOT edit those two files yourself.
Also repair short wait<=5s: boundedCaptureGrace should not produce fake transport
failure; support requested deadline with bounded capture, or concrete early input
refusal if owner genuinely cannot. Prefer usable short wait rather than arbitrary
minimum. Owner JSON rejection on stdout currently loses its actual reason in
ownerVerbResult; preserve it instead of guessed no-change claim.

Read publication state remains required: selection must distinguish actual review
attestation published/unpublished, not unit push or deletion of wording. Add actual
owner fact and coexistence test; keep TestNamedWorkSelection's meaningful states.

Parallel read owner is finishing fixes in separate checkout, ONLY unit_run.go /
read_sequence.go / standalone_read.go / standalone_read_test.go. Do not edit those.
Its API/return1 is primary artifacts/agents/read-owner-import metadata and side
artifacts/agents/manual-read-owner-return.md; root will integrate returned patch
under this exclusive boundary even while you edit disjoint files, then write
artifacts/agents/manual-read-owner-integrated.json. After the planning work, read
that marker once. If present, implement accepted FULL manual brief
plans/intent-manual-review-build-brief.md, all IM-C1..7. Public diagnostic start/show/
wait/stop uses StartRead/InspectRead/AdvanceRead/StopRead. Goal submission must stage
consistent paths under actual shared checkout lock -> UNCHANGED CommitStaged ->
branch push -> real read/whole closure/publication -> land. No CommitPatch shortcut,
second registry, numeric manual counter, stale whole-branch replay, token-as-lock,
--3way on persistent destination, destructive cleanup. Source=destination special
case and --after current unit commit replay are required. Named manual fixtures
are mandatory. If marker absent, complete remaining parent/planning proof first;
no fake backend. Root will not alter your CLI while you work.

Parallel test writer owns ONLY cmd intent_authority_positive_test.go and
intent_event_wait_owner_test.go. Its seven green positives cover coordinator,
floor, repair/migration and human event, so do not duplicate those. It adds question
wait now and may prepare carried fixture. Carried materialization is a CONFIRMED
production hole under root/Fable addendum: current landException never stages
candidate and invokes land.sh without --staged-only. CandidateOnly is PROJECTED
workspace tree (cannot read-tree it or erase registers). Do not patch this blindly;
root owns accepted extension and will delegate separately. Full carry stillrequired.

Keep broad steward installed-engine failures for final selected proof. Preserve
METASYSTEM_TESTING_WORKERS=9/inherited allowances. Root owns final immutable Sol
critique and exact-candidate diagnostic acceptance, no forged goal approval.
Up to200 NEW tools or40 actual minutes. Coherent return7 with actual exits, full
remaining gaps, owner vs seam proof and runnable public recipe. Output:
metasystem/artifacts/agents/intent-workflows-build-return-7.md.
