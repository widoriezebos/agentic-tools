Working Mode: design
Orchestrator Identity: m1 (lineage main-1788940932-18533-7fa6c2, coordinator under goal human-proof-fits-the-act)
Date: 2026-09-09

# Review brief: second critique of the human-proof design, on revision 2

FINDING IDS: chain-unique, continue the sequence: HPA-10, HPA-11, ...
never F-n and never a reused id. Report `round` as 1 in your return: it
is this job's own round; an earlier critic reported the chain's count
and its return failed validation on that field alone.

## What you are reading

`metasystem/plans/human-proof-fits-the-act-design.md`, revision 2,
revised in place on the Fable lane (chain human-proof-design2-20260909)
and landed at 7b6ac5e7e; its revision record at the end names each of
the nine first-round findings it folds. The first critic's register and
the orchestrator's dispositions (all nine accepted) landed beside it:
`metasystem/records/misc/human-proof-fits-the-act-critique-r1.md` and
`metasystem/records/misc/human-proof-fits-the-act-critique-r1-dispositions.md`.
The goal record is `metasystem/plans/goals/human-proof-fits-the-act.md`.

Write your register as a new file named
human-proof-fits-the-act-critique-r2.md in the records directory under
misc.

Read-only design critique; implement nothing, run no bed, and never run a
human verb with --by against the live ledger. Under R-60-m1 the loop
stops at the first round with no material finding: material only if two
implementers would build different things, or a claim the page rests on
that the tree does not support, or a decision that widens what an agent
may do. If nothing material remains, say so; that closes the design and
slice 2 builds.

## Settled, do not re-derive

The two-grade shape; the nine round-one dispositions; decision 5's
deferral of the stopped-claim quota predicate to goal
breach-stop-wedges-seat (m1d, building now); the TOTP idea stays out
(Wido postponed it).

## Mandate, in order of consequence

1. **The inventory is complete and closes HPA-01.** Section 1's matrix
   claims to name every human-only entry point in the tree. Enumerate
   independently from the code (the goal verbs in
   metasystem/cmd/metasystem/goalsync_mutations.go and
   metasystem/cmd/metasystem/goalsync_verbs.go, the brain verbs, session
   stop, the process verbs, the steward verbs, the mission runner in
   metasystem/internal/missionrunner/resolve.go) and name any entry point
   the matrix omits, and any matrix row whose "where the proof is
   checked" does not match the code.
2. **The revision's own two readings, flagged in its return.** (a) It
   grades arm as enrolled by keeping the classifier walk as the first
   gate and adding the enrolled-terminal proof after it; judge whether
   that reading, or the classifier alone, is what the goal's rule wants
   for a reopening act, and say which. (b) It graded mission start and
   mission resume under a closed stop fence as enrolled by the same
   reopening rule, an extension neither the brief nor the first critique
   named; confirm or refute that they reopen execution.
3. **Set-arc and the arc cascades.** The page grades set-arc by the state
   it ends in and claims, from reading, that no cascade beyond the branch
   at metasystem/internal/goal/verbs.go line 2502 reaches a claim-creating
   state under a human actor. Confirm the enumeration from the code.
4. **The authorityGrade stamp on intent arguments.** The page says that
   is the one change to journal intent arguments and that no renderer
   prints them; confirm by tracing the renderers of history lines, not
   only recover.go.
5. **Section 7's red-today claims.** Every canary's "red on the untouched
   tree" statement comes from reading; for each, say whether the reading
   holds, and name any that would be green today (the first critic found
   two such in revision 1).
6. **What must remain impossible.** After revision 2, can an agent, with
   or without --by and with any lineage exported, pass either grade
   anywhere? Name the route if so.

## Constraints

Wall-clock budget: 40 minutes. Return per the design-critic schema with
findings sorted by materiality, each naming the section and the
artifact it changes. Stop adding at a gap that needs a decision no page
has made; report it with the resolution you propose.
