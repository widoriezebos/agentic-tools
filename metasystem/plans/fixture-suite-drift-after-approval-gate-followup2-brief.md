Working Mode: build
Orchestrator Identity: m2+main-1788441779-14484-82d6ed (dispatch delegate under goal fixture-suite-drift-after-approval-gate)
Date: 2026-09-04

# Follow-up: the choice is the converted goal state

Decision on your gap: provision the lawful synced goal state in the fixture's agent repository, the way the product converts a repository, so the serving-goal leg opens its goal with `--tier 3` through the current parser. Do not restore the tierless invocation: every backlog item carries a tier now (R-54-m1), and a fixture that exercises the legacy parser tests a path the product has left. Keep the private engine build and the engine-variable routing you already made. Every path in your return is relative to the repository root (starting with `metasystem/`). The orchestrator reruns the suites seat-side.
