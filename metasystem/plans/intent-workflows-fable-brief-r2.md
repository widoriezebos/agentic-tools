# Fable critique: intent workflows, final design round

Kind: critique. Round 2 of 2 on provider session d55cdac2-eac6-4f23-874c-e5e0bbd137d8.
Failsafe round: 2. Budget 36 tool calls / 12 minutes; report ceiling 1800 words.
Root folded all six material findings; disposition record is
metasystem/plans/intent-workflows-design-dispositions.md. Read it and the FULL
revised design metasystem/plans/designs/intent-workflows.md. The capability map
metasystem/plans/intent-workflows-capabilities.md accounts for all 48 old commands
and 13 advanced outcomes. No implementation has started. User's complete scope
and machinery bypass remain as in round 1. No secrets, model launches, commits,
goal changes or product edits. Only write the review report below.

First run the same project design-of resolver and verify the new record exists.
Criteria remain:
> Would an implementer working from this design build **step 1** DIFFERENT, or WRONG, because of this finding?
> Does step 1 WORK, and is it SAFE, without this finding?
Usability failures against Wido's explicit no-internals/full-capability requirement
are real contract failures. Root remains responsible for the full release beyond
the first buildable slice. Ordinary decisions are not bookkeeping to auto-accept.

Confirm each IW-C1..C6 correction, especially root's alternative remedies:
- Failed revision never silently spends again on replay. --after N is an explicit
  public work attempt, which exists even if no result was produced. Matching prior
  requests rejoin before current-attempt inference; a deliberately new after-N
  correction is recorded under the same owner locks.
- Per-command relevant sets and completed/no-candidate behavior are now stated.
- Accepted unresolved material findings require revise, never premature closure.
- Queue-only is exactly existing land-ready, no tests/read collection/push, and
  ordinary land keeps its route's existing handover order.
- Bound generated disposition files cannot decide a changed current review.
- Close refusal classes have concrete public remedies. Actual failed-review retry
  is explicitly an extension of branch-read and dispatch follow-up, not a claim
  that every terminal failure is currently retryable. Retry uses the displayed
  failed examination number, preserves the same chain cap and proves the old
  process stopped; repeated retry-N attaches to its one recorded attempt.
- Fixture/session plumbing flags remain compatibility inputs but not discovery.

Attack remaining shape-level defects. If any residual is only a mechanical
choice with a falling trajectory, give an exact fixture obligation rather than
another prose loop. Name genuinely unexamined scope, particularly human operations,
question auth and exceptional landing; do not certify what you did not inspect.
Write full report to metasystem/plans/intent-workflows-fable-review-r2.md, with
exact design SHA256, actual model, material count, existing/new stable finding IDs,
criterion answers, concrete evidence and smallest correction. Return path/verdict.
