# Application testing design: round-one dispositions

Read the complete canonical return for application-testing-crit1, reviewed commit bdce3d4d1fc1396eeffa0e09eeaa97e8826f4197. Native register advancement succeeded. The register retains four severe material findings; two scope demotions do not erase the six findings or substitute for this adjudication. All runtime obligations remain unproved.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| ATC-R1-001 | refuted | The metadata omission is real and was main's preparation error. The claimed stale-proof outcome assumes that the critic's declaredOutputs is an implementation allowlist. Exact search `rg -n 'declaredOutputs' internal --glob '*.go'` returns record construction/storage, finding-register subject classification and tests; it has no implementation or landing consumer. `finding_register.go:734-750` reads the list into the critic subject path set. The design already explicitly names the omitted owners, and the implementer receives its own complete brief/diff boundary. Thus correcting this metadata does not change what is built, and a frozen historical critic record must not be rewritten or replaced to hide it. | Clarified the complete implementation boundary before section 12, including fully qualified omitted consumers and the repository-local rule. The implementation handoff must enumerate all of them. Retain the original review metadata and use the declared design document as the artifact for design-level amendments. |
| ATC-R1-002 | accepted | Retained BuildProofIdentity calls CompleteToolchainIdentityAt unconditionally. Per-group descriptors alone did not remove this top-level Go requirement. A prebuilt engine serving a non-Go app must admit and verify its native command without Go installed. | Section 6 defines BuildProofIdentityForContext without external Go commands; only the Go adapter resolves Go identity. Legacy schema-1 entry remains explicit. Generic structured evidence replaces mandatory legacy Go coverage for schema 2. TEST-01/04 include a no-Go native command proof and a Go-group refusal. |
| ATC-R1-003 | accepted | The original schema gave inputs and reports but no enforceable mutable-output ownership. Generic roots could either hash their own outputs or hide real tracked bin/artifacts inputs. | Section 4 now declares outputs, tools and external inputs; enforces private output containment, empty initial reports, tracked-input protection, symlink containment and post-execution input integrity; and distinguishes nested installation state from application paths. TEST-01 includes output creation with a tracked bin input. |
| ATC-R1-004 | accepted | The prescribed standard package floor implied all tests in the two large mixed packages. Their retained durations contradict a focused standard path. Named subsets must not falsely claim full package coverage. | Concrete standard groups now name source-inspected goal budget and mission decision/lease assertions with coverage disabled. Relevant new regression/consumer assertions remain required. Full package coverage retains its floor in cadence and coverage-sensitive deep; ordinary critical changes run their relevant deep specimens. TEST-03 verifies standard never expands these mixed packages to all tests. |
| ATC-R1-005 | accepted | Source-coupled trusted-base tests could fail on a lawful internal API refactor, and the proposed unspecified migration route was an avoidable new block. The required negative behaviors are visible through the public CLI/result boundary. | Section 8 replaces copied Go test source with base-controlled black-box fixture data and stable expected v1 CLI/result decisions, driven by the existing authenticated proof worker in the prebuilt base engine. Internal API changes no longer affect it. The public v1 contract remains supported; a future major-version migration is outside this feature, with no new bypass/approval protocol. TEST-12 names the internal-refactor negative. |
| ATC-R1-006 | accepted | The command/JUnit adapter serves the actual common-interface requirement and can run a Maven wrapper. No first-delivery consumer requires a separate reactor/dependency resolver. Native scope demotion does not make that unnecessary implementation worthwhile. | Removed the compiled Maven provider and phase-subsumption inference. Kept a Maven command configuration example and the generic argv/JUnit guard fixtures. TEST-04 and slice 2 now use only Go, section and command adapters. |

The same pass incorporated peer source fe7212a9, fetched after round one started: migrate the additional fixed-command checks in validate/recertification.go; preserve the recertified route's one-push frozen-target behavior; use whole-project trees in its actual nested post-commit wrapper; retain current mirror and coverage-floor inputs. Main integrated source through 2ab10814 without conflicts or lost records, ran the focused disjoint-merge proof with own exit zero in 4.970 seconds, and activated only that landed engine at generation 30. These are integration facts, not certification of the expanded feature.

Removed the proposed feature-specific public cost-comparison profile; cost acceptance remains a Go evidence helper using retained results. No product implementation or broad battery was run for these design amendments. Continue the same critic chain and stop at the first zero-material round; do not open a fresh chain to repair metadata or polish prose.

The revised contract continues at plans/coordinator-loop-prevention-testing-design.md. The original application-testing-contract-design.md draft is preserved unchanged because the normal carriage owner only recognizes the held goal's filename prefix for existing plans. This is the same design/critique history, not a new scope, new critic budget or native-record rewrite. Main does not claim the initial draft itself is now corrected or independently closed.

Main subsequently challenged the test carving at Wido's request. The concrete gap was that a delivery plan could launch expensive groups without a canary barrier and continue launching after a decisive failure. Sections 3/6 now require automatic ordered canary/standard/deep barriers, preserve cheap canaries during deduplication, stop later delivery launches, and retain selected unstarted groups as not-run. TEST-03 names the zero-later-launch negative. Section 9 and TEST-19 count complete command costs, expose phase overhead, and include an actual MetaSystem focused selection. These are changes to execution and acceptance, not another mode or workflow language. The same critic follow-up must challenge whether mandatory configuration and recurring selection remain proportionate to ordinary applications.
