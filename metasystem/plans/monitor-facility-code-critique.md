Verdict: REVISE. I found 11 material defects: 4 critical, 6 high, and 1 medium. These are implementation failures against the ratified matrix, not style concerns.

1. **CRITICAL — MON-06: the takeover sweep is neither serialized nor proof-or-refuse.** [`cleanupStaleRuns`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/sweep.go:69) reads outside the runs lock, uses one argv-nonce test for every custody mode, sends `SIGTERM`, calls `Assess` once, and succeeds without checking that anything transitioned. Because termination is asynchronous, `Assess` can still see the leader alive, yet takeover writes its completion stamp ([claim.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/claim.go:221)). It neither performs bounded drain nor forces `ended-unknown`. An adopted-unverified group can also put the record’s readable nonce in any member’s argv and become signal-authorized—explicitly forbidden.

2. **CRITICAL — MON-06: the required in-lock lease epoch recheck is absent.** Authorization happens in [`runCaller`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/run.go:24), before the runs lock. Inside [`Launch`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/verbs.go:50) and the other mutations, `ClaimEpoch` is only copied into records; it is never rechecked. A classified holder child can stall, its main can die and lose the lease, and the child can subsequently acquire the runs lock and mutate as the old epoch.

3. **CRITICAL — FIX-R6-04: the green cursor can permanently skip a green terminal.** `report.Scan` reads records outside the verdict flock, and [`TurnVerdict`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/goal/turnverdict.go:161) consumes that stale `ScanResult` without the promised re-read. A scan can read run A before it becomes green sequence 10, then observe run B after it becomes green sequence 11; [`decideGreens`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/goal/turnverdict.go:259) advances to 11, permanently suppressing A. The terminal counter also silently resets to 1 when malformed ([internal/run/run.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/run.go:210)), allowing duplicate sequences. The named test only supplies a handcrafted prefix-consistent `ScanResult`.

4. **CRITICAL — MON-04: waiters do not reliably terminate on their target lifecycle.** Delegate jobs have four terminal statuses, but [`JobWatch`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/watch.go:32) handles only `completed` and `failed`; `timeout` and `cancelled` block forever. Run watch records a generation/nonce target but never compares subsequent records with it ([waiter.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/waiter.go:195)), so rapid id reuse can make it report a different lifecycle. Removal compares only PID, not start time or target ([waiter.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/waiter.go:153)), contrary to identity-checked compare-and-delete.

5. **HIGH — MON-07: `Supervised` is not tied to the armed watcher.** [`readRunsPass`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/report/scan.go:242) never reads `state.json`, never matches its current watcher identity, hard-codes ten minutes instead of twice the loaded interval, and accepts arbitrarily future timestamps while the named process remains alive. Moreover, `Supervised` is assigned but never consumed by the verdict, so the required “supervision is not scanning your runs” warning cannot appear. The test itself creates no armed state and expects supervision to be true, certifying the weaker behavior.

6. **HIGH — MON-03: draining PGID custody is unbounded.** Census treats every draining record as custody solely by PGID ([internal/census/run.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/census/run.go:269)). `loadRunOwners` never checks `endedAt`, `windDownMin`, or current time ([internal/census/run.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/census/run.go:295)). If the watcher stops, a draining record can claim a reused process group indefinitely rather than for at most 120 minutes.

7. **HIGH — MON-02/MON-11: log evidence can be redirected through symlinks.** [`resolveLog`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/verbs.go:343) resolves only the directory, not the final file, and no read-time comparison exists. [`readTail`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/conclude.go:274) follows whatever the path names later. An adopted-pattern run can therefore retarget a symlink after registration and obtain green from another file. The `/tmp` check also uses an unseparated `os.TempDir()` prefix, admitting siblings such as `/tmp-escape`.

8. **HIGH — MON-03/MON-06: identity proofs are not preserved across bind/adopt/emptiness checks.** [`Bind`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/verbs.go:91) records the supplied PGID without comparing it to the leader’s kernel PGID. [`Adopt`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/verbs.go:259) never downgrades an old `adopted-verified` label when the new generation fails kinship. [`groupEmpty`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/verbs.go:390) ignores every per-process `Getpgid` error and can call the group provably empty when membership was actually unreadable, authorizing adoption or terminalization across an unknown.

9. **HIGH — MON-04/MON-11: structurally illegal records are treated as valid input.** [`Read`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/run.go:227) and [`List`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/waiter.go:227) only unmarshal JSON; neither invokes `Validate`. A JSON object with `status:"running"` and null identity reaches an unconditional pointer dereference in [`assessRunning`](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/conclude.go:108), taking down the standing watcher. An unknown status makes `run watch` block forever instead of returning malformed-record code 4, and invalid custody can enter census as adopted. `List` also returns filename order rather than the required `startedAt` order.

10. **HIGH — MON-01/MON-10: launcher failures do not become launch-failed, and fenced failures cannot be pruned.** After reserving the record, executable lookup or wrapper spawn failure simply returns ([cmd/metasystem/run.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/run.go:90)); the record remains launching until a watcher happens to apply the two-minute fence. Fence terminalization does not stamp `endedAt` ([conclude.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/conclude.go:250)), while prune explicitly skips records without it ([verbs.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/run/verbs.go:323)). Acknowledged launch failures therefore never age out through the promised prune path.

11. **MEDIUM — MON-09: the event proof is false and `run-swept` is dropped in production.** The registry nests `runId` under an invalid `run` key and declares closed enums as unrestricted strings ([event-registry.json](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/event-registry.json:594)). The test merely checks event-name substrings and emits every row using a synthetic `run` emitter ([run_conformance_test.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/events/run_conformance_test.go:14)). Production sweep uses the `lease` emitter, which the registry rejects, and the command-layer run emitter leaves `pidStartedAt` as zero ([cmd/metasystem/run.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/run.go:369)).

Evidence level: checked against the computed `aff8e37..04a1bbe` tree and traced through the named tests. I attempted the focused Go packages, but the read-only sandbox refused creation of the Go work directory, so I did not independently rerun the green host gates. No files were changed.

Proposed review receipt, not written: `type=review|outcome=reworked|skills=code-critique|verify=read-only-code-grounding+go-test-blocked-read-only|corrections=0|stop_loss=no|delegate=none|note=monitor-facility implementation critique found 11 material findings`

REVISE: the sweep, authority fence, waiter, attestation, custody, and green-cursor paths do not uphold the design’s proof boundaries.
