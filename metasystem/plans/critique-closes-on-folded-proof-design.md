# critique-closes-on-folded-proof

- Owner: m1e (goal 16 of plans/delivery-efficiency-plan.md), claimed for the design. **Revision 3, 2026-09-13**, written on the seat (Fable) under Wido's lanes after his word on section 11 (option (a)): this page is narrowed to the rules he has ruled on, no read of a subject already read clean and the close on a prior clean read, design critique failing safe at round two, and the measure; the certification record (rule 1 of the DONE line) moves to goal certification-closes-a-folded-bounded-finding, opened in his name, whose page starts from the two read records of this one.
- Goal and current status: revision 3 (narrowed to the buildable rules, section 11) with build specs 9b and 9c. Slice 0 landed 4fbd271c; slice 1 part 1a-i landed 20eecaf0; parts 1a-ii (units a, b, c) and 1b are not built.
- In flight right now: slice 1 part 1a-i (subjects computed before claim and published after it wins, SUBJECT_MISMATCH, the fold-time binding, the closure on the critic root) carried onto the tip, green in full on four packages and the fast gate, under its Opus read; worktree .claude/worktrees/ccf-s1ai-tip.
- Decisions made (and who made them): Wido, R-97-m1e (both rules; escalation over a third round). Wido, R-60-m1 (stop at the first round with no material finding; findings that fail the artifact test are demoted). Wido, R-42-m0 (three rounds is the ceiling). Wido, D81 (2026-08-16: two prose budgets spent without convergence exit to implementation behind fixtures, never a third prose budget). Wido, 2026-09-13 ("I agree with all your proposals"): option (a) of section 11. The seat: the mechanisms of sections 3 to 5.
- Waiting on the human: nothing.
- Dead ends (do not retry without new evidence): a path-keyed boundary and a "restoration" equivalence on reversed hunks (revision 1); textual hunks as a certification unit (revision 2); both belong to the certification goal now. A read subject shared by design, live and commit critics (revision 2, refused: the three read different things).
- Next step: part 1a is complete (4fbd271c, 20eecaf0, 1bbbfe78, 8e92a796, 971c7496) and unit 1b-i lands with this line. Next is unit 1b-ii from artifacts/reports/codex-ccf-slice1b-brief.md: the command and shell admission paths, the structured result handoff, the fixture legs and the CLI tests, built by Codex gpt-5.6-sol, read once by Opus, verified by the seat with the beds section 9c.6 names, and landed. After that the goal's own proof and conclusion.

## 1. What the audit found and what exists

The five-day audit: critique yield 68 percent by rounds; 26 percent of dispositions reported zero material findings; 137 fold-critique rounds cost 45.5 delegate hours; only 1 of 13 design chains ended on the skill's own stop rule. The pattern behind the cost: a critic reads a tree, finds bounded things, the implementer folds them, and the chain buys another whole read of the folded tree to learn what a test already proves; and a design loop that has stopped finding anything material keeps a third round on the calendar because nothing refuses it.

What exists (references to the tip of 2026-09-13):

- The finding register on the critic chain's root record (internal/dispatch/finding_register.go): `job critique-register-advance` folds each round (material findings pass the artifact test or are demoted; a `failed` round folds a synthetic unproven finding); `reviewRoundLimit` is frozen at dispatch from the goal's tier (build.go) under `metasystem.budget.review-round-max=3`; `job critique-register-close` is the four-row close table of plans/severity-tiered-rigor-design.md (a severe or unproven finding blocks until `goal accept-risk`; an empty unresolved set closes; bounded findings with budget left say "dispatch the next round"; bounded findings with the budget spent are deferred as review obligations on the goal). Exhaustion is checked before any successor record exists (dispatch.sh; `cap-exhausted-human-raise`, exit 10). Register entries carry `status` (open, disputed, resolved, deferred, accepted-risk) and `resolution` (withdrawn, out-of-scope, deferred, accepted-risk).
- The final-tree gate (internal/validate/conformance.go): `validate conformance --stage review` writes the immutable round artifacts `rounds/N/diff.patch` and `rounds/N/review.json` (`reviewedTree`, the project subtree); `--stage merge` refuses a critic whose `reviewedTree` is not the final project tree and issues the merge authorization on it. The schema-2 testing result's `candidateTree` is the whole-repository tree; landing validates its attempt owners (internal/landing/testing.go) and, after a base move, replays the certified patch and checks the post-image (`bindCertifiedChange`).
- The hazard close (internal/dispatch/hazard.go, `validateIndependentCritiqueReference`): the stamped critic must review the final work round and end at or after it; the stamp is written for a fresh critic dispatch only (review_reference.go), so a follow-up critic round that did review the terminal round cannot move it; `job review-reference-reconcile` is the repair.
- Landing (internal/landing/observe.go): a chain lands only closed, and the certified output is the closed critic's `reviewedTree`.
- The design-critique skill states the stop rule and the five-condition "fixtures as arbiter" exit in prose; nothing in code counts fold-read cycles, records a finding's grain, or refuses a redundant read. Metrics count critique rounds per chain; a landing's chain is on its commit trailer (`Landing-Provenance: chain=`), which the metrics loader does not read.

## 2. The rule, in one paragraph

A critic's read is bound to a read subject: the exact thing it read, typed by the kind of critic. After the read, a chain closes in one of two ways here: the read was clean, or the findings were folded and a new read of the folded subject is clean; a read is never repeated on a subject already read clean, and a chain whose terminal subject equals a subject a critic already read clean closes on that read through one closure object every gate consumes. A design loop closes at round two: a clean second round closes it, a mechanical residue on a falling trajectory becomes fixture obligations the implementation must discharge under its own critique, and a residue that is not mechanical or not falling goes to the human. No fourth round of anything. Closing a folded bounded finding without a new read, through a certification record, is the certification goal's rule, not this page's.

## 3. Decision 1: a read subject, and no read of a subject already read clean

A read subject is a typed identity the ENGINE computes and persists before the critic launches (never reconstructed by the critic), written to the critic round's `review.json` and copied into its return by reference:

- design critic: `{kind: design, designPath, contentDigest, reviewedCommit, declaredOutputsDigest}`;
- live code critic: `{kind: live, implementerRoot, reviewedMember, boundaryBaseProjectTree, reviewedProjectTree, immutableDiffDigest}`, computed at dispatch after any rebase, from the reviewed member's immutable `diff.patch` and `review.json` (validate conformance --stage review), so a follow-up whose base moved gets a new subject;
- commit critic: `{kind: commit, commit, selectedParent, tree, diffDigest}`.

Two subjects are equal only when their kind and every field agree; a live subject never equals a commit subject. At critic dispatch (fresh or follow-up), before any record exists, the dispatcher computes the subject the critic would read and refuses `REDUNDANT_READ` when a folded round of any critic chain reviewing the same implementer chain (or the same design path) read that exact subject and its register, folded through that round, held no open or disputed finding. The refusal names the round, the subject and the rule. An empty follow-up diff is the same case, the subject being unchanged. There is no restoration rule.

The refusal is durable: `artifacts/agents/<implementer root>/reads-refused.jsonl`, one line per event with a stable id (`<root>-<round>-<nonce>`), written under the finding-register lock by the dispatcher and mirrored under the chain's manifest with the round evidence (internal/dispatch/mirror.go learns the file), deduplicated by id when loaded.

The chain then closes on the prior read. `job critique-register-close` writes ONE canonical closure object on the implementation root, `closure: {criticRoot, round, subject, mechanism: "clean"}` (a certification, when its goal lands, adds `mechanism: "certified"` to the same object, never a second authority), and every gate reads that object: the hazard close (`validateIndependentCritiqueReference`) accepts the critic root the closure names when its latest folded round's subject equals the terminal work round's subject, whatever round the stamp `independentCritiqueJobRef` names; the stamp stays as an index that must equal the closure's critic root (a mismatch refuses by name); the final-tree gate (`mergeCritique`) and landing (`chainCertifiedOutput`) read the closure's subject for the reviewed tree. This is the absorbed stamp clause: a follow-up critic round that provably reviewed the terminal subject counts, and no stamp-only dispatch is ever needed. Bar (a) of plans/two-bars-for-changes-design.md is restated in the same landing as: a chain lands only on a tree a critic read clean, named by the closure object.

## 4. Decision 2: design critique fails safe at round two

- A design-critic chain's `reviewRoundLimit` is 2 at every tier where design critique exists (tier 3; tier 2 and tier 1 have no design critique, per the tier ladder): set in the common budget owner (internal/dispatch/build.go's resolution by role) and refused above 2 by `critique-budget-rebind` for the role. R-97-m1e narrows R-60-m1's tier-derived budget for this role only; tier eligibility is unchanged.
- The critic return schema moves to version 5 with a named validator and normalizer: every material finding carries `grain: mechanical|invariant` and, for mechanical, the artifact, the behaviour and the fixture that would prove it; a version-4 return (every historical return) normalizes to `grain: invariant` for its material findings, never mechanical. The critic root keeps `materialByRound[]`.
- Round 2 folded with no material finding closes the loop.
- Round 2 folded with only mechanical findings AND a falling trajectory (round 2's material count below round 1's): `job critique-register-close` defers each as one review obligation on the goal naming its fixture (`test: prove: <fixture>`), the design chain closes with them open, and the implementation that follows discharges each against the chain, the artifact and the test; that implementation runs under its own code critique because its goal is tier 3 (critique-always). The page header records the exit ("closed at round 2 on N fixture obligations").
- Round 2 folded with an invariant finding, with a severe or unproven one, or with a trajectory that is not falling: the close refuses with `cap-exhausted-human-raise` naming the findings and the human's two verbs (`goal accept-risk`, or a re-scope by `goal edit`), and round 3 is refused by the limit. Never a third automatic round.
- The skill's Round Budget section and docs/orchestration.md state the rule in these words; the stop hook's turn verdict prints, for an open critic chain on the seat's goal, its fold-read cycle ("design critique round 2 of 2 folded: 0 material" or "3 mechanical, falling, deferred as obligations").

## 5. Decision 3: the measure

Three figures from `internal/metrics`, printed beside `critique_rounds`, over the landed chains of a window. The loader reads each landing commit's `Landing-Provenance: chain=` and verdict trailers (ordinary `pass bar=a` landings counted; carried landings reported separately, with their original chain), each critic root's `reviews`, each round's persisted subject and fold outcome, and the mirrored refusal events, deduplicated by id.

- `rounds_per_landing`: for each landed implementation chain, its implementer rounds plus the rounds of every critic chain reviewing it; mean and maximum. Target under 3.
- `redundant_reads`: the share of critic rounds whose subject equals a subject an earlier folded clean round on the same implementer chain read. Target under 10 percent.
- `reads_refused`: the count of `REDUNDANT_READ` events in the window.

The zero-yield share stays out: it penalizes clean first reads, which are the aim.

## 6. Scope

Not in scope: the tier ladder, the code-critique round ceiling, the artifact test, the accept-risk path, the landing bars beyond the one restatement of bar (a); and the certification record, which is goal certification-closes-a-folded-bounded-finding (design lane, starting from records/misc/critique-closes-on-folded-proof-design-critique-r1.md and -r2.md).

## 7. The absorbed clauses, restated in full

1. closing-read-follows-the-change-not-the-label: a final round that changes no file, or whose diff is empty against the previously read subject, closes without a new read; the restoration case is refused here (a restored tree is a tree no critic read); a behaviour change dressed as a no-op still demands the read; the refusal names which rule applied (section 3).
2. critique-stamp-follow-up: the closure object accepts a critic chain whose latest folded round reviewed the terminal subject even when the stamp names an earlier round, so no stamp-only critic dispatch is needed (section 3).
3. design-loop-exit-in-the-skill: the skill's Round Budget section and docs/orchestration.md state the enforced stop rule and the D81 exit, and the stop-hook turn verdict prints the current fold-read cycle for an open critic chain (section 4).
4. severity-tiered-rigor-p2 slice 2b: the material stop and the close table read the unresolved set after accepted risks leave it; certification joins that reading when its goal lands.

## 8. Proof

| # | Obligation | Fixture | Home |
| --- | --- | --- | --- |
| 1 | The engine persists a typed read subject per critic kind before launch; a live subject after a moved base differs from the one before; a live subject never equals a commit subject | Go test | internal/dispatch, internal/validate |
| 2 | A critic dispatch onto a subject already read clean is refused before any record exists, named, and written as a durable event that mirrors with the chain; a follow-up whose diff is empty is the same refusal; a changed subject is dispatched | Shell leg in dispatch-fixtures.sh and a Go test | scripts/agents, internal/dispatch |
| 3 | The chain closes on the prior clean read through one closure object; the hazard close, the final-tree gate and landing read it; a stamp that names another critic root refuses by name | Go test | internal/dispatch, internal/validate, internal/landing |
| 4 | A design critic chain's limit is 2 at tier 3; a rebind above 2 refuses; round 2 clean closes; round 2 with mechanical findings on a falling trajectory closes with one obligation per finding naming its fixture; round 2 with an invariant, or a trajectory not falling, refuses with the two verbs and refuses round 3; a version-4 return normalizes to invariant | Go test on the register and a shell leg | internal/dispatch, scripts/agents |
| 5 | `rounds_per_landing`, `redundant_reads` and `reads_refused` computed on a fixture window with known landings (ordinary and carried), chains, subjects and events | Go test | internal/metrics |
| 6 | The skill and docs carry the rule; the turn verdict prints the fold-read cycle for an open critic chain | Shell leg on the stop hook fixture; Go test on the verdict | scripts/agents, internal/goal |

## 9. Slices, each landing alone

0. Schemas and readers, no behaviour change: the typed subject written to `review.json` and returns (readers accept its absence on old rounds); the schema-5 return validator beside version 4, dormant until slice 2; the refusal-event file format and its mirror; the closure object's reader accepting absence; the metrics loader reading landing provenance trailers.
1. The redundant-read refusal with its event, and the close on a prior read through the closure object consumed by every gate (section 3), rows 1 to 3.
2. Design critique at round two (section 4), rows 4 and 6.
3. The measure (section 5), row 5.

## 9b. Build spec after round 3 (D81 exit, 2026-09-13)

Written by a Fable design delegate from revision 3 and the three reads. Under D81 this section and the page are the spec; there is no revision 4. Where this section differs from section 3 (the closure on the critic root rather than the implementation root; the subject key without the boundary base), this section is the build decision.

Spec: plans/critique-closes-on-folded-proof-design.md revision 3, sections 3 and 9, as amended below; the round-3 read is folded as fixtures, not a revision 4. Paths are relative to the metasystem module.

### 1. Decisions taken for the build

**F-1 closure shopping.** The closure lives on the critic root, not the implementation root: `closure: {criticRoot, round, subject, mechanism: "clean"}`, written by `CritiqueChainClose` (internal/dispatch/close.go) in the one record write that sets `chainClosed`, after `CloseCheck` passes, under the finding-register and record locks; `CritiqueRegisterClose` never writes it, and the write is idempotent (absent: write; equal: unchanged; different: refuse by name, never overwrite). No shared slot, so no compare-and-set across chains and no join. Every gate already dereferences the stamp `independentCritiqueJobRef` to the critic root (`validateIndependentCritiqueReference` in hazard.go, `closedCriticReviewedTree` in internal/landing/observe.go); one shared reader `ReadClosure(root map[string]any) (Closure, bool, error)` serves all three. `mergeCritique` (internal/validate/conformance.go, line 1016 region) keeps its union over every critic root, so a second root with an open finding still fails merge. Concurrency is refused, not joined: `CritiqueReadAdmission` refuses `CONCURRENT_READ` when another same-role chain has an equal live subject and an unfolded, non-cancelled latest round. Commit subjects are exempt (HCL-09; the fixture `commit-subject-two` stands). Cut: the reservation store; the residual dispatcher race is bounded by the merge union.

**F-3 subject not bound to the read.** The engine computes the subject from immutable artifacts and writes `rounds/N/subject.json` before launch. Live: `{kind: live, implementerRoot, reviewedProjectTree (from the reviewed member's review.json), diffDigest (sha256 of its diff.patch)}`, provenance `reviewedMember`. Design: `{kind: design, designPath, contentDigest (sha256 of the page bytes in the critic workspace at admission), declaredOutputsDigest}`, provenance `reviewedCommit`. Commit: `{kind: commit, commit, tree, diffDigest}`, provenance `parent`. Before launch, a live subject's reviewed `workspaceRoot` must exist and its project snapshot (`gittree.Workspace.Snapshot("HEAD")`, the projection `reviewStage` uses) must equal `reviewedProjectTree`, else `SUBJECT_MISMATCH` before any record. After the read, at fold, `critiqueSubjectForRound` compares the return's `reviewedTree` (live, commit) or `reviewedCommit` (design) with the persisted subject; a mismatch folds the round as unbound (a synthetic unproven finding beside `foldProtocolError`), never a clean read. The fake adapter (internal/adapter/fake.go) fills `reviewedTree` from `subject.json` when present. Cut: a detached materialization per critic and a post-return workspace check; `boundaryBaseProjectTree` leaves the key because the reviewed tree already names the bytes read, the diff the change judged, and a new review.json field would trip the immutable-review reuse rule.

**F-4 the two prior-read cases.** Equality is over identity fields only; `reviewedMember`, `reviewedCommit` and `parent` are provenance. A no-op implementer follow-up therefore yields an equal live subject, and the hazard close accepts a closure whose subject equals the terminal work round's subject even when `reviews` names the earlier member and `endedAt` precedes it (the fresh-chain, distinct-session and effort checks stay). Closure ownership is uniform, on the critic root for every kind, so a standalone design chain closes at round 1 when its folded register is clean, and a follow-up on the unchanged page is `REDUNDANT_READ`. The round-2 rule is slice 2.

**F-5 slice 0 activates behaviour.** Slice 0 is types and decoders only: readers accept absence, nothing is written, no schema or record whitelist changes, no refusal token appears. Cut from slice 0: the schema-5 validator (slice 2), the mirror source (slice 1), the metrics loader (slice 3). Subjects never enter model returns: the return's existing `reviewedTree` or `reviewedCommit` is the reference validated against `subject.json`, so schema v4 is untouched by both slices.

**F-6 refusal durability.** `CritiqueReadAdmission` appends the event to `artifacts/agents/<prior clean critic root>/reads-refused.jsonl` under the finding-register lock by read-modify-write through `atomicfile.WriteText` (fsync and directory sync), releases the lock, and dispatch.sh calls `mirror_record <prior critic root>`; `mirrorSources` (internal/dispatch/mirror.go) carries `reads-refused.jsonl` from the payload directory when present. That root is terminal, so the manifest-keyed mirror is the idempotent transaction. On mirror failure the refusal stands, the event stays local, `mirror_fail` marks the record as today, and the next close, reap or `job mirror` carries it. Cut: an event-keyed mirror transaction; deduplication by id lives in the reader.

**Deferred.** F-2 (obligations as proof, `goal discharge-review-obligation`): slice 2. F-7 (trajectory and tier enumeration in build.go and rebind): slice 2. F-8 (carried landings' original chain, internal/landing/carried.go): slice 3.

Consequence: after slice 1 every follow-up onto a clean fake critic chain is refused, so `leg_happy_follow_up`, `resume-collision` and `close-race` in scripts/agents/dispatch-fixtures.sh must change the design page before following up. That is the rule biting, not a defect.

### 2. Slice 0 fixture obligations

Home internal/dispatch unless stated. Every row asserts absence is accepted or nothing new is emitted.

| id | obligation | test | finding |
| --- | --- | --- | --- |
| S0-1 | Two subjects of different kinds are never equal; two live subjects differing only in `reviewedMember` are equal; differing in `reviewedProjectTree` or `diffDigest` are not | `TestReadSubjectEqualityIgnoresProvenance`, `TestReadSubjectKindsNeverEqual` | F-3, F-4 |
| S0-2 | `readRoundSubject` on a round directory without subject.json returns present=false and no error; a malformed file is an error | `TestReadRoundSubjectAbsentOnOldRounds` | F-5 |
| S0-3 | `ReadClosure` on a root record without `closure` returns present=false and no error; a closure with an unknown mechanism is an error | `TestReadClosureAbsentOnOldRoots` | F-5 |
| S0-4 | `LoadReadRefusals` over a missing file returns an empty slice; two files carrying the same id yield one event; a malformed line is an error | `TestLoadReadRefusalsMissingAndDeduplicated` | F-6 |
| S0-5 | Advancing and closing a register through the existing helpers (`writeCriticRound`, `setCriticSubject`) leaves the root record without a `closure` key, writes no subject.json and no reads-refused.jsonl | `TestSliceZeroEmitsNothing` | F-5 |
| S0-6 | `validate conformance --stage review` writes review.json with exactly the keys diffArtifact, implementerJob, reviewedTree | `TestReviewStageWritesOnlyThreeFields` (internal/validate) | F-5 |
| S0-7 | No new refusal token exists: `go test ./internal/refusal` passes with register.go untouched | existing HCL03 tests | F-5 |

### 3. Slice 1 fixture obligations

Shell legs live in scripts/agents/dispatch-fixtures.sh cluster c beside `leg_review_target_flag_runtime`.

| id | obligation | test | finding |
| --- | --- | --- | --- |
| S1-1 | Dispatch writes rounds/1/subject.json of the right kind for a code critic on an implementer member, a commit subject and a design critic, before the record exists | shell leg `subject-persisted`; `TestComputeReadSubjectByKind` | F-3 |
| S1-2 | A no-op implementer follow-up (same tree, same diff) computes an equal live subject; a changed tree computes a different one | `TestLiveSubjectFollowsTheChangeNotTheMember` | F-4 |
| S1-3 | Reviewed worktree edited after its review: fresh critic dispatch refuses `SUBJECT_MISMATCH`, exit 11, no record, no payload | shell leg `subject-mismatch` | F-3 |
| S1-4 | A return whose reviewedTree differs from subject.json folds as unbound: register gains a synthetic unproven finding; close refuses | `TestFoldRefusesUnboundReturn` | F-3 |
| S1-5 | Follow-up onto a chain whose last folded round was clean refuses `REDUNDANT_READ` naming round, subject digest and rule, exit 11, no child record or payload | shell leg `redundant-follow-up` on `happy`; `TestReadAdmissionRefusesCleanSubject` | row 2 |
| S1-6 | Fresh chain onto a live subject another chain read clean refuses `REDUNDANT_READ`; the same for a design page | shell leg `redundant-fresh` | row 2 |
| S1-7 | A changed subject is dispatched: editing the page makes the design follow-up run; the three existing follow-up legs are updated to edit first | shell legs updated | row 2 |
| S1-8 | Second same-role chain on an equal live subject while the first is unfolded refuses `CONCURRENT_READ`; `commit-subject-two` still runs | shell leg `concurrent-live`; `TestReadAdmissionExemptsCommitSubjects` | F-1 |
| S1-9 | The event has a stable id, lands in reads-refused.jsonl under the prior critic root and in that chain's mirror manifest; with `.mirror-fail-once` the refusal still exits 11, the local event exists, and a later `job mirror` carries it | shell leg `refusal-durable`; `TestReadRefusalAppendUnderLock` | F-6 |
| S1-10 | `CritiqueChainClose` writes the closure once on a clean root; a repeat is unchanged; a different closure refuses by name; a root with open findings gets none | `TestCloseWritesOneClosure` | F-1 |
| S1-11 | Hazard close accepts a stamped critic whose closure subject equals the terminal work round's subject although `reviews` names the earlier member and `endedAt` precedes it; refuses when the closure is absent or its subject differs | `TestHazardCloseReadsClosureSubject` | row 3 |
| S1-12 | Merge requires each closed critic root's closure subject tree to equal finalTree and still unions every root: a second root with an open finding fails merge | `TestMergeCritiqueClosureAndUnion` (internal/validate) | F-1 |
| S1-13 | Landing selects the review whose reviewedTree equals the stamped closure's subject tree, falling back to today's order | `TestChainCertifiedOutputPrefersClosure` (internal/landing) | row 3 |
| S1-14 | `REDUNDANT_READ`, `CONCURRENT_READ`, `SUBJECT_MISMATCH` are rowed in internal/refusal/register.go with real sites | existing HCL03 tests | hygiene |
| S1-15 | A design chain folded clean at round 1 closes with closure round 1 | `TestDesignChainClosesAtRoundOne` | F-4 |

### 4. Codex build brief, slice 0

You are building slice 0 of goal critique-closes-on-folded-proof in a git worktree of the metasystem module: readers and types only, provably behaviour-free. No existing production file changes, nothing new is written to disk by production code, and no UPPER_SNAKE string literal appears in non-test Go under internal/dispatch (the refusal register test walks those literals). Do not commit.

New files, package `dispatch`:

- `internal/dispatch/read_subject.go`
  - `type ReadSubjectKind string`; `const (SubjectLive ReadSubjectKind = "live"; SubjectDesign = "design"; SubjectCommit = "commit")`.
  - `type ReadSubject struct { Kind ReadSubjectKind; ImplementerRoot, ReviewedMember, ReviewedProjectTree, DiffDigest string; DesignPath, ContentDigest, DeclaredOutputsDigest, ReviewedCommit string; Commit, Parent, Tree string }` with lowerCamel json tags and `omitempty` on every string.
  - `func (s ReadSubject) Equal(o ReadSubject) bool`: kinds must match; live compares ImplementerRoot, ReviewedProjectTree, DiffDigest; design compares DesignPath, ContentDigest, DeclaredOutputsDigest; commit compares Commit, Tree, DiffDigest. Provenance fields never participate.
  - `func (s ReadSubject) Digest() string`: sha256 hex over `canonicalJSON` of the identity tuple `[kind, fields in the order above]`.
  - `func decodeReadSubject(value any) (ReadSubject, bool, error)`: nil or absent returns (zero, false, nil); a non-object or unknown kind is an error.
  - `func readRoundSubject(agents, rootJob string, round int64) (ReadSubject, bool, error)`: reads `<agents>/<rootJob>/rounds/<round>/subject.json` through `readObject`; `os.IsNotExist` returns (zero, false, nil).
- `internal/dispatch/closure.go`
  - `const closureField = "closure"`; `type Closure struct { CriticRoot string; Round int64; Subject ReadSubject; Mechanism string }`.
  - `func ReadClosure(root map[string]any) (Closure, bool, error)`: absent returns (zero, false, nil); Mechanism must be `"clean"` (slice 1 adds nothing else here; the certification goal adds `"certified"` later); Round must be a positive integer via `numInt`; CriticRoot must match `validJobID`.
- `internal/dispatch/reads_refused.go`
  - `type ReadRefusal struct { ID, Reason, Role, CriticRoot string; Round int64; Subject ReadSubject; RefusedAt string }`, lowerCamel json tags.
  - `func LoadReadRefusals(paths ...string) ([]ReadRefusal, error)`: one JSON object per line, blank lines skipped, missing files skipped, malformed line is an error naming path and line, deduplicated by ID keeping the first, returned in first-seen order.

Tests (section 2): `internal/dispatch/read_subject_test.go` holds S0-1 to S0-5; S0-5 uses `writeCriticRound`, `setCriticSubject`, `CritiqueRegisterAdvance` and `CritiqueRegisterClose` from finding_register_test.go, then asserts no `closure` key on the root record and no `subject.json` or `reads-refused.jsonl` in the payload. `internal/validate/conformance_review_shape_test.go` holds S0-6: after a review-stage run, unmarshal review.json into `map[string]any` and assert exactly three keys. Existing style: table tests, `t.Helper()`, package helpers for temp repos, no new utilities.

Verification, in order, all green:

```
go build ./...
go vet ./internal/dispatch ./internal/validate
go test -race -count=1 ./internal/dispatch ./internal/validate ./internal/refusal
scripts/agents/go-gate.sh --fast
git status --porcelain   # only the five new files
```

Write `artifacts/reports/codex-ccf-slice0-result.md`: files added, test names with pass lines, the verification tail, and any point where the spec could not be followed literally. Do not commit; do not edit any existing file; do not touch plans/ or records/.

### 5. Open questions for Wido

none

## 9c. Build spec, slice 1 parts 1a-ii and 1b (D81 exit, 2026-09-13)

This is the build amendment to 9b, not a fourth design revision. Section 11 option (a) remains the scope. Paths and line numbers below are relative to the metasystem module at worktree HEAD `25e69aa0`. The implementation inspected is slice 0 at `4fbd271c` and slice 1a-i at `20eecaf0`. Proof names below are requirements, not claims that tests ran.

### 1. Decisions for the build

**D1. One shared reader, with a gate check.** Move the subject types, identity operations and closure decoder into `internal/readsubject`. Its production imports must be standard library only. Preserve the wire fields and digest bytes. The present identity operations ignore provenance, and hash a JSON identity tuple (`internal/dispatch/read_subject.go:34`, `internal/dispatch/read_subject.go:56`, `internal/dispatch/finding_register.go:1270`). The existing closure decoder checks shape, not whether a closure still names the last round (`internal/dispatch/closure.go:14`). Add `ReadClosedClosure(agents string, root map[string]any, members []map[string]any) (Closure, bool, error)` beside `ReadClosure`. All gate consumers use it. Pass every member of the critic chain, regardless of status. It validates the root, closed state, unique highest round, completed last round, folded round, clean register, persisted subject, return binding and folded subject digest. Require `closure.round == last critic round == findingRegisterRound`. Unknown mechanisms, malformed evidence and present but inconsistent closures refuse. Never downgrade them to an absent closure.

**D2. Keep the writer and authority.** `CritiqueChainClose` remains the only closure writer. It checks `CloseCheck` before one write containing both closure and `chainClosed` (`internal/dispatch/close.go:228`, `internal/dispatch/close.go:240`, `internal/dispatch/close.go:251`). The public Go verb does not take the shell chain lock (`cmd/metasystem/dispatch_verbs.go:1783`). The shell follow-up checks `chainClosed` before publication (`scripts/agents/dispatch.sh:2368`, `scripts/agents/dispatch.sh:2794`). Therefore every consumer must detect a later member even when the root already says closed. This part does not change the authority model or overwrite a stale closure to repair that race.

**D3. Absence has two lawful uses.** Retain historical reads and existing non-clean close outcomes. A register is clean only when empty or entirely resolved/withdrawn. The writer deliberately emits no clean closure for accepted-risk, deferred or out-of-scope entries (`internal/dispatch/finding_register.go:595`). Historical seven-field resolved entries normalize to withdrawn (`internal/dispatch/finding_register.go:805`). With no closure, historical rounds without subject evidence and lawful non-clean dispositions retain the existing gate path. They gain no earlier-member or earlier-time allowance. A completed, subject-bearing, clean folded round without closure refuses by name. A missing subject with a recorded folded digest is evidence loss, not historical absence. Full finding validation and goal decision checks stay with their current owners (`internal/dispatch/finding_register.go:779`, `internal/validate/conformance.go:1234`). This qualifies S1-11's blanket absence refusal. It preserves the accepted-risk path excluded from scope in section 6.

**D4. Hazard uses the terminal work round itself.** Keep `finalHazardWorkState`. It chooses the highest work round and rejects ties (`internal/dispatch/hazard.go:283`). Do not substitute the dispatch selector, which skips non-completed members and members without review artifacts (`internal/dispatch/read_subject_compute.go:87`, `internal/dispatch/read_subject_compute.go:94`). Derive the live subject from `finalState.job` using `liveReadSubject`, without selecting an earlier member and without requiring a retained worktree. Its immutable review and patch are the source (`internal/dispatch/read_subject_compute.go:120`). A valid live closure must name this implementer root and equal that complete subject. Only then replace the exact `reviews` and end-time tests (`internal/dispatch/hazard.go:344`, `internal/dispatch/hazard.go:367`). Preserve fresh-chain, distinct-session, provenance and effort checks (`internal/dispatch/hazard.go:337`, `internal/dispatch/hazard.go:347`, `internal/dispatch/hazard.go:354`). The stamp must equal `closure.criticRoot`. Missing terminal artifacts refuse this allowance.

**D5. Merge cannot select away unresolved findings.** Consume each relevant root's closure before accepting its tree. A valid current-tree closure is required for the new clean path. Preserve the lawful absence path in D3. Keep at least one eligible current-tree code critic. Retain the independence and goal decision checks (`internal/validate/conformance.go:1234`, `internal/validate/conformance.go:1271`). Remove the candidate exclusion based on `r.criticRoot`; use it as a required selected root instead (`internal/validate/conformance.go:1076`). Collect unresolved findings from all reviewing code-critic roots before stale-tree handling. Today stale-tree handling skips register validation, and stale failures disappear when another root matches (`internal/validate/conformance.go:1197`, `internal/validate/conformance.go:1299`). An older clean root may remain historical. An older open root must block. Preserve same-tree rigor-class conflict semantics (`internal/validate/conformance.go:1223`). A malformed or stale-round closure is a hard failure even on a historical tree. This is S1-12's union, including the selected-root call path.

**D6. Landing binds the selected patch as well as its tree.** Validate the stamped closure before the single-output shortcut (`internal/landing/observe.go:598`). A valid live closure selects an implementation-chain review by `reviewedProjectTree` and SHA-256 of `diff.patch`. `reviewedMember` remains provenance. A matching tree with a different patch does not qualify. No match refuses. Only lawful closure absence uses the existing order: stamped root return tree, terminal implementer, largest review-directory round (`internal/landing/observe.go:601`, `internal/landing/observe.go:608`, `internal/landing/observe.go:615`). Preserve patch replay and postimage comparison (`internal/landing/observe.go:726`, `internal/landing/observe.go:740`). Place the adjacent `selectOriginalCertification` reader here too. It currently requires the critic root's old `reviews` to equal the requested job (`internal/validate/recertification.go:359`). For a closure-backed read, join the requested member to the same implementation chain and its exact subject. Keep recertification's artifact hashes, proof owners and exact readback (`internal/validate/recertification.go:1119`). Update the bar (a) paragraph in `plans/two-bars-for-changes-design.md:206`: prior-clean-read acceptance uses the validated closure; existing non-clean decision paths keep their existing requirements.

**D7. Admission needs a record of earlier clean folds.** The current fold replaces the root's register and round; it does not retain the earlier register state (`internal/dispatch/finding_register.go:187`). Add dedicated root metadata `cleanReadRounds`, an ordered list of `{round, subject}` observations. Append only for a completed bound fold whose resulting register is empty or entirely withdrawn. Write it in the same root-record publication as the fold. Before replacing a pre-history register, also retain its current clean fold if its subject, return and recorded digest prove it. This preserves known evidence during the cutover. The list is proof of a read, never authority to close or land. Failed, cancelled, unbound and non-clean rounds add nothing. Do not guess an older fold from today's register. Old records without historical observations retain conservative compatibility. The historical observation remains eligible for refusal after a later changed subject. It cannot bypass D1: if its root has a later round, no gate may use the earlier read as that root's closure. The refusal must state this limit instead of promising that close will succeed.

**D8. Admission scope and order.** `CritiqueReadAdmission` runs after subject computation and a follow-up's preceding fold, before exhaustion accounting, claim publication or successor payload creation. Current subject computation and publication are separate (`scripts/agents/dispatch.sh:1700`, `scripts/agents/dispatch.sh:1876`, `scripts/agents/dispatch.sh:2611`, `scripts/agents/dispatch.sh:2800`). Move the follow-up admission decision ahead of the exhaustion call at `scripts/agents/dispatch.sh:2605`. Use the same computed subject for admission and publication. A replay of an already claimed follow-up retains its current replay path (`scripts/agents/dispatch.sh:2374`). Refuse `REDUNDANT_READ`, exit 11, for an equal proven clean subject in the same role and subject scope. Scope is the implementer root for live reads, or design path for design reads; full subject equality also applies. Role compatibility matters because merge only admits code-critic roots (`internal/validate/conformance.go:1072`). A warden read must not prevent the code critique merge requires. This makes the reusable-read domain explicit. Refuse `CONCURRENT_READ`, exit 11, when another same-role root has an equal live subject and its latest round is unfolded and not cancelled. Completed but unfolded also counts. Exclude the requesting root. Commit subjects remain exempt from both checks under 9b.1 F-1 and `commit-subject-two` (`scripts/agents/dispatch-fixtures.sh:1676`). Design reads have redundant admission only. No reservation store or lock held across launch is added. Merge's union covers findings from residual racing dispatches.

**D9. Only the redundant refusal writes the read-refusal event.** Store it under the prior clean critic root, as 9b.1 F-6 requires. `ReadRefusal` and its deduplicating loader already exist (`internal/dispatch/reads_refused.go:10`, `internal/dispatch/reads_refused.go:20`). Use a nonempty stable event id `<prior-root>-<read-round>-<nonce>`. Retries of the same event reuse it; separate admission attempts may have separate ids. Under the finding-register lock, reload state, choose the proof deterministically and replace the JSONL file with `atomicfile.WriteText(path, text, repoRoot)`. The API returns both durability and error (`internal/atomicfile/atomicfile.go:76`). A publication with uncertain directory durability is not a failed append and must not acquire a second id (`internal/atomicfile/atomicfile.go:108`). Report the doubt, retain exit 11, and attempt mirroring. A pre-publication failure still refuses launch and reports that the event was not recorded. Release the lock before mirroring. Add the file to `mirrorSources`, which currently gathers job and round evidence (`internal/dispatch/mirror.go:103`). The shell mirrors the result's explicit prior-root field, not text parsed from a diagnostic. `mirror_fail` currently writes a log, not a record flag (`scripts/agents/dispatch.sh:1149`). Preserve that behavior. Mirror failure retains the local event and exit 11; a later close, reap or `job mirror` repairs it. `CONCURRENT_READ` names the active evidence but creates no event on an active chain. Slice 3 counts `REDUNDANT_READ` events only, per section 5.

**D10. Activate with fixtures.** Change the page in the reviewed fixture workspace before `leg_happy_follow_up`, `resume-collision`, and `close-race`. Their follow-ups currently use the unchanged page (`scripts/agents/dispatch-fixtures.sh:1696`, `scripts/agents/dispatch-fixtures.sh:3355`, `scripts/agents/dispatch-fixtures.sh:3388`). Assert a changed subject digest and the original leg's result. Isolate scenario pages where a fresh design chain would otherwise inherit another scenario's clean read. Do not edit only a brief or bypass admission.

### 2. Settlement of the carried reads

The finding ids refer to round 3 and the round-2 non-material table in `records/misc/critique-closes-slice1a-i-code-read.md:74` and `records/misc/critique-closes-slice1a-i-code-read.md:162`.

| Finding | Settlement |
| --- | --- |
| N3-1 | Fold into A-1 through A-5. Every consumer requires the last critic round, including later running, failed and cancelled members. Public entry remains as inspected at `cmd/metasystem/dispatch_verbs.go:1783`. |
| N3-2 | Decline a runner-stamp change. Re-close remains byte-idempotent at `internal/dispatch/close.go:231`. The runner skips already closed roots at `internal/missionrunner/jobs.go:138`. No gate here consumes `runnerClosed`. |
| N3-3 | Decline new instrumentation solely to kill the three surviving mutations. The role check exists at `internal/dispatch/close.go:225`; terminal synchronization remains at `internal/dispatch/close.go:254`; the byte-identical repeat is asserted at `internal/dispatch/read_subject_compute_test.go:429`. None changes this build's observable gate contract. |
| N3-4 | Decline reopening S1-10 for this test shape. A cancelled round with a persisted subject is already driven through `CritiqueChainClose` at `internal/dispatch/read_subject_compute_test.go:504`. The open-finding assertion at `internal/dispatch/read_subject_compute_test.go:536` only calls register close, so do not cite it as writer proof. The actual writer invokes `CloseCheck` at `internal/dispatch/close.go:228`, whose unresolved guard is `internal/dispatch/close.go:64`. A-1's later-cancelled-member case adds the required consumer proof. |
| N3-5 | Decline changing the record-writer authority envelope. The internal verb uses that mode at `scripts/agents/dispatch.sh:3364`; supervision and custody-bound adapter access are intentional branches at `internal/authority/authority.go:97`. This part adds no new closure-writing authority. Admission event mutation uses holder-only authority. |
| N3-6 | Already settled in the landed patch. The verb list includes `__critique-close` at `docs/design/dispatch-sequence.md:50`. The close paragraph distinguishes the critic write from other roots' CAS at `docs/design/dispatch-sequence.md:319`. No duplicate documentation fix. |
| N3-7 | Settled by the three explicit carried rows below. |
| N-1, round 2 | Fold into A-2. Use hazard's exact terminal work member, not the artifact-filtered dispatch selector (`internal/dispatch/hazard.go:283`, `internal/dispatch/read_subject_compute.go:87`). |
| N-4, round 2 | Fold into A-1 through A-5 and B-1. Non-clean close outcomes keep their existing strict route; they never become clean-read observations (`internal/dispatch/finding_register.go:595`). |
| N-5, round 2 | Decline a diagnostic reorder. Subject mismatch still stops before claim (`scripts/agents/dispatch.sh:1700`); claim's later latest-member check also refuses (`internal/dispatch/claim.go:833`). Changing which explanation appears first protects no additional invariant. |

### 3. Reconcile every row of 9b.3

| Original row | Placement and evidence |
| --- | --- |
| S1-1 | Built in 1a-i. Kind computation is tested at `internal/dispatch/read_subject_compute_test.go:62`; live and commit fixture assertions are at `scripts/agents/dispatch-fixtures.sh:2872`, design at `scripts/agents/dispatch-fixtures.sh:3379`. Clarify the row: compute privately before claim; publish `subject.json` after winning claim and before launch, as `scripts/agents/dispatch.sh:1876` does. |
| S1-2 | Built in 1a-i: `TestLiveSubjectFollowsTheChangeNotTheMember`, `internal/dispatch/read_subject_compute_test.go:150`. |
| S1-3 | Built in 1a-i: `subject-mismatch`, `scripts/agents/dispatch-fixtures.sh:2886`. |
| S1-4 | Built in 1a-i: `TestFoldRefusesUnboundReturn`, `internal/dispatch/read_subject_compute_test.go:327`. |
| S1-5 | B-1, B-2, B-5. No admission call follows computation today (`cmd/metasystem/dispatch_verbs.go:1827`). |
| S1-6 | B-1, B-2, B-5. Same absent admission seam (`cmd/metasystem/dispatch_verbs.go:1838`). |
| S1-7 | B-6. Three unchanged-page follow-ups are named in D10. |
| S1-8 | B-3, B-5. Preserve the commit fixture at `scripts/agents/dispatch-fixtures.sh:1676`. |
| S1-9 | B-4, B-5, B-7. The reader exists; the mirror does not yet select the chain-level refusal file (`internal/dispatch/reads_refused.go:20`, `internal/dispatch/mirror.go:103`). |
| S1-10 | Built in 1a-i: writer at `internal/dispatch/close.go:217`; `TestCloseWritesOneClosure` at `internal/dispatch/read_subject_compute_test.go:373`; refused-close sequencing at `internal/dispatch/read_subject_compute_test.go:773`. A-1 adds consumer safety for N3-1. |
| S1-11 | A-1, A-2. Current exact-member gate is `internal/dispatch/hazard.go:344`. |
| S1-12 | A-1, A-3. Current return-tree comparison is `internal/validate/conformance.go:1197`. |
| S1-13 | A-1, A-4, A-5. Include recertification's adjacent selector at `internal/validate/recertification.go:359`. |
| S1-14 | SUBJECT_MISMATCH is built at `internal/refusal/register.go:74`. B-7 supplies the two new rows and rechecks exact emitting lines after formatting. |
| S1-15 | Built in 1a-i: `TestDesignChainClosesAtRoundOne`, `internal/dispatch/read_subject_compute_test.go:730`. |

No slice 1 row is unassigned. Schema 5, the design round-two policy, obligation discharge and metrics remain slices 2 and 3, per 9b.1.

### 4. Part 1a-ii obligations

Every row is currently MISSING or PARTIAL. The named tests are expected to fail on today's behavior. Author them before the behavior change. Extraction compatibility tests are additional guards, not claimed red evidence. If a new API prevents a base-tree test from compiling, demonstrate the failing assertion through the existing gate or a behavior-reverting mutation after it compiles.

| Row | Obligation | Severity | Files | Named proof and expected failure today | Source |
| --- | --- | --- | --- | --- | --- |
| A-1 | Shared stdlib types and one closure gate reader. Enforce root, highest round, completed status, fold and immutable binding. Distinguish absence from corruption and non-clean outcomes. | HIGH | `internal/readsubject/{subject,closure}.go`, dispatch call sites, both coverage ratchets | `TestClosureGateRequiresLastCriticRound`; `TestClosureGateMissingCleanClosure`; current `ReadClosure` accepts shape without these joins (`internal/dispatch/closure.go:14`). | 9b.1 F-1/F-3/F-4; D1-D3; N3-1/N-4 |
| A-2 | Hazard accepts a no-op terminal member and a critic follow-up through the stamped root's valid closure. It refuses wrong root, changed diff/tree, missing terminal artifacts and stale critic rounds. | HIGH | `internal/dispatch/hazard.go`, `hazard_closure_test.go` | `TestHazardCloseReadsClosureSubject`; `TestHazardCloseUsesTerminalWorkSubject`. The positive prior-read leg fails the exact job/time checks today (`internal/dispatch/hazard.go:344`). | S1-11; D4; N-1 |
| A-3 | Merge consumes closure trees, retains all unresolved roots, and cannot hide defects through selected-root or stale-tree paths. | HIGH | `internal/validate/conformance.go`, `conformance_closure_test.go` | `TestMergeCritiqueClosureAndUnion`, including selected-root and stale-open-root cases; `TestMergeCritiqueRejectsStaleClosureRound`. Current selection and stale handling omit them (`internal/validate/conformance.go:1076`, `internal/validate/conformance.go:1203`). | S1-12; D5 |
| A-4 | Landing validates closure before any shortcut and selects the exact tree and diff. Lawful absence retains fallback; present inconsistency refuses. | HIGH | `internal/landing/observe.go`, `observe_closure_test.go` | `TestChainCertifiedOutputPrefersClosure`; `TestChainCertifiedOutputRejectsUnmatchedClosure`; `TestLandingRejectsStaleCriticClosure`. The one-output shortcut accepts an unmatched closure today (`internal/landing/observe.go:598`). | S1-13; D6 |
| A-5 | Recertification selects and revalidates the same closure-bound original review. Preserve patch replay, ownership and exact evidence joins. State the bar (a) rule. | HIGH | `internal/validate/recertification.go`, `recertification_closure_test.go`, `plans/two-bars-for-changes-design.md` | `TestRecertificationUsesClosureForNoOpMember`; `TestRecertificationRejectsStaleClosureRound`. The exact root reviews check rejects the lawful no-op member today (`internal/validate/recertification.go:359`). | S1-13; section 3 bar (a); D6 |

### 5. Part 1b obligations

| Row | Obligation | Severity | Files | Named proof and expected failure today | Source |
| --- | --- | --- | --- | --- | --- |
| B-1 | Persist only proven clean fold observations atomically with the fold. Refuse repeats of earlier clean subjects without turning observations into closures. | HIGH | `internal/dispatch/finding_register.go`, `read_admission.go`, corresponding tests | `TestCleanReadHistorySurvivesChangedFold`; `TestCleanReadHistoryRejectsNonCleanRounds`. No history is retained today (`internal/dispatch/finding_register.go:187`). | S1-5/S1-6; section 3; D7 |
| B-2 | Refuse equal live and design clean reads, fresh and follow-up. Admit changed identities and distinguish role eligibility. | HIGH | `internal/dispatch/read_admission.go`, `read_admission_test.go` | `TestReadAdmissionRefusesCleanSubject`, `TestReadAdmissionChecksScopeAndRole`; paired changed-subject guards. No decision occurs after computation today (`cmd/metasystem/dispatch_verbs.go:1838`). | S1-5/S1-6/S1-7; D8 |
| B-3 | Refuse equal live same-role unfolded work on another root. Preserve cancellation, own-root replay, changed subjects and commit exemptions. | HIGH | `internal/dispatch/read_admission.go`, `read_admission_test.go` | `TestReadAdmissionRefusesConcurrentLive`, with running and completed-unfolded cases; `TestReadAdmissionExemptsCommitSubjects` is the paired compatibility guard. | S1-8; 9b.1 F-1 |
| B-4 | Durable, idempotent event append under the register lock. No lost append. Propagate pre-publication error and committed durability doubt truthfully. | HIGH | `internal/dispatch/reads_refused.go`, `reads_refused_test.go` | `TestReadRefusalAppendUnderLock`; `TestReadRefusalAppendPublicationOutcomes`. Only loading exists today (`internal/dispatch/reads_refused.go:20`). | S1-9; D9 |
| B-5 | Wire both shell entry paths before claim and before exhaustion. Return structured refusal evidence; preserve exit 11 and cleanup even if mirroring fails. | HIGH | `cmd/metasystem/{main,dispatch_verbs}.go`, verb tests, `scripts/agents/dispatch.sh`, `dispatch-fixtures.sh` | `TestDispatchCritiqueReadAdmissionResult`; fixture legs `redundant-follow-up`, `redundant-fresh`, `concurrent-live`. Each would launch or reach a later refusal today instead of the required read refusal. | S1-5/S1-6/S1-8; D8-D9 |
| B-6 | Page changes make the three original follow-up scenarios run and still prove their original outcomes. Test unchanged refusal first. | MEDIUM | `scripts/agents/dispatch-fixtures.sh` | `changed-design-follow-up`: refuses unchanged, then runs after a real page edit; also run `happy-follow-up`, `resume-collision`, `close-race`. The unchanged refusal assertion fails today (`scripts/agents/dispatch-fixtures.sh:1696`). | S1-7; 9b.1 consequence; D10 |
| B-7 | Mirror events into the prior root's manifest, recover failed mirrors, and register exact refusal sites. | HIGH | `internal/dispatch/mirror.go`, `mirror_test.go`, `internal/refusal/register.go`, `scripts/agents/dispatch-fixtures.sh` | `TestMirrorIncludesReadRefusals`; `refusal-durable` with `.mirror-fail-once` and later `job mirror`. The chain-level source is absent today (`internal/dispatch/mirror.go:103`). Run existing refusal completeness tests as a guard. | S1-9/S1-14; D9 |

### 6. Order, size and verification ownership

Land 1a-ii before activating 1b. Split 1a-ii into three reviewable units: 1a-ii-a shared package and reader; 1a-ii-b hazard and merge; 1a-ii-c landing and recertification. Budget about 700-1,200 changed lines each, including tests and moves. Split 1b into 1b-i fold history, admission and event/mirror owners, then 1b-ii CLI and shell activation with fixtures. Budget about 1,000-1,400 and 700-1,200 lines respectively. These are estimates. Count additions plus deletions before returning each unit. Split again along these owners if a unit exceeds about 1,500 lines. Do not activate half an admission path or omit a red case to meet the size budget. The seat lands each unit after its required proof. Builders do not commit.

The builder runs the ordered checks in the standalone briefs. A new `internal/readsubject` floor of at least 90.0 is required in BOTH `scripts/agents/coverage-ratchet.json` and `scripts/agents/coverage-ratchet-linux.json`. Never lower an existing floor, including dispatch's 75.9 (`scripts/agents/coverage-ratchet.json:30`, `scripts/agents/coverage-ratchet-linux.json:30`). The seat verifies actual coverage on the supported platforms. Fast mode alone omits the coverage ratchet (`scripts/agents/go-gate.sh:10`).

The builder sandbox has no network and runs no fixture beds. Through the enrolled engine, the seat runs `section/dispatcher-adapter-and-mission-runner-fixtures`, `section/conformance-fixtures`, `section/land-fixtures`, plus `policy-canary`, `adapter-canary`, and `command-interface-smoke` for 1a-ii. For 1b it runs the dispatcher bed in full, including clusters c and e, plus those canaries and the conformance and land beds for the completed admission-to-landing path. The contract names these groups at `testing.json:64`, `testing.json:70`, `testing.json:91`, and `testing.json:96`. Use retained structured group results, then the goal's delivery selection. Do not infer a bed verdict from a log tail.

### 7. Open questions for Wido

none

### 10. D4 scope amendment after the unit b read (2026-09-13)

**Decision: (c), a narrower rule. Keep freshness on the stamped critic root. Check the proving round's own session, admission and effort evidence before accepting its closure. Unit b is not safe to land after folding F-1 alone. Fold this amendment into unit b before landing.**

This qualifies D4 in section 9c at `origin/main`, commit `e63af263`. It does not reopen section 11's scope. Paths are relative to `metasystem/` in this worktree unless marked **unit b**. Those citations refer to the inspected sibling worktree `/Users/wido/LocalStorage/GitHub/agentic-tools-m1e/.claude/worktrees/ccf-s1aii-b/metasystem`. The independent finding is F-2, the unchecked proving round (`artifacts/reports/opus-read-unit-b.md:53`). Evidence here is a code read. No implementation or tests ran for this amendment.

**Reason.** The landed hazard gate reads the stamped record for admission, freshness, session separation and effort. Its exact `reviews` check and fresh-root requirement make that record the accepted reading job (`internal/dispatch/hazard.go:333`, `internal/dispatch/hazard.go:337`, `internal/dispatch/hazard.go:344`, `internal/dispatch/hazard.go:347`, `internal/dispatch/hazard.go:354`). Unit b retains those root checks, then returns success for a later round's matching closure (**unit b**, `internal/dispatch/hazard.go:338`, `internal/dispatch/hazard.go:373`, `internal/dispatch/hazard.go:392`). Its positive follow-up fixture supplies no session, effort, obligations or launch admission on that member (**unit b**, `internal/dispatch/hazard_closure_test.go:104`, `internal/dispatch/hazard_closure_test.go:170`).

The other readers do not compensate. `liveReadSubject` derives the implementation root, tree and patch digest from the reviewed implementation record and immutable artifacts (`internal/dispatch/read_subject_compute.go:120`, `internal/dispatch/read_subject_compute.go:157`). The fold checks the return's job and round, then binds its tree to the persisted subject (`internal/dispatch/finding_register.go:132`, `internal/dispatch/finding_register.go:147`). The closure reader checks membership, the last completed round, the clean fold and the bound return (`internal/readsubject/closure.go:153`, `internal/readsubject/closure.go:195`, `internal/readsubject/closure.go:202`, `internal/readsubject/closure.go:239`). These checks prove which subject the return covers. They do not prove the reading member's session or execution effort. Critic-only chains return before the hazard evidence checks (`internal/dispatch/hazard.go:247`). Merge's independence check compares effective models, which cannot replace the missing session and admission joins (`internal/validate/conformance.go:1271`).

**Mechanical rule and owner.** `validateIndependentCritiqueReference` in `internal/dispatch/hazard.go` owns the added checks. Keep the shared closure reader's existing contract. Do not add hazard policy to `internal/readsubject` or the finding-register writer.

1. Let **R** be `jobs/<independentCritiqueJobRef>.json`, the stamped root. Keep every existing root check except the two comparisons D4 replaces. In particular, require a completed critic, valid admission provenance, `parentJob: null`, `dispatchMode: fresh`, an explicitly empty `resumedSessionId`, a nonempty `sessionId` absent from the implementation chain's session set, and the existing effort checks. The current provenance helper requires the parent field and explicit resumed-session string (`internal/dispatch/hazard.go:421`, `internal/dispatch/hazard.go:456`). The session set already includes every recorded implementation-chain member session (`internal/dispatch/hazard.go:214`). A later valid round cannot repair an invalid root.

2. Read `ReadClosedClosure` using R and all its chain members. On a present valid closure, let **P** be the unique member whose `round == closure.round`. Select it from the same member collection supplied to the reader. Require its record path to be `jobs/<P.jobId>.json`, its `jobId` to be outside the implementation member-id set, its ancestry to resolve to R, its `status` to be `completed`, and its `role` to equal R's critic role. Missing, ambiguous or inconsistent selection refuses. Never select P by `reviews`, `subject.reviewedMember`, filename suffix or highest completed round alone. The closure reader already joins the selected member's job and round to `return.json` (`internal/readsubject/closure.go:243`). When the closure names round 1, P is R and the checks need run only once.

3. For P and every ancestor back to R, require a nonempty `sessionId` absent from the implementation session set and the same critic role as R. For each child, require `dispatchMode: follow-up`, `parentJob` naming the preceding critic member, `round == parent.round + 1`, the same `runtime` as that parent, and `resumedSessionId == parent.sessionId`. Refuse a missing parent, broken join or cycle. These joins preserve the fresh root's independent context through an intermediate round. Do not require each child to have a null parent or fresh dispatch mode. The dispatcher records the preceding session as the context source even when it opens a new session (`scripts/agents/dispatch.sh:2484`). The follow-up builder inherits role and runtime and records parent and round (`internal/dispatch/build.go:869`, `internal/dispatch/build.go:893`).

4. Admit both supported context paths. For `resumeMode: resumed`, require the child's `sessionId == resumedSessionId` and consumed capability `adapterVerb: follow-up`. For `resumeMode: fresh-context`, require a different nonempty child session and consumed capability `adapterVerb: dispatch`. Its `resumedSessionId` must still equal the parent session. Missing or other resume modes refuse for a closure-backed follow-up. Native resume copies the parent session (`internal/dispatch/build.go:856`); fallback selects `fresh-context` and `dispatch` (`scripts/agents/dispatch.sh:2652`). The capability admission owner permits both verbs for a follow-up (`internal/dispatch/launch_capability.go:152`).

5. Run `validateHazardEvidenceAdmissionProvenance(P, P.jobId)` on P's own record. Reuse its validation, with the context-path correction in step 4. Today it assumes every follow-up uses the `follow-up` adapter verb (`internal/dispatch/hazard.go:431`). Correct that expectation for an explicitly recorded fresh-context follow-up. Do not weaken any other provenance check or borrow missing values from R. An intermediate ancestor needs the session/context joins above; this amendment does not make it another proving read or impose P's effort test on it.

   The provenance input is P's `jobId`, `operationId`, `round`, `proofLevel`, `fingerprintVersion`, `fingerprint`, `parentJob`, `instanceTag` and `launchCapability`. Recompute the fingerprint from P's `sessionKey`, `dispatchMode`, `resumedSessionId`, `runtime`, `canonicalModelKey`, `role`, `launchMode`, `permissionEnvelopeDigest`, `productRoots`, `capMin`, `capRequest.minutes`, `inputHash`, `goalId`, `goalRevision` and `destructiveReach`. Require the existing proven identity, version, shapes and digest equality (`internal/dispatch/hazard.go:403`, `internal/dispatch/hazard.go:449`). The nine capability fields are `digest`, `jobId`, `operationId`, `instanceTag`, `adapterVerb`, `status`, `mintedAt`, `consumedAt` and `supervisor`. Require P's matching identities, consumed status, ordered valid times and exact supervisor identity, as the helper does (`internal/dispatch/hazard.go:430`). This is the existing record-consistency proof, not a new attestation scheme.

6. Apply the existing effort predicate to P as well as R. Compare P's `configurationObligations.builderEffortTier` and `configurationObligations.builderReasoningEffort` with the implementation chain's required independent-critic tier and effort. Compare P's `reasoningEffort` with that required effort. Call `runtimeProvesMaximalExecution` using P's own `runtime` and `requestedModel`. Missing fields, weaker values, an unsupported runtime/model or a lookup error refuse. These are exactly the root predicate's inputs (`internal/dispatch/hazard.go:354`, `internal/dispatch/hazard.go:363`). Do not substitute P's independent-critique obligation rows or R's runtime/model.

7. Retain the rest of D4. The valid live closure must equal the complete subject of `finalHazardWorkState.job` and name its implementation root. Subject equality alone replaces exact `reviews == finalState.job` and the critic/work end-time ordering. Do not reintroduce those comparisons on P. The terminal subject comes directly from its immutable `review.json` and `diff.patch`, without the artifact-filtered dispatch selector or a retained workspace requirement (`internal/dispatch/read_subject_compute.go:87`, `internal/dispatch/read_subject_compute.go:120`). Lawful closure absence keeps the old strict root path. A malformed closure, missing terminal artifacts or failed proving-record check never falls back to that path.

All added refusals return `OpError` code 9. Use `hazardEvidenceProvenanceRefusal` for malformed or inconsistent admission/capability evidence. Use `hazardCritiqueClosureRefusal` for role, context, session or effort failure. Keep `hazardCritiqueStaleRefusal` for invalid closure selection and terminal-subject evidence. Diagnostics name the stamped root, proving job, closure round and failing field; an ancestor failure also names that ancestor. These reasons and code are already owned by hazard (`internal/dispatch/hazard.go:178`, `internal/dispatch/hazard.go:399`).

**Obligation edits and additions.** Continue section 9c's A ids after A-5. A-2 is PARTIAL under this amendment; A-6 through A-9 are MISSING. These are builder proof requirements, not completed proof. The expected failures below are against unit b before this amendment, where the closure branch accepts P without these checks (**unit b**, `internal/dispatch/hazard.go:373`). Author the negative cases before changing that behavior.

| Row | Obligation | Severity | Files | Named proof and expected failure before this amendment | Source |
| --- | --- | --- | --- | --- | --- |
| A-2 (edit) | Preserve prior-read and follow-up acceptance only with admitted independent proving records. Keep all existing terminal-subject and stale-closure refusals. | HIGH | `internal/dispatch/hazard.go`, `hazard_closure_test.go` | Extend `TestHazardCloseReadsClosureSubject` with paired full-record acceptance and missing-proving-session refusal. The latter currently succeeds. Build a real follow-up admission fixture; the existing fresh helper fixes round 1 and fresh dispatch (`internal/dispatch/composition_test.go:697`, `internal/dispatch/composition_test.go:710`). | D4; 10 steps 1-7 |
| A-6 | Select the closure's member and preserve independent context through its ancestry. Root freshness remains mandatory. | HIGH | `internal/dispatch/hazard.go`, `hazard_closure_test.go` | `TestHazardClosureProvingRoundContext`: refuse missing P session, any implementation session, wrong role, a resumed-session mismatch, and an intermediate round using an implementation session. In a three-round fixture, also refuse round 3 naming R as parent while round 2 remains present. Keep the valid round-3 native-resume chain as the paired guard. These bad-member cases currently succeed with a valid closure. | F-2; 10 steps 1-4 |
| A-7 | Validate P's own admission and both lawful adapter paths. | HIGH | `internal/dispatch/hazard.go`, `hazard_closure_test.go` | `TestHazardClosureProvingRoundAdmission`: remove P's admission; corrupt its fingerprint; copy R's capability; leave its capability unconsumed; mismatch resume mode and adapter verb. Each currently succeeds. Full native-resume and fresh-context records must pass. A direct helper case, `TestHazardEvidenceAdmissionAcceptsFreshContextFollowUp`, currently rejects the lawful fallback at `internal/dispatch/hazard.go:432`. | F-2; 10 steps 4-5 |
| A-8 | Validate P's obligation rows, recorded effort and runtime/model mapping. | HIGH | `internal/dispatch/hazard.go`, `hazard_closure_test.go` | `TestHazardClosureProvingRoundEffort`: independently omit or weaken each builder obligation row and reasoning effort. Also use a runtime whose mapping admits R's model but excludes P's model. Keep R fully valid and P's unrelated admission evidence consistent. Each currently succeeds; a fully proven P remains accepted. | F-2; 10 step 6 |
| A-9 | Prove the new refusal through implementation-chain completion without making critics require another critic. Preserve lawful absence. | HIGH | `internal/dispatch/hazard_closure_test.go`, `composition_test.go` | `TestCloseCheckRejectsUnprovenClosureRound`: make all other close prerequisites valid, then close a governed implementation chain whose stamped root is valid but P lacks admission. Current unit b accepts it. Add a valid P and require success. Run `TestCriticOnlyHazardClosureDoesNotRecurse` as a preservation guard (`internal/dispatch/composition_test.go:774`). Add absent-closure guards that retain exact root reviews/time refusals. | F-2; D3-D4; 10 steps 5-7 |

Assert code, reason, proving job and the relevant field in negative cases. A refusal caused by unrelated fixture damage is not proof of these rows. Keep the native-resume and fallback positive controls so copying the root's fresh-only predicate onto P cannot satisfy the tests.

**Placement and landing.** Unit b owns this work because it introduces the allowance that needs the proof. Do not defer it to unit c or part 1b. Section 9c assigns hazard and merge to b, landing and recertification to c, and later admission activation to 1b (`plans/critique-closes-on-folded-proof-design.md:276`). Unit b may land after F-1 and this amendment are folded, the named tests pass, and the seat completes section 9c.6's existing proof. Folding F-1 alone leaves the unchecked proving-round acceptance described above.

**Open questions for Wido**

None. This settles the delegated D4 scope question while preserving the required independent critique and both supported follow-up paths. It does not request a weaker independence rule or a new schema.

**Replacement page next step**

- Next step: finish unit 1a-ii-b by folding unit-b read F-1 and section 9c.10's D4 amendment, including A-2 and A-6 through A-9, then obtain the seat's section 9c.6 proof and land it; continue with unit 1a-ii-c for landing, recertification and bar (a), then 1b-i and 1b-ii in the existing order and under the existing briefs as amended here.

Proposed receipt: design | critique-closes-on-folded-proof | settle D4 proving-round independence after unit-b F-2 | code read; implementation and runtime proof remain with unit b.

## 10. Critique record

Round 1 (2026-09-13, Codex gpt-5.6-sol, read-only): eight material findings, all folded. 1: the boundary was path-keyed (a fold that fixes the finding and changes unrelated policy in the same file passed; a hand-edited record could widen it; directories and renames were undefined): now derived from the register, one file per artifact, renames expanded, and every hunk attributed to exactly one finding. 2: the evidence model could not represent the proof contract (project subtree versus whole tree, joined attempts, no finding-to-test mapping, the owner validation bypassed): now both trees are recorded, evidence is sorted attempt ids validated by the existing owner validator, and the manifest maps each finding to a test by execution identity. 3: the restoration rule was unsafe (reversing approved hunks yields an unread tree) and tree equality conflated live and commit subjects and collided with the hazard close: now a typed read subject, exact equality only, no restoration rule, and a persisted closure binding every gate consumes. 4: the transition was incomplete (partial certification, crash half-state, status versus resolution, landing's patch replay dropped): now exact coverage of the eligible set, an idempotent write order under the lock, `resolved/certified`, `bindCertifiedChange` kept, bar (a) amended. 5: a test-only fold could prove nothing: now the `proof-gap` kind with additive test hunks, and a `correction` must change its production artifact. 6: decision 4 lacked the machinery: now the grain field, the trajectory, the role's cap in the budget owner and the rebind, R-97 over R-60 for the role, and the code critique after the fixture exit named. 7: the metrics were not computable: now the refusal event is persisted and the loader reads landing provenance, subjects and fold outcomes. 8: the slices did not land alone: reordered with a schema slice first. Sound as built, per the critic: no certification of severe or unproven findings; a certification is not a read; every consumer recomputes and fails closed; the stop at zero material findings and no fourth round; the refusal before a successor record; the landing patch replay kept.

Round 2 to revision 3 (2026-09-13, Wido's word: option (a)): the certification record and its four contract findings (1, 2, 4 in its certification half, 5) leave for goal certification-closes-a-folded-bounded-finding; the folds that apply to the kept rules are in: role-discriminated read subjects persisted by the engine before launch (finding 3); one closure object on the implementation root, the stamp kept as an index (finding 4); the falling-trajectory condition, the tier enumeration and a schema-5 return with grain, version-4 returns normalizing to invariant (finding 6); durable refusal events with ids, mirrored and deduplicated, carried landings reported separately (finding 7); a schema slice first, dormant until its consumer lands (finding 8).

## 11. Escalation after round 2 (2026-09-13, for Wido; answered the same day: option (a), this revision)

The round-2 findings, in the seat's words: (1) a textual hunk is not a stable or complete unit of change (binary, mode and whole-file changes have none; context drift changes its digest; one edit can serve two findings; the finding's test lives in a file other than its artifact); (2) a passing test does not prove a finding (an empty test passes; a whitespace edit can cite an old test; the owner validator proves execution, not relevance); (3) one read-subject shape cannot serve design critics, live code critics after a rebase, and commit critics; (4) the closure would have two authorities (the existing stamp and the new binding) and bar (a)'s wording binds a certification to a read that had findings; (5) the write order is not idempotent as written (the timestamp differs on retry; the register projection is undefined; the locks do not cover every record written); (6) the round-two exit omits R-97's falling-trajectory condition, the cap is not enumerated per tier, and the return schema (v4, strict) cannot carry optional grain fields; (7) the refusal events and landing provenance are not durable or scoped (mirroring, pruning, carried landings); (8) slice 0 is behavioural after all.

The seat's reading: findings 1, 2, 4 and 5 are the certification's proof contract, and a third revision would be a third prose budget on the same question (D81, and this page's decision 4); findings 3, 6, 7 and 8 are buildable amendments with the critic's shape. What is buildable now without a new proof contract: the redundant-read refusal and the close on a prior clean read under one closure object that every gate consumes (section 5, with finding 4's one-object amendment and finding 3's role-discriminated subjects); the design-critic cap at two with R-97's falling-trajectory condition and a schema v5 for the grain (section 6 with finding 6); the measure on durable events (section 7 with finding 7). Those are the second and third rules of the goal and the stamp clause, and they remove the reads the audit counted as redundant. The certification record (decisions 1 and 2) is the first rule and the largest saving, and it is the part whose proof contract is not settled.

The decision, with a recommended default: (a) narrow this goal to the buildable parts (sections 5 to 7 with the round-2 amendments), and open the certification record as its own design-lane goal that starts from the critic's amendments (change units keyed by blob and mode, counterfactual test runs, a deterministic core digest, one closure object), scheduled behind this one; or (b) fund revision 3 of this page with all eight amendments, a third prose round on the certification; or (c) accept the risk of building decisions 1 and 2 as revision 2 states them. The seat recommends (a): it lands the two rules Wido has already ruled on and the measure that shows whether they pay, and puts the unsettled contract on its own page where a falling trajectory can be seen.
