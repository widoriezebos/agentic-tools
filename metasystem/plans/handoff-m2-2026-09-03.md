# Handoff m2, 2026-09-03 (the takeover seat on Wido's Mac, git nickname m2)

Continues plans/handoff-m1-2026-09-03.md, which m1 owns; this file carries the m2 seat's state.

## Takeover state, written by the m2 seat (this Mac) at 15:40 local, 2026-09-03

- Owner now: the Claude session on m2 (git nickname m2, lineage main-1788441779-14484-82d6ed).
  The m1 records of chain path-class-build2 are not on this machine; the change is
  recovered from origin branch preserve/path-class-build2-r5 (metasystem subtree
  852f7250, the reviewed tree). Its patch applies cleanly on origin/main 97b4ef9b.
- Replayed here on that tree: five Go packages green under -race (internal/landing
  76.3% coverage), the land fixtures green (4 legs). Two fixture scripts are RED:
  1. scripts/agents/path-class-fixtures.sh needs ripgrep (`rg`), which this Mac does
     not have and the command inventory in docs/project-rules.md does not declare.
     The same check run with grep finds no reader of the deleted tables. Already on
     main (the first part); tier-1 backlog item opened: path-class-fixture-ripgrep.
  2. scripts/agents/static-reproof-fixtures.sh, the leg added in this chain that
     expects a register-carriage landing of an unlisted root file (README) to refuse
     with path-unclassified: on the round-5 tree the commit LANDS. The round-5 fold of
     PCM-CC8-001 makes an application-owned path in the root layout answer `outside`,
     and the outside row passes register carriage. Round 5 was never replayed against
     the fixtures on m1. This is a material finding for the closing review: the leg
     and the evaluator disagree; either the leg picks a path inside the installation
     that has no class, or the design's root-layout answer is wrong.
- Waiting on Wido (both needed before the closing review and the landing can happen here):
  1. Re-arm this Mac's steward: dispatch is refused ("census fingerprint does not match
     the armed code") and `metasystem up` reports ENROLLMENT_DRIFT after the pull and
     engine rebuild. Either at an agent-free terminal on this Mac
     (`bin/metasystem steward restart --repo .`), or by his verbatim word in the chat
     with a review date (the R-29-m2 path, `steward arm --temporary-human-word`).
  2. Move the claim of goal path-class-manifest from m1 to m2: `goal steal` is a
     human act (--by) and needs a fresh norm token row (goal=path-class-manifest
     minutes=3000 goalRevision=9), which the seat mints under R-58-m1 on his word.
- Not blocked, done: the 14 digest lines this Mac's steward wrote while its checkout
  was 750 commits behind are re-appended to records/narrator-digest.log (uncommitted).
- 17:15 local: Wido's word in chat ("I won't do that; you will have to") armed this Mac's steward
  through the temporary-word path, review date 2026-09-06. The fleet Telegram channel is configured
  in metasystem.conf.local and message id 8 was posted. Dispatch's census precondition passes at the
  outer repository scope. FOUND: every `metasystem up` (the stop hook runs it at each turn end) kills a
  runner whose first tick has not completed, so the steward never completes a tick on this Mac
  (backlog item up-kills-runner-before-first-tick). Do not run `up` while the runner is attempting.
- Lineage note: the checkout lease announces main-1788441180-48662-67e8bb for this session; the goal
  opens of this takeover were journaled under the seat-minted main-1788441779-14484-82d6ed.
- 18:1x local: Wido answered "yes" to moving the manifest claim here; token row R-70-m2 appended to
  memory/rulings.md (uncommitted). The steal was refused by the new execution-approval gate
  (human-approval-for-execution landed while this seat waited): "APPROVAL_REQUIRED ... only the
  human approves it with goal approve". The harness classifier blocks the seat from typing
  `goal approve --temporary-human-word`; Wido types it himself or grants the permission. Then:
  steal, dispatch the apply brief, conformance review, closing review, land.
- 18:5x local: THE SECOND PART IS LANDED on main as a1014fe6 "Landing rules by class", chain
  path-class-build2b on m2 (round 1 = byte-exact re-issue of m1's round-5 tree; closing review
  path-class-build2b-cc1 found the one material finding PCM-CC9-001, the unclassified-path fixture
  leg, folded in round 2 into the vendored layout; final review path-class-build2b-cc2 zero
  material). Records landed at 9bcfc6bb. Next under this goal: the second landing bar promotion
  (mint R-65 first), then release. The steward on this Mac still never completes a tick
  (up-kills-runner-before-first-tick) and the Stop hook still loops; the Stop entry removal in
  .claude/settings.json is Wido's to make (the harness refuses the seat).
