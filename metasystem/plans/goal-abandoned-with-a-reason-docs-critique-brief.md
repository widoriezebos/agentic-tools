Working Mode: design
Orchestrator Identity: m1b (lineage main-1789191336-90295-e4b24b, dispatch delegate under goal goal-abandoned-with-a-reason)
Date: 2026-09-14

# Review brief: the documentation amendment

FINDING IDS: GAWR-DOC-01 onward.

Why this review exists: section 11 of
metasystem/plans/goal-abandoned-with-a-reason-design.md (revision 7) is the
doc amendment for this goal. A round wrote it into
metasystem/docs/backlog-mechanism.md and it landed at e35aef8a with the
landing side. This read is the chain's independent critique.

What the round reports: "The drop rule" becomes the page's two cases (the
want evaporated, conclude it; the want survives while the pursuit stops,
abandon it, with what an abandoned goal does and the two nevers), and
"Concluding a goal" gains the sentence about an abandoned goal writing no
journey chapter. No other section changed; the audit and the fast gate
passed.

Round budget: 1 focused round. The work is landed, so a material finding
becomes a fix-forward, not a held landing. A finding is material only if
the text must change, and it names what is wrong.

# Mandate

1. Does the landed text say what section 11 decides, without adding a rule
   the page does not make or dropping one it does?
2. Does it read as this file's voice, plain and short, and does it fit the
   surrounding sections rather than sitting beside them?
3. Does anything in the file now contradict it, in particular any other
   place that tells a reader to conclude or to park work that will not be
   done?
4. Nothing else.

# Expected Return

The code-critic return schema, findings sorted by materiality, each with
file:line evidence and its rigor row. Every path in your return starts
with `metasystem/`.

# Gap Rule

stop and report a gap; never fill it silently.
