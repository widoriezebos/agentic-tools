Working Mode: implementation
Orchestrator Identity: root Codex
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| complete-public-planning-journey | 6000 |

You are rostered Opus5.5 completing the accepted planning workflow in an isolated
private checkout. Root authored design and Fable critiqued twice; user explicitly
permits machinery bypass and ignoring stale stop hooks. Root runs combined Sol
review, final coverage and integration. No paid fixture models or native agents.

Read PRIMARY absolute supplied planning design, parent design, Fable reports and
return7. Your seed is an unreviewed current implementation, not a certified base.
Do not edit primary. No commits/pulls/pushes, root plans/control/receipts/config.local.

Finish actual planning acceptance, in this order:
1. Real dispatch.sh follow_up from public review design --dispositions, using the
   authentic dispatch-b claim fixture. IP-C4/C5: refreshed subject, actual returned
   child, lost-response replay, intervening round/goal revision, live refusal/death
   proof/cap in SAME chain, positive first-critique claim. No fake dispatch callback
   as substitute. Existing operation lookup/recovery was implemented but these are
   unproved. No bypasses in production authority or weakened fixture gates.
2. Clean design review through public review design --dispositions must complete
   whole close without a user's separate close/collect. Current return7 only proves
   accepted-material refusal; positive fixture lacks an accepted ledger tree for
   register fold. Use authentic ledger-backed connection/dispatch bed, not fake
   close. Retain accepted-material refusal and exact frozen subject binding.
3. Actual author prompt contract: intent_design.go says "staged page you were
   given" but omits its path. Manager sets Page/Outputs but Claude command only
   sends brief/read packet. Confirm and fix through existing declared-output
   contract. The external fake author must read destination ONLY from actual
   prompt, not secretly know StartSpec.Page or scan request metadata.
4. Positive design cancel/dead-start recovery and author invoked from another
   checkout; clean publication/collision/replay. Use existing atomic Start recovery,
   never nil-supervisor alone as proof. Produce executable disposable CLI recipe
   using real launch process with fake provider + actual dispatch.sh, not just Go
   callback recipe. Root will rerun final binary. Starting point read-only recipe:
   /Users/wido/LocalStorage/agentic-tools-evidence/verbs-match-intent-20260925/
   intent-planning-cli-verification-recipe.md. It currently ends with manual close;
   replace that with public review completion. Keep fixture-only approval truthful.

Exclusive product/test scope:
- cmd/metasystem/intent_design*.go and intent_planning*.go;
- internal/launch/design_request*.go;
- internal/dispatch/design_chain.go;
- scripts/agents/dispatch.sh and dispatch-fixtures.sh, only needed existing
  plumbing/test registration for this workflow; logic/tests in Go where possible.
- new narrowly named cmd planning/dispatch integration test if necessary.
Shared CLI main/intent.go/intent_operations.go/intent_unit_review.go/help/docs,
testing.json and every other launch file are owned by main builder. If necessary
emit a tiny proposed shared-seam patch under ignored artifacts, DO NOT edit shared
files. Root/main integrate after your return. No carried/manual-owner edits.

Tests retain current discovery/gate coverage. Report new names for main to register.
Avoid parallel ratchet regressions: per-instance dependencies + t.Parallel unless
actual t.Setenv/global subprocess custody requires serial; explain concrete reason.
Bounded focused tests nine workers, inherited allowances intact. No broad suites
until combined candidate. Budget180 NEW tools or40 actual minutes; return coherent
checkpoint with actual exits, precise residual gaps, all changed paths and actual
owner versus seam. Output artifacts/agents/intent-planning-completion-return.md.
