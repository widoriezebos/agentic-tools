Working Mode: design
Orchestrator Identity: m1d (lineage main-1788941004-20871-6e7a43, coordinator under goal human-carried-landing-carry)
Date: 2026-09-11

# Review brief: second read of the carried-landing design, revision 4

FINDING IDS: chain-unique, continue the register: HCL-C-33, HCL-C-34, ... never F-n. A re-opened finding keeps its own id.

## What you are reading

`metasystem/plans/human-carried-landing-carry-design.md`, revision 4, at
commit 4d4dfa54a in your worktree (a local scaffold commit: the page
lands on `main` together with this read), sha256 5e4ad956cf52bad3274f69b478ff5fb850a73612a00ee4353edd566cdbbf2f87, folded by
job hcl-design4-20260911 on the Fable lane from your first read
(`metasystem/artifacts/agents/hcl-context/critique-r1.md`, with the
coordinator's dispositions at its end, all nineteen accepted). The fold
brief is `metasystem/artifacts/agents/hcl-context/design-fold-r1-brief.md`.
The "Declared Outputs" digest line the dispatcher stamps into your prompt
is the digest of the outputs manifest, not of the design.

Write your register as this new file: `metasystem/records/misc/human-carried-landing-carry-critique-r2.md`
If your runtime is read-only, return it in the job return; the
coordinator projects it.

Read-only design critique; implement nothing, run no bed. This is
fold-read cycle 1: the page should now be buildable; say so plainly if it
is, and name the fold if it is not. A material finding is one that changes
what gets built. Read the code at HEAD.

## Settled, do not re-derive

The dispositions in the read-1 register are binding. Points 01 and 03 on
the old page; the two proofs; the word as a workspace tree; one named
refusal or group per word; the review deferred never deleted; the cap and
the debt as asks; one implementation chain.

## Mandate, in order of consequence

1. **The nineteen, one line each.** For HCL-C-21 to HCL-C-32 and the
   re-opened HCL-C-02, -03, -04, -07, -11, -18, -19: is the finding
   answered by the text, with the fixture it needs? Where not, that is a
   finding.
2. **The match rule and `unneeded` (HCL-C-21, -22).** One word, one
   defect: find any landing the new rule lands with two defects, and any
   landing it refuses with exactly the named one. Check that `unneeded`
   leaves the word open and lands nothing.
3. **The transaction under the lease (HCL-C-04, -18, -19, -26).** The
   whole sequence inside one `lease run-held` section; the `carrying`
   journal entry before the push; recovery that keeps the rebased commit;
   the counselor postcondition. Find a crash or race that lands twice,
   consumes without landing, or leaves the obligation or the counselor
   line unwritten with no repair.
4. **Consumption and idempotence (HCL-C-24, -25, -23).** The ancestry
   scan anchored on the word's ledger commit; the full-payload replay
   check; the terminal `superseded` state.
5. **The ledger tip and the seat (HCL-C-28, -29, -30).** One fresh
   validated tip for word, cap and debt; the word's seat and the actor
   match; `--supersede` preconditions rechecked in the mutation.
6. **The authority wire (HCL-C-02, -27).** Generation zero refused outside
   a fixture-authorized root; the fence chosen for the new outcome and its
   old-engine fixture; the proof file as audit evidence only.
7. **The base judge (HCL-C-03).** The authenticated live-failure input;
   parsing without the failed engine; the list of files whose change
   makes the base judge refuse.
8. **Contract ownership and fixtures (HCL-C-31, -32, -07).** The surfaces
   and groups for the five packages; every fixture a future test with an
   exact oracle; the accepted-risk record schema complete.
9. **Buildability.** One chain: say whether it is buildable in two Sol
   rounds and name anything that can be a follow-up without weakening the
   goal's sentence.

## What is not in your mandate

Style, length, whether the feature should exist, durations. Do not run
beds. Do not re-derive read 1.

## Return

One register at the path above: findings by id (HCL-C-33 onward, or a
re-opened id), each with the section, the claim, the evidence (file and
line at HEAD), and what changes if it stands; then the nineteen one-line
verdicts; then a verdict line: buildable as is, or one fold naming which
findings. Wall-clock budget: 40 minutes.
