# Known Issues

A standing register of defects and limitations that are recorded but not scheduled: capability ceilings, accepted trade-offs, and dead ends that must not be silently retried. Handoff notes die with their stream; this register survives so the next session does not rediscover the same wall at full price.

Each entry carries a stable id, the date, the observable symptom with evidence anchors, the realistic cost when it bites, and the fix direction or named escalation lever. Append dated updates to an entry; never rewrite a concluded one. A recurrence is appended to the existing entry and raises its priority.

| Id | Date | Symptom and evidence | Cost when it bites | Fix direction or lever | Status |
| --- | --- | --- | --- | --- | --- |
| KI-1 | 2026-08-04 | `last-census.json` records `durationMs` while section 3.11 names `durationSeconds`; naming only, behavior correct | A reader or later consumer looks for the wrong field | Align the design text or the field at the docs pass (item 19 vicinity) | FIXED 2026-08-06: the documentation now names the field the artifact actually writes (durationMs), so a reader looking for a timing number finds it where it is |
| KI-2 | 2026-08-04 | Validation suite wall time grew 2m14s to 4m38s across items 3 to 23 on the same machine | CI cost and slower correction loops; a suite nobody runs locally stops catching environment defects | Profile the fixture set, parallelize independent groups, or split fast and slow suites; measure before optimizing | ACCEPTED 2026-08-06 with a measured trigger: the suite's wall time is recorded from timed green runs on a named machine, and a run exceeding 1.5x that number prints a notice. A notice, never a failure — timing must not make the gate unreliable again |
| KI-3 | 2026-08-04 | Adoption fixtures emit BSD `diff: Directory loop detected` diagnostics on symlinked skill registrations; exit status stays 0 | Noise that trains readers to ignore validator output | Compare registrations without following symlinks (`diff -r --no-dereference` or an explicit link check) | FIXED 2026-08-06: the adoption check compares two directory trees by walking them explicitly (same file lists, same file contents, same symlink targets) instead of shelling out to a tool whose warnings about symlinks could hide a real difference RECURRED 2026-08-14: the adopt idempotence check (second-run snap-vs-target) still shelled out to `diff -r`, which skipped the symlinked skill directories behind the same warnings; fixed the same day with the explicit walker there too. |
| KI-4 | 2026-08-04 | Census cost. The row's ORIGINAL cause was wrong and is corrected here by measurement: cost is not proportional to total machine processes (enumerating 1238 of them costs 46ms). A profile put 307ms of a 475ms census in six separate lsof invocations, one per agent-shaped candidate, because lsof costs ~51ms per call no matter how many pids it is asked about | A busy machine could push a scan past its interval and trip the new over-interval warning continuously | The warning is the tripwire; if it fires in practice, cache the signature-negative pid set between scans keyed by pid and start time | FIXED 2026-08-06: cwd resolution batched into a single lsof for all candidates, with per-pid fallback preserving the alive-versus-denied distinction. Census 442ms to 214ms on a 1230-process laptop; supervision fixtures S4-1..S4-14 green including enumerate-filter-resolve. Reopen trigger unchanged: a census whose duration exceeds its own interval |
| KI-5 | 2026-08-04 | Supervision fixture "S4-8 inferred announcement" timed out once under load in a delegate's environment; two clean runs here at 4m30s each | A flaky gate erodes trust in the suite and hides real regressions | Make the fixture's arming wait proportional to measured census duration rather than a fixed 12s cap, or serialize it away from other supervisor-starting fixtures; reproduce under parallel load before changing | RETIRED 2026-08-06: this was a single timeout under load whose cause is now addressed by the load-immunity rework and the faster process scan. If it recurs it reopens as its own row with fresh evidence |
| KI-18 | 2026-08-06 | Under the new fixture-fast supervision intervals, the census fingerprint changes mid-suite nondeterministically and every subsequent fixture dispatch refuses. Two full runs failed at DIFFERENT fixtures with the identical refusal; hypothesis: a component self-heal under fast heartbeat thresholds rewrites supervision state, which the fingerprint covers, while the armed fingerprint stays frozen at arming | The combined F-4 + load-immunity batch cannot pass the gate, so roughly twenty local commits wait unpushed; the gate itself is the blocked artifact | Decide at design level whether the fingerprint should cover live component instances at all (code+signatures+configuration may suffice, with instance custody checked separately) or whether self-heal must atomically re-record the armed fingerprint; then fix under the loop with an investigator reproduction harness OUTSIDE the full suite | FIX SHIPPED, PROOF INCOMPLETE 2026-08-06. The fix is merged and correct in every observation, including a captured real refusal naming both generations and both remedies; the suite-blocking symptom is gone. What remains is the 20-iteration harness proof: it passes at 1 iteration and fails at scale because its sandbox accumulates supervision state across iterations until arming cannot converge. That is scaffolding work, not product work. Next session: make the harness arm ONCE and heal within a single armed generation-chain rather than re-arming per iteration |
| KI-16 | 2026-08-06 | A mission whose host closes its streams in the same turn that meets the gate is recorded `parked all-streams-parked`, not `completed`: the runner processes stream updates before the completion measurement. The first BM-1 trial ended exactly this way with self-assessment=1 on the tree | A successful run reads as a park in the ledger and the scorecard's fence economy, understating success mechanically | Order completion before park classification in the runner's end-of-cycle sequence, with a fixture where the host both meets the gate and closes its stream in one turn | FIXED 2026-08-06 per D-M1: the runner measures the gate before concluding a park, fixtured on trial 001's exact shape |
| KI-17 | 2026-08-06 | Mission hosts leave delegate chains unclosed: the BM-1 trial's host dispatched two implementer chains, merged their work, and never closed either, so the scorecard's `everyChainClosed` validity gate fails on an otherwise strong run | Every mission scorecard reads invalid until hosts close chains, masking real validity signal under a constant one | Either the assembled mission prompt's standing rules gain the close-by-join duty the interactive contract already has, or the runner closes terminal chains at mission end and records that it did; decide at the next design pass, not silently | FIXED 2026-08-06 per D-M1: the runner closes terminal chains at mission end with a runner-closed stamp, so scorecard validity can hold and host discipline stays distinguishable from mop-up |
| KI-15 | 2026-08-05 | A delegate cannot run `scripts/validate-metasystem.sh`. The suite's mission-runner fixtures need real `ps` access and every delegate sandbox denies it, so the run dies with `Operation not permitted` late in the fixture | Briefs that make the full suite an acceptance criterion are asking for something structurally impossible, which turns a correct delegate into a gap-stop and puts the gate on the orchestrator without saying so. It cost a full round trip the first time it happened | Briefs ask the delegate for the checks it can run (syntax, targeted fixtures, its own new fixture) and state plainly that the orchestrator runs the suite outside the sandbox; longer term, either the census fixtures gain a no-ps mode or the delegate presets grant process visibility, which is a permissions decision, not a test decision | ACCEPTED 2026-08-06. Helper agents cannot run the full test suite because it watches real processes and their sandboxes forbid that. They now have --delegate-scope, which runs everything not needing process visibility and prints exactly which parts the orchestrator still owes. Reopen trigger: sandboxes gain process visibility |
| KI-14 | 2026-08-05 | The census classifies this session's own tool shells as UNTRACKED claude agents. A background zsh wrapper whose argv carries `.claude` paths and plugin environment exports matched the claude runtime signature (pid 47819 in the live stop-hook report), because signatures match argv substrings and the wrapper quotes entire heredocs containing those paths | Every long-running tool shell can appear as an untracked agent, training the operator to ignore the one report class that caught a real untracked peer session the same day | Sharpen the signature: match the executable or command word, not arbitrary argv substrings; the adapters own their signature tables, so the fix is per-runtime table precision plus a fixture with a lookalike wrapper argv | FIXED 2026-08-06: all three runtime signatures anchored to the command word at the start of argv; the S4-7 contract fixture already asserts positives and lookalikes per runtime |
| KI-13 | 2026-08-05 | The validation suite leaked a supervision owner for every fixture repository it armed. `agent_supervision_repo` was a single variable, reassigned as the suite moved between the agent, runner and selftest repositories and emptied before exit, so the cleanup trap shut down nothing. Two owners were found alive after 25 hours and 78 minutes, each running a watcher and reaper that scan every process on the machine every five seconds. Killing the components does not end it: the owner restarts them by design, so only shutting the owner down works | Compounding background load on the developer machine, invisible because the leaked supervisors are scoped to their own deleted fixture repositories and so never appear in this repository's census. A plausible contributor to KI-4's census cost and to the CI capacity failure in KI-10 | FIXED 2026-08-05: cleanup tracks every armed repository in an array and shuts all of them down. Proven by counting supervisors before and after a full run | FIXED |
| KI-12 | 2026-08-05 | The permissions envelope's `network` field is not honoured by any adapter. `scripts/agents/adapters/codex.sh` hard-codes `sandbox_workspace_write.network_access=false` on both the dispatch and resume paths, and no adapter reads the field at all. Both shipped presets deny network, so today the record and the behaviour agree by coincidence; an envelope requesting `network: allow` would be recorded as allowed and still be denied | A delegate cannot be given network even when a project wants it, and the job record would claim a permission the run never had. The same asserted-not-measured class as KI-8, pointing the other way | Read the field and map it per runtime (`network_access` for codex, the allowed-domains list for claude, the sandbox settings for devin), and prove it in each adapter's selftest by attempting a fetch and recording the observed result rather than the requested one | FIXED 2026-08-05 in `74e2dad`: the envelope's network field is read by every adapter and delegates get network by default; the status cell was never updated when the fix landed, which is the register's own drift class |
| KI-11 | 2026-08-05 | Dispatch refuses when the census is one second past its interval. A follow-up was refused with `census verdict is stale (age=61s interval=60s)` while supervision was healthy: the scan itself takes 393ms, and the freshness rule allows no margin for a scan that starts late on a busy machine | A refused dispatch costs a round trip and reads like a supervision failure when nothing is wrong. On a loaded machine it could refuse repeatedly | Allow a small grace above the interval, or judge freshness against the last scan's start rather than its completion. Related to KI-4, which anticipated census cost on loaded machines; this is that cost arriving as a refusal rather than a warning | FIXED 2026-08-06: a status check is accepted up to twice its own interval and never beyond three minutes; past that the refusal names the age, the limit, and the two things a human can do about it |
| KI-10 | 2026-08-05 | The process-owning fixtures need watcher, reaper, and census loops running for more than one repository at once. A free shared GitHub runner does not have the capacity: the suite reaches the isolated runner repository's arming, goes silent for 74 seconds, and exits 1 with no assertion message (run on `cd4558b`) | None to the work. The human ruled 2026-08-05 that the local suite is the gate and that free-tier capacity is not something we design around. The cost is losing a second machine's opinion on the heaviest fixtures, which had twice caught real portability defects | Accepted, not fixed. CI now runs with `METASYSTEM_SKIP_AGENT_FIXTURES=1` so its cheap checks stay useful instead of failing forever and training everyone to ignore the signal. If a second environment is ever wanted for these, it needs a paid or self-hosted runner. The open question worth keeping is the metasystem's own resource footprint, which has never been measured and matters for whatever machine runs it unattended | ACCEPTED |
| KI-9 | 2026-08-05 | A delegate in a job worktree cannot run git write commands. Git keeps a worktree's metadata in the main repository's `.git/worktrees/<name>/`, which is outside the job's declared writable roots, so `merge`, `commit`, `checkout` and anything else needing `ORIG_HEAD.lock` or the index lock is denied. Found when a brief told an implementer to merge a branch first (`implementer-20260805t063758z-3648`, exit 128 with the lock path named) | A brief that assumes a delegate can use git wastes a round, and the failure reads like a permissions bug rather than a design boundary | No code change: the metasystem already computes the base-to-working-tree diff itself, which is why implementers report a file boundary rather than a commit. Briefs must not ask a delegate for git writes, and the implementer preamble should say so plainly at the next docs pass (item 19) | ACCEPTED 2026-08-06. Helper agents do not run git commands; they report which files they changed and the orchestrator integrates the work, which happened roughly fifteen times in one session without a single case needing anything more. Reopen trigger: a task that demonstrably cannot proceed without the helper committing midway |
| KI-8 | 2026-08-04 | The recorded permissions envelope is asserted, not measured. `record_actual_workspace_write_scope` in `scripts/agents/adapters/runtime-common.sh` writes `effective.writeRoots = [workspace]` from the stated belief that every CLI makes its cwd the sandbox write boundary, and `fail_if_effective_wider_before_launch` then compares that asserted value against the request, so the two always agree and the guard cannot fire. The premise broke in practice: `codex exec resume` takes no `-C`, so follow-up rounds ran with the adapter's cwd and job `implementer-20260804t131716z-231f` wrote four new files into the main tree while its record claimed the job worktree. The cwd half is fixed (`codex.sh` now enters the workspace like `claude.sh` and `devin.sh` already did); the asserted-versus-measured half is not | A boundary the records claim but do not hold. With a peer agent in the same tree it is a collision risk, and every retrospective reading those records is reading a claim rather than an observation | Make the effective envelope an observation: the adapter `selftest` already has a permissions leg, and C4-8 already requires recording which fields were proven behaviorally versus only constructed as flags. Prove write denial outside the workspace there, per runtime, and let the record carry the evidence level | ACCEPTED 2026-08-06. Each agent runtime now DECLARES which parts of a sandbox it actually enforces (writes, reads, network) in its capability record, and every job record points at the declaration it relied on, so nobody can mistake what we asked for with what was enforced. The simulated runtime is probed for real in the test suite. Reopen trigger: any observed case of a helper agent exceeding its sandbox |
| KI-7 | 2026-08-04 | A sweep during the launch window can mark a starting job `process-lost`. `record_create` writes the record `pending` with a null `pid`, and the launcher patches identity only after the census resolves the pid; a `reap` in between finds no supervisor match, and `wind_down_group` treats an unrecorded pgid as "nothing to kill" and returns success, so the sweep proceeds to the terminal write. Found while diagnosing the `pending-loss` fixture flake (`scripts/agents/dispatch.sh` `reap_one_locked`, `wind_down_group`) | On a loaded machine the standing reaper could fail a job that was starting normally; the window is short, and no live occurrence is recorded yet | Distinguishing "launcher still starting" from "launcher died before recording identity" needs a signal, since the second case genuinely is lost: the start gate already exists as a candidate, or a grace window keyed on `startedAt`. This is adjacent to watch-list finding C11-3 (pre-spawn durable intent), triaged decide-now, so decide both together rather than patching here | FIXED 2026-08-06: a job that is still starting up carries its own startup allowance, and the sweep leaves it alone until that allowance runs out, so a healthy job is never mistaken for a dead one |
| KI-6 | 2026-08-04 | Evidence mirroring fails silently. Every `mirror_record` call site in `dispatch.sh` (reap, loss, timeout, close paths) swallows failure with `\|\| true`, so an unfilled or relative `evidence.root` means nothing is ever mirrored and no status turns red. Found when `close` refused the benchmark critique chain with "cannot close an unmirrored chain" after five completed rounds; the standing reaper had been failing every mirror into its log since arming | Paid raws live only in gitignored `artifacts/`, which `git clean` reaches, so the only copy of expensive evidence can vanish; the failure surfaces only at close time, long after the runs | Surface a failed mirror as a watcher-visible condition (a per-job field the watcher reports, not a log line), and have `arm-supervision.sh` refuse or warn loudly when `evidence.root` is a placeholder or relative. Related project-side gap: this repository has never declared its own evidence root, and both `metasystem.conf` and `docs/project-rules.md` still hold the shipped placeholder | FIXED 2026-08-06 (remaining half): mirror_record now appends every failure to artifacts/agents/mirror-failures.log with a timestamp before its caller continues, so a swallowed return code still leaves a durable, greppable trace at the moment of failure. UPDATE 2026-08-13: the logging half was not the last tooth — a dispatch-refused husk (setup phase, no mirror, held only by its fence reservation) listed as a closeable chain, its close refused with "cannot close an unmirrored chain", and the runner aborted the whole close sweep on that first refusal, stranding every real chain behind it and failing the mission runner (both reps of benchmark cohort bm-1-20260813t055303z lost their everyChainClosed gate to this). Fixed: CloseableChains skips setup-phase husks that mirrored nothing, and closeTerminalChains finishes the sweep and reports all failures by name instead of dying on the first |
| KI-19 | The runtime identity hash treats a CLI's self-written state as configuration. Codex stores seen-notice and migration bookkeeping inside its own config.toml; any interactive use rewrites those bytes, the hash changes, and dispatch demands a fresh probe though behaviour is identical — three spurious re-probes on 2026-08-07 alone, and one of them was initially misblamed on an unrelated Devin install. Fix through the loop: hash a filtered view that drops the CLI's self-written sections, with a fixture proving a notice-flag touch does not change identity while a model or permission change does. | OPEN |
| KI-20 | Design-critic follow-up rounds review a stale tree: the worktree freezes at round-1 dispatch, follow-ups never sync it, and the delegate cannot run git to sync itself — two consecutive rounds re-reported already-folded findings as absent on 2026-08-07 before the orchestrator noticed. The collaboration-loop design requires re-sync on follow-up (its CL-2-8 amendment); the shipped implementation covers the code-critic path only. Fix through the loop: dispatch follow-up syncs the worktree for read-only critic roles and records the reviewed commit, with a fixture proving a post-fold follow-up sees the fold. Interim: the orchestrator syncs by hand before each critic follow-up. | OPEN |
| KI-21 | Two main agents in one repository share a working tree with no coordination contract. Observed 2026-08-07 when the human launched a second session in this folder: both mains registered correctly and the census tracked both (that machinery held), but the peer launch churned the shared claude CLI config state, refusing the first session's dispatches until re-probe (KI-19's claude flavor), and both sessions share one git index and uncommitted state, so either can scoop the other's half-written edits into a commit — the 0b9ca1b pre-commit guard covers only new plan files. The sharpest tooth, proven by the peer session's transcript: the open-work stop hook COMMANDS every main to start any unclaimed open work, and the plans' in-flight lines were stale prose, so the second session was told to begin a design stream the first was actively running — it was one human rejection away from re-arming supervision under a live --wait and double-dispatching the same stream. Streams need an owning session recorded mechanically, not prose. Interim rule, effective now: one main per checkout; a second session works in its own clone or worktree. Durable fix through the loop: a main-agent claim on the working tree (advisory lock surfaced by the stop/start hooks) and staging discipline that refuses to commit files the session did not itself modify. | OPEN |
| KI-22 | Delegate returns echo the orchestrator's session id and the protocol error this causes is detected, then ignored. Every design-critic follow-up round of chain design-critic-20260807t063006z-8fb2 (r2-r7) is stamped failed/protocol_error — the return's sessionId is 'af965b26', copied from the brief's 'Orchestrator Identity: ... session af965b26' line, while the job record holds the real runtime session. assert-return-complete caught it EVERY round; follow-up treats protocol_error as resumable; the findings were used with no surfacing anywhere a human or orchestrator looks. Found by the peer session on 2026-08-07, reading from outside. Two fixes through the loop: the return's sessionId comes from the adapter's observation, never the delegate's claim (the normalization already backfills it — the claim must not win); and repeated protocol_error rounds surface in the turn report. Interim: brief headers no longer contain the word 'session' for the orchestrator. | OPEN |
| KI-23 | UNTRACKED reports nag forever for processes the human has already judged harmless. The human's idle Devin install (an editor ACP server whose cwd is the repo root) appeared in every stop-hook line on 2026-08-07 despite having no job records, no worktrees, and no writes. The classification is right; the repetition is noise that teaches the reader to skim, which is how a real untracked agent slips by. Fix through the loop, after the coexistence design lands (same files): an acknowledge command records pid, start time, and a human-stated reason; the census reports acknowledged processes once at acknowledgment and then stays silent about exactly that pid+start pair, expiring naturally with the process. New untracked processes still shout. Interim: launch Devin from outside the repository folder and the census correctly loses sight of it. | OPEN |
| KI-24 | TWELVE PRODUCT DEFECTS FIXED 2026-08-07, all committed; the remainder is now a single well-understood shape. Fixed: census dropped older announcements, then exact-set equality dropped newer ones, and the LEASE HELPER carried the identical bug independently; lease locks blocked forever; the lease helper resolved the census helper against the target root so no announcement could succeed without the harness present; the supervision owner claimed the checkout and refused its own parent; authentication and start-times preferred different process tables, splitting one identity in two; announce and classify used different readers; the census-writer lock released in two steps and left ownerless husks that block all future writers; dispatch's internal helpers were re-gated for holdership their outer command already held; the lease lock file was demanded rather than created; and an unclaimed checkout reported no holder, refusing every first write in a fresh repository. REMAINING: the S4 fixtures drive dispatch from contexts whose ancestor is a simulated agent, so the authority matrix classifies those callers as DELEGATE and refuses holder-only writes — correctly, by design. Aligning them means giving each fixture driver a real announced main identity rather than an agent-signature ancestor, in the phases that perform holder-only writes. That is fixture work of a known shape, not an unknown defect. UPDATE 2026-08-07: the fixture alignment is done for the supervision and mission suites, and doing it turned up seven more product defects, all fixed: the census-writer lock claimed in two steps (a contender could read an ownerless lock and refuse forever) and released without checking it still owned the lock (a departing writer deleted its successor's owner file); both directory locks in dispatch had the same two faults and now share one primitive that claims by renaming a fully-formed directory into place; supervision replaced its own newborn components because a component that has not yet heartbeat looks exactly like one that stopped, so every arming flapped and each flap left a moment with no census writer; `claim_for_announcement` was DEFINED TWICE and the second definition silently replaced death-only takeover, the stale-job sweep, takeover history, and inherited protocol-error reporting; the census deleted the announcement of every real main whenever a process fixture was installed, because absence from the simulated table was read as death; the stop hook answered an unidentifiable caller with OWNED-ELSEWHERE, which replaced the entire end-of-turn report including the refusal to walk away from open work; and the reaper and the dispatcher diagnosed a stalled handshake at exactly the same age, so which verdict a record kept was a coin flip. The fixture side of the alignment is one rule: a fixture that performs control-plane writes announces itself as the checkout's main first, which is what the authority matrix requires of any writer. | FIXED 2026-08-07: both gates green and pushed. The last three defects the alignment surfaced were the mission runner announcing itself as a second main in a checkout its own parent holds, an explicit `reap --job` returning success without looking at the job whenever the standing reaper held the lock, and the handshake timeout writing its verdict only from `pending` when the case it exists for leaves the record in `running`. |
| KI-25 | 2026-08-07 | One suite run in ten failed with `fatal: cannot lock ref 'HEAD'` in the agent-fixture repository, right after the conformance section; the same commit passed on the next run. RECURRED TWICE on 2026-08-08 as the sibling `fatal: Unable to create '<tmp>/agent-fixture/runner-repo/.git/index.lock': File exists`, both times taking down a chained gate+push at a cost of a full re-run (~7 min); both re-ran green with no change. Predates the lease-succession and mission-anchor work (it first hit before either landed), so it is fixture concurrency in the agent fixture's own scratch repository, not the harness under test. Worth a real diagnosis rather than more re-runs: the two lock names (HEAD and index) suggest two fixture processes sharing one scratch repo and on the run before it. Two writers touched that repository's HEAD at once — a fixture git command and a fake adapter's job, most likely — but the losing command was not named in the output | A green batch can be red-gated by chance, and a flake that nobody can attribute trains people to re-run instead of read | Reproduce with the agent-fixture section alone in a loop; if it recurs, name the two writers by having the fixture's git commands carry `GIT_TRACE` or by serialising job commits against fixture-driven ones | OPEN, one observation |
| KI-26 | 2026-08-07 | The version-2 return schema gave its new `claimed` object no `required` list, and OpenAI structured output rejects any object schema that omits one: `Invalid schema for response_format 'codex_output_schema': In context=('properties', 'claimed'), 'required' is required to be supplied`. Every codex delegate dispatch failed at `turn.failed` before the model produced a token. The schema fixtures passed throughout, because they validate returns against the schema and never hand the schema to the API that enforces it | Every delegated critique and implementation on codex is dead on arrival, and the failure looks like a runtime error rather than a schema defect | FIXED 2026-08-07: `claimed` lists both members in `required` and types them nullable, the shape every other object in this family already uses; normalization fills both members whenever it emits the object, and a fixture now covers a claim on one member with agreement on the other. Standing lesson: a schema handed to a provider is only proven by a live dispatch, so the first dispatch after any schema change is the test | FIXED |
| KI-27 | 2026-08-08 | `code-critic` dispatch requires `--reviews <implementer-job-id>` and refuses when none exists, but the standing collaboration model has the ORCHESTRATOR implement directly (AGENTS.md: orchestrator designs, delegator critiques, orchestrator implements, then critiques). Orchestrator-authored changes live in the working tree, not an implementer job record, so the formal code-critic path cannot review the most common kind of change the loop produces | The implementation-critique half of the loop has no mechanism for the orchestrator's own work; it gets done ad hoc through a design-critic brief pointed at the diff, which is not recorded as a code review | Give code-critic a way to review a tree or a diff, not only a job: e.g. `--reviews-tree <sha>` or `--reviews-worktree`, recorded in the job as the reviewed artifact. Until then, orchestrator self-implementations are reviewed via a design-critic brief over the diff, by a model different from the implementer | OPEN |
| KI-29 | 2026-08-08 | The `mission-timeout` agent fixture intermittently maps to a non-4 driver exit: it dispatches a mission job, waits for `running`, backdates `startedAt`, and reaps expecting a budget-cap timeout (exit 4). Failed once in ~3 full-suite runs, passed with no code change on the next. The plain `timed` timeout fixture (no mission) is stable, so the sensitivity is in the mission path's reach-running-then-backdate window. The fixture now dumps the job status and driver output on failure (kept), so the next occurrence is captured rather than re-guessed | A green batch can be red-gated by chance, and the loser reads as a real regression | Reproduce by looping the agent-fixtures section; if it recurs, capture whether the job became process-lost or completed and whether a standing-reaper sweep raced the explicit reap despite the lifecycle lock | FIXED 2026-08-09: the verdict race was real — reap_one judged process liveness before budget expiry, so an expired job read timeout from the dispatcher but process-lost from the standing reaper. Budget expiry now outranks process-lost for jobs that ran; scoped to running only after the suite caught pending→timeout being unlawful. Suite green repeatedly since. |
| KI-30 | 2026-08-08 | The mission runner's anchor commit (scripts/agents/mission-state.py) was a raw `git commit`, which a target carrying the pre-commit guard refuses (`agent commit requires scripts/agents/commit.sh; the live wrapper ancestry token is missing`). bm-2 is the first cohort whose provisioned target is adopted with the guard, so this mission-runner/guard interaction never surfaced; the first live Devin repetition failed at `mission start refused: mission anchor refused: anchor commit failed`. | Any adopted-target benchmark cannot start a mission; the failure looks like a Devin problem but is harness-wide | FIXED 2026-08-08: the anchor routes through the target's commit.sh wrapper when the guard hook is present (an adopted repo, where the runner holds the lease), and stays a raw commit where there is no guard (template, fixture sandboxes, which have no holder). Mission fixtures green. Note: an already-provisioned target still carries the pre-fix mission-state.py, so an in-flight cohort must be re-provisioned from the fixed code rather than resumed. | FIXED |
| KI-31 | 2026-08-09 | Benchmark provisioning's raw `git commit`s in the target pass the pre-commit guard only when the driver is ORPHANED (nohup + immediate parent exit -> ancestry walk finds no agent -> HUMAN -> sovereign). Launched with a live agent ancestor, the same commits classify DELEGATE and are refused for lacking the wrapper token -- discovered when attempt 3's launch shape kept the parent alive; the last-good commit fails identically under that shape, so this was never a code regression. RECURRED 2026-08-10 in the SUITE: validate-metasystem.sh's IL-14 new-plan guard fixture called the REAL guard, whose classifier answers MAIN under the orchestrator session's live ancestry and HUMAN when the suite runs detached — the fixture's verdict depended on invocation shape. Fixed for the fixture via the pre-commit-guard-fixtures.sh pattern (copied guard beside a refusing classifier stub -> fail-open HUMAN path); the underlying shape-dependence stands. | Provisioning and any direct-guard fixture succeed or fail based on process-tree accidents, and its commits evade the agent-commit discipline rather than satisfying it | FIXED 2026-08-10 through the loop (plans/provisioning-identity.md, one defect-driven sol round, six findings folded): the provisioner is the fresh target's first main — announce after adoption, holdership VERIFIED, both provisioning commits wrapper-carried (KI-30 anchor precedent), then the departing-main release (retire + lease-record removal; the KI-33 recurrence) before the arming step establishes its own identity. The kit gate proves the route with a refuse-raw/carry-wrapped control that is deterministic in every invocation shape, its seal step is explicitly a simulated-HUMAN commit (--no-verify, the one act the design assigns to the human), and preflight's pin is asserted against the contract's own bytes. Kit gate green from a live agent session — the first time ever. | FIXED |
| KI-32 | 2026-08-09 | The supervision OWNER is immortal by construction: `run_owner`'s `while true` loop has no exit path and never checks that the repository it supervises still exists, and the only stop is an explicit `--shutdown`. When a supervised directory disappears (fixture sandbox removed by its trap, cancelled cohort target, killed suite run, hand-stripped trap), the components die and the owner's self-heal RELAUNCHES them forever, spawning census helpers every cycle. Measured live: 16 owners (15 on deleted paths), 31 watchers, 54 orphaned __lock-owner helpers, 149 concurrent census processes, load 134 at 2126 processes; killing children was futile because owners respawn them, and only killing owners first stopped it (after: load 31, 1641 processes, 0 census). | Any leaked arming degrades the machine without bound; the stronger the self-heal, the worse the failure | DESIGNED: plans/supervision-lifecycle.md (owner self-termination on lost purpose, crash-loop breaker, intent lease, orphan janitor). Immediate workaround: kill supervision OWNERS before components. | OPEN |
| KI-33 | 2026-08-09 | A session can be locked out of its own checkout. `--shutdown` retires the main's announcement; re-announcing the SAME process mints a new mainId (main-<start>-<pid>-<fresh hex>), but the lease still names the retired mainId, and because that lease's pid is still alive the live-holder rule refuses to update it. classify then reports class=MAIN holder=false and every gated verb fails OWNED-ELSEWHERE against the session's own former identity. Hit live after the KI-32 cleanup; recovered by reconciling holderMainId to the live announcement (same pid+start proven, epoch untouched). RECURRED 2026-08-10 in provisioning-identity work: the provisioner announced+held, retired, and the subsequent arming's fresh mainId (same pid) was refused OWNED-ELSEWHERE by the lease still naming the retired-but-live identity. Worked around at the call site by the departing-main release (retire + lease-record removal, the S4-8 fixture precedent), now part of provision.sh's flow. | An ordinary shutdown-then-rearm can strand a live main, and the only exits are hand-editing custody state or killing the session | OPEN: the lineage work (plans/lease-succession.md) fixed cross-PROCESS succession; this is the same-PROCESS case. Candidate: announce should reuse the existing mainId for a live (pid, pidStartedAt) even after retirement, or the claim path should reconcile an identity that proves the same process. | OPEN |
| KI-34 | 2026-08-09 | The open-work scanner (`scripts/agents/open-work.py`, run by the stop hook) reads only THIS checkout's `artifacts/agents/jobs/`, so a critique round legitimately dispatched from a second-session worktree (the KI-21 paved path when the main lease is held elsewhere) is invisible: any plan truthfully naming that in-flight job is flagged STALE-PLAN, and its stream reads as idle open work. Hit live 2026-08-09 while round 9 of the supervision-lifecycle chain ran in `agentic-tools-slc-r4` (job `supervision-lifecycle-r9`, verified alive) — the stop hook refused the turn twice against a running job. Interim: plans whose work runs in a worktree word `In flight right now:` starting with "nothing in this checkout — ..." and carry the worktree job in prose, which is accurate per the field's this-checkout semantics but hides the work from the mechanism. | The one mechanism built to catch dropped work cannot see cross-checkout work, so it either cries wolf (training operators to ignore it, the KI-21 lesson) or is silenced by wording that hides real work from it | OPEN: teach the scanner about sibling worktree job roots (git worktree list of the same repository), or let a plan name its job's checkout explicitly for the scanner to follow. Through the loop, with a fixture where a worktree job keeps its plan un-stale. SECOND BLIND SPOT observed 2026-08-17 (wall implementation): background codex critique processes are not dispatch jobs, so `jobsInFlight` reads zero while a mandatory verification critique runs — the stop hook refused three turns against genuinely in-flight verification work. Same root cause: the scanner equates "work" with "dispatch job record". Interim: the plan's `Waiting on the human:` field carries the real blocker worded to avoid the leading none/nothing match. Durable fix should let a plan name a non-job in-flight process (pid or task id) the scanner can verify alive. | OPEN |
| KI-35 | 2026-08-16 | Dispatch creates one git worktree per delegate job under artifacts/agents/worktrees/<job> and NEVER reclaims it: 118 dirs / ~500MB accumulated on the serving checkout (125 git-registered worktrees). A slice-2 `janitor worktrees` observer classifying them by "job terminal + custody dead" was built and REVERTED (D89): its mandatory code critique (plans/wt-code-critique-r1.md, 5 structural) proved the verdict unsound — it green-lit implementer worktrees still holding UNMERGED work (a modified dispatch.sh in caps-census-gate-order), because terminality is not a data-release proof, custody death is not process-group death, and follow-up rounds (<job>-rN) alias the same workspace while <job>.json reads terminal. A sound automatic reclaim needs the full proof in plans/disk-hygiene-design.md ("The worktree-reclaim proof"). CORRECTED 2026-08-17 (plans/opus-window-review-dh.md finding 13): the earlier "safe manual cleanup" advice here was UNSAFE — `git worktree remove` does NOT protect ignored data (nine currently-clean delegate worktrees hold ignored artifacts/config/caches that removal deletes), a clean branch with committed-but-unmerged work passes git's check without any review/release proof, and a blanket `git worktree prune` is repository-global (dry-run on this host proposed pruning the unrelated wt-flakefix scratchpad). REPORT-ONLY until the journaled reclaim slice lands; any manual removal needs a per-candidate check of ignored contents, branch reachability, and downstream consumers first, and never a blanket prune. UPDATE 2026-08-18 (issue #5 quarantine): delegate worktrees now hold the ONLY copies of their commits' objects in <gitdir>/objects-quarantine, alternates-linked; worktree removal deletes them (breaking any unmerged branch and, unless promoted, even merged refs) and leaves a dangling alternates entry — the reclaim proof must add object promotion + alternates cleanup before any removal. | OPEN |
| KI-36 | 2026-08-17 | The first live devin-hosted mission (bm-2d rep 1, the D83 acceptance probe) surfaced two defects. (1) FIXED: the devin host launcher ran --permission-mode accept-edits; non-interactive devin rejected the orchestrator's first confirmable exec call ("Use --permission-mode dangerous to auto-approve"), the host exited 3, turn bm-2d-t1-d373 failed as host-failure — scripts/agents/hosts/devin.sh now runs the dangerous mode, the same globally-accepted residual the legacy delegate path carried (2026-08-08 ruling; D83 confines devin hosts to the VM for exactly this reason). (2) OPEN: the mission runner then disappeared ~11 minutes into its life WITHOUT A RECORDED TERMINAL TRANSITION — runner record left status=running/endedAt=null, runner log empty, pid gone, cohort declared abandoned/runner-process-gone. The verification round corrected the initial read: the failure ramp (loop.go fail()) prints NOTHING to stderr and finishRunner silently ignores record read/write failures, so a DESIGNED failure exit can leave exactly this state; ordinary external SIGTERM would too. Open suspects: silent finalization failure, or external termination — indistinguishable today, which is itself the defect: the ramp needs a log line and finalization failures need to be loud. Reproduce with instrumentation; a live specimen of the custody-death-proof backlog goal. | OPEN |
