# Known Issues

A standing register of defects and limitations that are recorded but not scheduled: capability ceilings, accepted trade-offs, and dead ends that must not be silently retried. Handoff notes die with their stream; this register survives so the next session does not rediscover the same wall at full price.

Each entry carries a stable id, the date, the observable symptom with evidence anchors, the realistic cost when it bites, and the fix direction or named escalation lever. Append dated updates to an entry; never rewrite a concluded one. A recurrence is appended to the existing entry and raises its priority.

| Id | Date | Symptom and evidence | Cost when it bites | Fix direction or lever | Status |
| --- | --- | --- | --- | --- | --- |
| KI-1 | 2026-08-04 | `last-census.json` records `durationMs` while section 3.11 names `durationSeconds`; naming only, behavior correct | A reader or later consumer looks for the wrong field | Align the design text or the field at the docs pass (item 19 vicinity) | OPEN |
| KI-2 | 2026-08-04 | Validation suite wall time grew 2m14s to 4m38s across items 3 to 23 on the same machine | CI cost and slower correction loops; a suite nobody runs locally stops catching environment defects | Profile the fixture set, parallelize independent groups, or split fast and slow suites; measure before optimizing | OPEN |
| KI-3 | 2026-08-04 | Adoption fixtures emit BSD `diff: Directory loop detected` diagnostics on symlinked skill registrations; exit status stays 0 | Noise that trains readers to ignore validator output | Compare registrations without following symlinks (`diff -r --no-dereference` or an explicit link check) | OPEN |
| KI-4 | 2026-08-04 | Census cost is proportional to total process count on the machine; 5.6s here after the fix, unmeasured on a loaded build host | A busy machine could push a scan past its interval and trip the new over-interval warning continuously | The warning is the tripwire; if it fires in practice, cache the signature-negative pid set between scans keyed by pid and start time | OPEN |
| KI-5 | 2026-08-04 | Supervision fixture "S4-8 inferred announcement" timed out once under load in a delegate's environment; two clean runs here at 4m30s each | A flaky gate erodes trust in the suite and hides real regressions | Make the fixture's arming wait proportional to measured census duration rather than a fixed 12s cap, or serialize it away from other supervisor-starting fixtures; reproduce under parallel load before changing | OPEN |
| KI-14 | 2026-08-05 | The census classifies this session's own tool shells as UNTRACKED claude agents. A background zsh wrapper whose argv carries `.claude` paths and plugin environment exports matched the claude runtime signature (pid 47819 in the live stop-hook report), because signatures match argv substrings and the wrapper quotes entire heredocs containing those paths | Every long-running tool shell can appear as an untracked agent, training the operator to ignore the one report class that caught a real untracked peer session the same day | Sharpen the signature: match the executable or command word, not arbitrary argv substrings; the adapters own their signature tables, so the fix is per-runtime table precision plus a fixture with a lookalike wrapper argv | OPEN. Update 2026-08-05 CONFIRMED at the root and partially fixed: the fake signature matched the bare word `fake`, so a commit message saying 'the fake host' made the tool shell an agent ancestor and flaked the nested-operator refusal fixture twice; the fake signature now matches only the process name. The claude and codex signatures remain loose (bare command words are genuinely their CLI argv), so the tool-shell false-positive class stays open for those two |
| KI-13 | 2026-08-05 | The validation suite leaked a supervision owner for every fixture repository it armed. `agent_supervision_repo` was a single variable, reassigned as the suite moved between the agent, runner and selftest repositories and emptied before exit, so the cleanup trap shut down nothing. Two owners were found alive after 25 hours and 78 minutes, each running a watcher and reaper that scan every process on the machine every five seconds. Killing the components does not end it: the owner restarts them by design, so only shutting the owner down works | Compounding background load on the developer machine, invisible because the leaked supervisors are scoped to their own deleted fixture repositories and so never appear in this repository's census. A plausible contributor to KI-4's census cost and to the CI capacity failure in KI-10 | FIXED 2026-08-05: cleanup tracks every armed repository in an array and shuts all of them down. Proven by counting supervisors before and after a full run | FIXED |
| KI-12 | 2026-08-05 | The permissions envelope's `network` field is not honoured by any adapter. `scripts/agents/adapters/codex.sh` hard-codes `sandbox_workspace_write.network_access=false` on both the dispatch and resume paths, and no adapter reads the field at all. Both shipped presets deny network, so today the record and the behaviour agree by coincidence; an envelope requesting `network: allow` would be recorded as allowed and still be denied | A delegate cannot be given network even when a project wants it, and the job record would claim a permission the run never had. The same asserted-not-measured class as KI-8, pointing the other way | Read the field and map it per runtime (`network_access` for codex, the allowed-domains list for claude, the sandbox settings for devin), and prove it in each adapter's selftest by attempting a fetch and recording the observed result rather than the requested one | FIXED 2026-08-05 in `74e2dad`: the envelope's network field is read by every adapter and delegates get network by default; the status cell was never updated when the fix landed, which is the register's own drift class |
| KI-11 | 2026-08-05 | Dispatch refuses when the census is one second past its interval. A follow-up was refused with `census verdict is stale (age=61s interval=60s)` while supervision was healthy: the scan itself takes 393ms, and the freshness rule allows no margin for a scan that starts late on a busy machine | A refused dispatch costs a round trip and reads like a supervision failure when nothing is wrong. On a loaded machine it could refuse repeatedly | Allow a small grace above the interval, or judge freshness against the last scan's start rather than its completion. Related to KI-4, which anticipated census cost on loaded machines; this is that cost arriving as a refusal rather than a warning | OPEN. Note 2026-08-05: this refusal was surfaced by hand because the repository's own hooks had never been installed; with them live the same staleness now reaches the operator automatically |
| KI-10 | 2026-08-05 | The process-owning fixtures need watcher, reaper, and census loops running for more than one repository at once. A free shared GitHub runner does not have the capacity: the suite reaches the isolated runner repository's arming, goes silent for 74 seconds, and exits 1 with no assertion message (run on `cd4558b`) | None to the work. The human ruled 2026-08-05 that the local suite is the gate and that free-tier capacity is not something we design around. The cost is losing a second machine's opinion on the heaviest fixtures, which had twice caught real portability defects | Accepted, not fixed. CI now runs with `METASYSTEM_SKIP_AGENT_FIXTURES=1` so its cheap checks stay useful instead of failing forever and training everyone to ignore the signal. If a second environment is ever wanted for these, it needs a paid or self-hosted runner. The open question worth keeping is the metasystem's own resource footprint, which has never been measured and matters for whatever machine runs it unattended | ACCEPTED |
| KI-9 | 2026-08-05 | A delegate in a job worktree cannot run git write commands. Git keeps a worktree's metadata in the main repository's `.git/worktrees/<name>/`, which is outside the job's declared writable roots, so `merge`, `commit`, `checkout` and anything else needing `ORIG_HEAD.lock` or the index lock is denied. Found when a brief told an implementer to merge a branch first (`implementer-20260805t063758z-3648`, exit 128 with the lock path named) | A brief that assumes a delegate can use git wastes a round, and the failure reads like a permissions bug rather than a design boundary | No code change: the metasystem already computes the base-to-working-tree diff itself, which is why implementers report a file boundary rather than a commit. Briefs must not ask a delegate for git writes, and the implementer preamble should say so plainly at the next docs pass (item 19) | OPEN. Update 2026-08-05: the complement bit twice in one day — when authority files change on main mid-chain, the delegate's worktree keeps the old contract and it cannot pull, so a follow-up telling it to read HEAD describes a file it does not have. Syncing the worktree is the orchestrator's job before any follow-up that cites amended authority |
| KI-8 | 2026-08-04 | The recorded permissions envelope is asserted, not measured. `record_actual_workspace_write_scope` in `scripts/agents/adapters/runtime-common.sh` writes `effective.writeRoots = [workspace]` from the stated belief that every CLI makes its cwd the sandbox write boundary, and `fail_if_effective_wider_before_launch` then compares that asserted value against the request, so the two always agree and the guard cannot fire. The premise broke in practice: `codex exec resume` takes no `-C`, so follow-up rounds ran with the adapter's cwd and job `implementer-20260804t131716z-231f` wrote four new files into the main tree while its record claimed the job worktree. The cwd half is fixed (`codex.sh` now enters the workspace like `claude.sh` and `devin.sh` already did); the asserted-versus-measured half is not | A boundary the records claim but do not hold. With a peer agent in the same tree it is a collision risk, and every retrospective reading those records is reading a claim rather than an observation | Make the effective envelope an observation: the adapter `selftest` already has a permissions leg, and C4-8 already requires recording which fields were proven behaviorally versus only constructed as flags. Prove write denial outside the workspace there, per runtime, and let the record carry the evidence level | OPEN |
| KI-7 | 2026-08-04 | A sweep during the launch window can mark a starting job `process-lost`. `record_create` writes the record `pending` with a null `pid`, and the launcher patches identity only after the census resolves the pid; a `reap` in between finds no supervisor match, and `wind_down_group` treats an unrecorded pgid as "nothing to kill" and returns success, so the sweep proceeds to the terminal write. Found while diagnosing the `pending-loss` fixture flake (`scripts/agents/dispatch.sh` `reap_one_locked`, `wind_down_group`) | On a loaded machine the standing reaper could fail a job that was starting normally; the window is short, and no live occurrence is recorded yet | Distinguishing "launcher still starting" from "launcher died before recording identity" needs a signal, since the second case genuinely is lost: the start gate already exists as a candidate, or a grace window keyed on `startedAt`. This is adjacent to watch-list finding C11-3 (pre-spawn durable intent), triaged decide-now, so decide both together rather than patching here | OPEN |
| KI-6 | 2026-08-04 | Evidence mirroring fails silently. Every `mirror_record` call site in `dispatch.sh` (reap, loss, timeout, close paths) swallows failure with `\|\| true`, so an unfilled or relative `evidence.root` means nothing is ever mirrored and no status turns red. Found when `close` refused the benchmark critique chain with "cannot close an unmirrored chain" after five completed rounds; the standing reaper had been failing every mirror into its log since arming | Paid raws live only in gitignored `artifacts/`, which `git clean` reaches, so the only copy of expensive evidence can vanish; the failure surfaces only at close time, long after the runs | Surface a failed mirror as a watcher-visible condition (a per-job field the watcher reports, not a log line), and have `arm-supervision.sh` refuse or warn loudly when `evidence.root` is a placeholder or relative. Related project-side gap: this repository has never declared its own evidence root, and both `metasystem.conf` and `docs/project-rules.md` still hold the shipped placeholder | PARTLY FIXED 2026-08-04: the human set the evidence root and it now mirrors (hash-verified manifests under the declared root, resolved through the new uncommitted `metasystem.conf.local`, which exists because the template repository cannot fill its own placeholders without every adopting project inheriting them). The silent-failure half stands: mirror failures are still swallowed at every call site and surface only when a chain refuses to close |

