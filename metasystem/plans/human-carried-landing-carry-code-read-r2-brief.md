Working Mode: implement
Orchestrator Identity: m1d (lineage main-1788941004-20871-6e7a43, coordinator under goal human-carried-landing-carry, closing read of chain hcl-build3-20260911, final work round 1)
Date: 2026-09-11

# Review brief: second read of the carried-landing implementation

FINDING IDS: chain-unique, continue the carry's register: HCL-C-50, HCL-C-51, ... never F-n.

## What you are reading

Chain hcl-build3-20260911 for goal human-carried-landing-carry
(metasystem/plans/goals/human-carried-landing-carry.md), its one work
round (job hcl-build3-20260911), reviewed tree
faccdb3b25ea9fc1d9ca6fa10a87864fe7a2fbfa (the installation subtree the
conformance review names as reviewedTree). This chain carries the work of
chain hcl-build2-20260911: its round 1 was the first read's subject, its
round 2 folded that read and hit its cap, and this chain applied that
fold on trunk 01124a60c, resolved four rebase conflicts as unions, and
finished it under `metasystem/artifacts/agents/hcl-context/build3-brief.md`. The specification
is metasystem/plans/human-carried-landing-carry-design.md, revision 6,
with three coordinator amendments recorded at landing: the stop list's
empty-staging rule excludes the rerun states; single-machine mode is out
of scope, asking `carry-remote-required`; `goal carry` speaks its word
lines after its transaction. The first read,
`metasystem/artifacts/agents/hcl-context/code-read-r1.md` (Opus,
hcl-cc1-20260911), found fifteen findings; the fold brief
`metasystem/artifacts/agents/hcl-context/build2-fold-r1-brief.md` holds
the coordinator's decision on each and is binding; the fold round and this chain built it.

Write your register as this new file: `metasystem/records/misc/human-carried-landing-carry-code-read-r2.md`
(a read-only runtime returns it in the job return; the coordinator
projects it).

This is the independent critique of the FINAL work round that a
DESTRUCTIVE-REACH chain needs to close. A material finding is folded and
re-read, so make each one exact. A material finding is: a behaviour the
design specifies that the code does not have; a behaviour the code has
that the design forbids; a way a carry lands what the human did not name,
or refuses a verified human on the machine's own judgement; a test the
design names that is missing or does not test what it says; a refusal,
ask or trailer that differs from the page's text; or a fold decision not
carried out as decided.

## Settled, do not re-derive

The design's decisions and the fold's decisions. The coordinator reruns
the sandbox-bound tests and beds on the host; do not run beds, read.

## Mandate, in order of consequence

1. **The fold, finding by finding.** For each of HCL-C-35 to HCL-C-46,
   HCL-C-48, HCL-C-49 and the counselor-file gap: one line, folded as
   decided at file:line, or not, with what differs. Rule of the fold:
   no fixture claims what it does not drive. Name every `TestHCL`
   function that is still a shell (a body that does not call the verb or
   path its name states) and every bed leg whose assertions would pass
   on a tree without the behaviour its name states.
2. **One word, one defect, with the ledger check ahead of the match
   (HCL-C-38).** Trace a chainless candidate with a ledger-class path
   staged beside its payload, and one with a records/counselor line, and
   one with two carryable defects.
3. **The wrapper's transaction (HCL-C-35, -36, -44).** land.sh's order:
   carry-status branch, then staging only for a fresh word; the EXIT trap
   closing the reservation on every non-push exit; the 08 lines before
   the push.
4. **The fence and local mode (HCL-C-39, -40).** Format 1 refused on the
   reservation and the record and by ValidateTree; `carry-remote-required`
   at the word, the reservation and the classification.
5. **The ordinary gate (HCL-C-46).** The candidate engine digest recovery
   for an ordinary landing equals the base's rule and its test; the carried
   red-result path is reached only through an explicit carried fact.
6. **What must not change.** The identity gate equals `goal set-budget`'s;
   a record failure is never carried; the temporary word is not a proof;
   nothing in the shared testing contract or the workspace receipt
   weaker; `sync-transport.sh` untouched; the trunk changes the rebase
   brought (the `Claimed` key `episodeAt`, the elapsed-budget origin)
   intact.
7. **What the implementer left out, by name.** The return of
   hcl-build3-20260911 lists under `gaps` the design fixtures it did not
   build (parts of HCL-C-45, -49, -36; block B names; block C legs and
   goal-cli scenarios). For each named gap say whether the BEHAVIOUR it
   would prove is proven elsewhere in the tree (a real Go test or a bed
   leg, at file:line) or is unproven; an unproven behaviour the page
   specifies is a material finding, a duplicate oracle is not.
8. **The four named checks of revision 6** (HCL-C-33 with its two-seat
   legs now, -25, -03, -34 with execution real): one line each.

## Return

Findings by id with the file and line in the reviewed tree, the design or
fold section, the claim, the evidence, and what changes if it stands;
then the fold lines of mandate 1; then a verdict line: closable as is, or
one fold naming which findings. Wall-clock budget: 60 minutes.
