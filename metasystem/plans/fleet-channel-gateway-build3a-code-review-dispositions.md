# fleet-channel-gateway — dispositions, build step 3a code review, first root (job fcg-build3a-cc)

reviewedTree 04560087, on the round-2 tree; four material findings and four notes. Three of the four material findings indict the design, not the code, and are accepted as true; the engine demoted them from this chain's register (artifact metasystem/plans/fleet-channel-gateway-design.md is outside the reviewed subject set; artifacts/agents/jobs/fcg-build3a-cc.json, demotions), and the seat carries them to the design chain's register for the daytime design round. The fourth was sent back as round 3 (plans/fleet-channel-gateway-build3a-fix2-brief.md). A second root (fcg-build3a-cc2) reviews the round-3 tree; this root's second round carries the register.

| Finding id | Disposition | Reasoning and evidence | Amendment |
|---|---|---|---|
| F-1 | accepted | design conflict FCG-MATCH-06 / FCG-C-22 against FCG-WORD-07: the inbox drops a trailing full stop from the token's last field and writes "approved for execution" for a stop question, while the resume authority rule (containsContiguousFields, unchanged by brief) drops nothing and will refuse the same text; the code conforms to its brief; the ledger receipt would promise an approval the resume refuses | carried to the design chain register as a design finding for the daytime design round; step 3c's brief must not wire the stop-question answer to resume until the two rules agree |
| F-2 | accepted | a verified bound reply whose text carries a newline cannot commit: FCG-WORD-07's verbatim reason against the one-line history grammar (file.go reason = rest of line); today's Poll passes the same text to goal.Answer, so the failure is latent already; under FCG-RECEIVE-03 a by-name rejection would stop every listener's prefix | carried to the design chain register as a design finding; the seat reports the latent Poll exposure to Wido |
| F-3 | accepted | design gap between FCG-MATCH-06 rule (a) via orphanPosts and the answer matrix rows' thread-non-null FROM tuple: a reply threaded to an orphan post while the question's thread is null is bound by name but has no committable row, so Publish journals it rejected until the new post-ref lands; transient, but every pass logs a refusal and stops the batch prefix; the code implements the design as written | carried to the design chain register as a design finding |
| F-4 | accepted | the budget matrix row was never exercised through ChannelInboundRequest; the disposition helper was unit-tested only, so the rowName selection in Mutate, the recorded phase with a non-null approvalUlid and null receipt, and ValidateCommit's acceptance of that shape were uncertified | round 3 adds TestChannelInboundPublishBudgetAnswerRows (both branches through Publish on the git-backed bed, row probes on ChannelMatrix, exact ULID and receipt); the second root (fcg-build3a-cc2) reviews the round-3 tree at reviewedTree 516c8e5c |
| N-1 | noted | an answer to an open question whose goal moved to done is rejected as "not live" — a permanent poison until channel skip; whether a question can outlive its goal is unsettled | listed for the design chain with F-1..F-3 |
| N-2 | noted | a zero SentAt records the year-one timestamp and the approval ULID derivation refuses negative milliseconds; providers always populate SentAt | no change |
| N-3 | noted | a token that is a field-prefix of another open question's token counts as present, so the longer token is unmatched under the design's "two or more tokens present" rule | no change; the rule is the design's |
| N-4 | noted | the decided record is written only on a successful Mutate; after a CAS-refused then lost attempt the caller sees OutcomeLost with the earlier decided values | rule for step 3c's brief: the caller keys on the result outcome, never on decided |
