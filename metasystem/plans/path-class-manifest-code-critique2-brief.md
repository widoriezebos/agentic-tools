Working Mode: implement
Orchestrator Identity: m1+main-1788333680-2840-7f79f4 (dispatch delegate under goal path-class-manifest)
Date: 2026-09-03

# Goal

Closing code review of slice 1 of the path-class manifest (chain root
path-class-build1, terminal round path-class-build1-r3; its round
evidence holds diff.patch and review.json with the reviewed tree). Your
round-1 register is metasystem/records/misc/path-class-manifest-code-critique-r1.md:
five material findings, folded by the correction brief
metasystem/plans/path-class-manifest-build1-fix2-brief.md. This is the
closing review: verify the five closures on the final tree and confirm
nothing regressed; the chain lands on your zero.

# Mandate

1. One verdict per finding PCM-CC1-001 to 005 against the final diff:
   the tracked-path test skips cleanly or enumerates without git inside a
   frozen export; internal/pathclass has a floor in both coverage
   baselines at the measured number; the waiver rule resolves by
   location and no longer refuses paths outside the installation that
   were waivable before; ResolvePath reads the oracle's error before its
   "outside"; exact revert refuses an unclassified path exactly as a
   behavior path, nested instruction files included.
2. Regression check on the whole slice-1 diff against the certified
   design (metasystem/plans/path-class-manifest-design.md revision 2,
   sections 1 to 4, 6 to 8) and the two closing obligations PCM-R2-001
   and PCM-R2-004: still met on the final tree.
3. Boundary: exactly slice 1's files plus the two coverage baselines;
   slice 2's files untouched.

A finding is material only if it changes what gets built and names the
artifact. If nothing material remains, say so; that closes the chain.

# Constraints

Wall-clock budget: 25 minutes. Return per the code-critic schema.

# Gap Rule

stop and report a gap; never fill it silently.
