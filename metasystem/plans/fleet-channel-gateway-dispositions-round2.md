# fleet-channel-gateway — dispositions, critique round 2 (job fcg-design-cc1-r2)

Design revision 3 (plans/fleet-channel-gateway-design.md) answers the
twelve material findings of round 2, reviewed at main e526a54e. One
table per file, as `validate critique-closed` joins it; the round-1
table stays at the end of the design.

| Finding id | Disposition | Reasoning and evidence | Amendment |
|---|---|---|---|
| FCG-C-16 | accepted | Publish refuses a second run of an opid whose journal entry is terminal non-confirmed (txn.go:517-544); a clone that abandoned its first attempt could never commit or confirm that message again, and recovery could not rebuild an intent whose opid carried the literal `inbox` machine (recover.go:209-229) | FCG-INBOX-02: every commit runs under the committing machine's own opid; the record carries `opid`; the inbox Mutate's three branches (write, LostToCompetitor naming the record's opid, `inbox record present without its transaction`); OutcomeLost is the loser's confirm authorisation (FCG-RECEIVE-03); recovery abandons a dead inbox entry without rebuilding; fixture FCG-12-ABANDONED-THEN-RETRIED |
| FCG-C-14 | accepted | the old engine's steward polls after every tick (runner.go:136-140) and acknowledges by offset (telegram.go:203-208); nothing on the ledger can stop it, and the legacy schema (question.go:29-58) had no defined conversion | FCG-MIGRATE-10: the cut-over is one ordered fleet action (disarm every steward, land, rebuild and migrate per machine, re-arm) written into step 4's landing message; replies of the window resurface as `unverified-migrated` inbox records with a list post; the field-by-field migrate mapping including all five kinds, the `migrated` lineage, the phase mapping and the `matched` case through goal.Answer; a migrate re-run is lost to its own earlier record; fixture FCG-12-MIGRATE |
| FCG-C-15 | accepted | "approve 123456." kept the digits; "order 123456 now" was refused forever and stopped the prefix | FCG-SECRET-15: StripCode drops trailing `.,;:!?` before testing the last field; a six-digit field elsewhere is a fact; the validator's `channel-secret` row refuses only the field StripCode would have removed and digits in machine-written fields; the one named limit (a six-digit fact last is read as a code); FCG-RECEIVE-03 names the poison case and `channel skip` |
| FCG-C-17 | accepted | the allowed-path row admitted questions and inbox only; FCG-STATUS-09 wrote listeners/ | FCG-INBOX-02: the listener record schema, `listeners/<machine>.json` in the allowed-path row, the heartbeat's own transition |
| FCG-C-18 | accepted | rule (a) matched an answered question's thread and the answer-state row then refused the record | FCG-COMMIT-05: outcome `late` — recorded, step consumed, binds nothing, one rejection post "already answered"; FCG-MATCH-06 (a) sends a non-open question to `late`; fixture FCG-12-LATE |
| FCG-C-05 | accepted | the answer object, posting.kind, ref and closing fields were untyped; no transition matrix; ask and migrate had no opid; "tip state is not the one it read" could not tell a legal edge from a rewrite | FCG-INBOX-02: the answer table, the `ref` type, posting.kind vocabulary, closedAt/By/Because types, `opid` on every record; the transition matrix with FROM tuples and the predicate (FROM → apply; own opid → AlreadyApplied; TO under another opid → LostToCompetitor; else `channel-transition`); every operation's opid is the machine's own |
| FCG-C-07 | accepted | with several open the token was ignored; with one open a generic "yes" became the human's word | FCG-MATCH-06 (b): the token binds alone with any number open (exactly one open question's token as a whole field); option labels bind only threaded; two tokens or none with several open → `unmatched` and the list; fixture FCG-12-UNTHREADED extended |
| FCG-C-08 | accepted | age-only takeover could duplicate a slow poster and the asker posted before Publish; http.DefaultClient has no timeout (telegram.go:20-25) | FCG-POST-08: one protocol for every post including the asker's; step (3) requires posting.by = me, a taken-over poster records its ref in `orphanPosts`; per-request HTTP deadlines (`channel.http-timeout-sec`) bound step (2) and DefaultPublishDeadline step (3), both under posting-stale-sec; rule (a) matches orphanPosts; fixtures FCG-12-POST-CRASH restated and FCG-12-POST-TAKEN-OVER-ALIVE |
| FCG-C-11 | accepted | goal.Approve needs the budget and a VerifiedChannelAnswerProof (approval.go:406-418, authority.go:179-190); the ULID had no timestamp; recovery had no approve case; a shared opid could not be retried on one clone | FCG-ANSWER-11: the exact RecordedChannelAuthority tuple and Approve call from the ledger answer; the ULID's 48-bit time is answer.at; the opid is the approving machine's own; the approval intent fence and the goal read (Approved.At, Authority channel) that prevents a duplicate row; requestForEntry gains `approve` with source channel; fixture FCG-12-BUDGET-TWO-STEP restated |
| FCG-C-12 | accepted | the fake could not tell listeners apart, the conflict row had no description, FAIL_AT had no action or selector, and commit-before-Confirm was not observable | FCG-EVIDENCE-12: listener identity in the bot token (`fake-telegram-token-<listener>`), one stream and one offset across tokens, journal rows with listener and sequence, `conflict.description`, `pauseBefore` by Telegram method (the `confirm` method name) so the fixture asserts the ledger while the fake holds the Confirm, FAIL_AT `<phase>[:<kind>]` exiting 3 once; every fixture restated with its pass condition |
| FCG-C-02 | accepted | step 2 either changed Poll's acknowledgement or left it ambiguous; step 3 cut over before WORD-07 and needed POST-08 | FCG-BUILD-13: five steps — fences/validator/recovery; provider contract with the old Poll calling Confirm at its cursor write (the existing fixtures prove the acknowledgement is unchanged); WORD-07 alone; the cut-over with POST-08, migrate, the open-work pass and `channel skip`; Listen with restart |
| FCG-C-10 | accepted | `steward arm` keeps a live runner (runner.go:409-416); PublishRequest has no context (txn.go:466-483), so a join was not bounded | FCG-POLL-04: step 5 lands with `steward restart` under R-37's word; the in-flight Publish finishes (60 s) and its Confirm runs under 30 s; `channel.stop-grace-sec` 100; a late entry is classified by recovery; the heartbeat's engine digest shows who has not restarted |
