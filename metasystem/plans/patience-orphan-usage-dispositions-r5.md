# Dispositions — patience-orphan-usage, critique round 5 (final)

Critic: design-critic-20260811t140724z-3a24 (codex gpt-5.6-sol, xhigh).
Verdict: 4 material. All 4 ACCEPTED — exact wiring corrections, no
structure challenged, so the loop stops by the diminishing-returns rule
after five rounds and 32 accepted findings.

| # | Sev | Claim (short) | Disposition |
|---|-----|---------------|-------------|
| POU-R5-001 | critical | Terminal delivery has no safe total position on the failure ramp | ACCEPT — terminal delivery runs ONLY at the completion conclude (a final ledger block exists); the failure ramp does only the idempotent O4 usage aggregation and no ledger annotation; interrupted missions re-list on resume. |
| POU-R5-002 | high | `- Landed unconsumed:` is not shipped grammar | ACCEPT — it is a NEW cycle-block annotation kind added beside satellites 1-2's annotations, parsers extended in the same change. |
| POU-R5-003 | critical | Missing-pgid vintage rule does not prove the writer dead | ACCEPT — a missing pgid can never satisfy the whole-group gate; such a record always aggregates `unavailable`. |
| POU-R5-004 | high | The 20-row limit is still contradictory | ACCEPT — stated once: ≤20 qualifying → all rows; >20 → 19 data + 1 overflow, 20 total, no other reading. |
