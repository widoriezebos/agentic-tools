# Verification of agent help and verb cleanup

Goal: verbs-match-intent. Root uses the human's explicit machinery bypass.
Evidence: `/Users/wido/LocalStorage/agentic-tools-evidence/agent-help-20260926`.
Status: implementation and applicable verification complete; this record accompanies publication.

## Subject and review

- Initial agent-help implementation: commit `1f0f51e485db7f906cfa240b1b9a46978f5e03e6`.
- Fable's cleanup subject: commit `6967c7efe42e0e4d43dfdcd9b8f4272f531271b1`, tree `a3d5684b983a6ac0577e43847b72fc9aed946eb1`. Zero material findings after conformance and adversarial review; report and dispositions are tracked here.
- Corrected runtime subject: commit `5a205570b764c9645714233c9026d38da4aa6c4f`, tree `ef11461601f42982221dacf74ba92d7ae826eeaf`. The delta after review changes help prose/audience and stale refusal-register source locations plus their exact expectation. It changes no execution or authority owner. Root reviewed it and reran the relevant checks.
- Incoming main `b486613f9` changes four Partner-design/roster documents only. Merged without conflict; no engine-source or test changes. Project record validation passed afterward (155 records, seven homes). This task's actual delegates remained Opus/Fable.

## Observed proof

| Proof | Observed result and limits |
| --- | --- |
| Actual CLI outside a repository | 201 help/refusal invocations passed on the corrected binary. All 44 current commands have text and structured help. Nine focused review forms work. Removed aliases/family discovery refuse; explicit maintainer help works. The unrelated working directory remains empty. |
| Human help | 29 existing pages remain byte-identical. The 24 changed pages are the intentional administration separation, capability-preserving run/job forms, clearer effects and removal of obsolete guidance. No blanket byte-identity claim survives the human's clean-house instruction. |
| Agent help | 72 lines / 769 words, versus the initial 161 lines / 1,073 words, with all 44 commands visible. Shared JSON/Partner metadata separates workflow, mixed and administrative scope. Audience labels never confer authority. |
| Fresh help-only agent | Opus answered eight of nine tasks and correctly reported that help cannot establish an obsolete engine prerequisite from its absence. It refused an unauthorized accept-risk continuation. The task actions were not executed; this is not a statistical benchmark. Clarifications from the trial are in the final help and dispositions. |
| Command group | All 173 current named tests and all nine shard processes passed. The protected contract additionally requires the explicitly retired engine-floor mutation test, so its result stays invalid with that one missing test. Root accepts the approved retirement and replacement proof; no pass or certificate was fabricated. |
| Focused final command checks | Help, administration, JSON, all-family discovery boundary, Partner rendering and form contracts pass after the last prose changes. The new done-job tests and UI option/default/wrong-target tests passed on their execution-identical source. |
| Changed package regression | Complete dispatch, interface-act, HTTP, lifecycle, goal, supervision, Partner-tool and web suites pass with the race detector and whole-package coverage. Refusal-register checks pass after repairing shifted source locations. |
| Coverage | Existing floors unchanged. Fresh reports: goal 82.5%, supervision 90.0%, Partner tools 85.9%, dispatch 78.6%, interface act 62.3%, HTTP 89.1%, lifecycle 75.9%, web 93.8%. Refusal data has no statements and retains its existing exemption. Exact old Go-produced reports cover unchanged packages; the incoming UI-project report matches that package exactly. The floor audit passes against an independent current package inventory. No fresh all-package run is claimed. |
| Static gate | `go-gate.sh --fast` passes: dependency and parallel ratchets, formatting, vet, staticcheck, refusal register, hook/Stop audits, record validation and build. It ran on the corrected immutable source. |
| Goal CLI | All 29 real-binary scenarios pass, including abandonment with no engine-floor setup. |
| Landing | All 45 named scenarios have successful exits: nine retained from the original run and 36 from the corrected run using the supported diagnostic filter (exit 0). The first run's stale-reference failure and deliberate stop remain failure/143. The existing recertification fixture skipped its second, moved-goal subcase after its source-posture prerequisite refused; the first recertified held-before-push refusal did execute and pass. This is combined scenario evidence, not a fresh whole-bed gate result. |

The full test selection was recomputed from destination policy and the candidate:
203 groups, deep mode. Root performed a semantic applicability assessment under
the explicit bypass: fresh proof covers changed execution owners and their actual
consumers; prior proof covers unchanged code/tests, with package identity checked.
The diagnostic native result retains every failure/blocked group. It is not a
successful governed full-selection receipt. The coverage join retains exact
observed summary lines; it does not invent percentages, counters, or timings.
The old raw whole-command timeout at 20 minutes remains failed evidence and was
not repeated; the native owner partitioned the current command group instead.

The moved-goal subcase's exact message was `SKIPPED recertified moved-goal route`,
followed by `chain-recertification-source-changed detail=worktree-posture: source
project/whole snapshot is neither original R/A nor retry M/T`. Reading the fixture
confirms this skip branch already existed and is unchanged. Its preceding route
asserted the held check before the single rejected push, parking and target-moved
refusal. The new abandonment behavior also passed the other seven landing routes,
all goal CLI scenarios and the unrelated-project owner tests. Root accepts this
bounded gap for the engine-floor removal; no proof of the skipped subcase is
claimed and its fixture setup is not broadened into this change.

## Completion questions

1. Observable contract: ordinary task help exposes 44 current commands and intent-based forms, separates justified MetaSystem administration, and no longer advertises the obsolete aliases or internal family catalogue. Abandonment has no engine-source Git prerequisite. The existing private process consumers retain their implementation API.
2. Owners: command descriptors own help and discovery; the public dispatcher owns routing; closeChain owns job completion; the existing run owner owns revision/review/wait; the existing UI lifecycle owns address/stop-time behavior; goal.Abandon owns abandonment and its authority/dependency/history invariants. No new policy evaluator, parser registry or execution framework.
3. Proof: the actual CLI, current command tests, changed package suites, existing coverage floors, static gate and real goal/landing fixtures listed above. Fable independently critiqued the computed diff; root owns certification.
4. Failure behavior: help/refusals run no task effects. Invalid/conflicting flags refuse before owners; qualified references remain exact. Job completion cannot conclude, approve or land a goal. Existing authority, timeout, retry, immutable-input and round-limit checks remain. Historic engine-floor lines remain readable; obsolete journals refuse replay. Tests include negative controls and no-publication assertions.
5. Limits: the moved-goal recertification subcase described above was skipped. No production UI restart, live delegated application build, new VM run or governed full-selection certificate. The code is verified through isolated actual CLI/owner fixtures. A general command dry run was only estimated and is not implemented or represented as proof here.
