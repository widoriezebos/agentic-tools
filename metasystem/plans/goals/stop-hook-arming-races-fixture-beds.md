# stop-hook-arming-races-fixture-beds

- State: queued
- Risk: severity=2 novelty=2 exposure=3 accumulation=2 basis="severity 2: it refuses a seat's turn end with a false cause and leaves no record of why, but wedges nothing and loses no work; novelty 2: the hook's arming step needs to tolerate or serialise against fixture-bed arming on the shared machine-wide registry, and the hook needs to write its refusal before it exits; exposure 3: every seat's every turn end on any machine where a fixture bed can run, and hook-freshness reads dead 5 of 5 here; accumulation 2: each occurrence costs a turn and a diagnosis, and the registry's fixture share grows with every bed run"
- Tier: 3
- Intent: The Stop hook's own supervision arming step fails when a fixture bed is arming fixture supervision at the same moment on the shared machine-wide registry, and the hook refuses the seat's turn end with 'supervision arming failed' while the checkout's real supervision is healthy. Worse, that hook run records nothing: no line in hooks.log, no stop-refusal record, so the cause cannot be read back afterwards and the seat has to diagnose it from health output and timing. Live on 2026-09-09 at 10:07Z on m1b during the process-owning beds for chain bolboc-build1. The same checkout reports hook-freshness=dead 5/5, no hook turn generation recorded, with no lawful remedy. DONE means the hook's arming tolerates or serialises against concurrent fixture arming, every refusal the hook issues is persisted with the underlying up aggregate before the hook exits, the hook records its turn generation on this checkout, and the fixture is a Stop during a concurrent bed arming that either succeeds or refuses with a recorded truthful cause.
- Origin: main
- Next step: Reproduce: start scripts/agents/dispatch-fixtures.sh in a worktree of this repository and, while its first scenario (dispatch) is arming fixture supervision, end a Claude turn in the main checkout; on 2026-09-09 at 10:07Z the Stop hook refused with 'Metasystem could not prove that stopping is safe: supervision arming failed' while metasystem health showed the checkout's own owner (pid 8310) holding the repository lock with census 9s fresh. Three facts to start from. (1) scripts/agents/supervision-hook.sh lines 829-830 set up_failure from the aggregate of its own metasystem up call and record_stop_failure; that run appended nothing to artifacts/agents/supervision/hooks.log (last entry 09:31Z), and artifacts/agents/supervision/stop-refusals/ does not exist, so the hook fails before it records why; the first job is to make the hook persist its refusal and the up aggregate before exiting. (2) The machine-wide registry ~/.metasystem/armed-checkouts.jsonl holds 28,244 rows of which 7,444 are fixture-bed rows (paths under agent-fixture), so bed runs are polluting the registry every arming reads and its mutation flock serialises; decide whether fixture beds should register at all, or under a scope the hook's up ignores. (3) HEALTH reports hook-freshness=dead (no hook turn generation is recorded) at failure 5/5 with no lawful remedy on this checkout, which is the same hook unable to record; find why the turn generation is never written here. The fixture is a Stop with a bed arming concurrently: the hook must either succeed or refuse with a recorded, truthful cause.
- OpenedAt: 2026-09-09T10:10:49Z
- Revision: 1
- Budget: elapsedLimit=1d attemptLimit=10 reservedJobMinutesLimit=1200 activeJobLimit=1 reviewRoundLimit=3
- BudgetExceptions: 0

History:
- 2026-09-09T10:10:49Z CFRH2BYFDHA6T4WXGTKZQNSRT0-m1b-c6925449 open actor=m1b+main-1788680071-18713-e76d5d targets=stop-hook-arming-races-fixture-beds
Integrity: sha256=df7b7cd1d17f6bb65b911846923200e98093559653b49aa8d58462931744bb79
