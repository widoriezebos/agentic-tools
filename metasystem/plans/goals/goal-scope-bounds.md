# goal-scope-bounds

- State: queued
- Intent: Goals get mechanical size bounds AND a split mechanism (Wido 2026-08-30): a big goal is welcome at intake, but before slicing it evolves into an arc of small goals - the scope norm triggers, the split verb is the remedy; concurrency is designed at two axes, scope (this goal: planning parallelism via arcs) and load (machine-concurrency-governor: execution parallelism via slots)
- Origin: human
- Next step: CONTEXT (Wido's reasoning chain, conversation of 2026-08-30 - binding background for the designer, who has no access to that chat): (1) It began with the work-distribution question: Wido asked whether slices are pieces of work anybody can pick from the backlog. Answer ratified: the GOAL is the unit anybody claims (any machine, via the shared ledger, with a budget tuple, within dependency and pin constraints); SLICES are the max-4-hour units INSIDE a claimed goal, carved by the claim holder - never picked independently, because two machines building slices of one goal concurrently is the entanglement Ruling S exists to forbid (the mega-batch of 2026-08-29 entangled ten concerns in one tree; R-20 in memory/rulings.md is its lesson). Cross-machine parallelism on one feature is achieved by SPLITTING into goals, never by sharing a goal. (2) From that Wido ordered bounds on goal size: without them we get huge goals that are harder to design, plan, and execute in parallel - 'concurrency is something to design for'. Inventory of what exists: R-2 (big tickets draft first, only the human's word opens arc-sized work) is the human front door but is conduct, not machinery - nothing mechanically refuses a bloated goal; the budget tuple at claim (elapsed/attempts/job-minutes/active-jobs, human-set per R-2, breaker-enforced per Law 1) bounds spend and per-goal concurrency but fires only at spend time; the slice law underneath (slice-norm-hours=4 in metasystem.conf, strict-form approval for oversized, reaper capDeadline enforcement, 50 percent grace at goal level) bounds the work unit but not the goal. The gap: scope has no mechanical floor at planning time. (3) Concurrency therefore has TWO AXES with different owners: SCOPE (this goal - planning parallelism: arcs of small goals are the parallel-execution shape, claimable per machine) and LOAD (machine-concurrency-governor goal - execution parallelism: per-machine slots so parallel passes never overload a host; born when load flipped a suite timeout on 2026-08-29). Do not merge the axes. (4) Wido then ordered the split mechanism: a really big goal is WELCOME at intake - nobody pre-shrinks intent - but it must evolve, almost automatically, into smaller goals BEFORE slicing starts. 'Almost' is deliberate: the machinery triggers (norm refusal), atomizes (split verb), and enforces (dependency-gated claims), but the split CONTENT - where the seams fall - is designed judgment in the Fable lane (R-25 lanes), because a bad decomposition is how concerns entangle. (5) Authority law for splits: members inherit the parent's origin and authority envelope; a split never mints new authority and never expands scope (members' combined intent stays within the parent's); human-origin parents split without a fresh human word, but the split DRAFT is ratified by the parent's origin tier (human-origin: Wido reads it; main-origin: coordinator ratifies). R-2 stays the front door for genuinely new big authority. THE BUILD, 3 slices of 4h: Slice 1 (design, Fable lane): (a) scope norm - configured ceiling metasystem.budget.goal-norm-job-minutes (proposal 1440m = six slices) checked at set-budget AND claim; over-norm refuses with the typed remedy naming split; exception only via arc membership (each member clears the norm) or the human's strict proven approval form the slice law already uses (goal= minutes= goalRevision=); (b) the split verb - 'goal split --id parent' takes drafted member definitions and atomically opens members bound to an arc named for the parent, records dependency edges, concludes the parent as decomposed-with-pointer. Slice 2 (implement, Sol lane): norm refusal + split verb in the goal machinery, fixture proof of atomicity, arc binding, dependency edges, origin inheritance, parent conclusion. Slice 3 (implement, Sol lane): claim-side integration - members claimable independently by any machine within recorded dependencies (unmet dependency refuses claim with the dependency named). Disjoint from the steward seam; claimable by either machine; budget tuple is the human's word at claim
- OpenedAt: 2026-08-30T16:08:07Z
- Revision: 4
- Budget: elapsedLimit=6d attemptLimit=8 reservedJobMinutesLimit=960 activeJobLimit=2

History:
- 2026-08-30T16:08:07Z ZBBC230KPCRGNH7WXR6CE9FNXM-m1-bf243850 open actor=m1+coordinator targets=goal-scope-bounds
- 2026-08-30T16:09:26Z B8VEFZ7EP7TPFG2V8MCTWJWJ0E-m1-bf243850 edit actor=m1+coordinator targets=goal-scope-bounds
- 2026-08-30T16:11:19Z 3WMXJV4RP1NPDZDDBFGFH8NB88-m1-bf243850 edit actor=m1+coordinator targets=goal-scope-bounds
- 2026-08-30T16:52:44Z 40PZFCFMKGDQB3F0FW8JQA06CF-m2-bc1be9cb set-budget actor=human:wido targets=goal-scope-bounds
Integrity: sha256=1cb718ea34ef21c6763a845510b1cefe94d8fd4ae5f175bc817a4b185725d6c3
