# retained-proof-reuse-crosses-claims-and-attempts

- State: queued
- Risk: severity=2 novelty=2 exposure=3 accumulation=1 basis="severity 2: a wrongly reused group lets a regression land unproven until the cadence battery catches it; novelty 2: reuse and identities exist and only their scope and composition change, but they are the judge; exposure 3: every landing and every retry on every seat; accumulation 1: the cadence run still sweeps the whole battery"
- Tier: 3
- Intent: Retained proof is reused wherever its execution identity still holds, so unchanged groups are skipped instead of re-run. Today (m1e evidence, 2026-09-11) a selected group is skipped as reused only when a retained terminal-success attempt of the SAME goal and the SAME accounting revision holds it (internal/proofrun/attempt.go retained scan), so every re-claim and every set-budget resets reuse to zero; a group that passed with complete collection inside a FAILED attempt never counts, so a retry after one load-sensitive failure re-runs the whole battery; and every group identity carries the installed policy engine file digest (cmd/metasystem/test.go PolicyEngineDigest, proofrun groupExecutionIdentity), so a rebuild-and-re-arm invalidates go and command groups whose inputs did not change. Evidence: attempt proof-mtwpz326 proved 42 groups green in 68 minutes (08:54Z to 10:02Z); the next attempt at the same revision, proof-mtwsgptr, re-ran 41 of them with zero reused and lost 65 minutes to one group, section/suite-progress-fixtures. DONE means: (1) a selected group is reused when ANY retained attempt on the seat holds a passed, collection-complete result with the same execution identity, regardless of goal, accounting revision, or that attempt terminal result, with validateRetainedGroupReuse still refusing forged or stale metadata; (2) a go or command group identity is its inputs, tools, discovery, environment, contract and behavior-policy digests, and the engine digests enter only for groups that execute the engine (section groups and steward-consuming groups, as today); (3) proven by a fresh attempt after a green battery on a candidate that moved only files outside a group inputs re-running only the groups whose identity changed, a retry after one failed group re-running that group only, and test verify still refusing when a required group has no matching retained result. Cross-machine reuse is out of scope. This changes the judge: own goal, own battery, three green cadence runs before trust (plans/suite-speed-plan.md section 9); land right after landing-runs-standard-deep-runs-at-cadence; do not fold into it. Wido word 2026-09-11: I want that added.
- Origin: human
- Next step: Design first, because this changes the judge: read the retained scan in internal/proofrun/attempt.go (goal and accounting-revision filter, terminal-success requirement) and the identity and projection code in internal/proofrun/test_build.go (groupExecutionIdentity, retained projection), write the design covering the three DONE items with the soundness argument for keeping the policy engine digest out of non-engine group identities, critique it, then build and land after slice 1 of the suite-speed plan.
- OpenedAt: 2026-09-11T12:00:24Z
- Revision: 1
- Budget: elapsedLimit=1d attemptLimit=10 reservedJobMinutesLimit=1200 activeJobLimit=1 reviewRoundLimit=3
- BudgetExceptions: 0

History:
- 2026-09-11T12:00:24Z NSAM5W0MV6P1BDGHYZ212KW6FA-m1e-7776b921 open actor=m1e+main-1789128024-87765-b9c0d2 targets=retained-proof-reuse-crosses-claims-and-attempts
Integrity: sha256=d3f88f052a06dc42283ffb501480442f4c8de2141dd4bb2b26cd7cca5c818d65
