# brief-declares-the-round-boundary

- State: queued
- Risk: severity=2 novelty=2 exposure=3 accumulation=2 basis="severity 2: a wrong boundary refuses a legitimate round or admits an unrelated one, both recoverable by a brief edit; novelty 2: a new brief header and a new conformance check, but both sit beside existing preflight and boundary code; exposure 3: every implementer chain on every runtime; accumulation 2: each undetected drift costs a read plus a fold round and the rounds compound across a program."
- Tier: 2
- Intent: The brief declares the round's boundary and the engine holds the builder to it before any read is spent. Today the implementer declares its own diffBoundary and conformance checks the diff against that self-declaration (internal/validate/conformance.go:782-810), the only scope rule is prose (scripts/agents/roles/implementer.md:3, 'add no unrelated work'), and the check for unrelated change is assigned to the critic (skills/code-critique/SKILL.md:31), so scope is judged last and by a paid read. Evidence 2026-09-14 (goal coordinator-context-stays-under-budget, slice 3 unit C): the build returned 3,164 changed lines against a 1,500-line ceiling stated in the brief, about 845 of them a journal subsystem that no design section or brief line named, wired into shared revival; the Opus read found it behind nine other findings; the refused-workspace cleanup unit did the same twice and was parked. DONE means: an implementer brief carries a 'Boundary:' header naming the repository paths the round may change (directories and globs allowed) and a 'Ceiling:' header with the maximum changed lines (additions plus deletions), both extracted by the brief preflight in internal/dispatch/brief.go beside 'Working Mode:'; conformance at review stage refuses a candidate whose changed paths fall outside the brief's boundary or whose changed lines exceed the ceiling, naming every offending path and the count, as a typed violation the seat checks before it dispatches a read; a brief without the headers is admitted unchanged. Fixtures prove: a round that adds an unlisted file is refused naming it; a round over the ceiling is refused with the count; a round inside both is admitted; a brief without the headers keeps today's behaviour.
- Origin: human
- Next step: Design first on the design lane (Codex gpt-6-astra): the two header forms, the path-match rule (repository-relative, the same dialect conformance already projects), the ceiling's count (git numstat over the merge-base diff, binary files excluded), the typed violation names, and where the seat reads the violation before dispatching a critic. Then build (Codex gpt-5.6-sol) behind the four fixtures in DONE, Opus read, seat lands. Cheap practice until it lands: the coordinator counts changed lines and lists untracked files on every return before spending a read. Opened at Wido's word 2026-09-14 in the delivery-efficiency program, next after coordinator-context-stays-under-budget.
- OpenedAt: 2026-09-14T15:28:36Z
- Revision: 1
- Budget: elapsedLimit=4h attemptLimit=6 reservedJobMinutesLimit=720 activeJobLimit=1 reviewRoundLimit=2
- BudgetExceptions: 0

History:
- 2026-09-14T15:28:36Z 8AZ0ZXR3DM2PT1EVT97Y1F7WYA-m1e-c6925449 open actor=human:Wido targets=brief-declares-the-round-boundary
Integrity: sha256=a0f80b1b2efbdced26da92a7c146289dd242f5d64ecd75f530ac826c8e720224
