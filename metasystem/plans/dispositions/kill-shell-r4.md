# Dispositions: kill-shell plan, round 4

Job: design-critic-20260811t234127z-ac2a (codex gpt-5.6-sol, xhigh).
10 findings, 10 material, all accepted.

| id | disposition |
| --- | --- |
| KS-R4-001 | accepted — the no-python check is staged like the here-doc rule: zero for PRODUCTION scripts at Phase A, a measured ratchet for fixture scripts reaching zero at Phase F. A fence that reds the suite on landing day would be a different contract. |
| KS-R4-002 | accepted — the target grammar gains its third legal shape: the SEQUENCER — a fixture driver that runs arrange-act-assert sections in order, every decision in verbs, guard clauses only. Phase F's drivers are lawful by name, not by exemption. |
| KS-R4-003 | accepted — the budget scopes to METASYSTEM-OWNED shell: the shipped payload allowlist plus template scripts. An adopted project's own deployment shell is not this fence's jurisdiction. |
| KS-R4-004 | accepted — honesty about limits: the fence mechanically enforces ratchets and structural counts; the zero-decisions invariant is enforced by the registry's per-script thin-shim VERDICT maintained under review discipline. The plan stops claiming syntax counts prove semantics. |
| KS-R4-005 | accepted — the round-1 correction overcorrected: the cap-authority lock PRIMITIVE is still shell mkdir/rmdir polling in both dispatch and arm-supervision. Phase B ports it to the Go owner-lock discipline under the shared-lock interoperability contract, with the mixed-era fixture proving both eras contend correctly. |
| KS-R4-006 | accepted — receipts leave the Phase D hook-policy list: the hook performs no receipt operation, and inventing one would be new behavior. Receipt state remains Phase A's receipt family, owned by receipt.sh's port alone. |
| KS-R4-007 | accepted — keep-deadline expiry is TEMPLATE-ONLY enforcement; adopted targets get warning-only. Time passing can never fail an installed target's validation. |
| KS-R4-008 | accepted — the Phase F assertion surface is decided: a fixture-only family (construct, mutate, corrupt, assert, poll, probe verbs) shipped like any other family, with domain validators used where they already exist. The ~189 replacements have one owner. |
| KS-R4-009 | accepted — the Go sweep gains the same disposition mechanism as shell: staged work carries a registry entry naming its governing plan (the janitor names records/supervision/supervision-lifecycle.md) and is exempt; deletion requires no-caller AND no-governing-plan. Dead-code-dies-first cannot eat parked-by-design work. |
| KS-R4-010 | accepted — the migration note is specified: docs/migrations.md, shipped in the payload, one entry per deleted script (path, replacement verb, date); the fence refuses a deletion whose entry is missing. A note adopters never receive is not a note. |
