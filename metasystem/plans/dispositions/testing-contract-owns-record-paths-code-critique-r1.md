# Dispositions: testing-contract-owns-record-paths code critique round 1, chain tcorp-cc1-20260910

Round 1 (Opus, 2026-09-10 11:59 to 12:08Z) read the engine landing (rounds 1 and 2 of job implementer-4f7f89d5ae26d31cd1fa3ae8, reviewed tree 22cad4bd485808fe55c8b1d7b92b49532d6368c4) and returned one material finding and three low ones. The material one is folded in round 3 and the chain closes on a fresh read of that round.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| TCF-01 | accepted | True and material: validation accepts a fallback naming a surface that also declares paths, and the selection then ignores that surface's patterns; through the command layer's adoption of a candidate fallback when the base has none, a candidate could strip a shared path's groups in its own landing. The critic ran it on a scratch contract (base app and core surfaces, candidate adds fallback core: core-unit disappears for src/core/x.go). | Folded in round 3: the fallback surface must have empty paths and empty dependsOn, refused otherwise; the committed-contract pin asserts both in its declared phase. |
| TCF-02 | accepted | A fallback with dependsOn would be pulled in by every change to its provider, making every such landing pay the full battery; the saved residual declaration has neither, so nothing ships wrong, but rigor is cheap here. | Folded with TCF-01: dependsOn refused on the fallback surface. |
| TCF-03 | accepted | The three command-layer lines that adopt a candidate's fallback when the trusted base declares none had no test; a regression fails closed but the second chain depends on exactly that handover. | Folded in round 3: a command-package test for the handover and for the base-declared case. |
| TCF-04 | noted | The residual surface's group lists are the union, but without a risk raise an unowned path under a low-risk goal plans standard mode, not the deep union; the full battery holds only when goal risk requires deep. This concerns the declaration, not the engine. | Carried to the chain that declares the residual surface: it declares the highest risk raise any surface carries (severity 2 today) and the pin asserts it; the design page's rule on unowned paths is amended in the same landing. |

Gaps recorded by the critic: it did not run the conformance review stage (read-only policy; it rebuilt the diff and confirmed the tree), nor the command tests, fast gate, contract check and plan probe (the orchestrator ran those green on round 2's tree), and it named the design tension with rule 6 of the testing-contract design page, carried to the declaring chain as above.
