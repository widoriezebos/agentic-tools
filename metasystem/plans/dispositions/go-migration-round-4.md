# Dispositions: go-migration, round 4 — the last master round before the rescope

Round 4 (gpt-5.6-sol, job go-migration-plan-r4, 11 material of
which 7 critical, NOT-CONVERGED). Trajectory across the chain:
11, 13, 13, 11 — stopped falling. All eleven verified and folded;
then IL-23 applied: the master plan freezes as strategy, and each
phase gets one just-in-time sol round over its own short
implementation note, fixtures adjudicating below that. Recorded in
the plan's "Review model from round 4 on".

| Finding id | Disposition | Amendment |
| --- | --- | --- |
| GO-MIG-R4-001 | accepted | The arming gate reclassified PORT -> REPLACEMENT: the closed design replaces arming (born-owning lock, registry reservation, armed-before-lock); strict conformance would preserve the ownerless window being designed away. |
| GO-MIG-R4-002 | accepted | Phase 0b is a REAL phase entry: registry bootstrap, gate wired at arm entry, janitor verb + suite-start invocation, held-flock fence, negative-proof fixture. |
| GO-MIG-R4-003 | accepted | The cohort teardown ledger moved from Phase 3 into Phase 0b: the flip's zero-leak bar depends on it and today's driver has no teardown at all. |
| GO-MIG-R4-004 | accepted | The inventory is GENERATED from a filesystem sweep, not curated — the four omitted live components (and any future ones) cannot hide from a sweep. |
| GO-MIG-R4-005 | accepted | The sweep covers *.sh too: every shell file classified DECIDES vs COMPOSES; deciders name their port phase; category waivers are not classifications. |
| GO-MIG-R4-006 | accepted | The census bundle covers the FULL nondeterministic input closure — clocks, env, timezone, path resolutions — all injected at replay. |
| GO-MIG-R4-007 | accepted | The flip holds the cap-authority lock across re-quiesce, conf edit, and re-arm; dispatch reads the selector under that lock at record creation — no admission straddles the flip. |
| GO-MIG-R4-008 | accepted | The matched pair supplements port conformance, never substitutes: counts must be equal within recorded variance and ANY unexplained difference (either direction) blocks deletion. |
| GO-MIG-R4-009 | accepted | Execution attestation: the binary stamps engine+build-hash into the artifacts it writes; extraction verifies the workload's own artifacts, not installation paperwork. |
| GO-MIG-R4-010 | accepted | Deployed-target rollback after deletion is RE-PROVISION from the tagged pre-cutover commit — replacement, the operation targets are built for — not an in-place re-adopt that adoption would refuse. |
| GO-MIG-R4-011 | accepted | The replay oracle compares the WHOLE stderr stream byte-equal after enumerated normalizations; the refusals manifest is for attribution and fixtures, not the comparison boundary. |
