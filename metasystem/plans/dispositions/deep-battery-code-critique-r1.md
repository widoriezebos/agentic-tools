# Dispositions: the seat's 2026-09-11 landings, code critique round 1, job code-critic-abb457065f1f63f2205a664a

Round 1 (Claude on claude-opus-5, 2026-09-12 00:00 to 00:40 local) read the range a584e5c8 to 7a6babbb over metasystem/ (twenty-two landings, subject commit 7a6babbb, tree 25ce0a759038f8b8ea58b1f4c3f9573f4cf977af) under the working rule R-98-m1e: the first independent reading of the fix-forward landings, one round, no second read. Eleven findings, five material, six gaps.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| F-1 gate cmd kill does not end the run | accepted | Measured by the critic: a terminate signal to the go test driver leaves the compiled test binary running to its end, so a unit red left the whole command package running under the race detector on the box the next groups measure. | go-gate.sh launches the cmd run with job control on, in its own process group, and the unit-red branch ends the group (TERM, wait, KILL). |
| F-2 shard launch error orphans running shards | accepted | Read at the launch loop: four early returns before the wait, so an error on a later shard returned while earlier shards ran inside a worktree the caller then removed. | The shards share one cancelable context; every launch failure cancels it and waits before returning. |
| F-3 bb1e2dfd's verification claim could not have held | accepted | True: the whole-package cmd run under the gate's environment was started before the bed edits that landing carried, so it verified an earlier tree; the dispatcher-bed literal test went red on main for about fifty minutes until 9a10d20e. The claim in the commit message is wrong and cannot be amended. | Recorded here, on the design page and in the seat's memory: a verification is of the final tree, never of a tree that predates the last edit; the working rule's "one verification each" means one of the tree that lands. |
| F-4 new process-tree test fails instead of skipping | accepted | Reproduced by the critic in its sandbox: the process listing was denied and the mission-runner package went red for a reason about the host. | The test skips with the repository's own message when the listing is unreadable. |
| F-5 merged coverage lines absent from the group log | accepted | Read: the log was written and digested from the shard buffer before the merge, so the retained evidence held only partial numbers. | The merged lines join the group's output before the log is written and digested; the caller receives no separate merge text. |
| F-6 processor budget applies per shard | noted | True: each shard's supervisor receives the group's whole processor-time budget, so the group can consume up to shards times the budget. Not material: the bound still ends a runaway shard; a per-shard share would make a fair split of an uneven partition the limiting shard. Left as is with this record. | none |
| F-7 process listing shelled out on every platform | noted | True: the start window's sampler runs ps on Linux as well, where /proc would do. Not material: one listing a second for at most two minutes. A Linux-native reader is a follow-up when a Linux host measures it. | none |
| F-8 witness fallback branch looks unreachable | noted | The critic read the exit-3 fallback in witness-gate.sh as unreachable from the current inner gate. Not material; the branch is the documented environmental-refusal path and stays until the inner gate's exit codes are re-read together. | none |
| F-9 scenario ceiling is still a wall clock | noted | True and already recorded: the bed's per-scenario ceiling is a scaled wall clock (fixture-budget bed-scenario), the shape R-35-m3 names; a progress-bound ceiling for scenarios is the same work as the go groups' consumption bound, for the beds. Follow-up under the program. | none |
| F-10 copied adoption target no longer proves delivery | noted | Intended: goal adoption-comparison-runs-once (39907ed2) proves delivery once, on the filled target; the copied target proves registration. The critic reads the same fact. | none |
| F-11 ownership re-probe also retries a proven foreign group | accepted | True and harmless (the critic answers the dispatch's question: a foreign group can never be signaled); half a second wasted per foreign wind-down. | The re-probe runs only while the verdict is indeterminate. |

Gaps recorded by the critic: no computed diff artifact rode with the dispatch (the subject was a commit, not an implementer job), so the reviewed tree is the subject commit's own; no fixture bed, race gate or proof attempt was run (the dispatch forbade them; the five dispatch clusters and the ten adoption scenarios were run through the engine by the seat: attempts proof-mtxfiflx and proof-mtxfrlt9, and cadence runs 8 and 9); the process listing was denied in the sandbox, so the sliding start window was read, not exercised; no Linux host; commit ce59b313 was excluded by the dispatch, and the critic notes that its carried-landing path skips the shared testing verification while 5c44e5f9 in this range makes that branch skip the static re-proof too, an interaction it did not judge: recorded for the seat that landed ce59b313 and put to Wido with this record; and a cluster split can turn a does-not-exist assertion vacuous, which the seat's own scan answers: every job id a cluster reads is made in that cluster or by a shared leg it runs first (one miss, happy-r2 for cluster e, was found by the live run and repaired before landing).
