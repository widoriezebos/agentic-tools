# Dispositions: kill-shell plan, round 2

Job: design-critic-20260811t232055z-a7a4 (codex gpt-5.6-sol, xhigh).
5 findings, 4 material, all five accepted (KS-R2-005 non-material:
the plan's per-phase fact refresh already owns sheet drift, and the
critic's own 573-citation audit found no semantic invalidation).

| id | disposition |
| --- | --- |
| KS-R2-001 | accepted — the characterization list gains the standing-vs-explicit reaper contention rule (a standing reaper skips a busy lifecycle lock; an explicit reap waits and fails on timeout) and the cap-authority ACQUISITION timeout beside its disappearance. |
| KS-R2-002 | accepted — the authority gate is defined three-dimensionally: the Phase C design presents a table mapping every driver operation to (caller classification, authority mode, exact job scope), proven equal to the shell router's current mapping by a test that walks the table against internal/authority's matrix. Any dimension differing fails the gate. |
| KS-R2-003 | accepted — the here-doc rule is staged and made decidable: Phase A BASELINES a ratchet over here-docs whose sink is a shell interpreter (piped to bash/sh or written to a path later executed — a syntactic pattern, with prompt/payload here-docs untouched); the ratchet reaches zero in Phase F when the fixture conversion removes the legitimate generated scripts. No immediate refusal lands before the code it would refuse can be ported. |
| KS-R2-004 | accepted — adoption keeps a thin bootstrap shim by necessity, exactly like go-gate: adopt.sh builds or locates the binary, then execs `metasystem adopt run`; zero decisions in shell, and the fresh-checkout path README documents stays valid. |
| KS-R2-005 | accepted (non-material) — recorded: the committed sheet is a kickoff snapshot; every phase re-runs its fact section, which is the standing defense against drift. |
