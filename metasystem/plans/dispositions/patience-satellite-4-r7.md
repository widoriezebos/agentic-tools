# Dispositions: patience-satellite-4, round 7

Job: design-critic-20260811t192014z-6212 (codex gpt-5.6-sol, xhigh).
4 findings, 4 material, all accepted.

| id | disposition |
| --- | --- |
| P4-047 | accepted — the governing documents move with the design: plans/stop-loss-satellites.md's satellite-4 section is amended to defer to the accepted design and name the deltas (one observable, contract-only floors, vocal-only), and docs/patience.md item 2 drops "each looping activity declares its observable" for the single-observable outcome. The design document is the authority; routing plans record outcomes, not stale intentions. |
| P4-048 | accepted — fold debt inside the table: rows 1-3 now quantify over MODEL EVIDENCE (the r6 definition) rather than "canonicalizable", so sentinels fall through in the table itself, not only in prose. |
| P4-049 | accepted — orphans leave the floor regime: an orphan is a damage report, not a patience breach, and books its vocal line at every booking while uncertified, floor-independent (its lineage is already the anomaly). Within the 20-line bound orphans rank after floor breaches (distance treated as zero), root ascending. The unreachable-warning hole closes. |
| P4-050 | accepted — the table's quantification basis becomes COUNTED jobs (terminal-uncertified plus damaged-status records — exactly the jobs in the patience count), so chains whose only counting jobs are damaged-status still select a floor deterministically. |

Also noted from the critic's gaps: these disposition tables are not in
the canonical joinable format, so closure was audited semantically.
Future rounds keep the same table but this is recorded as a known
tooling gap, not silent.
