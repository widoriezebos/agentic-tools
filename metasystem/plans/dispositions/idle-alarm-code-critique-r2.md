# Dispositions: idle-escalate-crit1, round 2

Chain under review: idle-escalate-build1 at its correction round
idle-escalate-build1-r3 (reviewed tree
3ce8eb9a5095d037d8b7476ec62cf6ec58706579). Critic follow-up:
idle-escalate-crit1-r2, zero material findings. Orchestrator: m1b.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| IGE-01 | noted | Re-reported resolved: no marker gates the escalation; the untrue text is gone; a test pins a mismatched marker on the third stop. Ran: goal package green on the round-3 tree. | none |
| IGE-02 | noted | Re-reported resolved: another branch's block stands on the third stop while the local records are written; a test pins it. | none |
| IGE-03 | noted | Re-reported resolved: the verdict makes no claim and no network call (two tests inject a seam that fails if called); the claim runs in the steward's critical section. Ran: steward and command packages green. | none |
| IGE-08 | noted | True as read: the failed-stop composition path still renders an idle refusal with the open-work preface in one corner. Not material; recorded for goal stop-message-truth. | none |
| IGE-09 | noted | True as read: on the claim-needed path the ordinary idle alarm is queued once by the tick before the continuation launches, pre-existing tick order outside the diff. Recorded on the goal for a later tidy. | none |
| IGE-10 | noted | True as read: extra intents from stops arriving before the tick converge through the one-active guard within a tick; the episode may name a cancelled intent. Not material; recorded on the goal. | none |
| IGE-11 | noted | True as read: the steward's claim publish lengthens an existing critical section a Stop announcement can wait on; the pre-existing fresh fetch already sat there. Recorded on the goal. | none |
