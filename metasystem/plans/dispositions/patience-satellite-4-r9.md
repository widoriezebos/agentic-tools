# Dispositions: patience-satellite-4, round 9

Job: design-critic-20260811t194931z-131f (codex gpt-5.6-sol, xhigh).
7 findings, 6 material; all seven accepted (P4-062 non-material but
right). Three findings moved core semantics, so the document is
regenerated whole again.

| id | disposition |
| --- | --- |
| P4-056 | accepted, two ways — (1) mechanically: a certification counts only with a NON-EMPTY evidence array (schema field, checkable, no content adjudication); (2) honestly: the design stops claiming the observable is proven VALUE. It is witnessed CONSUMPTION — the orchestrator's durable, accountable decision to accept a round. Patience bounds uncertified spend; whether certified work was truly valuable is the certification-hygiene satellite's and the grader's business, and the design now says so in its own voice. |
| P4-057 | accepted — the cumulative set-count violated the inherited rule. The count becomes the CURRENT DROUGHT: counted jobs strictly newer (in the r5 total order) than the newest accepted-certified job of the chain; with no certification, all counted jobs. Every certification resets the streak, so steady alternating progress never breaches. Older uncertified spend stays the Landed Returns section's nagging jurisdiction — patience measures drought, not lifetime debt. |
| P4-058 | accepted — patience is doctrine about who actually worked, and nobody worked in a setup husk. Counted jobs must show the delegate STARTED: terminal status completed, timeout, or cancelled, or failed past setup (a recorded handshake/running transition); pending-setup husks and launch/handshake failures are dispatch-infrastructure noise with their own vocal channels and leave the patience count. Supersedes r6/P4-042: the husk selection rows 7-8 are removed as unreachable. |
| P4-059 | accepted — patience evaluation, orphan reports included, runs ONLY when the sealed contract carries at least one patience entry. Unconfigured missions are silent and byte-identical structurally, not by promise. |
| P4-060 | accepted — an evidence record for rows 1-2 is the newest counted job with model evidence AND a valid role AND a valid runtime, all on that one record; records failing any of the three do not qualify, and the next-newest qualifying record is used before falling through. Selection is total by construction. |
| P4-061 | accepted — a projection filter, not a history rewrite: at assembly time, projected Patience lines whose chain root is now chain-closed are dropped. The prompt stays a pure function of (ledger, current chain-closed flags); the ledger keeps its history. |
| P4-062 | accepted (non-material) — the concept documents now distinguish the FUSE (a pure ledger replay, unchanged) from the patience OBSERVATION (derived from job records and the durable turn log, writing annotations the fuse never reads). |
