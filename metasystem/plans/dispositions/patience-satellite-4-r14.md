# Dispositions: patience-satellite-4, round 14

Job: design-critic-20260811t205155z-2601 (codex gpt-5.6-sol, xhigh).
2 findings, 2 material, both accepted — each proving a round-13 fix
overreached. One supersession recorded.

| id | disposition |
| --- | --- |
| P4-078 | accepted — recorded USAGE now trumps the never-started vocabulary: started(cancelled|failed) ⇔ recorded usage ∨ (non-empty effectiveModel ∧ error ∉ never-started vocabulary). The same handshake error names two different executions (pre-running rejection; post-run session mismatch after usage capture), and money provably spent is work regardless of what the error is called. |
| P4-079 | accepted, superseding r13/P4-077 with reasons — the promised "standard park ask" does not exist for the all-streams-inactive park: no helper creates it, the prompt ask vocabulary has no reason for it, and the answer path could not unpark it. Instead of inventing runner machinery for a corner, the crash contract now leans on what IS shipped: the park itself is visible through the standing open-work reporting surface, the ledger lines stay durable, and no park-path change happens at all — which also restores strict byte-identity for that path. A final booking's Patience lines in that corner wait for the human the same way the park does. |
