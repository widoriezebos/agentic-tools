# Dispositions: patience-satellite-4, round 11

Job: design-critic-20260811t201958z-fb2e (codex gpt-5.6-sol, xhigh).
4 findings, 4 material, all accepted. All four probe the round-10
cutover's edges; the pillars and the clean-evidence architecture
hold.

| id | disposition |
| --- | --- |
| P4-069 | accepted — the "already voiced" claim was too strong for one class: terminal identity-mismatched records are voiced by no wired jurisdiction today (the janitor is unwired — U1 in plans/go-production-grade.md). Fix without resurrecting the damage taxonomy: one AGGREGATE line, `- Patience: excluded=<count>`, booked in patience-configured missions when the input scan excluded records, projecting as `Patience: <count> record(s) excluded from patience — flag to the human.` No per-record identity is needed (that was the exclusion's whole reason), no floors, no taxonomy: a count and a human handoff. |
| P4-070 | accepted — rows 3-4 no longer consult the pre-witness root. The requested-model fallback becomes streak-scoped: the newest STREAK job whose requestedModel is model evidence (valid role and runtime on that same record). Pre-witness history now influences nothing anywhere, including selection. |
| P4-071 | accepted — a WITNESS must certify a job that is itself participating and STARTED (any terminal status). Certifying a husk or a never-started failure is ignored entirely and cannot reset a drought. |
| P4-072 | accepted — the started predicate is defined over shipped fields only: status ∈ {completed, timeout} proves started structurally (the CAS map reaches those statuses only through running); otherwise started ⇔ sessionEstablishedSignal true ∨ recorded handshake success ∨ non-empty effectiveModel ∨ recorded usage. Cancelled and failed records lacking all of these are husks and never count. |
