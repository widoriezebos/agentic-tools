# Dispositions: supervision lifecycle, round 9 — fold failures and cross-document conflicts

Round 9 (gpt-5.6-sol, job supervision-lifecycle-r9, 6 material + 1
non-material, verdict NOT-CONVERGED) verified the round-8 folds. The
count held at 6 but the kind narrowed again: no new surface, no
reframe — every finding is a place where two sections of the two
documents state one rule with different force, or where a round-8
fix was applied to one instance of a shape and not its sibling. All
six material findings verified against the documents and the
committed code (the lock-birth window and the sequential stop
ceilings were confirmed line-by-line in arm-supervision.sh) and
folded; the non-material one fixed in passing.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| SLC-R9-001 | accepted | Fold failure of SLC-R8-006: D-4 still said "recorded identity OR invocation signature", authorizing a kill on pid + start alone — the same-second pid-reuse hole the round-8 triple was built to close. Two normative statements of one rule is how the hole opened. | D-4 defers to REG-6 as the ONLY normative kill rule and summarizes the triple without independent force. |
| SLC-R9-002 | accepted | Round 8 removed the ownerless acquisition window from the registry lock and left it in the checkout lock: the committed armer creates `lock.d` empty, publishes `owner.json` only after the owner launches, and its join path refuses an ownerless lock forever ("no process identity exists to prove dead") — an armer killed in that window wedges a live checkout permanently, verified at arm-supervision.sh:581-610. | D-1: the checkout lock adopts REG-4's discipline verbatim — rename-born acquisition of a private directory whose `owner.json` names the ARMER as a reservation, atomically replaced by the owner's identity once captured; recovery is death-only and total; legacy ownerless directories are garbage after the bounded window. New Proof: checkout lock birth. |
| SLC-R9-003 | accepted | Fold failure of SLC-R8-002: a scalar `retiredThrough` naming "the highest generation verified dead at this stop" lets gen 3 record retiredThrough=2 while gen 1 was never verified, and compaction drops everything through 2 — the only records of a possible survivor, against the Proof. No rule forbade the skip. | REG-2: `retiredThrough` is a CONTIGUOUS WATERMARK — advanced only over a fully verified prefix, re-verified at every relaunch, pinned by any unverified older generation. REG-3 compaction notes the contiguity; Proof sharpened: an unverified generation survives even when a newer one was verified. |
| SLC-R9-004 | accepted | Fold failure of SLC-R8-005: intent freshness was checked at terminal-append time, AFTER the teardown D-1 mandates first — a teardown legitimately consuming the sequential 5s component ceilings (arm-supervision.sh stop_identity / stop_recorded_components) expires the intent and relabels an honest shutdown `terminated`; and the caller's owner-stop uses the same single ceiling, so it can KILL an owner still mid-teardown, before the terminal append. | D-1: the owner LATCHES the intent at exit-initiation, before teardown — attribution is about why the exit began, not how long it took; the caller's escalation waits the full owner-stop wait (D-6: 20s scaled, two component ceilings plus append slack; component ceiling fixed at the committed 5s scaled); a caller that does escalate appends the terminal itself as `reaped shutdown-escalated` (new REG-2 reason). Proof extended both ways. |
| SLC-R9-005 | accepted | D-4's janitor list omitted open claims with a provably dead owner on a LIVE checkout, though D-7 names "provably dead owners' surviving sets" and REG-2 carries reason `owner-dead` — one implementer leaves the dead owner's detached components running until a future arm, another reaps them now. | D-4: the janitor list gains the owner-dead case, ordered after checkout-gone — the arming gate resolves such claims only when an arm happens to run, and dead owners' components must not run on until one does. New Proof: owner dead, janitor alone. |
| SLC-R9-006 | accepted | Compaction retains bound unreleased custody but drops the cleanly closed claim whose `arming`/`armed` records CARRY the binding (the round-5 simplification put the custodyId on those records and nowhere else) — after compaction the custody re-reduces as UNBOUND, silently disarming the dead-custodian rule; keeping the claim instead violated the declared output. | REG-3: bound unreleased custody is retained TOGETHER WITH its bound claim's `arming`/`armed` records even when that claim is closed clean — the claim stays closed and absorbing, its two records ride along solely to carry the binding until `custody-released` lands. New Proof: binding survives compaction. |
| SLC-R9-N001 | accepted (non-material) | D-6 summarized the machine cap with three slot classes and corrected itself to four later in the same paragraph — a stale summary, unambiguous because D-4 and REG-3 agree on four. | D-6 states the four classes once and the duplicate sentence is gone. |
