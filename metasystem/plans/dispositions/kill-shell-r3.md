# Dispositions: kill-shell plan, round 3

Job: design-critic-20260811t233055z-5610 (codex gpt-5.6-sol, xhigh).
11 findings, 11 material, all accepted.

| id | disposition |
| --- | --- |
| KS-R3-001 | accepted — Phase F's scope statement completes: fixture DECISIONS (budget computation, cap scaling, watcher verdicts, cleanup selection) become Go verbs; bash keeps only section SEQUENCING. The end state's "logic of consequence" claim now has no fixture exemption. |
| KS-R3-002 | accepted — the coverage ratchet moves to a prerequisite of Phase A, not B: the first production ports are exactly what it must protect. |
| KS-R3-003 | accepted — Phase D names the hook's POLICY as the port surface: suppression windows, once-per-session stop blocking, protocol advancement, lease renewal, evidence collection, and receipts all become report/supervise verbs; the hook file keeps only the harness entry contract. |
| KS-R3-004 | accepted — the target contract gains the second legal shim shape: launch-wait-consult custody where a protocol requires regaining control after a child exits (hosts), with zero decisions; final-exec remains the default shape. |
| KS-R3-005 | accepted — a SHARED-LOCK REGISTRY is added: locks touched by more than one phase (cap-authority between dispatch and arm-supervision) carry an interoperability contract — the Go side must keep the on-disk protocol bit-compatible until the last shell participant ports, with a fixture proving mixed-era contention. |
| KS-R3-006 | accepted — the authority gate gains its fourth dimension: classification PROVENANCE. The gate must show the driver's caller classification derives from the same live lease/process inspection the shell router performs, not from caller-supplied claims; the matrix trusts its input, so the derivation is the security boundary. |
| KS-R3-007 | accepted — the adoption bootstrap must prove binary freshness: the build stamp equals the checkout's HEAD or the shim rebuilds before executing the verb. A stale gitignored binary can no longer carry an old adoption transform. |
| KS-R3-008 | accepted — the registry is specified: checked in beside the budget (scripts/agents/shell-dispositions.json), and `keep` is lawful only for scripts already satisfying the thin-shim contract or carrying a dated port entry the fence treats as debt with a deadline. |
| KS-R3-009 | accepted — watchdog JOB-FILE classification goes to the report family, not census: census owns process classification (the glossary's own boundary); the watcher's process-side checks stay with supervise/census. |
| KS-R3-010 | accepted — the budget list re-states the no-python check and the per-file function-count bound explicitly; target and phase now enumerate identically. |
| KS-R3-011 | accepted — the deletion pairing mechanism corrects: the allowlist exports tracked files, so deletion propagates automatically; what a deletion REQUIRES is its registry verdict plus an adopted-target migration note (the compat surface), not a phantom adopt.sh edit. |
