# Dispositions: patience-satellite-4, round 17

Job: design-critic-20260811t212324z-cdf8 (codex gpt-5.6-sol, xhigh).
2 findings, 2 material, both accepted — both pin-down items, no
mechanism moved.

| id | disposition |
| --- | --- |
| P4-083 | accepted — fold debt: the implementation sketch and verification list now carry the round-15/16 spend rule verbatim (spend fields alone; all-null usage proves nothing; tokens-unavailable Devin provider units count). |
| P4-084 | accepted — the never-started vocabulary names the shipped values exactly: `abandoned-setup`, `handshake_timeout`, `launch_failed`, `resume_collision`, `handshake_missing_session_id`, and the prefix family `permissions_mismatch:*` (verified against internal/dispatch/handshake.go:122-124 and scripts/agents/adapters/runtime-common.sh:97,106,247). Recorded-usage precedence (r14/P4-078) is unchanged: any of these errors WITH spend-proving usage still counts as started. |
