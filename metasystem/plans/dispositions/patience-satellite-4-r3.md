# Dispositions: patience-satellite-4, round 3

Job: design-critic-20260811t182632z-42d7 (codex gpt-5.6-sol, xhigh).
4 findings, 4 material, all accepted. Convergence: 15 → 13 → 4.
Applied as targeted edits to the named sections; the pillars and the
round-2 structure stand.

| id | disposition |
| --- | --- |
| P4-029 | accepted — "suffix" leaves the definition: the chain is the SET of jobs reachable from the root (the walk tolerates branches), and the count is the number of terminal, jobId-uncertified jobs in that set. Certified jobs leave the count wherever they sit; siblings simply belong to the set; one annotation per root stays well-defined. Barren early rounds under a later certified round keep nagging until certified or the chain closes — that is honest: their spend never landed witnessed value. |
| P4-030 | accepted — the boundary is stated: the damage-fallback chain (newest canonical effectiveModel → round-1 requestedModel → smallest configured floor for the role+runtime) applies only when MODEL EVIDENCE is missing or non-canonicalizable. A model that canonicalizes cleanly but matches no entry is configured-nothing for that pair: infinite patience, exactly like an absent role+runtime. A resolution table replaces prose. |
| P4-031 | accepted — an attributable record whose status is missing or outside the known vocabulary COUNTS in the patience count, deliberately diverging from the fence direction: fence projection acts on money, so losing sight must never finish a job; patience only speaks, so losing sight must never hide a drought. Fail-toward-vocal, stated with this rationale. |
| P4-032 | accepted — the overflow annotation projects as `Patience: <count> more chains are past their floors (see ledger).` in This Turn, after the detail lines. |
