Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal missionrunner-terminate-flake)
Date: 2026-09-02

# Goal

Independent critique of metasystem/plans/missionrunner-patience-design.md
(landed, in your worktree): the patience-doctrine port for the
terminate-group test family. Attack: (1) each progress signal against the
shipped code — can it stall while the product is healthy, or advance while
it is wedged; (2) the bounded retry's invariant — does the traced
nil-return distinction actually separate refused ownership proofs from slow
kills in every code path; (3) the masking answer — is stays-red actually
guaranteed for a persistent identity refusal, or can the retry loop
launder it; (4) the classifier table rows against the shipped outcomes;
(5) the cleanup handshake's exit-path coverage. Findings material and
grounded. A clean return closes the design; the build ships.

# Constraints

Wall-clock budget: 20 minutes. Return per the design-critic schema.

# Gap Rule

stop and report a gap; never fill it silently.
