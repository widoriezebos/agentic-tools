Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal digest-landing-race)
Date: 2026-09-02

# Goal

Independent critique of metasystem/plans/live-records-landing-design.md
(landed, in your worktree): the carry convention for machinery-written
tracked files inside the clean-tree landing gate.

# Attack

1. The claim that the cross-machine half is already solved: verify the
   union merge driver and carriage refusal are actually landed and cover
   the digest (the seat hand-merged fifteen digest conflicts on
   2026-09-01/02 AFTER that landing — reconcile the design's claim with
   that trail: adoption gap, attribute gap, or wrong claim?).
2. The carry convention's interleavings: writer appends during the
   re-stage; two landings race the same live path; a landing dies between
   re-stage and push; byte-equality across all three.
3. The registry surface: undeclared live paths still refuse; a new role
   adopts without touching land.sh; nothing else weakens the clean-tree
   guard.
4. The digest migration and its fixtures.

Findings material and grounded. A clean return closes the design phase;
the build ships.

# Constraints

Wall-clock budget: 25 minutes. Return per the design-critic schema.

# Gap Rule

stop and report a gap; never fill it silently.
