Working Mode: implementation
Orchestrator Identity: root Codex, main-1790272787-5030-13dde3
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| intent-workflows-continuation | 6500 |

Resume your existing session. Preserve all uncommitted product work. Root has
merged current origin/main (31f26c53e, goal-record changes only). No product
commits yet. Do not commit/pull/push, read conf.local, touch root plans/rulings/
AGENTS, or start real services. User's machinery bypass remains in force.
The complete accepted parent design and original build brief still bind.

Finish the remaining PARENT obligations, prioritizing the substantial untouched
IW-4/5/6 human/operations routes, then close the IW-2/3/7 holes in return 2. Read
the original brief's FINAL owner audit and exact administration appendix. Build
all authorized outcomes; do not stop at a facade or an argv-routing fixture.

1. Exceptional land and actual carry consume/delivery, accept-risk with authentic
   review inference/authority, split/group examples.
2. Targeted ask retry under existing poll lock and withdrawal synchronization,
   authenticated answer instructions; mission answer plus resume idempotence;
   process targets ui/mission/machine retaining all supported power.
3. repair goals/mission/waits and settings administration exact contracts in the
   appendix. Public show project records. Typed check remedies through existing
   owners, no new control store or shell policy.
4. Read-owner collected/published state in named selection, retained request match
   before ambiguity refusal. Positive automatic claim and foreign-holder fixtures.
   Public review closure distinguishes missing holder authority and record-writer
   permission from actual repairable state. Do not send every refusal to a blind
   repair replay. Accepted-risk consults the actual owner record, never prose.
5. Bounded review retry: actual old process proven stopped, same root/cap, replay
   after retry failure. Existing terminal status alone is NOT death proof; inspect
   actual dispatch owner. Exercise the real shell follow-up branch through a fake
   runtime and actual Go admission, including returned id adoption, not only a
   mocked FollowUp callback. Real goal-event wait owner fixture is required.
6. Migrate public docs/skills as they become true. Keep root AGENTS changes for root.
   Root is separately completing the design-author/same-chain design critique
   follow-on; don't implement that unaccepted draft yet. Keep close until all its
   meaningful capability has an actual replacement; don't hide an orphan route.

Your choice to retain decisions beside the immutable critic return is accepted.
Preserve the bound old reviewed attempt across failed corrections and detect the
newest authentic findings even if not collected/published. Reuse the owner parser.
Necessary goal_branch adapter and validate parser exports are within scope; no
parallel validation policy. The independent critic will assess this full diff.

Use focused meaningful tests, register every new test, preserve floors and no
weaker/skipped assertions. No global real-git fallback; use narrow actual Git
adapter fixtures only where the integration is the claim. Actual CLI runtime
artifact via explicit -o. Don't repeat passing whole packages unless changes need
it. Report exact owner proof vs seams and omissions. No final certification.

Maximum 150 NEW tool calls or 35 actual minutes this continuation; a coherent
checkpoint may finish earlier with honest reason. Root will continue the whole
work. Measurement correction: FIRST launch took 18m8.6s, 90 measured calls/97
turns, not 23 or 40 minutes. Return 2 actual launch measured 187 calls/100 turns;
provider/accounting counts differ, report only personally observed counts as such.
Return: metasystem/artifacts/agents/intent-workflows-build-return-3.md.
