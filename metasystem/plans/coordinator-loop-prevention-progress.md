# Coordinator loop prevention: integration and proof

Goal: coordinator-loop-prevention. Owner: main-1788759014-39092-24fa5c.

Current state, 2026-09-08 16:32 UTC: Wido explicitly approved expanding this feature to include the common application test interface, risk-selected canary/standard/deep execution, low-risk fix-forward completion, measured runtime reduction, and separately tracked test pruning. The native next step records this authorization at91dc240b; original tier3 and revision5 budget remain unchanged. Native design author application-testing-design started at16:18:53 on Codex gpt-6-astra/xhigh in its own worktree. Original final review completed with F-8 high and F-9 medium; both were accepted, registered and mechanically joined, not closed as fixed. An independent unchanged receipt canary passed once in7.046s without the custom GOCACHE; the critic discrepancy remains unattributed. No final battery has run. The original35source paths and immutable candidate d342 remain unchanged. Main has fetched remote; later changes are records/plans only and require no source integration before the current design read.

The investigation checkpoint in coordinator-loop-investigation.md retains cycles C1 and C2. This continuation retains their history and the same six-cycle budget; it is not a reset. C1 isolated ordinary proof admission and duplicate failure propagation. C2 is the isolated witness correction completed in native job loop-failstop. Its old-source focused fixture observed exit 23 concealed by a second gate and a successful wrapper exit. The candidate focused selection passed in both environments. Main conformance then reproduced a seed-environment isolation defect in the new test (W-1), retained for the builder's next correction. Independent code critique remains required.

- Cycle budget: 6

## User-directed integration

On 2026-09-08 the user explicitly directed integration of the completed remote stop/status/arm work before further loop work. Main fetched origin and rebased its proposed-design checkpoint onto the source commit a3131fbb and subsequent published goal/receipt records. Current integrated HEAD is 7896a80d. Existing narrator bytes were preserved exactly; all existing receipt rows were preserved across the union merge. Evidence: artifacts/coordinator-loop-prevention/stop-verb-integration/result.json and rebase.log, with a durable external mirror.

The proposal was revised to use proofrun's integrated durable launcher/suite/watchdog records and stoptransition inventory before independent critique. Round one completed on the revised design at 9ca4f88b, with six material findings. The full return was read, all six were joined to accepted dispositions and concrete amendments, and each now has a focused proof target. No shared admission implementation has begun. The isolated witness correction does not overlap the incoming shell source.

The upstream stop-verb receipt records a known failing rearm-launch-fails fixture and omitted landing-wrapper checks. Integration does not turn that statement into green evidence. The incoming goal records show a peer working on fixture-review-by-date-rolls-over; do not duplicate that repair without establishing ownership and relevance.

## Cycles

### Cycle C1
- Mechanism/question: does existing machinery admit or reject ordinary coordinator proof cycles?
- Novel decision-relevant fact: direct proof launch bypassed budget admission; a clean witness fallback reran a failed executed gate.
- Command/artifact/files: source traces retained in coordinator-loop-investigation.md.
- Contract signal for progress: isolate the missing admission facts and distinguish duplicate coverage from broader repeated work.
- Budget and stop condition: ten minutes per independent reader; no expensive tests.
- Recoverable checkpoint: delivered portability source 8ed97738.
- Result: an existing shared launch boundary was identified; duplicate landing coverage alone cannot explain the loop.
- Classification: falsified-continue
- Next action: a bounded design and the isolated failure-propagation correction.

### Cycle C2
- Mechanism/question: can the witness wrapper preserve an executed failure without a second gate invocation?
- Novel decision-relevant fact: a Go-driven old-source test observed two launches and final status zero after gate exit 23.
- Command/artifact/files: native job loop-failstop; its canonical round-one return and witness-before-stop-integration evidence.
- Contract signal for progress: exactly one launch with final status 23, plus retained preparation-only fallback.
- Budget and stop condition: 25-minute implementation target, 120-minute approved job ceiling; only focused checks.
- Recoverable checkpoint: the two exact candidate files and native return are mirrored outside the repository; worktree rebased onto 4b4d4ab5 without changing either candidate file.
- Result: delegate focused selection passed; main reran the same named regression selection against the integrated stop-verb source and it passed in 3.174 seconds. Independent code critique and final gate remain pending.
- Classification: contract-improved
- Next action: retain the correction while revising shared admission around the newly delivered proof lifecycle owner.

### Cycle C3
- Mechanism/question: does the integrated stop/status/arm source build, preserve closed-fence launch refusal, and retain the proof launcher and stop behavior our loop work must reuse?
- Novel decision-relevant fact: proofrun now owns durable process identity, creation claims, and stop integration; the prior lifecycle premise is obsolete.
- Command/artifact/files: proof build through scripts/agents/go-build.sh --out artifacts/coordinator-loop-prevention/stop-verb-integration/metasystem; focused Go tests of the exact stop-fence, proof launch, wrapped run, dispatch and stop-transition boundaries plus process command wiring.
- Contract signal for progress: fresh integrated engine builds and focused boundary canaries pass; source ancestry contains a3131fbb with no conflict or lost local records.
- Budget and stop condition: five minutes for build/canaries; no full suite; any failure is inspected before another run. Do not operate stop against this live checkout.
- Recoverable checkpoint: local HEAD 7896a80d, exact record backups and the isolated native witness worktree. This is continuation cycle three of six, not a new investigation.
- Expected classification and next action: contract-improved for successful integration; update the proposed loop design to use the new production owner before independent review. A failed focused check first requires diagnosis of that exact failure.
- Result: focused checks of six owning packages and the proof build passed. Activation first correctly refused an unpublished draft-checkpoint stamp. A fresh build from exact remote commit 4b4d4ab5 then activated successfully at generation 28 with the same Codex session and lease holder, and verified supervision components. The temporary source worktree was removed. No full suite ran.
- Classification: contract-improved
- Next action: revise the proposed design to extend proofrun's existing lifecycle and connect its retained attempts to dispatch's sole budget projection. Do not reopen portability or duplicate the peer's fixture repair.

### Cycle C4
- Mechanism/question: does extending the integrated proof lifecycle with retained budget/input/outcome facts prevent unaccounted repetition and carry valid measured coverage into normal landing?
- Novel decision-relevant fact: the stop lifecycle now exists and its running/done marker does not establish successful proof; the ordinary budget projection and coverage handoff remain absent.
- Command/artifact/files: revised coordinator-loop-prevention-design.md, one native design-critic chain under the accepted three-round limit, then implementation in the existing builder context after design closure.
- Contract signal for progress: a mechanically closed design using current owners, followed by focused launch-count, shared-budget, custody and coverage producer/consumer proofs.
- Budget and stop condition: design review target twenty minutes per round; native goal limits remain ten attempts, 1200 reserved job minutes and one active job. Stop at the first zero-material round. No full suite before implementation and independent code review settle.
- Recoverable checkpoint: integrated source and exact witness candidate snapshots; this is cycle four of the original six, with no no-progress cycle discarded.
- Expected classification and next action: contract-improved when the specified shared boundaries are implemented and focused proof passes; material design faults receive one joined disposition before correction, never an unbudgeted fresh critic root.
- Interim result: native design-critic round one completed with six source-supported material findings. Corrected the finalization/stop race, proof-only goal stop batch contract, receipt reservation/deadline, actual measurement transport, governed reservation link and CLI retry/outcome interfaces. The findings remain in the same chain; invariant findings were not relabeled as mechanical to bypass review. No full suite ran.
- Next action: same-chain review of the amended design, then existing builder continuation including W-1. The review should inspect changed contracts without repeating broad exploration already retained in round one.
- Round-two result: all six earlier interfaces resolved; one material finding exposed a receipt-publication failure window after proof success. Accepted it and specified one atomic terminal-success record containing the exact receipt payload, with recoverable canonical projection. Checks and detached cleanup precede success. The existing LOOP-9 canary now includes those fault points. Final/failsafe design round three reviews this correction on the same chain.
- Round-three result: zero material findings; the full return explicitly verifies the corrected transaction and its compatibility with the earlier resolved interfaces. The empty final-round disposition join passes. Native register close nevertheless refuses six earlier severe entries because the critic omitted explicit material:false withdrawal rows while recording resolution in evidence. The immutable returns and open register are preserved; no fourth review, risk acceptance or record edit was used. Main requested a bounded read-only search for a supported protocol resolution.
- Implementation continuation: the normal typed follow-up to loop-failstop carries the exact reviewed contract and W-1. Main removed only provenance artifact citations unavailable from the worker's runtime root after one pre-dispatch authority refusal; the complete implementation contract and canonical main evidence are retained. No full suite ran. Native register administration does not replace the actual zero-material verdict or authorize bypassing a later native refusal.
- Administrative finding: the normal source landing owner checks the selected builder/code-critic chain, not every design register on the goal. The design register mismatch is preserved for later resolution and does not block normal source delivery. No native records were edited.
- Implementation attempt result: loop-failstop-r2 reached its configured 120-minute cap at 09:28:47 UTC with status timeout and no canonical return. Its 31 changed source paths, full events and passing focused checks are retained in artifacts/coordinator-loop-prevention/timeout-r2-source and mirrored outside the repository. The patch is not certified. The canaries cover witness environment isolation, retained attempts, stop races, goal proof members, coverage components and receipt recovery helpers; they do not prove all public command wiring.
- Main's concrete boundary canary ran the selector invocation used by the new receipt command: the validator rejected `list` with its own exit 2 before any suite launch. Focused source review also identified incomplete script re-entry/status handling, coverage reservation/root wiring, live-record interference with receipt completion, joined-root input parity, final deadline timing and effective configuration identity. These existing-contract defects are consolidated in implementation-recovery-brief.md; the resolved coverage overbinding observation is distinguished from current findings.
- Recovery authority: native follow-up after timeout was explicitly refused before paid launch and directed a fresh dispatch. Main used that supported route as coordinator-loop-recover-timeout, preserving the same goal, revision, budget and all previous attempts. Its input is the exact retained source patch with SHA256 4228e2317274d361206ac078fe93d25ffa44e11a29eec94a69e439e2b15e0a48. This is source recovery within cycle C4, not a new investigation or critic-budget reset. The brief targets 45 minutes and a valid return within 60 minutes; the native ceiling remains 120 minutes.
- Delivery preparation: remote plans and goal records through 098402d8 were integrated without source changes; all four dirty local record/plan files were restored byte-for-byte under the existing short preservation locks. The normal lease classifier returns DELEGATE in the implementation worktree context, allowing a later checkpoint through commit.sh without another model job. A read-only owner trace found that holding narrator/steward locks throughout a full proof would stall stewardship; final proof preparation must preserve live records without that long lock.
- Source integration at 10:06 UTC: origin/main f32fbfeb adds the peer backlog priority and sequence work. Main rebased its four owned checkpoints while preserving all four dirty files byte-for-byte. The canonical engine was built from that exact landed source and activated at generation 29 with the same Codex writer identity and verified supervision components. The active implementation worktree was left untouched; it must integrate this source after returning, before review. No full suite ran. Evidence: artifacts/coordinator-loop-prevention/peer-source-integration-20260908T100620Z.
- Next action: read the complete recovery return, compute and review its exact patch, prove the remaining command boundaries with canaries, and run a fresh independent code critic. Only a settled reviewed candidate proceeds to the one final full proof and normal failure-stopping commit/push.

- Recovery returned successfully before its 45-minute target with no declared gaps. Main preserved all 35 paths, its full canonical return and event stream outside the repository. Rebase onto the peer-integrated main base succeeded; only cmd/metasystem/main.go changed through automatic merge. Native review conformance returned exact tree 99af9f7680fa5346e0a57b692ac7eef52eb1fe31. Main read all 6415 computed diff lines.
- Main's integrated command/priority canary passed with own exit zero in 4.190 seconds. A new focused fixture run with inherited outer proof context failed with own exit one in 0.8 seconds, exposing legacy-worker test environment isolation. Source conformance also found that legacy workers now receive a control root but the full gate requires an attempt id at coverage startup. Both findings and the actual canary are in artifacts/coordinator-loop-prevention/main-conformance-r1.json and review-canaries; no full suite was launched.
- Independent code review started at 10:28:08 UTC through the normal native code-critic role on configured claude-opus-5 at xhigh, reviewing the exact tree above. Its brief binds the accepted design, complete diff, main findings and concrete delivery-boundary questions; target 30 minutes, existing 120-minute ceiling. Corrections will return to the same resumable recovery implementer, followed by the same critic chain.

- Independent code review round one returned seven material findings: inherited proof context breaks two tests; legacy workers fail coverage startup; foreign nested fixture legs incorrectly claim coverage; spent-budget receipt recovery is unreachable; the coverage wrapper ignores the selected engine; a governed-record reread can dereference nil; and the historical witness test fails in an archive root. All findings are accepted with concrete artifact corrections in code-review-r1-dispositions.md. The complete return and primary evidence are mirrored outside the repository. The critic also confirmed existing top-level command/runtime proof gaps to close with tiny isolated fixtures. No full suite ran.
- The first correction dispatch refused before launch because the critic register had not yet folded its return. Main ran the exact supported critique-register-advance verb (own exit zero, advanced), then the unchanged dispatch started the same resumable Codex session at 10:53:03 UTC as coordinator-loop-recover-timeout-r2. This is one charged follow-up, not two attempts or a new chain. The brief targets 40 minutes and a complete return within 50 minutes, with the unchanged 120-minute ceiling.

- Correction round two returned at 11:30:50 UTC. Main integrated peer record-only changes through64b596ec and preserved all35 source hashes across rebase. Native conformance emitted d7dea8e07b5386b07eaac3c6e0e4e117144ec459; main reviewed all1146 lines of the source delta after its complete first-round diff review. Main reran nine command/wrapper canaries with inherited proof variables (own0,10.486seconds), then seven existing budget/custody/stop canaries (own0,1.1945seconds). Those named checks pass; they do not prove every described edge case or the full snapshot handoff.
- Independent review round two closed each old finding explicitly and reported F-8: producer eligibility compares physical root paths, rejecting the actual equivalent-source witness snapshot; the gate-fence fixture in the admitted root can instead claim the slot before its expected refusal. Main accepted this source-supported defect. The reviewer did not run a full receipt to failure. The conformance pass and earlier canaries missed this cross-script integration; neither is represented as end-to-end certification. The canonical register advanced successfully with F-8 retained as the only current material finding.
- The focused correction brief implementation-coverage-correction-r3.md preserves the accepted contract and requires a failing-then-passing tiny witness/receipt handoff canary. It returns to the same implementer session with a25minute target and35minute return bound; no full suite or broad package sweep. This is the tenth native attempt, within the existing approved envelope. No further native review/proof attempt is admitted without a human budget change.
- Delivery preflight established an independent existing mismatch: review_reference.go requires evidence.reviews to name the terminal implementation job; critic follow-ups inherit the original implementation job. hazard.go also requires the evidence pointer to name a fresh-context root. Thus the normal same-chain review continuation cannot satisfy this final native reference check. The supported source-closure route is a fresh final critic bound to the final implementation job. Main proposes using that as the final third code review, with all previous findings supplied, instead of paying for both a resumed review and a duplicate fresh review. This needs explicit approval as a disclosed exception, with all earlier rounds and findings retained; prior register administration remains visible and is not falsely marked closed. No native record edits, hazard downgrades or hidden budget reset are permitted.
- Current historical native accounting before the final correction was9attempts/1080reserved minutes (every120minute cap counts even after completion). The correction brings it to10/1200. The proposed final fresh review and full proof need two more bounded executions. goal set-budget is the human-owned command; it starts a new accounting revision, so any approval must disclose that fact and retain these historical totals. The full battery remains unrun while this delivery path is unavailable.

- Final focused correction returned before its25minute target. The new equivalent-snapshot canary and premature-claim canary were red before their production fixes and green after. The actual landing command, actual go-gate script, real descendant custody and authenticated coverage publication now pass through a controlled equivalent-source snapshot, with exactly one controlled validator launch and one measurement and no second launch on spent-budget receipt recovery. The expensive witness archive/full test bodies remain substituted in this tiny fixture; the final real validator is still required.
- Main read all382delta lines, accepted the35path conformance scope, and independently reran the three decisive canaries (own0,5.188seconds). All35files and the full native return/events are mirrored in the external evidence root under correction-return-r3. No final review or full certification is claimed.
- After the implementation chain became terminal, the supported review-reference-reconcile command refused with its own exit1: existing criticr2 reviews the initial implementer instead of terminal coordinator-loop-recover-timeout-r3. This confirms the source-level closure diagnosis. The minimal proposed finish is one fresh final critic carrying all prior findings, then the one full proof; paying for both a resumed third review and an extra duplicate review is unnecessary. Earlier registers remain visible and are not falsely closed. The fresh-context exception and new budget remain human decisions.


### Cycle C5
- Mechanism/question: can one existing proof/selection/landing path implement the user-approved common testing interface and risk-appropriate delivery while reducing repeated work, instead of imposing every MetaSystem fixture on every application and change?
- Novel decision-relevant fact: the user explicitly expanded the feature after measured battery-cost inspection; the existing proof path is Go-specific, and final review still exposes source/environment handoff mismatch F-8 plus an unexplained diagnostic failure F-9.
- Command/artifact/files: native design author application-testing-design, exact pre-dispatch application-testing-design-brief.md, battery-cost-inspection.json, final-review-dispositions.md, and application-testing-review-facts.md under artifacts/coordinator-loop-prevention; expected design output application-testing-contract-design.md in the author worktree.
- Contract signal for progress: a grounded, bounded implementation contract for one common interface, explicit risk selection and sufficient delivery proof, actual input/environment binding, measurable reductions and preserved distinct fault detection. Then focused implementation proof before one final appropriate validation.
- Budget and stop condition: native revision5 tuple2d/16attempts/1200reserved minutes/1active job/3review rounds; design author target30minutes, complete return within40minutes, native cap120. No tests or full battery in the design job. Existing stop-loss check returned own0 with4cycles and0no-progress; this is cycle5 of the same6, not a reset. Existing critique histories and unresolved findings remain intact.
- Recoverable checkpoint: worker tree d342 and all35exact source hashes, historical native returns, the user-approved scope record, and external primary-evidence mirror. The new interface does not invalidate or erase these.
- Expected classification and next action: contract-improved only with actual implemented/proved behavior; the next bounded step is main conformance on the authored design and one independent critique chain for the genuinely expanded scope. Stop on material nonconvergence or the declared bound; do not add unrelated services or silently enlarge review budgets.

- Design author returned at16:47:27UTC after28minutes34seconds on the verified Codex Astra/xhigh lane. Only application-testing-contract-design.md was written. Main read the complete design and final refinements, checked the one-path boundary, and mirrored exact design/return/events outside the repository. The contract uses existing proof/landing owners with risk-based modes, actual environment binding, shared runner results and bounded optimizations/pruning. Original F-8/F-9 remain open. Main prepared one independent design critique with explicit withdrawal protocol and necessity questions about native-adapter scope, standard package cost and frozen policy probes. No product changes, tests or full battery ran in this design phase.

- At17:12UTC, independent testing design round one completed in6minutes47seconds with six reported findings. Main read the entire return, advanced the native register (four severe findings remain; scope demotions do not erase the other two), and joined all six dispositions. Accepted Go-independent admission, explicit output/tool inputs, focused standard groups, public-boundary policy guards and deletion of an unnecessary Maven-specific provider. Refuted the claim that critic metadata acts as an implementation allowlist, while recording and compensating for the real incomplete metadata list. Amended the design and matrix in one pass; same-chain review follows, without a fresh budget or prose-polish rounds.
- Remote source fe7212a9 through2ab10814 arrived during review. Main fetched it without moving the active critic workspace, then integrated it after completion with own exit zero and restored both unrelated record files. Main now174ab83d. A focused disjoint-merge proof passed in4.970seconds; an engine built from exact landed2ab10814 activated at generation30 with the same Codex coordinator and verified supervision. The old35-path implementation worker remains untouched. New recertification command checks, one-push target policy, whole-project nested tree equality and mirror/floor identities are included in the testing cutover.
- The design checkpoint initially failed because Go discovered incomplete retained source snapshots under ignored artifacts. An ignored nested go.mod isolated only that evidence; the same normal checkpoint then passed without a full battery or product source change. A critic pre-launch metadata refusal was corrected before any model launch; the failed setup record is retained and does not consume goal budget under ReservationConsumesBudget. Exact evidence is mirrored outside the repository.

- Wido requested a critical assessment of efficient test carving and authorized incorporating its consequences now. Main identified and amended a concrete missing canary-first barrier: delivery stops launching after a decisive required failure; selected unstarted groups remain explicit, and deduplication cannot broaden a cheap canary into an expensive package sweep. Whole-command performance acceptance includes planning/setup and a real MetaSystem focused selection. This continues C5 and the same design critic chain; it adds no service, review budget, or implementation claim.

- Before implementation Wido clarified that expensive runs must collect all independent failures for one repair batch. Main corrected the just-checkpointed overbroad stop rule: complete each admitted stage and summarize every failure, block actual dependents, then stop before the next stage. The design now explicitly covers native runner/scenario collection and a tiny real two-failures-plus-success regression; repeated expensive runs after individual fixes remain prohibited.

- Same-chain testing review round two explicitly withdrew all six prior findings and found two remaining ambiguities: generic execution prerequisites and completeness of native failure collection. Main read and joined all eight dispositions. The final contract keeps groups independent, uses existing native setup/section prerequisites, and verifies an explicit expected inventory for generic JUnit commands; Go discovery remains automatic. A first-failure-only report cannot claim complete collection. Independent source inspection also fixed the comparison baseline: shared necessary correctness repairs plus identical narrowed scenario/timing plumbing, with real witness cost retained. The third permitted critic round is the failsafe; no fourth or new-root reset is authorized.

- The third and final testing design review returned zero material findings and explicitly withdrew all eight prior ids against commit1e461f79. Main read the entire return; no fourth round or fresh-root reset. The continuing design is ready for implementation, while the original author binding remains historical and no runtime/cost claim is made. The 60-minute final read-only reservation leaves capacity inside the unchanged approved tuple; implementation retains its 120-minute ceiling.
