# Proposal drafts — the historical residue sweep (2026-08-26)

Working Mode: design

The draft queue Wido directed (historical-residue-sweep, draft-first
law): every entry below is a PROPOSAL awaiting his word — schedule
(a goal opens citing the draft), fold (into a named live goal), or
drop (recorded here with the reason, never re-raised). Sources
verified against the live ledger and the done/ tree; the sweep's
raw two-list report is preserved in the session record. Register
hygiene (stale KI/flake rows misnaming concluded owners) was
corrected directly in this landing — bookkeeping, not new
commitment.

## Time-sensitive (decide first)

D-1. llm-wiki-pattern decision — plans/llm-wiki-pattern.md:168
carries a self-delete clause: the parked accept/implement/reject
decision EXPIRES 2026-09-03 or at the next retro. Proposal: put it
on a retro agenda before the clause fires, or let it lapse
knowingly (a lapse IS a drop — but it should be chosen, not
accidental).

## Cluster proposals (A-list debts, grouped)

D-2. HUMAN SURFACE, the unstarted family —
plans/human-surface-design.md F-1/F-2/F-3/F-5 (one-command signing,
prose-first mission flow, batch signing, refusal-message sweep);
F-4's "blocked by KI-18" is fossil (the fix shipped). An accepted
2026-08-06 design, zero execution. Proposal: ONE goal
(human-surface-execution) with the four features as slices, or an
explicit drop if the seal-flow rework superseded it. ~6-8h arc.

D-3. FLIGHT RECORDER's owed tail — plans/flight-recorder.md: two
queued legs with carried findings (FR2-008..015), the never-run
core code-critique, missing job-verdict/job-created events on some
flows, and the absent judgment layer (cycle-measured). Proposal:
one goal (flight-recorder-completion), slices per leg; the missing
events are measurement debt that actionable-metrics will feel.

D-4. ACP FLIP prerequisites that the flip outran —
plans/acp-transport-design.md: supervise_acp shell fixture
(success/handshake-failure/cancellation/cleanup — acp-fixtures
covers the verb, not devin.sh's branch), transport-keyed snapshot
identity, the D82 Mac-half cohort, and slice-B's four declared
limitations. Proposal: fold into seam-declaration-convergence
(same convergence family) or one acp-flip-debts goal; the Mac-half
cohort needs a Wido spend word either way.

D-5. THE 2026-08-13 REVIEW's unconverted residues — ~10 items from
docs/reviews/2026-08-13-delegated-decisions.md:470 (suite retry
token vs exit-9, durability staged tail, legacy fence allowance,
selftest-listener removal, stream.secondary grammar, nested
delivery-contract doc-drift, supervision re-arm subdirectory
config, legacy started dual-read). Proposal: a one-hour
adjudication pass with Wido — each becomes fold/schedule/drop; most
look drop-or-fold-sized, and carrying them unread is the real cost.

D-6. CRITIQUE CHAINS that ended with open material findings and
shipped anyway — ric r6 (8 structural), delegate-delivery r8
(empty_reply misclassification, one high), goal-system r10
(report/goal boundary). Proposal: one verification goal that
re-tests the three named defects against today's tree (some may
have been fixed since by adjacent work) and closes or schedules
each with evidence.

D-7. KNOWN-ISSUES completion set — KI-18's owed 20-iteration proof;
KI-25 git-lock flake diagnosis; KI-27 --reviews-tree for the
code-critic (verified absent; today's critic-workspace-custody item
is adjacent — reviewing a TREE would remove the live-checkout
critic problem at the root); KI-22's second half (protocol_error
rounds surfacing); the KI-28 numbering gap (Devin acceptance
bundle, designed not built). Proposal: KI-27 folds into
critic-workspace-custody (same fix family); the rest one
ki-completion goal with per-KI slices.

D-8. GO-PROGRAM TAIL — unattended-run TODOs (shell-fixture proof
list; cohort driver teardown ledger D-3), kill-shell's one
remaining human decision (adopted-engine delivery severance).
Proposal: the TODOs fold into the kit/driver arcs when next
touched; the severance decision goes on the retro agenda with D-1.

D-9. PARKED EXPERIMENTS AWAITING A WORD — stop-loss-last-defense
("awaiting human direction"), two-bars adversarial layer, the
cap-discovery experiment, comparison cohorts (Opus vs sol
delegates), the 0.019→0.981 confirming re-run "with the human
present". Proposal: a fifteen-minute batch ruling session — each is
schedule-or-drop; none should keep living in prose.

D-10. RECEIPT-STATS FLAKE — plans/known-issue-receipt-stats-flake.md
says STILL OPEN + instrumented, but it is in NEITHER register.
Proposal: register it now (flake registry row, first sighting from
the file's own evidence) so the three-strike law applies; the
FixtureEntryFor loud-refusal note rides whichever landing next
touches that file.

D-11. FLAKE ROWS below the promotion bar (five one-or-two-sighting
rows + one unnamed discarded-evidence sighting) — no action per the
registry's own law; listed so the sweep is complete. The unnamed
composite go-test row is the one exception worth a thought: its
evidence was discarded at capture, so it can never be diagnosed —
propose dropping the row with that reason at the next registry
touch.

## Fossil-status hygiene (batch, mechanical)

D-12. Plan files asserting dead states: mv1/mv2-findings-carried
headers claim "none dispositioned" (both fully dispositioned);
collaboration-loop's "waiting on trial 003" (concluded weeks ago);
patience-satellite-4's U1 jurisdiction pointing at a self-deleted
plan; agent-ease-assessment concluded with unfinished next-step
text. Proposal: one 45-minute hygiene pass updating status lines to
truth, citing this sweep; no behavior anywhere.

## Register corrections applied in this landing (done, not proposed)

KI-21, KI-19, KI-32, KI-36, KI-38, KI-39 rows: owners concluded —
rows now say FIXED/closed with the concluding evidence named.
KI-33's row now names its live owner (same-process-succession).
KI-22 row: shipped half closed, open half named (D-7 carries it).
