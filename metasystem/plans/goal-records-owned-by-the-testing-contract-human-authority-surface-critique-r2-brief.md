Working Mode: implement
Orchestrator Identity: m1+main-1788940932-18533-7fa6c2 (dispatch delegate under goal goal-records-owned-by-the-testing-contract)
Date: 2026-09-10

# Review brief: the human-authority surface carrying the hp-terminal change (chain ha-surface1-20260910, reviewing round ha-surface1-20260910-r2)

FINDING IDS: chain-unique, continue at HAS-05, never F-n. You are the
second critic on this chain. Report `round` as 1 in your return: it is
this job's own round. One focused round.

Why: round one added the human-authority and report-scan surfaces to
metasystem/testing.json (your predecessor ha-surfacecrit1b-20260910:
four notes, nothing material; dispositions in the records directory
under this goal's name). Its landing receipt failed on this Mac in the
new humanauthority-standard group with every test passing: one test in
internal/humanauthority on main skips outside Linux and the proof run
counts a skipped expected test as a failed group (now goal
skipped-test-fails-its-group-on-the-other-os). The reviewed change of
chain hp-terminal-build1-20260909 (fourteen rounds, twelve critics,
nothing material left; its briefs and dispositions are in the plans
and records directories under the name hp-terminal-grade-for-stopping-acts)
removes both OS-specific system-login files from internal/humanauthority
and rewrites its tests so none skips on Darwin; it could not land because
its paths were unowned until round one. Round two applies that
fourteenth-round diff verbatim on top of round one, nothing else.

Scope: the computed diff of round two against round one's tree. What
to attack, in order: (1) the join, that the applied change is byte for
byte the hp-terminal chain's fourteenth-round diff (the artifact sits
in this checkout's artifacts directory under the hp-terminal job's
rounds directory, round 14, file diff.patch; compare) with nothing
added, dropped or hand-resolved; (2) that go test -json on
internal/humanauthority at the round's tree reports no skipped test on
Darwin, and which tests would skip on Linux (report them as a note, not
a defect, unless a Linux seat could never pass the group either); (3)
that the surfaces of round one still own every path the combined
change touches (cmd/metasystem session_stop, identity, goalsync_mutations
and their tests; internal/humanauthority; internal/goal; internal/identity;
internal/lease; internal/refusal; scripts/agents) or that the paths not
owned by the two new surfaces are owned elsewhere in the contract; name
any path that is owned by nothing. Do not re-review the hp-terminal
change on its merits: twelve critics did.

# Mandate

1. The join is exact and complete.
2. The combined candidate can pass its own delivery plan on this Mac
   (no skipped expected test in the selected groups).
3. Every changed path has an owner in the candidate contract.

If nothing material remains, say so; that closes the chain and it
lands.

# Constraints

Wall-clock budget: 20 minutes. Return per the code-critic schema with
the reviewedTree from the review record beside the computed diff (the
conformance validator refuses from a sandbox; say so).

# Gap Rule

Stop adding at a gap that needs a decision no page has made; keep what
is written; report the gap with the resolution you propose. Never
delete written work.
