Verdict: not ready. I found 10 material findings: seven critical, two high, one medium. Evidence is from reading HEAD `246c635`; no tests were run and no files were changed. The pre-existing untracked `metasystem` file was untouched.

1. **MF-R2-01 — CRITICAL — Backlog item 1 is still replaced, not folded.** The human contract explicitly requires a blocking `dispatch.sh watch --job <id>`, the exact waiter command printed after non-waiting dispatch, and Stop-hook detection of that live waiter ([backlog-notes.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/backlog-notes.md:10)). R2 explicitly declines per-job waiters. The current public dispatcher has no `watch` verb, while `--wait` merely creates an identity-free `.waiting` marker that no production reader validates ([dispatch.sh](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/dispatch.sh:602), [dispatch.sh](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/dispatch.sh:1471)). A checkout-wide watcher can update records, but cannot wake a session or prove that session has a waiter. “Jobs this session dispatched” is also uncomputable: job `sessionId` is the delegate’s resumable session, the scanner receives neither the host session nor `mainId`, and the hook supplies the session only after `report.Scan` has run ([handshake.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/handshake.go:97), [goal.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/goal.go:249)). An implementer would build repository-health checking, not the required waiter-to-wake mechanism.

2. **MF-R2-02 — CRITICAL — A fresh watcher heartbeat does not attest that the run pass worked.** The production component writes its heartbeat before work, calls `WatcherPass`, logs returned errors, and continues ([supervise_component.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/supervise_component.go:117), [supervise_component.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/supervise_component.go:159)). Thus the heartbeat may stay fresh while every run sweep fails. The stronger shipped `ArmedNow` predicate also requires live owner/watcher/reaper and a fresh successful census, but contains no successful-run-pass attestation ([verifyarmed.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/supervise/verifyarmed.go:35)). R2 must define an authoritative last-successful-run-pass fact; otherwise Stop can permit a blind turn exactly when the run reader is broken.

3. **MF-R2-03 — CRITICAL — Group custody still loses live descendants at terminalization.** R2 says terminal records own nothing, while also acknowledging descendants may outlive the direct workload and retain its log descriptor ([monitor-facility-design.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:99), [monitor-facility-design.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:109)). When the wrapper writes the sidecar and exits, surviving group members immediately lose custody and become `UNTRACKED`. The same defect occurs when a launch fence or takeover sends one signal and terminalizes without proving group death; the shipped lease sweep sends only `SIGTERM` and then marks terminal immediately ([sweep.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/sweep.go:121)). The design must choose group drain, bounded wind-down with proven death, or continuing terminal custody.

   The stated PID-reuse proof is also insufficient: records compare start times only to whole seconds, so the repository’s kill-capable identity discipline requires an argv tag as a third factor ([identity.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/identity/identity.go:8), [custodian.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/identity/custodian.go:41)). `launchNonce` must be observable in the wrapper argv and part of census leader proof.

4. **MF-R2-04 — CRITICAL — Adoption is not generation-fenced.** The cited delegate-job compare-and-swap compares only `status` ([record.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/record.go:242)). R2 permits `adopt` to replace identity while leaving the record `running`. A watcher can inspect generation A, adoption can install generation B as `running→running`, and the watcher’s stale `expect=running` conclusion can still terminalize B. A flock does not help if the decision was made before acquiring it.

   Worse, adoption resets only PID/start/PGID, leaving `launchNonce`, evidence mode, `hungSince`, and the old exit sidecar intact ([monitor-facility-design.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:78)). The old nonce-matching sidecar can therefore conclude the adopted process. Adoption needs a new generation token and evidence reset, or conclusion must re-evaluate the whole generation under the lock.

5. **MF-R2-05 — CRITICAL — The terminal evidence table contradicts itself.** The transition table maps “identity provably gone, no evidence” to `vanished`; the evidence section maps the same dead-process/no-sidecar/no-pattern row to `ended-unknown` ([monitor-facility-design.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:81), [monitor-facility-design.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:107)). An implementer must choose one verdict.

   “Unknown identity surfaces and stays” is not representable either: neither the record nor `RunFact` has a probe-state/error field. Missing or unreadable adopted logs also have no outcome; the unreadable rule names records and sidecars, not logs. Finally, `expect` has only green/red/hung continuations, so `vanished` and `ended-unknown` still have no mechanically selected continuation.

6. **MF-R2-06 — CRITICAL — Lease and authority integration remains unsafe.** `holder-only` admits `HUMAN`, while `RequireHolder` deliberately supplies humans no `mainId` or claim epoch; r2 requires all lease coordinates to be non-null ([authority.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/authority/authority.go:28), [verbs.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/verbs.go:211)). An implementer must either violate existing authority or invent ownership coordinates.

   Authorization alone is point-in-time. Operation-spanning fencing is provided by `lease run-held`, which holds the lease lock across the mutation ([verbs.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/verbs.go:303)); r2 mentions an epoch recheck only for `adopt`, not launch/register/ack/prune. The stale-epoch sweep also has no specified run terminal target. Finally, an `adopted-unverified` process cannot carry a newly minted nonce in its argv, so it cannot satisfy the proposed takeover proof. Copying the job sweep would skip signaling and still terminalize the record, leaving live untracked work.

7. **MF-R2-07 — CRITICAL — The reservation and rollback contract is internally inconsistent.** The pending record must exist before any process, yet the schema requires integer PID/start/PGID fields rather than nullable pre-bind fields. R2 then says the launch fence writes `launch-failed`, while the launcher’s error path removes the record ([monitor-facility-design.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:50), [monitor-facility-design.md](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:88)). The real dispatch precedent retains a failed terminal record; it never deletes its reservation ([dispatch.sh](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/dispatch.sh:145)). Deletion can race a wrapper bind, erase audit evidence, and leave a launched wrapper without a record. One serialized outcome must own rollback.

8. **MF-R2-08 — HIGH — Verdict composition is still underspecified against the actual exclusive ladder.** “Busy suppresses only the goal clause, exactly as today” is factually wrong: today Busy suppresses Open, Waiting, and Unreadable as well ([turnverdict.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/goal/turnverdict.go:167)). Appending run failures to `ScanResult.Unreadable` is insufficient because the hook renders only `Display`, never `Diagnostics` ([supervision-hook.sh](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/supervision-hook.sh:111)). R2 must require mixed-state display tests such as Busy+Unreadable and Open+Unreadable.

   “Green surfaces once” also has no acknowledgment or session-state mechanism: the only added slot is `blockedUnwatchedDigests`. An implementation must guess whether green auto-acks globally, repeats forever, or needs another per-session digest.

9. **MF-R2-09 — HIGH — The promised audit events would be silently dropped.** Pruning relies on events as the surviving audit trail, but the event registry is a closed catalogue, has no run component/events, and the canonical identifier set has no `runId` ([event-registry.json](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/event-registry.json:10), [emit.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/events/emit.go:20)). Unknown events or unauthorized emitters are silently discarded ([emit.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/events/emit.go:170)). The blast radius and obligations omit registry changes, event schemas, allowed watcher/lease/run emitters, and conformance tests.

10. **MF-R2-10 — MEDIUM — Public grammar remains incomplete.** The prose uses `custody:"adopted-unverified"` but the schema omits it. `launchNonce` and `verdictPattern` have no length bounds despite MON-10 claiming every bound is enforced. “Resolved” log paths do not decide nonexistent targets or symlink replacement. Record and sidecar names share `runs/*.json`, so an existing-style glob would ingest `<id>.exit.json` as a malformed run record unless the filename grammar explicitly distinguishes them. Prune also does not say whether it removes the sidecar, which affects ID reuse and residue.

Disposition audit:

| Round-1 finding | Round-2 result |
| --- | --- |
| R1-01, waiter contract | Open: replaced by checkout health; MF-R2-01 |
| R1-02, production host | Host corrected; success-attestation contract remains open; MF-R2-02 |
| R1-03, wrapper versus custody | Process group added; terminal group-drain and third-factor proof remain open; MF-R2-03 |
| R1-04, launch race | Reservation order corrected; nullable pre-bind state and rollback remain open; MF-R2-07 |
| R1-05, lifecycle schema | Hung flag and most fields corrected; outcome table and continuations remain open; MF-R2-05/MF-R2-08 |
| R1-06, lease coordinates | Coordinates added; human authority, operation fencing, and sweep outcome remain open; MF-R2-06 |
| R1-07, typed verdict facts | Typed run facts added; job ownership and full display composition remain open; MF-R2-01/MF-R2-08 |
| R1-08, serialization | Lock owner named; same-status adoption and deletion races remain open; MF-R2-04/MF-R2-07 |
| R1-09, authority | Verb rows added; human coordinates and unverified takeover remain open; MF-R2-06/MF-R2-10 |
| R1-10, evidence wire | Sidecar and bounded tail added; contradictory death row and adoption replay remain open; MF-R2-04/MF-R2-05 |
| R1-11, unknown visibility | More outcomes named; unknown identity and mixed-state unreadables still do not reach humans reliably; MF-R2-02/MF-R2-05/MF-R2-08 |
| R1-12, grammar | Most headline bounds added; custody, nonce/pattern bounds, path policy, and sidecar lifecycle remain open; MF-R2-10 |

Proposed receipt, not written because this was review-only: `type=review|outcome=reworked|skills=design-critique|verify=skipped|corrections=0|stop_loss=no|delegate=none|note=monitor-facility r2 design critique found 10 material findings; revision required`

REVISE: checkout health is still substituted for the required per-session waiter, while group termination, adoption generation fencing, and stale-epoch takeover remain ambiguous enough to ship live untracked work or a false terminal verdict.
