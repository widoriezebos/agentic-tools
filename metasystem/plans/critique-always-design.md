# critique-always

- Owner: m1e (goal 18 of plans/delivery-efficiency-plan.md), claimed 2026-09-12 21:55Z.
- Goal and current status: critique follows the tier, not the hazard class (Wido, 2026-09-10: "if the risk is high, even though it is a small change, it should get a critique"). A chain on a tier-2 or tier-3 goal needs an independent critique before it closes whatever its class; a tier-1 chain closes without one (R-54-m1's tier-1 clause stands). Built by Codex gpt-5.6-sol from the seat's brief (section 2) in a worktree; read by Opus; integrated, landed and verified by the seat.
- In flight right now: nothing. Landed 894a269e, verified through the engine, concluded a5050811 in Wido's name (2026-09-13 01:00 local).
- Decisions made (and who made them): Wido, 2026-09-10 (the small-change lane's second decision): the tier decides the critique, derived from the four recorded risk answers, never from the size or the class of the change. R-90-m1 (Wido): MECHANICAL builds run at xhigh through the configured per-model line; the hazard floor in the table stays medium. The seat, 2026-09-12: the seven build decisions of section 2.
- Waiting on the human: nothing.
- Dead ends (do not retry without new evidence): none.
- Next step: settled; nothing open on this page. The critic's class choice and the MECHANICAL chain's landing lane are scheduled as goal mechanical-chains-at-tier-two-and-three-close-and-land (queued, tierless).

## 1. What changes

Today `requiredConfigurationByHazard` (internal/dispatch/hazard.go) and the `destructiveReach` table in scripts/agents/role-packets.json fix the independent-critique duty per class: MECHANICAL never, DESIGN-BEARING and DESTRUCTIVE-REACH always. Admission records the class row on the job record as `configurationObligations`; chain close (`validateHazardCompletion`) recomputes the duty from the members' classes and refuses closure without a critic job that proves maximal effort. So a MECHANICAL chain on a tier-3 goal closed without a read, against Wido's word.

## 2. The build decisions (the seat's brief to Codex)

- D1. `EffectiveObligations(class, tier)`: the class row stays the floor; when `independentCritiqueRequiredByTier[tier]` (1 false, 2 true, 3 true) is true and the row does not already require a critique, the critique becomes required at the rows every critique-requiring class already carries (maximal, xhigh). Tier 0 (no goal, or unknown) keeps the class floor. Builder rows and live proof stay class-keyed.
- D2. Both surfaces carry the tier map: role-packets.json gains a top-level `independentCritiqueByTier`; the table reader requires it; `ResolveHazardConfiguration` refuses a table whose map is not exactly the Go rule, as it already refuses a weakened class row.
- D3. Admission records the effective obligations: the compose verb and the dispatcher pass `--goal-tier`; the claim and the setup record EffectiveObligations(class, tier); the follow-up match compares against the effective obligations of the record's tier.
- D4. Close enforces the tier: `validateHazardCompletion` derives the duty from each member's class and the root record's `goalTier` (absent means the class floor). The critic still proves maximal/xhigh and a fresh distinct session; a critic for a tier-required read on a MECHANICAL chain is dispatched at the critique rows (DESIGN-BEARING for the critic job).
- D5. Proof, in internal/dispatch: a MECHANICAL tier-3 chain refused closure without a critic and closed with one; tier 2 refused; tier 1 closes without one; a chain without a goal keeps the class floor; the tier map tampered or missing is refused; admission records the effective obligations at tier 3 and tier 1; the follow-up match accepts the effective obligations and refuses the bare class row at tier 3.
- D6. Shell beds that close a MECHANICAL chain on a tier-2/3 goal without a critic are reported, fixed only when one obvious line does it; the seat runs the beds after integration.
- D7. AGENTS.md and wow.md untouched (the word cap); no clock, sleep or silence window in any test.

## 3. Proof

- internal/dispatch: `TestHazardDutiesGateChainCompletion` (a MECHANICAL chain at tier 3 refused closure without a critic and closed with one dispatched at the critique rows; at tier 2 refused; at tier 1 closes; a goal-less record and a record without the goalTier key keep the class floor; a same-class critic at tier 3 refused by name), `TestHazardConfigurationRefusesAWeakenedMinimum` (the tier map tampered or missing refused with the reader's and the resolver's messages), `TestClaimLaunchRecordsEffectiveTierObligations` (tier 3 records the critique duty, tier 1 the floor; builder rows and live proof untouched by the tier), `TestTierThreeMechanicalCompositionCarriesEffectiveFollowUpObligations`, the invalid-tier refusal; `internal/dispatch/provenance_test.go` derives its expectation through the resolver.
- cmd/metasystem: `TestComposeRolePacketCommandCarriesGoalTier`, `TestOrdinaryLaunchCallSitesUsePreparedClaimStateMachineUnderLock` (the three compose calls pass the tier; the source-admission preflight runs before the binding is resolved and the real compose after it).
- Both packages green in full under the race detector; the fast gate green (gofmt, vet, staticcheck, refusal register, build) on the seat, where the network reaches the pinned staticcheck.
- Through the engine on the landed tip 894a269e (generation 66): diagnostic attempt proof-mtyyfody ran section/dispatcher-adapter-and-mission-runner-fixtures and section/land-fixtures, both passed (347 s wall).

## 4. Critique record

Codex ran its own reader during the build (two rounds; round 1 moved the goal-binding resolution earlier in dispatch.sh, which the Opus read then reversed). Round 1 by the lane's critic (Opus, code-critique, record records/misc/critique-always-build-critique-r1.md): two material findings, both folded. F-1: the dispatcher's goal-binding block had been moved 145 lines earlier to feed `--goal-tier` to the validate-only compose, which never reads a tier; the move changed the refusal precedence for live callers (a binding refusal pre-empted the source-admission preflight and the usage checks) and widened the goal-revision window before its lock. Restored to its site; the validate-only call carries the local's zero, and the callsite test now pins the preflight before the binding and the real compose after it. F-2: a MECHANICAL tier-3 chain's critic dispatched at MECHANICAL is refused at close for effort, permanently and without saying why (the critic proves the critique's effort only through its class's builder rows). Folded as the design said: the refusal now names the critic's rows, the required rows and the class to dispatch at, docs/orchestration.md carries the rule in the dispatch section (the sentence Codex had put in the review-round paragraph read as a tautology there, N-1), and a test closes on the named refusal. Non-material folded: N-2 (the builder rows and live proof asserted untouched), N-4 (the absent goalTier key tested), N-6 (the reader's refusal asserted by message). Recorded, not changed: N-3 (a dead branch), N-5 (test style), N-7/N-8 (the validate-only path and its flag bound), N-9 (a goal-bound record always carries a tier 1 to 3, so the gate cannot be bypassed by omission), N-10 (a tier raised mid-chain does not govern the open chain: the root's tier is frozen at admission), N-11 (a MECHANICAL chain has no landing lane through land.sh --chain today; the duty bites at close), N-13 (the recipe digest changes with the table, nothing pins it). Residual for a later goal: a critic role's effort could follow the required critique rows by construction instead of by the dispatcher's class choice.
