# Final Opus code review dispositions

Goal: host-runtime-setup. Reviewer: host-setup-opus-final, actual round1 and
third overall code review. Exact subject: host-setup-recover1-r5,
project tree6855026caf1045e76ed7e0384d96c05855e240ec.

The review returned one material finding. It is accepted and remains open;
there is no certification, waiver, fourth review, or further authorized job.
The prior two Fable reviews and canonical register remain unchanged.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| HOST-C1-001 | noted | Independently verified repaired on the final source: main's complete Claude bed passes; both corrected broader supervision scenarios pass; the only remaining scenario also fails on published main. The five added fixture lines preserve every prior assertion and product guard. Prior acceptance of the historical defect remains truthful. | Typed R3 and R5 corrections retained; recovery-r5-main-results.json and recovery-r5-native-comparison.json |
| HOST-C1-007 | noted | The old check function has no production caller; the declaration field still has a live self-check command. This optional cleanup changes neither behavior nor proof. | none |
| HOST-C1-008 | noted | The degraded receipt-launcher diagnostic remains outside the brief's normal receipt/Stop guarantee. The observation is non-material. | none |
| HOST-C3-001 | accepted | Main reproduced both runtime setup and its check exiting1 for a complete minimal installation shape below an application Git root, using the exact final built engine. The error says the repository has neither a nested template nor an adopted root installation. Read adopt.sh: payload/genesis/project-rule writes precede this newly introduced call, and its existing guard code explicitly supports nested targets. The validator's adopted mode makes the same call. Although the new design names only root and nested-template layouts, the changed callers accept this existing input; the introduced failure after writes is not dismissed as out-of-scope. Full partial-adoption sequence is source-derived, not claimed as an executed adoption. | Required correction at layout resolution and/or adoption preflight; no source edit or additional job is authorized. Evidence: artifacts/host-runtime-setup/final-nested-adoption-proof.json |

The prior five withdrawn findings HOST-C1-002..006 stay withdrawn; no new
contradicting evidence was returned. The critic's unrelated incomplete summary
of historical adoption failures is not promoted as authoritative: the full
primary log and coordinator correction remain the complete earlier record.

The final review's material finding is classified unproven and cannot close
through an out-of-scope disposition or prose refutation. The old design
register remains independently open. Full-suite reds and unexecuted adoption
proof also remain. No source is landed and no generated setup is installed
on main. The two extra jobs approved under R-84-m1c are both complete.
