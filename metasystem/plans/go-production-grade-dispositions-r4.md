# go-production-grade — critique round 4 dispositions (focused follow-up, fresh budget round 1 of 3)

Critic: Codex (same thread), 2026-08-11. This round is the exhaustion-rule follow-up enumerating R3-F1..F7. Critic's verdict: R3-F1..F5 and R3-F7 RESOLVED; 1 material finding (R4-F1, the unresolved remainder of R3-F6); 3 non-material staleness spots. All dispositioned below; 0 refuted.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| R4-F1 | accept | Correct join: the r3 disposition claimed exit codes, event emission, and readback/retry were specified, but the plan text only promised the record-verb contract would "state" them — the choice itself was still delegated to the implementer, on the one surface where a wrong guess manufactures state/event divergence. | The B5 outcome model is now decided *in the plan* (Phase 4): pre-publication failures keep today's semantics; committed-but-durability-unknown retries the sync once, then stays committed and reports success with a stderr durability warning — never converting a committed transition into a reported failure; uniform across the six inventoried writers with recorded-exception escape; per-writer fault-injection tests assert outcome and visible state. Reserved decision 4 approves this table, so implementation follows a decided rule, not a judgment call. |
| R4-F2 (staleness sweep) | accept (record) | Non-material: the verdict synopsis still said "two phases" behavior-changing after Phase 0a became the third; the standing cross-build and verification commands were amd64-only after R3-F7's matrix widened; one "reserved decision 7" reference survived the renumbering. | All three corrected: synopsis says three steps (0a, 1, 4); the gate guard and verification commands build both amd64 and arm64; the supported-platforms reference points at decision 8. |
