Working Mode: implement
Orchestrator Identity: m1b (lineage main-1788680071-18713-e76d5d, dispatch delegate under goal enroll-terminal-refuses-macos-terminal)
Date: 2026-09-06

# Review brief: fresh independent critique of the final tree (chain enroll-login-build1, work round enroll-login-build1-r2)

FINDING IDS: chain-unique, ELF-01, ELF-02, ... never F-n.

Why this review exists: chain completion under DESIGN-BEARING reach
requires a fresh-context critic chain whose reviewed job is the final
work round. Round 1 of the earlier critic chain (enroll-login-crit1)
found one material item, ELN-01, which the correction round
enroll-login-build1-r2 folded; that critic chain's own follow-up round
resolves its register. This chain is the independent examination of
the FINAL tree, in a fresh session, and closes the build chain.

Round budget: 1 focused round. R-60-m1's rule: a finding is material
only if it changes what gets built and names the artifact it would
change.

Threat model: as in
metasystem/plans/enroll-terminal-login-node-code-critique-brief.md (the
walk admitting a process an agent controls or one that is not the
operating system's login program; the widened darwin readers changing
what other ancestry walks see; the proof record; the linux build; the
refusal text; weakened tests; the fake reader's owner default), plus
the corrected macOS command test proving a vacuous precondition. Out of
scope: an agent that starts its own session and enrolls from it (the
build brief records this as a boundary the goal neither opens nor
closes); taste.

Scope: the whole computed diff of the chain at its final work round.
The orchestrator persisted the diff and the reviewed tree hash at
metasystem/artifacts/agents/enroll-login-build1/rounds/2/review.json
(the diff sits beside it); take reviewedTree from that record.
Contract: metasystem/plans/enroll-terminal-login-node-build-brief.md
(decisions D1 to D7 and the measured facts), the correction decision
D8 in metasystem/plans/enroll-terminal-login-node-fold2-brief.md, and
the goal record
metasystem/plans/goals/enroll-terminal-refuses-macos-terminal.md.

Facts the orchestrator established (evidence level: ran, on this Mac,
from a Terminal.app shell, on the round-2 tree): the identity,
human-authority and whole command packages pass; the callers of the
widened parent reader (packages gaterun, validate, lease) pass; the
linux arm64 cross-build and vet pass; the live identity test that walks
to a real root-owned login with withheld arguments ran and passed
without skipping; gofmt prints nothing. Round 1's dispositions are in
metasystem/plans/dispositions/enroll-login-code-critique-r1.md.

# Mandate

1. The rule admits exactly the setuid-root system login program with
   withheld arguments, on both reads, and nothing else: trace every
   path through stableRead and name the outcome each fact combination
   produces; compare with D1 and D2.
2. The darwin readers answer for a root-owned process without changing
   what they answer for user-owned and dead processes: read the sysctl
   and proc_info code against the struct layouts the existing readers
   document.
3. The pins of D6 exist and exercise the shapes they claim; the
   corrected macOS command test still launches the copied test binary
   by a relative path, still asserts both capability checks, and its
   precondition is a real assertion about the reader.
4. The refusal text names the process, the reason and the tmux shape,
   still carries the outcome code, and every stated reason is true for
   the branch that emits it.
5. Nothing outside the declared boundary changed; no test weakened.

If nothing material remains, say so; that closes the chain and the fix
lands.

# Constraints

Wall-clock budget: 40 minutes. Return per the code-critic schema with
the reviewedTree from the persisted round-2 review record. Gap rule:
stop and report a gap; never fill it silently.
