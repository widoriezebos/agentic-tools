Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal supervision-hook-wrong-root)
Date: 2026-09-02

# Goal

Revision 4, two-finding fold of
metasystem/plans/supervision-hook-root-design.md (register:
metasystem/records/misc/hook-root-critique-r3.md, landed, in your worktree).

# The folds, by id

- SHR-R3-ENGINE-INSTALLATION-PAIR-01: the validation must bind the engine
  answer to the SAME installation every shell-owned consumer then uses —
  under a METASYSTEM_BIN override included; state the pairing rule the
  critic's evidence demands.
- SHR-R3-GIT-STEERING-01: the worktree mapper clears or ignores inherited
  git steering environment variables (GIT_DIR, GIT_WORK_TREE, and kin) so
  an engine-less delegate worktree cannot be falsely classified; state the
  exact sanitation.

Consistency pass; self-grade; reject condition restated.

# Constraints

Wall-clock budget: 15 minutes. The two folds only.

# Expected Return

Version-2 implementer JSON; diffBoundary exactly
metasystem/plans/supervision-hook-root-design.md (that one file).

# Gap Rule

stop and report a gap; never fill it silently.
