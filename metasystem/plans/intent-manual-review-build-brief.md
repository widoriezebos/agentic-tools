Working Mode: implementation
Orchestrator Identity: root Codex, main-1790272787-5030-13dde3
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| combined-intent-workflows-planning-manual | 14000 |

Root-authored manual-review design is accepted after Fable rounds3->2, final
bounded fixture exit. Read designs/intent-manual-review.md, both reports and
intent-manual-review-dispositions.md in full. Parent IW and planning IP remain
required; this is an additional observed capability gap, not a substitute for
unfinished obligations. No third design round. Independent Sol review remains
mandatory, no product certification from acceptance. User explicitly permits
machinery bypass; total estimate includes all current product/tests, task-only
size override, no changed committed gates or dependencies.

Implement first the diagnostic review slice, then authentic manual goal submission
and delivery using the exact accepted smaller owners. Goal completion requires both.
Do not implement a new manual registry/counter, CommitPatch shortcut, general
workflow scheduler/cache/importer, fake UnitRunner result or fabricated attestation.
Existing namespace follows intent; all public subjects and refs work from cwd.

Product boundaries extend accepted files with focused internal/launch read sequence
extraction/retained request and tests; existing internal/gittree snapshot/scratch
helpers only as needed; internal/goal/branch range/read/commit helper owner and
cmd/metasystem goal commit wrapper, manual view/admission adapters and tests;
relevant help/public docs/skills and testing.json. Root owns plans/AGENTS/rulings/
receipts. No commits, pulls, pushes, real services/paid test providers, conf.local.

Required specifics, not optional follow-up suggestions:
- review changes --brief FILE [--goal G]; review diff PATCH --brief FILE [--goal G].
  Independent diagnostic feedback, NEVER certification. Existing roster/admission
  and read partition/compaction/cap owners, no paid builder. Freeze inputs/runtime
  before spawn. Public REF supports show/wait/stop review; stop cancels the sequence
  and every owned live child, so a stopped request cannot spawn a next partition.
  Retain outputs before disposable context cleanup. Goal association uses actual
  existing admission, does not invent approval or weaken owner refusals.
- Full-checkout snapshot from Git top-level, complete unignored tracked/untracked
  changes, even if invoking nested metasystem/. Original HEAD/index/files unchanged.
  Private context excludes ignored source/conf.local, not a sandbox claim about
  shared Git config. Fake reader that writes cwd cannot change original files.
- Identical diagnostic request rejoins including failed/finished; --retry N only
  admits once proved stopped, using accepted atomic startup recovery. Do not use
  terminal status as proof or buy extra read rounds by changing a wrapper id.
- Existing builds and standalone reads share partition policy. Preserve their
  existing tests and counting/compaction behavior, including larger package/wide
  inputs without mode flags. A partial read never yields a complete/clean answer.
- review G --changes|--patch PATCH --brief FILE [--work NAME]; manual correction
  --after COMMIT from public status/review, unique prefixes allowed. No fake numeric
  manual attempt ordinal. review goal G is unambiguous for EVERY valid goal name;
  reserve subject words and generate collision-safe continuations for existing
  ids design/job/unit/commit/changes/diff/goal. Goal form takes same goal flags.
- Manual work is authoritative existing branch range, never UnitRunner spoofing.
  Merge views coherently so status/review/land see real manual work, coexistence and
  explicit correction of existing work; stale build bookkeeping cannot overshadow
  a newer committed subject. Ambiguity names work; no silent replacement.
- Freeze patch+brief, lawful claim and goal worktree preparation. Establish holder
  BEFORE bounded checkout-mutation lock, recheck inside, mint identity token inside.
  The old commit token was NOT a lock. Shared goal commit wrapper protects raw and
  public commits; no nested lock acquisition and release before push/review/wait.
  The exact owner lock/refuse behavior must be tested, not an in-process mutex only.
- DISTINCT destination: clean or exactly same captured candidate; stage via plain
  git apply --index --binary, NO --3way. Reject conflict preserving index/files.
  Source=destination: captured edits already exist, so checked stage captured paths
  rather than double-apply. CommitStaged unchanged consumes consistent staged paths,
  binary patch and index; preserves path/range/claim/divergence/installation guards.
- Replay from real range and unit name BEFORE staging/empty-workspace inference;
  compare target unit's parent/tree, not whole branch including other units/reads.
  Explicit --after binds old actual commit; exact already-amended outcome rejoins,
  mismatch/stale/unavailable base refuses. Compare with commit owner's scratch
  three-way application semantics for adoption, without creating a commit or
  source mutation. Branch CAS/range checks still decisive. Push's existing journal
  handles lost responses; CommitRequest.OpID alone is NOT commit idempotence.
- Actual committed independent examination + bound decisions + whole close +
  collect + publish + land. No preliminary diagnostic feedback substituted for
  required review. Reuse reviewCommit lifecycle rather than duplicate certification.
  Amendment preserves later work and removes only obsolete target attestation.

All IM1..4 require concrete implementation and proof. Named final fixtures include
TestManualCaptureFromNestedCheckout, TestManualStageRefusalPreservesCheckout,
TestManualSubmissionReplayAndAmend (interleaved repeated/different submissions,
no lost index/input/token, stale/amended/adopted replay, lost commit/push response,
other work preserved); plus TestIntentManualDiffReview, TestStandaloneReadPartition,
TestStandaloneReadReplayAndRecovery and TestIntentManualWorkDelivery. Meaningful
existing tests can prove multiple rows; no empty test-name wrappers. Preserve
coverage floors, fake Git for behavior fixtures and only narrowly named actual
Git adapters where their interaction is the claim.

Run focused changed-owner tests first, then an executable isolated public journey
with fake providers and real custody/capture/commit/review owners. Retain actual
commands/exits and distinguish mocked delivery from actual transaction proof.
Do not run repeated broad suites while parent/planning/manual still change. Use
METASYSTEM_TESTING_WORKERS=9 and preserve inherited other worker limits/caches.
Root runs final exact-candidate selected acceptance and independent Sol critique.

Each continuation stops at a recoverable coherent checkpoint at150 NEW tools or
35 actual minutes, honest metrics and precise remaining work. Never claim full
completion with partial IW/IP/IM critical/high rows. Root will resume same context.
