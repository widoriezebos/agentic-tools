# Shared proof admission and finite delivery

Status: independent design critique completed at 96248b74723851ef2d11f22003fff28324578f8e with zero material findings in round three. The final empty findings-to-dispositions join passes. Native register closure remains incomplete: the critic explicitly resolved prior findings in evidence but omitted the same-id material:false rows the register requires. No findings, native records or risk decisions have been rewritten to hide that mismatch. Implementation proceeds through normal admission against the actually reviewed contract; the retained register issue needs a lawful resolution before claiming the entire workflow closed. The isolated witness correction and shared implementation still require independent code critique.
Goal: coordinator-loop-prevention. Author: main Codex/Astra, xhigh.
Source baseline: stop/status/arm source a3131fbb, integrated through origin/main 4b4d4ab5. The progress record carries the source integration and focused canaries. This revision replaces the earlier proposal's obsolete assignment of proof process lifecycle to run.
Review budget: three rounds maximum, failsafe round 3; stop at the first round with zero material findings. Fixture-expressible findings may move to named implementation obligations only under the existing critique skill's rules. No fresh critic root resets that budget.

## Required outcome

Every supported agent runtime uses the same decision before MetaSystem starts an expensive proof. A running or completed proof cannot become a new unaccounted attempt merely because the coordinator changes runtime, run name, log path, or scratch directory. Successful coverage is consumed by normal landing when its relevant inputs still match. A real gate failure propagates without an implicit full-gate rerun.

The current production consumers are the validation, adoption, Go gate and commit coverage entrypoints. Direct arbitrary shell commands outside these entrypoints are not claimed to be governed. Mission stop-loss, existing delegate admission, goal approval and the human's right to authorize further work remain intact.

## Grounded diagnosis

The accompanying investigation records exact source anchors and independent traces. In particular:

- `proofrun.LaunchSuite` owns actual suite child creation, a checkout stop-fence creation claim, a durable launcher/suite/watchdog record, and a second fence read. It still has no accepted-goal reservation or durable cross-run proof identity. `internal/proofrun/record.go` stores one overwriteable file per suite with running/done status; done precedes final watchdog and evidence judgment and is not success.
- `dispatch.EvaluateGoalRevisionAdmission` already judges the accepted goal, its stop fence, elapsed limit, attempts, reserved minutes and concurrency without requiring a governed obligation.
- `internal/stoptransition/families.go` already inventories and stops proofrun's process records. Monitored run wrappers are a separate family and already have a suite join; an additional wrapper is unnecessary. A goal-bound ordinary run becomes BUDGET_UNKNOWN in `dispatch.ProjectBudget`, so setting GoalId alone is not a budget reservation.
- A live stalled main produces steward notification; ordinary investigation stop-loss is explicitly invoked rather than automatically applied at proof admission.
- `witness-gate.sh` conflates a failed executed gate with an unavailable witness and can launch the plain full gate again.
- Commit coverage always reruns touched packages. Landing receipts do not carry coverage measurements. The existing landing `fullBatteryCommand` begins with `--fast` and is not evidence of full coverage.

## Ownership and scope

Extend the existing `proofrun` record owner with retained admission/outcome facts and the existing `dispatch` admission/projection owners to count them. Keep proofrun's launcher, watchdog and stop integration as the sole proof process lifecycle. Extend the existing `landing` receipt owner for reusable coverage evidence. Keep coverage judgment in `audit.ParseCoverage` and `audit.CheckCoverage`. Shell changes only pass arguments, environment/context and exit status to these Go owners.

Do not introduce a second budget counter, a new custodian service, a new provider-specific stopping policy, a generic workflow language, or a cache that pretends an old full suite just ran. Do not repurpose an obligation revision of zero as an accepted governed obligation.

## Ordinary proof reservation

Add retained attempt records under the proofrun owner's artifacts/agents/proof-runs/attempts directory, keyed by a unique attempt id. The existing stoppable process record links to its attempt; it is not a second process manager. An attempt records schema version, id, goal id, accepted claim/accounting revision, nullable budget epoch, reserved minutes, observed minutes, start/deadline/end, proof scope and input identity, control and execution roots, authenticated launcher identity, terminal result, previous attempt and retry evidence when present. Reserve before either child starts. Failed starts, killed launchers and unknown outcomes retain their reservation. A missing terminal result is never success.

Give admitted proof process records a unique process key containing their attempt id and a per-launch id, preserving Suite as the logical progress/selector name. Nested launches may share one attempt but never overwrite each other's process records. Inventory must discover every concurrent admitted proof rather than overwrite one suite-named file. Read existing records through their legacy suite key when no attempt key is present; do not reinterpret historical running/done records as successful attempts or import old proof as authority. Stop transitions continue to consume the same process identities through proofrun.ReadRecords and proofrun.Stop.

The canonical control/accounting root is separate from the scratch execution root. It owns the checkout stop fence, creation claims, process inventory, goal lock and attempt records; only command execution and source/evidence input discovery use the execution root. This preserves the ability of canonical metasystem stop to find the proof. Admission order is caller classification, accepted goal binding, goalrevision.Acquire, then the proof owner's mutation lock, in-lock caller/epoch recheck, EvaluateGoalRevisionAdmission, repeat decision and durable reservation. The goal lock serializes this with delegate/governed reservations. Release admission locks before starting or waiting for children. Preserve the new stop-fence creation claim and its second read around child creation; its generation is distinct from the goal budget epoch.

Charge each new top-level proof through the existing ProjectBudget: one attempt, its positive reserved duration, and one active execution while live. Preserve the projection's Claimed.AccountingRevision and nullable budget-epoch rules so later claims do not erase accountable attempts. Use the existing rounding convention for observed duration. Retain ordinary proof history while the accepted goal accounting epoch can consume it. Source changes, new ids and new scratch directories never reset accounting. Malformed or missing required accounting facts produce BUDGET_UNKNOWN, never a free attempt.

Use dispatch.cap-min as the default positive reservation and dispatch.cap-max as its ceiling, with the accepted goal limit authoritative. Add the resulting absolute deadline to the existing sibling watchdog; expiry uses its identity-checked stop ladder and retains failed/unknown evidence. A caller may request a smaller positive bound, never more than the configured or accepted remaining limit. Finalize successful attempt outcome only after child exit, watchdog exit, banner/section checks and relevant-input parity all succeed. Keep the existing done-file notification early so the watchdog can exit, but move the process record's markDone after final judgment: the live finalizing launcher remains in ordinary stop inventory. No new process state is needed.

Terminal success is serialized with checkout stop/arm by stopfence.Acquire, then the goal-revision lock, then the proof mutation lock. Recheck both fences, the admitted generations, deadline and input parity before writing success; record done only after the durable terminal outcome. Admission never acquires the checkout transition lock while holding either later lock. A stop that wins this ordering prevents success; a finalization that wins has completed before stop begins. A failure to acquire or read these authorities is failed/unknown, never success. Inventory also retains any live recorded identities for nonterminal attempts, including a reserved launcher whose process-set publication has not yet occurred. Stop uses the same identity-safe proofrun stop functions for that launcher.

Goal breach-stop joins these attempts through the existing internal/dispatch/stop.go owner. Extend internal/goal/stop.go's StopBatch with optional observedProofAttempts and pendingProofAttempts arrays of typed members, preserving old job-only records. Each member binds attempt id, process keys, goal/accounting revision, machine and claim epoch to the existing stop id/fence epoch/capability generation, and retains cancellation/join evidence. ReconcileStopBatch inventories every accountable nonterminal attempt for the stopped goal. Extend the existing dispatch command and scripts/agents/dispatch.sh batch driver to recheck each member's custody, persist cancellation intent, ask proofrun to stop its recorded process sets, and join only identity-proven termination plus a durable cancelled/failed/unknown terminal result. Preserve foreign-custody report-only behavior; never invoke checkout-wide stop for a goal stop. Unknown liveness, malformed records or an unjoined creation remain indeterminate/pending; zero delegate jobs alone cannot mean complete. Stop completion and goal resume require both member families to be joined. A reservation cancelled before child creation cannot subsequently launch. Cancellation runs after the goal lock is released, just as job cancellation does today; no signal or wait occurs under admission locks.

The command layer wires admission and outcome callbacks, avoiding a proofrun-to-dispatch dependency cycle. An ordinary proof does not require a separate human-approved recurring obligation. Existing governed runs retain their contract. If a verified governed run wrapper already reserves this exact execution, link to that reservation and do not count the same execution twice; the proof outcome still belongs to proofrun. The retained attempt has an optional reservationOwner containing control root, run id, run generation, launch nonce, goal revision, obligation revision, attempt ordinal and nullable budget epoch copied from the authenticated run record. cmd/metasystem/run.go exports METASYSTEM_PROOF_RUN_ROOT and METASYSTEM_PROOF_RUN_ID after Bind and before starting its workload. The command layer authenticates these locators using the live wrapper identity and caller ancestry, then the exact stored Governed binding. A locator without that binding refuses. The linked proof's deadline is no later than the existing reservation's deadline, and ProjectBudget charges that durable owner once while retaining the proof outcome separately. After process exit, the exact persisted link is joined to the existing terminal governed owner; missing or contradictory history is BUDGET_UNKNOWN. No obligation revision of zero is invented.

## Shared launch and nested proof context

Apply the reservation path at the shared Go launch owner used by `validate-metasystem.sh` and `adopt-fixtures.sh`; route standalone full `go-gate.sh` and commit coverage through that same owner. Fast static checks and focused test commands remain the small checks they currently are.

Add --control-root, --goal and an optional positive --cap-min at the proof command boundary. Propagate METASYSTEM_PROOF_CONTROL_ROOT and METASYSTEM_PROOF_ATTEMPT to real children; the proof owner authenticates these locators against the live recorded launcher identity and actual caller ancestry. Publish that exact launcher identity with the reservation before spawning the first child, so an immediately starting child does not race process-set publication. The tuple is a locator, never authority by itself. Nested fixtures and snapshot gates stay inside that reservation and deadline, including across execution roots. Missing, dead, forged, cross-goal or mismatched context refuses rather than silently making work free. Existing MS_SUITE_PROGRESS_ACTIVE strings alone do not prove parent custody.

Resolve control root and goal from authenticated parent context first, then an authenticated native delegate locator where present, otherwise explicit options or the installation's active caller/claimed goal. The existing delegate variables are METASYSTEM_HOOK_DELEGATE_STATE_ROOT, METASYSTEM_HOOK_DELEGATE_INSTALLATION_ROOT and METASYSTEM_HOOK_DELEGATE_JOB; lease.HookDelegate authenticates them. An ambiguous scratch root requires explicit configuration/input. Do not infer shared custody from Git directory names or a provider's name. Existing delegate verification capability limits remain: the main owns the full suite outside delegate sandboxes.

Standalone coordinator calls must resolve an accepted claimed goal before expensive proof launch. An ambiguous accounting root or goal refuses with the exact required input. Existing explicitly goal-free human/bootstrap and exact fake-runtime fixture paths keep their supported behavior; an active coordinator may not declare its claimed goal's work free by changing the scratch root. Verify this boundary using the existing lease/caller and delegate-custody owners rather than creating another runtime detector.

## Identity and repeat decisions

A proof identity contains its declared scope/command class, complete relevant source manifest including modes, effective proof configuration, selected section inventory, platform and toolchain identity. Reuse the existing manifest framing and behavior-surface policy. ENGINE equality may authorize coverage reuse only; it never stands for equality of an entire validation/adoption suite. Full-scope identity must include the complete non-runtime source/input inventory, not just ENGINE. Runtime name, process/run ids, log names and scratch locations are not distinguishing inputs.

Under the proof-owner mutation lock, inspect retained attempts for the same owning goal and proof identity before reserving:

1. A live attempt refuses a duplicate launch and identifies that run for observation.
2. A successful attempt refuses a duplicate expensive launch and identifies its evidence for the delivery consumer. This is not a forged new success result or a new receipt.
3. A failed/unknown attempt requires an explicit retry decision. The decision names the prior run, the diagnosed cause, a concrete evidence artifact and why another attempt may succeed. The engine records the evidence digest and charges the new attempt to the same goal budget. Missing diagnosis or the words "technical issue" alone do not satisfy the decision contract.
4. Machinery may make at most one automatic retry for a typed transient failure before the proof body starts, such as a temporary process-resource error. It must record that classification and the failed attempt. A command's generic nonzero exit, test assertion failure, missing executable/configuration or unknown outcome is not automatically classified as transient.
5. Changed relevant inputs permit a new attempt within the same goal budget and retain the earlier outcome. General repeated-measurement workflows are outside these delivery entrypoints; do not add a new workflow language or free measurement exception.

Retry evidence is accountable coordinator judgment, not a claim that Go can infer the semantics of arbitrary failure logs. Hard budget admission and retained history are mandatory even when the retry explanation is supplied. The independent critic must reject any route where a new root/id/runtime erases prior attempts.

The CLI accepts --retry-decision PATH with strict version-1 JSON: schemaVersion, priorAttempt, cause, evidencePath and rationale. Unknown fields, missing/nonempty-text failures, an unreadable/non-regular evidence artifact, or a prior attempt outside the same goal/input history refuse. Resolve evidencePath relative to the decision file; persist its bytes' SHA-256 and the explanation in the new attempt. The engine validates accountable structure, not whether prose proves a diagnosis. Automatic retry uses the same retained decision shape with a typed pre-body failure and an automatic flag, and can occur once in that unchanged-input chain.

Every launch decision emits a PROOF-RESULT JSON line on stderr with schemaVersion, disposition, attemptId, priorAttemptId when applicable, evidencePath and exitStatus; --result PATH additionally writes the same object atomically for structured consumers. Newly executed success returns 0 with disposition executed; an executed child failure preserves its own exit and uses disposition failed. No-child decisions return 75 for live-duplicate, 76 for reusable-success, 77 for retry-required, and 78 for admission-refused; malformed CLI input remains 2. The disposition distinguishes a child that itself exits with one of those numbers. Shell consumers propagate these refusals without fallback, polling loops or converting 76 to a new green result. Landing may consume the named existing successful receipt after its ordinary binding checks; it never publishes a newly executed receipt from a no-child outcome.

## Failure propagation

In the clean witness path, distinguish inability to prepare/use the optimization before execution from the exit of an executed full gate. Once the full gate actually starts, preserve and return its nonzero exit. Do not launch the plain full gate after a failed executed gate. A successful gate that cannot publish a valid witness is an evidence failure, not authority to rerun automatically. Existing setup-only fallback remains available only before expensive execution has begun.

This change does not revive a dead controller witness, extend its lifetime by assertion, accept seed/fast results as full proof, or weaken any assertion.

## Coverage handoff and landing

Extend the existing landing receipt with a versioned coverage component written by the actual full gate producer after successful measured coverage, complete inventory judgment and unchanged relevant input checks. Bind it to ENGINE bytes and modes, behavior policy version, platform, complete Go toolchain identity and the exact selected coverage ratchet bytes. Preserve the actual package measurements and the producer's command class. Fast, seed, interrupted, incomplete, failed or mutated runs cannot publish reusable coverage authority.

The existing landing test-receipt command owns the outer reservation for a canonical complete validator. Its existing --root remains the receipt/control root; add --goal and --cap-min, with the same caller resolution and retry/result options as proof launch. CreateTestReceipt receives admitted-execution wiring from cmd/metasystem/landing_verbs.go that uses proofrun's existing launcher, not a second lifecycle. Detached-worktree preparation stays in landing. That launcher runs the original literal command in the detached execution root and supplies the authenticated parent locator. Child success is provisional until landing's completion callback has checked the candidate and cleaned up the detached tree; only then can the launcher commit terminal success. The nested validator joins that reservation; it does not charge a second attempt. The attempt's single absolute deadline replaces the independent 40-minute bound on this admitted path, including nested children and cleanup; legacy non-admitted receipt commands retain their existing bound. The receipt persists the control root, goal, accounting revision, attempt id and deadline in an optional versioned proof binding. Its command field remains the literal canonical validator command.

Use one pendingCoverage field on that retained attempt as the durable producer channel. It lives at the canonical root, outside every detached or witness snapshot. At the beginning of the actual unseeded full gate, a Go coverage-begin handler authenticates parent custody and atomically claims the empty producer slot for the current gate process's exact identity and expected ENGINE/policy/toolchain/platform/ratchet inputs. The canonical validator's outer full gate runs before its nested fixtures; only this first authorized producer can claim the slot. Nested gates cannot replace it. A stale ambient flag cannot claim a slot without live parent ancestry and matching expected source identity. A standalone admitted full gate may own its own slot. Fast and seed paths never claim one.

After all gate stages, tests and inventory/floor judgment pass, and before shell cleanup deletes the coverage log or package list, a Go coverage-complete handler authenticates the same live producer and parent attempt, checks before/after relevant inputs, calls audit.ParseCoverage and audit.CheckCoverage on the actual log and independent package inventory, and fills that pending field. Its version-1 payload contains producer identity/class, complete package inventory and measurements, ENGINE manifest/digest with modes, policy version, platform, full toolchain identity, ratchet digest, and the producing attempt id. Shell only passes the two input paths and context. Incomplete/error records may remain diagnostic but cannot become coverage authority. No witness file is used as a substitute for measurements or revived after its controller exits.

The receipt transaction has one durable commit point: the retained attempt's atomic terminal-success write includes the complete exact receipt payload as deliveryReceipt. Before that write, child/watchdog/selector proof is checked, then landing's completion callback checks both candidate projections, embeds the measured coverage component in the proposed receipt, and successfully closes the detached worktree. These preparation and cleanup steps run outside admission/finalization locks and within the admitted deadline. The source parity observation is captured before its checked execution tree is removed; a deleted scratch directory is not mistaken for a source mutation after lawful cleanup. The existing terminal ordering then rechecks deadline and stop/goal authority under transition, goal and proof locks and writes success together with that prepared immutable receipt payload. A failed or interrupted check/cleanup leaves no terminal success and no reusable receipt; retain failed/unknown evidence and the explicit diagnosed-retry path. No callback returns authoritative success before these steps.

After the atomic attempt commit, landing writes the existing canonical TestReceiptPath projection from deliveryReceipt. If interruption or a filesystem error prevents that projection, the exact successful receipt remains recoverable in the attempt. The next unchanged receipt request checks ordinary candidate posture and the successful attempt binding, then restores that identical payload and original execution time without starting a child, reserving again or inventing evidence. A malformed/mismatched projection is never trusted; only the exact payload from the successful bound attempt can repair it. Legacy successful attempts lacking deliveryReceipt cannot acquire one by inference. The receipt consumer verifies a versioned proof binding against this terminal attempt and payload, so an orphan projection cannot authorize landing. This is a recoverable projection of the existing proof record, not a second transaction owner.

The producer's measurements therefore survive snapshot deletion, while an inner full gate followed by an outer validator failure never authorizes reuse. Coverage consumers require the successful enclosing binding and recheck the current bound inputs and floors through Go. Repeating a request returns the same valid exact receipt, including recovery of its projection when necessary; it does not invalidate evidence first or stamp a new execution time. No missing receipt can be synthesized from a successful attempt that did not durably commit its exact payload.

The normal coverage boundary consults this component through Go. Matching evidence reuses its measurements and performs zero additional coverage test launches. Absent or mismatched evidence runs the existing affected-package coverage check. Goal and peer-record changes alone may preserve ENGINE equality; they do not invalidate coverage. Source/test/mode, toolchain/platform, policy or floor changes require fresh coverage. Preserve all ordinary index/working-tree, review-chain, static proof and landing checks.

Normal full-width landing currently accepts only the literal fast-plus-two-fixtures command in landing/observe.go and landing/tierone.go. Add acceptance of the real unselected, unseeded canonical scripts/validate-metasystem.sh receipt, produced by CreateTestReceipt and accompanied by complete successful selector/progress evidence. Preserve acceptance of the existing command for compatibility, but never treat it as measured coverage. A selected section or arbitrary command string cannot qualify as the canonical complete validator. This lets the real final full suite reach normal landing without executing the older partial battery again. Extend the existing producer's coverage component through the nested snapshot/witness path; publish usable coverage only after the producing full gate succeeds, and publish the enclosing full receipt only after its complete command succeeds.

Future final verification uses the real receipt producer and its supported bound; do not import the temporary R27 helper JSON as production authority. The existing 40-minute receipt-command limit is below the measured 56-minute full suite. Use the existing configured proof reservation/bound consistently, within the accepted goal budget, rather than introduce another timeout knob.

## Required focused proof

| Obligation id | Severity | Design source | Required behavior | Owner | Code proof | Test proof | Runtime proof | Status | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| LOOP-1 | HIGH | Ordinary proof reservation | Accepted budget has one atomic reservation/projection across ordinary proofs and delegates |dispatch admission/ProjectBudget and proofrun attempts (`metasystem/internal/dispatch/budget.go`) | internal/dispatch/budget.go, internal/proofrun/record.go | `TestOrdinaryProofSharesGoalBudget`: simultaneous proof/delegate reservation, exhaustion, accounting revision and nullable epoch | O/C: TestOrdinaryProofSharesGoalBudget and real governed CLI prove shared atomic reservation, exhaustion and one charge ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-2 | HIGH | Shared launch context | Runtime, worktree, log and run-name changes cannot evade input identity or accounting |cmd proof-run caller wiring and proofrun manifest (`metasystem/cmd/metasystem/proof_run.go`) | cmd/metasystem/proof_run.go, internal/proofrun/manifest.go | `TestProofContextAcrossRuntimesAndRoots`: Claude/Codex/Devin locators and renamed scratch roots | Template configuration regression and effective-identity negatives pass; actual host configuration preflight passes. Corrected root/nested public receipt canary passes; source hashes verified in `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-source-20260909T110725Z/correction-conformance.json` | READY_FOR_RUNTIME | Complete independent review and the final selected main proof |
| LOOP-3 | HIGH | Identity/repeat decisions; CLP-R1-006 | Live/successful duplicates create no child; explicit retry binds prior evidence; shell outcomes distinguish reuse from execution |proofrun admission and cmd launch result (`metasystem/internal/proofrun/record.go`) | internal/proofrun/record.go, cmd/metasystem/proof_run.go | `TestProofRepeatDecisionProtocol`: launch counter, strict retry JSON, all exit/disposition mappings | C/COL and settled public app specimens: real execution, evidence-bound retry, repeat76/77 and zero new child launches ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-4 | HIGH | Failure propagation and bounded transient retry | One automatic pre-body transient retry maximum; real gate failure preserved; fixture modes isolated |proofrun launch and witness shell plumbing (`metasystem/internal/proofrun/launcher.go`) | internal/proofrun/launcher.go, scripts/agents/witness-gate.sh | `TestProofTransientRetryBound` and `TestWitnessGate` selections, including W-1 seed/force environment control | O/C plus settled wrapper/seed canaries: bounded transient retry, exact native23, one launch and empty seed snapshot ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-5 | HIGH | Coverage handoff; CLP-R1-004 | Actual measured full coverage survives snapshot cleanup and is reused only after complete enclosing success |landing receipt coverage and audit (`metasystem/internal/landing/receipt.go`) | internal/landing/receipt.go, scripts/agents/go-gate.sh, scripts/agents/coverage-delta.sh | `TestCoverageReceiptProducerConsumer`: one test executable invocation; mutation/floor/toolchain/platform/policy negatives; inner green then outer red | C/NESTED: actual production coverage consumer reuses enclosing-success measurement; archive/frozen source parity and mutation/floor negatives passed ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-6 | HIGH | Shared context; CLP-R1-005 | Parent custody is authenticated; governed reservation is durably linked and charged once |command-layer caller authentication and dispatch projection (`metasystem/cmd/metasystem/run.go`) | cmd/metasystem/run.go, cmd/metasystem/proof_run.go, internal/proofrun/record.go | `TestProofParentCustody` and `TestProofGovernedReservationJoin`: forged/stale/cross-goal locators, exact terminal owner join | O/C/F1: real authenticated parent/joined helpers retain one charge and reject stale/forged locators and parity changes ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-7 | CRITICAL | Finalization; CLP-R1-001 | Stop can find every finalizing proof and a winning stop prevents success |proofrun launcher and stoptransition inventory (`metasystem/internal/proofrun/launcher.go`) | internal/proofrun/launcher.go, internal/stoptransition/families.go | `TestProofFinalizationStopRace`: watchdog done barrier, live inventory, transition-lock ordering, input parity | O/B1: real finalization stop race and cancellation-before-child tests pass with no successful stopped attempt ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-8 | HIGH | Goal breach-stop; CLP-R1-002 | Proof-only stop batch stays open until exact cancellation and terminal join |goal StopBatch and dispatch stop driver (`metasystem/internal/goal/stop.go`) | internal/goal/stop.go, internal/dispatch/stop.go, cmd/metasystem/dispatch_verbs.go, scripts/agents/dispatch.sh | `TestProofOnlyGoalStopBatch`: reserved/live/finalizing phases, unrelated goal survives, unknown identity refuses | O/B1: real proof-only goal stop joins reserved/live/finalizing work and preserves the unrelated goal ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-9 | HIGH | Receipt reservation; CLP-R1-003 and CLP-R2-001 | Detached validator shares one reservation/deadline; terminal success includes its recoverable exact receipt |landing receipt and command-layer proof callback (`metasystem/internal/landing/receipt.go`) | internal/landing/receipt.go, internal/proofrun/record.go, cmd/metasystem/landing_verbs.go | `TestCanonicalReceiptProofContext` and `TestReceiptPublicationRecovery`: fault after child success, during cleanup, before terminal commit, and after commit before projection; zero repeat child launches for recovered success | C/NESTED: actual receipt projection fault and exhausted-budget recovery preserve identical committed payload with one child/reservation ; artifact index: `/Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/final-obligation-evidence-map-20260909.md` | DONE | Retain focused evidence and final source conformance |
| LOOP-10 | HIGH | Delivery boundary | Existing review, source parity, floors, stop/arm and normal landing remain enforced |landing and existing verification owners (`metasystem/internal/landing/observe.go`) | internal/landing/observe.go, internal/landing/tierone.go, scripts/agents/commit.sh | Existing focused landing/stop tests plus canonical-command receipt acceptance and selected/seed refusal ; `metasystem/internal/landing/proof_receipt_test.go` | Final selected main proof proof-mtu31dao-c7b6007c884af06e returned 1 in 1064.398 seconds: 28 groups passed, eight failed and four deep groups were blocked. Complete result and failure logs: /Users/wido/metasystem-evidence/agentic-tools/coordinator-loop-prevention/fixture-boundary-failures-20260909T125508Z/. Earlier real public root/nested application landings remain preserved but do not replace this failed main verdict. | CONTRADICTED | Complete the admitted fixture correction batch, prove failed cases, then final selected main receipt and normal publication. |

## Round-one disposition join

The full canonical return is retained at artifacts/agents/coordinator-loop-design-review/rounds/1/return.json. All six findings are accepted because each names a missing interface or failure mapping supported by current source. The design remains proposed pending the same critic chain's next round; this is not fixture-based early closure of invariant findings.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| CLP-R1-001 | accepted | launcher.go marks the process done before final judgment; families.go skips done records. An unlocked last fence read also races stop. | Delay process done, retain inventory, serialize terminal success with the existing transition lock; LOOP-7 |
| CLP-R1-002 | accepted | dispatch/stop.go and goal/stop.go can complete a batch using pendingJobs alone. | Typed proof members, exact cancellation and join in existing driver; LOOP-8 |
| CLP-R1-003 | accepted | receipt.go creates a detached worktree and runs a command under an independent 40-minute bound without accepted custody. | Receipt owns outer reservation, command-layer callback and shared absolute deadline; LOOP-9 |
| CLP-R1-004 | accepted | go-gate.sh deletes actual measurements; the live witness has no measurement field; receipt.go observes only command exit. | Authenticated pendingCoverage field on the existing retained attempt, completed by actual producer and embedded after enclosing success; LOOP-5 |
| CLP-R1-005 | accepted | run.go exports no descendant locator, and the proposed attempt omitted the durable governed reservation link. | Authenticated wrapper locator, exact persisted reservationOwner, sole ProjectBudget charge; LOOP-6 |
| CLP-R1-006 | accepted | proof_run.go exposes no retry input or distinguishable no-child dispositions. | Strict retry-decision JSON and explicit result/exit mappings with shell-visible assertions; LOOP-3 and LOOP-4 |

## Round-two disposition join

The complete canonical return is retained at artifacts/agents/coordinator-loop-design-review/rounds/2/return.json. It explicitly resolves all six round-one findings and reports one material transaction-ordering defect. This is the last design amendment before the declared third/failsafe round; no budget reset or severity downgrade is used.

CLP-R2-001 is accepted. Its mechanically joinable disposition is in coordinator-loop-prevention-design-r2-dispositions.md. The amendment makes checks and cleanup prerequisites to success, atomically commits the exact deliveryReceipt payload with terminal success, and recovers its canonical projection without rerunning. LOOP-9 owns the fault-injection proof.

First reproduce the failed-witness double-launch and duplicate coverage behavior with focused Go-driven fixtures on old source. Do not rerun historical expensive failures. The reviewer may challenge this design's premise or choose deletion/reuse when it names the current requirement that remains satisfied.

## Delivery and stopping rule

Independent design critique precedes shared admission and coverage implementation. The isolated witness correction follows its complete failure-propagation contract without a separate design artifact, as permitted for mechanically small work. Native implementation and a fresh independent code critic follow. Main runs focused canaries while changing. Once the final candidate and review are settled, one final full battery gates normal commit/push in the same failure-stopping chain. Any failure is reported with its specific outcome before another repair cycle is contracted. No automatic new critic root, budget reset, full-suite fallback, or unrelated repair extends this work.

The user-approved shared testing design supersedes the earlier blanket final-validator requirement with risk-selected delivery proof. Original budget, custody, retry, stop and coverage invariants remain. Evidence keys O/C/F1/B1/COL/NESTED refer to the retained external `final-obligation-evidence-map-20260909.md`; final source delivery is separately recorded by LOOP-10 and TEST-21. No historical or controlled-body canary is described as a completed full battery.
