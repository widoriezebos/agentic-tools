Working Mode: build
Orchestrator Identity: m2+main-1788441779-14484-82d6ed (dispatch delegate under goal stop-hook-wedge-on-enrollment-drift)
Date: 2026-09-04

# Answer to the gap on chain shw-build1

approve. Exactly your proposed contract: the repeat rule applies to
every record_stop_failure diagnostic; the record is schemaVersion 1
with sessionId and a causes object keyed by the SHA-256 digest of the
cause, each value holding cause, count, firstAt, lastAt; for arming
failures the remedy is the exact `metasystem up` diagnostic; for every
other fixed diagnostic and for deadline expiry the remedy text is "A
human or steward must restore supervision outside this seat, then
retry."; the deadline parent stages the input payload before launching
its worker and resolves the payload's session and repository through
the installed canonical engine; if the refusal record cannot be read
or atomically updated, surface that failure as a non-blocking
systemMessage so record corruption can never recreate the loop.

Everything else as in metasystem/plans/stop-hook-wedge-build-brief.md.
Build now; the wall-clock budget is 60 minutes from this round's start;
return before it ends even if something is red, naming it.
