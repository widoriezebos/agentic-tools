# time-bound-tests-run-on-artificial-clocks

- Owner: m1e, claimed 2026-09-12; Wido's word R-104-m1e ("all time-bound tests use artificial clocks ... that is the only way forward").
- Goal and current status: inventory first, then conversion in slices, each landed by human commit with the loaded sweep as its proof.
- In flight right now: nothing; slices 1 to 5 landed (1f27c506, staticcheck orphans removed in 56bc8fbb) and the goal concluded on 2026-09-12.
- Decisions made (and who made them): Wido, 2026-09-12: a test that fails under load is a defect of the test, never a reason to widen a floor or retry; the fix is an injected clock and an injected process prober, with a few named end-to-end wiring proofs left.
- Waiting on the human: nothing.
- Dead ends: none yet.
- Next step: settled. The runner-loop beds' real-clock backoff (section 2, missionrunner) is the one follow-up, for the efficiency program, not this goal.

## 1. Inventory (2026-09-12, tip d25c2ee0)

Raw universe, test files under internal and cmd: 113 spawn a process (`exec.Command`), 107 read the wall clock (`time.Now`), 29 sleep, 27 kill through `Process.Kill`, 12 through `syscall.Kill`, 2 read `groupAlive`. The wall-clock readers are mostly stamps and are not the class; the sleepers and killers are the prime suspects because each waits for or asserts a process or timer state.

Sleeping test files (29): cmd/metasystem/identity_probes_test.go, cmd/metasystem/proof_run_test.go, internal/adapter/selftest_test.go, internal/boundedexec/boundedexec_test.go, internal/channel/fake/shutdown_test.go, internal/channel/telegram/telegram_test.go, internal/dispatch/proof_attempt_test.go, internal/dispatch/watch_test.go, internal/goal/attention_test.go, internal/goal/turnverdict_idle_test.go, internal/identity/identity_test.go, internal/identity/survivors_test.go, internal/landing/receipt_test.go, internal/lease/claim_test.go, internal/lease/classify_test.go, internal/lease/refusals_test.go, internal/lease/sweep_test.go, internal/lock/lock_test.go, internal/missionrunner/host_process_test.go, internal/missionrunner/launch_progress_test.go, internal/missionrunner/preflight_fixture_test.go, internal/missionrunner/winddown_test.go, internal/proofrun/test_command_test.go, internal/report/stopblock_test.go, internal/run/run_test.go, internal/steward/ledgerattention_test.go, internal/steward/runner_test.go, internal/supervise/arming_test.go, internal/supervise/watcher_test.go.

Killing test files (36): the sleepers above that spawn, plus cmd/metasystem/goal_test.go, hold_test.go, landing_verbs_test.go, internal/dispatch/ownerlock_direct_test.go, internal/gaterun/fence_test.go, gaterun_test.go, guard_test.go, internal/goal/journal_test.go, recover_test.go, internal/identity/enumerate_darwin_test.go, internal/janitor/killproof_test.go, internal/lease/hook_delegate_test.go, internal/missionrunner/stop_test.go, internal/proofrun/supervisor_darwin_test.go, supervisor_test.go, internal/steward/runner_test.go, internal/stoptransition/proof_attempt_inventory_test.go, internal/supervise/owner_test.go, internal/up/up_test.go.

Seams that already exist: 66 production files carry an injectable clock or prober (`Now func() time.Time`, `Prober identity.Prober`, `Signal func(int, syscall.Signal) error`); the watchdog, the stop ladder, the arming owner and the reconciler landed today are driven that way. The shell fixture beds: 33 scripts wait on `sleep` or a deadline loop; those are wiring proofs by nature and get bounds an order of magnitude above their expected time, not fakes.

## 2. Dispositions

Each file in section 1 gets one of three, recorded here as the slices land:

- converted: the logic under test now takes a clock and a prober (or a signal function) and the test drives them; no wall time passes.
- wiring proof: a real process is required to prove the seam itself (a real fork, a real signal, a real pipe); the test is named here, its bound is at least ten times the expected duration, and it waits for the fact it asserts, never for an instant.
- redundant: the behaviour is proven by a converted test elsewhere; the file's real-process case is deleted.

Wiring proofs wait on `wiringBound` (30 s, declared once per package in its test files): an order of magnitude above what any of their facts needs on a loaded box.

### proofrun (slice 2, 2026-09-12)

- `supervisor_test.go`: TestSupervisorRunawayUnderMeasuredContention deleted as redundant. It compared wall times and CPU shares of real busy trees under real competitors, a claim about the kernel's scheduler, not about the supervisor; the supervisor's runaway verdict keyed on CPU seconds is proven on the scripted tree reader (TestSupervisorKillsSIGQUITIgnoringBusyChildAfterCounterRises), and the real reader's CPU sums by TestSupervisorDescendantAndReapedChildCPUAreCounted, which waits for the CPU it asserts. Its competitor helpers went with it.
- `test_concurrency_test.go`: converted. TestStageRunsIndependentGroupsSideBySide read a stopwatch (three two-second groups under five seconds); now each group announces its start in a rendezvous directory and waits for the other two before it reports, and the progress log must show every start before any end. A stage that ran them one after another fails loudly; a loaded box changes nothing.
- `launcher_test.go`: converted where it slept. TestPreparationCrossingDeadlineCannotCommitSuccess's preparation waits for the deadline to pass instead of 700 ms. The other launches run fixture watchdog scripts, so their Silence and SectionCap are inert; TermGrace and KillGrace of a millisecond only ever reach a process that has already ended.
- `stop_test.go`, `watchdog_test.go`, `reconcile_test.go`: already converted; fake probers and signal functions, the ladders' waits bound facts the fakes settle at once.
- `test_command_test.go`: wiring proof. A hanging version helper under a 50 ms context bound must report the bound; more load only makes the bound surer.
- `supervisor_darwin_test.go`, `supervisor_linux_test.go`: wiring proofs of the platform tree reader against real processes, no clock read.

### supervise, identity, lease, landing (slice 3, 2026-09-12)

Wiring proofs throughout: every spot is a wait for a real-process fact (a readable argv after the exec window, a reap after a kill, a helper's gate, an owner's death) whose bound was two to ten seconds. Each package now declares `wiringBound` (30 s) in `wiringbound_test.go` and every such wait uses it: `supervise/arming_test.go` (the helper's gate wait, the takeover sweep's reap, the requested owner's death, a cleanup), `identity/identity_test.go`, `identity/survivors_test.go`, `lease/claim_test.go`, `lease/classify_test.go`, `lease/sweep_test.go`, `lease/refusals_test.go`. Left as they were: `landing/receipt_test.go` (90 s over a helper doing real git work), `lease/refusals_test.go` TestRecordLockAcquisitionIsBounded (a 0.2 s lock bound must release within 5 s, twenty-five times its expectation), `supervise/watcher_test.go` (a 5 ms census under a 1 ms interval must warn; load only widens the gap), `identity/enumerate_test.go` (our own start time within a day). `janitor` and `census` tests read no clock.

### the rest of the inventory (slice 4, 2026-09-12)

- `internal/channel/telegram/telegram_test.go`: converted. TestReceiveAppliesRequestDeadline read a stopwatch (the deadline had to fire between 0.9 and 2 s); now the fixture poll never answers, so only the request deadline can end the receive and the reported error is the whole proof (the fixture consumes the request body so the server notices the cancellation, and the test releases the handler before the server closes; the first cut blocked the server's close for the harness's ten minutes).
- `internal/report/stopblock_test.go`: converted. The refusal's wait for an overlapping writer (`stopRefusalLockWait`, 100 ms in production) is set by the test: 30 s makes a writer released after 40 ms brief by construction, where 100 ms let a loaded box turn it wedged; zero makes a never-released writer wedged by construction (TestStopRefusalReportsAWedgedWriter).
- `cmd/metasystem/proof_run_test.go`: converted where it slept; the deadline-crossing preparation waits for the deadline to pass instead of 700 ms.
- Wiring proofs moved to `wiringBound` (declared in each package's `wiringbound_test.go`): `cmd/metasystem` (identity_probes_test.go, proof_run_test.go's governed-command parent and ready waits, hold_test.go), `internal/boundedexec` (the spawning script's child and the group's end), `internal/channel/fake` (the base URL, connection states, cancellation), `internal/dispatch` (the stop helper's ready file, the watch's waiter record and return), `internal/run` (the watch's waiter record and return), `internal/adapter` (the selftest listener's port file), `internal/steward` (the loop's evidence stall window and the runner record).
- Outside the section 1 inventory, a module-wide search for stopwatches (`time.Since`) and sub-second bounds found five more tight upper bounds, now on `wiringBound`: `cmd/metasystem/brain_boot_test.go` (a boot with a 250 ms deadline had to return within 2 s; its inputs command now sleeps 120 s, so a boot that waited for it is minutes and one that kept its deadline is well inside the bound), `internal/dispatch/record_bound_test.go` (a wedged record lock's refusal within 5 s), `internal/adapter/selftestrun_test.go` (a one-second turn ceiling within 5 s), `internal/channel/fake/fake_test.go` (a long poll returning within 2 s of its update, a paused request resuming, a malformed control ending a request; the external test package declares its own `wiringBound`), `internal/steward/component_evidence_additional_test.go` (a contended health read's busy error within 1 s).
- Left as they were, none able to go red under load: `internal/acp` (context bounds of 50 to 150 ms whose only assertion is that the bound fired, and a 2 s ceiling on a 150 ms bound), `internal/steward/health_cost_test.go` (a CPU-time ceiling, by Wido's 2026-09-11 word), `internal/dispatch/occupancy_index_test.go` (a 10 s ceiling on an immediate claim), `internal/contract/helpers_bound_test.go` and `internal/dispatch/gitcmd_test.go` (30 s), `internal/boundedexec` TestRunKillsAHangingCommand (a 300 ms bound must release within 10 s), `internal/lock` TestOwnerlessDirectoryIsGarbageAfterWindow (a lower bound: the window must have elapsed), `internal/goal` attention_test.go (a 30 s failsafe) and turnverdict_idle_test.go (a fetch asleep for 10 s must not hold the verdict), `internal/steward/ledgerattention_test.go` (30 s), `internal/dispatch/proof_attempt_test.go`'s helper (asleep until killed). `gaterun`, `goal/journal_test.go`, `goal/recover_test.go`, `stoptransition`, `up`, `janitor` read no clock in their tests.

### the shell beds (slice 5, 2026-09-12)

The contract runs twenty-one scripts; their waits were read one by one. Converted: `scripts/agents/supervision-fixtures.sh`'s monitor leg (S4-16) slept one second before reading a live watch and two and a half seconds before concluding a two-second run, two instants a loaded box overran; it now waits, through the bed's own `wait_until` under the harness cap, for the watch's STILL WORKING reading and for the wrapper's exit sidecar. Bounds raised to 30 s: `scripts/agents/land-fixtures.sh` (the steward runner's death after disarm, the runner record after arm; five seconds before). Wiring proofs left as they were, each a wait for a fact under a cap of the harness or an order of magnitude above the fact: `dispatch-fixtures.sh` (the batched ask within ten seconds, the steward end-to-end under `harness_fixture_cap`, the arming-window retries bounded by three attempts), `suite-progress-fixtures.sh` (fixture suites' own output cadence, the reap under `reap_cap`), `fixture-bed-scenarios-fixtures.sh` (the grandchild pid under `harness_fixture_cap`), `goal-cli-fixtures.sh` (the brain fake within 30 s), `supervision-go-fixtures.sh` (a three-second no-churn window, an assertion that only gets stronger with time), `witness-gate-fixtures.sh` (60 s foreign controllers checked at once). `supervision-fixtures.sh`'s remaining sleeps are a stale-census retry (2 s, fifteen times), an opt-in test pause, a fixture adapter made slow on purpose, and fixture run lifetimes.

### missionrunner (slice 1, 2026-09-12)

- `winddown_test.go`, `host_process_test.go`: converted. The wind-down ladder runs on the `windDown` seam (clock, sleep, group probes, ownership) and `stopSignal`; `winddown_fake_test.go` proves nine verdicts on a fake kernel in no wall time: dead group, unowned group, TERM-honouring, TERM-immune through KILL after at least the grace's 2 s floor, indeterminate then owned, indeterminate throughout, recycled pgid never killed, zombies after KILL, a survivor after KILL. Deleted as redundant: TestTerminateGroup, TestTerminateGroupKillsThroughATermImmuneOwnedGroup, TestTerminateGroupNeverSignalsAForeignGroup. Wiring proof: TestTerminateGroupLeaksNoGroupsUnderCompression (real tagged groups of both signal shapes through the real probes and signals), readiness and death on `wiringBound`; its holders live 600 s, an order of magnitude past the bound, so a group gone within the bound was wound down and never dead of old age, and a wind-down that reports a leak is classified as one before any wait.
- `stop_test.go`: converted. TestStopTurnReportsSurvivorWhenSignalsDoNothing walks the turn's whole TERM/KILL ladder (`terminateGroup`) on the artificial clock against a live group nothing signals; `waitRunnerGone` reads the same clock and is driven by the two runner proofs on the default seam. Wiring proofs: TestStopDeadRunnerReleasesLeaseAndClosesOrphanHost and TestStopLiveRunnerSignalsOwnedGroup (a real group, a real signal); their compressed grace is gone, because a grace bounds the fact and a compressed one turned a slow reap into an escalation to KILL and a red.
- `launch_progress_test.go`: converted. The CPU-progress tracker takes its sampler; TestTreeCPUProgressOnAScriptedSampler drives the first-sample rule, the once-a-second gate, growth and an unreadable tree on scripted readings and artificial times. Wiring proof: TestProcessTreeCPUSecondsCountsDescendants (a real burner under a shell; waits for the growth it asserts on `wiringBound`; the shell's own timer ends the burner should the test die before its cleanup).
- `preflight_fixture_test.go`: converted where it slept. TestLaunchLockSerializesStartDecisions waits for the competitor's arrival at the launch lock (the `launchLockAttempted` hook) instead of 300 ms. Wiring proofs: spawnTaggedHold's argv readiness and the nested-birth teardown sweeps on `wiringBound`; TestNestedCheckoutMissionBirth's terminal wait of 90 s over a nested engine run.
- `host_process_test.go` lifecycle proofs (TestStartProcessLifecycle, TestExitCodeShapes): wiring proofs on `wiringBound`; the not-yet-exited reading moved off the 100 ms child onto TestWaitForBoundsItsWait's long-lived one, which also asserts that a 200 ms bounded wait releases within 5 s.
- Runner-loop beds (`outage_posture_test.go`, `capexhausted_test.go`, `drain_test.go` TestDrainStallEndToEnd, `faulted_test.go`, `preflight_fixture_test.go` TestWallPreflightPreconditions and TestArmAndPreflightFullPass, `birth*_test.go`): wiring proofs by nature. They drive real fake-host processes through the real loop; every window is a ScaledWait that bounds a fact, with a real-fact floor where the event needs one. The loop's own sleeps (the overload backoff in loop.go, the drain poll in drain.go, the handshake poll in host.go) stay on the real clock because they pace real processes; an artificial clock there would run through a deadline before the process it waits for has moved. Their cost (12 to 32 s each at scale 10) is the backoff and the floors, not load sensitivity; shortening them is the efficiency program's, not this goal's.

## 3. Order

missionrunner and proofrun first (seven of the eight defects of 2026-09-12), then supervise, identity, landing, lease, janitor, census; then the cmd tests; then the beds' bounds. Each slice: convert, run the package three times under the race detector beside eight burners on the eighteen-core box, land by human commit with one independent critic (R-98-m1e).

## 4. Critique record

Round 1 (2026-09-12, missionrunner slice, independent code critic): two material findings, both folded. F-1: the wiring proof's holders lived 30 s, the same as the death wait, so a leaked group could pass by dying of old age; holders now live 600 s and a wind-down error is classified before any wait. F-2: TestStartProcessLifecycle read "not exited" off a 100 ms child, an instant; the reading moved to a long-lived child. Non-material notes folded: the stop_test wording, a select on the launch-lock hook so a launcher failing before the lock fails fast, the fake kernel's unused recycled-pgid field. Left: TestWaitForBoundsItsWait's 5 s ceiling on a 200 ms timer, and the CPU wiring proof skipping when the first ps read fails. After the landing the go gate's staticcheck refused the land bed's brain scenarios on the tip (the fake kernel's re-declared seam struct read as unused fields, the supervisor fixture's waitReady as an unused method); the seam is a named type and the orphan is gone (56bc8fbb).
