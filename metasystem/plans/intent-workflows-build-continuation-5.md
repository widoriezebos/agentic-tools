Working Mode: implementation
Orchestrator Identity: root Codex, main-1790272787-5030-13dde3
Date: 2026-09-26

| Unit | Changed lines |
| --- | --- |
| complete-parent-and-planning | 10000 |

Resume the same Opus implementation. Root read full return 4 and its appendix.
Main including new UI sitting work is now merged through ee33be672. Product
stash reapplied cleanly; no product commit exists. Root preserves backup stash.
Parent and planning accepted designs/briefs/fixtures remain mandatory. Manual
review design is still in Fable final critique: DO NOT implement it yet.

Complete unfinished PARENT work first, then accepted PLANNING, without leaving
real authority/owner proof as repeated future notes. This iteration has concrete
existing seams below. Product/tests/docs/skills only; no commits/pull/push, real
services or paid test providers, conf.local, plans/rulings/AGENTS edits. No new
dependencies or changed policy. User machinery bypass covers task size override.
Additional explicit diff boundary: internal/steward health result's typed remedy
facts and their tests, internal/dispatch critique-register fold and its tests,
plus existing accepted product owners. Do not touch unrelated new main UI work.

1. Finish bounded retry. Your run3 found actual register refusal for timeout.
   Fold an admissible capped critic as failed/non-counting with no invented
   findings, through existing register owner, preserving chain cap and death proof.
   Test owner first (timeout/budget-cap/process-loss, failed retry replay/live/cap),
   then actual shell follow-up including returned-child adoption. Do not hide the
   failure by retaining a test that expects the old refusal as final acceptance.
   Run4 was still background-running when you returned; inspect its actual exit,
   and clean up only task-owned fixture processes if necessary. Do not infer pass
   from tail. Avoid running ALL dispatch scenarios again for one branch: existing
   fixture-bed-scenarios diagnostic selection can select named clusters; inspect
   its actual support. Prior runs prove dispatch-a only at their candidate. A
   full green unchanged cluster need not be repeated at each focused correction.
2. Positive auto-claim EXISTS. announceProofFixtureHolder(t, root) in
   cmd/metasystem/proof_run_test.go:2553 probes REAL os.Getppid with KernelProber,
   then lease.AnnounceWithPair(...,"fake","m1") in ONLY synthetic fixture root,
   yielding holder epoch1. Existing positive TestHolderSetBudgetRebindsEpochForProofAdmission
   (goalsync_mutations_test.go:211, helper261) uses it. Use newWorkBed (intent_work_test.go:145)
   with approved/unclaimed initializer; small helper extension is fine. Its real
   UnitRunner and workStarter avoid actual children. bed.lineage="m1". Assert
   classifyVerbCallerWith(root,os.Getppid(),authorityFacts.repositoryTop) MAIN/holder/epoch.
   Set connection.endpoint=dependencies.endpoint; connection.claimCheck can call
   REAL goalBranchClaimCheckWith(root,id,ep, machine func=>"mac-cli", canonicalRoot
   func=>bed.root()). This injects location/config only, retains actual ledger and
   lease. Assert confirmed build, one claim publication/history, mac-cli+m1,
   StopCapability epoch equals holder, repeat no extra claim. Foreign owned goal:
   SAME authenticated fixture, assert GOAL_BRANCH_NOT_HOLDER and exact not-claimed-by
   mac-cli+m1 reason, unchanged ledger and zero starter launches. Existing nonconfirmed
   check allows unrelated setup failures and is insufficient.
3. Positive administration/carry source seams are in implementation-checklist.
   Use existing real-owner positive fixtures with exact public adapter binding.
   Missingconfig/ledger is not authority proof. Complete the actual carried landing
   owner transaction, not only carry refusal plus a fake land.sh callback. You may
   reuse an existing real owner integration scenario by adding the public route;
   do not replace a behavior fixture with a new real-Git setup (local project rule).
4. Real wait owner: goal event (IW-C8), proof/file and question. Public producer
   yields refs; repeat/deadline uses actual wait records, fake only external facts.
5. Retained build/revision request comparison BEFORE multi-work ambiguity;
   collected/published and built-unread selection; old reviewed findings binding
   and newest authentic unresolved review after failed correction. These were
   accepted mandatory fixtures, not optional naming requests.
6. Whole-close refusal must distinguish unclosable findings, actual record-writer
   permission, and I/O failure. Currently closable+any close failure is assumed
   holder/lease; root suspects this misdiagnoses I/O. Prove actual owner outcomes.
7. ROOT FOUND typed check mapping still fails parent contract. publicHealthRemedy
   sends Census/Hook/Ledger back to check (a loop), maps every budget/epoch problem
   to bare status, and fallback instructs check --json for internal ownerRemedy.
   Roles bundle distinct causes (health.go:1095-1210 and1280-1335): missing budget,
   open breach stop, malformed record, same-lineage epoch mismatch, foreign lineage.
   Carry bounded typed remedy facts at the health producer (goal/id and real
   actionable cause); map these to existing public actions, or explicit accurate
   human task when no automation exists. No parsing raw English, no second health
   engine, no diagnostic/internal prerequisite. Preserve health decisions/schema
   compatibility. Drive remedies for at least those real distinct causes. This is
   existing accepted typed-remedy design, not new scope.
8. Public commit/design closure replacements before hiding close. review commit
   --dispositions must compose existing whole-close/collect/publish just as goal;
   design is handled by accepted planning. Preserve all compatibility calls.

Then implement planning-continuity completely as brief4 specifies: design author
without build claim/worktree, actual safe conditional project publication, atomic
stranded Starting recovery, lawful claim at first design critique with actor/epoch/
round cap/accounting, OPERATION -> actual child replay and actual subject refresh,
failed/capped review retry, design status/wait/show/stop, public proof refs. Do not
silently drop IP-C4/C5 named fixtures. Root will continue same context if necessary.

Use focused tests first. Register new Go tests in testing.json; preserve latest
main entries and all coverage floors. METASYSTEM_TESTING_WORKERS=9, keep inherited
other allowances. No unrelated broad test repeats. Provide executable isolated
runtime recipes (fake provider/real owners), honest partial gaps and exact exits.

Maximum150 NEW tools or35 actual minutes. Launch measurement counters are cumulative
on this resumed session: checkpoint4 calls380/tools392; newtools104, actual24m50.7s.
Return5 path: metasystem/artifacts/agents/intent-workflows-build-return-5.md.
Reconcile IW1..7 and IP1..4 without claiming completion from a partial checkpoint.
