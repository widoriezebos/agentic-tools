# Instruction Ledger

Standing ledger of instruction changes adopted by retros (`skills/retro/SKILL.md`). Rows enter as `ADOPTED` with `Review by` naming the next retro; that retro replaces the status with a verdict: `KEPT` (the expected effect is visible), `KEPT-UNPROVEN` (cheap and uncontradicted, but no supporting evidence yet), `AMENDED`, or `REVERTED`. Two consecutive `KEPT-UNPROVEN` verdicts revert by default, which is how "unsupported after two reviews" stays trackable when each verdict overwrites the last.

| Id | Retro | Change | Owner doc | Evidence pattern | Expected effect | Review by | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| IL-1 | retro-2026-08-03 | Fixture waits carry named ceilings like every run | `docs/orchestration.md` | no hang-class receipt recurs | validation never exceeds its ceiling silently | retro-2026-08-04 | KEPT: fifteen-plus suite runs this period under heavy parallel load, no hang, ceilings printed on every fixture wait |
| IL-1b | retro-2026-08-04 | Every timing budget on a process-backed path scales with measured load, not only the waits a fixture writes: adapter and dispatcher windows count too | `scripts/agents/fixture-budget.sh` | no fixed-second budget goes red under load | a budget that is generous on a laptop stops being a red gate on a loaded runner | next retro | KEPT-UNPROVEN: no timing budget failed this period, including with two Codex jobs and the suite running together, but no run demonstrably survived because of the scaling; next unproven review reverts by default |
| IL-2 | retro-2026-08-03 | Verify asserts world-state facts over agent narration | `skills/verify/SKILL.md` | no narration-marker defect recurs | retro-2026-08-04 | KEPT: the grader's decisive claims were replayed orchestrator-side (grade.sh against the seed, probe identity by diff) rather than accepted from the return's narration, and the conformance mismatch was resolved against the computed diff |
| IL-3 | retro-2026-08-03 | Audit falls back to grep when rg is absent | `scripts/audit-metasystem.sh` | audit runs on machines without ripgrep | retro-2026-08-04 | KEPT-UNPROVEN: the audit ran in every suite run but rg was present every time, so the fallback path still has never executed; second review without direct evidence retires it |
| IL-4 | retro-2026-08-03 | Agents launch only through the dispatcher; recordless agent processes are UNTRACKED defects | `docs/orchestration.md` | no untracked-launch incident recurs | retro-2026-08-04 | KEPT: the census named a real untracked peer Codex session (pid 19907) through the live stop hook, the first time it has caught one automatically rather than by hand |
| IL-4b | retro-2026-08-03 | Session-start hooks arm supervision on every runtime | `scripts/enforcement/` | an unarmed session cannot occur on a hook-supporting runtime | retro-2026-08-04 | AMENDED: counter-evidence: supervision had to be armed by hand in the Mission Zero target, because a freshly adopted repository has had no session and so no session-start hook has fired. Arming on session start cannot cover first use; provisioning must arm explicitly. Amended as IL-13 |
| IL-12 | retro-2026-08-05 | Ending a turn with unblocked open work is refused once, not merely reported | `scripts/agents/supervision-hook.sh` | no turn ends with open work unacknowledged | advice that competes with an agent's sense of being finished loses; a refusal does not | next retro | KEPT: fired live for the first time, refused the turn end once, and all four of its signals were real work: two stale-plan claims, three unrecorded human-blocked items, one genuine untracked peer |
| IL-11 | retro-2026-08-05 | The repository that builds the metasystem runs under it: its own lifecycle hooks are installed and the suite fails if they are not | `scripts/validate-metasystem.sh` | no metasystem mechanism is fixtured but never live in this repository | a mechanism that has never fired here cannot be believed to work anywhere | next retro | KEPT: decisive: every hook firing this period, including the stop hook's first live refusal and the census catch, exists only because the repository now runs under its own metasystem |
| IL-10 | retro-2026-08-05 | The continuation signal is checked for accuracy, not just emitted, and reaches every runtime through the dispatcher rather than through hooks alone | `scripts/agents/open-work.py` | no turn is lost to a plan that misdescribes itself | a stale plan cannot send the check confidently after the wrong work | next retro | AMENDED: mixed: STALE-PLAN fired correctly twice against a plan claiming in-flight work that was gap-stopped, and falsely several times between rounds of a live delegate chain, when no job record is running but the conversation is open. Amended as IL-16 |
| IL-9 | retro-2026-08-05 | A rule stated in more than one place is checked mechanically, not by critique rounds: plans retire a term with a `RETIRED:` marker and the suite fails when any plan still prescribes it | `scripts/assert-plan-consistency.sh` | no critique round's findings are wholly drift between statements of one rule | paid rounds stop being spent on contradictions a script finds instantly | next retro | AMENDED: counter-evidence: round 4 of the artifact critique found three findings (BA-4-1, BA-4-2, BA-4-3) of exactly this class, dispositions landing in one file while the same claim survived in another, because the mechanical check covers plans and not the benchmark artifacts. Amended as IL-15 |
| IL-8 | retro-2026-08-04 | Continuation is checked by the metasystem, not requested in prose: the end-of-turn hook reports a plan that names an unblocked next step while nothing is in flight | `scripts/agents/open-work.py` | no turn ends silently with unblocked work named in a plan | stopping short becomes a visible fact instead of something only a reader notices | next retro | KEPT: the continuation check fired live for the first time and its report was actionable in full |
| IL-7 | retro-2026-08-04 | Stage explicit paths, never a directory, whenever another agent may hold work in the same tree | `docs/orchestration.md` | no peer file is committed by a stream that does not own it | a peer's in-flight file cannot be swept into an unrelated commit | next retro | AMENDED: violated repeatedly by its own author: `git add -A plans`, `-A scripts` and `-A benchmark` throughout the period, with a peer Codex session live. No peer file was captured, which is luck rather than compliance. Prose has now failed twice; amended to a mechanism as IL-14 |
| IL-5 | retro-2026-08-04 | Retained or extracted critique findings are count-verified against the round's verdict before the round is called closed | `skills/design-critique/SKILL.md` | no finding-loss incident recurs | a retention that drops findings fails instead of reading as complete | next retro | KEPT: every critique round's material count was read mechanically from the return and round 5's zero was verified before the chain was closed by join |
| IL-6 | retro-2026-08-04 | A design or plan edit that ships no code and no fixture states which shipped artifact it will change and when | `docs/project-adaptation.md` | design-to-implement receipt ratio returns toward parity | plan edits stop outrunning shipped code unnoticed | next retro | KEPT-UNPROVEN: design edits this period all rode with the artifact they changed, so the rule was never the binding constraint; next unproven review reverts by default |
| IL-13 | retro-2026-08-05 | Provisioning arms supervision explicitly; session-start hooks are the steady-state path, not first use | `docs/orchestration.md` | a freshly adopted repository has never had a session, so no session-start hook has fired in it; Mission Zero's preflight refused until supervision was armed by hand | no future first run of an adopted repository is blocked on unarmed supervision | retro-next | ADOPTED |
| IL-14 | retro-2026-08-05 | Directory staging is refused mechanically while a peer session is live, rather than forbidden in prose | `scripts/` | IL-7 was violated throughout the period by its own author with a peer Codex session live; the file it was written to protect was untouched only by luck | zero directory-staged commits while a peer is live next period | retro-next | ADOPTED |
| IL-15 | retro-2026-08-05 | The mechanical multi-place consistency check covers the benchmark artifacts, not only the plans | `scripts/assert-plan-consistency.sh` | round 4 of the artifact critique found three findings that were all a disposition landing in one file while the claim survived in another, in exactly the pair the check does not read | no critique round finds a spec-manifest seam a mechanical check could have found | retro-next | ADOPTED |
| IL-16 | retro-2026-08-05 | An open delegate chain counts as work in flight, so the staleness check reads the chain, not only the job record | `scripts/agents/open-work.py` | STALE-PLAN fired falsely several times between rounds of a live chain, the same false-positive family as the census signature noise, which teaches the operator to ignore a true report | no false stale-plan report between rounds of a live chain | retro-next | ADOPTED |
| IL-17 | retro-2026-08-05 | A commit is gated on the suite run that produced its verdict, in one chain; no verdict is read from a log tail or a previous shell | `docs/project-rules.md` | two pushes on a red suite in one day, both because a check's failure did not stop the chain and the verdict was read from a log tail rather than an exit status | zero pushes on a red suite next period | retro-next | ADOPTED |
| IL-18 | retro-2026-08-05 | A brief that cites authority keys is checked against that authority before dispatch | `docs/orchestration.md` | seven gap-stops on one implement chain, six of them briefs citing manifest keys the manifest did not contain or containing an internal contradiction; each cost a full round trip | fewer than two gap-stops per implement chain next period | retro-next | ADOPTED |
| IL-19 | retro-2026-08-05 | A receipt is appended in the same commit as the work it describes, never in a bookkeeping-only commit | `docs/project-rules.md` | 80 of 97 commits touched plans or docs against 39 touching code, and receipts.log was the single most-edited file in the repository at 40 touches; the retro's own inversion test | bookkeeping-only commits fall below one in five next period | retro-next | ADOPTED |

Period numbers, retro-2026-08-05: 44 receipts over 1.6 days, 42 shipped, 1 parked, 41 corrections, 6 caught by verify, 1 stop-loss. 97 commits: 80 touched plans or docs, 39 touched scripts or benchmark, 24 touched both. Mission Zero completed; the BM-1 case, its grader and its calibration shipped; two critique chains closed by join.
