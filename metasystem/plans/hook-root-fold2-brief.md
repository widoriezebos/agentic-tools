Working Mode: design
Orchestrator Identity: m0b+main-1788250419-3170380-8a1fb3 (dispatch delegate under goal supervision-hook-wrong-root)
Date: 2026-09-02

# Goal

Revision 2 of metasystem/plans/supervision-hook-root-design.md: fold all
five findings of metasystem/records/misc/hook-root-critique-r1.md (landed,
in your worktree; the critic's full return is durable at
metasystem/artifacts/agents/design-critic-d72e332214149decc5d43c85/rounds/1/return.json).

# The folds, by id

- SHR-ROOT-01: replace or strengthen the marker so a linked delegate
  worktree or stray conf cannot claim the world — the critic's evidence
  names what actually distinguishes the governing checkout (the supervision
  and artifacts state, the enrolled engine pin); pick an authoritative
  predicate and state its uniqueness argument.
- SHR-WORKTREE-01: decide the worktree question explicitly — which world a
  hook firing inside a linked delegate worktree reports, with the reason
  grounded in what turn reporting is FOR (the seat's session, not the
  delegate's sandbox).
- SHR-EXIT-01: fix the prescribed snippet so every new failure path honors
  benign exit 0 under set -euo pipefail — the critic quotes the exact
  construction that would abort instead.
- SHR-FIXTURE-01: specify session identity for both nested fixture cases so
  each can assert its expected block.
- SHR-CONSUMER-01: correct the up-consumer claim per the critic's evidence
  and redo the blast analysis for that consumer.

Consistency pass; self-grade; reject condition restated.

# Constraints

Wall-clock budget: 25 minutes. The five folds only.

# Expected Return

Version-2 implementer JSON; diffBoundary exactly
metasystem/plans/supervision-hook-root-design.md (that one file).

# Gap Rule

stop and report a gap; never fill it silently.
