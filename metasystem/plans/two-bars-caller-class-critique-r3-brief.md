Working Mode: design
Orchestrator Identity: m1b+main-1788333346-60696-6a3256 (dispatch delegate under goal two-bars-for-changes)
Date: 2026-09-02

# Goal

Round 3, the declared failsafe round, of your critique of
metasystem/plans/two-bars-caller-class-design.md, now revision 3
(landed, in your workspace). Round 2 closed on three accepted findings
— TBCC-R2-LAND-SIDE-DOOR, TBCC-R2-NONRUNNING-WORKER,
TBCC-R2-AMBIGUOUS-MACHINE-LINEAGE — with the orchestrator's evidence in
metasystem/plans/two-bars-caller-class-dispositions-r2.md, which also
records the fold's gap answers. Revision 3 folds them: the landing
driver refuses inside a job worktree through a second engine verb,
`lease job-worktree`, sharing the worker rule's geometry function; the
worker rule requires the status literal `running`; the wrapper owns its
three trailers with a pre-mint check over the message bytes it can
read, a refusal of message sources it cannot read, and a post-commit
count that rolls back a hook-injected duplicate.

# Inputs: decisions already taken, so you do not re-raise them

- The two additions to the trailer monopoly (refusing unreadable
  message sources; the post-commit count and rollback) are ACCEPTED by
  the orchestrator; confirm or strike them on their merits only.
- The engine verb for the landing driver's geometry check (one owner
  for the rule) is ACCEPTED.
- The pre-commit guard's token geometry (design F13) is DECIDED as its
  own next slice and is out of this design's box.

# Review brief

Round budget: this is round 3 of three, the failsafe. After it, only a
demonstrated requirement failure or a shape-level defect reopens
prose; mechanical-grain findings become fixture obligations and
implementation begins with a mandatory code critique. Threat model,
scope and materiality criterion unchanged from round 1
(metasystem/plans/two-bars-caller-class-critique-brief.md).

Verify first that each round-2 finding is actually folded, by reading
the revised sections, and say so per finding id. Then attack revision
3 where it is new: the landing driver's refusal (is any lawful landing
refused by the geometry rule; does `json get` on the boolean behave as
claimed); the `running` literal (any lawful worker with a non-running
status); the trailer monopoly (a message shape git parses as a trailer
block that the wrapper's pre-mint read misses, or the reverse; the
`-m` joining rule; the post-commit count's rollback preserving the
index); and the fixture legs 7.2 and 7.5 as written against the beds.

Return format: the design-critic schema; stable identifiers
TBCC-R3-<name>; for each material finding say whether it is
mechanical-grain (a value, a format, a bounded choice) or
invariant-grade (a contract or a shape); a clean verdict is
`verdictMaterialCount: 0` with any non-material observations recorded.

# Constraints

Wall-clock budget: 25 minutes. Do not rewrite the design.

# Gap Rule

stop and report a gap; never fill it silently.
