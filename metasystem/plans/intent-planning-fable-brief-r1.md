# Fable critique: complete public design work

Kind: critique. New usability iteration, round 1 of at most 2; failsafe round 2.
Maximum 32 tool calls / 12 minutes, report ceiling 1900 words. Root authors and
adjudicates; Fable independently critiques, as Wido explicitly requested.

Read the full metasystem/plans/designs/intent-planning-continuity.md and relevant
parent contract designs/intent-workflows.md. First run project design-of --goal
verbs-match-intent --root <worktree>/metasystem and verify the new draft record.
The local live executable is /Users/wido/LocalStorage/GitHub/agentic-tools-m1e/metasystem/bin/metasystem.

This is a concrete new requirement failure found after the first built usability
checkpoint: configured design authoring and same-chain design critique still need
internal commands; built help design exits 2; proof/path waits execute but are not
discoverable. The parent Fable C1-C8 chain is CLOSED on named fixtures, and Opus is
implementing those in this worktree NOW. Do not edit product files, call agents,
run builds/tests, change goals, commit, or read/copy metasystem.conf.local. Read-only
source inspection is permitted. Write ONLY the report named below. Current product
files may change during your read; name source functions and read concrete owners.
This critique concerns the NEW planning capability design, not unfinished code.

User explicitly authorized repeated design/critique/build/usability iterations and
machinery bypass. We are not silently buying a third round for an unresolved old
finding. Critique this new record and its smallest proposed owner composition.

Criterion (verbatim):
> Would an implementer working from this design build **step 1** DIFFERENT, or WRONG, because of this finding?
> Does step 1 WORK, and is it SAFE, without this finding?

An actual user/agent task that still requires internal knowledge is a requirement
failure. Challenge unnecessary mechanisms and the parent premise. Report a real
missing capability, an authority/data-loss/duplicate-spend risk or a guess an
implementer must make; do not grow a general workflow framework. State both test
answers for every material finding. Separate notes and unexamined scope.

Focused checklist:
1. Does the design's public grammar complete author -> visible draft -> findings
   -> corrected draft -> same bounded critique, including manual author edits?
2. Is launch.design the correct smallest owner given normal implementer preamble
   forbids design/plans? Admission, exact brief/config/source binding and old-writer
   custody must remain honest. Manager.Start's reservation/custody is not assumed
   to have an idempotent design-request API already.
3. Does staged output plus new project publication preserve a changed document,
   give reachable public recovery, avoid stale replacement after a newer attempt,
   and avoid passing an existing live page as disposable declared output?
4. Does the existing dispatch follow-up actually preserve design subject, finding
   register and cap? Is the frozen prior-round request sufficient for replay,
   including after completion and multiple legacy roots, without CLI store scans?
5. Is root orientation/advanced proof-file wait discovery a capability-preserving
   small change? Any missing actual first-use input should be named precisely.
6. Identify the smallest owner/contract correction, with a named fixture, for each
   material gap. Do not certify unexamined owner behavior.

Write complete report to metasystem/plans/intent-planning-fable-review-r1.md.
Include reviewed design SHA256, actual model, MATERIAL count, stable IP-C1... IDs,
concrete file/function evidence, both criterion answers and smallest correction.
Return report path and verdict. Root will preserve the exact draft and adjudicate.
