# Records

This tree holds concluded history — critique rounds, dispositions,
facts files, finished designs, one area per directory; append-only
for agents and humans (the goal engine alone mutates
records/goals/ under its ledger rules); live intent belongs in
plans/ and living registers in memory/.
