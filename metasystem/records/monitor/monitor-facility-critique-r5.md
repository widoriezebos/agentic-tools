Verdict: r5 is not ready. Six material findings remain: two critical, three high, and one medium. The event disposition is closed; its remaining “three rows” count is nonmaterial because the four rows themselves are explicit.

1. **MF-R5-01 — CRITICAL — `adopted-verified` still leaves signaling authority ambiguous.** “Target’s group session leader” could mean the process-group leader, whose PID equals the process-group ID, or the session leader, whose PID equals the session ID. Those differ for background process groups. The real code exposes ancestry and process-group membership, but no session-ID primitive ([design](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:123), [group enumeration](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/supervise/proc.go:253), [Darwin ancestry](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/identity/enumerate_darwin.go:86)). Because this label later authorizes group signaling, the implementation cannot guess.

2. **MF-R5-02 — CRITICAL — The provisional draining verdict is not representable.** Draining must carry a frozen provisional verdict, but the declared record has no such field ([schema](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:80), [transition](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:112)). It also does not define whether `endedAt` is stamped on entry to draining or terminalization, leaving the `windDownMin` clock unspecified. Recomputing pattern evidence later is observably different because surviving descendants may continue changing the log. This needs typed persisted state and a focused transition test.

3. **MF-R5-03 — HIGH — Exclusive waiter registration does not complete the wake contract.** Waiter records carry `session` and `mainId`, but `WaiterLive` reduces them to “some live waiter exists” ([registration](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:45), [facts](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:212)). Because `watch` is open and exclusive, another main can occupy the slot, prevent the caller’s waiter, and still suppress the caller’s UNWATCHED block. The design must require owner/session correlation or explicitly define custody transfer. It must also assign exit behavior for live-owner collision, unknown waiter identity, lock timeout, and write failure; code `1` is already “run ended red” ([exit map](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:53)). MON-04 does not force either decision.

4. **MF-R5-04 — HIGH — The combined job/run once-only key remains undefined.** `JobFact` contains no generation or nonce, while the combined digest is said to use sorted lifecycle triples ([facts and digest](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:219)). Real job records have only an immutable, collision-refused `jobId`, not a run-style triple ([job record](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/build.go:161), [collision rule](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/record.go:153)). Pin a tagged encoding such as `job:<id>` versus `run:<id,generation,nonce>`; otherwise cross-kind deduplication remains an implementation choice.

5. **MF-R5-05 — HIGH — The attestation obligation still gates the obsolete contract.** The corrected body requires `(id,generation,launchNonce)`, `Supervised`, freshness, and a live armed watcher. MON-07 still requires `(id,generation)`, calls the fact `Watched`, and omits watcher liveness ([body](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:179), [MON-07](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:301)). Because implementation is gated row by row, that row can approve the exact lifecycle-reuse defect r5 claims to close. The freshness authority should also be the armed state’s loaded `intervalSec`; the component consumes an armer-supplied flag rather than rereading current configuration ([component](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/supervise_component.go:38), [armed state](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/supervise/disk.go:101)).

6. **MF-R5-06 — MEDIUM — The green cursor prevents duplicates by silently losing valid greens.** A green can become visible after a later-ended green because draining delays terminalization. A new green can also share the cursor’s second-precision timestamp. Since greens at or before the cursor are discarded, either case may never surface at all ([cursor contract](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:237), [current timestamp precision](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/record.go:444)). This provides at-most-once, not “each green once.” Use a lock-assigned monotonic terminal sequence, or weaken the promise and test accordingly.

The event changes match the real string-payload emitter and closed registry, including the single `run-swept` owner and `run-cas-refused` row ([emitter](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/events/emit.go:43)). I therefore did not count the stale “three rows” wording in MON-09.

Evidence: checked by reading HEAD `f408635`. The design-obligation gate was run and refused completion because MON-01 through MON-09 remain `PARTIAL`; no product tests were run because the facility is not implemented. No files changed, and the pre-existing untracked `metasystem` binary was untouched.

Proposed review receipt: `type=review|outcome=reworked|skills=design-critique|verify=design-gate-refused-partial|corrections=0|stop_loss=no|delegate=read-only-exploration|note=monitor-facility r5 critique found 6 material findings`

REVISE: r5 still leaves the draining verdict, adopted signaling predicate, waiter ownership, mixed lifecycle key, attestation gate, and green-once ordering open enough to change what gets built.
