Verdict: r6 is not ready. Four material findings remain, ranked by severity.

1. **MF-R6-01 — CRITICAL — `job:<jobId>` is not a lifecycle key in the real repository.** Creation refuses an ID only while its record exists ([record.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/record.go:153)), while evidence collection later removes terminal records ([gc.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/evidence/gc.go:368)); the test proves that deletion ([gc_test.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/evidence/gc_test.go:167)). Because dispatch accepts an explicit `--job-id`, a later job can reuse the ID while the earlier digest remains in `blockedUnwatchedDigests`. The later live job can then be silently treated as already blocked. MON-05 needs a genuine job-incarnation discriminator or a permanent non-reuse authority.

2. **MF-R6-02 — CRITICAL — Waiter ownership is correlated, but waiter target identity is not.** The waiter filename and record contain only kind, public ID, owner, and waiter-process identity ([design](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:53)); they carry neither the run’s generation/nonce nor a job incarnation. Run-ID reuse is explicitly legal ([design](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/monitor-facility-design.md:201)), and job reuse is possible as above. A still-live waiter from an old lifecycle can therefore refuse the legitimate waiter with exit 64 and falsely satisfy `WaiterLive` for work it never watched. MON-04 needs lifecycle-bound waiter records or an equivalent prune/reuse fence.

3. **MF-R6-03 — HIGH — The exact open watch command cannot derive the declared HUMAN owner key.** `lease classify` supplies `mainId` and session data for an authenticated main, but a HUMAN classification carries no session ([classify.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/classify.go:260), [verbs.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/verbs.go:185)). Yet the exact printed commands contain no `--session`, while the waiter key requires `human:<session>`. An implementer must either change the promised command, invent an unstable process-derived identity, or correlate unrelated humans. The interface must name the canonical HUMAN session source.

4. **MF-R6-04 — MEDIUM — A terminal sequence plus a maximum cursor still does not guarantee each green once.** The design does not require the run scan to hold the runs lock or otherwise observe a prefix-complete snapshot. A scan can read run A as active, then A terminalizes at sequence 10 and B at sequence 11, then the same scan reads B as green and advances the cursor to 11. A’s green is permanently discarded. Temporarily unreadable run A creates the same hole. Current code performs `report.Scan` before entering the goal-state lock ([goal.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/cmd/metasystem/goal.go:249), [turnverdict.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/goal/turnverdict.go:121)). Moreover, session entries are evicted after 30 days or 128 sessions and malformed state resets to empty, permitting replay ([turnverdict.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/goal/turnverdict.go:303), [turnverdict.go](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/goal/turnverdict.go:320)). MON-05 needs prefix-consistent scanning, an unreadable freeze rule, and a scoped retention/recovery promise.

The pgid-only predicate, persisted provisional verdict and draining clock, and corrected MON-07 attestation are otherwise implementable against HEAD `71211002dcfc12dbda92d70fafd5ace63561bcce`. I did not count stale wording or the expected `PARTIAL` gate result because neither changes implementation.

Evidence: checked by reading HEAD; the design-obligation gate was run and reported MON-01 through MON-09 incomplete, as expected for an unimplemented facility. No product tests ran and no files changed. Because this is round six, the critique skill’s second three-round budget is exhausted; another round requires a human ruling.

Proposed review receipt: `type=review|outcome=reworked|skills=design-critique|verify=design-gate-refused-partial|corrections=0|stop_loss=no|delegate=read-only-evidence|note=monitor-facility r6 critique found 4 material findings`

REVISE: lifecycle reuse can suppress both the unwatched block and the rightful waiter, while the green cursor still lacks the prefix-consistent observation needed for exactly-once delivery.
