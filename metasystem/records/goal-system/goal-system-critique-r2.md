Not ready. Applying the design-critique materiality test, only G-02 and G-13 close cleanly. The following findings would change implementation or observable behavior.

1. **CRITICAL — G-04 remains unresolved: the required cross-runtime behavior is deferred, not dispositioned.** Shipping one conforming runtime plus a future contract does not satisfy “working identically … any runtime.” The proposed table is also factually premature: the repository ships Codex and Devin `Stop` configurations ([Codex hooks](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/scripts/enforcement/codex-hooks.json:16), [Devin hooks](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/scripts/enforcement/devin-hooks.json:14)), while another shipped path says only one runtime has been observed firing them ([dispatch.sh](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/scripts/agents/dispatch.sh:93)). Conformance must distinguish declared, installed, observed, and blocking-capable—not assert “no surface.” Wido’s pending sign-off is a genuine blocker.

2. **CRITICAL — G-03 remains unresolved: delegates do not receive the facility required by item 14.** The unresolved default-versus-opt-in choice leaves implementation behavior undecided, and the projection carries only id plus intent—not the explicit next step the requirement says every agent receives. Claude delegate settings currently install only `SessionStart`, not a turn-end hook ([claude.go](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/internal/adapter/claude.go:58)); excluding mission-prompt integration also leaves headless mission hosts without a causal read path.

3. **CRITICAL — G-05 remains unresolved: the original silent-omission incident is still possible.** “Programs start by creating a goal” remains prose, with no gate coupling program/backlog creation to `goal open`. Worse, the grammar alternates between exactly one and at most one Current goal; parking explicitly leaves none, and a valid ledger containing only queued/done goals reaches the all-clear branch without `--and-none`. The claimed goal-free declaration has no record in the grammar, so silent absence and declared absence cannot actually be distinguished.

4. **CRITICAL — G-07/G-08 remain unresolved: the human-reserved boundary is bypassable.** A holder can directly change `Origin: human` to `Origin: main`, run a reconcile that merely validates the resulting grammar, then complete or park the goal. Nothing requires reconcile to authorize semantic deltas against the accepted record. Moreover, the existing authority classifier defines any caller with no recognized agent ancestor as `HUMAN` ([classify.go](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/internal/lease/classify.go:254)), and `HUMAN` receives unconditional authority ([authority.go](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/internal/authority/authority.go:25)). That classification is not authenticated human identity.

5. **HIGH — G-01/G-09 are only partially resolved: the block-once state machine is unspecified.** The verb has no declared repository/session inputs, state path, state schema, locking, or cleanup. One signature slot cannot preserve both today’s resettable open-work signature and the permanent “this goal revision was already blocked” set. Sequence: goal G blocks, open work O blocks, O clears, G returns; overwriting one slot makes G block twice. Concurrent Stop calls can also both observe “unseen” without an atomic check-and-record. The current behavior is explicitly per-session state ([supervision-hook.sh](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/scripts/agents/supervision-hook.sh:172)); moving ownership does not define these invariants.

6. **HIGH — The reshape does not compose with the existing hook contract.** The hook has an advisor early exit, watchdog suppression, protocol-growth reporting, running-work status, evidence collection, and lease cursor updates ([supervision-hook.sh](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/scripts/agents/supervision-hook.sh:118)). The proposed verdict schema contains none of those facts. If shell still composes them, display ownership remains split; if it does not, existing interactions disappear. The design must define whether the verb owns the complete final hook response or an explicitly composable component, including the mapping to Claude’s `decision/reason` and `systemMessage` envelopes.

7. **HIGH — G-14 remains unresolved: the grammar cannot represent the command surface.** There is no parked record, no goal-free declaration or timestamp, and a Done goal loses `Origin`, making `reopen` unable to preserve authority. ID uniqueness, open-with-existing-current behavior, promotion while another current exists, unpark collision, reopen’s required next step, and idempotency remain unspecified. Saying a legal-transition table will exist “in the contract” is not supplying that table.

8. **HIGH — G-07’s manual-edit detection is not implementable from the specified revision.** The exposed revision hashes only the Current block, so edits to queued or done records are invisible. No accepted whole-ledger digest or durable baseline is defined. A direct editor also ignores the engine’s flock and can write between the engine’s read and atomic rename, after which the engine may overwrite the edit without any later mismatch. Reconcile needs whole-ledger CAS semantics and authority-aware delta validation.

9. **HIGH — G-11 remains unresolved: precedence is not a single next-step authority.** `Evidence` is optional and its references are neither existence-checked nor required to cover serving plans. The scanner suppresses a plan waiting on the human ([openwork.go](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/internal/report/openwork.go:230)), allowing the goal’s potentially contradictory imperative to surface. The proposed regression proves only “not nothing left,” not agreement, derivation, or safe suppression between the two next-step fields.

10. **HIGH — G-06 is only partially resolved: failure transport is internally incomplete.** The result schema has no ledger-status or diagnostics field despite claiming a distinct no-ledger field. Unreadable files, block-state corruption, lock timeout, and state-write failure have no outcome. “Malformed exits 1” is also ambiguous: `turn-verdict` must still return transportable degraded JSON, whereas a nonzero exit under the hook’s `set -e` path can suppress the verdict entirely. Degraded state must also compose with an open-work block rather than disappear behind it.

11. **HIGH — G-10 is not actually composed.** There is no join key from a run record to a goal, no selection rule when several runs claim one goal, and no mapping for green, red, or hang. “MAY read” leaves the item-14 implementer free to build or omit behavior, while the proof obligations drop all three terminal outcomes. Separating write authority is correct; the claimed read seam is not yet a contract.

12. **HIGH — G-15 introduces a new unbounded prompt surface.** Only `Next step` is size/control-character bounded, but delegates receive `id + intent`. Intent and id have no byte or control-character bounds, so the claimed envelope safety and injection containment do not apply to the text actually projected into briefs.

13. **MEDIUM — G-12 is only partially resolved: ledger lifecycle and adoption remain contradictory.** A live “example block” is either parsed as real work or needs an example/comment syntax not defined. Current adoption deletes template plan state and creates fresh skeletons ([adopt.sh](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/scripts/adopt.sh:171)), while its fixture asserts an exact ledger set ([validate-metasystem.sh](/private/tmp/claude-501/-Users-wido-LocalStorage-GitHub-agentic-tools/af965b26-11a5-4d56-b6c2-8f1df989f9f3/scratchpad/wt-quality/metasystem/scripts/validate-metasystem.sh:4096)). Those surfaces and `docs/project-adaptation.md` need named amendments. “History beyond ten is git” is also false unless every pruned transition was committed before pruning.

14. **MEDIUM — G-16 remains unresolved.** Missing proofs include program-start registration, every zero-current state, the complete transition matrix, duplicate IDs, concurrent Stop decisions, direct-edit/write interleavings, origin-downgrade reconciliation, takeover revalidation of stale next steps, existing hook-message composition and advisor paths, fresh-adoption behavior, startup/resume consumption, three run outcomes, and real host/delegate delivery on each runtime.

Disposition audit: **resolved:** G-02, G-13. **Partial:** G-01, G-06, G-09, G-12, G-15. **Unresolved:** G-03, G-04, G-05, G-07, G-08, G-10, G-11, G-14, G-16.

Evidence level: checked by reading the design and current repository contracts; no runtime tests were run because this is a design-only review.

Proposed receipt: `RECEIPT|type=design|outcome=reworked|skills=design-critique|verify=read-only-code-grounding|corrections=0|stop_loss=no|delegate=none|note=goal-system r2 leaves registration, cross-runtime delivery, authority, and block-once state materially open`

REVISE: r2 still cannot guarantee that intent reaches every agent exactly once because registration, authority, per-session state, and runtime delivery remain incomplete or deferred.
