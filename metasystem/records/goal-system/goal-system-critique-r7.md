Not ready. Four material findings remain.

1. **HIGH — GOAL-21 does not establish its mission-seat fact.** The supported live-holder branch explicitly preserves the existing main’s lineage; only unattended missions receive a mission lineage. Callers beneath that live holder can still classify as its work. Therefore an active attended mission does not imply a mission-owned checkout lease, while GOAL-21 tests only the conditional “if lineage is mission-shaped, refuse.” It can pass without enforcing the promised total refusal. The matrix must require every active mission path—including live-holder launch—to expose an authoritative mission-seat fact, or select the mission lease/runner identity instead. [Live-holder branch](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/missionrunner/launch.go:181), [explicit prior scope](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/lease-succession.md:275), [ancestor classification](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/lease/classify.go:260), [GOAL-21](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/goal-system-design.md:482)

2. **HIGH — Gate activity can disappear before `Unreadable` can observe a failure.** Gate registration is best-effort: the suite suppresses registration errors, and `Register` returns successful emptiness when process identity is unreadable. On the read side, unreadable markers are silently skipped and malformed markers deleted; registration also writes non-atomically, so a concurrent scan can delete a partially written live marker. GOAL-17 covers directory enumeration failure only. It can pass while a running gate has neither `Busy` nor `Unreadable`. The matrix must cover registration failure, per-marker read/parse failure, and the register-versus-scan race. [Best-effort caller](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/validate-metasystem.sh:75), [registration contract](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/gaterun/gaterun.go:58), [silent reader paths](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/gaterun/gaterun.go:95), [GOAL-17](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/goal-system-design.md:478)

3. **HIGH — The claimed mission-runner “existing freshness rule” does not exist.** Runner heartbeats are written but the production status decision reads the runner record and kernel identity, not heartbeat freshness. Heartbeat sidecars also survive completion. GOAL-16 names only “runner heartbeat” and a positive live-mission test, so existence-only, timestamp-only, lease-correlated, and kernel-correlated implementations can all satisfy the row while producing different dead/completed outcomes. The row must define the authoritative correlation and test live, completed, crashed, stale, and mismatched identities. [Heartbeat path](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/missionrunner/engine.go:136), [heartbeat writer](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/missionrunner/loop.go:165), [actual status rule](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/missionrunner/status.go:29), [GOAL-16](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/goal-system-design.md:477)

4. **HIGH — Watchdog state remains cardinality-unbounded.** Round 7 gives per-session watchdog files 30-day pruning, but that is not a count bound: arbitrarily many fresh sessions can create arbitrarily many files, and pruning them can itself make Stop latency unbounded. GOAL-04 caps only the aggregate verdict map; its watchdog requirement covers identifier normalization, not file eviction or count. Fold watchdog signatures into the capped map or add an explicit bounded eviction rule and fixture. [Proposed state rules](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/goal-system-design.md:217), [current per-session file](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/supervision-hook.sh:105), [GOAL-04](/Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/goal-system-design.md:465)

The parser’s third consumer and exit-3 proof contradiction are closed; D66 and D67 remain uncontested. I excluded stale blast-radius wording and naming polish because the matrix resolves them without changing the build.

Evidence: checked the named production paths by reading them; ran the design-obligation gate and observed the expected exit 1 for PARTIAL critical/high rows. No runtime tests ran and no files changed.

Proposed receipt: `RECEIPT|type=design|outcome=reworked|skills=design-critique|verify=read-only-code-grounding+design-obligation-gate-exit-1|corrections=0|stop_loss=no|delegate=none|note=goal-system r7 leaves attended mission identity, lossless gate inventory, runner liveness correlation, and watchdog cardinality materially open`

REVISE: the matrix can pass while an attended mission or unrecorded gate still permits an incorrect goal decision, and while runner and watchdog state remain lifecycle-ambiguous or unbounded.
