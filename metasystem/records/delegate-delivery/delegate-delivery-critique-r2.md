Verdict: **8 material findings.** The per-attempt snapshots, separate provider-exit signal, and attempt/channel/digest provenance are sound improvements. Evidence is from live code reads and read-only `jq` probes of stored ATIF; no tests were run.

1. **HIGH — The durable repair claim is still not implementable, so r1 finding 7 remains open.** The current record compare-and-swap compares only `status`. While the record remains `running`, two `{returnRepairs: 1}` metadata updates both succeed; the second is not a failed claim. Moreover, r2 gives repair eligibility to both `devin-collect` and `adjudicate.go`, even though adjudication is deliberately pure and record mutation must travel through the dispatch authority path. This requires a dispatch-owned conditional operation that atomically requires `running && returnRepairs == 0`; it cannot be implemented within the stated blast radius. [RecordCAS compares status only](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/dispatch/record.go:242>) [adjudication’s pure boundary](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/adjudicate.go:11>) [conflicting r2 ownership](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:26>)

2. **HIGH — The proposed collector cannot validate host returns or recover them end to end, so the host half of r1 finding 11 is unresolved.** Its fixed inputs and validator are job-shaped, but a host has a turn record and orchestrator identity: `turnId`, `missionId`, `cycle`, runtime/model, plus the special announced-or-observed session rule. A schema-valid wrong-turn stdout return would therefore block a valid named file before the mission runner rejects it. Even if collection accepts a named return, `host.FinishTurn` still fails whenever `raw.out` is empty. The blast radius omits the required `internal/host` and `internal/missionrunner` changes. [job-shaped collector interface](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:37>) [host identity contract](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/missionrunner/adjudicate.go:78>) [empty raw is failure](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/host/result.go:79>)

3. **HIGH — The transcript ceiling does not bound the actual lifecycle, leaving r1 finding 8 open.** Delegate usage extraction and settlement read and decode the complete transcript before collection; the outcome table deliberately preserves that ordering. Both ultimately use unbounded `os.ReadFile`. The design also only explicitly suppresses downstream transcript normalization for a transcript-selected reply, leaving stdout/named delivery able to reach the existing per-brace scanner. A collector-local 8 MiB ceiling therefore does not prevent the claimed post-CLI wedge. [current pre-collection reads](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/scripts/agents/adapters/devin.sh:343>) [usage’s unbounded read](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/usage/usage.go:202>) [settlement’s unbounded read](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/devin.go:243>) [normalizer’s brace scanning](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/return.go:132>)

4. **HIGH — Exit 1 collapses incompatible failures and can launch an unauthenticated repair.** R2 maps every mechanical failure “as if the channel were absent.” That category includes an unreadable schema or record, an over-limit candidate, and inability to write the accepted snapshot or provenance. Those are harness failures, not empty delegate replies, and must not trigger a paid repair. An oversized higher-priority candidate must also fall through rather than shadow a valid lower channel. Worse, an 8 MiB refusal supplies no watermark, while “stop after 4096 steps” records a false boundary when additional initial steps exist; either case lets repair mining include pre-repair material. [r2 exit mapping](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:136>) [r2 watermark and step limit](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:83>) [current tests require unreadable schema to refuse](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/adjudicate_test.go:118>)

5. **HIGH — Transcript mining is still not authenticated as final intent, so r1 finding 3 was refuted rather than resolved.** R2 accepts any in-window, schema-valid write argument and explicitly allows denied or failed writes to deliver. The stored empty-stdout transcript contains exactly this shape: a denied write to `/tmp/design-critic-return.json` made while preparing a temporary validation draft. Full schema validation proves shape and job identity, not that an arbitrary or unsuccessful write was the intended final return. If this broad recovery is intentional, the design must state that tradeoff honestly and define the designation rule; calling it authenticated is false. [r2 mining predicate](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:74>) [proof explicitly admits denied writes](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:200>) [stored denied temporary write](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/artifacts/agents/devin-selftest-20260808t080024z-37986-main/rounds/1/transcript.atif.json:1600>)

6. **HIGH — Validating before normalization deletes currently supported return behavior.** Today `NormalizeReturn` first extracts JSON from prose, code fences, wrapper result strings, or transcripts and reconciles claimed session/model against observed identity; only then does `return-complete` validate the canonical result. R2 instead requires each raw snapshot to pass schema-and-job validation before that normalizer runs. Pinned fenced/wrapper replies and replies with honest-but-different claimed identity would be rejected before reconciliation. This either needs per-candidate normalization before canonical validation or explicit approval to remove supported behavior. [current normalization order](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/adjudicate.go:42>) [pinned fenced and identity reconciliation](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/return_test.go:33>) [pinned wrapper support](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/return_test.go:95>)

7. **HIGH — The outcome table is not attempt-aware and conflicts with the existing repair taxonomy.** “Reapply this table” sends a nonzero repair call toward generic provider failure and a zero-exit/no-delivery repair toward `empty_reply`. The current after-repair state maps both a nonzero repair and an invalid or missing repair candidate to `protocol-error`, which uses a distinct durable record and recovery path. The table also moves repaired-session settlement ahead of repair validation, contrary to today’s ordering. A separate repair-attempt outcome table is required; otherwise implementers must guess which error and recovery contract survives. [r2 table](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:136>) [current after-repair mapping](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/adjudicate.go:119>) [pinned protocol-error behavior](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/internal/adapter/adjudicate_test.go:169>)

8. **MEDIUM — Transcript provenance cannot audit the mining decision.** The proposed source record retains attempt, channel, accepted-byte digest, and rejected channels, but not the selected step identifier, tool-call identifier, target path, tool outcome, watermark, or digest of the bounded transcript snapshot. With cumulative transcripts and deliberately accepted denied calls, the artifact proves which bytes were delivered but not why that transcript event qualified or whether it was in-window. [proposed provenance fields](</Users/wido/LocalStorage/GitHub/agentic-tools/metasystem/plans/delegate-delivery-design.md:151>)

REVISE: The design still lacks an atomic repair claim and a single bounded, identity-aware collection contract that preserves both delegate and host lifecycle invariants.
