# Live proof register: delivery-candidate-is-the-workspace, chain dcwl-build3-20260911

Job dcwl-verify2-20260911 (claude, claude-opus-5, verifier) drove the final round-3 tree 3eeab385 through the real command surface in scratch repositories built by the tree's own land bed, with step 7 (the no-tree receipt) added for the closing read's DCW-19. Projected verbatim from artifacts/agents/dcwl-verify2-20260911/rounds/1/return.json.

## What was done

I made a scratch git checkout of the chain tree 3eeab385, whose metasystem subtree is 71862805, the tree round 3 reviewed. I put the proof engine at its bin/metasystem and used it as the bed's source engine. I then sourced the land bed's own helper functions, taken verbatim from lines 36-147 and 150-605 of the chain's metasystem/scripts/agents/land-fixtures.sh, and drove four legs with them. Each leg uses the bed's recipe: a contract-bearing seed with a bare origin, clone one and a peer clone, an armed steward runner, and each leg's engine built by the chain's go-build.sh. Clone one took receipts, the peer made its move, and clone one landed with land.sh --chain --test-receipt --staged-only --skip-transport. I captured every verifying command's own exit code in a variable.

The ledger leg covers step 7 (receipt without --tree), step 1, the trunk contrast and step 2. The records leg covers step 3, the input leg step 4, and the cutover leg step 6, with the old engine built from 6bc19ba1c. I ran step 5 with the proof engine against trees built from the real chain layout. Every behaviour happened as the brief describes. Afterwards I confirmed all seven runners had exited, deleted every temporary directory, and restored the job's object quarantine to its original empty state. The checkout was never changed. About 40 minutes of the 60-minute budget were used.

## Evidence

- [ran] Step 7, in clone one before any move, with payload.txt staged: receipt_env_run <local>/bin/metasystem landing test-receipt --root . --mode auto --goal fx --cap-min 1   (no --tree; receipt_env_run is the bed's helper that runs a command in a clean environment with the leg's fake process identity file and private supervision registry)
  => Exit 0. Output ended "TEST-RESULT sufficient=true tree=7a57f3e504a66d342aff4080934fad2c1ed9d9d3 selected=candidate-smoke,policy-protection". The receipts directory artifacts/agents/landing/receipts held exactly one file, 7a57f3e504a66d342aff4080934fad2c1ed9d9d3.json. That name, and the receipt's tree field, equal clone one's index tree from `git write-tree`. The receipt's binding fields (index and worktree trees before and after) all name that same tree, and it carries workspace.tree c641df2f171e7ff3e0432139187cbd4d5bea55d7.

- [ran] Step 1, the brief's exact command in clone one (after moving step 7's receipt aside): receipt_env_run <local>/bin/metasystem landing test-receipt --root . --tree $(git write-tree) --mode auto --goal fx ; then landing workspace --root . --tree 7a57f3e5… with the leg engine and with the proof engine
  => Exit 0. The receipt at receipts/7a57f3e504a66d342aff4080934fad2c1ed9d9d3.json has tree 7a57f3e5… and the workspace object {"excludes":["memory/receipts.log","plans/goals","plans/goals-accepted.json","plans/goals.md","records/counselor","records/goals","records/narrator-digest.log"],"tree":"c641df2f171e7ff3e0432139187cbd4d5bea55d7"}. `landing workspace` for the receipt tree printed c641df2f171e7ff3e0432139187cbd4d5bea55d7 from both the leg engine and the proof engine, exit 0 each, so it equals the receipt's workspace.tree. This receipt carries the same proof attempt id (proof-mtw903q0-7005123bf0cbe45f) and timestamp as step 7's, so it was republished from the proof step 7 had just retained for the same tree, not run fresh.

- [ran] Step 2, the contrast before the chain, same state and same receipt: receipt_env_run env METASYSTEM_BIN=<main checkout>/metasystem/bin/metasystem bash <local>/artifacts/trunk/land.sh -m <msg> --chain ledger-move-chain --goal fx --test-receipt <local>/artifacts/agents/landing/receipts/7a57f3e5….json --staged-only --skip-transport   (land.sh as it is at this checkout's HEAD, identical to commit cbe443a41; the engine binary is stamped cbe443a41)
  => Exit 2. land.out: "!! STEP FAILED: test receipt for staged candidate (exit 2)" and "land refused: the receipt at …/receipts/7a57f3e504a66d342aff4080934fad2c1ed9d9d3.json names tree 7a57f3e504a66d342aff4080934fad2c1ed9d9d3 but the staged candidate is 0e801c9a0b7d5ab5458f2df5a56e21930fb18b63; make the receipt against this exact candidate". Afterwards clone one's HEAD and origin's main were both still B (84887b3a520496c731f24f9dce0603f70d3590bc).

- [ran] Step 2 with the chain: the peer ran `goal open --id peer-goal` and pushed (the bed's publish_peer_ledger_move); clone one ran fetch and `merge --ff-only origin/main`, then receipt_env_run env METASYSTEM_BIN=<pass-through wrapper around the leg engine> bash <local>/scripts/agents/land.sh -m <msg> --chain ledger-move-chain --goal fx --test-receipt <same receipt> --staged-only --skip-transport
  => Origin moved from A=82b03cb18ac84dc5c08d242795686c9d969e5f91 to B=84887b3a520496c731f24f9dce0603f70d3590bc. B's parent is A, and A..B changes only plans/goals/peer-goal.md. Clone one's staged candidate became 0e801c9a… and still maps to workspace id c641df2f… (the proof engine gave the same id for both trees). The landing exited 0, running every step through "push origin (attempt 1 of 3)". Clone one's HEAD 85b164ba7fff9d02a77ab467c5fe40e6780e8ce4 equals origin's main, and HEAD^1 is B. Origin's main contains plans/goals/peer-goal.md, and payload.txt reads "ledger landing". The commit wrapper logged the receipt path and "verdict=pass bar=a". The wrapper's call log shows site 8 made two `landing workspace` calls, for the receipt tree and the candidate tree, and no `test verify` before the commit. The one `test verify` came after the rebase, at the new commit, exit 0.

- [ran] Step 3: the peer committed and pushed a new records/misc/peer-note.md (publish_peer_records_move); clone one fetched and fast-forwarded, then ran the same land.sh command with chain records-move-chain and its own receipt (receipt taken with the brief's step 1 command, exit 0)
  => A..B (A=bfa9b2a7…, B=751bd201af1c1b97fd08a96d1003a362b13cd747) changes only records/misc/peer-note.md. The workspace ids differ: receipt tree 0be73dff… maps to cac19c403437aedb3c0b1c9130401a3ad50b8299, moved candidate f44b22d4… maps to b0f7fbb3444257df9b162bb6f20dac0b5f688fc8. The landing exited 0. Clone one's HEAD cf5847d0… equals origin's main, HEAD^1 is B, and origin has records/misc/peer-note.md. land.out has zero lines containing proof-input-moved-after-receipt, and its step "test receipt for staged candidate" passed. land.sh keeps the output of that verification and prints it only on failure, so land.out does not show the call. The pass-through log does: while HEAD was still B (751bd20), site 8 ran `test verify --root <local> --tree f44b22d450a23f3366abdd821dfa95daebfc5a0a --mode auto --purpose delivery --goal fx`, exit 0. A second `test verify` after the rebase, at cf5847d, also exited 0.

- [ran] Step 4: the peer committed and pushed scripts/application-input.txt (publish_peer_input_move); clone one, without fetching first, ran the same land.sh command with chain input-move-chain and its own receipt (receipt exit 0)
  => A..B (A=df15eecc…, B=760483ad6b2dbebeb383854a47d5313bfaf4fd78) adds scripts/application-input.txt containing "fixture peer input move". The file did not exist at A, so the bed's helper creates it rather than appending to it. It falls under the policy-protection group's declared input scripts/**. The landing exited 1, with "!! STEP FAILED: verify shared testing proof after rebase (exit 1)". land.out carries "proof-input-moved-after-receipt: group policy-protection was proved on tree 6f35129bf03b887a08c2e71b1e50592de58d462a with a different input identity; moved declared paths: scripts/application-input.txt" and "TEST-GROUP policy-protection status=not-run reason=missing-proof". Origin's main is still B. Clone one's rebased local commit b35167bd… is not on origin, and B is its ancestor. The call log shows site 8 accepted the receipt as an exact match (no `landing workspace` call), and the post-rebase `test verify` exited 1.

- [ran] Step 5, in a scratch checkout of chain tree 3eeab385 (real layout, with a metasystem/ subdirectory): <proof engine> landing workspace --root <scratch>/metasystem --tree <T> for T0 = 3eeab385ca4d182a41a1b3c2d59071d0d26f0713, T1 = T0 plus new files metasystem/plans/goals/zz-verifier-probe.md and metasystem/records/counselor/zz-verifier-probe.md (70ef377fa4fba1653825f79bd569ba159cc73a8e), T2 = T0 plus new file metasystem/internal/zz_verifier_probe.go (0adb76663c2bcc244eaaf91ab0643bcb86e92e88)
  => All three exited 0. T0 printed 23c73b0bb5fed85ab31f35aa349e269db74621a1. T1, which differs only under plans/goals and records/counselor, printed the same 23c73b0bb5fed85ab31f35aa349e269db74621a1. T2, which differs under internal/, printed a different id, ae821efae7599bfed6f8daefd4cc0fc7713eae81.

- [ran] Step 6, the bed's receipt-cutover recipe: git archive 6bc19ba1c metasystem/, then METASYSTEM_BUILD_STAMP=<seed commit> bash <old-src>/metasystem/scripts/agents/go-build.sh --out <leg>/old-engine, installed as clone one's engine; old engine takes a receipt for tree d8411147…; old engine runs `landing observe --root <local> --tree d84111472437596ec70b94a6a5ec1bf2fc2d0d71 --chain cutover-exact-chain --goal fx --actor fixture-machine+land-receipt-fixture --test-receipt <local>/artifacts/agents/landing/receipts/d8411147….json`; then the candidate-engine receipt for the same tree (taken in the peer) is copied to that path and observed again; then the old receipt is restored and observed
  => The old engine built, exit 0. Its receipt command exited 0 and wrote receipts/d84111472437596ec70b94a6a5ec1bf2fc2d0d71.json with no workspace field. Observing that receipt: exit 0, mode "observe", verdictTrailer "pass bar=a". The candidate engine's receipt for the same tree has the same file name and workspace.tree e68f0681609e858cc7b6617d935828318de7c6bf. At the same path, the old engine's observe exited 0 with mode "observe" and verdictTrailer "would-refuse code=chain-test-receipt-refused". With the old receipt restored at that path, the verdict was "pass bar=a" again. The refusal appears as a verdict; the observe command itself exits 0.

- [ran] Proof binary as the checkout's engine (my first attempt): the bed's install_cutover_engine put the proof binary at clone one's and the peer's bin/metasystem, the steward runner was armed with it, then the step 7 and step 1 receipt commands ran; earlier, the same commands ran with the proof binary invoked from its own scratch location
  => Arming succeeded (runner pid 29042). Both receipt commands then exited 1 with "metasystem test run: TEST_POLICY_ENGINE_REQUIRED: retained destination engine does not bind the captured policy base: rebuilt engine carries build stamp dev-609071da2-dirty; automatic re-arm is bounded to landed commits". Invoked from its own location, the proof binary exited 78 with "METASYSTEM_FAKE_PROCESS_IDENTITY_FILE is set but metasystem.runtimes is not fake in <scratch root>", because the engine reads the fixture configuration from the directory it is installed in. Neither outcome involves the change under test.

- [read] Engine provenance: go version -m on both binaries; git --no-optional-locks -C <dcwl-build3-20260911 worktree> diff --stat 3eeab385ca4d182a41a1b3c2d59071d0d26f0713 -- . ; ls-files --others on that worktree's engine paths; find Go files newer than the proof binary; git rev-parse 3eeab385:metasystem; read metasystem/artifacts/agents/dcwl-build3-20260911/rounds/3/review.json
  => The proof binary has sha256 1f66d9aa5f3b14112cd17686a1efe110966cf9824a2e597592a782250fbad6d6, stamp dev-609071da2-dirty, no -trimpath, and embedded source paths under the dcwl-build3-20260911 worktree. That worktree's diff against the chain tree is empty, it has no untracked engine files, and no Go file there is newer than the binary. The chain tree's metasystem subtree is 71862805a8598eaf41ef46e119cd737d7f4d534f, which is round 3's reviewedTree. The leg engines were built by the chain's go-build.sh from my checkout of that tree. The trunk engine is stamped cbe443a41, an ancestor of this checkout's HEAD with no engine-source difference from it.

- [ran] Cleanup: kill -0 on each steward runner pid (12047, 29042, 81811, 81921, 81975, 82151, 87111) under bash; rm -rf on my six temporary leg directories and the scratch area by exact name; test -e on them; git status --porcelain in this checkout
  => Every pid returned "No such process". The directories are absent. The checkout has zero changed or untracked files and HEAD is still 0ed08eea3. The job's git object quarantine has zero entries.

## Riskiest part, as the verifier wrote it

All seven behaviours and the trunk "before" contrast happened as the brief describes, and nothing contradicted the expected behaviour. The weakest link is which binary did the work. The proof engine binary itself (sha256 1f66d9aa…, build stamp dev-609071da2-dirty) ran step 5, the workspace-id comparisons in steps 1 to 3, and the bed's seed setup. It did not take any receipt or run any landing. Every receipt, every landing and every steward runner used the bed's own engine for each leg instead: the land bed (metasystem/scripts/agents/land-fixtures.sh) builds that engine from the same chain source with the chain's own go-build.sh and stamps it with the seed commit. I tried the proof binary first. With it installed as clone one's engine, both receipt commands exited 1 with "TEST_POLICY_ENGINE_REQUIRED: … rebuilt engine carries build stamp dev-609071da2-dirty; automatic re-arm is bounded to landed commits". That refusal is deliberate engine policy, not the change under test, so I took the bed's designed path. That the two binaries come from the same source rests on reading, not on comparing bytes. The worktree the proof binary was built in (named in its embedded paths) shows an empty diff against the chain tree 3eeab385, has no untracked engine files, and has no Go file newer than the binary. The bed's builds came from a checkout of that same tree, whose metasystem subtree 71862805 is the tree round 3 reviewed. Builds without -trimpath embed their source directory, so the bytes cannot be compared. Two smaller points: step 3's call to `test verify` shows only in a pass-through call log, not in land.out, and step 6's refusal is a verdict text from a command that exits 0.

## Gaps the verifier named

- The proof binary itself did not take any receipt or run any landing, which departs from the brief's instruction to use it for every step. The engine refuses its development build stamp (dev-609071da2-dirty) as a checkout's policy engine. Receipts and landings therefore ran on the bed's own build of the same chain source, stamped with the seed commit. Source equality rests on an empty diff, not on comparing bytes.
- metasystem/bin/metasystem does not exist in this worktree. For the "before" contrast I used the main checkout's metasystem/bin/metasystem (stamp cbe443a41) as METASYSTEM_BIN, with land.sh as it is at this checkout's HEAD, which is identical at cbe443a41. That land.sh was copied into clone one's git-ignored artifacts/trunk/ directory so that it resolves the same repository root.
- Step 3: land.out alone does not show site 8 calling `test verify`, because land.sh prints that output only on failure. The call, and its exit code 0 while HEAD was still B, come from a pass-through wrapper set as METASYSTEM_BIN. The wrapper logged each engine call and the local HEAD, then ran the leg engine unchanged.
- Step 4: the peer's change creates a new file under the declared scripts/** input rather than appending to an existing file, which is what the bed's helper does. The refusal came at the post-rebase verification (exit 1), not at site 8.
- Step 6: the old engine's refusal is the verdict text "would-refuse code=chain-test-receipt-refused" from `landing observe`, which exits 0. I did not drive a full landing with the old engine over a receipt that carries the workspace field. I also skipped two parts of the bed's cutover scenario: landing the exact old receipt, and the old engine refusing after a ledger move.
- In steps 2 and 3, clone one fast-forwarded onto the peer's tip before landing, as the bed does, so the landing's own rebase had nothing to do. A ledger-only landing that starts at A and rebases across B during land.sh was not driven. The input case did rebase across B.
- Step 1's receipt was republished from the proof step 7 had just retained for the same tree, rather than produced by a fresh test run.
- Not run: the bed's two checks that local changes left after the receipt still refuse (one staged, one unstaged) inside the ledger scenario; the bed as a whole through its scenario harness; and enrolled-engine resolution, which the design says the bed cannot reach.
- My shell inherited this job's GIT_OBJECT_DIRECTORY and GIT_ALTERNATE_OBJECT_DIRECTORIES. As a result my first scratch clone and git plumbing wrote 11 unreferenced objects and an alternates file into the job's object quarantine under the checkout's git directory. I confirmed all 12 files were mine: the quarantine was created empty at 02:43:24 and every file was written later by my commands. I moved them out, and the quarantine is empty again. No file in the checkout changed.

## Coordinator disposition (m1d, 2026-09-11)

All seven behaviours and the trunk "before" contrast were observed as designed; nothing contradicted the expected behaviour. The verifier's weakest link — that the bed's own seed-stamped engine, built from the same chain source, took the receipts and ran the landings, while the development-stamped proof binary could not be enrolled as a checkout's policy engine — is the engine-binding rule working as designed, not a gap in the proof: the bed's engine is the candidate's source stamped for the scratch repository. The chain closes on this proof.
