# hp-terminal-grade-for-stopping-acts, critique round nine: dispositions

Chain hp-terminal-build1-20260909. The ninth critic
hp-terminal-crit9-20260909 (claude-opus-5) reviewed the fold
hp-terminal-build1-20260909-r11 at tree
7824b3a652d4117a7cee673a0431cd8c67b445a3 and returned one material
finding and two notes. Round twelve folds all three.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| HPT-28 | accepted | Recovery rebuilds the actor from the journal entry's machine and lineage only, so a stranded human park replays with Parked.By "<machine>+<lineage>" instead of "human:<name>", and this chain's unpark gates, which test for the "human:" prefix, then admit any agent with no proof. | Round twelve makes recovery rebuild the actor exactly as journaled, carrying the "by" name the live act stamped, so the replayed record is what the admitted act would have written; a test proves the replayed park keeps the human's name and an agent's unpark of it is refused. |
| HPT-29 | noted | The recovery wrapper replaces the typed error's own condition with one fixed sentence keyed on the verb, so an agent's own stranded park of a human-origin goal is told to re-run at the human boundary. Wording only. | Round twelve appends the typed error's own condition to the cannot-replay sentence. |
| HPT-30 | noted | main gained rows and exclusions in the refusal register after the chain's base; the computed diff is clean but the landing needs a rebase and the eight citation lines a re-check. | Round twelve's follow-up rebases the worktree; the brief asks for the re-check. |
