# land-sh-omits-the-full-width-chain-receipt, critique round one: dispositions

Chain lsr-build1 after its round-one build, critic lsr-critic1 (claude,
claude-fable-5-1, xhigh), reviewed tree
15e13e198fe5125405cb1258b2956222bb0e99b8. The critic returned zero
material findings and six notes. The critic had no shell; the
orchestrator's seat-side run of the landing fixture bed on the reviewed
worktree under the stock bash 3.2 (all seven isolated legs green, the new
full-width-chain scenario included) stands as the executed proof. Three
notes are operator-facing refusals with no reason, which the seat
communication rules forbid, so they are folded in a second round although
not material.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| F-1 | noted | A missing or unreadable receipt file fails the step with no sentence. True; not a defect in the lane's decision. | Folded in round two (plans/land-sh-omits-the-full-width-chain-receipt-fold-r2-brief.md): the step says why. |
| F-2 | noted | The three new usage refusals print only the usage line. True. | Folded in round two: each prints a "land refused:" sentence first. |
| F-3 | noted | The early refusal names a source file and a subtree the operator computes; the engine exposes no verb printing the battery command, and duplicating its literal in shell was refused by the brief. | none; the recipe lives in the chain-landing memory and the goal record. |
| F-4 | noted | The tests-plus-receipt usage error has no fixture leg. True. | Folded in round two: the scenario gains that leg. |
| F-5 | noted | land.sh checks the receipt's tree only; a wrong command or status is still refused by the evaluator, after the static re-proof. Accepted: the evaluator is the authority and the earlier check is a convenience. | none |
| F-6 | noted | The scenario seeds from live registers, so it depends on those rows persisting; the other landing scenarios share the shape. | none |
