# land-sh-omits-the-full-width-chain-receipt, critique round two: dispositions

Chain lsr-build1 after its round-two fold, critic lsr-critic2 (claude,
claude-fable-5-1, xhigh), reviewed tree
13963bf2faefc7ea642e5e04f03b30814e8a5ac8. The critic returned zero
material findings and four notes; the chain closes on it. The critic had
no shell; the orchestrator's seat-side runs of the landing fixture bed on
the reviewed worktree under the stock bash 3.2 (all seven isolated legs
green after each round, the four-leg full-width-chain scenario included)
stand as the executed proof, and the chain lands through the new lane
itself as the goal's own proof.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| F-1 | noted | The parser's own refusal of a malformed --test-receipt flag (no value, or twice) prints the usage line only, like every other malformed flag in land.sh; the fold's sentence rule covered the lane's refusals, not the parser's. | none |
| F-2 | noted | No leg pins that an area-width root lands without a receipt; the implementation reads gateWidth and refuses only "full", which the critic confirmed by reading. A leg for it is a good later addition. | none; recorded here. |
| F-3 | noted | A readable non-JSON receipt reports "no tree field" rather than "not JSON"; one step less precise, still a plain refusal. | none |
| F-4 | noted | The receipt tree check runs after staging so the pathspec form sees the real candidate; the fixture legs use --staged-only only. | none; recorded here. |
