# rearm-remedies-and-adoption-notes, critique round one: dispositions

Chain rra-build1 after its round-one build, critic rra-critic1 (claude,
claude-fable-5-1, xhigh), reviewed tree
1f448200dee8debc09a9f8ba045a2c390a5d0dad. The critic had no shell; the
orchestrator's seat-side proof in the builder's worktree (the up and
steward packages green, vet and gofmt clean, both scripts parse) stands
as the executed proof. The adopt fixture bed's frozen gate (the full
race and coverage suite) did not complete under the machine's load
that hour: three unrelated tests with five- and ten-second bounds
timed out (the fake channel's token routes, the mission runner's group
termination pair), all three green in isolation on main; the bed's two
new legs are still owed a seat-side run before the goal concludes.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| F-1 | accepted | Real: the arm lock is a blocking exclusive flock, so a holder never produces the open or take error; those errors come from permissions, a full or read-only filesystem, or the descriptor limit. The wording was the brief's, so the brief's premise was wrong, not the builder's conformance. | Folded in round two (plans/rearm-remedies-and-adoption-notes-fold-r2-brief.md): the two arm-lock classes name the lock file and the permission, space and descriptor repairs; the tests follow. |
| F-2 | accepted | Real: the detached test came before the preset-key read, so a detached target with a preset landing ref got the "not seeded" note while the key stayed configured. The Go seeder reads the key first. | Folded in round two: the preset read comes first, and a third fixture leg pins detached-plus-preset. |
| F-3 | noted | The brief's "four not-landed messages" counted the resolver's four returns; only two carry the not-landed wording, and the change keys on exactly those two. The other two (unresolved or unrecognised stamp) keep the terminal remedy, which fits: a locally built engine always has its commit locally. | none; recorded here. |
| F-4 | noted | The replace trigger requires the invoking binary's canonical path to equal the recorded install path. This is the conservative reading of the brief (a human arming from a copy elsewhere would otherwise re-enroll a new install path); the human verb passes the running executable, so the checkout's own engine hits the path. Accepted as the design. | none; recorded here. |
| F-5 | accepted | The fallback sentence for an unrecognised before-mint error had no test. | Folded in round two: one table case pins it. |
