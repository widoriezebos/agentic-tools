# human-proof-fits-the-act, design critique round two: dispositions

Critic human-proof-crit2-20260909 (codex gpt-5.6-sol, xhigh) reviewed
revision 2 of plans/human-proof-fits-the-act-design.md (commit
7b6ac5e7e) and returned seven material findings, one critical, and one
note. All seven are accepted. The critical one, HPA-10, is a hole in
the tree today, not only in the page, and is opened as its own goal
(fixture-human-authority-mintable-from-conf) so it is fixed whether or
not this design proceeds. Whether this goal takes a third design
revision or is cut to its minimal slice is put to Wido; this record
holds whichever way that goes.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| HPA-10 | accepted | Critical and live: fixtureauth derives fixture mode from the worktree's metasystem.conf alone, so an agent that writes metasystem.runtimes=fake and passes --fixture-human-authority holds a valid enrolled-grade proof; revision 2 would register that flag on six more human verbs. | The design replaces the command-line fixture grant with a test-only injected prover, or a capability an agent cannot create by editing repository bytes; the flag is never registered on a verb that grants or widens authority. Opened as goal fixture-human-authority-mintable-from-conf for the tree as it is today. |
| HPA-11 | accepted | A missing authorityGrade on a pre-change journal entry cannot be read as terminal: the untouched builder let an agent with an exported lineage write human-attributed intents with no proof, so recover would complete them without either walk. | A missing stamp grants no grade and terminalizes toward a fresh human invocation; only an explicitly human-ratified migration record grandfathers an old entry. |
| HPA-12 | accepted | resolve-taint --restore starts a new sequence segment and can move a mission from parked to running, exactly like adopt-disputed-tree. | Both resolution variants take the enrolled grade; the matrix, the refusal row and the fixture obligation cover both. |
| HPA-13 | accepted | The shipped channel-authority contract (set-obligation and resume through the authenticated-channel class; approve and norm exceptions through the verified-answer class) is neither inventoried nor encoded consistently. | The matrix lists every channel consumer with its class, says whether a channel-authorized journal keeps that class as its grade, and forbids coercing channel authority into another grade. |
| HPA-14 | accepted | GradeRefused cannot render the recovery row without the failed enrolled attempt, and no proposed interface carries it. | The CLI builder returns the engine request plus the structured enrolled attempt, and the renderer takes that attempt; one ephemeral carrier, named. |
| HPA-15 | accepted | Enroll refuses unreadable or incomplete records today, and a replacement's generation is undecided; resetting to 1 can recreate an old terminal-derived lineage. | The conservative rule: the refusal says no command can safely complete until the corrupt record is repaired, and names how; no enroll command is printed that may fail or reuse an identity. |
| HPA-16 | accepted | goal reopen's all-parked-arc branch checks only Actor.Human and is rebuilt by recover through the same closure; the matrix omits it. | A terminal-grade reopen row, included in the Actor.Human-without-Authority engine fixture. |
| HPA-17 | noted | set-next reaches Edit's human branches through the same builder and helper the matrix already grades. | none. |
