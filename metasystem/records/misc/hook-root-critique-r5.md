# Hook-root design critique — round 5 (closing round on revision 5)

Chain: revision 5 (landed 68130d238, sha256 8718c15ae2591e9a9e34612b2033ec8c57cdb68390fb77dfc2e5d077928045e9) -> critic hook-root-critique4b-20260905-r2 (claude, design-critic, round 2 of root hook-root-critique4b-20260905), 2026-09-06. THREE material findings, all high: revision 6 is required before any build. This root has spent both of its review rounds; revision 6 needs a new critic root.

## SHR-R5-DEADLINE-OVERRIDE-ENGINE-SPLIT-01 — high, material=True

CLAIM: Revision 5 regresses the earlier one-engine pairing rule inside the deadline parent. The design says that under an override a turn “runs one engine throughout,” and sets the parent validator to `METASYSTEM_BIN` when supplied, but it derives the refusal-record world with `deadline_canonical path state-root` and keeps `report stop-block` on that canonical engine. If the installation's executable canonical engine predates `path state-root` while a compatible override supplies the worker engine, the worker governs the turn successfully but the parent cannot resolve a timeout record and takes the record-failure allow branch. Cases 10 through 13 use an override that merely forwards to the same canonical engine, so they cannot detect this split. The design must choose and prove one timeout-safe engine rule for validation, state-root resolution, and refusal reporting under an override; until then it is not ready to build and turn-verdict-hardening slice 1b remains unsafe.

EVIDENCE: metasystem/plans/supervision-hook-root-design.md:153-167 says `METASYSTEM_BIN` replaces the engine and an overridden turn runs one engine throughout. The parent section at lines 530-542 selects an override validator but invokes the canonical engine for `path state-root`; the blast table at line 947 keeps canonical `report stop-block`. The failure map at lines 696-698 then allows record failure, while cases 11 and 13 at lines 889-918 use wrappers that execute the primary canonical engine rather than a genuinely different compatible engine.

## SHR-R5-SKEW-FIXTURE-VALIDATOR-01 — high, material=True

CLAIM: The proposed old-engine fixture cannot prove the named skew block it requires. Case 7 specifies a stub that handles `runtime list` and “exits 2 for every other argument,” then claims the executable parent validator validates the worker's skew literal. The parent actually uses that stub for multiple `json get` and `json strip` calls; all return 2, so validation fails and the parent replaces the literal with its generic block. The raw-literal fallback is reached only when the validator is not executable. An implementer must guess whether to give the stub working JSON commands, bypass the parent, or change parent control flow. The old-engine proof for the round-four fail-closed fold therefore remains incomplete.

EVIDENCE: metasystem/plans/supervision-hook-root-design.md:820-833 defines the exit-2 stub and asserts `does not answer path state-root` exactly once. metasystem/scripts/agents/supervision-hook.sh:143-175 enters structured validation whenever the validator is executable and reserves raw-literal comparison for the `elif` no-validator branch. Revision 5's self-grade at metasystem/plans/supervision-hook-root-design.md:1158-1164 also proposes a non-executable override as skew fallback proof, but the double executable check maps that firing to the missing-engine literal instead.

## SHR-R5-TEMPLATE-MARKER-SECOND-AUTHORITY-01 — high, material=True

CLAIM: The shipped shell-side template-marker block is a second state-root authority and revision 5 does not dispose of it. Revision 5 explicitly replaces only current lines 258 through 274; current lines 275 through 288 then derive `state_root` again from a template marker and can exit 1 or substitute the harness root after `path state-root` has already returned the authoritative repository. The branch is inert in the named fleet template layout only because the returned repository is already the nested installation, but it can reject an engine-approved adopted installation inside a repository that also carries the marker. Revision 5 must explicitly delete the marker and template-installation decision, leaving at most `state_root=$repo` as a compatibility alias, and must retire or repurpose the missing-template fixture to assert the canonical missing-engine cause and absence of writes. Leaving its present block-only assertion would let the fixture pass for either authority. This unaccounted pre-verdict exit also makes turn-verdict-hardening slice 1b unsafe.

EVIDENCE: metasystem/plans/supervision-hook-root-design.md:391-395 says the state-root verb replaces metasystem/scripts/agents/supervision-hook.sh:258-274. The surviving code at metasystem/scripts/agents/supervision-hook.sh:275-288 sets `state_root=$repo`, tests `development/metasystem-design.md`, may exit 1, and may replace the root with `harness_root`. The compiled authority already makes that decision at metasystem/internal/stateroot/stateroot.go:100-108 and 157-163. The fixture at metasystem/scripts/agents/supervision-hook-fixtures.sh:444-462 has no canonical engine and asserts only some blocking decision, so revision 5's new double executable check reaches the missing-engine block before the template-marker branch without proving that the second authority was removed.

## Critic-declared gaps (verbatim)
