# Code read register: delivery-candidate-is-the-workspace implementation, closing read 3

Job dcwl-cc3-20260911 (claude, claude-opus-5, code-critic) read the final work round dcwl-build3-20260911-r3 (reviewed tree 71862805a8598eaf41ef46e119cd737d7f4d534f; whole-project tree 3eeab385): the reviewed candidate of chain dcwl-build2 re-applied on 609071da2 plus eight fixes. Material findings: 0 of 3. Projected verbatim from artifacts/agents/dcwl-cc3-20260911/rounds/1/return.json.

## DCW-25 (low)

**Claim.** The eighth fix resolves the accepted index tree before the run, but the result file is still named after the raw --tree flag (metasystem/cmd/metasystem/landing_verbs.go:114). Without --tree, every run in one checkout writes and reads the same file, artifacts/agents/proof-runs/delivery/testing-result-.json. On the composed path the verb then passes the file's own tree to CreateTestingReceipt (:140). At that call site, the check in PrepareTestingReceiptPayload that the result's tree equals the receipt tree (metasystem/internal/landing/testing.go:33) compares a value with itself. Design section 3 (site 6) and section 2 (site 11).

**Evidence.** Read. The base 609071da2 already named the file after the raw flag, so the shared name is not new. runTestRun rewrites the file before it returns success or the reusable-success exit (test.go:808 and :913), so a stale file needs two no-tree runs racing in one checkout. Even then nothing wrong can land. PublishCommittedReceipt refuses when the live index is not the accepted tree (receipt.go:214-216). PrepareTestingReceiptPayload still requires the live index to equal the result's tree exactly or by workspace projection, and the working posture to equal it exactly (testing.go:37-50), so a composed receipt is written only for a tree the index holds. Sites 7, 8 and 9 each verify again. What changes if it stands: nothing for correctness. Naming the file after acceptedIndexTree and passing acceptedIndexTree to CreateTestingReceipt would keep both checks meaningful.

## DCW-26 (low)

**Claim.** The no-tree test covers only the first-run (executed) path at a toplevel installation. The exact-reuse path that round 2 broke is tested only with --tree given (metasystem/cmd/metasystem/landing_verbs_test.go:485-487 and :755 against :1544-1575). Design section 8.4 and the second read's DCW-19 text asked for both paths.

**Evidence.** Read. TestLandingTestReceiptModeWithoutTreePublishesIndexReceipt runs runRecertifiedLandingFixture with prefix empty and omitTree true. It takes one receipt, checks the file at TestReceiptPath(root, index tree) (:747-754) and returns. runSharedTestingReceiptRecovery proves exact reuse after a staged records-only move, but its public call passes --tree. Both forms now flow through the one acceptedIndexTree variable (landing_verbs.go:105-113), so the only code specific to the no-tree form is the StagedTree call at :107. The coordinator's disposition asked for one test, and that is met. What changes if it stands: nothing now; a second no-tree call after a records move would pin the reuse path too.

## DCW-27 (low)

**Claim.** The DCW-22 fix prints 'site 8 refused a ledger-only move' whenever the ledger landing fails for any reason, not only when land.out shows site 8's first sentence (metasystem/scripts/agents/land-fixtures.sh:1295-1300). Design section 8.2 says the fixture greps for that sentence and then reports the line.

**Evidence.** Read. The line is printed unconditionally in the leg's failure branch, before the land.out dump. A failure at site 7 (the observer) or site 9 (post-rebase verify) would also be labelled as a site 8 refusal, though the dump that follows shows the real cause. The leg's pass and fail outcomes are unchanged. What changes if it stands: only how readable a failure is.

## Evidence the critic recorded

- [ran] cp .git/worktrees/dcwl-build3-20260911/index $TMPDIR/b3.index && GIT_INDEX_FILE=$TMPDIR/b3.index git -C metasystem/artifacts/agents/worktrees/dcwl-build3-20260911 write-tree; git rev-parse 3eeab385ca4d182a41a1b3c2d59071d0d26f0713:metasystem => The build worktree's staged index is the whole-project tree 3eeab385ca4d182a41a1b3c2d59071d0d26f0713, and its metasystem installation subtree is 71862805a8598eaf41ef46e119cd737d7f4d534f, the tree round 3's review.json names. git status shows exactly the 31 declared paths staged, with nothing under plans/. A plain git write-tree in the worktree was refused by the sandbox (index.lock not permitted), so I used a copy of the index.
- [ran] git diff --stat 6c28684cd 609071da2 -- metasystem/cmd metasystem/internal metasystem/scripts metasystem/testing.json metasystem/docs/project-rules.md => Empty. Main changed none of the changed paths between the second read's base and this chain's base, so the difference between the second read's candidate and the reviewed tree is exactly the fixes.
- [ran] git diff c27379772a9fefb83d2e2ab5b63a2c2d3aadf66d 71862805a8598eaf41ef46e119cd737d7f4d534f -- cmd internal scripts docs testing.json => Nine files changed, 93 insertions and 23 deletions: landing_verbs.go, landing_verbs_test.go, test.go, test_test.go, receipt.go (one new method, AcceptedIndexTree), testing_test.go, register.go, land-fixtures.sh (one line) and validate-section-selector.sh (one line). That is the whole of the eight fixes.
- [read] Fix 1, DCW-19 (material in the second read): read metasystem/cmd/metasystem/landing_verbs.go:95-147 and metasystem/internal/landing/receipt.go:88-122 and :186-242 => Present and complete. The --mode branch resolves the accepted index tree once, before the run (landing_verbs.go:105-113). With --tree it uses ResolveTree; without it, it uses StagedTree of the project toplevel. These are the same two calls prepareTesting makes at test.go:223-232. The branch passes that tree to test run (:115) and to PublishCommittedReceipt (:135). The composed path gives CreateTestingReceipt the result's CandidateTree (:140), which prepareTesting set to that same tree. The legacy --command branch passes preparation.AcceptedIndexTree() (:193, :230). PrepareTestReceipt already required that value to be a full tree id equal to the index (receipt.go:89-102), so the legacy behaviour is unchanged. PublishCommittedReceipt refuses anything that is not a tree id (receipt.go:187-189). The new test TestLandingTestReceiptModeWithoutTreePublishesIndexReceipt (landing_verbs_test.go:485-487) omits --tree and requires the receipt at TestReceiptPath(root, index tree) (:747-754).
- [read] Fix 2, DCW-20: read metasystem/cmd/metasystem/test_test.go:207-239 and test.go:955-960 => Present. Without METASYSTEM_POLICY_PROBE_WORKER the worker refuses a request that lacks CandidateEngineBuildIdentity, with exit 3 and 'input-bound candidate engine is absent'. With the variable set to 1 it gets past that check and stops at the next one ('input-bound policy engine changed').
- [read] Fix 3, DCW-21: read metasystem/internal/refusal/register.go:113 against test.go:1139, :1142, :1191 => Present. The proof-input-moved-after-receipt row now names test.go:1139, which prints the environment variant of the refusal. The other two printing lines are 1142 and 1191.
- [read] Fix 4, DCW-22: read metasystem/scripts/agents/land-fixtures.sh:1295-1300 => Present. The ledger-move-lands failure branch prints 'site 8 refused a ledger-only move' and keeps the land.out dump. It prints the line on any landing failure without grepping for site 8's sentence (DCW-27).
- [read] Fix 5, DCW-23: read metasystem/internal/landing/testing_test.go:112-136 and :194-222 => Present. The 'other tree' case stages and unstages its own records file to get a second tree, so it no longer reads a variable set by an earlier subtest. Its value is built when the table is built, in the parent test, so a -run filter that selects only this subtest still gets it. The keyWhole variable is now local to its own subtest.
- [read] Fix 6, DCW-24: read metasystem/cmd/metasystem/test.go:503-519 and :589-623; test_test.go:186-205 => Present. The candidate engine build environment forces GOAMD64=v1, GOARM64=v8.0 and GOARM=7, overriding inherited values, with a one-line comment. The build-context string in the identity names all three (:620). The toolchain closure is computed under the same pinned environment (:606-611). The unit test feeds foreign values (v4, v9.5, 5) and requires the pinned ones.
- [read] Fix 7, the section declaration: read metasystem/scripts/agents/validate-section-selector.sh:22, metasystem/scripts/validate-metasystem.sh:1006, :1102, :2912-2914 and metasystem/testing.json:15 and :55 => Present. The catalog declares fixture-bed-scenarios-fixtures, matching the run_section name in validate-metasystem.sh and the section of the testing.json group section/fixture-bed-scenarios-fixtures.
- [read] Fix 8, the accepted tree after exact reuse: read metasystem/cmd/metasystem/landing_verbs_test.go:1544-1575 (runSharedTestingReceiptRecovery) with test.go:792-813 and receipt.go:214-218 => Present and complete. After exact reuse, ExactReusableTestResult returns the original result with its original tree T. Publication now compares the live index with the tree this run judged, while the receipt's own tree and path stay T. The recovery test stages a records-only file, runs the public command on the moved index, and checks three things: no receipt is written at the moved tree's path, the original bytes remain at the original path, and no group runs again. test verify on the moved tree is sufficient.
- [read] Reach of the fixes => None reaches beyond its finding. Resolving the tree before test run adds only one behaviour: if the index moves between the verb's read and test run's own read, test run now refuses ('delivery candidate must equal the real whole-project index') instead of silently judging the newer index. That fails closed. The legacy branch change is a rename whose value is unchanged. The DCW-24 pins change the candidate engine's bytes only on hosts that set those variables, which is what the disposition asked for.
- [read] Mandate 1, the three walls: read receipt.go:525-632, :459-478; landing_verbs.go:27-56; land.sh:365-412; diff of land.sh against 609071da2 => Nothing new. At site 7, readTestReceipt runs only the path, strict-decode and structure checks before step 0 (:525-566). Step 0 calls the verify core only for a schema-2 receipt that carries workspace (:567-574), before the workspace recomputation, engine identity, sufficiency, ownership, bindings, coverage and posture. It refuses on a core error, an insufficient result, or a judged tree whose installation subtree is not --tree (:459-478). The callback is set only with --test-receipt and without --recertification, and it verifies the index. A receipt without the field computes nothing new. The index posture accepts exact, then W, then the key (:618-627), and that key comes from the verify core's run on this index. land.sh's only change is site 8. Steps 3 and 5 print the same sentence, byte for byte (:394 and :409). Site 9 is not in the diff.
- [read] Mandate 2, engine identity: read test.go:589-701, :1206-1257, :1046-1079; go-build.sh:55-104 => The identity is the ENGINE projection tree (engineProjectionTree over policy enginePaths into an empty index), the toolchain closure, the platform and a fixed build-context string. It is committed with a fixed author, committer, encoding and year-2000 dates, and never takes T or W. GOFLAGS=-mod=readonly is set in the build environment (:518). go-build.sh sets only CGO_ENABLED and the -ldflags stamp, so GOFLAGS reaches go build. retainedCandidateEngineDigest matches only identity version 2 with an equal build identity, and the receipt fallback also requires receipt and testing agreement. The worker's digest check (test.go:966-971) and validateTestingReceiptEngineIdentity (testing.go:189-210) are unchanged. Section-group identity is stable across a ledger-only move on this tree: none of the seven excluded paths is an engine path, and verify folds in the digest found by build identity.
- [read] Mandate 2b, the compatibility shim: grep policyProbeWorkerEnvironment in the base 609071da2 and the reviewed tree; read test.go:955-960, test_protection.go:19-25, :238, :356-373, test_build.go:207-210, test_result.go:77-81, :282-290, testing.go:30-31 => A door kept open for old engines only, not a weakening. Only the two probe launchers set the variable (test_protection.go:238 and :373). A current engine always sends the build identity: every runTestRun request carries candidateEngine.Commit, and the frozen worker probe copies the outer value (:356). So only an older engine's probe reaches the digest-only branch. A version-1 result cannot serve as delivery evidence under a current engine: PrepareTestingReceiptPayload requires version 2 and a build identity; ExactReusableTestResult compares version and build identity; retainedCandidateEngineDigest requires version 2. test_result.go:80-81 still refuses a version-2 result without a valid build identity. The variable's other effect, skipping the frozen corpus (test_protection.go:25), was already on the base.
- [read] Mandate 3, exact reuse and publication: read test_result.go:273-311, testing.go:25-100, receipt.go:186-242 => ExactReusableTestResult compares the digests, engine version and build identity, purpose, modes, group lists, sufficiency, validity and every selected group identity. It does not compare CandidateTree, BaseCommit, PolicyBaseCommit or PlanDigest. The only callers of PublishCommittedReceipt are the three sites in landing_verbs.go, and each passes a resolved tree id. Site 5 checks the index exactly and then by W, before and after owner validation (testing.go:38 and :48, via :87-100). The working posture stays exact.
- [ran] Mandate 4 and 6: diff of internal/gittree/gittree.go against the base; the nine-file diff; git status of the build worktree => The gittree diff removes no line, so Snapshot, StagedTree, SnapshotRelevant and FilterTree are unchanged. registers.go, the beds' harness, testing.json, commit.sh and the recertified path did not change between the second read and this tree. commit.sh is not among the 31 paths.
- [read] Trigger lines DCW-01 to DCW-13 on the reviewed tree => DCW-01 met: coverage exact, then W, then key at receipt.go:601-617; site 8 steps at land.sh:390-411; the records and input legs are unchanged since the second read. DCW-02 met: site 9 unchanged (not in the land.sh diff), new calls gated on the field at land.sh:371-373 and :393-396 and receipt.go:569-574, cutover leg unchanged. DCW-03 met: retainedCandidateEngineDigest at test.go:1206-1257 keys on build identity only. DCW-04 met: -mod=readonly at test.go:518; platform and build context, now including GOAMD64, GOARM64 and GOARM, at :619-620. DCW-05 met: legs unchanged since the second read. DCW-06 met: registers.go unchanged. DCW-07: design text only, nothing in code. DCW-08 met: step 0 at receipt.go:567-574 with details at :459-478; callback at landing_verbs.go:47-52. DCW-09 met: PrepareTestingReceiptPayload keeps its signature (testing.go:25), widening only the index posture; proof_run.go is not in the diff. DCW-10 met: cutover leg unchanged since the second read. DCW-11 met: harness self-initialisation unchanged. DCW-12 met: process-group reap unchanged. DCW-13 met: ToolchainClosureIdentity called at test.go:611 under the pinned build environment.
- [inferred] Verdict => Closable as is. Zero material findings; DCW-25, DCW-26 and DCW-27 are low and recorded without action.

## Gaps the critic named

- The runtime is read-only, so I did not write metasystem/records/misc/delivery-candidate-is-the-workspace-code-read-r3.md. This return is the register for the coordinator to place.
- I did not run 'metasystem validate conformance --stage review' (the runtime is read-only). I confirmed the reviewed tree from a copy of the build worktree's index and computed the round-2-to-round-3 difference with git between the two installation subtrees.
- The design page on main hashes to a9a2755936cc97ad5719c4829dfe5a874d88762f650ee5535c5b907910cb69f2, not the brief's 1622cf7cfc4fd10259e23eabb0fdae2d1ba7a59a67666cea679f2b951bcca83b. That is the page I read, as both earlier reads also found.
- As the brief required, I ran no Go tests and no beds. Every behavioural claim here comes from reading the code.
- I re-read only the changed line of the bed scripts (land-fixtures.sh:1297). fixture-bed-scenarios.sh, fixture-budget.sh, fixture-bed-scenarios-fixtures.sh and the rest of land-fixtures.sh are byte-identical to what the second read covered, so mandate 5 rests on the first two reads for them.
- I did not measure whether real Go builds produce identical bytes; the design-named determinism fixture still builds through a stub script (DCW-18c).

## Coordinator dispositions (m1d, 2026-09-11)

No material finding; the chain closes on this read. The three low findings are carried as follow-ups on the goal's journey chapter, none changes DONE:
- DCW-25 (the shared result file when `--tree` is omitted, and a self-comparison on the composed path): follow-up, name the result file after the resolved accepted tree.
- DCW-26 (the exact-reuse path without `--tree` is untested): follow-up test.
- DCW-27 (the ledger leg's failure line prints on any failure): follow-up, grep the sentence first.
