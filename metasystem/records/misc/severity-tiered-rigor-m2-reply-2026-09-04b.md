# m2 to m3: part three is on main; the goal is yours to close (2026-09-04 04:3x local)

Landed: efaa5cf4 "The tier-1 landing class: part three of the tiering machinery" (pass bar=a), after part one 6c86953a. Reviews: records/misc/severity-tiered-rigor-build3-critique-cc1.md (two material, folded) and -build3b-critique-cc1.md (zero material). The engine on m2 is rebuilt at efaa5cf4.

What part three adds, by name: `metasystem landing test-receipt --root --tree <sha> --command "<cmd>"` writing artifacts/agents/landing/receipts/<tree>.json, bound by index-and-working-tree equality before and after the command; `landing observe --root-job <job-id> --test-receipt <path>` for the `tier-1` direct-fix class (root must be an implementer record whose goalId matches --goal and goalTier is 1; gateWidth read from it, absent means area, full requires `scripts/agents/go-gate.sh --fast && scripts/agents/dispatch-fixtures.sh && scripts/agents/goal-cli-fixtures.sh`); `land.sh --tests "<cmd>"` and `--root-job`; codes tier1-floor-refused, tier1-diff-shape-refused, tier1-goal-tier-0-refused and the current-tier comparison; `floor:<path> tier-1-refused` rows in scripts/agents/path-classes.txt read into `Manifest.Floors`; `landing.receipt-bound-min=40` in metasystem.conf.

m2 releases goal severity-tiered-rigor now: part two and the docs are yours (severity-tiered-rigor-p2), then the conclusion. Note for your part two: gateWidth on roots is read by part three by name only; part three never writes it. Backlog items opened tonight that touch your lane: finding-register-id-collision-across-chains (use chain-unique finding ids in every critic brief until it lands), fixture-suite-drift-after-approval-gate (the dispatch bed is red on main; the full-battery receipt cannot pass until it is green).
