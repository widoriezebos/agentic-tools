# Third read of the carried landing (Opus, hcl-cc3-20260911)

Chain hcl-build3-20260911, round 2, reviewed tree fdfcd002b69a1a39fe681956d9226e245aea7127. 8 findings, 4 material.

## Gaps the critic named

- The role is read-only, so metasystem/records/misc/human-carried-landing-carry-code-read-r3.md was not written. This return is the register for the coordinator to project: the findings, then the fold lines, the mandate traces and the four named checks as evidence entries, then the verdict entry.

- No land bed leg was run: not carried-prefixed, carried-second, carried-red-battery, carried-intent-failure or carried-crash-local. HCL-C-67 comes from reading the code and the fixture, not from a run. A search of artifacts/ and records/ found no host bed log written after round 2, so the coordinator's host results were not seen.

- Go tests run in the reviewed tree extracted to $TMPDIR: TestHCL57 on the reviewed tree and on one mutant copy, and TestHCL34PlanExecutesEveryFixture. The full package selections (internal/goal, internal/landing, cmd/metasystem, internal/refusal, internal/testpolicy) were not run.

- HCL-C-69's alternative closer was checked only by grep: the goal discharge-review-obligation command shows no counselor or records write. That closer was not driven end to end.

- The provider tool catalog and context isolation are unobserved, so this critique is advisory about runtime independence.


## Findings

### HCL-C-66 (high, material)

Claim: The testing contract's own fixture-inventory test fails on the reviewed tree, and the new unit proof for the seat's second carry never runs under the contract. Round 2 added TestHCL51CarriedCounselorAppendBelongsToItsRow (internal/landing/hcl_carried_test.go:40), the unit proof for fold item 2 (HCL-C-51: the counselor line of a carried landing). It did not add that name to the named test list of the carry-landing-standard group (testing.json:38). No other group over internal/landing runs all tests; landing-command-standard names two unrelated tests. TestHCL34PlanExecutesEveryFixture (internal/testpolicy/contract_test.go:177-236) is the named check HCL-C-34 of revision 6. It requires every TestHCL function in internal/landing to be executed by the plan for a change to hcl_carried_test.go, so it fails. The effects: the carry-plumbing-standard group, which names TestHCL34, is red for any delivery that selects it, and the fold's HCL-C-51 proof is silently outside the battery. Neither the implementer's proof list (internal/goal, internal/landing, cmd/metasystem, internal/refusal) nor the host bed runs include internal/testpolicy, so nobody saw it. If it stands: add the name to carry-landing-standard's tests list in testing.json.

Evidence: Ran in the reviewed tree extracted with git archive: `go test -count=1 -run '^TestHCL34PlanExecutesEveryFixture$' ./internal/testpolicy`. Output: FAIL, contract_test.go:209: owner metasystem/internal/landing/hcl_carried_test.go does not execute TestHCL51CarriedCounselorAppendBelongsToItsRow. Read: jq over testing.json lists the groups whose packages include internal/landing. landing-command-standard names TestLandingTestReceiptCanonicalCLIUsesSectionSelector and TestReceiptCanaryEnvironmentIgnoresAmbientProofControls. carry-landing-standard names eight TestHCL functions and omits TestHCL51. The scanner regex is contract_test.go:251, `^func (TestHCL[[:alnum:]_]+)\(`.

### HCL-C-67 (high, material)

Claim: The carried-crash-local leg is fold item 8's recovery proof (HCL-C-59), and its closing check cannot pass on a correct implementation. After the rerun, the leg requires origin's main to point at the commit the first run left locally (land-fixtures.sh:824-825, 'rerun did not push the preserved local commit'). In the carried fixture the goal ledger lives on origin's main (goal.sync-branch refs/heads/main, land-fixtures.sh:499, 613-616), so every goal transaction moves that branch. First, the leg crashes with SIGTERM after the intent entry. The exit trap then abandons the reservation, a ledger commit on main that the leg itself asserts at 800-806. The rerun's local: branch therefore rebases the carried commit onto the moved origin (land.sh:905-911), reserves again and rebases again (land.sh:762-770), so it pushes a different sha. Second, after the push, 'complete carried goal record' (land.sh:858) publishes the carried row as one more ledger commit on main, so origin's tip is never a code commit. The existing after-push crash leg reads origin's tip only before the record is written (land-fixtures.sh:845). The page's oracle is different: HCL-06-CRASH-BEFORE-PUSH-RESUMES (design 1236-1239) counts one commit on origin and one carrying row, and HCL-19-RECOVERY-KEEPS-REBASED-COMMIT (design 941-945) expects a rebased commit found by `git log --grep 'Carry: <opid>'`. The fold says the rerun 'keeps the rebased commit and pushes it'. The page kills at the pause with the reservation still open, while the leg's trap abandons it, so the leg drives the re-reserve branch instead. Either this leg is red on the host, or the local: recovery branch has no passing proof. If it stands: replace the tip-equality check with the page's oracle: exactly one commit on origin whose message has Carry: <word>, whose workspace projection equals the word's, and one carried row on the goal.

Evidence: Read, not run. land-fixtures.sh:783-831 (the intent-failure and crash-local block): the crash seam at 788 is METASYSTEM_LAND_FIXTURE_CRASH=before-push, local_commit is read at 794, origin is asserted not to equal it at 797-798, the closer is asserted at 800-806, and the rerun and the tip-equality check are at 815-825. land.sh:615-626 fixture_pause sends kill -TERM $$, and the EXIT trap at 242-261 abandons. land.sh:903-918 local: branch, 757-773 ensure_recovery_reservation, 843-863 finish_carried_publication. internal/goal/txn.go:689-736: a confirmed goal transaction pushes by compare-and-swap to the sync branch. The implementer's round-2 return, gaps entry 3: the leg 'was still inside process-owning setup after 30 seconds ... requires the coordinator's host run'. A search of artifacts/ and records/ for files newer than round 2's return.json found no bed log that names carried-crash-local.

### HCL-C-68 (medium, material)

Claim: The new carried match table (fold item 5, HCL-C-57) does not protect the rule that a group word cannot land beside an ordinary code refusal. That is the case where a carry would land a refusal the human did not name. The page's HCL-21-TWO-FAILURES-ONE-NAME (design 871-874) has two failures: the ordinary observation refuses missing-declaration and a group is red, and the word names either one alone. The table's row 'two failures under one group ask' drives something else: two failing groups under a passing ordinary observation. No row pairs a group word with a non-chain ordinary refusal. I replaced the allowed-ordinary rule in decideCarriedMatch (internal/landing/carried.go:322) with `true`, so a group word matches beside any ordinary refusal. All eight rows still pass. The production code is correct today, but the proof the fold required before landing does not guard it. If it stands: add a row with past=group:G, ordinary would-refuse missing-declaration, delivery with G missing or failing, and expect carry-refusal-mismatch.

Evidence: Ran on a scratch copy of the reviewed tree in $TMPDIR (the repository was not touched). Unmodified tree: `go test -count=1 -run '^TestHCL57' ./internal/landing` gives ok. Mutant, with line 322 changed to `allowedOrdinary := true`: the same command with -v gives PASS for all eight subtests, including two_failures_under_one_group_ask and closed_chain_lands_when_named_group_is_its_only_red. Read: hcl_carried_test.go:108-137. Every group-word row uses ordinary=pass or code chain-test-receipt-refused.

### HCL-C-69 (medium, material)

Claim: The seat's second carry works in the carried-second leg only because the leg deletes the record the human's debt payment wrote. On a real seat, paying the first carry's debt with goal accept-risk makes the next carried landing refuse. Design 07 (c) (design 1383-1395) says goal accept-risk for chain human-carried writes a counselor line, and it does so to records/counselor/accepted-risk-register.jsonl (internal/counselor/register.go:125). That file is tracked in this repository and classed as a record (path-classes.txt:32). The next landing first stops on the unstaged change (land.sh:397-400). If the human stages it, the carried classification refuses record-not-owned, because every records/counselor path except carried-landings.jsonl is refused in carried mode (internal/landing/observe.go:177-181). The leg runs goal accept-risk (land-fixtures.sh:1031-1035), then `rm -f` removes accepted-risk-register.jsonl and both lock files (1036-1038) before staging the second candidate. The fixture does not seed that register, so the rm hides the refusal instead of testing through it. The page says that after goal accept-risk for that obligation the next carry lands (HCL-08-DEBT-PAID-LANDS, design 1735-1736). The code refuses a verified human's chosen closer on the machine's own judgement. The other closer, a code critic of the commit, writes no counselor record and remains open. The code follows fold item 2 as worded, so this is a gap in that decision that its own leg surfaced; the coordinator decides the rule. Side note: the lock files are ignored in this checkout only through .git/info/exclude, which each clone sets for itself. If it stands: either admit an append-only accepted-risk-register line whose finding is carried:<commit>[:battery-red] of a carried row on the tree, or record the gap. In either case the leg must stop deleting the register silently.

Evidence: Ran: `git ls-files metasystem/records/counselor` lists accepted-risk-register.jsonl and misclassification-register.jsonl. `git check-ignore -v metasystem/records/counselor/carried-landings.jsonl.lock` names .git/info/exclude:10 as the only ignoring rule. Read: path-classes.txt:32 `install:records/ record`. observe.go:177-181 refuses any records/counselor path other than carried-landings.jsonl in carried mode. counselor/register.go:125 `appendRegisterLine(root, acceptedRiskRegisterSource, ...)`, and compute.go:14 sets that source to records/counselor/accepted-risk-register.jsonl. land-fixtures.sh:1031-1038. A grep of the goal discharge-review-obligation command code for counselor or records writes found none.

### HCL-C-70 (low)

Claim: This is the one path in land.sh that does not resolve under a prefixed installation (mandate 2), and it is trunk code outside this chain. check_rulings_id_mints reads staged paths from `git diff --cached --name-only`, which prints them relative to the repository top (metasystem/memory/rulings.md). It then hands that path back as a pathspec from the installation directory (land.sh:312), where git reads pathspecs relative to the working directory. Under this repository's metasystem/ prefix the diff is always empty, so the rulings id-mint check never fires here. Round 2 only added empty-array guards to the function. Recorded for the coordinator's backlog; not actioned in this chain.

Evidence: Ran from metasystem/: `git log --oneline -1 -- metasystem/memory/rulings.md` prints nothing. `git log --oneline -1 -- memory/rulings.md` prints a584e5c8c. Ran: diff of the function between 01124a60c:metasystem and the reviewed tree shows only the two `if (( ${#array[@]} > 0 ))` guards.

### HCL-C-71 (low)

Claim: carried-red-battery's trailer check is weaker than the page. It accepts `Carried-Battery: red missing=<anything> failing=<anything or nothing>`, so a group-list parse that wrote the wrong names would still pass. The page's oracle is `failing=<g>` (HCL-05-RED-BATTERY-LANDS, design 839-841). The obligation check (finding carried:<commit>:battery-red) and the advisory line check are exact. Recorded.

Evidence: Read: land-fixtures.sh:1019-1020 regex `^Carried-Battery: red missing=[^[:space:]]+ failing=`; commit.sh:747-759 json_csv parse.

### HCL-C-72 (low)

Claim: The base-judge fence table (TestHCL58BaseJudgeFenceOwners) has only positive rows. The page also expects a non-owner path under --judge base, and every row under --judge live, to pass the fence (design 922-925). Without those rows, a fence that asked on every candidate would pass the test. It also uses new probe.txt files instead of the page's named files. The fold scoped the table to positive rows, so this is recorded, not a finding against the fold.

Evidence: Read: hcl_carried_test.go:71-91; carried.go:377-418.

### HCL-C-73 (low)

Claim: reserve_carry validates the returned row before arming the exit trap's abandon (land.sh:712-714). If goal carrying exits 0 with a malformed output line, land.sh exits 3 without closing a row that may exist. The ask prints the raw output, so it does say why, but not that a row may be open. This is an edge case of malformed engine output only.

Evidence: Read: land.sh:699-715.


## Fold lines, traces and named checks

- {"command": "cat rounds/2/review.json; git archive fdfcd002b69a1a39fe681956d9226e245aea7127, faccdb3b25ea9fc1d9ca6fa10a87864fe7a2fbfa and 01124a60c:metasystem into $TMPDIR; diff -rq between them; git rev-parse c14ed599515dd2065f14a4dbdab274f647e22493:metasystem", "level": "ran", "observed": "review.json names reviewedTree fdfcd002b69a1a39fe681956d9226e245aea7127, which is the metasystem subtree of the implementer's staged tree c14ed599. Round 2 changed 15 files against round 1: goalsync_mutations.go and its test, landing_verbs.go and its test, goal hcl_carry_test.go, txn_test.go, verbs.go, landing carried.go, hcl_carried_test.go, observe.go, commit.sh, land-fixtures.sh, land.sh, path-classes.txt and testing.json. Against trunk 01124a60c, 56 paths differ, and all of them are inside the round's diffBoundary."}

- {"command": "fold line, host section item 1 (duplicate manifest line): cmp base and reviewed scripts/agents/path-classes.txt", "level": "ran", "observed": "Folded as decided. path-classes.txt is byte-identical to trunk, so only trunk's install:testing.json line remains."}

- {"command": "fold line, host section item 2 (ledger-path code in the ask)", "level": "read", "observed": "Folded as decided. commit.sh:641-647 prints '<code>: <refusal>' for a carried refusal, and the carried-ledger-path leg keeps its assertion on ledger-path-not-goal-verb (land-fixtures.sh:962)."}

- {"command": "fold line, host section item 3 (second seat's payload)", "level": "read", "observed": "Folded as decided. payload-b.txt is owned by the fixture-carry surface (land-fixtures.sh:462). The two-seat legs also moved to the run's real clock, with a +2h clock for expiry (674-682, 900), and the peer's test run gets METASYSTEM_GOAL_NOW (768)."}

- {"command": "fold line 1, HCL-C-50 (read the goal file prefix-aware): grep every git call in rt3 land.sh and commit.sh", "level": "read", "observed": "Folded as decided. land.sh:827 reads `git show \"$carried_ledger_tip:./plans/goals/$landing_goal.md\"`, and git resolves the ./ form from the installation directory at the top or under a prefix. land.sh has no other rev:path lookup. commit.sh's only one is `$settled_tree:${prefix%/}` (553), which is top-relative on a tree object and correct. The base judge builds under worktree/prefix (commit.sh:331). The Go fence strips the prefix before matching owners (carried.go:384-397). The carried-prefixed leg moves every tracked seed file under metasystem/ and clones into local-prefixed/metasystem (land-fixtures.sh:63-96, 666-668). It then runs the generic landing checks, including the eight pre-push lines, which call the git show. The one path that does not resolve under a prefix is trunk's rulings check (HCL-C-70, not material). I did not run the leg."}

- {"command": "fold line 2, HCL-C-51 (the seat's second carry)", "level": "read", "observed": "Folded in code as decided. observe.go:177-230 admits an append-only change to records/counselor/carried-landings.jsonl only when every added line decodes with id cl-<opid> and deep-equals counselor.CarriedLandingLine of the carried row with that opid on the ledger tree. Every other records/counselor path, new or existing, is refused record-not-owned. TestHCL51 drives the admission and the new-file refusal, but the testing contract never runs it (HCL-C-66). The carried-second leg deletes the accept-risk register the debt payment wrote (HCL-C-69). A second copy of an already-present line is not refused; not material."}

- {"command": "fold line 3, HCL-C-52 (the trap closes the row)", "level": "read", "observed": "Folded as decided. land.sh:246-257: when the intent entry exists the trap runs `goal carried --entry` first, then abandons and prints the abandon's refusal. Why it works: the takeover admits a descendant of the live owner (journal.go:325), a mutation refused as 'not on origin' goes through terminalFromMutate (txn.go:653-655), and AbandonCarrying skips terminal entries and terminalizes dead-owner ones (verbs.go:3563-3570). The carried-intent-failure leg asserts a terminal entry and the closer row (land-fixtures.sh:800-810); the implementer ran it and it passed."}

- {"command": "fold line 4, HCL-C-53 to HCL-C-56 (the four low defects)", "level": "read", "observed": "Folded as decided. HCL-C-53: land.sh:837-839 appends :battery-red, checked by the red-battery leg (1023-1024). HCL-C-54: land.sh:695 and 735 print ${carried_by#human:}, and goalsync_mutations.go:78-81 exits 2 on a human: prefix. TestHCL54 discriminates, because without the check the same arguments exit 3 at goalsync_mutations.go:90-93. HCL-C-55: landing_verbs.go:49-52 exits 2 for any judge other than live or base, and for base without --live-failure (TestHCL55). HCL-C-56: empty-array guards at land.sh:324-342, 377-381 and 387-390. carried-crash and carried-crash-local rerun without --staged-only (853-855, 818-820)."}

- {"command": "fold line 5, HCL-C-57 (match table and red-battery leg)", "level": "ran", "observed": "Partly folded. hcl_carried_test.go:108-137 has the eight named rows, with decisions typed as the refusal codes. A verify error now goes through decideCarriedMatch (carried.go:252-255). The rows assert decisions, not refusal text. The two-failures row does not drive the page's case, and a mutant passes all rows (HCL-C-68). The carried-red-battery leg lands group:fixture-carry on a failing group through commit.sh's red path, with --direct-fix register-carriage so the ordinary observation passes (land-fixtures.sh:711-714, 731, 758-760, 973-974). It asserts the red trailer, the :battery-red obligation and the advisory line (1018-1026). The list check is loose (HCL-C-71). TestHCL57 passes on the reviewed tree."}

- {"command": "fold line 6, HCL-C-58 (the fences)", "level": "read", "observed": "Folded as decided. The fence table drives the page's ten directories and five files, each as a new file under --judge base asking carry-base-judge-blind and naming the path (hcl_carried_test.go:71-91). The generation-zero test refuses carry-word-unproven in a plain root and admits in a runtimes=fake root (93-106), through carryWordAuthorityRefusal, which observeCarried calls at carried.go:184. The dead-live-judge and no-judge legs are named follow-ups, so not findings. The table has no negative rows (HCL-C-72, not material)."}

- {"command": "fold line 7, HCL-C-60 (the verb refusals)", "level": "read", "observed": "Folded as decided. The replay table covers the goal, then the fourteen fields, each naming the field (hcl_carry_test.go:136-164), through compareCarriedReplay, which the Mutate calls (verbs.go:3761). The supersede precondition table has the six rows: expired, consumed, foreign seat, non-carry target, in flight, push before row (166-228), through carrySupersedePrecondition (verbs.go:3274). The done table refuses an open word and passes an expired one (230-241, doneCarryRefusal at verbs.go:1210). The obligation and in-flight debt asks run at the word (243-270, carryDebtAskAt at verbs.go:3256). Abandon from another seat and the reservation's compare-and-swap are named follow-ups."}

- {"command": "fold line 8, HCL-C-59 (recovery leg carried-crash-local)", "level": "read", "observed": "Not folded as decided. The leg exists (land-fixtures.sh:783-831), but its closing check cannot hold, and it crashes after the intent entry with the trap abandoning, where the page kills with the reservation open (HCL-C-67). The ledger repair leg and the goal recover tests are named follow-ups."}

- {"command": "follow-ups named by the fold", "level": "read", "observed": "The implementer's gaps name the HCL-C-58 judge legs, the HCL-C-59 ledger repair and recover tests, the HCL-C-60 cross-seat abandon and compare-and-swap tests, HCL-C-61 and HCL-C-64 as deferred, and HCL-C-62, -63 and -65 as not actioned. None of these is raised as a finding here."}

- {"command": "mandate 3: trace every exit of land.sh between the reservation and the push", "level": "read", "observed": "Each exit leaves a closed row or prints why it could not, except the malformed-reservation edge case (HCL-C-73). Fresh path: the abandon is armed at land.sh:714. A fetch failure (934), carry-forward plumbing or rebase-conflict asks (663-697, whose explicit release is repeated idempotently by the trap because AbandonCarrying returns AlreadyApplied on an abandoned row, verbs.go:3586), commit.sh asks (937), a trailer check (939) and an intent creation failure (845) all run the trap with no entry, so it abandons and prints on failure. After the intent entry: the advisory's exits, the fixture seam, a failed push (854) and the moving-origin ask (852) run the trap, which terminalizes its own entry and then abandons. SIGTERM and SIGINT run the EXIT trap. SIGKILL leaves the row to expiry and goal recover, which the design accepts. The local: branch arms at 914, re-reserves at 762, and releases through the same trap. The origin: and ledger: branches never arm."}

- {"command": "mandate 4: the second carry's admission rule", "level": "read", "observed": "The admission accepts only complete appended lines whose id is cl-<opid> and whose content equals the carried row with that opid on the ledger tree (observe.go:193-230). A new goal-less file under records/counselor is refused record-not-owned (178-180); TestHCL51 drives both. Gaps: TestHCL51 is outside the contract (HCL-C-66), and the accept-risk register blocks the next carry (HCL-C-69)."}

- {"command": "mandate 6: what must not change: cmp trunk against reviewed for sync-transport.sh and path-classes.txt; round-2 diff list; testing.json diff", "level": "ran", "observed": "sync-transport.sh and path-classes.txt are byte-identical to trunk. Round 2 did not touch internal/humanauthority, cmd/metasystem/test.go, the receipt code or the workspace projection. goalsync_mutations.go changed only by the human: prefix refusal, so the identity gate is as the second read found it. commit.sh changed only the carried refusal print, and the record-failure checks run before observe, unchanged. testing.json changed only by adding test names to carry-goal-standard and carry-landing-standard. All 56 trunk-to-reviewed paths are within the diffBoundary, and trunk's a584e5c8c manifest line is intact."}

- {"command": "mandate 7: the four named checks of revision 6", "level": "ran", "observed": "HCL-C-33: the interleave and both trailer-without-row legs drive land.sh's own reservation, now on the run's clock with the peer's payload owned (land-fixtures.sh:870-949). Seat A still crashes and reruns instead of pausing, a named follow-up under HCL-C-64. HCL-C-25: the replay table drives the goal target, then the fourteen fields, through the function the Mutate calls. HCL-C-03: the fence table drives the page's fifteen owners, and the fence strips the installation prefix. HCL-C-34: TestHCL34PlanExecutesEveryFixture is red on the reviewed tree (HCL-C-66)."}

- {"command": "verdict", "level": "inferred", "observed": "Not closable as is. One fold: HCL-C-66 (name TestHCL51 in carry-landing-standard), HCL-C-67 (give carried-crash-local the page's oracle), HCL-C-68 (add the group-word-beside-a-code-refusal row) and HCL-C-69 (a coordinator decision on the accept-risk register in carried mode; at minimum the leg stops deleting it). HCL-C-70 to HCL-C-73 are recorded, not actioned."}
