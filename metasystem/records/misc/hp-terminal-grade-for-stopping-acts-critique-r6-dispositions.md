# hp-terminal-grade-for-stopping-acts, critique round six: dispositions

Chain hp-terminal-build1-20260909. The sixth critic
hp-terminal-crit6-20260909 (claude-opus-5) reviewed the fold
hp-terminal-build1-20260909-r8 at tree
057c7c36dd1dd4abfa37925901afb3fd2db0d138 and returned two material
findings and one note. The seat gate on the same tree was green: all
four packages, the live walk reaching process 1 through nine nodes with
the agent named, proof-grades passing its agent-descended branch, the
hook suite. Round nine folds all three.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| HPT-20 | accepted | The --arc cascade forms of release, park and unpark (releaseArcRequest, parkArcRequest, unparkArcRequest in internal/goal/verbs.go) gate on the bare presence of --by and never call requireHuman; cmd/metasystem/goalsync_mutations.go picks the proof-carrying builder only when the arc flag is empty. An agent shell with --by Wido --arc x parks human-opened goals, lifts a human's park, or releases another pair's claim with no walk run. The scenario only exercised the single-goal forms. | Round nine routes the arc forms through the same proof-carrying builder, puts every member act behind the same requireHuman row as its single-goal twin, refuses the cascade on any member's refusal with the runtime named, and adds the three arc forms to the proof-grades scenario (agent refusals everywhere; allows on an agent-free bed). |
| HPT-21 | accepted | Round eight surfaces a matched runtime only at the top of the tree, so an unreadable, changed, reused or cyclic ancestor above the agent renames a correct refusal to a bookkeeping outcome; the act is still refused but the runtime is never named and the scenario's exact-refusal check would fail on such a host. | Round nine makes an agent match the decisive fact: any exit after a match is AGENT_IN_AUTHORITY_CHAIN naming the runtime, the later uncertainty recorded on the proof without renaming the refusal; tests for a matched agent below each kind of failing ancestor. |
| HPT-22 | accepted | The lease classifier's parent loop has no `current > 0` guard, so the root's new "parent none, known" costs one lookup against process 0; harmless, nothing misclassifies. | Round nine adds the guard the other callers of identity.ParentPid have. |
