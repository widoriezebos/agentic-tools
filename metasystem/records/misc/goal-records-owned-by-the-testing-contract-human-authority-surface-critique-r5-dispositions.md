# goal-records-owned-by-the-testing-contract, human-authority surface, critique round five: dispositions

Chain ha-surface1-20260910, round five (the ParentPid sibling sweep).
The critic ha-surfacecrit5-20260910 (claude-opus-5) reviewed tree
ec8cafa2b45e218104a4194f0872c2aa51e9e675 and returned one material
finding and one note. Round six folds the finding.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| HAS-13 | accepted | Round five named the top-of-tree answer as the cause without reproducing an UNTRUSTED answer, and its three adapted walkers (the census ancestor search, the lease succession check, the hook-delegate check) are not on the lease classifier's path; the classifier already stopped at parent zero, so round four's and round five's engines classify identically. The seat confirmed: with the round-five engine all three beds pass detached from a terminal (35 scenarios), so the trigger is the receipt's own bed. | The cause is outside this chain: since 016b83e2 (receipt-beds-run-the-candidate-engine, landed 2026-09-10 14:41Z) the receipt's section beds export METASYSTEM_BIN pointing at the candidate engine, so the agent scripts copied into fixture scratch repositories run that engine instead of the copy enrolled there and classify callers UNTRUSTED; m1b's priority-one goal receipt-section-beds-do-not-export-the-engine fixes it, and until it lands no seat can take a sufficient receipt. This chain waits for that landing, then one dispatcher rebase round (main changed testing.json under it) and its critic, then lands. No round six on the classification. |
| HAS-14 | noted | The sweep is sound hardening with four tests that fail on round four's code; it guards states production does not reach today. | Kept. |
