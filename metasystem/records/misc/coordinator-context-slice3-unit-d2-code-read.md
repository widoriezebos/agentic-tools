# Independent read: coordinator-context slice 3, unit D2

Subject: the uncommitted diff in .claude/worktrees/ccb-d2 as of round 2. It covers 5 files, with 214 additions and 11 deletions.
Brief: metasystem/artifacts/reports/codex-ccb-slice3-d2-brief.md. Authority: design section 8c (D3-6, CCB-3-11, CCB-3-12, 8c.13, 8c.14) and brief v2 section D.
Paths below are relative to metasystem/ in the ccb-d2 worktree unless marked otherwise.

What I ran. I never ran either fixture bed. I made no commit and left the live worktree untouched.
Every mutation ran on scratchpad/d2-copy and was restored (cmp identical). The throwaway probe test was deleted.
METASYSTEM_BIN was unset for every Go command.

Round 2 note. I started on the 202-line round 1 tree. The builder then changed only the runner block in scripts/agents/supervision-hook-fixtures.sh (lines 851-891, 23 more lines). The other four files are byte-identical to the copy I tested, so the (b), (c) and (d) evidence still applies. I read the round 2 block, and it needs no new material finding. Its staging steps all have precedent: writing the accepted ref (this bed :291 and :369, brain-fixtures.sh:89), `goal release` (brain-fixtures.sh:94, dispatch-fixtures.sh:358) and `goal approve --fixture-human-authority`. The engine accepts that flag only for an exact fake-runtime root (cmd/metasystem/goalsync_mutations.go:664), which is why the runner switches metasystem.conf to fake for the approve and restores the same bytes before the claim. `bash -n` passes on the current file. The builder reports it saw a real `handoff recorded:` line after the change and a refusal without it. That means round 1's staging would have failed red on a correct tree, and round 2 fixes that.

Material findings: 2.

## Findings

| Id | Severity | Material | Claim | Evidence |
| --- | --- | --- | --- | --- |
| F-1 | medium | yes | The `instructions` surface edit widens standard selection for every docs, skills and roles file, not only the two touched files. It adds a bed that the brief says is red on trunk. | testing.json:25 adds context-standard, context-foundations-standard, section/supervision-and-census-fixtures and section/conformance-fixtures to `instructions.standard`. The two touched files already reach all four groups through the new context-budget paths (testing.json:16). Probe: a throwaway `testpolicy.Select` test on the copy, comparing HEAD testing.json with the worktree testing.json in standard mode. docs/orchestration.md went from 12 to 47 groups and roles/steward-continuation.md from 11 to 47; those rises come from context-budget. docs/some-other-page.md, skills/refactor/SKILL.md and roles/implementer.md each went from 11 to 15 groups, and that rise comes only from the instructions edit. The brief asks only that "the touched ... instruction inputs" select the four groups. Its environment note says section/supervision-and-census-fixtures fails four legs on the unmodified tip. So every instruction-only landing would now select a bed that is red on trunk. conformance-fixtures.sh:52-58 (the `instruction_standard` read and check) then locks that widening in. |
| F-2 | medium | yes | The orchestration sentence says to hand off when the session is "dead". The context role reports dead only over the ceiling. The design and the product line both say to hand off over the bound. | docs/orchestration.md:21: "says the session is dead". internal/steward/context.go:315-316: `roleDead` only when the call is "over the ceiling". context.go:321-326: over the bound the role stays alive and its reason reads "over the bound: run metasystem context handoff --root <installation>". Design plans/coordinator-context-stays-under-budget-design.md:255 (D3-6): "When the Stop hook's health line says the last call is over the bound". Brief v2 section D, orchestration paragraph: "says the last call is over the bound". The builder copied the D2 brief exactly, so the error is in the brief's wording, not the build. Still, the shipped instruction waits for the ceiling while the product asks for a handoff at the bound. The conformance grep (conformance-fixtures.sh:18) checks only the command string and cannot see this. |
| F-3 | low | no | If a handoff leg cannot read its judgment JSON, the bed exits without naming the leg. Runner failures now name their stage, but this read does not. | supervision-hook-fixtures.sh:967-968. The awk helper exits 0 even when the section is missing. `json get --value "" --field verdict.display` then fails under `set -e`, and the bed exits with only the engine's stderr. This fails closed and can never pass falsely. Round 2's trap (:851-856) names runner failures, though the first stage label, "claim", also covers the proc, lease and migrate steps. The same silent exit appears in conformance-fixtures.sh:25, :50, :52 and :60 when a line is missing. Proof on the copy: renaming the context-standard id gave rc=1 with no message (scratchpad/d2-proof-contract.sh). |
| F-4 | low | no | The runner pipes the Stop payload into the hook under pipefail, where every other Stop leg in this bed redirects from a file. | supervision-hook-fixtures.sh:886-890: `printf ... \| METASYSTEM_BIN=... bash supervision-hook.sh` inside a `set -euo pipefail` runner. The payload is about 90 bytes and the hook holds the read end open. A SIGPIPE needs the hook to exit before reading, and the leg would fail in that case anyway, so it cannot pass falsely. Commit 9bc768c0 moved this bed off pipes today. A file redirect would match the bed's convention. |
| F-5 | low | no | The foreign-session leg requires a block, which is stronger than the brief's "no allowance". | supervision-hook-fixtures.sh:972 requires `decision == block`. D2 brief item 3 asks only that the foreign leg get no allowance, and v2 asks for "unchanged normal behavior". Round 2 gives the predecessor an accepted claim, so a block is the likely normal verdict. If the normal verdict allows the Stop instead, the leg goes red on a correct tree. It can never go green on a broken one. I did not run the bed, so the seat's run settles it. |
| F-6 | low | no | The two touched instruction files now select the full fallback breadth of 47 standard groups. | testing.json:16 adds both files to context-budget. Its standard list must equal the fallback list (cmd/metasystem/context_verbs_test.go:486-491). The probe shows 12 to 47 and 11 to 47 groups. This meets the brief. It is a cost note for an efficiency seat, not a defect. |

## Adversarial layer: can a leg pass when the allowance is broken?

No. Every named mutation fails at least one leg, and no leg's own pass condition survives a break of its own case.
The exact display that :978 requires has one producer, internal/goal/turnverdict.go:399 (grep of internal, cmd and scripts; context_verbs.go:186 prints a different shape).

| Mutation | claude leg | codex leg | foreign leg | Reason |
| --- | --- | --- | --- | --- |
| Allowance branch deleted (turnverdict.go:393-414) | fails | fails | passes, by design | Display cannot equal the sentence at :978. The predecessor now holds an accepted claim, so the ordinary verdict should also block, which :976 refuses. |
| `if live` inverted to `if !live` | fails | fails | fails | Matching legs lose the allowance. The foreign display becomes "handoff recorded: ; end this session", which :973 refuses, and the missing decision fails :972. |
| `if live` replaced by `if true` | passes | passes | fails | The foreign session gets an allowance, caught at :972-973. |
| Seam called with mainId or "" instead of the session (turnverdict.go:394) | fails | fails | passes | No live handoff matches. |
| Session comparison removed in steward (handoff_capture.go:1086) | passes | passes | fails | The foreign display carries the nonce and has no decision. |
| ShouldBlock true on the allowance | fails | fails | passes | :976 requires no decision. |
| Display text changed | fails | fails | passes | Exact equality at :978. |
| Frozen facts dropped on the allowance return | fails | fails | fails | The judgment section is null, so :968 fails. The failure does not name the leg (F-3). |
| Closure removed in cmd/metasystem/goal.go:710-712 | fails | fails | passes | Same as deleting the branch. |

Shell hygiene. Helpers are defined before use: fixture-stop-report.sh is sourced at :18, the facts helper is at :783, the runner is written at :844 and the leg function is at :895. Its calls are at :984-986.
Cleanup. The runner is a synchronous child and leaves no process behind. Its EXIT trap is cleared at :891. `up` is forwarded to exit 0 and evidence-gc is a stub. The staged intent, lease, claim, ledger and bare origin all live under `$tmp/<leg>`, removed by the bed's EXIT trap (:81-92). Handoff capture reads no HOME or TMPDIR.
METASYSTEM_BIN is scoped to the hook command only. The fake-runtime switch happens only inside the leg's own installation.

## Checklist

- (a) PASS, with F-5 open to the bed. The three legs are unconditional top-level calls (:984-986). This bed is a linear script with no scenario list or dispatch case; the only `case` is an unrelated one at :551. So the legs run whenever scripts/validate-metasystem.sh:1047-1051 runs the script. Each leg builds its own installation. It stages an accepted claim and an admitted handoff from a live predecessor started with `exec -a <runtime>` (:952, runner :851-884); the runner's `set -e` and stage trap fail the leg on refusal, and the nonce is checked at :960-962. It drives the real hook (:886-890) and requires the helper's success first (:965-966). It reads `verdict.display` from the "Original turn verdict and frozen judgment" JSON (:967-968; internal/report/stoppresentation.go:1248). It checks the exact sentence (:978), the context role (:969) and the allow shape (:976-977). The foreign leg requires no allowance (:972-974). No leg reads the per-session .txt or the provider text for the sentence. I did not run the legs. The builder reports only a manual staging run, not the legs.
- (b) PASS. `bash scratchpad/d2-proof-b.sh` on the copy. Intact: rc=0. Role sentence replaced: "steward-continuation role does not verify a named context handoff", rc=1. Orchestration sentence removed: "orchestration instructions do not hand off a dead context", rc=1. Restored: cmp identical, rc=0. The added testing-contract assertion (conformance-fixtures.sh:21-65) failed with a named line on four mutations: internal/goal package, TestContextVerbUsage, instructions conformance group, conformance inputs. My first attempt ran under zsh and was discarded; the bash rerun is the evidence.
- (c) PASS on commands and flags. FAIL against the design on the trigger condition (F-2). steward-continuation.md:10-19 names `artifacts/agents/context/handoffs/<nonce>/state.json` (matches handoff_capture.go:615 and :833). It names `metasystem context verify --root <installation> --nonce <nonce>` and says to stop on anything but `ok` (verify prints `ok sha256=`, context_verbs.go:268). It says to continue the named goal with the live ledger and job records as authority. I built the copy with METASYSTEM_BIN unset. `context verify --help` lists -nonce and -root. `context handoff --help` lists -cancel, -json, -root and -scratch. The orchestration sentence sits in the bounded-read paragraph.
- (d) PASS on the listed checks. FAIL on scope (F-1). python3 json.load passes. Groups changed: context-standard (testing.json:52; orchestration input, internal/goal package, 30 tests) and section/conformance-fixtures (:75; orchestration input). Surfaces changed: context-budget (:16), instructions (:25) and residual (:26). The residual change is forced by the equality check at cmd/metasystem/context_verbs_test.go:486-491. No floor, targetMs, race, coverage, deep or critical value changed. All 30 listed tests exist, and the set equals exactly the tests added by 14fe2fec (24), 0995650b (2) and fcc948ae (4). On the copy, `go test -count=1 -timeout 40m -run '^TestContextTestingContractSelectsProof$' ./cmd/metasystem/` passes, and so do the committed-contract tests in internal/config.

## Verdict

Send back: (1) F-1, restore `instructions.standard` at testing.json:25 to its base value and remove the instruction_standard check at conformance-fixtures.sh:52-58, since context-budget already covers the touched files; (2) F-2, change "says the session is dead" at docs/orchestration.md:21 to "says the last call is over the bound" as D3-6 states, or amend the D2 brief if the seat meant dead.
