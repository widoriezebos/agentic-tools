# Code read register: delivery-candidate-is-the-workspace implementation, closing read

Job dcwl-cc1-20260911 (claude, claude-opus-5, code-critic) reviewed the final work round dcwl-build2-20260910-r2 (reviewed tree 32c57bfd75329926b6312da3713b00dda014a010) against design revision 3 (sha256 a9a2755936cc97ad5719c4829dfe5a874d88762f650ee5535c5b907910cb69f2 as landed at 6c28684cd with both coordinator notes). Material findings: 0 of 5. The critic's runtime is read-only; this register is projected verbatim from artifacts/agents/dcwl-cc1-20260911/rounds/1/return.json.

## DCW-14 (low)

**Claim.** Site 5 records the receipt tree, not the observed index, in the receipt's binding when the new workspace fast path is used (metasystem/internal/landing/testing.go:75-76; design section 3, site 5, and section 4.4 step 2). If the index moved by ledger-only paths while a battery ran, PrepareTestingReceiptPayload accepts it because the workspace projections are equal (testing.go:38 and the after-check). It then writes the proved tree T into all four binding fields (index before/after, worktree before/after) instead of the index trees it actually saw. The design does not say what the binding should hold in this case.

**Evidence.** Before this change the four binding values always equalled T, because any other value refused, so exact-case receipts are byte-identical to before. The readers require all four bindings to equal receipt.tree exactly (receipt.go:592-597), so recording the observed index would make the receipt unreadable. That is presumably why the implementer wrote T. It is not material because workspace equality was checked before the write, and sites 7, 8 and 9 each recompute coverage on their own. What changes if it stands: the coordinator records this as the design's decision (the binding names the tree the posture was accepted against), or asks for the observed trees in a separate field.

## DCW-15 (low)

**Claim.** A schema-2 receipt without the 'workspace' field whose tree is not the landing candidate now refuses with an empty diagnostic detail (metasystem/internal/landing/receipt.go:612-613, surfaced by metasystem/internal/landing/observe.go:184-190; design sections 2.3 and 4.4, 'readTestReceipt behaves as today' for such receipts).

**Evidence.** The old non-exact check ('schema-2 whole-project receipt does not project to landing candidate') was replaced by the shared coverage refusal, which uses the new error type. So observe.go now sets detail to 'testing-receipt: receipt does not cover the candidate: ' followed by an empty group list. The refusal code chain-test-receipt-refused and the verdict are unchanged, and nothing new is called for such a receipt, so behaviour is the same. Only the diagnostic changed, and it reads as if groups were about to be listed. If it stands, nothing needs to change; optionally keep the old wording for receipts without the field.

## DCW-16 (low)

**Claim.** The moved-path filter behind the proof-input-moved-after-receipt line is not the input digest's matcher (metasystem/cmd/metasystem/test.go:1194-1202; design section 4.2, 'with the same glob matcher the input digest uses'). testInputManifestContains understands only an exact path or a 'dir/**' prefix.

**Evidence.** A script scan found every input in metasystem/testing.json is either an exact path or ends in '/**', so this repository's contract gets correct lines. A contract with a pattern such as 'src/*.go' would print 'no declared path moved; the environment or a tool identity changed' when a matching file did move. Only the diagnostic line is wrong in that case; test verify still exits 1 and lists the missing groups, so the refusal stands. If it stands, nothing changes for this repository; a follow-up could reuse the digest's matcher.

## DCW-17 (low)

**Claim.** The input-move-refuses leg moves a different declared input than design section 8.2 names (metasystem/scripts/agents/land-fixtures.sh:583-588 and :1360-1393). The design names scripts/agents/coverage-delta.sh; the leg writes a new scripts/application-input.txt.

**Evidence.** The implementer declared this in its round-2 return. In the bed, scripts/agents/** is also an engine path (metasystem/internal/behaviorsurface/policy.v2.json enginePaths), so moving coverage-delta.sh would move the engine build identity and the enrolled engine's source projection as well. The leg still proves what 8.2 asks: the code, the group id and the moved path in land.out, origin still at the peer commit, and the local commit not pushed. The engine-path variant is covered in Go by metasystem/cmd/metasystem/landing_verbs_test.go:1590-1604 (coverage-ratchet.json under scripts/agents, through printMovedProofInputsWithoutCandidateEngine). I did not verify which check would have failed with the design's path. If it stands, the coordinator records the departure against 8.2.

## DCW-18 (low)

**Claim.** Three tests prove their claim only indirectly. (a) The workspace-path unit test (metasystem/internal/landing/testing_test.go:152) gives the reader a verify result whose group identities equal the receipt's, so the key path would accept even if the workspace comparison were broken. (b) records-move-lands greps a step banner that land.sh prints on every path through site 8 (metasystem/scripts/agents/land-fixtures.sh:1343). That site 8 reached step 5 (test verify) follows only by elimination from :1336 (the workspace projections differ) plus the successful landing. (c) The design-named engine determinism fixture (metasystem/cmd/metasystem/test_test.go:47-76) builds through a fixture go-build.sh script. Its equal digests prove the build identity and stamp are stable across a records-only move, not that the real Go compiler emits identical bytes.

**Evidence.** None lets a defect through on its own. For (a), the workspace path only skips a cheap identity comparison and runs after step 0, so the verify core's verdict still decides. For (b), design 8.2 asks land.out to show step 5; the elimination argument holds because the leg asserts the projections differ and the landing succeeds. For (c), the design claims byte identity only for an identical build context and relies on Go's -trimpath -buildvcs=false build for it; the variation the fixture targets, the old whole-tree stamp, is what it tests. If it stands, nothing needs to change.

## Evidence the critic recorded

- [ran] git diff-index --cached --stat 32c57bfd75329926b6312da3713b00dda014a010 (in the dcwl-build2-20260910 worktree); git diff --stat 6c28684cd 32c57bfd75329926b6312da3713b00dda014a010 => The build worktree's staged index equals the reviewed tree exactly (no difference). The diff from its base commit 6c28684cd to the reviewed tree is 30 files, 2322 insertions and 209 deletions, matching the implementer's declared boundary of 30 paths. No path under plans/ changed.
- [ran] git show 32c57bfd:metasystem/plans/delivery-candidate-is-the-workspace-design.md | shasum -a 256; git log -- that page => The design page in the reviewed tree (and on main) hashes to a9a2755936cc97ad5719c4829dfe5a874d88762f650ee5535c5b907910cb69f2, not the brief's 1622cf7c.... Its only revision-3 commit is 6c28684cd, which carries both coordinator notes (17:50 and 22:50). That is the page I read.
- [read] Mandate 1, the three walls: read metasystem/internal/landing/receipt.go:522-628, metasystem/internal/landing/observe.go:181-191, metasystem/cmd/metasystem/landing_verbs.go:41-53, metasystem/cmd/metasystem/test.go:222-236 and :1044-1077, metasystem/scripts/agents/land.sh:365-411 and :117-128 => readTestReceipt runs only path, strict-decode and structure checks (526-563) before step 0. Step 0, the verify-core callback, runs at 566-571 for a receipt carrying 'workspace'. It comes before the workspace recomputation (572), the engine identity (575), sufficiency (582), attempt ownership (585), the four bindings (592-597), the exact/W/key coverage (598-614) and the index/worktree posture (615-627). No fast path runs before it. verifyTestingReceipt (456-475) refuses on a core error, an insufficient result, or a judged tree whose installation subtree is not --tree, with the three details the design names. The callback is set only with --test-receipt and no --recertification (landing_verbs.go:46-51). An empty tree resolves to the staged index (test.go:228-229). commit.sh passes --goal to both test verify (:272-273) and landing observe (:462-466). A receipt without the field calls nothing new: land.sh sets the flag only when 'json get --field workspace' succeeds (:370-373) and refuses with today's sentence before any new verb (:393-396), and in Go verifiedAvailable stays false, so neither the callback nor the W helpers run. land.sh runs site 8 with errexit off (set +e at :128), so step 5's exit status is captured. Site 8's step-5 first sentence (:409) is byte-identical to the legacy sentence (:394). Site 9 (verify_current_testing_proof) is unchanged.
- [read] Mandate 2, engine identity: read metasystem/cmd/metasystem/test.go:503-517, :532-560, :587-700, :742-747, :1204-1250, :1376-1390; metasystem/internal/proofrun/execution_context.go:92-157; metasystem/internal/behaviorsurface/policy.v2.json:3-10; grep GOFLAGS in metasystem/scripts/agents/go-build.sh => candidateEngineBuildIdentity takes the ENGINE projection tree of the installation subtree. It is built with ls-files -s over enginePaths (cmd/**, internal/**, scripts/agents/**, go.mod, go.sum, records/misc/goals-migration-manifest.md) and update-index --index-info into an empty index. It adds ToolchainClosureIdentity (the reported go version and go env tuple, the SHA-256 of the symlink-resolved go executable's bytes, and GOROOT/VERSION, with a missing VERSION returned as an error), the platform, and a fixed build-context string, and commits it with fixed author, committer, encoding and 2000-01-01 dates. No whole-project tree or workspace projection enters it. GOFLAGS=-mod=readonly is set in the build environment (515), and go-build.sh does not override GOFLAGS. The binary is stamped with that commit because bindMaterializedCandidateCommit moves the detached HEAD to it. Build and verify compute the identity under environments that differ only in proof-custody variables (inheritedTestingEnvironment), which change neither go version nor go env. retainedCandidateEngineDigest matches only on identity version 2, an equal build identity, sufficiency and validity (1205-1215). Its receipt fallback reads every receipt file but still requires an equal build identity and receipt/testing agreement. No match is made on the whole-project tree or its projection. The worker's check that the engine file's digest equals the request's (test.go:964-969) is unchanged. validateTestingReceiptEngineIdentity (testing.go:192-208) keeps versions 0 and 1 and requires, for version 2, a non-empty build identity equal between the receipt and its testing result. Section-group identity is stable across a ledger-only move: none of the seven excluded paths is an engine path, and the digest the verifier folds in is found by build identity.
- [read] Mandate 2b, the compatibility shim: grep policyProbeWorkerEnvironment; read metasystem/cmd/metasystem/test_protection.go:19-40, :225-240, :350-375; metasystem/cmd/metasystem/test.go:935-1000; metasystem/internal/proofrun/test_build.go:205-215; metasystem/internal/proofrun/test_result.go:75-82 and :279-298; grep NewTestResult callers => This is a door kept open for the old engine only; it does not weaken the current engine. The variable is set only by the policy engine's own probe launchers (test_protection.go:238 for 'test plan', :373 for 'test worker') when they run the frozen public-version-1 corpus against the candidate engine. A current engine always sends the build identity: every testingRunRequest caller passes candidateEngine.Commit, and runFrozenWorkerProbe copies the outer value (test_protection.go:355-357). So only an old policy engine's probe can send a digest-only request. The worker still authenticates (test.go:976-980) and still checks the engine digest (966). NewTestResult marks version 1 when the request lacks the field (test_build.go:207-210). Its only callers with an empty field are the probe-gated worker, since all runTestRun and verify callers pass it. A version-1 result cannot become delivery evidence under a current engine for four reasons. PrepareTestingReceiptPayload requires version 2 and a build identity (testing.go:30-31). ExactReusableTestResult compares version and build identity (test_result.go:285-286). retainedCandidateEngineDigest requires version 2. And every group identity folds the policy engine digest, so no old-engine attempt matches after the cutover. test_result.go:79-81 still refuses a version-2 result without a valid build identity.
- [read] Mandate 3, exact reuse and publication: read metasystem/internal/proofrun/test_result.go:266-312, metasystem/internal/landing/receipt.go:183-240, metasystem/cmd/metasystem/landing_verbs.go:120/178/215, metasystem/internal/landing/testing.go:27-99 and :102-121 => ExactReusableTestResult no longer compares CandidateTree, BaseCommit, PolicyBaseCommit or PlanDigest. It compares the four contract and policy digests, the candidate engine digest, version and build identity, purpose, modes, required and selected groups, sufficiency and validity, and every selected group's execution identity against the current identities. PublishCommittedReceipt takes the accepted index tree, refuses anything that is not a tree id, and is given the landing --tree at all three call sites. It compares the index to that tree and the working posture exactly to receipt.tree. Site 5 compares the index exactly and then by workspace projection, before and after owner validation (testing.go:38, the after-check, and :87-99), keeps the working posture exact, and keeps its signature, so proof_run.go is untouched. CreateTestingReceipt goes through PrepareTestingReceiptPayload, so composed receipts carry 'workspace' too.
- [read] Mandate 4, the projection: read metasystem/internal/gittree/gittree.go:284-356, metasystem/internal/landing/registers.go, metasystem/internal/landing/receipt.go:477-492, and the gittree and registers tests => FilterTreePrefixes is a new sibling. FilterTree, Snapshot, StagedTree and SnapshotRelevant are untouched (the gittree diff only adds lines). It lists files with ls-files -z from the toplevel against an isolated index and removes them with update-index -z --force-remove --stdin. An absent path is a no-op. Its test covers a directory prefix with siblings kept, an absent prefix returning the same tree, a file mixed with a directory, and equality with 'git rm -r -f --cached --ignore-unmatch'. WorkspaceExclusions returns the seven paths sorted, as a copy. ProjectWorkspaceTree joins the installation prefix (registers.go:48); InstallationWorkspaceTree does not. The nested and toplevel test proves both. Every reader recomputes the receipt's workspace field and refuses on a different tree or a different exclusion list (validateTestReceiptWorkspace, called from readTestReceipt:572 and PublishCommittedReceipt).
- [read] Mandate 5, the beds: read the new parts of metasystem/scripts/agents/land-fixtures.sh (make_leg receipt branch, the arm/disarm runner lifecycle, scenarios at :1235, :1313, :1360, :1398), metasystem/scripts/agents/fixture-bed-scenarios.sh, metasystem/scripts/agents/fixture-budget.sh:270-274, metasystem/scripts/agents/fixture-bed-scenarios-fixtures.sh, metasystem/testing.json, metasystem/scripts/validate-metasystem.sh; git merge-base --is-ancestor 016b83e2f 6bc19ba1c; git log 6bc19ba1c..6c28684cd -- metasystem/cmd metasystem/internal => The fixture commit stub refuses unless the observer's verdict is 'pass bar=a', so site 7 is really exercised. ledger-move-lands takes the workspace path at sites 8 and 7 and includes the unstaged and staged drift probes. records-move-lands proves the workspace projection differs and takes the key path at site 7. input-move-refuses fails at site 9 with the code, group and path. receipt-cutover builds the old engine from 6bc19ba1c, which contains 016b83e2f and has no engine-path change between it and the base. It proves the old receipt lands exactly (with the control observed as 'pass bar=a'), that the moved landing refuses with the byte-exact sentence and no unknown-verb text, and that the copied field-bearing receipt is refused by the old engine at the same path. No leg would pass as a whole on the base tree. Cutover proofs 1 and 2 are regression guards meant to pass on base, and proof 3 plus the version-2 assertion fail there. The harness self-initialises the budget when the scale is not a positive integer (:65), scales the new bed-scenario cap (1 to 120 seconds), runs each child in its own process group under set -m with stdin from /dev/null, polls to a deadline, logs the ceiling line, reaps the group with TERM, a five-second grace, KILL, wait and a check that the group is empty, and returns rc=124. The parent cleanup reaps the same way. The new bed has the four scenarios with the design's assertions. testing.json adds one surface, one section group and a cadence entry. validate-metasystem.sh adds the fixture-list, syntax-check and run_section rows.
- [read] Mandate 6, what must not change: git diff 6c28684cd 32c57bfd --stat and read of the gittree, testing.json and commit.sh paths => commit.sh is not in the diff. No group was removed from testing.json and the goal-records surface line is unchanged. The recertified path stays exact, because the callback is nil when --recertification is set, so a field-bearing receipt gets field checks and the exact rule only. No working-tree posture is filtered: the worktree is compared exactly at sites 5, 6 and 7. The refusals introduced by 1b12f5349 ('Make testing risk-selected and retain proof for landing') are still made. The only relaxations are the design's own: the index posture at sites 5 and 6, and coverage at sites 7 and 8. The legacy version-0 digest fallback was removed, which fails closed.
- [ran] python3 scan of metasystem/testing.json group inputs for glob forms other than an exact path or a trailing /**, and for inputs under plans/, records/ or memory/receipts.log => No input uses any other glob form, so the simplified moved-path matcher agrees with the input digest for the shipped contract. No group declares a ledger path as input.
- [read] DCW-01 trigger (key is the selected component identities; the workspace projection is the floor; a records-only move lands; a declared-input move refuses) => Met. The coverage order exact, then workspace, then key is at receipt.go:598-614, and site 8's steps are at land.sh:389-411. records-move-lands (land-fixtures.sh:1313) lands a records-only move. input-move-refuses (:1360) refuses a declared-input move, with the path changed as in DCW-17. The follow-up for pushing an existing commit remains outside this goal's DONE.
- [read] DCW-02 trigger (site 9 takes no new flag; sites 7 and 8 gated on the field; a cutover leg) => Met. verify_current_testing_proof is unchanged and test verify gained no flag. land.sh:370-373 and :393-396 gate the new verbs, and receipt.go:566-571 gates the callback. The cutover leg is land-fixtures.sh:1398.
- [read] DCW-03 trigger (retained engine evidence keyed by build identity; records-only success; engine-input refusal) => Met. test.go:1204-1250 matches on build identity only. test_test.go:47-76 shows a records-only move keeps the commit and digest. landing_verbs_test.go:1590-1604 shows an engine-path input move refuses with the named line.
- [read] DCW-04 trigger (-mod=readonly forced; toolchain and platform bound; byte identity only in an identical context; a fixture) => Met. GOFLAGS=-mod=readonly is at test.go:515. The platform and build context are in the identity at test.go:617-618 and the toolchain closure at execution_context.go:116. The fixture at test_test.go:47-76 builds through a stub build script (DCW-18).
- [read] DCW-05 trigger (end-to-end legs on the production shell-to-engine path with field-bearing receipts; the cutover leg; a real per-leg watchdog) => Met. Four receipt legs run with enrolled fixture runners (arm_receipt_runner) and end with steward disarm and a survivor check. The per-scenario ceiling is enforced at fixture-bed-scenarios.sh:77-96.
- [read] DCW-06 trigger (the two legacy ledger files excluded) => Met. registers.go:12-18 lists plans/goals.md and plans/goals-accepted.json, registers_test.go checks the list, and docs/project-rules.md names both.
- [read] DCW-07 trigger (the counselor rationale corrected) => Met. This was a design-text correction only, and the code has nothing to do for it.
- [read] DCW-08 trigger (the observer refuses before every fast path on an error, an insufficient result or another tree; named details; a deleted-attempt test) => Met. The refusal is at receipt.go:566-571 with verifyTestingReceipt at 456-475, and observe.go:184-190 copies the detail. testing_test.go:213 covers all three conditions on an exact tree. landing_verbs_test.go:1640-1662 deletes the attempt and gets the sufficiency detail naming both groups.
- [read] DCW-09 trigger (site 5's residual is a failed attempt; no 'success without receipt' lifecycle; proof_run.go named) => Met. PrepareTestingReceiptPayload keeps its signature and only widens the index comparison (testing.go:38 and the after-check). No lifecycle change was made, and proof_run.go is untouched as the design's section 11 says.
- [read] DCW-10 trigger (a real older engine; old receipt exact success, moved-tip exact refusal, rejection of a field-bearing receipt) => Met. The old engine is built from 6bc19ba1c at land-fixtures.sh:1408. The three proofs are at :1443-1459, :1500-1532 and :1461-1474. Per the 22:50 coordinator note, the control is the old engine's own receipt at the same path.
- [read] DCW-11 trigger (the shared harness initialises the budget itself; standalone and inherited starts covered) => Met. The initialisation is at fixture-bed-scenarios.sh:64-66, and the budget-standalone and budget-inherited scenarios are in fixture-bed-scenarios-fixtures.sh.
- [read] DCW-12 trigger (a process group per scenario; TERM, grace, KILL, reap; the same on parent signals; a TERM-ignoring descendant test) => Met. fixture_bed_reap_group is at fixture-bed-scenarios.sh:24-46, set -m at :81, and the cleanup reaps at :52. The ceiling-reaps-group and signal-reaps-group scenarios assert the three group lines and that no process survives.
- [read] DCW-13 trigger (the toolchain closure bound; a same-report, different-bytes unit test) => Met. ToolchainClosureIdentity is at execution_context.go:116-157. The test at execution_context_test.go:75 uses a fake go script: the identity differs from the real toolchain, two copies give the same identity, and a missing VERSION file is an error.
- [inferred] Verdict => Closable as is. No material findings; DCW-14 to DCW-18 are recorded as non-material.

## Gaps the critic named

- The runtime is read-only, so the register file metasystem/records/misc/delivery-candidate-is-the-workspace-code-read-r1.md was not written; this return is the register for the coordinator to place.
- I did not run 'metasystem validate conformance --stage review --job' because the runtime is read-only. I computed the diff with git between the build branch's base 6c28684cd and the reviewed tree, and confirmed the build worktree's staged index equals the reviewed tree.
- The design page I read hashes to a9a2755936cc97ad5719c4829dfe5a874d88762f650ee5535c5b907910cb69f2 (revision 3 plus both coordinator notes, commit 6c28684cd), not the brief's 1622cf7cfc4fd10259e23eabb0fdae2d1ba7a59a67666cea679f2b951bcca83b.
- As the brief required, I ran no beds and no Go tests, so every behavioural claim here comes from reading the code.
- I did not measure whether real Go builds are byte-deterministic. The design-named determinism fixture uses a stub build script (DCW-18).
- The reason for DCW-17 (why the design's coverage-delta.sh path was swapped out in the bed) is the implementer's stated one. I did not confirm which check would have fired.

## Coordinator dispositions (m1d, 2026-09-11)

No material finding; the chain closes on this read. The five low findings are carried as follow-ups, none changes the goal's DONE:
- DCW-14 (binding fields hold T on the workspace fast path): recorded as a follow-up line for the receipt schema ("record the observed index beside T"); readers require the four bindings to equal the tree, so the choice is the only readable one today.
- DCW-15 (empty detail on a field-less non-exact receipt): a one-line message fix, follow-up.
- DCW-16 (moved-path filter understands exact paths and `dir/**` only): true for every input in this repository's contract; follow-up when a contract declares a finer glob.
- DCW-17 (input-move leg moves scripts/application-input.txt, not coverage-delta.sh): accepted; coverage-delta.sh is an engine path in the bed, so moving it would move the engine identity too; the leg proves what 8.2 asks.
- DCW-18 (three indirect tests): accepted as stated; none lets a defect through alone; follow-up to tighten.
