# human-acts-derive-their-lineage, critique round one: dispositions

Chain hal-build1b after its round-one build, critic hal-critic1 (claude,
claude-fable-5-1, with a shell: seven of twelve evidence entries ran),
reviewed tree 32d552665ef1c6b072ad2d27ccd195029194509e. The
orchestrator's seat-side proof: the cmd/metasystem package, vet, gofmt,
bash -n, and the full goal-cli fixture bed green with the new
human-lineage leg.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| F-1 | accepted | Real: goal enroll-terminal builds its request with the placeholder name "terminal", so on an unenrolled checkout with no lineage it takes the new human path and refuses by naming itself as the repair; on an enrolled checkout it derives from the previous generation while publishing the next. | Folded in round two (plans/human-acts-derive-their-lineage-fold-r2-brief.md): enroll-terminal derives its lineage from the enrollment it publishes, after Enroll returns. |
| F-2 | accepted | Real and the brief's premise was too wide: only six verbs obtain a terminal proof; park, done, edit and the rest accept a name and never prove ancestry, so with the variable unset any shell passing a name would record a terminal lineage without proof, where before it refused. Decision: the derivation itself requires the terminal proof (humanauthority.Prove on the invoking process), so an agent shell cannot derive and a person at the terminal can, for every verb. | Folded in round two: syncReq proves the terminal before deriving; a failed proof refuses with a sentence naming the terminal. |
| F-3 | accepted | The fixture's opid-differs check cannot fail (fresh ulid); the journal grep is the real assertion. | Folded in round two: the leg compares the opid's lineage-hash suffix instead. |
| F-4 | noted | A corrupt enrollment file gets "has none"; enroll-terminal then reports the real cause. | Folded in round two as a wording fix: the refusal carries the reader's error. |
| F-5 | noted | discharge-review-obligation requires --by but passes an empty name to syncReq; outside the brief. | Folded in round two (one-line: pass the name). |
