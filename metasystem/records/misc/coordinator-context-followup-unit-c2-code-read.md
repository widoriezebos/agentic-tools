VERDICT: land

Closing read (Opus) of coordinator-context-stays-under-budget, follow-up unit C2 (the intent path), amendment 8c.15, decision D15-9. Worktree /Users/wido/LocalStorage/GitHub/agentic-tools-m1e/.claude/worktrees/ccb-c2 at HEAD c15d23be with five uncommitted files. Every probe and mutant ran in private copies built from git archive of HEAD plus the five changed files, each sha256-checked against the live file. Material findings: 0. Non-material findings: 2.

### F-1. Severity low. Material no.

Claim: `TestContextHandoffVerb` now runs every branch through the symlink, not only the JSON branch that the page's P2 row names.

Evidence: context_verbs_test.go:651-654 replaces `container` with the link and installs `useContextHandoffIdentity(t, filepath.Join(link, "metasystem"))` once for the whole test, so the text record, the `HANDOFF_REFERENCE` refusal and the wrong-delegate branch also go through the link. No assertion was removed or weakened; the only other change in the test is the stub's expected root. Direct, unlinked roots stay covered by `TestContextVerifyAndCancel` and `TestContextHandoffCancelByHuman`, which still call `useContextHandoffIdentity(t, root)` and record through the real container. Because the stub is installed once per test, the page's wording is hard to follow per branch. Not material.

### F-2. Severity low. Material no.

Claim: nothing tests D15-9's rule that `IntentPath` stays empty on every error return, so moving the assignment ahead of staging, preparation and the readback survives.

Evidence: my early-assignment mutant removes the assignment at handoff_capture.go:1019 and puts the same statement directly after `result.Intent = intent`. That is before the `StageHandoffIntent` error check, supersession, preparation and `readExactLiveIntent`. Every steward test matching `Handoff` still passed (ok 89.3s), and so did `TestContextHandoffVerb`. The page's rule table gives P1 only a success-path witness. The two tests that compare the whole result with the zero value (handoff_capture_test.go:430, :546) cover refusals that return before anything is staged. The code conforms: the assignment is the last statement before the only nil-error return, and its one caller returns at context_verbs.go:201-203 without reading the result on error. Not material. An optional strengthening would assert `result.IntentPath == ""` in a test that fails after staging, for example through the `beforeHandoffPrepare` seam.


### Mutation table

Each mutant was applied alone in its own private copy, and the applier refused any pattern that did not match exactly once. `git diff -U0` in each copy showed exactly the intended line. Tests ran with `-count=1 -timeout 40m` under GOCACHE=/tmp/opus-c2-gocache and GOTMPDIR=/tmp/opus-c2-tmp, with METASYSTEM_BIN unset. The unmutated baseline, `-run 'Handoff|HCL03|ContextVerify|Revival|Prune|Intent'` over the three packages, was green: steward ok 134.8s, refusal ok 0.58s, cmd/metasystem ok 10.9s.

| Rule | Mutant, alone | Test run | Result | Failing line and observed value |
| --- | --- | --- | --- | --- |
| P1 | handoff_capture.go:1019 assignment removed | TestHandoffResultNamesTheLiveIntentPath | killed | handoff_capture_test.go:718 intent path="" want=".../artifacts/agents/steward/intents/6200000000000003.json" |
| P1 | same removal | TestContextHandoffVerb | killed | context_verbs_test.go:679 JSON handoff stdout carries "intentPath":"" |
| P1 (argument root) | handoff_capture.go:1019 joined from `intentsDir(stateRoot)` instead of `intentsDir(root)` | TestHandoffResultNamesTheLiveIntentPath | killed | handoff_capture_test.go:718 intent path="/tmp/.../root/artifacts/..." want="/private/tmp/.../artifacts/..." |
| P1 (argument root) | same | TestContextHandoffVerb | killed | context_verbs_test.go:686 intent path="/tmp/.../container/metasystem/..." want canonical "/private/tmp/.../metasystem/..." |
| P2 | context_verbs.go:206 back to `filepath.Join(stateRoot, "artifacts", "agents", "steward", "intents", result.Nonce+".json")` | TestContextHandoffVerb | killed | context_verbs_test.go:686 intent path="/tmp/.../container/metasystem/..." want canonical "/private/tmp/.../metasystem/..." link="/tmp/.../container" |
| D15-9 empty on error (no named rule) | handoff_capture.go:1019 moved to directly after `result.Intent = intent`, ahead of the staging error check | steward `-run Handoff` (every Handoff test) | survived | F-2 |
| D15-9 empty on error (no named rule) | same | TestContextHandoffVerb | survived | F-2 |
| P3 | register.go:47 `HANDOFF_HUMAN_UNPROVEN` Site 1055 back to 1053 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_HUMAN_UNPROVEN site "handoff_capture.go:1053" is not an emission line |
| P3 | register.go:48 `HANDOFF_LAUNCH_IN_FLIGHT` Site 473 back to 472 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_LAUNCH_IN_FLIGHT site "handoff_capture.go:472" is not an emission line |
| P3 | register.go:49 `HANDOFF_NOT_FOUND` Site 1207 back to 1205 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_NOT_FOUND site "handoff_capture.go:1205" is not an emission line |
| P3 | register.go:50 `HANDOFF_NOT_HOLDER` Site 288 back to 287 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_NOT_HOLDER site "handoff_capture.go:287" is not an emission line |
| P3 | register.go:51 `HANDOFF_NOT_LIVE` Site 1126 back to 1124 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_NOT_LIVE site "handoff_capture.go:1124" is not an emission line |
| P3 | register.go:52 `HANDOFF_NO_GOAL` Site 362 back to 361 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_NO_GOAL site "handoff_capture.go:361" is not an emission line |
| P3 | register.go:53 `HANDOFF_OTHER_SESSION` Site 1076 back to 1074 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_OTHER_SESSION site "handoff_capture.go:1074" is not an emission line |
| P3 | register.go:54 `HANDOFF_OTHER_PENDING` Site 957 back to 956 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_OTHER_PENDING site "handoff_capture.go:956" is not an emission line |
| P3 | register.go:55 `HANDOFF_REFERENCE` Site 232 back to 231 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_REFERENCE site "handoff_capture.go:231" is not an emission line |
| P3 | register.go:56 `HANDOFF_STATE_MISMATCH` Site 1239 back to 1237 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_STATE_MISMATCH site "handoff_capture.go:1237" is not an emission line |
| P3 | register.go:57 `HANDOFF_STATE_TOO_LARGE` Site 796 back to 795 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_STATE_TOO_LARGE site "handoff_capture.go:795" is not an emission line |
| P3 | register.go:58 `HANDOFF_UNOBSERVABLE` Site 317 back to 316 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_UNOBSERVABLE site "handoff_capture.go:316" is not an emission line |
| P3 | register.go:59 `HANDOFF_WAIT_IN_FLIGHT` Site 416 back to 415 | TestHCL03HandoffCaptureSitesAreEmissionLines | killed | register_test.go:103 row HANDOFF_WAIT_IN_FLIGHT site "handoff_capture.go:415" is not an emission line |

The thirteen P3 reverts ran one at a time in one copy. After each run the register was restored; after the loop it matched the unmutated copy byte for byte (`cmp`), and the `TestHCL03` group passed.

### Checks with no finding

Conformance. The five changed files are exactly the Boundary, and there are no untracked files; the brief and result sit under the gitignored artifacts/ directory. Numstat is 52 additions plus 15 deletions, 67 lines, under the 400 ceiling and close to the page's estimate of about 70. handoff_capture.go changes two lines: the `IntentPath string` field on `HandoffResult` (:93) and the assignment (:1019). context_verbs.go changes only the projection (:206), and the file no longer names the intents layout (a grep for `intents` finds nothing). `contextHandoffOutput` keeps its four fields and JSON tags (:45-50). There is no change to `CancelHandoff`, `handoffCancelReason`, `HandoffCanceller`, `HandoffHumanAct`, the cancel tests, `Intent`, `BriefPath`, `StageHandoffIntent`, `PrepareIntent`, any fixture script, any role contract, docs or testing.json. No exported `IntentPath` function exists. `gofmt -l` and `git diff --check` report nothing.

Empty on error, set after the exact live readback. Every return in `Handoff` before :1019 returns either `HandoffResult{}` or the local `result`, whose `IntentPath` is never assigned there. The assignment follows the `readExactLiveIntent` error check (:1015-1018) and precedes the only nil-error return (:1021). `root` and `nonce` are not reassigned between the readback and the assignment, so the joined string is the exact path `readExactLiveIntent` opened (:1025). The verb returns at context_verbs.go:201-203 on any error, before it prints.

Canonical root. `handoffCanonicalRoot` (:136-149) applies `filepath.Abs`, `filepath.EvalSymlinks` and `filepath.Clean` (handoff_state.go:154-164). A steward probe in a private copy called `Handoff` with `root/`, `./root`, `<absolute link>/` and `root//`, from a working directory holding a symlink to the installation. Every `IntentPath` was absolute and equal to the `EvalSymlinks` root joined with `artifacts/agents/steward/intents/<nonce>.json`, under the same prefix as `StatePath`. A verb probe ran `context handoff --json` with `container/`, `./container`, `<link>/`, `<link>/metasystem/`, `container/metasystem` and `container/../container/`. The classified root stayed `/tmp/.../container/metasystem`, while every printed `intentPath` and `statePath` shared the canonical `/private/tmp/.../metasystem` root, and the projection had four keys each time.

Other readers agree. In the same probes, after each handoff through a non-canonical root: `os.SameFile` held between the printed path and the same nonce joined under the link. `LiveHandoffForSession` through the link, which is the Stop allowance's reader at cmd/metasystem/goal.go:711, returned the new nonce. `LiveIntents` through the link, which `CompleteRevival` reads (revive.go:100), listed exactly that nonce. Supersession removed the predecessor's file at its printed path and left its tombstone under `cancelledDir` reached through the link. `PruneContext` through the link, 1000 hours later, kept the live intent and its state directory. `CancelHandoff`, `LiveHandoffForSession` and `PruneContext` canonicalize with the same helper, so they build the same string `Handoff` prints. The `ls` at scripts/agents/dispatch-fixtures.sh:5473 lists the intents directory of `$steward_repo` after a refused `steward revive` and never reads the verb's output. goal-cli-fixtures.sh:1083 and supervision-hook-fixtures.sh:2261 run `find` over the directory, and supervision-hook-fixtures.sh:1000 records through the text form. None of them parses `intentPath`.

JSON shape. The object keeps `nonce`, `statePath`, `digest` and `intentPath`, in that order and all strings. Only the value of `intentPath` changes, and only when the given root is not canonical (for example `/var` against `/private/var` on macOS). Its prefix then matches `statePath`, which already came from the canonical root. Outside the verb's own test, a search of the whole worktree, excluding plans, records and artifacts, finds no reader of `intentPath`. The text line `handoff recorded: <nonce> state=<path> sha256=<digest>` is unchanged.

Register. A scripted comparison of register.go against HEAD found 13 changed rows. With `Site` masked, each is byte-identical to its HEAD row, so no other field changed. Each new `Site` names a handoff_capture.go line that calls `refusal("<CODE>", ...)` for its own code, and that line's text equals the text at the row's HEAD `Site` in the HEAD file. The shift is +1 for sites between the new field (:93) and the assignment (:1019), and +2 after it. Codes with several emitters (`HANDOFF_NOT_FOUND`, `HANDOFF_NOT_HOLDER`, `HANDOFF_NOT_LIVE`, `HANDOFF_REFERENCE`) keep the emitter HEAD named. The one steward row outside that file, `CONTEXT_EVIDENCE_RETIRED` at contextreport.go:59, still names its emission line, and contextreport.go is unchanged.

Named tests. Under `-race` with `-count=1`, the five named tests passed on the unmutated copy: `TestHandoffResultNamesTheLiveIntentPath`, `TestContextHandoffVerb`, `TestHCL03HandoffCaptureSitesAreEmissionLines`, `TestContextHandoffCancelByHuman` and `TestCancelHandoffAdmitsAProvenHumanAct`. Timings: steward ok 6.8s, refusal ok 1.2s, cmd/metasystem ok 7.2s. The diff does not touch either human-recovery test.

Not run here. I did not run `go build ./...`, the full race suite, `scripts/agents/go-gate.sh --fast` or any fixture bed. The builder's sandbox could not complete the cmd/metasystem race suite, so that suite and the beds stay with the seat.

Live worktree. At the start and at the end, HEAD was c15d23be and the status listed the same five modified files with nothing untracked. The sha256 of each of the five files matched the start record (the diff of the two hash records was empty). Nothing was written to the live worktree.
