# Tiering machinery, slice 2b: code review (chain str-p2-build-2c-cc1)

Reviewed tree a1ce1f300cc05298196c6394fe428f8e045ca733 (chain str-p2-build-2c, round 1). Critic: Claude Fable 5.1. Brief: plans/severity-tiered-rigor-p2-2b-code-review-brief.md. Eight material findings, two notes; one correction round follows (plans/severity-tiered-rigor-p2-2b-fix-brief.md). The critic had no shell: every green claim in this review is the implementer's or the dispatching seat's, as the critic's own gap line says.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| F-1 | accepted | The fold reads the subject set from diff.patch keeping only `metasystem/`-prefixed paths, but conformance writes that file project-relative (this chain's own diff.patch: `a/cmd/metasystem/delegate.go`); every code-critic and warden fold would be refused as "no changed paths". Critical. | Correction round: the subject reader accepts the project-relative form conformance writes (one prefix rule, owned by the install-prefix helper), with a test feeding a real diff.patch. |
| F-2 | accepted | Roots dispatched before this slice carry a register but not the two counters; `criticRoundsConsumed` errors on a missing counter and is on the path of fold, exhaustion advance and close-with-unresolved; the rebind verb does not repair them. Material. | Correction round: missing counters read as zero consumed and the seam's limit, the rebind verb writes both. |
| F-3 | accepted | Every design-critic launch now passes `--design metasystem/scripts/agents/roles/design-critic.md`, but the fixture bed has no `metasystem/` directory, so dispatch.sh dies before any assertion; masked today by the known-red cause. Material. | Correction round: the fixture bed carries the design file where the rule expects it, or the fixture passes a bed-relative design; the die check stays. |
| F-4 | accepted | STR3-GAP05-ACCEPT-THEN-CLOSE never calls the verb; the pair refusal and the three-step human write are unproven. Material. | Correction round: the test drives `AcceptedRiskDecision` through the injected prover as the sibling human-only verbs do. |
| F-5 | accepted | STR2-CRITIC-UNION-11's two named fixtures (clean plus material same-tree chains refuse; differing class refuses) have no test. Material. | Correction round: both fixtures in conformance_test.go with two critic roots. |
| F-6 | accepted | Three fixture halves on BuildRecord and the delegate front door are unproven (canonical outputs file writes its digest on the root; the root carries `reviewRoundLimit: 3`; design dispatch without `--outputs` refused). Material. | Correction round: one BuildRecord test on role design-critic asserting the three root fields and the limit; one delegate test for the refusal. |
| F-7 | accepted | Decision eleven's compatibility line is applied in mergeCritique and CloseCheck but not in `CritiqueRegisterClose`, so a register-less chain can never be closed and never reaches the restored merge path. Material. | Correction round: the three readers agree; absent register is the pre-2b path in all three. |
| F-8 | accepted | The close table's first row says "print each with artifact"; the verb and CloseCheck return at the first blocking entry. Material (low). | Correction round: collect and print every blocking entry, then refuse. |
| F-9 | noted | Two refusals name no next step (severe-blocks-close; malformed round accounting). Folded into the correction as message text: the first names `goal accept-risk` and the budget raise plus rebind; the second names the rebind verb. | Correction round (text only). |
| F-10 | noted | The `..` segment check in ParseDeclaredOutputs is unreachable behind ParseArtifactRef; the refusal text differs from the rule, the line number is right. Left as is; recorded for the design owner. | none |

For the fleet, found by the dispatching seat's gate run on this chain and reproduced on clean main: since landing c285d5a0 (m0, "Only the human approves work for execution") main is red on TestAuthenticatedChannelApprovalRequiresTheTokenOnce (internal/goal), the dispatch-fixtures `dispatch` scenario, and the goal-cli scenarios structured-budget, scope-bounds and archive-and-prune, all with the new approval refusals. Not this slice's to fix; m0's.
