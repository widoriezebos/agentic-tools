# Missionrunner patience spike — executable verdicts

Job implementer-9159613b56eed6b68e44b78f (codex gpt-5.6-sol, worktree
only, seven executable tests plus two structural adjudications against
the real internal/missionrunner surface), 2026-09-02. Targets: the
nine round-1 findings (records/misc/missionrunner-patience-critique-r1.md).
Revision 2 folds these verdicts; the full return with transcripts is
durable in the round evidence.

Design-evidence spike: seven executable tests plus two code-read adjudications run against the real internal/missionrunner process surface (proc.go, host.go), internal/identity, and internal/census, in the worktree only; nothing lands. Verdicts per finding. DC-PAT-001 SURVIVES: on a live group, an injected pid-enumeration failure made the design's down() predicate report death (alive=true, zero substantive pids, down=true); rule — the observation carries a walk-completeness bit (false on enumeration error or any in-group probe error) and down requires either group absence or a COMPLETE walk with zero substantive members; the amended rule answered correctly on the same observation, so uncertainty is distinguishable from death. DC-PAT-002 SURVIVES: the real terminateGroup with a non-matching tag refused ownership (nil return, group untouched, deterministically), and the design's wait shape then returned success when the fixture expired naturally; rule — (a) fixture holds must outlive the observation ceiling (sleep 600, never sleep 30) and (b) refusal is detected immediately, with no stall wait: nil return plus one complete observation showing a substantive member counts one refusal. DC-PAT-003 NEEDS RULE: confirmed structurally — every healthy kill across 30 loaded rounds produced exactly 2 distinct fingerprints, so no intermediate progress exists and the stall is a timeout; but measured kill-to-down latency was 58 microseconds to 5.8 milliseconds at load average 14.35 on 4 CPUs, four orders of magnitude under the 30-second failsafe; rule — the design states the stall is a hang failsafe over a millisecond-scale kernel event with the measured margin recorded, and (via the DC-PAT-002 rule) the stall never adjudicates a nil return, only post-kill death. DC-PAT-004 SURVIVES: 78 census scans over 8 seconds under process churn changed the design's whole-census fingerprint 11 times while the wedged target group's restricted fingerprint changed 0 times; rule — fingerprint only target-relevant evidence: the enumeration error plus rows (tagged or signalable-indeterminate) whose process group is the target or whose pid resolves to it. DC-PAT-005 SURVIVES: with an invalid heartbeat-interval environment variable, the real terminateGroup returned a configuration error in 476 microseconds while the TERM-immune group stayed fully alive and no SIGKILL was ever sent; the TERM-responsive variant then died of the already-sent SIGTERM, so the design's error branch would pass as died-late and launder the configuration failure; rule — only the exact kill-through error text ('survived the kill-through window') enters the patient error branch; any other non-nil error is an immediate test fatal. DC-PAT-006 SURVIVES (structural): the census indeterminate row carries no start identity, so the resolver rule's only inputs (pid, zero group id, later Getpgid) cannot detect pid reuse by construction; rule — invert the join kernel-first: scan, then take a complete group observation, and grant census custody only when every current substantive member pid appears among that scan's signalable row pids. DC-PAT-007 SURVIVES: a leader running 'sleep 600 & exit' was reaped while the group held a live substantive member, so the reap channel provably does not mean the group left the table; rule — cleanup waits on both the reap channel and group absence, and failsafe expiry is t.Errorf (red), not a log line. DC-PAT-008 SURVIVES (read-level, pure specification contradiction): rule — add a distinct classification value for the ceiling case (leaked-oscillating) that the caller counts as leaked and reports with the oscillation wording; no signature violation is needed. DC-PAT-009 SURVIVES: the deterministic stays-red case exists with zero fakes — real terminateGroup, wrong tag, sleep-600 fixture, immediate refusal detection — and exhausted 3 of 3 attempts in about 30 milliseconds with no laundering window; rule — revision 2 must require that test and assert the refusal-exhaustion red fires. Measurements demanding load ran under three concurrent Go compile loops (load average 14.35 on this 4-CPU guest); all load processes were time-bounded and self-terminated, and every spawned fixture group was killed by exact process-group id with a verified exit.

## Evidence commands

- [ran] cd /home/wido.guest/m0b/agentic-tools/metasystem/artifacts/agents/worktrees/implementer-9159613b56eed6b68e44b7
  -> All seven spike tests pass in 11.3 seconds. DC-PAT-001: injected enumeration failure on a live group gives alive=true pids=[] designDown=true, ruleDown=false. DC-PAT-002/009: wrong
- [ran] cat /proc/loadavg && nproc && GOCACHE=$TMPDIR/gocache GOTMPDIR=$TMPDIR go test -v -count=3 -run 'TestSpikeDCPA
  -> Load average 14.35 on 4 CPUs while three bounded concurrent go-build loops ran; 15 further healthy-kill rounds all show kill-to-down between 58 microseconds and 5.8 milliseconds wi
- [ran] GOCACHE=$TMPDIR/gocache GOTMPDIR=$TMPDIR go vet ./internal/missionrunner/
  -> Clean; the spike file compiles against the real package with no findings.
- [read] sed -n '94,182p' internal/missionrunner/host.go && sed -n '106,119p' internal/missionrunner/missionrunner.go
  -> terminateGroup sends SIGTERM at host.go:120 BEFORE the ScaledWaitAtLeast and Interval calls at host.go:125-131 that return configuration errors, and SIGKILL only at host.go:151; In
- [read] sed -n '56,61p;165,229p' internal/census/tagged.go
  -> IndeterminateProcess carries only PID, PGID, Reason, and Universe — no start identity (DC-PAT-006); ScanTaggedProcesses enumerates the entire process table and records foreign (per
- [read] sed -n '108,170p;209,345p' plans/missionrunner-patience-design.md
  -> Section 3.1 defines down as not-alive OR zero substantive pids with a nil list on enumeration error (DC-PAT-001); sections 3.4 and 3.5 both return only a classification and a censu

## Spike-declared gaps

- The brief names 'internal/proc' but no such package exists; the process surface the findings actually cite is internal/missionrunner/proc.go plus internal/identity and internal/census, and the spike ran against those. If the orchestrator meant a different package, the spike did not cover it.
- DC-PAT-001's per-process probe-error variant (a live, non-zombie group member whose argv read fails transiently) was not forced executably — only the enumeration-failure variant was; the probe-error omission path is confirmed by code reading of the design's section 3.1 walk alone.
- DC-PAT-006 was adjudicated structurally (the rule's inputs cannot carry identity) with a scripted resolver; a physical pid exit-and-reuse race between scan and resolve was not produced, as pid reuse cannot be forced deterministically on this guest.
- DC-PAT-008 is a pure specification contradiction with nothing executable; its verdict rests on reading the design text, not on a test run.
- DC-PAT-002's laundering was demonstrated with a 3-second fixture against a 30-second stall; the shipped 30-second fixture against the 30-second stall is an exact race whose winner is nondeterministic by construction — that nondeterminism is itself the finding, but no run of the exact 30-versus-30 case was performed.
