# Tiering machinery, slice 2b: closing review after the carry and the coverage round (chain str-p2-build-2d-cc1)

Reviewed tree fdc9882590965651fce4cddbe8eefa1e8056db8a (chain str-p2-build-2d, round 1: the twice-corrected 2b diff of chain str-p2-build-2c carried onto main at eefeb16c, parts one and three included, plus the tests-only coverage round; 46 files). Critic: Claude Fable 5.1. Brief: plans/severity-tiered-rigor-p2-2b-close-review-brief.md. Mandates 1 to 5 hold: F-1 (a 40- or 64-hex object id), F-2 (the cap-driver and cap-warden block on the new regime, judged by reading), F-4 to F-6, the merge resolutions and the coverage tests are confirmed as ordered; nothing from the earlier rounds reopened. One material finding, three notes. Critic gap: its sandbox could not see the delegate worktree and it judged by the diff alone; the worktree exists and is gated by the seat (Go gate, every package, return-schema fixtures, coverage 76.7 and 80.8 against floors 75.9 and 79.9).

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| STR2P2B-01 | accepted | The fold reads `root["demotions"].([]any)` and refuses "has malformed demotions" when the member is absent; a critic root written before this slice has no such member, so the first post-landing fold of a pre-slice chain that demotes a finding is refused. Same class as round one's F-2 (counters absent on pre-slice roots), which was closed by tolerance. Medium; true by reading the fold against main's BuildRecord. | Third correction (plans/severity-tiered-rigor-p2-2b-fix3-brief.md): absent reads as empty and is written, malformed still refuses, mirroring `exhaustions`; one test each way. |
| STR2P2B-02 | noted | A header whose path itself contains the token " b/" mis-parses; quoted headers are skipped. No such path exists here; F-4's ordered scope (one space) is met. | Recorded; not built. |
| STR2P2B-03 | noted | `goalReviewRoundLimit` still returns the constant three while main's budget carries a per-tier review-round limit; the rebind discards the tier. The recorded seam of the original brief, not a regression. | Slice 2a rebinds the seam to the tier. |
| STR2P2B-04 | accepted, cosmetic | The design-critic checks added to dispatch.sh are tab-indented in a space-indented function. | Third correction re-indents those lines; bytes only. |

Ceremony note: under R-73-m3 a further correction goes to Wido with evidence. The seat put the choice to him at 08:30 (accept the risk and land, or correct) and, with no word by 09:00 on the deadline day, takes the ladder's own path rather than the one that needs his word: the correction is small, funded, and inside the deadline; accepting a material finding is his to say, not the seat's. This record is the evidence; his later word overrides.
