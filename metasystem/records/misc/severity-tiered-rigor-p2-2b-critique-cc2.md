# Tiering machinery, slice 2b: re-review after the one correction (chain str-p2-build-2c-cc2)

Reviewed tree aea3bbefb86cf1112bf0de87c783e8cf90d84736 (chain str-p2-build-2c, round 2, 44 files against main at de92aa8b). Critic: Claude Fable 5.1, no shell. Brief: plans/severity-tiered-rigor-p2-2b-re-review-brief.md. The nine round-1 fixes are confirmed as ordered (mandate 1) and the round-1 properties hold (mandate 2); of the seven recorded decisions the critic names one that is not mechanical (the SHA-256 fixture, F-1). Three material findings, three notes.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| F-1 | accepted | The new design-blob provenance check (`designBlobSource`, internal/dispatch/build.go) accepts only a 64-hex object id; this repository and the fixture bed are SHA-1 (40 hex), so every design-critic dispatch is refused at build-record after landing. The test passes only by creating a SHA-256 repository. Critical; verified by the seat (`git rev-parse --show-object-format` is sha1 here, the regex is `incarnationRe`). | Second correction: a git object id is 40 or 64 hex (its own pattern, not the content-digest pattern); the test runs in a default SHA-1 repository. |
| F-2 | accepted | The dispatch-fixtures cap-driver and cap-warden block (lines 2124 to 2202 in the worktree) still encodes the retired first-exhaustion regime: its critic follow-ups fold against a reviewed implementer that never had conformance run, its rigor rows carry no artifact, and its expected refusal texts are unreachable after the diff. Masked by the known-red `dispatch` scenario. High. | Second correction: the block is rewritten to the new regime (conformance on the reviewed implementer, artifact-bearing rigor rows, the "review-round limit is exhausted" texts) against a nested reviewed workspace. |
| F-3 | accepted, out of scope for this slice | At a toplevel adoption the install prefix is empty while the artifact grammar demands the nested project name, so a code-critic or warden fold there can never produce a subject set. True and design-owned; this repository is nested. | Backlog goal for the design owner (the artifact namespace of a toplevel adoption); the second correction nests the fixture bed's reviewed workspace instead of changing the grammar. |
| F-4 | noted | A `diff --git` header for a path containing a space has six fields and is dropped from the subject set; no such path exists here. | Second correction, if cheap: parse the header by the ` b/` separator; else recorded. |
| F-5 | noted | Two parenthetical properties (cancelled rounds excluded; exhaustion advance on a root missing both counters) are proved only on the counter-present path; the demanded test exists and passes. | Second correction: one assertion each on the legacy path. |
| F-6 | noted | Dead branches of the removed first-exhaustion regime remain in CritiqueExhaustionAdvance. | Second correction: remove them. |

Ceremony note: the plan m2 and m3 agreed under R-73-m3 sends a second correction to Wido with evidence. At 01:35 on the deadline day the seat proceeds without waiting for him, as m2 did on part one an hour earlier: the deadline is his order, the findings are fixture-and-mechanical, and the goal's budget refuses the next slice until he approves a raise anyway, which is where his morning word lands. This record is that evidence.
