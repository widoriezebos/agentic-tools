# human-proof-fits-the-act, design critique round one: dispositions

Critic human-proof-crit1-20260909 (codex gpt-5.6-sol, xhigh) reviewed
revision 1 of plans/human-proof-fits-the-act-design.md (commit
b7fb35a2d) and returned nine material findings, six high. All nine are
accepted; revision 2 folds them on the Fable lane before any build. The
critic ran no bed and no live human verb, as the brief required; its
register is records/misc/human-proof-fits-the-act-critique-r1.md, built
by the orchestrator from the returned JSON because the critic's runtime
was read-only.

| Finding id | Disposition | Reasoning and evidence | Amendment |
| --- | --- | --- | --- |
| HPA-01 | accepted | Real and the most consequential: migrate and repair bypass syncReq, RecordFleetEnrollment has no human guard, and goalsync_mutations.go proves only when the lineage is absent, so an exported lineage plus --by reaches several human acts with no proof. | Revision 2 carries a positive inventory of every human-only entry point and explicit proof plumbing for migrate, repair and fleet enrollment; the exported-lineage bypass becomes the first canary, proven in a scratch ledger, never the live one. |
| HPA-02 | accepted | Clearing a pin lets every machine claim; moving it lets the destination claim. Both widen. | set-pin moves to the enrolled grade; set-priority stays terminal. |
| HPA-03 | accepted | The table is a catch-all, and unpark, set-arc and discharge-review-obligation grant eligibility, create a claim or remove a barrier. | Revision 2 replaces the table with a transition matrix: every human verb, its precondition, what it grants, its grade. |
| HPA-04 | accepted | arm, steward arm and restart, brain withdraw and resolve-taint adopt-disputed-tree reopen execution; they are not stops. | Enrolled grade for the reopening transitions; terminal grade for their stopping twins; the mission-runner files enter scope. The page says which walk arm uses today (process ancestry, not enrollment) and keeps that. |
| HPA-05 | accepted | Migration imports approvals and budgets, yet enroll-terminal refuses before a synced backlog exists. | Revision 2 permits local enrollment before migration and defers the fleet-cutoff publication until the backlog exists; migrate then takes the enrolled grade. This is the orchestrator's disposition; it widens nothing. |
| HPA-06 | accepted | Section 6 misses ArmTemporary's steward caller, the help text and the runner's temporary-word carry-forward. | Revision 2 names every deletion site; re-arm keeps historical fields as inert provenance and never mints temporary authority from them. |
| HPA-07 | accepted | Prove returns a plain error with no enrollment, so no code selects the recovery row. | One structured no-enrollment outcome shared by Prove and ProveTerminal, mapped to the enroll-terminal command and tested. |
| HPA-08 | accepted | The active-other-terminal refusal omits that no prior authority is needed, that enrolling retires the first terminal, and that the command must be retried; a literal placeholder is not a command. | Revision 2 states all three in the refusal text and orders the name provider before this build, or prints that no complete command exists. |
| HPA-09 | accepted | Two of four canaries are already green today, and terminal liveness lies outside the tree reader. | The exported-lineage bypass is the primary red canary; today's refusals become regression guards; recovery executes the printed command and checks the old terminal is retired; a liveness probe is injected. |
