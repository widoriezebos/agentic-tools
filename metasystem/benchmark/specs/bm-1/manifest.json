{
  "id": "bm-1",
  "version": "0.1",
  "title": "taskrun: a dependency-aware task runner (Java, Maven, command line)",
  "comparisonEligible": false,
  "comparisonEligibleNote": "Version 0.x is a trial spec. benchmark-compare refuses any cohort below 1.0. Promotion to 1.0 happens once trial runs have set the fences from observed use, per the promotion rule in plans/benchmark-spec-bm1-design.md section S-6.",
  "product": {
    "sourcePaths": [
      "src/main/**",
      "src/test/**",
      "pom.xml"
    ],
    "entryPoint": "java -jar target/taskrun.jar",
    "requirementCount": 26
  },
  "seededGap": {
    "requirement": 10,
    "decisionFile": "DECISIONS.md",
    "entryGrammar": "requirement <n>: <key>: <value>",
    "declaredRuleKey": "order-rule",
    "allowedValues": [
      "alphabetical",
      "config-order",
      "dependency-depth"
    ],
    "note": "Published in spec.md in full: the entry grammar, the key, and the three permitted values with their exact semantics. A builder cannot satisfy a machine-checked declaration whose format is withheld (BA-1-1), and a rule without executable semantics leaves the grader holding a hidden right answer (BA-2-1)."
  },
  "metrics": {
    "acceptance": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null
    },
    "requirement_coverage": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null
    },
    "cache_correctness": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null
    },
    "failure_propagation": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null
    },
    "determinism": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": 1
    },
    "config_errors": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null
    },
    "format_agreement": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null
    },
    "build_clean": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": 1
    },
    "own_tests_pass": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null
    },
    "mutation_catch": {
      "domain": [
        0.0,
        1.0
      ],
      "direction": "max",
      "bound": null
    },
    "plan_seconds": {
      "domain": [
        0,
        null
      ],
      "direction": "min",
      "bound": 20,
      "note": "Requirement 25: 1000 tasks in a single chain, cache primed once, then the median of three timed runs from process start to exit. Pinned because 'an ordinary developer machine' is not a measurement."
    },
    "dependency_count": {
      "domain": [
        0,
        null
      ],
      "direction": "min",
      "bound": 0,
      "note": "Compile-scope and runtime-scope dependencies in pom.xml. Test-scope JUnit 5 is expected and not counted."
    },
    "gap_handled": {
      "domain": [
        0,
        1
      ],
      "direction": "max",
      "bound": null
    }
  },
  "metricsNote": "A null bound is deliberate and not an omission: calibration (item S-C) sets it from the probe scores. Inventing these numbers now would encode a guess as a threshold.",
  "noiseFloors": null,
  "noiseFloorsNote": "Unknown until repeated runs measure the spread. Until then this spec produces scores, not verdicts.",
  "fences": {
    "cycles": 8,
    "jobs": 12,
    "concurrency": 2,
    "jobCapMin": 15,
    "wallClockHours": 3,
    "hostTurnCapMin": 20,
    "ledgerCycleBudget": 8,
    "ledgerNoGainBudget": 3
  },
  "fencesNote": "Provisional and unapproved. These are an experiment, not a belief: trial runs record what they consume and each fence becomes ceil(observed_max * 1.5), excluding trials that parked at that fence. A human must approve them before any run spends money.",
  "roster": null,
  "rosterNote": "Pinned by exact model identifier when a run is prepared, never by a phrase like 'cheap models', because comparability requires two cohorts to share an identical roster.",
  "seed": {
    "path": "seed/",
    "description": "A git repository containing README.md, spec.md, and an empty tests/ directory. Empty because BM-1 builds from scratch."
  },
  "grader": {
    "path": "grader/",
    "heldOut": true,
    "heldOutNote": "Never copied into the repository the agents work in. Provisioning copies spec.md and seed/ only.",
    "mutation": {
      "status": "deferred, not designed",
      "note": "Not built in grader v0.1, and the deferral is a disposition of BA-2-2, not a postponement of the old text: that finding refuted the claim that mutating the builder's own code is architecture-neutral. With no reference corpus, mutants come from arbitrary builder source, and attributing a surviving mutant to the requirement whose tests should have caught it is not derivable from requirements-map.json alone. A later version needs a design of its own, critiqued before it is built. Until then mutation_catch stays in the metric table with a null bound and is never emitted."
    },
    "calibration": {
      "probes": "small purpose-built programs, one per metric, each exhibiting exactly the flaw that metric must catch",
      "note": "Calibration proves each check fires. A full correct implementation is a wildly expensive way to test a checker and is not built."
    },
    "outsideOnlyException": "Requirement 24 is graded by reading pom.xml. No finite set of runs can prove an unexercised branch imports nothing, so the outside-only rule states its one exception rather than pretending (BA-2-11).",
    "v01Scope": {
      "builds": [
        "grade.sh emitting metric=<name>=<value> lines in the mission gate grammar, exit 0 when measurement ran",
        "the held-out acceptance suite, one or more checks per requirement, driving the built jar from outside; per-requirement lines metric=requirement_<n>=1|0",
        "behavioural batteries: cache_correctness, failure_propagation, determinism (each repetition from a deleted cache), config_errors, format_agreement (cold cache both sides)",
        "build_clean, own_tests_pass, plan_seconds per requirement 25, dependency_count from pom.xml (the stated outside-only exception), gap_handled (declared rule checked against behaviour on a disagreeing configuration)",
        "seed_respected: wrapper intact and functional, release level unchanged, no compile-scope dependency added, inherited build still working, computed as a diff against seed/",
        "calibration probes, one per metric, each failing exactly its metric on every repetition, scores recorded in grader/calibration.md"
      ],
      "doesNotBuild": [
        "mutation_catch",
        "any reference implementation",
        "noise floors, which come from repeated runs"
      ]
    }
  },
  "language": {
    "runtime": "Java 21",
    "buildTool": "Maven, via the wrapper shipped in the repository",
    "entryPoint": "java -jar target/taskrun.jar",
    "buildCommand": "./mvnw -o -q package",
    "testCommand": "./mvnw -o test",
    "testIdentifierForm": "<fully.qualified.ClassName>#<methodName>",
    "note": "Spring Boot was considered and dropped: the deliverable is a command-line tool and a web framework would have added ceremony without adding anything the benchmark measures."
  },
  "environment": {
    "delegateNetwork": "allowed",
    "delegateNetworkReason": "Per-spec, not a benchmark-wide rule, and allowed here. This spec is idiosyncratic enough that no published task runner satisfies it: the stdout grammar, the four states, the cache directory, the JSON shape and the decision-record rule are all ours, so a downloaded implementation still has to be rewritten to match, which is most of the work. The constraint worth protecting, no third-party dependencies, is graded from pom.xml rather than enforced by cutting the network, which is the right place for it: a requirement should be scored, not made impossible to violate, or we never learn that an agent would have violated it.",
    "denyMechanism": "A spec that does need isolation sets dispatch.permissions.network=deny in the provisioned target's metasystem.conf, which narrows every preset for every role. Reasons that would justify it: a published solution a search would hand over wholesale, or a metric that cannot tolerate run-to-run variance from fetched content.",
    "maven": "Builds still run with -o against a pre-populated repository, because the build must be deterministic and must work whether or not the network is up. That is a spec requirement, not a sandbox constraint."
  }
}
