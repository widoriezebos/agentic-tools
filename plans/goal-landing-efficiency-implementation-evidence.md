# Goal landing efficiency implementation evidence

Implementation is in progress. No implementation commit or push has been made. This report separates observed improvements from remaining acceptance work; it does not certify the candidate.

The approved [design](goal-landing-efficiency-improvements.md) is implemented against `9498700a9e246f09395213f8539d22e0ea196731`. The earlier [analysis](goal-landing-efficiency-analysis-2026-09-20.md) remains historical evidence, not a benchmark of this implementation. Source and immutable review snapshots are preserved outside temporary storage at `/Users/wido/LocalStorage/agentic-tools-evidence/goal-landing-efficiency-20260920`.

## What changes

The testing planner and retained-result store own join, tip and prefix proof. Each prefix computes its own requirements and consumes compatible observations. Seal no longer owns a second native test gate. Current policy, input identity, freshness and live publication authority are checked when evidence is consumed.

Execution identity is separate from policy selection. Adding an unrelated group can reuse a prior group, while changing a consumed source, tool, declared external input or semantic environment invalidates it. The Go adapter generates package groups with transitive import inputs and declared shared runtime inputs. Application orchestration uses the same command/JUnit adapter as the portability fixture.

Admission freezes the complete selection and allocates missing producers atomically. Consumers retain original producer references and wait only on earlier reservations. Real prerequisites suppress dependent launches after a failure; independent failures can be collected together. Fresh diagnostics have durable episodes, so resuming the same episode can consume its observation while a new decision requires native execution.

The resource owner admits heavy work and exclusive resources. Its custody correction retains admission until native descendants and declared fixture children have demonstrably ended, including launcher or worker failure. Real public entrypoint, nested execution and failure canaries pass; the exact-candidate delivery proof remains outstanding.

## Measured portable cohort

These are cumulative external counters from seven public CLI scenarios in a small shell/JUnit application. The baseline uses production source from `9498700a9`; its fixture build stamp belongs to the disposable application. A candidate-build entrypoint wrapper supplies the build counter, so it is not a measurement of compiler work.

| Scenario | Baseline build calls | Candidate build calls | Baseline native calls | Candidate native calls |
| --- | --- | --- | --- | --- |
| Cold A | 1 | 1 | A: 1 | A: 1 |
| Unchanged A | 2 | 1 | A: 1 | A: 1 |
| Add independent B | 3 | 1 | A: 2, B: 1 | A: 1, B: 1 |
| Change A input | 4 | 1 | A: 3, B: 1 | A: 2, B: 1 |
| Docs-only change | 5 | 1 | A: 3, B: 1 | A: 2, B: 1 |
| Declare a shared input | 6 | 1 | A: 4, B: 2 | A: 3, B: 2 |
| Change the shared input | 7 | 1 | A: 5, B: 3 | A: 4, B: 3 |

This cohort avoided six build-entrypoint invocations and one native A execution. Both versions correctly avoided native work for the docs-only change and reran both consumers when the shared input changed. The individual wall-clock samples do not establish a latency percentage or p50/p95 improvement. The old implementation cannot supply a passing equivalent of the new full batch/moved-trunk scenario; that baseline is unavailable, not zero-cost.

## Runtime evidence and limits

| Behavior | Observed evidence | Remaining scope |
| --- | --- | --- |
| Exact prefixes | Three cumulative prefixes consumed real retained results and passed the final series verifier without additional compatible native launches. | Owner-driven join, owner crash, successor tick and atomic bare-origin publication also passed. |
| Red prefix | A green tip could not conceal a failing earlier prefix; receipt composition refused it. | Final candidate delivery selection. |
| Moved remote | A real bare-origin change forced production reassembly; old receipts were rejected and only the affected B check reran. Every moved prefix was verified. | A separate fixture completed the full production owner/tick/transport lifecycle without a manual state bridge; only B reran, giving A1/B2/C1. |
| Restart and freshness | Reconstructed state reuses retained observations; the same fresh episode does not duplicate execution, while a new episode does. Expiry must match at write, read and receipt consumption. | Full owner recovery is proven in the portable lifecycle; final-tree delivery remains outstanding. |
| Failure collection | A broken prerequisite H blocked A while independent B failed and C passed. Repairing H/B ran H/B/A and reused C; a later A-only change reused H/B/C. Public final verification passed. | Final source review of the narrow blocked-observation correction. |
| Host capacity | A real ordinary grandchild that closed inherited descriptors reproduced premature capacity release. | Normal completion, launcher/worker death, cancellation, live-owner fixture drain, all public entrypoints at capacity one and nested guardian-loss canaries pass. An unproved orphan remains blocked even after its child dies; cleanup must be established. Final delivery proof remains. |
| Publication authority | A revised claim after the first stale-lease push is refused before retry publication. Disabling the new check produces one unauthorized retry push. | Full final delivery proof. |
| Format migration | The original 949 public planner/worker proved a candidate containing the new engine under an existing schema-1 contract, including its frozen protection corpus. | Final-tree verification and actual staged enrollment. |

## First selected delivery run

The first actual Stage A run used the installed 949 policy engine and completed all 58 required groups in 16.47 minutes: 20 passed, 10 failed and 28 were invalid. It did not authorize publication. Twenty-six invalid section groups hit the same detached-worker authorization refusal before executing their bodies, so this timing cannot predict a successful full selection. The other two invalid groups correctly refused missing protected test names, despite native package exit 0.

The correction batch preserves strict checks. It fixes the detached-worker boundary and an admission lock that could wait indefinitely, supplies missing native-producer fixture ownership, retains protected test names, normalizes the policy encoding, and satisfies the existing test and refusal inventories. A stale fixed clock in the old batch fixture produced already-expired native deadlines; deriving the fixture from its current governing clock made a real two-member receipt-and-landing case pass. A cheap parser regression fixes the cost fixture's comparison of JSON-escaped versus decoded report text. Focused before/after evidence is retained. The independent second read of the detached-worker authorization and admission correction returned `land` with no material findings; the next exact-candidate delivery run remains required.

Historical base 57f353384 has a policy entry for a token-window test absent from its own source. Its old automatic batch precondition therefore remains fail-closed. The approved Stage A terminal publication route must prove the corrected candidate's complete selected contract, including real restored named coverage; it does not use that broken old batch join. Real launch settings remain zero, meaning no imposed cap. The restored positive-window test uses an explicit synthetic fixture setting, alongside the zero-default test.

Primary artifacts: `rollout/stage-a-first-delivery/` contains the structured insufficient result, all 58 native logs, their hashes and the frozen candidate. No native package log is substituted for the structured verdict.

## Second selected delivery run

Stage A tree `c2a0e3706d10c86ec37020181a62413d807a7627` completed all 58 groups in 10.22 minutes: 28 passed, four failed and 26 were invalid. It remains insufficient. The corrected batch lifecycle, protected test inventories, context-cost fixture and several other first-run failures passed. This installed-old-engine run retained no reusable native observations after the changed engine inputs; it is not evidence of the intended performance improvement.

The detached authorization canary had modeled the attempt root and group working directory as equal. Actual delivery admits a whole-project tree and launches from a nested installation, so all 26 sections and the direct fast gate still refused. The correction must bind the candidate at its admitted project scope while separately checking the authenticated native process and its working directory. Acceptance requires an actual old public executor run in that layout, beyond a synthetic worker authorization test. Independent failures also exposed a stale clean-marker count, two parallel admission fixtures colliding on the non-waiting host guard, an expired clock in another binary fixture, and two binary launches outside the existing shared fixture isolation helper. These corrections are being batched; strict refusals remain intact.

Primary artifacts: `rollout/stage-a-second-delivery/` contains the complete structured result, native logs and hash manifest. The failed attempt is `proof-mu9sdn0v-3a00520935429844`. No implementation publication has occurred.

A subsequent exact-host diagnostic selected the two affected groups plus the three mandatory canaries. The real static-placeholder section passed, proving its corrected nested-root authorization; the direct fast gate still failed. Its native command inherited the installed 949 authentication executable, whereas sections receive the candidate executable. The host Go gate needs a compatibility handoff to the candidate's existing authenticator for the same actual directory, preserving candidate-tree and process binding. That diagnostic took about 24 seconds and prevented another premature broad run; its complete artifacts are in `rollout/stage-a-first-layout-diagnostic/`.

The next actual diagnostic reached the fast gate's static analysis and passed the section again. All three mandatory canaries reused retained evidence. The Go gate now authenticates through the current source only when the inherited verifier cannot handle the admitted layout; it retains the same actual directory and pins read-only modules, disables Go workspaces and disables CGO for that verifier. Existing unauthorized-relaunch and coverage-boundary regressions pass. Static analysis then identified ten mechanical issues (unused members/assignment and error-string capitalization), which are being corrected without altering admission or proof behavior. The complete second diagnostic is preserved in `rollout/stage-a-second-layout-diagnostic/`.

The final short diagnostic on tree `6ab4e3ea1cd03d8cb22e1eabc02e4b588352837b` passed through the actual installed engine, including both the fast gate and static-placeholder section. Attempt `proof-mu9tpn6k-c3f659c66713cbfa` finished with exit 0; its complete result and native log hashes are in `rollout/stage-a-third-layout-diagnostic/`. The fast gate passed formatting, vet, staticcheck, refusal and parallel inventories, hook and decision-surface audits, and the candidate build. This clears the focused launch-path blocker; it is not complete delivery proof. The next run recomputes the complete destination-selected contract.

## Third selected delivery run and final conformance corrections

Stage A tree `387ea3f7cf9a36f87f82198832425ca5bc8cf83e`, attempt `proof-mu9ts9c3-91c7d09501e1579f`, completed all 58 required groups in 927,625 ms (15.46 minutes): 40 passed, 11 reused and seven failed. Native launch accounting reports 47 test launches, zero builds and 70 other launches; reuse accounts for ten test groups and one build group. These counts demonstrate real reuse in this run, not an intent-to-landed speedup. The result remains insufficient. Full structured results, logs and hashes are retained in `rollout/stage-a-third-delivery/`.

All seven failures are section fixtures. One release assembly error removed the executable bit from the old land fixture script, so 45 scenarios never started; overlay mode and assembler mode checks are corrected externally. The suite-progress fixture exposed a real output regression: the resource custodian persisted watchdog diagnostics but did not forward them to the launcher's public stream. A correction preserves regular-file output after launcher loss and restores live/final forwarding. Other focused corrections address an incomplete fake engine's runtime declaration, a legacy migration fixture using an unsupported risk field, placeholder validation preceding a dangling-link assertion, an umask-dependent mode fixture, and elapsed-budget cases whose initial claims did not use the same governing clock as their future breach. Actual broad evidence also exposed the approve-to-budget shortcut checking human authority before the existing brain refusal. Each correction must retain its original assertion and pass the relevant focused case; no broad green is claimed.

The final design conformance audit identified three unfinished implementation requirements beyond these defects: the Section 6 batch cost forecast, a dedicated typed additive requirement transport instead of delivery `--groups`, and moving named Go-test source protection out of the batch owner to the existing testing owner. These are required parts of the accepted generic design. An additional native portable fixture will prove resource-capacity wait, an actual elapsed breach fence for an active claim, member removal and survivor publication. The existing elapsed exemption for a claim explicitly marked land-ready remains authoritative. Delegates are completing these in Stage B while Stage A remains a coherent schema-1 upgrade.

The forecast design's independent Sol read found two material concurrency/observability issues. The coordinator accepted them and chose explicit snapshot semantics: a forecast does not atomically reserve budget, the existing native admission remains authoritative after any spending race, and stored status never claims its historical estimate is current. Same-revision spending races and mutable-evidence/status cases are named fixture obligations. Bounded metadata reads are permitted; the forecast starts no native build or test and adds no proof store or scheduler. Design and verbatim review are retained under `rollout/batch-cost-forecast-design.md` and `reviews/batch-cost-design-r1-fulltree/`. The initial critic failed to write an artifact; an authorized exact-session continuation recovered the actual findings. That protocol failure and tool-cap overrun remain recorded.

## Rollout and review

Rollout has two stages. A introduces the new engine while retaining a schema-1 host contract. After A is installed and enrolled, B activates the schema-2 host contract and its associated host tests. Normal new results use schema 2 and attempts use schema 4. A narrow private bridge emits an old-format response only for explicitly synthetic invalid protection probes; it never converts normal or successful proof. The rollback floor after new records exist is the A engine with a schema-1 contract, not raw 949.

Sol readers reviewed immutable, Git-resolvable source snapshots through the actual MetaSystem launcher. Findings and coordinator dispositions remain in the durable evidence directory. Accepted second-round findings are corrected with decisive regressions; their original `fix first` verdicts are preserved, without a third review. The final resource reads found one live-owner fixture cleanup defect and missing runtime coverage for exclusive-resource forgery, nested completion followed by guardian loss, and public entrypoint contention and launcher loss. These accepted corrections and runtime cases now pass, with raw logs and exits retained in the disposition ledger. The original fix-first verdicts remain unchanged. The separate nested-installation second read returned `land` with no material findings after a project-root compatibility regression failed before and passed after the correction. Selected delivery proof, obligation closure and actual publication remain outstanding. The existing enrolled-terminal commit wrapper will carry both stages under Wido's explicit authority, while preserving actual review provenance and exact testing proof.

The final frozen Stage A source probe at tree `dad7c7789e3ecc0fd36687fbeb4f5a4ca4993251` passed against the unchanged 949 public planner/worker and its six frozen policy cases. The fixture substitutes its own existing schema-1 contract, so this proves cross-version source compatibility rather than exact host-contract certification. Subsequent cleanup and nested-installation corrections still require the actual destination-selected delivery proof.

The terminal authority dependency is resolved. With Wido's explicit authorization to use and enroll tmux, the coordinator ran the supported enrollment, prior-claim release and tier-3 approval commands successfully, then claimed the goal under its own actual lineage. No authority record, enrollment or proof receipt was fabricated. The clean baseline engine was subsequently armed successfully through the supported terminal command (accepted-engine generation 168). Final candidate enrollment and publication remain outstanding.

At 15:47 CEST, two section-diagnostic preparations had failed compilation before any test verdict. Both snapshots accidentally included four unfinished B-only cost/forecast source files; freezing the first contaminated snapshot did not correct it. The four files were explicitly removed from A, preserved in B, and added to the release overlay exclusion inventory. The corrected A command package and selected goal-budget tests pass. Further A promotion uses explicit immutable snapshots and a command compilation check. No failed preparation is reported as a test run.

The actual independent output/authority read returned `fix first` with AO-1: an ordinary suite stream stops draining when its public writer fails, allowing a writing child to fill the pipe before custody cleanup. This is accepted for correction with a native output-volume regression. The earlier public-writer test covered watchdog forwarding only. The reader checked both brain authority-order corrections without a further material finding. Its full source-evidenced verdict is retained at `reviews/stage-a-output-budget-r1/source/metasystem/review-findings.md`; the candidate contamination and 15 calls against a 10-call brief remain limitations.

The completed 11-group diagnostic (`proof-mu9vkp38-79294df8e2f0ad6e`, candidate `1d11a8192a3f9f8099baf84943f6a184e26c473a`) took 952533ms: eight groups passed, one reused, one failed and one was invalid. Every land-fixture scenario passed (45 isolated legs); the goal CLI, dispatcher, fixture-bed, suite-progress and fast-static checks passed. The remaining supervision failure was an independent temporary repository inheriting the outer proof-worker locators; the existing isolation helper fixes it, with the frozen-source named case failing before and passing after. Adoption exposed a missing shipped parallel-ratchet asset and similar nested-fixture context/setup faults; its failing capture exceeded the existing 512MiB evidence bound, which remains unchanged. Full logs/hashes are retained at `rollout/stage-a-section-diagnostic-completed/`.

AO-1 is closed after the native large-output before/after regression, a passing six-case cluster, and final independent source review on exact A tree `1ba1d8d7f5d465114eea31803cdbeeceb7731d5e`. That read returned land with zero material findings. Its local run was blocked by reviewer sandbox cache permissions, and actual reader calls exceeded the stated brief (seven versus five); both limits remain recorded. This is scoped source review with separately executed evidence, not a certified job chain or a full delivery verdict.

The adoption repairs passed both focused public scenarios on a complete private A snapshot: default adoption, including the dangling registered-skill refusal, and filled-delivery comparison (160.45s). The actually adopted target passed dependency/parallel ratchets, its native fast gate, and sufficient delivery verification. Adoption now ships `testing-parallel-ratchet.json`, already classified as an installation behavior asset, instead of omitting a file required by the shipped Go gate. No gate or evidence-size bound was relaxed. Logs/exits: `rollout/adoption-a-focused.phvMOn/`; private source commit `7c2192c2b55ee1f989d5419bc4c1d5eab0dc14c9`.

## Fourth delivery and approved batch isolation

The fourth Stage A run completed all 58 groups on tree `b86f1a305c7306d31fdfe1a165f3acb524fb1b98`: 46 passed, nine reused, and three failed in 1,431,829 ms (23.86 minutes). Attempt `proof-mu9wdxec-53a9dc93cea251cc` exited 1; its complete structured result and native hashes are in `rollout/stage-a-fourth-delivery/`. This is insufficient delivery evidence.

The remaining failures exposed a synthetic wait fixture using the host's live admission directory, a normal source-pipe closure incorrectly treated as a public-output failure, and batch lifecycle tests assuming an idle host while one-shot queue cleanup could itself defer useful work. Corrections are being verified in a complete private snapshot of that exact A tree. A real `exec.Cmd.Wait` closed-pipe check fails before and passes after; the high-volume failed-output test still drains and returns red, and the previously failing `records-move-lands` scenario passes. The actual candidate wait executable passes the isolated synthetic fixture. FIFO, live-owner and one-shot queue checks pass. No production host lock or failure assertion is disabled. A future-clock attempt in the lifecycle fixture proved invalid because native terminal time must follow admission time; it is retained as an invalid run and the clock change is reverted.

Wido approved safe batch splitting in the current goal. Native Git witnesses prove removal of a red A and an inapplicable dependent B while preserving independent C, with ambiguous composition errors held and no partial return. A real command/JUnit red run, fresh base diagnosis, survivor proof/verification and remote survivor-only publication pass in `focused-final/batch-split-native-third.log`. The fenced-member public canary has proved actual fence detection and return; it also found a stale private candidate branch after seal moved the base. That recovery correction and final survivor publication remain under verification.

The B-only budget forecast and cold-build screen now pass the public post-handover spending-race case: typed refusal durably returns the member with zero additional candidate builds and zero application commands. Stored forecasts remain explicitly historical. The screen is not a reservation: a later spend can still cause a preparatory build before final admission refuses, and earlier policy rearm is outside it. Budget pooling and adaptive batch sizing remain follow-ups. These runtime counts do not establish a measured twofold intent-to-landed speedup.

At 17:03 CEST the complete private A named batch lifecycle passes (170.97 seconds), along with actual-candidate wait, named land replay, queue checks and the same six-test proof ownership/output selection under the race detector. The initial race selection failed on synthetic admission contention without a race warning; adding the existing test-only fixture mutex to its missing helper makes the unchanged selection pass. Eleven explicit A correction paths were promoted together; the next actual selected delivery remains required.

Fifth selected A delivery completed on `362fec6d7d4b666c5066bdace6e787395dd6cd87`: 51 passed, seven failed, no reuse, 23.49 minutes; archive `rollout/stage-a-fifth-delivery`. The registry anchor mismatch accounts for five failed groups, and an unisolated synthetic host admission accounts for the frozen-worker failure. Focused corrections pass. The two failed witness scenarios pass in a replay; the original child output was removed, so the precise archived cause remains unproved. Independent source analysis establishes a real output truncation race in the suite pipe wait ordering. A deterministic regression and correction precede the next delivery attempt.

B public fenced-member acceptance now passes on a coherent past-timestamp fixture timeline: actual shared capacity wait, completed elapsed-stop batch and exact queued-proof cancellation, zero native commands before release, B returned, healthy A proved and published, and A's handback completed. The existing three-member portable owner regression and moved-base branch case also pass. Evidence: `gle7-native-fence-result.md` and its exact logs. Earlier fixture stop initialization and future timestamps were invalid acceptance setups, retained as such. The real changed-base candidate branch and cross-tree retry defects were corrected independently with focused before/after checks.

The host schema-2 contract now declares the actual static prerequisites: the refusal registry check precedes the static gate, and supervision, adoption and landing fixtures require that gate. A focused selector canary verifies closure without selecting unrelated Go work; the existing real-command scheduler witness verifies that red prerequisites suppress only dependent commands. This addresses the repeated cheap-failure amplification observed during the A bridge rollout.

The complete five-case B batch lifecycle passes in 410.35 seconds, including two green units, red ejection, moved trunk, withdrawal and status/wait (`focused-final/batch-lifecycle-b-final-20m.log/.exit`). Its earlier combined run hit the chosen ten-minute test timeout under contention; all individual scenarios also passed, and no product timeout was raised. The final B review is split into four disjoint source shares through the existing MetaSystem read gate, preserving the exact baseline, candidate, patch hashes and actual job identifiers in `reviews/b-final-r1-fulltree/split-manifest.json`.

The suite output loss is reproduced independently of the original witness-gate symptom: a gated native child tail fails before and passes after custody-backed regular-file spooling. Finite final-prefix draining also passes against a self-extending writer. Actual public legacy borrowing with no inherited resource files, child cleanup, early failure after child start and landing receipt routes pass. Four production callers explicitly request custody without adding a reservation. Direct unmanaged library callers retain their earlier pipe behavior. The frozen correction is receiving its independent source read before the sixth selected delivery.

The final B first review completed four disjoint actual Sol reads. Six material findings are accepted in `reviews/b-final-r1-fulltree/root-dispositions.md`; the public splitting source share has no material finding. Every reader made fourteen calls against a twelve-call brief; that deviation is retained. The candidate-scoped retry correction has a failing-before regression and seven passing focused cases; the other accepted corrections and one bounded confirmation read remain outstanding. No first-round fix-first verdict is rewritten as clean.

Independent Sol source review of the eight output correction paths on private tree `f4736924c01b8a825f002d8a2c0ac4b5aac044fd` found zero material defects. The reader ran no tests; retained focused execution evidence is separate. Thirteen explicit private-A corrections, including the registry anchors and compatibility fixture isolation, are promoted together. Metadata audits, schema-1 contract checks and source compilation pass before the sixth exact selected delivery.

The sixth selected Stage A result is insufficient: 50 passed, seven reused, one failed in 22.41 minutes on `33ce7332e2f923a8e47a233fe3e78eecfcd295b2`. Complete results are archived in `rollout/stage-a-sixth-delivery`. The sole failure is adoption copied-delivery: retained orphan.out contains host-admission busy and exit 78, so the required registration refusal was never evaluated. A synthetic-only admission correction preserves the actual copied target runtime configuration and strict diagnostic.

Final B confirmation reads on `8e128ad522afb32c924702277516ff43ae27fcb9` report zero material findings. Mechanical closure passes for their empty current findings arrays; historical accepted findings remain in their original record and root correction ledger. Cost used its ten-call allowance; split used eleven against ten. Actual reviewer artifacts govern when the launcher parser misses a verdict. The full owner-driven moved-base multi-return witness also passes in 338 seconds: lawful A/B/C joins, real origin conflicts affecting A and B, two attributed returns, exact C proof and C-only remote publication/receipt, then complete claim handback. Native counts were a1/b1/c1 before the move and a2/b1/c1 afterwards; C was reused. This public topology is distinct from the native Git dependent-patch fixture; public join still refuses a statically stacked dependent unit. Final release selection and publication remain outstanding.

The bounded adoption correction passes the exact copied-delivery scenario in 49.12 seconds (`rollout/a6-adopt-copied-after.log/.exit`). It uses a separate fake authority directory for synthetic host admission and preserves the copied target’s Claude/Codex configuration. Positive registration, drift and strict orphan assertions all ran. Root independently inspected the complete 14-line test-only diff; identical bytes are promoted to A and B. No production guard or assertion was relaxed.

The seventh-run request was refused before preparation at attemptLimit 10/10, with 133/1200 proof minutes consumed. Its plan/refusal is archived separately; no native result was created. Under the user’s standing authorization, the supported enrolled-terminal `goal budget` command raised only the attempt allowance to 14 and recorded exception 1. Other limits remain unchanged. The goal-only remote commit `4d34582ec73ad032b568b9ffee67306df2409987` is fast-forwarded; no ledger was edited manually.

The seventh selected Stage A delivery completed during the usage pause on tree `19eedfe3821c2a6fb0a2a3ceea88973f181369c9`, attempt `proof-mua1bsvt-1afd2d30e93ea538`: 40 passed, 17 reused and one failed in 1,238,752 ms (20.65 minutes). Archive: `rollout/stage-a-seventh-delivery/`. The land records-move fixture's sterile environment dropped synthetic admission settings, causing busy exit 78 before its receipt assertion. The shared environment constructor now retains the private admission directory and config-backed fake checkout through all four callers; direct fake full-width-chain receipts bind the same context. Root inspected this test-only correction, preserved B-specific test names and promoted the exact bytes with a hash manifest. The exact records case passes; the direct sibling remains focused-unrun after the delegate's declared investigation limit. No production arbitration or assertion changed. The eighth selected run is required before A publication.

All delegates were checked after the quota reset. The resumed migration trace confirms that the enrolled A engine can plan the exact B schema-2 tree with 154 required groups, including 95 concrete Go package groups; batch-prefix planning also passes. B-only additive requirements remain applied by B after the retained A policy decision. This is planning evidence only, with the exact source and engine identities in `rollout/a7-to-b-preflight/preflight-result.md`; it does not prove native B execution.

The first eighth-run request was refused before native preparation at the goal's elapsed allowance. `1d` means eight working hours; observed elapsed was 8h1m12s. No native result exists for that refused request, archived separately under `rollout/stage-a-eighth-admission-refusal/`. Under the user's existing authorization, the enrolled-terminal budget command extended elapsed allowance to 12h (`1d4h`); all other allowances remain unchanged. The goal-only commit `3982209e914597c2efc47ed8941ca0e9bc82ce2c` is fast-forwarded, with product work preserved.

## Compatibility stage published and installed

The eighth selected Stage A run is sufficient: tree `4c923e0c2287c6f08c75f2de3a7314d1d31533ad`, attempt `proof-mua68a0u-8462116cb4e33adf`, 32 passed and 26 reused in 1,017,570 ms (16.96 minutes). All 45 land scenarios passed, including the records and full-width paths. Actual `test verify` passed with zero native launches. Complete raw evidence, hashes and an archive of the exact tested Git tree are retained in `rollout/stage-a-eighth-delivery/`.

Stage A is published as `7ebb8e308f0210cb66632774f3564a53a0273de2` and installed through the supported build/arm commands: accepted-engine generation 169, supervision generation 180 and checkout arm 69. Publication first detected different terminal PATH/locale values; reproducing the exact whitelisted test environment restored sufficient verification without rerunning tests. The workspace guard then required three ignored local locations to be temporarily held outside the checkout; same-filesystem renames preserved them and restored all three after publication. The new-plan hook acknowledgement reflects the user's explicit request for these plans. No proof guard was bypassed, no ignored content was committed and the real local secret configuration was not read or copied. A single real CLI receipt accompanied the product commit.

The final Stage B source is assembled on the installed A baseline. Its immutable final-review tree is `8e128ad522afb32c924702277516ff43ae27fcb9`; exact source comparison finds only two subsequent test-only corrections (adoption fixture admission and shared land fixture admission), both independently inspected by the coordinator and exercised in sufficient A8 runtime proof. The final B selected run, obligation closure, publication, installation and goal completion remain outstanding.

Cumulative timing uses retained attempts across budget revisions. The first seven selected runs consumed 132.57 minutes; A8 adds 16.96 minutes. This excludes focused tests, diagnostics, review and the idle quota pause. A budget edit begins a new spending episode while retaining the elapsed claim origin, so its current consumption counter is not a cumulative engineering-time measure. No overall percentage landing-time improvement is claimed.

## First final implementation delivery: complete failure collection

The first Stage B selected delivery is insufficient on exact candidate `dff84e0214d2258db96decf935d62fef5cca07f3`, attempt `proof-mua7anhi-e222926e5c557bf6`, plan `6212688ff7a52ba92a64278a2c90f6d65077701fde169c3faaa689d846f28eff`. It collected all independent groups: 139 passed, 12 failed, three blocked in 3,749,899 ms (62.50 minutes), with zero reuse. Fast-static failure correctly prevented native adoption, land and supervision fixture launches. Raw structured results, native log hashes and tested-candidate archive are preserved in `rollout/stage-b-first-delivery/` and `rollout/stage-b-first-candidate/`. Group failure counts are not distinct defect counts: several tests appear in named and whole-package groups.

Fourteen focused fixture corrections preserve actual assertions and isolate packet caps, host admission, stream capture, native producer registration and real Git publication identities. A dead helper reported by staticcheck was removed; focused staticcheck passes. The provenance fixture explicitly uses its existing owner seam, while separate public command/JUnit lifecycle tests cover real prefix proof. Exact changes and focused evidence are recorded in `rollout/b1-accepted-corrections.json`. No production admission or proof identity was weakened.

The final result also detected eleven missing protected test IDs. Replacement names cannot satisfy the installed engine's same-package coverage floor, and no supported retirement API exists. Real checks are being restored under their stable names and re-added through the canonical contract editor. Their fixture-selection regression exposed missing exact script-to-section mappings in this application's contract; that coverage is being restored without adding application-specific executor logic. The complete correction batch must pass inventory checks and B2 selected delivery before publication.

Cumulative selected runtime through B1 is 212.03 minutes (149.53 minutes for A1–A8 plus 62.50 for B1), excluding focused testing, reviews and the quota pause. The matched portable fixture reduced candidate-build entry requests from seven to one; this is eliminated duplicate work, not a measured total latency percentage. There is no before/after p50/p95 landing estimate.

The protected-name correction now passes all eleven restored tests in their original packages. Canonical policy, source inventory, host coverage, static prerequisite and fixture-mapping checks pass. Fourteen exact-path surfaces restore the fifteen missing fixture-script mappings; the semantic merge's required residual/context union is retained. A real concurrency regression adds one documented walltime failure ceiling, with the walltime, serial and shared-main ratchets passing. Root conformance and the immutable correction manifest distinguish these post-B1 changes from the earlier independent source review; no new blanket review cycle is claimed. The corrected B2 source still requires its own sufficient selected result.

## Stage B2 complete failure collection

B2 on `9a6bc43fc5a17c97eb7782e80971c10c34c4682e`, attempt `proof-muaa9oxk-0aef832384e40712`, ended insufficient: 6 failed, 151 passed, 9 reused in 71.24 minutes. The complete result, native logs and hashes are archived under `rollout/stage-b-second-delivery/`; its exact source is in `rollout/stage-b-second-candidate/`. Nonpassing groups: `go-affected/internal-proofrun-3247c2001b46`, `go-batchtest`, `section/agent-protocol-fixtures`, `section/adoption-fixtures`, `section/land-fixtures`, `section/supervision-and-census-fixtures`. Root reviewed the correction batch, including: the portable owner fixtures use their existing deterministic clock and host sampler; the withdrawn reservation fixture isolates admission state and requires successful reservations; canonical host setup adds the already shipped Claude PreToolUse registration. Focused checks passed. Further corrections cover private goal-test admission, isolated recertified landing admission, idempotent regular completion markers, early and queued stop-fence checks, and an adopted application contract that avoids recursively testing MetaSystem. All relevant focused evidence and limitations must be present before promotion. The full corrected selection has not run. Exact correction hashes and evidence are in `rollout/b2-accepted-corrections.json`. All fifteen correction paths are frozen and focused checks pass; the stopped-fence epoch finding is fixed and independently confirmed. B3 must prove the corrected candidate before publication. The supported elapsed allowance is 16 hours, published in goal-only commit `5c0610372b06f9b6fb248c5f4d1994f1de2e0d96`, with its original elapsed origin preserved and other limits unchanged. A diagnostic pass after a failure never made B2 sufficient.

Automatic isolated Go diagnostics reran the three portable failures and the admission sequence case. All passed in isolation, but the runner preserved the original failures. The three portable native reruns alone took 559.82 seconds; this is diagnostic test time, not an overall speedup measurement. The accepted fixture fixes remove dependence on host readiness/shared state instead of relaxing assertions or relying on retries.

## Stage B3 complete failure collection

Candidate `47677dcf8b3c162ea8e55a5f642ee236188607f2`, attempt `proof-muadjss2-973e56f2a850c8d7`, ended insufficient: 3 blocked, 11 failed, 2 invalid, 142 passed, 8 reused in 63.68 minutes. The full structured result and native logs are archived in `rollout/stage-b-third-delivery/`; the exact source archive is `rollout/stage-b-third-candidate/`. Nonpassing groups: `batch-buildb-standard`, `batch-buildcd-standard`, `dispatch-proof-standard`, `go-affected/cmd-metasystem-09d86339d69b`, `go-affected/internal-landing-batch-d31deb631028`, `go-affected/internal-proofrun-3247c2001b46`, `go-affected/internal-refusal-ed3773452572`, `go-affected/internal-testenv-f16ebbd7e868`, `go-batchtest`, `section/suite-progress-fixtures`, `section/witness-gate-fixtures`, `test-environment-standard`, `fast-static-build`, `section/adoption-fixtures`, `section/land-fixtures`, `section/supervision-and-census-fixtures`. The corrected portable fixtures passed concurrently after their legacy batch locks were isolated in the existing validated fixture namespace. The owned checkout must itself authorize fake-runtime fixtures; real projects retain the production lock. The moved-base fixture advances its clock from the surviving member's join without moving time backwards; its final-source public run passed. A separate deterministic regression exposed and fixed a standalone daemon clock that never advanced its maximum wait; the supervised component and one-shot tick retain their existing semantics. The seal regression now observes a real nonblocking kernel lock while performing a real join during forecast, then checks stale-seal refusal; its timer, worker goroutines and obsolete walltime allowance were removed. Seven refusal source-location anchors and their exact-site expectations were reconciled with the earlier stop-fence insertion and request-scoped fixture helper, without changing emitted codes or widening the audit. Goal TestMain preserves the shared harness exit code while retaining isolated admission and cleanup. Request-scoped admission directories isolate temporary fake-root Go fixtures without process environment mutation; default production locking, census and explicit shared-contention domains remain intact. The two independent shell fixture beds now select their own validated fake-runtime admission namespace. Their original children had waited for global capacity held by their outer proof; exact owned processes were terminated after identity rechecks, preserving two invalid group results and allowing independent checks to finish (`rollout/b3-stalled-fixture-stop.json`). The full suite-progress and witness-gate beds passed on a coherent disposable candidate snapshot after correction. Focused checks and scoped independent source reads passed. Exact correction paths, hashes and limitations are in `rollout/b3-accepted-corrections.json`. Diagnostic passes never made B3 sufficient. The next assembled candidate still needs its own complete selected proof.

## Stage B4 complete failure collection

Candidate `61f304a46046da04f5d4eb935a5c7998f2bfc821`, attempt `proof-muag35zx-add5076f5cee88f3`, completed with 158 passed groups, seven reused groups and one failed adoption fixture in 59.29 minutes. All 45 landing fixture legs and the other nine adoption scenarios passed. The default adoption scenario scrubbed its outer proof identity then waited on the enclosing proof’s global host admission; exact owned fixture processes were stopped and its failure retained. The eight-line fixture correction gives only that nested validation a separate temporary, config-authorized admission directory. Its real adopted runtime and exact dangling-link refusal assertion are unchanged. The exact nested command reached that refusal under the corrected admission selection. A separate standalone full-default attempt stopped before scenarios because its witness preflight encountered a govulncheck SSA panic; this is not passing scenario evidence. Full selected proof of the corrected script remains required. The complete B4 result is in `rollout/stage-b-fourth-delivery/`, the intervention in `rollout/b4-stalled-adoption-stop.json`, and the correction/source review and focused artifacts in the same durable evidence root. A script-only edit changes declared inputs for 138 of 166 groups because those groups currently include `scripts/**`; the other 28 are eligible for machine-checked reuse. The next public plan and verification decide actual reuse.


## Stage B5 complete failure collection

Candidate `d6dd5af7a2c8c83f022f09571347fdeaa4f0d191`, attempt `proof-muaig8re-ce5aee9ec2e3a809`, ended insufficient: 1 failed, 137 passed, 28 reused of 166 required groups in 53.20 minutes. Nonpassing groups: `go-affected/internal-landing-batch-d31deb631028`. The batch join lock test failed its five-second Git callback readiness precondition before testing lock ownership. Every other selected group passed or reused valid evidence; the original failure remains failed. The correction proves lock and capacity behavior through real nonblocking kernel locks, once-per-wait resource observation, state/child-exit synchronization and complete private-slot saturation. The existing deterministic fixture census is restricted to actual fake control roots and canonical temporary namespaces; symlink escapes and overflow are refused. Exact refusal-site locations and decreased walltime allowances are audited. The exact 11-file correction and reviewed focused evidence are bound in `rollout/b5-accepted-corrections.json`; complete B5 native evidence remains archived at `rollout/stage-b-fifth-delivery/`. Focused command, owner, batch, admission, census-boundary and shared source checks passed. A new exact selected delivery proof remains required; the full matched old/new performance cohort, four-hour opt-in rehearsal and final Linux runtime remain unmeasured or unrun. B5 is not sufficient; exact B6 destination-selected proof and verify are still required. No new release estimate is claimed.


## Stage B6 complete failure collection

Candidate `86eb889c84af6c957ed69a65f4c39ffde7f2cdcb`, attempt `proof-muaktsf0-bbfea4b9bba087d1`, ended insufficient: 155 passed, seven reused, one failed and three blocked of 166 required groups in 48.53 minutes. Only `fast-static-build` failed: pinned staticcheck reported S1005 for a discarded receive assignment in `proof_run_test.go`; its three dependent sections were blocked. The private correction changes only `_ = <-finished` to `<-finished`; pinned focused staticcheck exited 0. The original B6 group remains failed and the focused check does not certify delivery. Exact B7 selected proof and verify are required; publication, installation and goal completion remain pending. No release ETA is asserted.


## Final selected delivery proof

Candidate `390357e8e1675048ad5bceceefdc1b479e775ace` passed its complete selected delivery proof: 54 passed, 112 reused of 166 required groups in 49.72 minutes. Attempt `proof-muamxtj4-3c765243d2af4133`, plan `c29ac807003d841df1278207388d7938efff8445e838c56a10bdc145e4c5c08b`; the supported exact-tree verification exited 0. Source and native evidence are retained outside the checkout under `goal-landing-efficiency-20260920/rollout/`. The real generic command/JUnit application publication, prefix refusal, recovery, capacity and batch-splitting witnesses are linked in the obligation table. This establishes the capability proof; publication and installation of this release are still pending. The staged document-only publication tree must pass the public plan and verify commands before the supported commit/push flow.

Critical and high capability obligations GLE-1–8 are now supported by the final selected proof and the linked public runtime witnesses. GLE-9 remains medium and partial: workflow/output-ownership checks exist, but model-wait reduction is unmeasured. GLE-10 explicitly retains the Section 11 full matched-cohort and latency-distribution measurement gap; this release does not claim that the full performance acceptance program is complete. The four-hour opt-in host rehearsal and final Linux runtime proof were not run. The seven-case portable cohort reduced candidate-build entrypoint calls from seven to one; this is neither compiler elapsed time nor a measured overall landing speedup. The separately authorized next goal makes all tests parallel-safe, bounds configurable concurrency by available CPU capacity, and removes load-sensitive timing assumptions. Completed B1–B4 attribution is retained in `rollout/b1-b4-critical-path-attribution.md`: B4 preparation was about 24 seconds; overlapping command Go and batch Go groups ran for about 42 minutes, with late static admission and integration fixtures forming the remaining tail. Those failed development runs do not establish an old-versus-new landing speedup.
