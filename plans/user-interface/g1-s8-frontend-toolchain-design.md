# g1-s8 Frontend toolchain and embedded bundle

- Gate 1, state `designed`, revision 4 after Sol's round-3 design review (D12), author Claude on Fable as a D23 delegate, 2026-09-21.
- Revision 4 answers H1 to H3 of [Sol's round 3](g1-s8-frontend-toolchain-design-sol-review.md): the audit forces online mode and includes every dependency type at the invocation itself, with fixtures showing that ambient `offline` and `omit` configuration cannot make it green; `scripts/agents/path-class-fixtures.sh` joins the exclusion slice with its own exclusion and fixture; a poisoned-tree guard proves the exclusion complete empirically, since two sweeps each missed a walker; `xargs` is portable non-zero. Sol's three corrections (`--include=dev` clears only `dev`; npm writes JSON to stdout and logs to stderr; `audit-level` defaults to `null`, read as `low`) are folded in where the document relies on them. Nothing Sol confirmed is reopened.
- Revision 3 answers G1 to G4 of [Sol's round 2](g1-s8-frontend-toolchain-design-sol-review.md): the audit reads npm's JSON and refuses on any count, `info` included; the verification runs the Git-fed gofmt, never `gofmt -l internal cmd`; `scripts/agents/brain-fixtures.sh` joins the exclusion slice with a `node_modules` prune and a fixture; the gofmt pipeline drops index entries that no longer exist on disk; G6's traversals are recorded as known and harmless. Nothing Sol confirmed is reopened.
- Revision 2 answers E1 to E6 and E12 of [Sol's review](g1-s8-frontend-toolchain-design-sol-review.md): an engine-side exclusion for the dependency tree from a slice that lands first; exact versions, `--ignore-scripts`, an audit gate; the manifest as publish point; `Dialog.Overlay`; reserved paths and `Accept` parsing; the digest's text rule and canonical manifest; Node `24.21.0`.
- Refines the [master read](../user-interface-design.md) at `ui-development` commit `0a5ef07a5` and [Astra's second review, section 4](../user-interface-design-critique-astra-r2.md). Depends on `g1-s1` revision 6, the exclusion slice below, and D1, D7, D8, D13, D14, D15, D20, D21, D23; the technical half of D21 step 2, on which `g1-s9` puts the shell.

Paths are relative to `metasystem/` unless they start with `plans/`. Code claims were verified by reading the source at `762446dfe` and the Go 1.27.1 tree, revision 3's additions at the working tree of `e0d2beefe`, revision 4's at `70cd8d776`; nothing was executed. npm claims cite `npm/cli` at tag `v11.19.1`, the tag Sol verified, with paths relative to that repository and its vendored `node_modules` (Arborist 9.9.1, `@npmcli/config` 10.12.0, nopt 9.0.0, npm-registry-fetch 19.1.1, make-fetch-happen 15.0.6, npm-audit-report 7.0.0); Node `24.21.0` bundles npm 11.19.0, and that the cited lines are unchanged between the two patch releases is not verified, so the first run records the npm version it drove. Registry, npm, Git, and upstream facts were read on 2026-09-21.

## Outcome

A React page, built ahead of time, committed, and embedded in `metasystem`, shows the facts from `/-/health`, a button opening a modal dialog, the path requested, and a link to the open-source notices; any path reloads to it. Nothing is fetched from outside the server, no inline script runs, and the dialog's scroll-lock style is allowed by a per-response nonce. `go test ./internal/ui/web/` fails when the bundle is stale or mismatches its manifest. A build without a bundle says so. Installing the dependencies changes no engine digest.

## Scope and non-goals

In scope: the layout and its fence, including what the exclusion slice must deliver; the npm toolchain, Node pin, and bundle script; `internal/ui/web`; the manifest and its tests; routes, fallback, content types, and the policy in `internal/ui/httpd`; development mode; notices; Vitest; the modal check. Not: the exclusion slice's code; the shell, a router, themes, tokens (`g1-s9`, `g1-s18`); `/api/` (`g1-s7`); caching beyond `no-store`; sign-in; `internal/ui/lifecycle`.

## Existing code this builds on

The [g1-s1 design](g1-s1-server-lifecycle-design.md): checks, five headers, `GET /-/health`, placeholder at `/`, 404 otherwise, wired in `cmd/metasystem/ui.go`. The build has no pattern (`scripts/agents/go-build.sh:99` to `:101`); `./...` excludes `_` directories and nested modules (Go 1.27.1 `src/cmd/go/alldocs.go:3208` to `:3213`). ENGINE is `cmd/**`, `internal/**`, `scripts/agents/**`, `go.mod`, `go.sum`, one record (`internal/behaviorsurface/policy.v2.json:3` to `:10`); its walk skips only `.git` and the prefix filter (`policy.go:347` to `:436`) and governs admission (`internal/dispatch/governed.go:27`). Proof groups declare `metasystem/internal/**` (`testing.json:50`, `:52`, `:60`, `:75`, `:76`, `:79`, `:102`); every match is hashed (`internal/proofrun/test_build.go:1780` to `:1808`). The gate runs gofmt, the ratchet, and the stop-surface audit (`scripts/agents/go-gate.sh:520`, `:170`, `:584`; `cmd/metasystem/audit.go:132`, `:143`). Embed refuses `..`, symlinks, and nested modules; `all:` includes dot and underscore names (`src/embed/embed.go:68` to `:96`). Adoption is `git archive HEAD` under an allowlist containing `internal`, merging the top-level `.gitignore` (`scripts/adopt.sh:228` to `:234`, `:246`, `:365`, `:373` to `:379`).

## Contracts

### Source layout

```
internal/ui/web/                 Go package web
  embed.go web.go digest.go accept.go contenttype.go *_test.go testmain_test.go
  bundle/                        //go:embed all:bundle
    README.txt                   committed, so the pattern always matches
    bundle.json                  the manifest, written last
    dist/                        Vite output, emptied on every build
      index.html assets/app.js assets/app.css assets/*.woff2 THIRD-PARTY-NOTICES.txt
  _app/                          the React source, the npm project root
    package.json package-lock.json .nvmrc .npmrc index.html vite.config.ts tsconfig.json
    src/ public/ scripts/ node_modules/ (ignored; excluded below)
```

**The fence.** Go tooling over `./...` never enters an underscore directory; no nested `go.mod`, no `ignore` directive. The engine's own walkers do enter it: installed dependencies would join the ENGINE digest and the proof-input digests (E1); underscore, `.gitignore`, and pruning `.go` files fence none of them. A separate slice, specified next, adds the exclusion; until it is on `ui-development`, no dependency may be installed in this repository: no `npm ci`, no `npm install`, no restored cache. Revision 1's `postinstall` prune of `.go` files is dropped: `--ignore-scripts` skips lifecycle scripts, the prune left a window after install, and once the walkers are excluded and gofmt is fed from Git, a `.go` file inside `node_modules` is invisible to every tool that renders a verdict, a claim the slice proves with the poisoned-tree guard below rather than by enumerating walkers, two sweeps having each missed one; the traversals that remain are listed under the slice as known and harmless.

`bundle/` is inside the package directory of the engine's own module: no module crossing, no `..`. `Dist()` is `fs.Sub(bundle, "bundle/dist")`; `README.txt` and `bundle.json` are never served. `metasystem/.gitignore` gains one line, `/internal/ui/web/_app/node_modules/`, anchored so a root installation does not ignore the application's own `node_modules`, in the top-level file because adoption drops nested ones; the line is the exclusion slice's to add, not this slice's, because that slice's guard plants a tree at this path and the Git-fed pipelines below must already ignore it. Adoption ships `_app`, `bundle/`, and the Go files, never `node_modules`; source ships because the payload rebuilds engine source (`adopt.sh:242` to `:245`), the bundle because the adopter has no Node.

### The exclusion slice

Lands before this slice, by its own design, critique, and build; the planner numbers it. It edits `internal/behaviorsurface`, `internal/pathpattern`, `internal/proofrun`, `internal/parallelratchet`, `internal/audit`, `scripts/agents/go-gate.sh`, `scripts/agents/brain-fixtures.sh`, `scripts/agents/path-class-fixtures.sh`, and `metasystem/.gitignore`, and adds `scripts/agents/dependency-tree-guard.sh`; `g1-s8` touches none of them. It must deliver:

**Membership.** `policy.v2.json` `nonRepositoryPaths` gains `internal/ui/web/_app/node_modules/**`, and `Policy.Includes` (`policy.go:279` to `:312`) returns false for a path matching `nonRepositoryPaths` in every projection, not only LANDING (`:306`). The existing list is the home: its class already means content on disk that is not repository content (`.git/**`, `bin/**`, rendered docs, `metasystem.conf.local`; `policy.go:42`, `policy.v2.json:76` to `:82`), and ENGINE (`:304`) and PAYLOAD (`:308`) not honouring it is the gap E1 fell through. The glob is exact because the grammar allows an exact path or a `/**` prefix and nothing between (`:54` to `:56`, `:147` to `:156`), and installation-relative, naming the same tree nested or at the root (`NormalizePath`, `:183` to `:213`). Every consumer of `Includes` follows unchanged: the digest walk (`:347` to `:436`), `ListPaths` (`:442` to `:475`), admission (`internal/dispatch/governed.go:27`), the export (`internal/proofrun/manifest.go:164`), and the classifiers at `cmd/metasystem/proof_run.go:2157`, `landing_batch_land.go:359`, `rearm_on_landed.go:243`, `internal/steward/rearm_resolver.go:576`, `internal/gaterun/weight.go:248`. A walk may `SkipDir` a matched directory; that is speed, not membership.

**Walkers without a policy** skip a directory named `node_modules` at any depth, as `vendor` is skipped by name in `parallel.go:123`, `internal/testselect/select.go:199`, `internal/gopackages/select.go:278`, and `internal/landing/batch/unitgate.go:307`: `pathpattern.Pattern.Expand` (`pattern.go:139` to `:171`, beside its `.git` rule at `:151`; `testing.json`'s `metasystem/internal/**` declarations reach it through `test_build.go:1786`); `parallelratchet.ScanParallelTests` (`parallel.go:123`); `audit.discoverStopSurfaceFiles` (`stopsurface.go:304`; its Git-tree twin at `:326` to `:349` needs nothing); `proofrun.readManifest` (`manifest.go:96` and `:101` through `hardExcluded`, `:148` to `:154`), so a frozen candidate receives no copy of the tree. One name repeated four times beats a shared predicate: `pathpattern` imports nothing internal, `parallelratchet` only `atomicfile`, `audit/stopsurface` only `gittree`, and a predicate would thread a `behaviorsurface` import through packages another agent has in flight. One fixture test per walker: a `node_modules/x_test.go` beneath the root is not seen.

**A shell walker with a verdict** (G3). `scripts/agents/brain-fixtures.sh`, scenario `brain-actor-seam-coverage` (`:338` to `:387`, under `set -euo pipefail`, `:2`, with `root` the module root, `:4`), runs `cd "$root" && find cmd/metasystem internal -type f -name '*.go' ! -name '*_test.go' -exec grep -H -E '<the four expressions at :340>' {} +`, drops one known line, sorts (`:339` to `:342`), and compares the result with an exact allowlist (`:343` to `:380`), refusing on any difference (`:382` to `:386`). `section/brain-fixtures` is an acceptance and cadence surface (`testing.json:102`, `:128`), and the `brain-seat` group runs it on every change to the script itself (`:17`). A dependency's `.go` file containing `goal.Actor{` would add a line and refuse the scenario. The slice inserts a name-based prune ahead of the file test, so the scan reads `find cmd/metasystem internal -type d -name node_modules -prune -o -type f -name '*.go' ! -name '*_test.go' -exec grep -H -E '<unchanged>' {} +`, and moves the scan into a function that takes its root. Fixture, in the same scenario, before the allowlist comparison: under `$tmp` (`:31`) plant `seam/cmd/metasystem/seen.go` and `seam/internal/x/node_modules/p/hidden.go`, each a single line containing `classifyVerbCaller(`; the function run at `seam` must print exactly one line, naming `cmd/metasystem/seen.go`. The section's verdict then covers the prune and the allowlist together.

**A second shell walker with a verdict** (H2). `scripts/agents/path-class-fixtures.sh`, `TestDeletedListsHaveNoReader` (`:87` to `:134`, under `set -euo pipefail`, `:2`, with `root` the module root, `:4`), derives its roots from `scripts/agents/path-classes.txt`: every `install:` key classed `behavior` that exists on disk (`:92` to `:94`), which includes `cmd/`, `internal/`, and `scripts/` (`path-classes.txt:15`, `:16`, `:18`). It then runs `grep -rnE -I --exclude-dir=reviews --exclude=journey.md "$pattern" -- <roots>` from `$root` (`:100` to `:103`), where `$pattern` is `register-carriage-paths|instruction-bearing-paths|neverDirectFix`, assembled at `:90` by string concatenation so the script's own text never matches its own scan (`scripts/` is a root). Grep status 0 means a reader was found: the matches are printed and the script exits 1 (`:106` to `:109`); status 1 is clean; anything else is a failed search and also exit 1 (`:110` to `:114`); the `repo:` roots get the same treatment from the parent directory (`:118` to `:133`). The test runs unconditionally (`:369`); the script is the second half of `section/static-reproof-fixtures` (`validate-metasystem.sh:1185` to `:1191`), an acceptance group (`testing.json:108`) selected for the `static-reproof-fixture` and `gate-plumbing` surfaces, both of which name this script (`:36`, `:13`), so no `testing.json` edit is needed. A text file under `internal/ui/web/_app/node_modules` containing `neverDirectFix`, a README or a changelog, is one grep line and a refused section; `-I` skips only binaries. The slice adds `--exclude-dir=node_modules` to both greps (`:102`, `:121`), beside the `--exclude-dir=reviews` already there, which is what makes the option known-portable on this script's grep, and moves each search into a function taking the directory to search from and the roots to search. Fixture, in the same test, before the real search: under `$tmp` (`:6`) plant `readers/cmd/seen.txt` and `readers/internal/x/node_modules/p/notes.txt`, each one line containing `neverDirectFix`, written with the same split-string concatenation as `:90` so the fixture script stays clean under its own scan; the function run at `readers` over `cmd/ internal/` must return 0 and print exactly one line, naming `cmd/seen.txt`. Without the exclusion it prints two lines and the fixture refuses; without the seen file it would prove nothing about the search.

**Known traversals, left alone** (G6). Four more walks meet the installed tree and change no verdict today; the reasons are recorded so the next reader does not re-derive them. This list is a record, not a proof of completeness: round 2's sweep missed `brain-fixtures.sh` and round 3's missed `path-class-fixtures.sh`, so completeness is claimed only by the guard below, never by this paragraph. The metasystem audit's instruction inventory (`internal/audit/metasystem.go:129` to `:134`) walks the whole root through `auditFindNamed` (`:393` to `:419`), skipping only `.git` and the top-level `artifacts`, and appends every file named `wow.md`, `SKILL.md`, `AGENT.md`, or a runtime's instruction file (`:507` to `:513`, `internal/runtimes/runtimes.go:352`) to `Report`; the verdict is `Violations` alone (`cmd/metasystem/audit.go:92` to `:100`), and the two verdict-bearing scans walk named roots that never include `internal` (`auditScanRoots`, `metasystem.go:34` to `:46`; `auditResidueMarkers`, `:275` to `:289`, over `plans/*design*.md`), so a dependency shipping an `AGENTS.md` adds a report line and nothing else. Working-unit import discovery (`internal/landing/batch/unitgate.go:87`, `:91`) enumerates through Git (`workingGoFiles`, `:102` to `:115`, the same pathspec as the gofmt pipeline below), and although `packageImports` (`:300` to `:335`) walks every directory but `vendor`, `.git`, and nested modules, it parses only files in that Git set (`:326` to `:328`): an ignored tree is entered and never read. `testselect.discoverPackages` (`internal/testselect/select.go:158` to `:192`) enters underscore directories, skipping only dot names, `testdata`, `vendor`, `artifacts`, and `bin` (`:194` to `:204`); nothing outside the package imports `internal/testselect` (only `select_test.go` does), so E10's inference is now a verified fact. From `scripts/`, `fixture-budget.sh:182` to `:197` (`harness_fixture_warn_if_engine_stale`, called by six fixture scripts, `brain-fixtures.sh:28` among them) walks the module root for a `.go` file newer than `bin/metasystem`, pruning only `.git` and `artifacts` (`:188` to `:190`), to print a stale-engine warning and continue (`:191` to `:196`); a freshly installed dependency `.go` file could raise that warning falsely, and no verdict depends on it. None of these joins the slice; pruning them by name would be speed, not correctness.

**gofmt.** `gofmt.go:423` to `:433` recurses into every directory and `:93` to `:95` skips only dot-prefixed file names, so `go-gate.sh:520`, which runs from `metasystem/` under `set -euo pipefail` (`:18`, `:21`), becomes a Git-fed list:

```
unformatted=$(git ls-files -z --cached --others --exclude-standard -- '*.go' \
  | while IFS= read -r -d '' f; do if [[ -f "$f" ]]; then printf '%s\0' "$f"; fi; done \
  | xargs -0 -r gofmt -l 2>&1) || gofmt_rc=$?
```

Every Go file Git sees, tracked or new, never an ignored tree: a plain pathspec's `*` matches `/` (gitglossary, pathspec: "`*` and `?` can match directory separators"), the pathspec `unitgate.go:103` already relies on. `-z` prints each name verbatim and NUL-terminated, never quoted (git-ls-files, `-z`); `read -d ''` and `printf '%s\0'` carry it unchanged; `xargs -0` hands it whole to gofmt; so spaces and non-ASCII names survive. The `-f` test is G4: `--cached` lists every index entry, "all tracked files", and an unstaged deletion is a separate state (`--deleted`), so a tracked `.go` file deleted only in the working tree is listed; without the filter gofmt receives a path that does not exist, exits 2, and `:526` to `:527` refuses, where the old directory walk simply never saw the absent file. The test is an `if` without `else`, so a skipped entry leaves the loop's status zero under `pipefail`. Behaviour: no matches, `xargs -r` starts no gofmt (BSD and GNU both accept it), `unformatted` is empty, `gofmt_rc` is 0, green; a tracked file deleted in the working tree is skipped and the gate is green as before; a tracked file that is unformatted is printed by `gofmt -l`, gofmt and xargs exit 0, and the non-empty output is the red at `:528` to `:529` ("gofmt would change"; `:529`'s unquoted `printf` splits a name with spaces for display only, the verdict is unaffected); a file gofmt cannot parse makes gofmt exit 2 and xargs non-zero (H3: POSIX promises only a value from 1 to 125 when an invoked utility fails; GNU and several BSDs choose 123, macOS documents 1; the gate compares with zero and nothing else, `:526` to `:529`; not executed here), the red at `:527` naming the status and the message; a failing `git ls-files` (no repository) gives `gofmt_rc` its status through `pipefail` with git's own message on the gate's stderr, since only the gofmt stage is captured. Sol's round 2 (G7) counted the same 1,601 files under the old and the new enumeration on this checkout. A "no `.go` beneath `node_modules`" rule alone is not enough, for the reasons under the fence.

**Digest neutrality**, what makes this safe beside in-flight work: an exclusion whose glob matches nothing changes no digest. The glob matches nothing while `_app` is absent; no existing `nonRepositoryPaths` entry lies under an `enginePaths` root; under PAYLOAD, `docs/paper/.obsidian/**` and `docs/paper/rendered/**` lie under `docs/**` but are absent from this checkout. The slice records, with `_app` absent, that for each projection `bin/metasystem behavior-surface digest --root <toplevel> --prefix metasystem --projection P --endpoint check` (`cmd/metasystem/behavior_surface.go:145`) prints the same digest from binaries built at the base commit and at the slice's commit, over the same tree, and that `audit parallel-ratchet` and `audit stop-decision-surface` agree. That slice lands; only then does the first `npm ci` run.

**The guard**, `scripts/agents/dependency-tree-guard.sh`, the slice's proof that the exclusion is complete. Two sweeps have each claimed to have found every walker and each missed one, so a third sweep would be worth nothing. The guard proves the property the fence asserts, that nothing under the dependency directory changes any verdict, without depending on anyone having enumerated the walkers correctly: it plants an adversarial tree, runs the repository's own validation with and without it, and requires every verdict to be identical. It runs from `metasystem/`, needs `bin/metasystem` built from the checkout and a clean `git status --porcelain`, and refuses to start otherwise or when `internal/ui/web/_app/node_modules/poison` already exists.

Planted, under `internal/ui/web/_app/node_modules/poison/`, creating `_app` when absent and removing everything it created on exit (`trap ... EXIT`), the tree being ignored by the slice's `.gitignore` line:
- `seam.go`: `package poison`, not a test file, unformatted (space-indented, so gofmt would rewrite it), holding each of the four expressions `brain-fixtures.sh:340` scans for, `goal.Actor{`, `Actor.Human =`, `classifyVerbCaller(`, and `lease.ClassifyVerbAt(e.Root`, one per line inside a raw string; and a copy at `poison/node_modules/deeper/seam.go`, one nesting down, since the exclusions work by name at any depth;
- `broken.go`: `package poison` followed by `func {`, which gofmt cannot parse (exit 2) and no Go tool can build;
- `serial_test.go`: `package poison` with `func TestPoison(t *testing.T) {}` and no `t.Parallel()`, the shape `ScanParallelTests` counts and `discoverStopSurfaceFiles` selects (`stopsurface.go:289` to `:324`, kinds at `:351` to `:358`);
- `späce d_test.go`: the same content under a name with a space and a non-ASCII letter, for every `find`, `xargs`, and pathspec on the way;
- `notes.txt`: one line holding the three tokens of `path-class-fixtures.sh:90`, written by concatenation as that script writes them;
- `AGENTS.md`, `wow.md`, `SKILL.md`, and `CLAUDE.md`: one line each, names the metasystem audit's instruction inventory collects (`internal/audit/metasystem.go:507` to `:513`);
- `up`: a symbolic link to `../../../../..`, which is `internal/`, so a walker that followed links would loop or double-count;
- `go.mod` declaring `module poison` and `package.json` declaring a `postinstall` script, so anything treating the tree as a module or a project shows itself.

Run, twice, once with the tree absent and once planted, each command's exit status and output captured: `git status --porcelain`; for each of ENGINE, LANDING, and PAYLOAD, `bin/metasystem behavior-surface digest --root <toplevel> --prefix metasystem --projection P --endpoint check`; `bin/metasystem audit parallel-ratchet`; `bin/metasystem audit stop-decision-surface`; `bash scripts/audit-metasystem.sh`, whose verb prints its report inventory to stdout and its violations to stderr (`cmd/metasystem/audit.go:92` to `:101`); `bash scripts/agents/go-gate.sh`; `bash scripts/validate-metasystem.sh` with no argument, which selects every section (`validate-metasystem.sh:30` to `:31`, `:65` to `:71`) and prints one `SECTION GREEN: <id>` or `SECTION RED: <id> (exit N)` line per section (`:374`, `:383`); and `bin/metasystem test run` (`cmd/metasystem/test.go:1309`) for one group whose inputs declare `metasystem/internal/**`, `section/brain-fixtures` (`testing.json:102`), keeping the result's `inputDigest` (`internal/proofrun/test_result.go:47`, computed at `test_build.go:1006` and `:1013`); the flags of that verb are read from `runTestRun` by the guard's author and are not verified here.

Compared, byte for byte, refusing on the first difference and naming it: every exit status; the `git status --porcelain` output, which proves the ignore line; each digest; the stdout of the ratchet and stop-surface audits; the stderr of the metasystem audit, its violations, while its stdout, the inventory, is required to differ by exactly the four planted instruction files and nothing else, which makes G6's record empirical; the gate's final verdict line; the ordered section verdict lines of the validation run; and the `inputDigest`. Timings and paths under `$TMPDIR` are not compared. The guard is not a validation section, because it runs the validation itself; it is run and both transcripts kept by the exclusion slice, with `_app` absent, and again by this slice after the first `npm ci`, with the real `node_modules` present in both runs and only the poison planted and removed; O11 names both. Without the exclusions the guard refuses at the first digest; the enumerated exclusions are what make it pass, and the guard is what makes their completeness a measured fact rather than a searched one.

### The build

**Node `24.21.0`**, decided: the Active LTS (Krypton, 2026-09-07, npm 11.19.0), supported to April 2028; `26.9.0` is Current until its LTS transition on 28 October 2026. Recorded in `_app/.nvmrc` and `engines.node`; enforced by the bundle script, which refuses unless `process.version` is `v24.21.0`, naming both. A developer on `26.8.2` installs `24.21.0` with a version manager that honours `.nvmrc` (`nvm`, `fnm`, `mise`) and leaves the Homebrew Node alone.

**Installs** are `npm ci --ignore-scripts`; `_app/.npmrc` carries `ignore-scripts=true` and `save-exact=true`; `package-lock.json` is committed; `package.json` pins every version exactly. No direct dependency declares an install script (registry, 2026-09-21); the native binaries of TypeScript, Rolldown, Lightning CSS, and `@tailwindcss/oxide` are optional platform packages. Transitive install scripts are not verified here: the first `npm ci --ignore-scripts` with green `npm test` and `npm run bundle` proves none is needed; a package that needs one is a finding, never a reason to drop the flag. Playwright's browser: `node_modules/.bin/playwright install chromium`, into the user cache.

**Audit** (G1, H1). After `npm ci --ignore-scripts`, the bundle script runs, in `_app`, exactly

```
npm audit --json --no-offline --include=dev --include=optional --include=peer
```

with stdout and stderr captured separately, and continues only when all five hold: the exit status is 0; stdout parses as a JSON object; `metadata.vulnerabilities.total` is the number 0; `vulnerabilities` is an object with no keys; and `metadata.dependencies.total` equals the number of `packages` entries in `package-lock.json` less one, the root `""`. Anything else refuses and prints both streams as npm wrote them, stdout first: npm writes the JSON report, and in `--json` mode the buffered endpoint-error object, to stdout, and every log line to stderr (`lib/utils/display.js:292` to `:300`, `:378` to `:386`, `:441`; `lib/utils/audit-error.js:20` to `:37`), so an unreachable registry, a non-JSON answer, or an npm that emits another shape refuses rather than skips, with the reason visible.

Why each flag, against the source. `npm audit` copies npm's flattened configuration into Arborist and overrides only `audit: true` (`lib/commands/audit.js:63` to `:73`), so ambient `audit=false` is already harmless, but everything else ambient reaches `AuditReport`, whose `#getReport` returns no report at all, without a request, when `offline === true` (`workspaces/arborist/lib/audit-report.js:317` to `:321`); a null report skips `#init` (`:100` to `:106`), so `toJSON` serializes `total: this.size`, 0, and an empty `vulnerabilities` object (`:28` to `:89`), which satisfied every one of revision 3's four checks: a bundle would have passed having audited nothing. `offline` is a Boolean configuration (`workspaces/config/lib/definitions/definitions.js:1593` to `:1601`) that `NPM_CONFIG_OFFLINE`, `--offline`, or a user, global, or project `.npmrc` can set; the command line outranks every other source (npm docs, config: "sorted by priority: Command Line Flags, Environment Variables, npmrc Files, Default Configs"), and `--no-offline` is nopt's negation of a Boolean (nopt 9.0.0 README: "To negate boolean flags, do `--no-whatever`"), so the invocation itself forces `offline=false`. No cache can stand in for the request: the bulk advisory call is a `POST` (`audit-report.js:334` to `:340`), and make-fetch-happen sends a request through its cache only when `storable`, which is false for anything but `GET` and `HEAD` (`lib/fetch.js:96` to `:99`, `lib/cache/policy.js:74` to `:77`), so `prefer-offline` and `prefer-online` cannot touch it either. Omission: `AuditReport` keeps `opts.omit` as a set (`:93`) and `shouldAudit` drops every node whose `dev`, `optional`, or `peer` flag is in it (`:354` to `:368`; `lib/node.js:496` to `:515`), so `omit=optional` or `omit=peer` from any source audits less than the tree. `omit` admits exactly `dev`, `optional`, and `peer` (`definitions.js:1608`) and defaults to `['dev']` under `NODE_ENV=production` (`:1603`); `include` admits `prod`, `dev`, `optional`, and `peer` and is applied last: `buildOmitList` first folds the legacy `only`, `production`, `dev`, and `optional` settings into the two lists, then removes from `omit` every type named in `include` (`:23` to `:49`, the filter at `:48`; `:1010` to `:1025`, "regardless of the order"). Sol's correction stands: `--include=dev` alone prevents `dev` from being omitted, `NODE_ENV=production` included, and clears nothing else; naming all three types clears every omission the configuration can express, whatever its source. The fifth check pins the tree: `metadata.dependencies.total` is the inventory less the root (`audit-report.js:47`), counted before any omission (`:52` to `:69`), and the virtual tree is one node per lockfile `packages` entry (`workspaces/arborist/lib/arborist/load-virtual.js:79`, the tree `audit` loads, `lib/arborist/index.js:273`), so an audit of an empty or different tree, whatever caused it, refuses; that equality is not executed here, and a mismatch on the first run is a finding to explain, never a reason to drop the check. Also verified and harmless: only a command-line `--prefix` moves the audited project, ambient `prefix` does not (`workspaces/config/lib/index.js:697` to `:716`); the two other null-report paths, a tree of the root alone and an empty bulk body (`audit-report.js:319`, `:330` to `:332`), need a lockfile without dependencies, which `lockfile.test.ts` already excludes. Not verified: whether ambient workspace configuration on a project without workspaces (`filterSet`, `:360`) is an npm error; if it is, the exit status refuses, and the fifth check does not see it.

The count is the rule because a threshold was not: npm's `audit-level` is "the minimum level of vulnerability for `npm audit` to exit with a non-zero exit code", its values `info`, `low`, `moderate`, `high`, `critical`, `none`, and its default `null` (`definitions.js:305`), which the reporter reads as `low` (`npm-audit-report/lib/index.js:21`, `options.auditLevel || 'low'`), so revision 2's `--audit-level=low` let an informational advisory through, and an ambient `audit-level=none` would let everything through the exit code; the exit code ranks `info` below `low` (`npm-audit-report/lib/exit-code.js`: the list `['info', 'low', 'moderate', 'high', 'critical', 'none']`, non-zero when some count at or above the level is positive). The JSON is `AuditReport.toJSON`: `metadata.vulnerabilities` carries `info`, `low`, `moderate`, `high`, `critical`, and `total`, the number of vulnerable packages, and `vulnerabilities` holds one entry per such package whatever its severity (`audit-report.js:75` to `:86`), so an advisory of any level makes both non-zero. No `--audit-level` is passed and none is relied on; a non-zero exit refuses whatever the threshold, and the count refuses the rest.

There is no exceptions file, no `--force`, no environment variable, and no flag that skips the audit: the human's rule is no vulnerabilities. Plainly: an advisory with no fix available blocks every `npm run bundle` until the dependency graph or the advisory changes. The committed bundle keeps serving, and `go build` and the Go tests need no npm, so the engine is not blocked, only new frontend builds. The builder stops and reports the advisory, the package, its path through the direct dependency, and whether a fixed release exists. The ways out are a version change in the tables above, or an `overrides` entry in `package.json` pinning the transitive package to a fixed release, each a revision of this document because versions here are exact; or the advisory being withdrawn. Editing the script is not a way out. "No vulnerabilities" means none known to the configured registry at audit time, as Sol notes, not an absence of unknown ones. The registry is not pinned by the audit: `npm ci` and the audit read the same `registry` configuration, so the mirror that installed is the mirror that is asked, and a mirror whose bulk endpoint answers an empty object is a clean report; that is the human's choice of registry, not a bypass the script can close.

Scripts: `dev` (`vite`), `typecheck` (`tsc --noEmit`), `test` (`vitest run`), `bundle` (`node scripts/bundle.mjs`), `check:modal` (`node scripts/modal-check.mjs <address>`); no `postinstall`; `node_modules/.bin` binaries, never `npx`. `bundle` runs, in order: the Node check; `npm audit`; removes `bundle/bundle.json`; `typecheck`; `vite build`, which empties `dist/`; `scripts/notices.mjs`; then writes `bundle/bundle.json`. The manifest is the publish point (E3): removed before the first destructive step, written after the last, so a failed build leaves no manifest, and no manifest, or an unparsable one, is an absent bundle everywhere: `ReadManifest` errors, `TestBundleMatchesManifest` fails, and `cmd/metasystem/ui.go` passes no bundle to the handler.

`vite.config.ts`: `base: "/"`; `plugins: [react(), tailwindcss()]`; `build.outDir: "../bundle/dist"`, `emptyOutDir: true`; `assetsInlineLimit: 0` (no `data:` URLs); `sourcemap: false`; `manifest: false`; `modulePreload: { polyfill: false }`; `rolldownOptions.output` (Vite 8 bundles with Rolldown): `entryFileNames: "assets/[name].js"`, `chunkFileNames: "assets/[name].js"`, `assetFileNames: "assets/[name][extname]"`; `server.host: "127.0.0.1"`, `port: 5173`, `strictPort: true`, and the proxy under Development mode. `tsconfig.json`: `strict`, `noEmit`, `lib: ["ES2022", "DOM", "DOM.Iterable"]`, `moduleResolution: "bundler"`, `jsx: "react-jsx"`, `verbatimModuleSyntax`, `isolatedModules`, `include: ["src", "scripts", "vite.config.ts"]`. Not verified: that TypeScript 7.0.2 accepts every option named; the first `npm run typecheck` proves it. No fallback compiler.

Runtime dependencies, exact versions from the registry:

| Package | Licence | Why |
| --- | --- | --- |
| `react` 19.3.0, `react-dom` 19.3.0 | MIT | D1 |
| `@radix-ui/react-dialog` 1.1.23 | MIT | The modal; its scroll lock is the nonce path: `react-remove-scroll` `^2.7.2` (2.7.2) → `react-remove-scroll-bar` `^2.3.7` (2.3.8) and `react-style-singleton` `^2.2.3` (2.2.3) → `get-nonce` `^1.0.0` |
| `get-nonce` 1.0.1 | MIT | `main.tsx` calls `setNonce` on the instance Radix uses; 1.0.1 satisfies `^1.0.0`, so the lockfile holds one copy (`lockfile.test.ts`) |
| `lucide-react` 1.47.0 | ISC | D15's icon set; the close icon |
| `@fontsource-variable/inter` 5.3.0 | OFL-1.1 (registry field, `LICENSE`, upstream `rsms/inter`) | The interface face |

Development dependencies, exact:

| Package | Licence | Why |
| --- | --- | --- |
| `typescript` 7.0.2 | Apache-2.0 | `tsc --noEmit`; the native compiler, its binary an optional platform package |
| `vite` 8.3.0 | MIT | The bundler; Rolldown 1.2.9 beneath, no esbuild |
| `@vitejs/plugin-react` 6.1.1 | MIT | Peer `vite ^8.0.0` |
| `tailwindcss` 4.3.3, `@tailwindcss/vite` 4.3.3 | MIT | Peer `vite ^5.2 \|\| ^6 \|\| ^7 \|\| ^8` |
| `vitest` 5.0.1 | MIT | Peer `vite ^6.4 \|\| ^7 \|\| ^8`; engines include `^24.0.0` |
| `@types/react` 19.3.0, `@types/react-dom` 19.3.0 | MIT | Types |
| `playwright` 1.63.0 | Apache-2.0 | The modal check only |

Reaches the shipped bundle: the runtime table and its lockfile closure, exactly what `notices.mjs` lists, plus the CSS Tailwind emits. Build-time only, never in `dist/`: the development table and its closure, including Lightning CSS (MPL-2.0). Only Inter's Latin and Latin-extended `wght` faces are bundled, through a hand-written `@font-face` block in `src/fonts.css` naming `inter-latin-wght-normal.woff2` and `inter-latin-ext-wght-normal.woff2` under `@fontsource-variable/inter/files/`. Not included: a router (`g1-s9` decides), a DOM test environment, a linter or formatter, source maps, a favicon, any CDN or remote font, a Markdown renderer (D6).

Deferred to the first slice that uses them: `@fontsource-variable/jetbrains-mono` 5.3.0 (OFL-1.1) and `react-resizable-panels` 4.13.1 (MIT). E12: current `react-resizable-panels` exports no `setNonce` and applies its cursor style through `CSSStyleSheet` and `document.adoptedStyleSheets` (`lib/index.ts`, `lib/global/cursor/updateCursorStyle.ts` on `main` at 4.13.1; the review found the same at 4.12.4). The slice that installs it must prove in the browser, under this policy, that the cursor style applies with zero `securitypolicyviolation` events; whether the policy governs constructed sheets is not verified here.

### The embedding package

```go
package web
//go:embed all:bundle
var bundle embed.FS
const NoncePlaceholder = "__METASYSTEM_CSP_NONCE__"
func Dist() fs.FS                             // bundle/dist
type FileDigest struct{ Path, SHA256 string } // "/" separators; lower-case hex
type Manifest struct {
    SchemaVersion int               `json:"schemaVersion"` // 1
    SourceDigest  string            `json:"sourceDigest"`  // "sha256:<hex>" over _app
    Source        []FileDigest      `json:"source"`
    Files         []FileDigest      `json:"files"`         // every file under dist/
    Tools         map[string]string `json:"tools"`         // node, vite, typescript, tailwindcss
}
func ReadManifest() (Manifest, error)         // ErrNoManifest when absent; an error when unparsable
func SourceDigest(dir string) (string, []FileDigest, error)
func AcceptsHTML(accept string) bool
func ContentType(name string) (string, bool)  // false outside the table
```

**Digest rule**, in `digest.go` and `_app/scripts/digest.mjs`. Walk `dir`. Skip any directory named `node_modules` and any entry whose name begins with `.` except `.nvmrc` and `.npmrc`. A symbolic link anywhere is an error: its target could change the build without changing the digest. Each remaining regular file contributes its path relative to `dir` with `/` separators, ASCII only, and the SHA-256 of its content: for a text file, after every `\r\n` becomes `\n`; otherwise byte for byte. A text file is `.nvmrc`, `.npmrc`, or one whose extension is among `.ts .tsx .mts .cts .js .mjs .cjs .jsx .json .css .html .svg .txt .md`; everything else (`.woff2`, `.png`, ...) is bytes. Modes and times are ignored. Lines `<hex>  <path>\n` are sorted by path bytewise and concatenated; the digest is `sha256:` plus the hex SHA-256 of that text. Fixture: `.nvmrc` = `24.21.0\n`, `a.txt` = `x\r\n`, `b/c.txt` = `y\n`, `d.bin` = the two bytes `0d 0a`, `.hidden` = `z`, `b/node_modules/q.txt` = `q`; the digest is `sha256:f8c8104462398072962c697d4b860b1e76da903a0fa41933ffc541bcbb3e1399`, `d.bin` contributing `7eb70257593da06f682a3ddda54a9d260d4fc514f645237f5ca74b08f8da61a6`, its raw bytes. Both implementations assert the literal.

**Manifest canonical form** (E6). `Source` and `Files` sorted by path bytewise ascending, paths ASCII only; `Files` digests are of the bytes as served, never normalised; `Tools` has exactly the four keys, package versions from each `package.json`, `node` from `process.version`. The script writes `JSON.stringify(manifest, null, 2) + "\n"` with keys in the declared order; no timestamps anywhere. Go only reads it.

**Content types**, a fixed table, since `mime.TypeByExtension` reads host files: `.html` `text/html; charset=utf-8`, `.js` `text/javascript; charset=utf-8`, `.css` `text/css; charset=utf-8`, `.json` `application/json`, `.txt` `text/plain; charset=utf-8`, `.svg` `image/svg+xml`, `.woff2` `font/woff2`, `.png` `image/png`.

### Serving

`httpd.New(info Info, bound net.Addr, bundle fs.FS) http.Handler`; `Info` gains `BundleDigest string`, filled by `cmd/metasystem/ui.go` from `web.ReadManifest().SourceDigest`; when `ReadManifest` errors, the digest is empty and the bundle passed is `nil`, which the handler treats as absent without opening it. An unexported constructor also takes `nonce func() string` for tests. The `g1-s1` checks and headers run first, unchanged. Then:

1. Reserved: `/-/health` as before plus `"bundleDigest"`. The prefixes `/-`, `/api` (`g1-s7` takes it), and `/assets`, exact or with anything beneath, are never the page (E5): what is not health or a served static file under them is 404.
2. Static: `path.Clean`, strip the leading `/`, require `fs.ValidPath`, refuse `index.html` and directories; a regular file is served with its table content type, or 404 when its extension is outside the table. `/index.html` is therefore the page under the next rule, never a redirect.
3. Page: `/`, or any remaining path whose request accepts HTML: `index.html` with the nonce substituted, 200. Everything else: 404. `AcceptsHTML` splits `Accept` on commas and parses each element with `mime.ParseMediaType`, which lower-cases the type (`src/mime/mediatype.go:144`) and separates parameters; a malformed element is skipped. The request accepts HTML iff some element's type is exactly `text/html` (not `text/*`, not `*/*`) and its `q` parameter is absent or parses with `strconv.ParseFloat` to more than zero. No `Accept` header: not accepted.

Absent bundle: at construction the handler reads `index.html`; when the bundle is `nil` or that fails, every page response is 503 with the text `MetaSystem interface: this executable was built without the interface bundle. Rebuild it from a checkout that contains internal/ui/web/bundle/dist, then run: metasystem ui restart`; static lookups are 404; health answers with an empty `bundleDigest`. `Cache-Control: no-store` stays on every response.

### Content security policy

Header `Content-Security-Policy` on every response. On a page response:

```
default-src 'none'; script-src 'self'; style-src 'self' 'nonce-<nonce>'; img-src 'self'; font-src 'self'; connect-src 'self'; base-uri 'none'; form-action 'none'; frame-ancestors 'none'
```

On every other response the same string without ` 'nonce-<nonce>'`. No `unsafe-inline`, `data:`, host source, `report-uri`, or Trusted Types.

The nonce is 16 bytes from `crypto/rand` in `base64.RawStdEncoding` (22 characters, a valid CSP `base64-value`), generated once per page response for header and body. `_app/index.html` carries `<meta property="csp-nonce" nonce="__METASYSTEM_CSP_NONCE__">` and nothing else inline: no `<script>` without `src`, no `style` attribute, no `<style>`. At construction the handler splits the embedded `index.html` on `web.NoncePlaceholder`; per response it writes the parts around the nonce. `src/nonce.ts` returns the meta element's `nonce` IDL property, not `getAttribute`, because browsers hide the attribute after parsing; `main.tsx` calls `setNonce(nonce)` from `get-nonce` before the first render, so `react-style-singleton` sets it on the `<style>` it appends. React `style` props write CSSOM properties, which the policy does not govern. Vite's `html.cspNonce` is not used.

### Development mode

`npm run dev` starts Vite on `127.0.0.1:5173` with hot reload and proxies `/api` and `/-` to `http://127.0.0.1:7878`, overridable by `METASYSTEM_UI_PROXY_TARGET`, with `changeOrigin: true` (the Go server sees `Host: 127.0.0.1:7878`) and `Origin` removed; the browser's `Sec-Fetch-Site: same-origin` is forwarded and passes. No check is loosened; nothing development-only exists in Go. Vite serves the page without the policy and with the placeholder literal.

### Licence notices

`scripts/notices.mjs` reads `package-lock.json`, walks the runtime closure from the root's `dependencies` (nested `node_modules` resolution, ignoring `dev` and `optional` entries), and for each package reads `name`, `version`, `license`, and its `LICENSE*` or `LICENCE*` file, refusing when one has none. It writes `dist/THIRD-PARTY-NOTICES.txt`: a header, then per package the name, version, identifier, and text: over-attribution, even of a package tree-shaking drops, and no bundler hooks. The file is committed, embedded, served at `/THIRD-PARTY-NOTICES.txt`, and linked from the page as "Open-source notices", a link `g1-s9` keeps. Build tools are not listed.

### Frontend tests

Vitest runs in the `node` environment, `vitest run`, development-time only. Four tests: `scripts/digest.test.ts` (the fixture literal, from a temporary directory); `scripts/lockfile.test.ts` (`package-lock.json` has exactly one key ending in `/get-nonce`, at 1.0.1); `src/nonce.test.ts` (`readNonce` returns the element's `nonce` property, or `""`); and `scripts/audit.test.ts` (H1), the fixture that the audit gate cannot be made green by ambient configuration.

**`audit.test.ts`.** The bundle script exports `auditArgs()`, the argument list written above, and `runAudit(env, args)`, which spawns `npm` from `PATH` in `_app` with the given environment, applies the five checks, and returns `{ verdict, stdout, stderr }` without exiting. The test starts `http.createServer` on `127.0.0.1` port 0, records every request's method, path, and body, gunzipping when `Content-Encoding: gzip` is present, which npm sets for the bulk call (`gzip: true`, `audit-report.js:338`; `npm-registry-fetch/lib/index.js:72` to `:80`), and answers every request `500` with a JSON body, so nothing reaches a network and every audited case ends in a refusal through the endpoint-error path. The base environment is the test's own plus `NPM_CONFIG_REGISTRY=http://127.0.0.1:<port>/` and `NPM_CONFIG_CACHE=<tmp>`. Cases, each asserting that `runAudit` refuses and that the server received exactly one `POST /-/npm/v1/security/advisories/bulk`: (1) `NPM_CONFIG_OFFLINE=true`; (2) `NPM_CONFIG_USERCONFIG=<tmp>/.npmrc`, that file holding `offline=true` and `omit=optional`, the npmrc layer; (3) `NPM_CONFIG_OMIT=optional`; (4) `NPM_CONFIG_OMIT=dev` with `NODE_ENV=production`; (5) `NPM_CONFIG_OMIT=peer`; (6) `NPM_CONFIG_AUDIT=false`; (7) no ambient setting, the baseline. In every case the decoded body's set of package names and versions equals the baseline's, and the baseline names `vite` and at least one package the lockfile marks `"optional": true`. Two controls prove the fixture has teeth, running `auditArgs()` with one flag removed: under (1) without `--no-offline`, the server receives no request and `runAudit` reports green with `total` 0, the hole Sol found; under (3) without `--include=optional`, the body lacks every optional package. The controls are what fails without the fix; the cases are what must hold with it. The peer case cannot fail without its flag unless the tree has a peer-only node, which is not asserted; it holds either way and stays.

## Behaviour

**Page.** `src/App.tsx` renders the heading `MetaSystem interface`; the facts fetched from `/-/health`; `path: <location.pathname>`; a button `Open a dialog`; the notices link. The dialog is `Dialog.Root`, `Dialog.Trigger`, and `Dialog.Portal` containing `Dialog.Overlay` and `Dialog.Content` with `Dialog.Title`, `Dialog.Description`, a sentence, and `Dialog.Close` with a lucide icon. `Dialog.Overlay` is required (E4): at 1.1.23 the scroll lock is mounted by `DialogOverlayImpl`, which wraps its element in `RemoveScroll` (`packages/react/dialog/src/dialog.tsx:216` to `:251` on `main`, whose `package.json` reads 1.1.23), so a dialog without `Overlay` never appends the `<style>` and would pass the nonce proof by never exercising it.

| Case | Required result |
| --- | --- |
| Node differs from `.nvmrc`; the audit's JSON counts any vulnerable package, of any severity including `info`; npm exits non-zero or prints no JSON object; the audited tree's package count is not the lockfile's | Bundle script refuses, naming both versions, the vulnerable packages with their severities, npm's stdout and stderr, or both counts |
| `offline`, `omit`, `audit`, or `audit-level` set by environment variable or `.npmrc`, or `NODE_ENV=production` | The same bulk request to the registry, naming every lockfile package, and the same verdict as without them (`audit.test.ts`) |
| Source changed after the last bundle | `TestBundleIsCurrent` fails listing changed, added, and removed paths and says `run npm ci --ignore-scripts && npm run bundle in internal/ui/web/_app` |
| A `dist/` file not matching `bundle.json` | `TestBundleMatchesManifest` names the file whose digest differs, is missing, or is extra |
| `dist/` empty or absent; or `bundle.json` absent or unparsable | `go build` succeeds through `README.txt`; the server serves the 503 statement; `TestBundleMatchesManifest` fails (`bundle missing` without a manifest) |
| `/assets/missing.js`, also from the address bar; `/assets/`; `/assets`; `/api/goals` and `/api`; `/-`; `/nothing` with `Accept: */*`, `text/html;q=0`, `text/*`, or no `Accept` | 404 |
| `/some/deep/path` with `Accept: TEXT/HTML;level=1, */*;q=0.1` | The page, 200 |
| Dialog opened in the embedded build | `body` overflow hidden, one `<style nonce>` in `head` whose nonce equals the meta's, zero `securitypolicyviolation` events |

## Change boundary

New: `internal/ui/web/` (Go sources, tests, `testmain_test.go`, `bundle/README.txt`, `bundle/bundle.json`, `bundle/dist/**`, `_app/**` except `node_modules`). Edited, and nothing else in them: `internal/ui/httpd/` sources for routes, policy, `Accept`, and constructor; `internal/ui/httpd/*_test.go` from `g1-s1`, only the cases asserting the placeholder at `/`, the constructor's arity, the 404 rule, and the exact health payload; `cmd/metasystem/ui.go`, the `NewHandler` wiring and `Info.BundleDigest`; `docs/architecture.md`, one package-map row for `ui/web`. The `metasystem/.gitignore` line is the exclusion slice's (H2's guard needs it) and is already on the branch when this slice starts.

Must not be touched: the exclusion slice's files (`internal/behaviorsurface`, `internal/pathpattern`, `internal/proofrun`, `internal/parallelratchet`, `internal/audit`, `scripts/agents/go-gate.sh`, `scripts/agents/brain-fixtures.sh`, `scripts/agents/path-class-fixtures.sh`, `scripts/agents/dependency-tree-guard.sh`, `metasystem/.gitignore`); `testing-parallel-ratchet.json`, `testing.json` (it names no surface under `internal/ui`, which falls to `residual`), `go.mod`, `go.sum`, `internal/testenv`, `internal/testutil`, `internal/testselect`, `internal/testpolicy`, `internal/testexec`, `internal/hostload`, `internal/gopackages`, `cmd/metasystem/test*.go`, `cmd/metasystem/audit.go`, `cmd/metasystem/main.go`, `internal/ui/lifecycle`, `internal/config`, `scripts/**`, `metasystem.conf`, and any other existing `*_test.go` or `testmain_test.go`. No new Go module dependency: `embed`, `io/fs`, `crypto/rand`, `crypto/sha256`, `encoding/json`, `mime`, `strconv`, `net/http`, `path` suffice.

Conventions, because the builder sees only this document: every new package has `testmain_test.go` with `func TestMain(m *testing.M) { os.Exit(testenv.Main(m)) }`; tests never call `os.Setenv`; every top-level test and independent subtest calls `t.Parallel()`, the ratchet allowing an unlisted package zero serial tests; no sleeps, polling, or fixed ports (`httptest`); temporary trees under `t.TempDir()`; `testutil.Expect` and `Require`; the nonce generator injected; `cmd/metasystem/ui.go` wires and prints only. The Go tests read `_app` and `bundle` relative to the package directory and need no Node. The first install happens only after the exclusion slice is on the branch; the bundle is rebuilt as the last commit after rebasing (D7).

## Verification

Go tests, package `web`: `TestSourceDigestFixture` (the literal, symlink and non-ASCII errors); `TestBundleMatchesManifest`; `TestBundleIsCurrent` (skips when `_app` is absent); `TestDistIsServable`; `TestIndexIsStrict` (one placeholder; nothing inline; no `http:` or `https:` in `src` or `href`); `TestReadManifestAbsent`, `TestReadManifestUnparsable`; `TestAcceptsHTML`. Package `httpd`, on `fstest.MapFS` bundles with the fake nonce: each Behaviour row; the policy and the five `g1-s1` headers on every response class; body nonce equals header nonce and differs between responses; the 503 for `nil` and for a bundle without `index.html`.

Obligations the code critique checks by name:

- **O1, exclusion.** The exclusion slice is on the branch before the first install. With `node_modules` installed and absent, the same binary prints the same ENGINE, LANDING, and PAYLOAD digests, `audit parallel-ratchet` and `audit stop-decision-surface` agree, `section/brain-fixtures` and `section/static-reproof-fixtures` pass, and the gate's gofmt, vet, and staticcheck pass with the tree installed, the gofmt also with a tracked `.go` file deleted only in the working tree; the slice's fixtures, the seam-scan prune and the reader-scan `--exclude-dir=node_modules` among them, are present, and the `.gitignore` line is the slice's.
- **O2, no inline, nothing external.** `TestIndexIsStrict` passes on the committed bundle.
- **O3, nonce end to end.** Fresh per response, in header and body; `main.tsx` calls `setNonce` before rendering; `App.tsx` renders `Dialog.Overlay`; the modal check asserts the dialog row.
- **O4, honest 404.** The reserved prefixes, exact and beneath, never receive the page; `AcceptsHTML` is the parsed rule, not a substring test.
- **O5, staleness.** Both digest implementations assert the fixture literal; the text-extension list and the exclusions are exactly those written here.
- **O6, absent bundle.** `go build ./cmd/metasystem` succeeds with `bundle/dist` removed; the server answers the 503 statement, also when only `bundle.json` is missing.
- **O7, notices.** Every package in the lockfile's runtime closure, with its licence text; the page links to the file.
- **O8, no loosening.** The `g1-s1` checks and headers are as designed; the proxy exists only in `vite.config.ts`.
- **O9, supply chain.** `package.json` versions equal the tables; `.npmrc` as written; no `postinstall`; the lockfile committed; the bundle script runs the Node check and exactly `npm audit --json --no-offline --include=dev --include=optional --include=peer` in `_app`, captures stdout and stderr separately and prints both on refusal, and refuses unless the exit status is 0, stdout parses, `metadata.vulnerabilities.total` is 0, `vulnerabilities` is empty, and `metadata.dependencies.total` equals the lockfile's `packages` count less one; `scripts/audit.test.ts` has the seven cases and two controls as written, hermetic on a loopback server; no `--audit-level`, no exceptions file, no bypass of any kind, ambient configuration included.
- **O10, deterministic manifest.** Two bundles from one tree are byte-identical in `dist/` and `bundle.json`; `Source` and `Files` sorted as written.
- **O11, the guard.** `scripts/agents/dependency-tree-guard.sh` exists in the exclusion slice as written under The exclusion slice: it plants exactly the listed tree, runs the listed commands with it absent and present, compares the listed outputs, and refuses on the first difference, naming it. Its transcript with `_app` absent is recorded by that slice; its transcript with the real `node_modules` installed and the poison planted beside it is recorded by this slice after the first `npm ci`, and both are green. Removing the membership rule or any name-based skip that a compared output depends on makes it refuse; the export's exclusion (`readManifest`) is observed only if `test run` freezes a candidate, which is not verified here, and keeps its own per-walker fixture regardless.

Commands, from `metasystem/`: `go build ./...`; `go vet ./internal/ui/... ./cmd/metasystem/`; `go test ./internal/ui/...`; the gate's gofmt pipeline, `git ls-files -z --cached --others --exclude-standard -- '*.go' | while IFS= read -r -d '' f; do if [[ -f "$f" ]]; then printf '%s\0' "$f"; fi; done | xargs -0 -r gofmt -l`, expecting no output, or `bash scripts/agents/go-gate.sh`, which owns it, never `gofmt -l internal cmd`, which recurses into `node_modules` (G2); `bin/metasystem audit parallel-ratchet` without `--update`; the digest comparisons of O1; after the first install, `bash scripts/agents/dependency-tree-guard.sh`, its transcript kept for O11. From `internal/ui/web/_app/`: `npm ci --ignore-scripts`, `npm run typecheck`, `npm test`, `npm run bundle` twice with `git status --porcelain` clean between.

Walkthrough, by Claude after the code critique: build `bin/metasystem`; `ui start`; in a real browser see the health facts; open the dialog and confirm in devtools a `<style nonce>` in `head`, `body` overflow hidden, no policy violation, every request to the server's origin; reload at `/some/deep/path`; open `/THIRD-PARTY-NOTICES.txt`; `curl -i /assets` gives 404, `curl -i /` gives 200 with the nonce in header and body; `npm run check:modal -- http://127.0.0.1:7878` passes; `npm run dev` shows the page through the proxy; remove `bundle/dist`, rebuild, `ui restart`, see the 503 statement, restore. The modal check is Claude's, never a Go test's or the gate's.

## Open questions

None. The Node line is decided under The build.
