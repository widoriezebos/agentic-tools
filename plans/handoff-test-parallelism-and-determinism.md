# Test parallelism and Git-free tests: final candidate handoff

Updated 24 September 2026, after the performance correction and Opus implementation roster change. Continue **finish-test-repairs-and-integrate**. Do not claim a new goal or stop unfinished work because of metadata. User authorizes commits, pushes, Main integration, tmux, and machinery workarounds. Main integration and final matching validation remain outstanding. The earlier full runs were halted at the user's request; do not restart them. The user subsequently authorized the three performance changes below.

## Live state after cleanup and performance implementation

**Checkpoint 25 September 2026, 03:55 CEST:** Continue the same goal; no new goal. Linux v12 has finished: 44 of46 native sections passed, including the full race/coverage Go gate and all landing scenarios. Only supervision nested-override and adoption default failed. Brittleness-first diagnosis identified the final supervision scan after holder release with suppressed JSON errors, and an adoption admission path outside the scenario temporary root. Two scoped fixture repairs preserve the ownership and dangling-link assertions, keep the holder alive through its check, retain filename/read status, and create the private admission directory under the bed TMPDIR. Opus authored both; independent Sol review closed with no unrefuted material findings. Corrected supervision passes on native Linux; the actual default adoption bed reports a passing scenario in62seconds. These are focused diagnostics, not a whole-suite pass.

Both corrections are in private combined commit293f0aafe7101b648fd674af3f6f988bccaa01f6 and remote backupc9c67d0008226df9824241c45e745198e637eaa7. V13 preparation includes them and this record, with parent3f5d75dd. Main's v12 index and Mac attempt stay frozen until that run ends; it has passed its command and batch package groups and more than100 groups without a recorded failure so far. Exact current counts, terminal statuses, references and own exits remain in P/ACTIVE.json. Ordinary corrected-tree validation and Main publication are still required. Both changed fixture scripts conservatively invalidate section execution identities; use ordinary unchanged-input reuse and do not fabricate or transplant a receipt. Do not claim old failed runs green.

The focused adoption section entry unexpectedly arms a complete witness Go gate before selecting its scenario. That redundant diagnostic was cancelled before the scenario; no pass was claimed. The existing fixture-bed helper, with its own minted capability and a distinct inherited progress root, reaches just the actual default scenario and proves the temporary-root repair. No additional runner or proof framework was added. The installed policy worker is still landed3a9; install the newly landed scheduler only after Main integration, then measure end-to-end duration. Whole-suite speedup remains unmeasured.

This checkpoint supersedes the older states below.


**Checkpoint 25 September 2026, 02:56 CEST:** All three narrow final repairs have focused passing proof and closed independent Sol reviews: testing-contract cadence memberships and a fast canary; second-goal fixture authority configured before opening that goal; process-group cleanup rechecking kernel birth and group ownership after orderly shutdown. The cleanup regression fails deterministically before the repair and passes afterward. A leaderless surviving group remains a visible failure; cleanup never silently treats it as gone or signals an unowned group. Wido's rule to investigate brittleness before changing failed assertions is captured in development/project-rules-local.md.

Mac v10 remains frozen at tree ce9d1048c35b3dda38a49899588179c368227a83 while its independent groups finish. Its known failures are the repaired cadence guard and watcher cleanup (the latter passed retry, which was correctly kept as a failed original run). Linux v10 completed: supervision passed, adoption failed on the same cadence guard, and land passed47/48 with only the repaired second-goal authority case red. That landing scenario now passes on the corrected private source (316seconds, actual exit0). Never report these earlier whole runs as green.

Combined v12 preparation at E/rollout/final-candidate-20260925performance-v12/source includes all three reviewed repairs. Its parent will be actual origin/main3f5d75dd88184e436e8b93cae3025e478fb74627: the goal-steal operation moved Main with only the goal-owner record, which must be retained. Product base3a9ec4f9 is unchanged. Main's live index stays on v10 until its attempt ends. Finish collecting all groups, then validate the repaired candidate through the existing Mac delivery command and ordinary native Linux full validator with six guest workers. Parent terminal enrollment must remain alive for both suites. Do not mutate the candidate mid-run.

The fixture script and testenv changes alter the engine projection. Selected section proofs therefore need fresh execution; the earlier claim that44sections could be reused applied only to the metadata-only v11 delta. Normal matching proof reuse remains enabled for unaffected identities. All final broad validation, Main product publication, installation of the landed scheduler improvements, and subsequent measured timing remain outstanding. Do not claim an end-to-end speedup from runs using the old trusted policy worker. Exact live identities, review paths, and own exit records are in P/ACTIVE.json.

This checkpoint supersedes the older states below.


**Checkpoint 25 September 2026, 02:01 CEST:** Main has advanced to `3a9ec4f9b3c92550d75be2da5efe835d97088fb6` (37 incoming seat-presence files). Private integration retained the dependency seams, Git stubs, deleted obsolete inventory, zero launch caps, and both protected launch selectors. Opus resolved conflicts; independent Sol review closed with zero material findings. Main HEAD is now that base and staging preserves the work. Backup `fd6bf37fb0c2a2fb3d1fb3edc5d7611421cfdfc2` is pushed to `backup/test-parallelism-repairs-20260924`; Main product delivery remains pending.

Trusted installed engine is clean landed `3a9ec4f9`, generation179. Root `up` verified live Codex pid5030/start1790272787 and writer lease `main-1790272787-5030-13dde3`; supervision generation192. The existing goal was transferred through enrolled human `goal steal` to that live owner under the user's takeover authorization. Same goal and standing approval remain; machinery creates a new accounting episode, so historical goal receipts must not be represented as fresh. Prior artifacts and native results are retained. Stop capability already has lease epoch7. No new goal was opened.

Linux v9's three remaining sections ended red: missing shared TestMain in ledgerfence, one newly serial conftailor test, missing notification configuration in landing fixtures, and four insufficiently diagnosed abandonment exits. Two test-only setup repairs now pass focused checks; config tests gained meaningful seat-setting coverage and deterministic initialization against inherited environment variables. Independent Sol review closed. Mac merged steward full race/coverage passed80.5%; seat75.0%; repaired config83.3%. Native Linux changed-package checks run under transport `parallel-linux-integrated-seat-20260925-r2` and measured seat75.0/config83.3 so far. The first transport attempt stopped at an un-fetched private commit before tests; explicit snapshot fetch repaired it, with console output retained. Existing floors remain unchanged; only the newly landed seat package gets its measured75.0 floor on each platform.

An Opus delegate at `P/land-final-fixture-dependencies` owns the final landing-fixture setup/diagnostic repair in a private source. Assemble only its reviewed final patch. V10 source is being assembled at `E/rollout/final-candidate-20260925performance-v10/source`; its current commit is an interim native-check snapshot, not the final candidate. No Mac v10 suite has started. Check `P/ACTIVE.json` and each run's own exit file, including files named simply `exit`, before reporting a process as running. Next: final fixture correction and focused confirmation, immutable v10 freeze/build, matching delivery validation, Main commit/push, then install landed performance changes and measure actual suite timing. Do not claim a whole-suite improvement from the old worker's71.7-minute run.

This checkpoint supersedes older states below.

**Checkpoint 25 September 2026, 01:28 CEST:** Continue finish-test-repairs-and-integrate; no new goal. Main was fetched and advanced to 8126403495c3b55290abfbe3d6df2adeeeb1c4a7 with inherited staging and the unstaged narrator append preserved. Incoming changes were only a design document and the launch test selector. Latest remotely verified backup before this checkpoint: 6a96e54e57615eec58b9c2494394db7ab7d2c4d5 on backup/test-parallelism-repairs-20260924. P/ACTIVE.json below remains the source for live process and candidate identities.

Mac v4 completed all 187 groups in 71.7 minutes: 176 passed, 8 failed, 3 invalid. It used the retained older policy worker, so this is not a measurement of the newly implemented duplicate suppression. Failures were two renamed mandatory test selectors, stale quoted skill text and missing skill registrations, adoption runtime configuration, and fixture authority/project-memory setup. Detached adoption failure preservation also hit its 512 MiB cap; native failures and original logs remain available. No green whole-suite verdict is claimed.

Linux v5 native race/coverage tests all passed, then coverage policy refused missing new package registrations and nine floor drops. Meaningful focused tests repaired the drops, stable package-owned ledgerfence tests removed shard-dependent measurement, and existing floors were preserved. Native Linux repaired-package race/coverage checks passed. The composed audit of retained plus fresh coverage passes; it is an honest manual composition, not a fabricated canonical gate witness. All 36 remaining Linux sections outside adoption, supervision and land passed. Those three sections still need final repaired validation.

Final repairs use per-handler HTTP clocks (a reproduced global race removed), self-contained fixture intent/doctrine/question records, complete synthetic human identity, and adaptation of runtime/model pairs without guessing vendor models. A frozen schema-1 migration fixture independently requires the old launch test selector even after Main changes its current selector: partition the existing nine cases into two substantive entry points, each case once, preserving both names and assertions. That is the final compatibility repair; do not rename back and forth or weaken the frozen guard. Opus authors and Sol reviews are retained under P and E/reviews. Main integration/push and measurement after installing the landed engine remain outstanding.

This checkpoint supersedes every older checkpoint below.

**Newest checkpoint, 24 September 23:57 CEST:** Continue the existing goal. User explicitly ordered: get suites green, commit and push, then use measured timing to identify and implement further speedups. Check every test failure for brittleness first and repair the cause before assertions; captured in development/project-rules-local.md. Main base ab02dee7c6f3791d8feb249757bc251860a99be0; only upstream design documentation was merged since697bbc5e. Preserve staged inherited work and unstaged narrator append.

V3 validation ended red on both hosts. Linux's Go run found four failures: stale positional launch-setting assertions, a renamed test missing from the contract, stale embedded skill text, and an ACP permission-window wire-order race. The first three and ACP are repaired by Opus and independently reviewed by Sol; ACP's new regression was made parallel after the serial ratchet caught it. Mac waited on two dirty markers from dead owners; exact owner/process-group/flock checks permitted archived operator reconciliation. Mac then stopped before native tests because protection probes shared caller refs and Main advanced during the corpus. Opus isolated probe metadata against the admitted immutable commit; failing-before, passing-after and mutation witnesses passed. Sol reviewed it. Full six-case first-transition proof is still required in the next suite.

**P/ACTIVE.json is the live authority for attempts and delegates.** No v3 full suite remains running. V4 preparation/output paths replace performance-v3 with performance-v4. Do not run a duplicate if ACTIVE says one is alive. All original three performance patches remain staged. Installed policy engine is still clean Main generation178, so its retained worker does not yet contain candidate dedup or probe-isolation implementation; verify candidate behavior directly and measure the newly installed runtime after landing. Do not claim the old worker exercises the new scheduler.

Three final repair directories under P: final-drift-repairs, final-acp-order-repair (superseded full patch final-acp-parallel-correction), final-probe-isolation-repair. Their corresponding reviews are under E/reviews; ACP final one-line confirmation uses final-acp-order-repair-round2-20260924. All output survives outside Main. Main product commit/push remains pending; backup ref is backup/test-parallelism-repairs-20260924. Preserve warm Go cache. No new goal, new framework, or broad redesign.

Earlier checkpoints below are historical and do not describe live runs.

**Newest checkpoint 23:32 CEST:** All reviews closed, including integration-repair round2 `20260924t212229-e8d81bc0b5`, tree `a9778c15e306d18440de6ab0f472863d5510758f`. Both attempts to start v2 validation stopped BEFORE tests: Mac policy engine was behind new Main; concurrent terminal enrollments interfered with Linux. Origin advanced only the seat-awareness design document to `697bbc5e1458e651131a7e62d956c575fc631252`; that is now Main's base with staged work preserved. Trusted engine rebuilt from that clean Main source and rearmed to generation178. Root session5030 was rebound with `metasystem up` from the agent, and goal next confirms the existing claimed goal. No new goal.

Use **v3** candidate preparation `E/rollout/final-candidate-20260924performance-v3/prepare.sh` and Mac output `E/rollout/final-delivery-20260924performance-v3`; prior v1/v2 candidates are superseded. V3 will run both suites under ONE enrolled parent shell that stays alive, with Linux's duplicate enrollment omitted in the external wrapper `P/linux-from-enrolled-parent.sh`. Full validation is NOT running yet at this checkpoint. Prior Mac observer43969 was stopped. New immutable candidate bindings and active process records must be read from the v3 paths. Performance work and source-reference corrections are complete; no further code work planned unless final validation finds a concrete defect.


Updated 24 September 2026 23:17 CEST; this section overrides historical job/ETA notes below. Continue the existing goal. All three requested performance changes are implemented and staged; full combined validation and Main publication are still outstanding. No full suite is active at this checkpoint.

- Main base is now `220afab65485035953ca552c4f916638cf8928a8`, fetched from origin. The incoming roster change is reconciled. The only merge conflict was the obsolete isolation inventory: preserve its removal as the user explicitly requested. The live unstaged `metasystem/records/narrator-digest.log` append remains untouched.
- Proof reuse: six-file patch applied after independent Sol review `20260924t204644-405b4df862`, tree `baa94ff466011a0a712706e76949775645311d08`, zero material findings. Repeated planner children fell from66to37 in the measured diagnostic. Same-run AUTO plans already selecting full DEEP work are retained; first-transition underselection is guarded.
- Dedup: final implementation is `P/dedup-composition-fix/implementation.patch` (do not reapply its full patch to Main). Production review closed at round2, tree `325c660f52bfc0dfd8371a4246c843d0dff0ac95`, Sol read `20260924t210632-7ca40f891e`; CPU budgets/sharding remain distinct. Real-contract preparation diagnostic removes256duplicate test executions, but critical-path timing remains unmeasured. The later11-line composition correction makes the two added tests parallel with private admission state and fixes a shifted refusal source anchor; it is included in the current integration-repair review.
- Heavy tests: two-conflict decision moved to the existing strict stubbed batch owner (136.42seconds to0.22–0.26seconds); real owner/Git witnesses retained. All three frozen corpus layouts still prepare/freeze/normalize and must have identical commits and CLEAN working trees; one six-case worker then runs (185seconds to73seconds, single samples). Independent review closed at round2 tree `d640e6ef0b58a50219827d7ded5416392f5ba3ff`, Sol read `20260924t210630-dcf5dd67bd`. Coverage fixture isolation repair also covered by that review. Applied Main.
- Root combined race checks passed in18.9seconds. Composition found stale source anchor and two serial new tests; corrections passed exact race witnesses and root source-anchor/parallel-ratchet checks. No ceilings/exemptions raised.
- Batch join defect: verification recalculated automatic worker allowance from current free RAM, potentially rejecting the proof just run. `P/retained-admission-fixture-pin/implementation.patch` is the final3file patch, applied Main. The join now passes its actual result.Workers through a private verification request. A deterministic failing-then-passing helper proves the rejection; real eject-red lifecycle passes164seconds. Its old intermittent failures are consistent with this cause but were not captured with live diagnostics. Baseline freshness fixture explicitly configures2workers; changed-width subtest keeps its refusal assertion. New integration-repair independent review `E/reviews/performance-integration-repairs-20260924` is pending, covering this patch and the11-line composition correction, exact source tree in snapshot.json.
- Worker override distinction: plural `METASYSTEM_TESTING_WORKERS=9` is explicit configuration and skips automatic RAM selection; singular `METASYSTEM_TEST_WORKERS` is inherited ceiling and must remain intact (currently unset). Final Mac wrapper's9-worker assertion is therefore valid. Public automatic verification can use the recorded result.Workers through the existing plural config override; no new CLI/policy added. Read `P/verify-worker-drift-read.md`.
- Cleanup complete: about51GiB recovered,207GiB free at completion. Source/commits/refs/patches/proof logs/active clones/warm cache preserved. Record `P/storage/cleanup-summary.json`.
- Latest verified remote backup before this checkpoint is `3b4f7d09529d93878a0d1699d02a93b6c043e15f` on `backup/test-parallelism-repairs-20260924`; refresh after this handoff to capture all completed changes. No Main product commit yet.

`E=/Users/wido/LocalStorage/agentic-tools-evidence/test-parallelism-20260921`; `P=E/coordination/performance-opus-20260924`. Fresh preparation script: `E/rollout/final-candidate-20260924performance-v2/prepare.sh`; fresh Mac runner: `E/rollout/final-delivery-20260924performance/run.sh`. Candidate preparation v1 built d1e7c7f99 at tree e2c8f2b7; it is superseded by the two final source-anchor corrections. Use v2 preparation. Mac runner is syntax checked and not started. Linux established runner `E/coordination/linux-final-private-candidate-prepared/run-final-normal.sh`, fresh RUN_ID `final-performance-20260924`, same exact candidate,6guestworkers. Use human tmux, already enrolled. Installed trusted engine remains generation177; do not rebuild it unnecessarily. Warm cache `E/rollout/final-selected-delivery-20260924bx/go-cache` must be preserved.

Next: close bounded integration-repair read, final focused merged verification, freeze/build exact candidate, launch required Mac and Linux runs concurrently, observe actual native overlap/dedup/timing, reuse valid matching passes for repairs, then integrate/push. No new goals, framework, redesign or unrelated cleanup. Current ETA given to user23:02 was1.5–2.5hours, LOW confidence conditional on final validation passing. No full-suite speedup or completion claimed.

## Authority and boundaries

Root Astra coordinates Claude Opus 5.5 (`claude-opus-5-5`) implementation and Sol6 reviews, as the user most recently requested. This supersedes the earlier Sol implementation authorization. Only root edits Main; delegates own isolated clones. Ordinary tests stub Git including setup. Necessary native Git adapters retain their actual integration assertions. No new inventory, audit system, emulator, dependency, or unrelated scope. User explicitly added bounded CLI help fixes; broad command restructuring already belongs to queued goal `verbs-match-intent`. Related `human-goal-verbs-forgiving` is done.

## Current performance slice

Implement only the three authorized changes: remove duplicate same-configuration execution, shrink heavyweight decision/failure fixtures using existing stubs, and eliminate measured repeated proof-reuse operations. The acceptance conditions are in `plans/test-suite-redesign.md`. No whole-suite speedup is proven; earlier 2x/8x claims do not describe the full suite.

Main base is `89d3674aa3a93929c6905e322f5cebfa4ea2ef36`; inherited staged work is backed up as candidate `58919d4e9dc51ac89b95f87beebdc3094115495f` on `backup/test-parallelism-repairs-20260924`. Preserve the live unstaged narrator append. Installed trusted engine is generation 177 at the Main baseline. Do not rebaseline again from older handoff sections below.

Four actual Opus children are supervised under `E/coordination/performance-opus-20260924/{dedup,fixtures,reuse,fixture-repair}`. Each directory holds `launch-input.json`, `process.json`, `events.jsonl`, and eventually `exit.json`; requested and observed model is `claude-opus-5-5`. The native launch build adapter currently hardcodes Codex, so these use directly supervised Claude CLI under the user's existing machinery-workaround authority. Their private source paths are in launch-input.json. Sol implementers stopped and handed over preserved edits. The small fourth assignment finishes already identified goal-layout/strict-stub fixture repairs; it is not another performance project. Do not start duplicate jobs.

At transfer: dedup has a traced design but no edits; fixtures has a two-file scenario migration, not yet tested; reuse has measured duplicate DEEP planning and temporary profiling that must be excluded; fixture-repair has two formatted, untested fixture edits. The separate one-file deadline-receipt fixture repair passed focused/Git-denied checks and has been applied to Main's index. No new full suite until focused correctness and actual mechanism observations justify it. Collect meaningful before/after measurements from existing runs; do not rerun only to repackage reports.

NEVER read, echo, copy, or diff actual `metasystem/metasystem.conf.local`: secrets. Transport only the tracked index tree. Preserve all inherited work and the four zero launch caps. Mac uses nine testing workers. Linux uses six workers on six CPUs and 16 GiB RAM. Preserve inherited `METASYSTEM_TEST_WORKERS`. Full Linux remains required for this runner/process goal; future Linux validation is selective.

## Locations

- Main: /Users/wido/LocalStorage/GitHub/agentic-tools-m1e
- E: /Users/wido/LocalStorage/agentic-tools-evidence/test-parallelism-20260921
- C: E/coordination/git-free-tests-20260923
- T: /Users/wido/LocalStorage/transport/test-parallelism-20260921
- UI clone: /Users/wido/LocalStorage/GitHub/agentic-tools-ui

Main HEAD is now `d76c71e3ba04e8edde533ee693330c1405bd0af0`, the incoming UI merge. Root reconciled it with all accepted test work before final validation. Accepted product edits are staged; the narrator may have legitimate unstaged append-only entries. Goal revision15 has 15-day elapsed,40-attempt,1800-minute,8-active budgets; no fence at last supported read.

## Accepted source

**330 accepted units; 1,583 original test parents converted.** `C/git-conversion-count-live-330.json` records zero remaining ordinary labels in the fixed mapped cohort and zero remaining identified mixed CMD policy claims. This is not a certified repository-wide denominator. The 38 mapped mixed parents overlap ordinary claims;34 physical parents remain justified adapters. Reclassifications and mixed-policy extraction receive no whole-parent conversion credit.

Every accepted patch and root disposition is in `C/accepted`. Root read each whole diff and independent read before applying. Latest units:

-325 governed/retry2: actual process custody, retry accounting and budget behavior with file fixtures; shared helper snapshot reconstructs accepted inputs without Git. Patch21ca9c61e03dd8850d8cc8f66ebe72f31f1d9dc234eb280ea8422f5d379d4bae. Required legacy explicit-tree contract preserved.
-326 receipt-clock3: budget refusal, five clock positions, delayed/cancel and shell receipt cases; exact five-parent Git-denied race/atomic proof passed20.019s. Patchf028edc7a6edf3afa8f2a5b442d00c4650dfc8be4175df29ddc1280c4bb9c915.
-327 branch policy2: through/later exclusion and lagging/endpoint exclusion exercise actual selection/fold/patch owners with four typed fact readers. Git-denied race proof passed. Native ancestry/tree witnesses unchanged. Patch8a1fe8d3256688e89db12bfc03831b7169b53d45f93b0c27af66da279a44140a.
-328 branch/chain verdict policy: existing read seam reaches actual commit wrapper and strict verdict owner; parallel LandSeries table proves branch refusal/pass and chain behavior with Git denied, race/atomic. Native trailer/push witnesses retained. Patch600c740c3636990ebc965410992aa85b9a57b56345c58030867d4aa90664363c.
-329 CLI help: user-scoped launch record guidance and correct --id examples, family-specific errors, sole goal help before mutation with shared-option caveat. Focused tests pass, four new tests fail on base,40 rebuilt CLI checks pass. Independent read20260924t181403-936d3693b8 returned land/zero findings. Patch6338cd62e20d76631f400f0e1d581f621bb53005a05f0666425a845001ee95c6. Broad help review at E/coordination/help-review.md covered42families/433verbs statically and sampled runtime behavior, not433 handler executions.

Final shadow E/units/after-by305-backup/source compiled CMD default/tagged and batch at329. Both static modes identified the same unused pre-snapshot assignment in proof_run_test.go. Mechanical correction330 removes it and declares root at the existing consumed read; patcha85d626dc27ba6977f6b2d13fe26e6ab7929fa8d1e2970bc3e031e6ca256c767. The delegate is repeating only affected CMD compilation/static. All product code is frozen; only evidenced final failures may change it. Actual per-unit proof paths and native read IDs are in acceptance records. Do not rerun focused tests for report packaging.

## Durable recovery

Remote branch `backup/test-parallelism-20260924` last root-verified through324 at `30cbe7d894b96fd74ff78a3a9dcae01964c67e6b`, parent ea0acfbc56129efb085c49a1bb0535c29ece873f, tree0b94da84f3bbf33370f1ada1b9bee73a2e5f06a2. Source is the shadow above. E/units/after-by305-backup/root-verification324.json and C/remote-backup-20260924-latest.json retain replay, append-only receipt, compilation/static and remote evidence.325–330 need the next normal fast-forward backup push.

Private shadow receipt history differs lawfully from Main after the common prefix; never overwrite it with Main's ledger. Use the actual frontend E/rollout/final-diagnostic-20260924candidate274-retry1/candidate-frontend receipt add with explicit private --root and --file; the shell wrapper pins Main. Root alone commits/pushes.

## Final validation

E/rollout/final-candidate-20260924prepared/prepare.sh creates a private commit from Main's exact index, parent47dbe0f18; it transports to a clean standalone clone and builds a candidate-stamped external frontend. It does not move Main. Its bindings.env supplies candidate repo/commit/tree/frontend. No local configuration is copied.

Mac: E/rollout/final-delivery-20260924prepared/run.sh, human tmux,9workers. Uses test plan/run/verify against Main control/index, exact final tree, deep delivery, --all-groups --force-groups,120-minute cap. Requires prior168 selected IDs remain covered and current required IDs are selected. All-groups means continue independent work after a failure. No diagnostic-only selection or artificial batch tip. Observer E/rollout/final-selected-delivery-20260924bx/watch.py takes the output directory; also directly inspect independent runner argv because helper processes can inflate counts.

Linux: E/coordination/linux-final-private-candidate-prepared/run-final-normal.sh with same clean candidate, candidate-stamped POLICY_ENGINE and fresh RUN_ID; sources safe explicit release-test-environment.sh, enrolls human tmux and uses ordinary proof-run launch with private execution root/Main control root. Guest standalone clone under /home/wido.guest, native Git2.55, six workers, ordinary full validator with race/coverage floors. Actual stage table, custody and native exit required; no synthetic receipt. Scripts were read and syntax/preflight checked; final runs have not yet begun at this handoff write.

Preserve warm cache E/rollout/final-selected-delivery-20260924bx/go-cache and pinned staticcheck C/steward-deadcode-build/go-cache/80/80346ab3a04b363533ecdef504967486f3f921df1a319f4bd3a6fa738215df85-d/staticcheck. Do not build duplicate cold caches. Use direct authorized host execution if sandbox cache/sysctl restrictions interfere.

## Previous measurement and remaining delivery

Latest full diagnostic was candidate274, not this candidate: E/rollout/final-diagnostic-20260924candidate274-retry2/result.json. All168groups executed:162pass,5fail,1invalid; native67m14.521s, wrapper68m56s. Nine actual Go runners observed. Three causes have focused passing repairs286(source guard),300(actor helper allow-list and nested adoption),312(restored exact protected manifest test). Matching full proof remains necessary. A prior55-minute run completed less work and is not a valid speed comparison. Targeted conversions are substantially faster; no overall full-suite speedup is yet proven.

After shadow checks: update remote backup, freeze exact candidate, launch Mac and Linux, report actual start, watch concurrency and structured outcomes. Fix only evidenced failures, reuse valid evidence. Then fetch/reconcile, append final receipt, consume matching test verify, commit and normal push/integrate. Never claim all tests parallel, full speedup, or completion before the observations support it. Latest ETA22:30–01:30CEST remains low confidence.

Historical detail is preserved in C/handoff-before-final329-20260924.md and earlier snapshots; use canonical accepted evidence rather than stale active-work sections there. Host had87GiB free at20:19; VM previously66GiB. Claude distilled old artifacts/logs to .gz and DISTILLED.txt; check those before declaring evidence missing. No active source, run or warm cache may be cleaned.

## Reconciliation with the UI merge, 24 September 20:46 CEST

All330 accepted units were pushed and verified on GitHub backup/test-parallelism-20260924 at4ad556a3c046eaec87e8155bd57474d326863440. A final remote check found the UI branch had moved Main to d76c71e3b. Before any new full run, root merged that upstream into an isolated copy of local candidate43b4f27. There were19 conflict paths. Three Sol delegates preserved upstream multi-block/human-proof semantics, typed notice history and UI inputs alongside the existing Git-free fixtures, per-call transport seams, parallel worker declarations and source guards. Incoming general roster/UI language rules are retained; explicit current-session Sol delegation still applies. Four zero window caps remain.

Root caught and fixed a semantic interaction in the automatically merged Tick caller: public Deliver would journal a second time under the typed notice owner. Both default transport slots now use raw deliver; the existing composed Tick test observes one typed row. The full goal package passed-race once in49.286s (1227 pass events,2 skips,zero failures). Three newly incoming multi-block tests also use fake endpoints and passed their exact Git-denied race check. Refusal source guards pass after51 source anchors moved; all293 codes remain. The37 actor sites match the merged brain allow-list. Combined CMD default/tagged, goal/steward compilation and pinned staticcheck across33 changed Go packages all passed in24.025s. No full suite has run on this merged candidate yet.

E/units/final-ui-reconcile contains the original conflict stages, resolution-frozen.patch, focused evidence and frozen-merge.json. Reviewed source freeze tree390c434984c0b4d6618a9bbb2546cef6d9430d5d; resolution patch SHA2934e48a4cffe80157cc1671c089fa051ad424d3584d6e37755fe5e38b0bfa49. The first independent reader20260924t184452-f76fc86ab2 refused an untrusted archive directory before any model calls; an empty private .git fixed that tooling precondition. Independent native Astra read20260924t184651-34a440fb40 is running against a separate frozen copy. Its material verdict is still required; no new redesign is permitted. The only excluded focus-patch body is the obsolete6322-row inventory deletion, with owner evidence retained.

Root applied the merged tree to Main's index and advanced only its base reference47dbe0f18→d76c71e3b. No Main product commit or push occurred. The live narrator file was untouched. main-rebaseline-result.json records the operation. New documentation/receipt packaging follows that source freeze and does not change tested code. The earlier final-candidate-20260924prepared source43b4f27 and engine are superseded: do not launch them. Prepare a fresh final-candidate-20260924reconciled from the current Main index and new parentd76c71e3b. Existing Mac/Linux scripts consume its new bindings; Mac output final-delivery-20260924prepared has never started.

Next: finish the bounded independent read, freeze/build the new candidate, push its private backup reference, launch Mac9 and Linux6 concurrently from human tmux, observe actual concurrency and structured outcomes, then consume matching proof and integrate Main. E/coordination/git-free-tests-20260923/final-integration-live.json supplements this page.
