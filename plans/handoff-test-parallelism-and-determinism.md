# Test parallelism and determinism — active handoff

Updated 21 September 2026, 16:48 CEST. This supersedes the older chronological handoff. The durable operational continuation is `/Users/wido/LocalStorage/agentic-tools-evidence/test-parallelism-20260921/coordination/live-status-latest.md` (E below). Do not infer completion from a delegate report or a previous green run.

The goal remains `tests-parallel-and-deterministic`, claim epoch3, main lineage `main-1789893000-16595-1a7a6a`. The first goal, goal-landing efficiency, is already delivered. Root coordinates on Astra; actual implementation delegates use Sol xhigh in private clones through the metasystem launcher. The user authorizes commits, pushes to main, installs, tmux enrollment and necessary fixes. No further user decision is currently needed. Current working completion estimate is 19:00–21:00 CEST, provisional until release and platform validation finish.

## Release boundary

MAIN is `/Users/wido/LocalStorage/GitHub/agentic-tools-m1e`, HEAD/origin `f3186921e851cd49c923415cd60ab2b96f5fbceb`. Integration I is `E/worktrees/integration`, with component base `5f5854c9fb8efcdad404267a0a7218fe49bcfb05`. The currently installed engine remains `df89a6b7`, enrollment170/supervision182. Enrolled tmux pane `%12` has shell PID77919; preserve other panes.

Required order: C1 compatibility selected proof, publish/install; C2 activation selected proof through installed C1, publish/install; then complete standing validator through installed C2 on macOS and natively on Linux. C1 keeps workers0 and legacy wire while installing dual readers and dormant backends. C2 activates authenticated worker policy, identity, coverage and frozen candidate checks atomically. A candidate never supplies its own standing validator. Normal C2/macOS execution unsets GOMAXPROCS; C1 focused proof uses2. Linux can carry the explicitly labelled restricted-CPU witness.

The last C1 selected run, `E/rollout/c1-delivery-static-fixed`, ended unsuccessfully at16:42:23: 100 groups passed,64 reused,2 failed;82.43minutes. Attempt `proof-mub9wial-7c5f0c49130333f3`, tree `af7faca11110ac1201645dc7d82c1dfaa8c7e305`. All166 groups were collected; no missing groups. The two failures are:

- `goal-decision-standard`: default candidate admission helper omitted the private host-admission namespace. Accepted correction preserves custom callbacks and asserts private namespace use in the existing budget-rebind test. Five focused normal/race cases passed30.405s and the complete fast gate passed. It is merged I and MAIN; metadata `coordination/c1-admission-isolation-main-applied.json`.
- `engine-binding-standard`: one stall fixture stayed in blocking `os.ReadFile(started)` open, so simulated time never advanced. Root deliberately sent SIGQUIT to the exact owned test PID39062 to capture stacks and settle this already-red attempt; this is recorded diagnostic termination, not an organic test failure. Exact stacks are `audit/c1-hang-go-stack.txt`. Sol correction `20260921t142854-d7e65ef6d4` is complete. Root strengthened the callback to identify the specific stalled command; the checkout-HEAD case first completes an unrelated config command. The deliberately broken callback was rejected, and corrected normal/race replay passed25.881s. Both files are merged MAIN and I; evidence `rollout/rearm-stall-settlement-root/result.json`.

MAIN may be edited now that this attempt is terminal. Collect and prove both fixes before staging the next tree. `rollout/c1-delivery-isolation-fixed/run.sh` is prepared but has not run and has no tree stamp. C1 publication scripts STILL name the failed static-fixed run: update to the eventual sufficient run before executing. Publication uses exact-tree test verification, a shipped receipt, commit.sh --push, HEAD/origin equality, go-build.sh, and arm. Preserve matching passing group evidence; never relabel the two red groups.

## Integration and review

Policy, allocator, scheduler, Go partitioning, freeze, native lifecycle, process isolation, timing, fixture events and supervision changes are composed in I. Each original unit's maximum three actual independent reads remains binding; proof/build retries did not consume or create read rounds. Native, timing, fixture-event and supervision chains have closed. No fourth unit read is authorized.

The final combined independent Astra read round1 (`20260921t135717-7fd0c7420e`) examined the complete40104-line diff at snapshot56f8d16e… and found four material issues. Two command-fixture schema/namespace issues are corrected and root normal/race proof passed49.829s. The performance-Go nested allowance issue is corrected at each leaf and rerun owner, root normal/race passed29.130s, and four files are merged I. Two readiness waits additionally observe actual early group completion; root normal/race passed2.904s/10.521s. The operational-result retention issue is under the bounded follow-up described below. Round2 must review the complete recomputed diff, concentrating on corrections and final source/data composition; maximum3 combined rounds.

Two actual Sol corrections remain active; two completed providers are listed for composition:

- rearm-stall-settlement, `20260921t142854-d7e65ef6d4`: complete, root proof and MAIN/I composition done.
- dispatch-semantic-clock, `20260921t143216-574ec3eb0f`: seven dispatch mission/lease/fence stamps still used real time; same authorized semantic clock must govern seeds and consumers without cap increases.
- retained-error-finalization, `20260921t143358-3fa9d1b645`: preserve all drained groups and explicit not-run later groups on operational failure. Root found the all-native-green/progress-write-error boundary must still make delivery insufficient, and new tests must not increase serial-test ratchets. Seed51c885a… contains the previous accepted implementation; final merge must include its full delta against original9c5144… . Prior root normal/race passed111.562s but did not cover these additional boundaries, and that candidate is not yet merged I.
- fixture-receipt-clock, `20260921t143358-5de9daeff5`: Go shell/public/private clock checks passed, but real full-width-chain failed caller classification because the source engine received fake process identity under genuine source configuration. Correct the actual caller/engine/environment boundary; do not weaken fixture authority. The accepted command child namespace provider is already composed into this private source. Preserve both recorded root failures: `rollout/fixture-receipt-clock-root` and `rollout/fixture-receipt-clock-root-fixed`. The real fixture engine corrected caller classification, but the next run failed goal-item-not-held at commit; the former global fixture lineage became receipt-array-only. Trace claim/commit ownership and correct every same-owner caller before the next focused run.

Every delegate correction needs root decisive replay, exact hash-preserving composition, final registration and review. Temporary merge tools in E/coordination create previews and reject conflicts/hash drift; do not copy whole shared files across producers.

## Checked inventory and final tests

Three disjoint inventory ranges are composed in I:6112 rows,17 honest pending. The final reader rejects pending classifications and UNOWNED; all four negative controls passed. Proposal SHA `d83f3db7b7f38d979a703e4d64c032e9cef2e4fa9305617f2d8a34c16b4893ec`, reconciliation `audit/inventory-final-merge-facts.md`. Nine pending command rows are obsolete after accepted fixes and require refresh; seven dispatch rows and one land row await the active providers. Do one final source refresh after all corrections, review every changed/new fingerprint and owner, require zero unresolved rows, then pass the ordinary full inventory scanner. A refreshed unknown operation is never auto-approved.

129 new test names were registered across11 policy groups, metadata `coordination/final-test-registration-applied.json`. New tests from the latest corrections need a final Go-AST registration delta. Refusal registry sites were mechanically reconciled and its full package passed; source shifts from final test.go changes require another coordinate check. Preserve all coverage, test, parallel and static ratchets; do not raise floors, waive failures or delete baseline tests.

Final acceptance scripts and matrix: `audit/final-acceptance-commands.md`, `rollout/c2-delivery`, `rollout/c2-publication`, `rollout/macos-full-prepared`, `rollout/linux-final-prepared`. They have not run. Lima metasystem-debian is ready (Linux arm64,4CPUs/8GiB,Go1.27.1,ample disk). After published C2, transport its exact commit to the dedicated bare repo, prepare the native build, bind its real digest, and execute the prepared host-governed/guest-native proof. Do not copy host live ledgers, PID custody or actual local configuration into Linux. Independent correctness work may overlap only with correct control-root ownership; shared CPU results cannot support matched timing claims.

Budget: elapsed16h,12 attempts,720 reserved job-minutes,8 jobs,3 review rounds; ends about21:36CEST. Only elapsed was extended under user authority. Do not silently change other members. Inspect current admission rather than assuming unused capacity. The old stop-status f5c is a historical13:22 observation; supervision was repaired and the active seat action is continue this goal. Unrelated parked human decisions do not block this work. Retro debt is known and remains for the quiet integration point.

The UI clone is `/Users/wido/LocalStorage/GitHub/agentic-tools-ui` on ui-development and has user-owned edits: never overwrite them. CLI help and reviewed UI design are included in pending C1. All four context caps remain0/unlimited. Never read, echo, diff, archive or copy actual metasystem.conf.local. Existing inherited evidence is durable under the two dated evidence roots.

Completion requires published/installed C2, sufficient exact selected evidence, complete installed-engine macOS/Linux validators, zero unresolved inventory, closed review findings, the generic command/JUnit public proof, meaningful parallel/restricted-CPU evidence, updated design obligations and honest cost measurements. Historical49–56-minute runs are context, not a matched speedup baseline. Do not conclude this goal early or return a final completion answer while any required work remains.

C1 combined MAIN source proof passed normal/race and the full refusal package in71.781s (`rollout/c1-collected-final-root/result.json`). The complete fast gate is running with output under the evidence root; it does not overwrite the installed engine. Next selected output is c1-delivery-isolation-fixed; source will freeze again once its tree is stamped.
